# 第一创新点实验总控（当前执行版）

<!-- PRECOMPACT_SNAPSHOT_START -->
## PreCompact 自动快照

> 此区块由项目级 Hook 自动维护；服务器事实需实时核验。

- **触发时间**：`2026-08-05T11:51:59+08:00`
- **当前活动计划**：`.Codex/docs/2026-08-04-R2最终物理旁路正式实验计划.md`

### 当前执行态摘要

**任务 1、2 已完成，任务 3、4 正在收口；任务 5 尚未启动。** TCP/UDP 正式数据角色已就绪；QUIC 仅允许种子验证，正式权重为 0。当前不需要 GPU；进入任务 5 前会明确通知用户所需显卡和预计时长。
<!-- PRECOMPACT_SNAPSHOT_END -->

- **状态日期**：2026-08-05
- **研究对象**：基于物理信息神经网络的生成式恶意流量检测方法
- **主基座**：`Qwen3-1.7B`
- **文档职责**：只保存第一创新点的实时裁决、冻结合同、关键制品、活动任务、阻塞和下一门禁
- **长期边界**：[开题改进交接文档](开题改进交接文档.md)
- **精简前全文**：[2026-08-04 备份](../.Codex/docs/archive/2026-08-04-第一创新点实验总控-精简前备份.md)

## 1. 当前裁决

1. 第三章固定保留 PINN，R2“共享守恒与协议自适应多动力学”是当前唯一活动的章级候选；公式、算法名和正式章名尚未冻结。
2. R2 计算图固定包含共享守恒、TCP、UDP、条件启用的 QUIC 专家和 `UNKNOWN` 回退。ICMP、低置信协议、其他协议及未就绪 QUIC 只走共享守恒与回退，不为标题完整性新增无真值专家。
3. 静态 18 维旁路已经因种子 42 的 `T-P<T-A` 被否决；该结论只否决静态直接注入，不否决当前因果多窗口最终 R2。
4. ns-3 与 qlog 只提供训练期特权物理监督。推理不得使用队列真值、ACK 真值、RTT 真值、丢包/PTO 真值、在途字节、拥塞窗口、端点 qlog、TLS 密钥或最终测试统计量。
5. 现有 59 条 qlog 只允许字段、掩码、路由、残差和种子机制验证，`formal=false`、训练权重为 0；不能启用正式 QUIC 专家，不能进入硬门槛、置信区间或 QUIC 效果主张。
6. 候选实验的操作门禁仍为 `NO-GO`，统计状态为 `NOT_READY`。检测开发六视图已经闭合，TCP/UDP v2 物理池已发布为 `review_pending`；独立复审、v2 模型角色绑定、其余运行时收据和总体验收尚未闭合，不能启动 15 个候选运行。
7. `Qwen3-1.7B` 只在候选统计得到允许升级的 `GO` 后进入 59,656 条正式实验；不得把候选级 DistilBERT 或历史基线结果写成最终 Qwen 结论。

## 2. 最新已核验数据与制品入口

### 2.1 冻结底座

- 三源统一预算服务器根：`runs/data-frozen/dataset-v1/`。统一主索引为 1,410,346 条，正式预算为 59,656 条，旧候选预算为 10,000 条。
- `master_records.parquet` SHA-256：`16458a4a191c51eefc58717e16609790b9914936a2470ef29099bdfbc70af703`。
- `budget_checksums.json` SHA-256：`38883c7ef8b1e47ebd218514e71f95474cebaa8637df6b5203326802546a6c2e`。
- TQH-C2 双物化得到 35,235 条流和 2,475,729 个包；载荷 Merkle：`51deee4da715e234763c63e0aa061d20afb29ec37e7ffcdd487e936ef8b329cf`。
- 现有 `dataset-v1` 和 `dataset-v1-shared-b0` 只读，R2 新制品不得原地覆盖。

### 2.2 R2 主候选根

- 本地根：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-candidate-v1/`。
- 载荷 Merkle：`ebaf0b7ab7563bc7665a5efb4bb0c792274767ad97ff27729267e2520cb7b921`。
- 检测 `train-fit` 为 7,579 条：GeNIS 3,973 条、TQH-C2 3,606 条；这是从既有 10,000 条共同候选中稳定筛出的检测监督部分，**不得再次切分**。
- 本地接口现有 30,316 个检测历史窗口与 7,579 条路由记录；ns-3 开发物理部分为 46,080 个 0.1 秒窗口，可形成 11,520 条四窗序列。
- 角色清单：`thesis/experiments/llm_probe/configs/r2_final_sidecar_role_manifest_v1.json`，SHA-256 `69cc3be2cc3668f0b90937496f02a37a5d6b6aa57bb346969f385e4e5e0f9d28`。
- 开发拆分合同：`thesis/experiments/llm_probe/configs/r2_final_detection_development_split_v1.yaml`，SHA-256 `389de958126b25b91997f870d2cb4b21df090f826159c4e9ed45a897d2c3c968`。
- GeNIS/TQH-C2 的 calibration 与 dev-validation 检测、共同历史和路由六份派生视图已经在服务器完成物化并回收本机；服务器与本机的文件 SHA-256、模式哈希、逐行语义哈希和稳定顺序均为 6/6 一致。
- 校准集为 7,200 条，开发验证集为 7,199 条；跨分区样本和独立组重叠均为 0，全部记录声明最终测试不可见。六视图载荷 Merkle 为 `7ee5fe8fae8ccd6ec5a83f829dbd38f5a1b587b7611c3c32a436540c4097146d`。

### 2.3 ns-3 TCP/UDP 物理池

- 正式 attempt5 根：`thesis/experiments/llm_probe/runs/ns3-data/r2-protocol-paired-v0/`；启动制品根：`thesis/experiments/llm_probe/runs/launchers/r2-ns3-protocol-matrix-v0-attempt5/`。
- attempt5 完成 512/512 条运行和 256/256 个严格 TCP/UDP 配对，失败、跳过均为 0；总计 61,440 个 0.1 秒窗口，可确定性组成 15,360 条四窗序列。
- 包守恒与网络层字节守恒违反数均为 0，首个突发配对的 TCP/UDP 窗口、NewReno 身份、拥塞窗口和守恒证据闭合；独立复审已通过。
- `matrix-summary.json` SHA-256：`574118e3a3ddddc21f23fdcb9c770ab7f9b3f86dd73feb018bd067d181234819`。
- `matrix-state.json` SHA-256：`db80f0ca42840a8b5b9ec53294844ee552f5cee739d6df532716a5701155c50f`。
- `source-lock.json` SHA-256：`95708a0fffd702f65ec18acfe9ca1c806ba893b5d4f816643bdc59534ec8d38b`。
- `ns3-trace-contract.json` SHA-256：`b90e4b657b0b5e8cece9af577e05f24d84da58cc7af33a596f21aadcd5b481d9`；trace 合同载荷摘要：`89390f51b077118b6c1246ece6b68496f4e720528eb69a23ebb7a280210907fe`。
- 物理硬裁决以独立仿真运行为统计簇；TCP 与 UDP 各至少 30 个独立簇。训练拟合残差不得替代未见物理面板。
- 上述 attempt5 是旧版物理池，不能替代新增发送者级 TCP 真值的 v2 正式池。旧 attempt3 的 501 条成功结果没有与修复结果拼接。
- 事件配对修复后的 v2 attempt4 身份为 `r2-protocol-dynamics-v2-formal-attempt4-event-pairing-v1`：同一新源码锁从零运行 512/512，失败 0，复用 0，严格 TCP/UDP 配对 256/256；场景源码锁为 `8da08bd2f50ed6a4c5178761d814112f50ee18ab395e871ad27f52dc15d8b980`。
- 本机正式池：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-tcp-udp-physics-v2/`，状态 `review_pending`。包含 46,080 个开发窗口、11,520 条四窗口序列、184,320 条发送者级 TCP 真值和 23,040 条 UDP 真值；TCP/UDP 开发配对 192/192。
- 正式池制品清单 SHA-256 为 `9ed2703a40e73c9f5dd37f72c9aad5c18175bb1cca907bd3d9a149362dd17694`，非自引用载荷 Merkle 为 `cd55f6fb82c03f8b68f00b9e3224e6ff814b9ac7fe7ac7652cf29dd5a43a5a3e`。512 份收据和 1,536 个运行文件哈希已核验；公开检测最终测试未读取。

### 2.4 QUIC qlog 种子池

- 种子根：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-quic-seed-v1/`；载荷 Merkle：`9391c52a18ed60b3d6ad136031993ab1c80ee31aa022d6ea9bbf51e41cee7eba`。
- 59 条连接共有 439,527 个 100 毫秒窗口；其 qlog 审计包含 1,421,638 条有效 ACK、3,777 条有效端点丢包以及 268,936 条完整 RTT、在途字节和拥塞窗口状态，无法关联关系与核心异常均为 0。
- 显式 PTO 事件为 0，必须保留缺失掩码；不得推断、填零或用 ACK 延迟替代 PTO。
- 连接分区根：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-quic-seed-partition-v1/`；41/9/9 条连接分别用于 train-fit/calibration/dev-validation，可形成 109,860 条四窗序列；载荷 Merkle：`24f888bb48c71d86cf24485ad639ca3500b855400276fe152633429612bf677f`。
- 分区只用于机制检查，不能突破 59 条证据边界。正式合同要求新增 600 条通过来源与真值审计的连接，并按实现与配置组冻结后，才能建立正式 QUIC 训练与统计主张。
- 当前扩充采用确定性用户态 UDP 障碍代理，不声称具备内核 `netem` 或网络命名空间语义。单条端到端合同已经通过：客户端、抓包和代理退出码均为 0，响应为 4,194,304 字节，PCAP、qlog、代理事件与配置哈希齐全。
- 40 条字段矩阵正在运行；本次写回核验时成功 9、失败 0、未完成 31。该矩阵尚未完成，不能写成 QUIC 种子扩充已经闭合，更不能启用正式 QUIC 专家。

## 3. 数据、拆分与信息预算合同

1. 检测监督池只含 GeNIS 与 TQH-C2；ns-3 与无恶意标签的 qlog 只进物理辅助池，两池通过共同可观测历史和适用掩码连接，不拼成可调权总分。
2. 7,579 条 `train-fit` 已冻结，不再拆 calibration 或 validation。GeNIS 10,399 条和 TQH-C2 4,000 条既有验证来源，必须在各来源内部按独立 `group_id`、不看标签、确定性拆为 calibration 与 dev-validation。
3. 同一会话、连接、捕获、仿真运行及其重叠窗口不得跨开发训练、校准和验证边界；窗口不是独立统计重复。
4. 最终测试在候选级完全不可见：不读取样本、标签、指标、元数据、路径内容或任何派生统计量。开发验证只用于一次冻结裁决，不调参、不换检查点、不改阈值。
5. 分类器可见共同文本、数值和历史在五组间逐位一致。协议只进入停止梯度的物理适用掩码与强度门，不得拼入分类文本或分类数值视图。
6. 路由只允许被动可观测协议证据。标签、`profile`、采集配置、cell、场景、地址、端口、连接标识、qlog 路径和模型预测均禁止。
7. 若任何协议字段或协议派生描述进入分类输入，信息预算即改变，HGB、XGBoost、DistilBERT、普通监督 Qwen、仅状态监督和标准 PINN 均须在相同视图上重跑。
8. 五组共享相同的 `sample_id`、`evaluation_cluster_id`、顺序、标签、基础字段、训练预算、优化器、早停、检查点和评价脚本；组独有缺失必须修复后重跑完整受影响矩阵。

## 4. F 五组冻结合同

| 组别 | 冻结输入或结构 | 实验责任 |
| --- | --- | --- |
| `F-A` | 关闭协议专家，以等参数量统一支路替代 | 标准物理基线，排除参数容量解释 |
| `F-H` | 与 `F-P` 同时间范围、同可观测性的原始因果历史 | 排除历史信息本身的贡献 |
| `F-P` | 共享守恒、TCP/UDP/条件 QUIC、适用掩码、停止梯度路由和 `UNKNOWN` 回退 | R2 主方法 |
| `F-S` | 分割后在同层内整段共同置换物理历史与掩码 | 排除容量、结构和正则化贡献 |
| `F-R` | 只从开发训练池抽取形状和合法字段块匹配的随机物理历史 | 排除随机旁路贡献 |

- 冻结种子为 42、43、44，共 15 个完整运行；不补有利种子、不删失败种子。
- `F-S` 与 `F-R` 在训练前一次性物化并记录哈希；不得按轮次重生成，也不得使用标签、预测、校准区、验证区或最终测试供体。
- QUIC 未正式就绪时，有效路由必须是 `UNKNOWN`；不能回退 UDP。正式入口必须把 `quic_expert_ready` 与训练清单、实现/配置组、所需真值、检查点和门禁收据绑定。
- 每组必须由独立 Python 进程启动。静态探针已证明 SwanLab 0.9.0 同进程第二次初始化可能复用失效客户端并触发 401，禁止在同一进程串行初始化多组。

## 5. 统计门槛

### 5.1 统计程序

- 主裁决不使用三个种子做正态检验、配对 t 检验、方差分析或 Wilcoxon 检验。
- 固定两层配对自助法：10,000 次重采样，分析种子 `20260804`；第一层重采样训练种子，第二层在每个种子内按 `evaluation_cluster_id` 配对重采样。
- 五组共享同一批种子位置和簇索引；报告每个种子的原始指标与效应、三种子等权均值、样本标准差、双侧 95% 区间和族校正单侧 95% 同时置信下界。
- 常规检测族、困难检测族和物理族分别做族内多重比较控制；不得先看结果再挑困难面板或用次指标挽救主门槛。

### 5.2 硬门槛

| 门槛 | 必须同时满足 |
| --- | --- |
| `G0` 完整性 | 15 个运行完成；代码、配置、数据、拆分、种子、样本、参数预算、路由和最终测试不可见收据齐全 |
| `G1` 常规非劣 | GeNIS 与 TQH-C2 上，`F-P` 对四个对照的三个种子、等权汇总及族校正同时下界均严格大于 `-0.021` |
| `G2` 困难泛化 | 至少一个预注册合格困难面板中，`F-P` 对四个对照的三个种子、等权汇总及同时下界均严格大于 0；TQH-C2 协议标签匹配面板对 `F-A` 也必须严格为正 |
| `G3` 物理收益 | 未见状态误差与无量纲物理残差分别相对 `F-A` 三个种子为正、等权改善至少 10%、同时下界为正，并稳定优于 `F-H/F-S/F-R` |

- TCP 与 UDP 分协议报告；只有两者的两项物理指标均满足方向和区间条件，才能声称“跨协议稳定”。
- 任一检测面板中，`F-P` 相对任一对照的三种子等权宏平均 F1 增益严格超过 0.10，状态转为 `AUDIT_REQUIRED`，先查重复、泄漏、协议标签捷径、划分和弱基线。
- 互斥最终状态为 `NOT_READY`、`AUDIT_REQUIRED`、`INVALID`、`NO_GO`、`GO_TO_FORMAL_R2_NARROW`、`GO_TO_FORMAL_R2_BROAD`；缺少正式 QUIC 时，任何 `GO` 必须附加 `_TCP_UDP_ONLY`。
- 禁止挑种子、降低阈值、看最终测试调参、按预测选样本、改变标签或来源比例、删除基线擅长样本，以及事后改变面板、主指标或置信区间方法。

## 6. SwanLab 与强制运行制品

1. 15 个运行各有唯一运行标识、组别、种子、状态、退出码、检查点、日志和资源记录；不得只保留最好种子或截图。
2. 每个运行在线记录主次指标、训练时间、峰值显存、吞吐、延迟和失败状态，并保存服务器本地 SwanLab 日志；云端状态、标量和本地制品必须一致。
3. 运行包绑定代码版本、正式配置、数据与拆分清单、五组视图、随机/置换制品、检查点和统计合同的内容哈希。
4. 保存逐样本预测、逐簇物理真值与预测、掩码、专家与回退使用记录、`sample_id`、`evaluation_cluster_id` 和困难面板成员关系。
5. 统计包保存每种子的原始指标、全部配对效应、置信区间、同时下界、有效重采样数、门槛布尔值、证据路径、失败原因及唯一最终状态。
6. SwanLab 初始化失败、制品缺失或哈希不一致时记为 `NOT_READY`；不得用离线手工均值替代缺失的正式运行记录。

## 7. 活动任务与阻塞

### 7.1 数据任务 1、2

- 已完成：本地主候选接口、检测开发六视图、同一新源码锁的 TCP/UDP v2 512/512 正式矩阵，以及本机 v2 正式物理池原子发布。物理池仍为 `review_pending`，不能跳过独立复审。
- 待完成：独立复审 v2 正式物理池并绑定模型角色清单；完成 QUIC 用户态字段矩阵并写回来源、真值与哈希证据。QUIC 当前仍只作种子机制验证，正式权重保持 0。

### 7.2 模型任务 3、4

中期独立审查曾列出 7 项重要门禁，正式训练前必须由最终运行时收据和总体验收确认全部闭合：

1. QUIC 监督掩码必须由输出专家身份唯一决定，不能被调用方专家名绕过。
2. 最终测试路径、角色和哈希必须在打开 Parquet 前拒绝，不能读取后再报错。
3. `F-S/F-R` 供体分层必须绑定实际 `effective_route_index`，不能只看原始 QUIC 名称。
4. 公共与物理序列必须绑定 `sample_id`、稳定顺序、划分、字段语义和数组哈希，拒绝裸数组错位。
5. 路由生成器和允许证据字段必须机器绑定；布尔值只接受真实布尔或精确 0/1，拒绝字符串转换。
6. `quic_expert_ready` 必须绑定检查点内训练清单、独立连接数、实现/配置组、真值字段和门禁版本；调用参数不得覆盖失败收据。
7. `F-A` 必须有可执行的等容量统一支路和硬参数匹配收据，不能只比较张量形状。

此外，最终入口还需证明物理专家先训练后冻结、分类反向无物理梯度、外层 `.train()` 不重新开启专家训练模式、路由信号不重复加门，以及单组单进程硬拒绝。

绑定清单发布后的晚期父目录交换缺陷已经完成最小修复与独立故障注入复审；严重问题 0、重要问题 0、次要问题 0，**该限定代码门禁已经放行**。该裁决不替代数据合同、检查点、其余运行时门禁或任务 5 总体验收。

### 7.3 当前阻塞结论

- 检测开发六视图的数据门禁已经放行，TCP/UDP v2 正式物理池已经发布但待独立复审；模型角色清单和拟合配置尚未绑定 v2 的路径、模式、发送者级 TCP 伴随表与精确哈希。
- 任务 5 不启动，GPU 不申请；任何提前运行都不能进入正式裁决。
- 当前没有正式 `F-*` 预测，不得宣布 R2 成功、失败或已具备 QUIC 证据。

## 8. 下一步唯一顺序

1. 由新的独立审查代理复核 v2 正式物理池的来源锁、掩码、TCP 发送者级主键、UDP 守恒、模式、顺序、语义哈希与原子发布；严重或重要问题才作废并最小修复。
2. 将已经放行的检测六视图和全新 v2 物理池绑定到模型任务 3、4，更新角色清单、拟合配置和加载器，生成参数预算、梯度隔离、最终测试读取前拒绝、有效路由、检查点和单组单进程收据。
3. 与上述工作并行完成 QUIC 用户态字段矩阵并写回证据；在 600 条正式连接合同完成前继续保持 `formal=false`、训练权重 0 和 `UNKNOWN` 回退。
4. 独立复核最终数据合同、入口和全部运行时门禁；严重或重要问题为 0 后，执行无 GPU 的 `G0` 完整性门禁。
5. `G0` 通过后运行 `F-A/F-H/F-P/F-S/F-R × 42/43/44`，每组独立 Python 进程；不读取最终测试。
6. 按冻结合同执行两层配对自助法、分协议和困难域分析，输出唯一状态；不补种子、不改阈值。
7. 只有得到允许升级的 `GO`，才进入 59,656 条、`Qwen3-1.7B`、种子 42/43/44 的正式 R2、消融、外部强基线、跨拓扑、跨基座和效率验证。

在第 6 步前不重启 R1/R3，不扩展过时基线流水账，不以历史失败路线替代当前 R2 门禁。

## 9. 关键链接

- [论文总体交接](开题改进交接文档.md)
- [R2 最终物理旁路正式实验计划](../.Codex/docs/2026-08-04-R2最终物理旁路正式实验计划.md)
- [R2 最终旁路统计裁决合同](../.Codex/docs/2026-08-04-R2最终旁路统计裁决合同.md)
- [R2 最终旁路数据准备报告](../.Codex/docs/2026-08-04-R2最终旁路数据准备报告.md)
- [R2 最终旁路模型实现报告](../.Codex/docs/2026-08-04-R2最终旁路模型实现报告.md)
- [R2 最终旁路中期独立审查](../.Codex/docs/2026-08-04-R2最终旁路中期独立审查.md)
- [R2 检测开发六视图最终验收](../.Codex/docs/2026-08-04-R2检测开发六视图最终验收.md)
- [R2 绑定清单晚期交换最终独立复审](../.Codex/docs/2026-08-04-R2绑定清单晚期交换最终独立复审.md)
- [R2 ns-3 TCP 物理真值 v2 实施计划](../.Codex/docs/2026-08-04-R2-ns3-TCP物理真值-v2实施计划.md)
- [R2 TCP/UDP v2 正式物理池物化报告](../.Codex/docs/2026-08-05-R2-TCP-UDP-v2正式物理池物化报告.md)
- [QUIC 用户态可复现链路障碍修订计划](../.Codex/docs/2026-08-04-QUIC用户态可复现链路障碍修订计划.md)
- [ns-3 attempt5 实施报告](../.Codex/docs/sdd/task-r2-data-rebuild/reports/report-task-05t-burst-integer-scheduler-attempt5.md)
- [ns-3 attempt5 独立复审](../.Codex/docs/sdd/task-r2-data-rebuild/reports/review-task-05t-burst-integer-scheduler.md)
- [QUIC qlog 修复报告](../.Codex/docs/sdd/task-r2-data-rebuild/reports/report-task-06q-quic-qlog-physics-audit-fix.md)
- [QUIC qlog 独立复审](../.Codex/docs/sdd/task-r2-data-rebuild/reports/review-task-06q-quic-ack-fix.md)
- [TQH-C2 双物化报告](../.Codex/docs/sdd/task-r2-data-rebuild/reports/report-task-03-tqhc2.md)
- [PINN 公式证据台账](../.Codex/docs/PINN公式证据台账.md)
- [2026-08-04 精简前全文](../.Codex/docs/archive/2026-08-04-第一创新点实验总控-精简前备份.md)
