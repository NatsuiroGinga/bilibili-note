---
title: wiki 笔记类型与模板规范
date: 2026-07-14
tags:
  - 类型/参考
  - SCHEMA
aliases:
  - SCHEMA
  - 笔记模板规范
  - wiki 宪法
---

# wiki 笔记类型与模板规范

> 这是 wiki 层的"宪法"。AI 创建新笔记时必须遵守对应的模板。

---

## 笔记类型一览

| 类型标签 | 含义 | 目录 | 示例 |
|:--|------|------|------|
| `类型/MOC` | 内容地图，聚合索引页 | wiki/*/ | [[wiki MOC\|INDEX.md]] |
| `类型/设计` | 技术方案设计文档 | wiki/*/docs/ 或 wiki/*/specs/ | [[北斗资源上下线及踢点策略技术文档]] |
| `类型/排查` | 故障排查记录 | wiki/*/cpu-top/ 等 | [[MCDN CPU 高负载机器总览]] |
| `类型/参考` | 结构化知识库（表结构、常量、配置） | wiki/*/rules/ | [[mysql]] |
| `类型/论文` | 论文阅读笔记（raw/papers 对应的结构化和理解） | wiki/papers/ | [[mHC]] |
| `类型/方法论` | 工作流、设计原则、最佳实践 | wiki/methodology/ | [[AI 知识库设计原则]] |
| `类型/交付` | API 文档、交付格式、指标定义 | wiki/*/docs/ | [[mcdn_vanished_bandwidth 交付格式]] |
| `类型/计划` | 实现计划、任务分解 | wiki/*/specs/ | [[dual-channel-config-version-check-plan]] |

---

## 通用必填字段

每篇笔记（无论类型）必须包含：

```yaml
title: "笔记标题"
date: YYYY-MM-DD
tags:
  - 主题标签（按域/技术栈/关键词）
  - 类型/MOC  # 或对应的类型标签
```

通用推荐字段：

```yaml
aliases:
  - 别名1
  - 别名2
related:
  - "[[关联笔记]]"
  - "[[另一篇关联笔记]]"
```

---

## 各类型模板

### 1. MOC 类（`类型/MOC`）

用途：聚合索引页，列出该域/主题下所有笔记的入口链接。

必填字段：
- title / date / tags（含 `MOC` + `类型/MOC`）

推荐字段：
- aliases（如 "go-op MOC"、"主页"）

正文结构：
```markdown
# 标题

简介（1-2 句说明这个 MOC 覆盖的主题）

## 子类别 1

- **[[笔记标题|别名]]** — 一句话说明
- **[[另一篇笔记]]** — 一句话说明

## 子类别 2
...
```

规则：
- 每个 `[[链接]]` 必须带别名 + 一句话说明——AI 不需要点进去就知道内容
- MOC 可以嵌套（index → 域 MOC → 主题 MOC）

### 2. 设计类（`类型/设计`）

用途：技术方案、架构改造、系统设计文档。

必填字段：
- title / date / tags（含 `类型/设计`）
- 主题标签（如 `#go-op`、`#丢包率`）

推荐字段：
- `status`: "已设计，待实现" | "已实现" | "已废弃"
- aliases / related

正文结构：
```markdown
# 标题

> 状态：xxx
> 最后更新：YYYY-MM-DD

## 背景
（为什么需要这个设计）

## 目标
（要达成什么）

## 技术方案
（怎么做的，含方案对比和选择理由）

## 数据流 / 架构图
（可选，用 ASCII art 或文字描述）
```

### 3. 排查类（`类型/排查`）

用途：故障排查、性能分析、根因定位。

必填字段：
- title / date / tags（含 `类型/排查`）
- 主题标签

推荐字段：
- `date`: 排查日期
- related（指向总览 MOC 或修复方案笔记）

正文结构：
```markdown
# 标题

> 分析对象：xxx
> 状态：在线暴冲 / 已下线 / 正常

## 现象
（发现了什么）

## 数据
（含关键指标表格）

## 根因分析
（因果链推导）

## 结论 / 操作建议
```

### 4. 参考类（`类型/参考`）

用途：结构化知识库——表结构、常量定义、配置参数、命令速查。

必填字段：
- title / date / tags（含 `类型/参考`）
- 主题标签

正文结构：无固定模板，以表格和列表为主，确保 AI 可快速检索。

### 5. 论文类（`类型/论文`）

用途：raw/papers/ 中 PDF 的结构化理解笔记。

必填字段：
```yaml
title: "论文标题"
authors: [第一作者, 第二作者]
year: 2025
date: YYYY-MM-DD  # 笔记创建日期
journal: "会议/期刊"
source_pdf: "[[raw/papers/xxx.pdf]]"
tags:
  - 领域标签
  - 类型/论文
```

推荐字段：
```yaml
key_finding: "一句话核心结论"
method: "用了什么方法"
baseline: "对比了哪些 baseline"
aliases:
  - 短引用名
  - FirstAuthor2025-Title
```

正文结构：
```markdown
# 论文标题

> 作者, 年份, 期刊/预印本 · 页数

## 一句话
（读完就能引用的核心摘要）

## 背景：问题的演进
（A → B → 问题 → 本文方案）

## 方法核心
（关键技术细节，公式/架构）

## 实验结果
（关键数据对比表）

## 我的理解
（用自己的话解释：为什么有效、类比、直觉）

## 与相关工作的关系
（文献引用链）

## 疑问 / 待验证
（未解决的问题）

## 原始摘要
> 论文摘要原文

## 文献信息
- DOI/arXiv 链接
```

**关键规则**：`source_pdf` 字段必须指向 `raw/papers/` 中的对应 PDF。这建立 raw ↔ wiki 的显式关联，允许 AI 在需要时退回源材料验证。

### 6. 方法论类（`类型/方法论`）

用途：工作流、设计原则、最佳实践。通常是外部文档的结构化理解。

必填字段同论文类（但来源不一定是 PDF）。

正文结构同论文类（保留「我的理解」「疑问/待验证」）。

---

## 规则速查

| 规则 | 说明 |
|------|------|
| 必填字段 | title / date / tags + 对应类型标签 |
| 论文的 source_pdf | **必填**，指向 raw/ 中的 PDF |
| 新笔记的入链 | 创建后必须被至少一个 MOC 或相关笔记引用 |
| tags 正交性 | 每篇笔记同时标注主题标签 + 类型标签 |
| 文件命名 | 中文优先，可混用日期前缀；避免文件名含 `&` 等特殊字符 |
| raw 层不可变 | 只增不改，所有修改在 wiki 中进行 |
| output 可覆盖 | 临时产物，不沉淀知识 |
