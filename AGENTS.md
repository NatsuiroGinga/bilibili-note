# AGENTS.md

<!-- 规则加载哨兵：NOTE_ROOT_20260724 -->

本文件只规定本毕业论文仓库长期有效且每次都必须遵守的边界。

## 规则发现与子域路由（强制）

- 编码代理从项目根走到当前目录加载规则，每层只加载 `AGENTS.override.md`、`AGENTS.md` 或回退文件中的第一份；近层规则优先，组合上限为 32,768 字节，运行中不会动态发现新规则。
- 从仓库根处理子域前主动完整读取局部规则：论文正文和证据读 `thesis/AGENTS.md`；`llm_probe` 再读 `thesis/experiments/llm_probe/AGENTS.md`，涉及 `scripts/` 时继续读其 `AGENTS.md`。
- 原件、知识库、恢复文档和过程记录分别读取 `raw/AGENTS.md`、`wiki/AGENTS.md`、`output/AGENTS.md`、`.Codex/docs/AGENTS.md`。
- 会话绑定 RWKV 或摘要含 `RESEARCH_ROUTE=RWKV` 时先读 `.Codex/docs/RWKV/AGENTS.md`，该路由取代 PINN/R2 默认入口。

## 仓库边界

- 研究对象固定为加密恶意流量检测的训练方法及训练系统，不得转为流量生成或传统分类器论文；正式题名按两份 `output/` 恢复文档裁决。非论文历史资料未经用户明确要求不得成为默认上下文或论文结论。
- 原件、结构化知识、高层交付和过程证据分别归入 `raw/`、`wiki/`、`output/`、`.Codex/docs/`。
- 外部证据按 `raw/` 原件到 `wiki/` 全文核验与结构化笔记，再到 `thesis/` 或 `output/` 可公开论断晋级；未通过真实性、全文和论断证据门槛的内容只能标为推论或待验证假设。
- `ns3/` 只保存本机参考材料，正式构建与运行位置由当前路线合同规定。

## 上下文恢复协议（强制）

- 先按用户当前显式绑定、会话摘要哨兵、明确研究对象的顺序确定唯一路线；未显式指定且任务属于当前论文主线时，默认绑定 RWKV。禁止同时加载两套路线上下文后自行推断。
- RWKV 路线先完整读取 `.Codex/docs/RWKV/AGENTS.md` 与 `.Codex/docs/RWKV/RWKV路线总控.md`，再按任务章节读取唯一当前恢复卡：第三章任务读 `RWKV第三章恢复卡.md`，第四章任务读 `RWKV第四章恢复卡.md`，跨章任务显式读取两张卡。只有追溯第三章候选存废时才追加 `2026-08-08-第三章候选方案登记册.md`；只有事实冲突、制品追溯或用户明确要求时才经 `RWKV历史交接入口.md` 读取历史交接。`RWKV当前恢复卡.md` 仅作旧链接兼容页，不是默认入口。除跨路线比较或用户明确返回 PINN/R2 外，不读取 PINN/R2 恢复文档。
- PINN/R2 是历史路线；只有用户明确指定时，才按顺序完整读取 `output/开题改进交接文档.md` 和 `output/第一创新点实验总控.md`，并仅在接口核验或事实冲突时读取其链接的详细证据。
- 完成所属路线恢复前不得运行实验或重新分派实验任务；涉及服务器时先查询代理状态，再只读核对日志、GPU 进程、持久会话和磁盘，防止重复启动。
- 运行事实以原始日志和制品为准，验收以实现或审查报告为准，需求以实施计划为准，研究边界以所属路线总控为准；冲突时先用新证据修正所属路线总控。
- PINN/R2 的确定变化按 `output/AGENTS.md` 的职责分流写回；只有同时影响课题定位和实验状态时才同步两份恢复文档。RWKV 的公共合同与跨章裁决写回 `RWKV路线总控.md`，第三章或第四章的结果、路径和状态写回对应章节恢复卡；除非用户正式决定替换论文主线，不写入 PINN/R2 总控。RWKV 会话摘要保留字面哨兵 `RESEARCH_ROUTE=RWKV`。

## 凭据与远程访问

- 登录信息、密码、令牌和其他凭据只能来自环境变量、系统密钥存储或既有登录状态，禁止打印、回显或写入仓库、配置、日志、计划、命令清单和汇报。
- 远程动作前从所属路线总控读取当前服务器与凭据键的规范来源，不在长期规则中写死服务器或变量；切换、重启或回收后先只读核验身份、根目录、磁盘、依赖和持久化制品。
- 不得沿用旧进程、运行目录、模型、数据或工具链假设，也不得把预期关机或服务器回收计作科学失败。

## 研究证据纪律（强制）

- 不得盲从用户提出的比例、模型、指标门槛、技术路线或实现方式。先把建议视为待验证假设，核对数据特性、全文文献、既有实验和资源约束，再明确给出采纳、修正或否决理由；禁止为了即时迎合而在没有新证据时反复改变方案。用户在获知证据、风险和替代方案后作出的最终决定仍应执行，但不得据此突破学术诚信、数据泄漏和结果真实性边界。
- **魔法数字门禁（强制）**：凡数据划分比例、样本量、窗口或序列长度、候选网格、阈值、误报预算、置信水平、随机种子、训练轮数、批量大小、早停条件和资源阈值等数值，在写入方案、代码或配置前必须建立可追溯依据。每个数值至少登记：用途和值、全文文献页码或公式（没有则明确写“无直接文献依据”）、数学或统计推导、真实数据诊断或源侧实验制品路径、适用边界、是否接触最终目标标签和核验状态。科研数值必须同时通过机制依据与本数据可行性核验；文献未给出精确值时只能称任务化设定，并通过不读最终目标标签的数据诊断或源侧实验确定。工程资源数值以真实容量、峰值测量和日志收据为依据，不强求论文来源。任一必填依据缺失时标为“未验证”，不得进入快速实验、正式实验或论文结论；启动器应优先机械断言已冻结的依据台账及其哈希。禁止依据最终测试结果事后补写理由或调整数值。
- 禁止空想式裁决。方案提出、主任务选择、理论主张、候选排序、支持、证伪和冻结必须有可追溯的全文文献或实验数据支撑；纯概念分析、模块名称、审查意见和语言上的合理性只能形成待验证假设。
- 文献用于证明机制来源、研究空白和实验设计依据，不能替代本课题实验；代理实验用于发现信号和缺陷，不能冒充正式模型结论。没有证据时必须明确标为“未验证”，并给出最小验证实验。
- **文献调研检查点（强制）**：多篇论文、预计超过 30 分钟或可能发生上下文压缩的调研，开始扩展检索前必须在任务目录建立 `task_plan.md` 与 `notes.md`。每完成一篇全文或一组查询，立即落盘查询式、来源 URL、原件路径、证据等级、关键页码或公式位置、纳入/排除理由、与当前候选的关系、待办和 Zotero 状态；不得等任务结束后凭对话记忆补写。
- 文献代理至少每 30 分钟更新一次可恢复检查点，明确已完成项、下一篇或下一查询、未关闭疑点、无法获取原文清单和精确阻塞。发生上下文压缩、代理重启或跨会话交接后，必须先重读检查点与已落盘原件/笔记再继续，禁止只依赖压缩摘要重复检索或重做裁决。
- 原始论文、结构化全文笔记、索引和 Zotero 条目是文献事实源；聊天消息只用于进度通知。检查点记录不得阻塞已经满足数据与运行门禁的真实实验，宽泛调研应拆成可独立恢复的候选或主题子任务。
- 每个实验裁决必须关联配置、数据版本、基线、指标、原始日志和制品路径。不得只报告挑选后的最好数字，也不得依据最终测试选择数据、字段、候选或超参数。
- 两个效果量级参照来自**不同来源，不得混为一谈**（2026-08-17 核对原件后厘清，此前断句歧义已导致一次误引）。其一，朱焱雷第三章表 3-7（印刷 p.42）报告的是 PGD-20 鲁棒准确率的**绝对百分点提升**：BUPT-CNN 由 `55.60%` 提至 `64.90%`（`+9.30`），ET-BERT 由 `63.50%` 提至 `75.80%`（`+12.30`），且 `12.30` 超过单模块增益之和 `9.00`；其全文级主张是较前沿基线鲁棒性提升 `4.2%~8.6%`。其二，“错误相对下降 `16.1%`”是**本课题早期先导实验自定的门槛**，朱焱雷论文全文搜索 `16.1` 零命中，**不得写成“参照朱焱雷第三章 16.1%”**。
- 上述两者都只作工作量与效果量级参照，不是机械硬门槛。引用朱的数字时须注明是绝对百分点而非错误相对下降。最终方案综合主指标绝对值与增量、错误相对下降、多种子一致性、置信区间、困难分面、效率、显存和机制消融后裁决，并以实际实验结果为准。

- 根 `.ignore` 只排除历史归档、依赖、工具缓存和可再生临时物；不得排除规则、恢复文档、论文与实验源码、原件、数据、模型、运行制品、SwanLab 或普通日志。
- `llm_probe/runs/` 受 Git 忽略；核验结果必须使用总控中的明确路径或 `rg --no-ignore`，不得因默认搜索无结果判断制品不存在。
- 历史备份只按明确路径只读核验；不得为缩短上下文删除历史或把历史内容复制回活动恢复文档。

## 实验工程开发范式（强制）

- 凡是能够用确定性条件、哈希、模式、行数、状态码或资源阈值表达的校验，优先固化为幂等的单入口门禁脚本；脚本使用清晰的 `if-else` 或等价结构，校验通过后直接进入下一阶段，失败时才停止并输出诊断证据。禁止把同一组可机械化条件拆成多轮人工命令、重复思考或重复汇报。该规则只减少执行摩擦，不得省略最终测试隔离、数据泄漏防护、科学评价或无法机械判定的研究裁决。
- 只有提出新研究机制、模型结构、公式、数据合同或实质改变冻结行为时才使用 `brainstorming`；常规启动、重复运行、参数扫描、结果查询、只读诊断、同步及根因已明确的修复不得借此重开冻结合同。
- 非平凡实现或缺陷修复固定使用 `writing-plans` → `subagent-driven-development` → `daily-coding`。主代理先把文件边界、接口、验收命令、制品路径和阻塞条件写入持久化实施计划，再由 `subagent-driven-development` 按任务派发独立实现代理；每个实现代理必须读取并执行 `daily-coding` 的先读后改、最小变更、类型安全、安全检查、运行验证和临时制品清理清单。
- 同一任务范围同一时间只允许一个实现代理修改，主代理负责计划、台账、任务交接和最终验收，不得绕过已分派实现代理直接写生产代码。实现代理不得覆盖、回滚或暂存其他代理与用户的既有改动。
- 实验工程代码不创建或运行人工夹具、单元测试和集成测试。生产代码完成后只执行编译、导入或命令入口检查，随后尽快运行冻结开发区的真实数据物化与快速实验，以真实日志、指标和制品暴露问题；编译检查不作为科学验证。出现无效结果或异常时由原实现代理依据真实证据修复，不为形式创建独立审查代理。报告写入 `.Codex/docs/`，记录修改文件、编译或入口检查、真实实验结果和遗留风险。
- **禁止运行 `black`**：不得在本仓库调用 `black` 进行格式化或检查，也不得因格式化要求延迟真实实验。Python 修改只执行与任务直接相关的语法、导入和静态代码检查；除非用户当轮明确授权，不运行任何自动格式化器。
- 文字复审不能替代真实数据、共同预算基线、消融和统计实验；只有泄漏、划分、标签、指标、因果时序、预算、公平性、不可运行或数学条件错误等会使结果无效的问题才暂停受影响实验。
- 新增模型、候选方法、消融变体、配置、运行目录、SwanLab 运行或子任务时，主展示名称必须直接表达其机制、角色或实验目的，使读者无需查表即可区分；纯字母数字代号不得作为唯一名称。确需稳定短键时，将其作为附加内部标识，并在配置、日志、汇总表和进度汇报中始终同时给出有意义的展示名称。
- 新增或修改数据视图、物化器、加载器或模型适配器前，必须先冻结生产端与所有已知消费端共享的最小接口：模式版本、主键、切分、标签连接、字段清单、历史表示、制品发现清单和兼容策略。禁止只实现生产端再让每个模型补一次性适配器；同一研究数据由统一清单和统一加载器服务传统模型、神经基线与候选方法。兼容性在首次真实实验前检查，不能等全量物化完成后才发现消费端仍绑定旧合同。

## 技能编排（强制）

本仓库使用 Claude Scholar 技能集（本机 `~/.claude/skills/`，上游 `https://github.com/Galaxy-Dawn/claude-scholar`）。技能只提供工作流与规范，**不裁决研究路线、数据合同、候选存废或实验结论**；与本文件、子域 `AGENTS.md` 或路线总控冲突时，一律以仓库规则为准。

### 按阶段路由

| 阶段 | 触发情形 | 技能顺序 |
| --- | --- | --- |
| 机制构思 | 新研究机制、模型结构、公式、数据合同，或实质改变冻结行为 | `research-ideation` → `brainstorming`（仅此情形，常规启动与重复运行不得借此重开冻结合同） |
| 文献 | 多篇论文、预计超 30 分钟或可能上下文压缩的调研 | `planning-with-files` 先建 `task_plan.md` 与 `notes.md` → `literature-reviewer` 代理 → `citation-verification` |
| 文献检索入口 | 文献检索、相关工作、直接近邻、引用核验、研究空白、相似论文或主题论文请求 | `literature-hybrid-search`：先查本地混合索引，再按证据缺口决定是否扩展在线候选 |
| 实现 | 非平凡实现或缺陷修复 | `writing-plans` → `subagent-driven-development` → `daily-coding` |
| **第三方 API** | **首次使用某库、或对签名/属性/参数名有任何不确定** | **`npx ctx7@latest library <名称> "<查什么>"` → `npx ctx7@latest docs <libraryId> "<查什么>"`**（强制，先查后写）；等价入口 `find-docs` 技能或 `plugin:context7` MCP。记录库名、目标机实际安装版本、查询来源与核验到的签名 |
| 调试 | 缺陷、异常行为、运行失败 | `systematic-debugging` 与 `bug-detective` 并用，一次只改一个变量 |
| 实验分析 | 实验产出制品后、写入任何结论前 | `results-analysis` → `results-report` |
| 图表 | 论文图件与结果表 | `publication-chart-skill`（选型与规范）→ 产出落 `thesis/figures/第N章/` 既有合同 |
| 正文 | 起草或改写章节 | `paper-miner`（仅提取结构与论证模式）→ `ml-paper-writing` 的论断门与证据优先 → `citation-verification` → `writing-anti-ai` |
| 自审 | **每章定稿后（强制门禁，见下）** | `paper-self-review` |
| 沟通 | 全程 | `expression-skill` |

### 章节定稿自审门禁（强制）

**任何一章写完后、进入下一章或交付前，必须执行 `paper-self-review`，不得跳过。**
自审未出报告的章节记为「未定稿」，不得据其内容更新路线总控，也不得作为后续章节的既定前提。

自审必须覆盖以下六项，逐项给出通过或不通过，不得只给总评：

1. **结构完整性**——小节齐备、层级与本章写作规范一致、编号连续无缺无重。
2. **逻辑一致性**——研究问题与方法匹配，实验设计支撑假设，结论落在实验范围内。
3. **论断审计**——这是本仓库最看重的一项。逐条列出本章的主张，
   按 `保留／弱化／改写／删除` 裁定，并写明所用证据、缺失证据与过度断言风险。
   输出用技能规定的 `Claim Audit` 结构。
   **凡无法指向具体制品路径或已核验文献的主张，一律弱化或删除。**
4. **引用完整性**——上标序号与参考文献条目一一对应，无占位残留。
   本项不替代 `citation-verification`，只检查对应关系。
5. **图表质量**——每图每表有题注、被正文引用、编号与目录登记一致，
   规格符合根 `AGENTS.md` 的学位论文图件合同。
6. **写作质量**——段落长度、肯定性断言处数、禁止项（反引号／加粗／脚本名／局限词）
   按本章写作规范逐项扫描。

**与上游技能的三处差异，以本仓库为准**：

- 技能的「Discussion addresses limitations」一项**不适用**：中文学位论文不写局限性小节，
  边界用中性措辞写在本章小结。该项改为检查「结论是否越出实验范围」。
- 技能面向英文投稿的「journal/conference requirements」改为「学校排版模板与图件合同」。
- 自审**不替代** `results-analysis`（统计）与 `citation-verification`（题录核验），
  它只检查「本章主张是否正确使用了已有证据」。

自审报告写入 `.Codex/docs/`，记录：章节、日期、六项裁定、论断审计表、
以及必须修改的阻断项清单。阻断项全部关闭后该章才算定稿。

### 三处与上游默认不同的裁决

1. **英文投稿技能停用**：本仓库是中文学位论文，`nature-writing`、`nature-polishing`、`nature-response`、`nature-data`、`latex-conference-template-organizer` 与会议 LaTeX 模板一律不用；`/mine-writing-patterns` 入口禁用，`paper-miner` 只用于读结构。仅在明确的英文投稿或真实审稿场景下才可启用。
2. **分析门禁只卡结论，不卡实验**：`results-analysis` 的 blocker-first 门禁（锁定分析单元、主指标、种子/折数、来源、比较族）**作用于"把结果写入正文或路线总控"这一步**，不得用于阻塞已满足数据与运行门禁的实验启动。格式化、重复静态检查、归档与非阻断性审查同样不得延迟实验。
3. **图表按下述学位论文图件合同产出**：`publication-chart-skill` 负责图型选择、信息密度与配套表；渲染规格按本合同执行。历史图件的 `180 mm` 宽与 Hiragino Sans GB 字体已判定为缺陷（A4 版心不足 180 mm、该字体非中文学位论文常规），不再沿用。

### 本地混合文献检索路由（强制）

- 遇到“文献检索、相关工作、直接近邻、引用核验、研究空白、相似论文、主题论文”等请求，先运行 `uv run --project scripts/literature_search --locked python -m scripts.literature_search status --json`，再用 `uv run --project scripts/literature_search --locked python -m scripts.literature_search query "查询" --scope local --mode hybrid --offline --json` 检索 `wiki/papers/**/*.md`。
- 索引不存在或 `stale=true` 时，按 `scripts/literature_search/README.md` 构建；模型依赖不可用时可先用 `--mode lexical`，但必须明确向量通道未运行。
- 只有本地证据不足、用户明确要求近期或外部工作、或需要验证 DOI/题录时，才改用 `--scope all`；`all` 必须同时保留本地结果和在线来源状态，任一在线来源失败不得丢弃本地结果。
- OpenAlex、Semantic Scholar、Crossref、Hugging Face Papers 和 Google Scholar 的返回一律视为外部题录或摘要候选。Google Scholar 请求必须调用 `$google-scholar`，程序化检索固定使用 JSON；`clusterId`、引用数和排序只作发现元数据。候选没有本地全文原件与结构化全文笔记前，不得升级为全文证据、写入论文结论或冒充已读全文。
- `scope=all` 的 GitHub 结果只进入 `code_candidates`，HF 模型、数据集和 Space 只进入 `hub_candidates`，均不得进入论文 RRF。HF Papers 可作为论文候选来源，但未有本地全文前仍是未核候选。
- 需要 Google Scholar 或学术搜索时调用 `$google-scholar`；需要源码时优先调用已安装的官方 `github:github`；需要 HF 模型、数据集、Space 或论文关联时调用已安装的 `hf-cli`、`huggingface-papers`、`huggingface-datasets`。skills.sh 上第三方 GitHub 技能采用量较低且来源弱于官方插件，不重复安装。所有调用只读；正式纳入仍按 `raw/` 原件、`wiki/` 全文笔记和 Zotero 题录的流程晋级。
- 该入口只检索和报告，不自动写入 `raw/`、`wiki/` 或 Zotero；需要正式纳入时继续执行 `raw/AGENTS.md` 与 `wiki/AGENTS.md` 的全文入库工作流。

**学位论文图件合同（2026-08-13 重定，规格取自实测朱焱雷学位论文原件）**

实测依据：`pdfinfo` 得页面 A4 `595.3×841.9 pts`；`pdfimages -list` 得图件为 RGB JPEG、`300–477 ppi`、印刷宽 `77–165 mm`；`pdffonts` 得内嵌 `DengXian`、`SimHei`、`SimSun`、`STZhongsong`、`TimesNewRomanPSMT`、`CambriaMath`、`EuclidMathOne`。

| 项 | 规定 | 实测依据 |
| --- | --- | --- |
| 页面 | A4 | `595.3×841.9 pts` |
| 最大宽度 | `165 mm`；常用 `110–150 mm`；小图 `75–80 mm` | 实测最宽 `2048 px @ 316 ppi = 164.6 mm`，多数落 `130–150 mm` |
| 高度 | 单图 ≤ `140 mm`，保证图与图题同页 | 实测最高 `1647 px @ 300 ppi = 139 mm` |
| 分辨率 | ≥ `300 ppi`，推荐 `400 ppi` | 实测 `300 / 316 / 323 / 348 / 374 / 399 / 408 / 477 ppi` |
| 中文字体 | 图内标注用黑体 `SimHei`，正文体例字为等线/宋体 | 实测内嵌 `SimHei`、`DengXian`、`SimSun`、`STZhongsong` |
| 西文与数学 | `Times New Roman`；数学符号 `Cambria Math` | 实测内嵌 `TimesNewRomanPSMT`、`CambriaMath`、`EuclidMathOne` |
| 图内字号 | 最小 `8 pt`，主标注 `9 pt`（小五） | 缩放到版心后须可读 |
| 输出格式 | `PNG` ≥ 300 ppi（提交与 Word 插入）+ `PDF` 矢量（备用与放大核查）；`SVG` 作可编辑源 | 朱的图为位图嵌入；本仓库额外保留矢量以便核查 |
| 配色 | 允许 RGB 彩色；**同时要求灰度可读**（线型、标记、填充图案区分，颜色只作辅助） | 朱用 RGB 彩色；灰度可读为本仓库额外要求，覆盖黑白打印 |
| 线宽 | 主线 ≥ `1.0 pt`，辅助线 ≥ `0.5 pt` | 缩放与黑白打印后不丢失 |
| 落点与命名 | `thesis/figures/第N章/`，命名 `图N-M-中文说明` | 见 `thesis/AGENTS.md`，未变 |
| 溯源 | 每章重写 `图件清单.json`，记录生成脚本、环境、尺寸、字体、数据来源键路径 | 数值可回溯，图不可手工改 |

历史 `180 mm` 超出实测最大印宽 `165 mm`，Hiragino Sans GB 不在朱论文内嵌字体之列，二者均不再沿用。正文不引用 `runs/`、`output/` 或临时目录中的图片。若学校下发了正式排版模板，以模板的版心与字号为准并更新本合同。

### 执行纪律

- **文档修改前必须有 Git 快照（强制）**：每次更新任一文档前，先执行
  `git status --short -- <文档路径>`。若目标文档存在未提交变更，必须先只暂存并提交这些目标文档，
  再开始新一轮修改；若目标文档工作树干净，则当前 `HEAD` 已是修改前快照，不创建空提交。
  同一项任务涉及多份文档时，可按一个原子范围共同提交；禁止夹带无关文件、凭据、运行缓存或其他代理的非目标改动。

- **第三方 API 一律先查文档再落笔（强制）**：语法检查、导入检查与「以前见过这个接口」都不能证明运行时属性存在。
  凡属性访问（`run.public`）、关键字参数（`experiment_name=` vs `name=`）、或首次使用某库，写代码前必须查文档。
  入口按 `~/.claude/rules/context7.md`：先 `npx ctx7@latest library <名称> "<查什么>"` 取 `/org/project` ID，
  再 `npx ctx7@latest docs <ID> "<查什么>"`；单问题不超过 3 条命令，查询中不得含凭据。
  配额报错时告知用户并建议 `npx ctx7@latest login` 或设 `CONTEXT7_API_KEY`，**不得静默退回训练数据**。
  **注意该用户级规则文件不一定被加载进会话**（本仓库 2026-08-13 实测：`~/.claude/rules/` 下 9 个文件只加载了 6 个，
  `context7.md` 不在其中），故在此复述关键条款，以仓库规则为准。
  本仓库已发生两起同型事故：`torch.flatnonzero` 不存在导致 R1 M1 失败；
  2026-08-13 `swanlab` 埋点凭记忆写 `run.public.cloud.project_name`，实测 0.9.0 的 `Run` 无 `public` 属性（公开属性仅 `id/name/path/url/dir/config/log*`），推上服务器后第一个 run 即崩。
  **同一事故中还产生了第二个错误：主代理宣称 `experiment_name=` 无效，实测该参数经向后兼容映射有效**——
  即「凭记忆断言接口不存在」与「凭记忆断言接口存在」同样不可靠，两个方向都必须查文档。
- 技能与仓库规则冲突时**必须显式说明冲突点与采纳理由**，不得静默择一。
- `results-analysis` 未产出分析制品前，结论只能标为「实验待证」，不得写入正文或路线总控的「已完成事实」。
- 摘要级证据与全文核验证据不得并列陈述；仅有摘要的来源不能支撑正文论断。
- 技能调用不改变本文件既有的证据等级、最终测试隔离、凭据保护与子代理分级要求。

## 子代理模型与推理强度（强制）

- 子代理名称必须以可读任务名开头，并追加模型族与推理强度后缀，例如 `lspr24_materialization_monitor_terra_low`。创建时的进度通知和最终任务报告必须同时明确记录完整模型名与 `effort`；不得让用户依靠任务名称猜测，也不得假定 `list_agents` 会显示这两项信息。
- 对已创建且无法重命名的子代理，主代理必须在进度汇报中给出“代理名 → 模型 → `effort`”映射；后续新代理统一执行上述命名规则。
- 新建子代理时必须根据任务难度显式指定模型和推理强度，避免所有任务默认继承最昂贵配置。文件盘点、进度查询、简单下载使用 `gpt-5.6-terra` 与 `low`；规格明确的单模块实现和常规数据处理使用 `gpt-5.6-terra` 与 `medium`；多文件实现和复杂实验诊断使用 `gpt-5.6-sol` 与 `high`；理论方案设计和复杂文献综合使用 `gpt-5.6-sol` 与 `max`；只有影响论文主路线且普通强度无法解决的极难裁决才使用 `gpt-5.6-sol` 与 `ultra`。
- 显式指定模型或推理强度时，`fork_turns` 必须使用 `none` 或有限正整数，禁止携带完整会话历史。优先把需求、接口和证据写入任务简报文件，子代理只读取简报及必要的局部规则，以减少上下文和成本。
- `followup_task` 不能更换既有子代理的模型或推理强度。继续同一任务且原模型合适时复用既有代理；需要改变模型档位时新建子代理，并通过任务简报交接，不能为了省一次分派让低配代理处理超出能力的任务，也不能让高配代理长期执行机械任务。
- 上述分配表只是初始值，后续必须根据同类任务的真实反馈动态升降级。每次子代理报告至少观察需求理解是否准确、完成轮次、返工次数、编译或真实运行是否成功、交付物是否完整、耗时和上下文消耗；主代理在下一次同类任务简报中记录模型档位调整及原因。
- 同类任务连续两次一次完成、无关键遗漏且耗时合理时，下一次可先降低一级推理强度；低强度代理出现一次明确的推理不足、跨文件遗漏或无效返工时先提高一级推理强度；同类任务连续两次仍因能力不足失败时，从 `terra` 升到 `sol`。如果失败来自简报缺信息、外部资源不可用、服务器离线或需求冲突，应修正上下文或阻塞项，不得错误地通过提高模型档位掩盖问题。
- `NEEDS_CONTEXT` 优先判为任务简报问题，补充精确接口和证据后复用原档位；实现完成但存在实质性疑虑、反复误解科研语义或无法解释实验异常时再升级。达到 `sol/high` 后仍连续失败，不自动堆到 `ultra`，先拆小任务或重新判断技术路线。
- 科研实验没有提升、候选被证伪或数据不支持假设属于有效研究结果，不算子代理失败，也不触发模型升级。只有执行过程的理解、实现、复现和证据交付质量参与代理档位反馈。

## 调试纪律（强制）

- 缺陷、测试失败或异常行为同时使用 `systematic-debugging` 与 `bug-detective`：先定位根因，再提出一个可证伪假设，一次只改变一个变量并运行最小验证。
- 修复必须增加覆盖原问题的回归测试；连续三次修复失败后停止补丁并与用户讨论架构，不得尝试第四次。
