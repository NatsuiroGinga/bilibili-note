# AGENTS.md

<!-- 规则加载哨兵：NOTE_ROOT_20260724 -->

本文件只规定本毕业论文仓库全域、长期有效的边界。专用规则放在最接近适用文件的子目录，不在根级复制。

## 语言、工具与交付

- 所有回答、文档、注释和说明用简体中文；代码语法、标识符、命令、路径及无官方译名的技术名称可保留英文。
- 代码与文本搜索只用 `rg`，遍历和按名查找只用 `fd`；默认排除 `.git/`、`vendor/`、`deps/`、`cache/`、`archive/`、构建产物和依赖目录，不搜索 `.env`、私钥、令牌文件或敏感配置。
- 手工创建或修改文件只用 `apply_patch`；禁止用 `sed`、`sd`、Python、重定向或 `cat` 写文件，格式化器和项目要求的结构化生成工具除外。
- `functions.exec` 的 FREEFORM JavaScript 源码禁用反引号模板字符串：不得承载 shell、补丁、用户文本或文件内容。静态值用 JSON 兼容双引号；多行用双引号行数组 `.join("\n")`；动态值用参数对象/拼接；文档只按路径读取。`apply_patch` 输入同样只用 JSON 转义字符串或安全行数组。
- 过程文档统一放在 `.Codex/docs/`；原件、结构化知识、高层交付、论文正文分别放入 `raw/`、`wiki/`、`output/`、`thesis/`。
- 文件任务须报告输入、输出、已修改文件、未修改范围和验证；未验证状态不得报告为完成。

## Codex 规则发现与子域路由

- Codex 每次会话从项目根走到当前目录；每层依次检查 `AGENTS.override.md`、`AGENTS.md` 和配置的回退名，只纳入第一份非空文件。近层后加载并覆盖冲突的上层规则；运行中不动态重建指令链。
- 未显式配置时按官方默认的组合上限 `32,768` 字节管理活动路径；新增规则或修改 `project_doc_max_bytes`、回退名时，重新计算根到最深适用目录的组合字节并记录配置来源。
- 从根处理子域前完整读取局部规则：论文读 `thesis/AGENTS.md`，正文再读 `chapters/AGENTS.md`，图件再读 `figures/AGENTS.md`；实验读 `thesis/experiments/llm_probe/AGENTS.md`，脚本再读其 `scripts/AGENTS.md`；原件、知识、高层交付和过程记录分别读 `raw/`、`wiki/`、`output/`、`.Codex/docs/` 下的 `AGENTS.md`。
- 新增 `AGENTS.override.md` 前须证明普通分层无法表达需求，并核对同层 `AGENTS.md` 将被完全忽略的后果。

## 仓库与研究路线边界

- 研究对象固定为加密恶意流量检测的训练方法及训练系统，不得转为流量生成或传统分类器论文。非论文历史资料未经用户明确要求不得成为默认上下文或论文结论。
- 先按用户当轮显式绑定、会话摘要字面哨兵 `RESEARCH_ROUTE=RWKV`、明确研究对象的顺序确定唯一路线；未显式指定且任务属于当前论文主线时默认绑定 RWKV。禁止并行加载多套路线上下文后自行拼接裁决。
- RWKV 恢复顺序固定为 `.Codex/docs/RWKV/AGENTS.md`、`RWKV路线总控.md`，再按任务读取第三章或第四章唯一恢复卡；跨章任务才读两张卡。候选追溯和历史交接只按 RWKV 局部规则追加，该局部规则只增补路线，不替代当前目录自动加载的工程规则。
- PINN/R2 是历史路线，只有用户明确指定时才读取 `output/开题改进交接文档.md` 和 `output/第一创新点实验总控.md`；不得把历史路线状态写入 RWKV 总控或章节恢复卡，反之亦然。
- 完成所属路线恢复前不得运行实验或重新分派实验任务。涉及服务器时先查询代理状态，再只读核对日志、进程、持久会话、磁盘和制品，防止重复启动。
- 事实优先级：原始日志和制品证明运行事实；实现或审查报告证明验收状态；实施计划证明需求；路线总控与章节恢复卡证明当前研究边界。冲突时以较新的原始证据修正所属路线事实文档，不以聊天摘要覆盖。

## 研究证据与实验有效性

- 用户提出的比例、模型、指标门槛、技术路线和实现方式先作待验证假设；核对数据、全文文献、既有实验和资源约束后给出采纳、修正或否决理由。用户知情后的最终决定仍不得突破学术诚信、数据泄漏和结果真实性边界。
- 方案、主任务、理论主张、候选排序、支持、证伪和冻结须有可追溯全文文献或有效实验数据。先确认合法可得数据和共同预算下未饱和的强基线；概念、模块名、文字审查和摘要只能形成“未验证”假设。
- 科研数值写入方案、代码或配置前须登记用途和值、全文页码或公式（没有则写“无直接文献依据”）、数学或统计推导、真实数据诊断或源侧实验路径、适用边界、是否接触最终目标标签和核验状态，并同时通过机制依据与本数据可行性核验；工程资源数值以容量、峰值和日志收据为依据。缺项标“未验证”，不得进入实验，也不得事后按最终测试补理由或改阈值。
- 每个实验裁决关联配置、数据版本、基线、指标程序、原始日志和制品路径；验证按真实泛化轴及主体隔离，时间不是必需轴。不得只报告最好数字、接触最终测试后选择设计，或削弱基线。
- 模型、表示和两项机制按数据结构、合法字段和泛化轴选择；机制来源不限，但直接移植不构成创新。严格 `2×2` 的 C00、两单机制和 C11 不得有第三模块；入文须两单机制各较 C00 在主分布外指标正增益、C11 优于两者，协同另有预冻结交互或活动证据，且不得靠降低训练域性能获得。
- 证据按多保真逐级止损：真实数据诊断、共同预算强基线、低保真先导、`2×2` 与运行诊断、冻结正式运行。各级看结果前冻结样本、泛化轴、指标、最小效应或无效区、停止条件和最大成本，仅过门者进入下级；无有效信号即否决、缩小或重构。训练只用单一冻结种子，禁止多种子训练；配对重采样只量化样本不确定性，不冒充训练随机性。
- 只有数据泄漏、划分或样本关联错误、标签或指标错误、违反因果时序、比较预算不公平、实现不可运行、资源安全失败或数学条件不成立等会使结果无效的问题才暂停受影响实验；其他不确定性改写为实验假设、运行断言或适用边界。
- 方案复审须区分“设计可行、实验待证、实验支持、实验否决”，没有有效实验制品只能标“实验待证”；同一问题无新增代码、数据、实验结果或合同变化时不重复发起文字复审。
- 多篇论文、预计超 30 分钟或可能压缩上下文的调研，在任务目录建立 `task_plan.md` 与 `notes.md`；每完成一篇全文或一组查询即记录查询式、URL、原件、证据等级、页码、纳入理由、待办和 Zotero 状态。原件、结构化全文笔记和题录是事实源，聊天只作进度通知。

## 项目检索与外部证据

- 文献、相关工作、路线、实验、正文或跨文档研究判断，先运行 `uv run --project scripts/literature_search --locked python -m scripts.literature_search status --json`，再按唯一作用域执行离线混合查询：论文 `paper`、项目 `project`、实验 `experiment`、正文 `thesis`，明确跨域才用 `all`；该 uv 项目仅是本地文献检索工具环境，不属于实验环境；精确代码和行号之后用 `rg` 补充。
- 索引不存在或 `stale=true` 时按 `scripts/literature_search/README.md` 重建并复查；模型依赖确实不可用才降级 `lexical` 并披露向量通道未运行。结论记录作用域、模式、索引清单或构建时间、`stale=false` 和关键命中路径。
- 在线题录、摘要、Google Scholar、OpenAlex、Semantic Scholar、Crossref、Hugging Face 和代码仓库只作候选发现；没有 `raw/` 原件与 `wiki/` 全文笔记的来源不得升级为全文证据，正式纳入时执行 `raw/AGENTS.md` 与 `wiki/AGENTS.md`。
- 首次用第三方库或接口不确定时，核验目标版本并查官方文档或 Context7，记录库、版本、来源和签名。语法/导入通过不能证明运行时接口存在，事故细节留专用手册。

## 协作代理独立复核

- 协作代理的计划、代码、审查、状态和结论一律视为待核候选，不因措辞完整、自报 `READY`、静态检查通过或已提交而自动接受；主代理结合当前合同和原始证据独立判断，可采纳、修正或否决。
- 接受实现前至少核对目标文件差异、关键配置、运行身份、输入与模型哈希、第三方接口约束和会使结果无效的门；接受实验结论前至少核对原始状态、日志、manifest、指标制品与主体和泛化轴隔离。代理自审不替代主代理验收，聊天汇报不替代制品。
- 代理提出的新科研主张、阈值、超参数、文献映射或存废建议仍须过全文或本项目实验的证据门；与用户最新裁决、恢复卡、冻结合同或原始制品冲突时以优先级更高且更新的事实为准并把差异写入看板，不得为维持代理成果修改基线、指标或解释。复核取最小相关范围，不借“防止盲从”重复全部工作或拖延已满足门禁的实验；发现实质缺陷回到原实现者修复或重新分派，保留失败证据和旧提交。

## 实施、技能与验证

- 非平凡实现/修复按 `planning-with-files`、`writing-plans`、`subagent-driven-development`、`daily-coding` 执行；在 `.Codex/docs/` 留边界、文件、验收、制品和阻塞计划。主代理管所有权/交接/验收，每范围只一实现代理。
- 生产代码子代理简报须列 `required_skills` 和唯一收据；改码前登记技能绝对路径、SHA256、读取时刻，交付逐项证据。主代理只运行一次验证器；缺失、漂移或越权退原代理，不重审且不阻塞实验。
- 新研究机制、模型结构、公式或数据合同用 `research-ideation` 后再 `brainstorming`；常规启动、重复运行、参数扫描、结果查询、只读诊断和根因明确的修复不得重开冻结合同。
- 缺陷或异常同时用 `systematic-debugging` 与 `bug-detective`，先定位根因，提出一个可证伪假设，一次只改一个变量。回归验证形式服从近层实验规则；连续三次基于新证据的修复仍失败时停止补丁并讨论架构。
- 实验产出写入结论前执行 `results-analysis` 和 `results-report`；论文、图表、知识库和 Git 任务读取相应近层规则与技能。技能只提供工作流，不裁决研究路线、数据合同、候选存废或实验结论。
- 能用哈希、模式、行数、状态码或资源阈值表达的校验优先固化为幂等单入口门禁，通过后继续，失败才停止并输出诊断；机械化不能省略最终测试隔离、泄漏防护、科学评价或人工研究裁决。
- 修改后运行最小且有意义的验证，格式化、静态检查、测试和真实运行服从近层规则；不得以无关全量检查、归档或非阻断审查延迟已满足门禁的真实实验。

## 本机资源保护

- 本机筛选限模型 `≤100` 万参数、最多 `6` 轮，可实体分层抽样，仅用于排序/止损；正式结论须全规模运行。
- 实验重任务同刻最多一个；API 批量或多进程转换最多两个、总并发最多四个；启动前 `pgrep`，`TaskStop` 后确认子进程退出。
- 数据必须 `mmap`，禁全量载入和多进程训练；随机访问受磁盘 IO 限制，数小时任务须中途落盘。
- 子代理简报须声明不得在本机做实验计算，并指定服务器测量入口。

## 凭据、远程与破坏性操作

- 登录信息、密码、令牌和其他凭据只能来自环境变量、系统密钥存储或既有登录状态；禁止打印、回显或写入仓库、配置、日志、计划、命令清单、URL 和汇报。
- 远程动作前从所属路线总控读取当前服务器与凭据键来源，不在长期规则写死地址或变量；服务器切换、重启或回收后先只读核验身份、根目录、磁盘、依赖和持久化制品。不得沿用旧进程、运行目录、模型、数据或工具链假设，也不得把预期关机或回收计作科学失败。
- 删除、覆盖、强制推送、历史改写、`git reset --hard`、`git checkout --` 等破坏性操作前，列出准确对象并取得用户明确授权。原始数据、冻结数据、模型、检查点和日志不得删除唯一副本。

## 文档快照与 Git 所有权

- 改文档前运行 `git status --short -- <目标路径>`；已改目标只暂存、提交作快照，干净则沿用 `HEAD`，不建空提交。
- 共享暂存区非空/含他人文件时不建快照、不清空或夹带提交，保留 `HEAD`/阻塞记录。只操作本任务文件；目标被他人改动或暂存即停止协调。
- 根 `.ignore` 不得排除规则、恢复文档、论文、源码、原件、数据、模型、运行制品、SwanLab 或日志。核验受忽略的 `llm_probe/runs/` 用总控路径或 `rg --no-ignore`，不得默认无结果即判制品不存在。

## 子代理模型与推理强度

- 首轮文献、综述、实验计划和方案由 `gpt-5.6-sol` 负责：核对全文、原件与代码接口，落盘证据、实验臂、停止条件、范围和验收。科研主线外的盘点、查询、整理、下载和清单维护优先低强度 `gpt-5.6-terra` 或 `gpt-5.6-luna`；工程和诊断由 `gpt-5.6-terra` 中高强度或 `gpt-5.6-luna` 高强度依 Sol 计划执行，不改变研究合同。
- `gpt-6-astra` 仅在同时具备以下条件后派发：路径可查的 Sol 制品；直接相关的真实数据或实验制品（数据版本或哈希、运行或诊断身份、日志或状态、指标或观测量）；明确裁决问题和下一动作。零训练的真实数据病灶探针属于证据；合成夹具、计划、公式、未运行代码、聊天、自报 `READY` 和仅静态检查不属于证据。
- Astra 只处理证据后的高影响方向、矛盾、晋级或淘汰裁决及精读复核；不得用于首轮宽泛检索、空白构思、常规文档、无证据方向判断，也不得为过门运行无关实验。
- `effort` 由主代理按复杂度、耦合、证据、失败代价、耗时和既有表现选择并记录。显式指定时 `fork_turns` 只能为 `none` 或有限正整数；`followup_task` 不改变配置，调整时新建代理交接；先修正需求、上下文或外部资源问题，不以升档掩盖，实验否决不计能力失败。
