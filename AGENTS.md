# AGENTS.md

<!-- 规则加载哨兵：NOTE_ROOT_20260724 -->

本文件只规定本毕业论文仓库全域、长期有效的边界。专用规则放在最接近适用文件的子目录，不在根级复制。

## 语言、工具与交付

- 所有回答、文档、注释和说明使用简体中文；代码语法、标识符、命令、路径及无官方译名的技术名称可保留英文。
- 代码与文本搜索只使用 `rg`，文件遍历和按文件名查找只使用 `fd`；默认排除 `.git/`、`vendor/`、`deps/`、`cache/`、`archive/`、构建产物和依赖目录，不搜索 `.env`、私钥、令牌文件或明确敏感配置。搜索 Go 代码须加 `--type go`。
- 手工创建或修改文件只使用 `apply_patch`；禁止用 `sed`、`sd`、Python、重定向或 `cat` 写文件。格式化器和项目明确要求的结构化生成工具除外。
- 过程文档统一放在 `.Codex/docs/`；原件、结构化知识、高层交付、论文正文分别放入 `raw/`、`wiki/`、`output/`、`thesis/`，并读取对应局部规则。
- 文件任务须报告输入、输出、已修改文件、未修改范围和验证；未验证状态不得报告为完成。

## Codex 规则发现与子域路由

- Codex 每次会话从项目根走到当前目录；每层依次检查 `AGENTS.override.md`、`AGENTS.md` 和配置的回退名，只纳入第一份非空文件。近层规则后加载并覆盖冲突的上层规则，空文件跳过；运行中不会动态重建指令链。
- 未显式配置时，按官方默认且更保守的组合上限 `32,768` 字节管理活动路径；新增规则或修改 `project_doc_max_bytes`、回退名时，重新计算根到最深适用目录的组合字节并记录配置来源。
- 从根处理子域前主动完整读取局部规则：论文读 `thesis/AGENTS.md`；正文再读 `thesis/chapters/AGENTS.md`；实验读 `thesis/experiments/llm_probe/AGENTS.md`，脚本再读其 `scripts/AGENTS.md`；原件、知识、高层交付和过程记录分别读 `raw/AGENTS.md`、`wiki/AGENTS.md`、`output/AGENTS.md`、`.Codex/docs/AGENTS.md`。
- 会话绑定 RWKV 或摘要含字面哨兵 `RESEARCH_ROUTE=RWKV` 时，主动读取 `.Codex/docs/RWKV/AGENTS.md`；该文件只增补 RWKV 路线，不替代当前目录自动加载的局部工程规则。
- 新增 `AGENTS.override.md` 前必须证明普通分层无法表达需求，并核对同层 `AGENTS.md` 将被完全忽略的后果。

## 仓库与研究路线边界

- 研究对象固定为加密恶意流量检测的训练方法及训练系统，不得转为流量生成或传统分类器论文。非论文历史资料未经用户明确要求不得成为默认上下文或论文结论。
- 先按用户当轮显式绑定、会话摘要哨兵、明确研究对象的顺序确定唯一路线；未显式指定且任务属于当前论文主线时，默认绑定 RWKV。禁止并行加载多套路线上下文后自行拼接裁决。
- RWKV 恢复顺序固定为 `.Codex/docs/RWKV/AGENTS.md`、`RWKV路线总控.md`，再按任务读取第三章或第四章唯一恢复卡；跨章任务才读两张卡。候选追溯和历史交接只按 RWKV 局部规则追加。
- PINN/R2 是历史路线。只有用户明确指定时，才读取 `output/开题改进交接文档.md` 和 `output/第一创新点实验总控.md`；不得把历史路线状态写入 RWKV 总控或章节恢复卡，反之亦然。
- 完成所属路线恢复前不得运行实验或重新分派实验任务。涉及服务器时先查询代理状态，再只读核对日志、进程、持久会话、磁盘和制品，防止重复启动。
- 事实优先级：原始日志和制品证明运行事实；实现或审查报告证明验收状态；实施计划证明需求；路线总控与章节恢复卡证明当前研究边界。冲突时以较新的原始证据修正所属路线事实文档，不以聊天摘要覆盖。

## 研究证据与实验有效性

- 用户提出的比例、模型、指标门槛、技术路线和实现方式均先作为待验证假设；核对数据、全文文献、既有实验和资源约束后给出采纳、修正或否决理由。用户知情后的最终决定仍不得突破学术诚信、数据泄漏和结果真实性边界。
- 方案、主任务、理论主张、候选排序、支持、证伪和冻结必须有可追溯全文文献或有效实验数据。概念分析、模块名称、文字审查和摘要只能形成“未验证”假设；文献证明机制来源与研究空白，不能替代本课题实验。
- 科研数值写入方案、代码或配置前必须登记用途和值、全文页码或公式（没有则写“无直接文献依据”）、数学或统计推导、真实数据诊断或源侧实验路径、适用边界、是否接触最终目标标签和核验状态。科研数值须同时通过机制依据与本数据可行性核验；工程资源数值以容量、峰值和日志收据为依据。缺项标为“未验证”，不得进入快速或正式实验，也不得事后根据最终测试补理由或改阈值。
- 每个实验裁决关联配置、数据版本、基线、指标程序、原始日志和制品路径；不得只报告最好数字，不得看过最终测试后选择数据、字段、候选、超参数或门槛，不得削弱基线。
- 证据顺序默认是真实数据门禁、共同预算强基线、单种子先导、机制消融与运行诊断、多种子正式实验。前一阶段无有效信号时否决、缩小或重构候选，不得用新增公式、模块或复审轮次代替实验。
- 只有数据泄漏、划分或样本关联错误、标签或指标错误、违反因果时序、比较预算不公平、实现不可运行、资源安全失败或数学条件不成立等会使结果无效的问题，才暂停受影响实验；其他不确定性改写为实验假设、运行断言或适用边界。
- 方案复审必须区分“设计可行、实验待证、实验支持、实验否决”。没有有效实验制品只能标“实验待证”；同一问题无新增代码、数据、实验结果或合同变化时，不重复发起文字复审。
- 多篇论文、预计超过 30 分钟或可能压缩上下文的调研，在任务目录建立 `task_plan.md` 与 `notes.md`；每完成一篇全文或一组查询即记录查询式、URL、原件、证据等级、页码、纳入理由、待办和 Zotero 状态。原件、结构化全文笔记和题录是事实源，聊天只作进度通知。

## 项目检索与外部证据

- 文献、相关工作、路线、实验、正文或跨文档研究判断，先运行 `uv run --project scripts/literature_search --locked python -m scripts.literature_search status --json`，再按唯一作用域执行离线混合查询：论文 `paper`、项目 `project`、实验 `experiment`、正文 `thesis`，明确跨域才用 `all`。精确代码和行号在混合检索之后用 `rg` 补充。
- 索引不存在或 `stale=true` 时按 `scripts/literature_search/README.md` 重建并复查；模型依赖确实不可用才降级 `lexical`，并披露向量通道未运行。结论记录作用域、模式、索引清单或构建时间、`stale=false` 和关键命中路径。
- 在线题录、摘要、Google Scholar、OpenAlex、Semantic Scholar、Crossref、Hugging Face 和代码仓库只作候选发现。没有 `raw/` 原件与 `wiki/` 全文笔记的来源不得升级为全文证据；需要正式纳入时执行 `raw/AGENTS.md` 与 `wiki/AGENTS.md`。
- 首次使用第三方库或对接口、属性、参数名不确定时，先核验目标环境实际版本，再查该版本官方文档或 Context7；记录库名、版本、来源和已核验签名。语法或导入通过不能证明运行时接口存在，事故细节留在专用故障手册而非常驻规则。

## 实施、技能与验证

- 非平凡多步骤任务使用 `planning-with-files`，在 `.Codex/docs/` 保存计划、发现和交付物；计划写明边界、文件范围、验收命令、制品路径和阻塞条件，仅在新证据改变范围时修订。
- 新研究机制、模型结构、公式或数据合同使用 `research-ideation` 后再 `brainstorming`；常规启动、重复运行、参数扫描、结果查询、只读诊断和根因明确的修复不得重开冻结合同。
- 非平凡实现或缺陷修复按 `writing-plans`、`subagent-driven-development`、`daily-coding` 执行。主代理负责计划、文件所有权、交接和最终验收；同一任务范围同一时间只允许一个实现代理修改，不覆盖、回滚或暂存他人改动。
- 缺陷或异常同时使用 `systematic-debugging` 与 `bug-detective`，先定位根因，提出一个可证伪假设，一次只改一个变量。回归验证形式服从近层实验规则；连续三次基于新证据的修复仍失败时停止补丁并讨论架构。
- 实验产出写入结论前执行 `results-analysis` 和 `results-report`；论文、图表、知识库和 Git 任务读取相应近层规则与技能。技能只提供工作流，不能裁决研究路线、数据合同、候选存废或实验结论。
- 能用哈希、模式、行数、状态码或资源阈值表达的校验，优先固化为幂等单入口门禁；通过后继续，失败才停止并输出诊断。机械化不能省略最终测试隔离、泄漏防护、科学评价或人工研究裁决。
- 修改后运行最小且有意义的验证；格式化、静态检查、测试和真实运行服从近层规则。不得以无关全量检查、归档或非阻断审查延迟已满足门禁的真实实验。

## 凭据、远程与破坏性操作

- 登录信息、密码、令牌和其他凭据只能来自环境变量、系统密钥存储或既有登录状态；禁止打印、回显或写入仓库、配置、日志、计划、命令清单、URL 和汇报。
- 远程动作前从所属路线总控读取当前服务器与凭据键来源，不在长期规则写死地址或变量；服务器切换、重启或回收后先只读核验身份、根目录、磁盘、依赖和持久化制品。不得沿用旧进程、运行目录、模型、数据或工具链假设，也不得把预期关机或回收计作科学失败。
- 删除、覆盖、强制推送、历史改写、`git reset --hard`、`git checkout --` 等破坏性操作前，列出准确对象并取得用户明确授权。原始数据、冻结数据、模型、检查点和日志不得删除唯一副本。

## 文档快照与 Git 所有权

- 每次修改文档前运行 `git status --short -- <目标路径>`。目标有未提交变更时，只暂存并提交这些目标文档作为快照；目标干净则当前 `HEAD` 已是快照，不创建空提交。
- 共享暂存区非空或含他人文件时，不创建快照提交、不清空暂存区、不夹带提交；先保留 `HEAD` 快照并记录阻塞。若目标文件本身被他人修改或暂存，停止并协调文件所有权。
- 工作树可能含用户或其他代理改动。只修改、暂存和提交本任务拥有的文件；不得覆盖、回滚、删除或格式化无关改动。发现直接冲突时停止并询问。
- 根 `.ignore` 不得排除规则、恢复文档、论文、实验源码、原件、数据、模型、运行制品、SwanLab 或普通日志。`llm_probe/runs/` 受 Git 忽略，核验时用总控明确路径或 `rg --no-ignore`，不得因默认无结果判定制品不存在。

## 子代理模型与推理强度

- 子代理名使用可读任务名并追加模型族与强度；创建通知和最终报告明确“代理名、完整模型、`effort`”。显式指定模型或强度时，`fork_turns` 只能为 `none` 或有限正整数，并通过任务简报传递接口、证据、文件所有权和验收。
- 初始分档：盘点、查询、简单下载用 `gpt-5.6-terra/low`；单模块实现和常规数据处理用 `gpt-5.6-terra/medium`；多文件实现和复杂实验诊断用 `gpt-5.6-sol/high`；理论设计和复杂文献综合用 `gpt-5.6-sol/max`；只有影响主路线且普通强度无法解决的极难裁决才用 `gpt-5.6-sol/ultra`。
- `followup_task` 不改变既有代理档位；需要升降级时新建代理并用简报交接。同类任务连续两次一次完成、无关键遗漏且耗时合理，可降一级强度；低强度出现推理不足或跨文件遗漏先升一级，连续两次能力不足才从 `terra` 升 `sol`。
- `NEEDS_CONTEXT`、外部资源不可用、服务器离线和需求冲突先修正简报或阻塞项，不以升档掩盖。`sol/high` 仍失败先拆任务或重判路线，不自动升 `ultra`。候选无提升或被实验否决是有效研究结果，不计代理失败。
