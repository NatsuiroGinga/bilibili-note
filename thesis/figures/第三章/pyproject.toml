[project]
name = "chapter3-publication-figures"
version = "0.1.0"
description = "第三章的机制示意图与实验结果图（机制随章节重锚变化，此处不写死机制名）"
requires-python = ">=3.11,<3.13"
dependencies = [
    "matplotlib>=3.10,<4",
    "pillow>=11,<13",
    "numpy>=1.26,<3",
]

[dependency-groups]
dev = [
    "black>=25.1,<26",
    "ruff>=0.12,<1",
]

[tool.uv]
package = false
