# 已撤下的第三章图件

本目录只存放**已从正文体系撤下**的旧图件，保留原文件以便追溯，不得再被正文引用，
也不出现在 `图件清单.json` 的 `figures` 段（只在 `withdrawn` 段登记撤下理由）。

| 文件 | 撤下日期 | 理由 |
| --- | --- | --- |
| `图3-10-攻击类别分面.{svg,pdf,png}` | 2026-08-26 | 旧图取自已作废的 `runs/diagnostics/ch3-full` 运行，且旧图自身在图面上声明该实验「已知存在缺陷，只作定性参考」（Category 字段对恶意流几乎全空，「(空)」类占五类正例的 99.84%）。当前权威制品——正式 CPA×ELP 四格 BF16 运行、统一指标总表、共同首次告警预算包络——均不含任何按攻击类别的分面字段，无法换底重绘。 |
| `图3-3-因果前缀跨流聚合.{svg,pdf,png}` | 2026-08-31 | 第三章重锚到 CEM-BER，表示层机制由因果前缀跨流聚合（CPA）改为因果实体记忆的读写与交叉注意力。旧图描述的机制不再是本章内容。同名号的新图为 `图3-3-因果实体记忆的读写与交叉注意力`。 |
| `图3-4-实体级可学Lp池化.{svg,pdf,png}` | 2026-08-31 | 第三章重锚到 CEM-BER，决策层机制由实体级可学幂平均池化（ELP）改为预算感知实体排序的训练目标。旧图描述的机制不再是本章内容。同名号的新图为 `图3-4-预算感知实体排序的目标构造`。 |

恢复条件：

- 图 3-10：若正文保留攻击类别分面，需要新增一次按 `Category` 字段分面的目标年评价运行，
  产出可追溯的分面指标制品后重新生成；在此之前正文不应引用图 3-10。
- 旧图 3-3 与旧图 3-4：只有正文重新采用 CPA 或 ELP 机制、且对应机制合同重新生效时才可恢复；
  两图不承载实验数据，恢复即重绘，不需要补运行。
