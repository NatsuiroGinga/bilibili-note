# 三目标梯度合成设计：逐流 BCE、实体级 BCE、BER 排序

<!-- RESEARCH_ROUTE=RWKV -->

- 日期：2026-09-03
- 分支：`codex/ch4-dtep-pbc-20260819`（worktree `.worktrees/ch4-dtep-pbc-20260819`）
- 触发：[四格设计的不对称与改法](四格设计的不对称与改法.md) 第 6.5 节用户裁决——补实体级 BCE 作四格共同底座，
  训练目标由两项变三项，**现有两任务梯度控制器与全部两任务分析随之失效**（该文 6.4 节第二条）
- 性质：**设计文档与形式化论证**。本文不写生产代码、不跑训练、不下实验结论
- 落点依据：机制方案属「会被后续引用的阶段性成果」，按仓库根 `AGENTS.md` 的产出文档分流判据落 `thesis/`

## 证据等级约定（四级，不得混写）

| 标签 | 含义 | 引用要求 |
| --- | --- | --- |
| **【全文核验】** | 原件或结构化全文笔记可回指 | 给节号、定理号、式号或附录号 |
| **【本课题实测】** | 本仓库运行收据、代码或已核验的历史裁决 | 给制品路径或文件行号 |
| **【本文推导】** | 从已核验定义出发的数学推导，可机械核验，但**未经本课题实验** | 给推导所依赖的前提与成立区间 |
| **【设计推论·未验证】** | 推理，须由实验裁决 | 给最小证伪实验 |

**第三级与第四级必须分开**：本文有若干结论是三行可验的代数事实（例如控制器对排序梯度的尺度不变性），
把它们与「加了这个机制会更好」这类推测并列陈述会误导后续裁决。

---

## 零、Research Question Card

按 `research-ideation` 技能的 Research Contract 模板（`references/research-contract.md` 第 12–29 行）填写。
**本卡的支持与证伪判据在看到任何 `g_e` 相关数据前写定**——`g_e`（实体级 BCE 对共享参数的梯度）
在本仓库**从未被单独计算过**，故下列判据不可能被已有数据平凡满足。

### 声明：写卡前已看过哪些数据

按仓库规则，用已看过的数据验证由它提出的假设是循环论证，故先列清单：

| 已看过 | 出处 | 与本卡判据的关系 |
| --- | --- | --- |
| C01 前两轮 `gradient_control` 收据（`‖g_f‖`、`‖g_r‖`、余弦、`c_scaling`） | `entity-ranking-diagnostics-{1,2}.json`，转录见 [BER 方案](BER-预算感知实体排序方案.md) 3.6 节 | 只用于确定**当前处在哪个工作区间**（饱和区），不作为任何判据的通过条件 |
| MLP O11 骨干上的历史梯度读数（12 批中 9 批 `c=1`） | `.Codex/docs/RWKV/2026-08-28-机制二梯度信号实测裁决.md` | 只用于说明区间会随骨干反转，不进判据 |
| 缩容档四格计时（C00-half `5.6` 分/轮、C01-half `14.7` 分/轮） | [四格设计的不对称与改法](四格设计的不对称与改法.md) 6.2 节 | 只用于估代价 |
| 实体级 BCE 关闭路径的逐位等价核验（53 个参数 `torch.equal` 全通过） | `.Codex/docs/RWKV/RWKV第三章恢复卡.md` L2 条 | 作为验收判据 V1 的**已通过部分**，见第八节 |
| **未看过：`g_e` 的任何范数、方向、与 `g_f` 或 `g_r` 的夹角** | — | 判据 S1、S2 全部建立在这批未测量上 |

### 卡片

```md
## Research Question Card

Question:
  训练目标由「逐流 BCE ＋ BER 排序」两项扩为「逐流 BCE ＋ 实体级 BCE ＋ BER 排序」三项后，
  三个梯度应当按什么结构合成，才能同时满足：(a) 保留「C00 训练目标不因加入 BER 而变差」
  的一阶保证；(b) 不引入顺序依赖；(c) 在实体级 BCE 权重为零时严格退化为现有控制器？

Type: confirmatory（结构性问题，答案由数学结构与已冻结的四格科学要求共同约束，不是开放探索）

Hypothesis:
  H1（分组假设）：逐流 BCE 与实体级 BCE 是**同一分类目标的两个粒度**，不是两个相竞争的任务；
      因此二者应先求和成单一「底座目标」，再与排序目标做既有的两路合成，
      而不是把三者当作三个对等任务做对称协调。
  H2（尺度假设）：三项之间的梯度范数失衡**不需要新机制处理**——排序侧的正尺度已被现有控制器
      按构造精确消去（第一节推论 P1），底座侧的全局尺度被 AdamW 吸收（推论 P3），
      故唯一实际起作用的自由量是底座内部的混合比 `w_e‖g_e‖/‖g_f‖`，它作用于方向而非幅值。

Why it matters:
  四格的科学主张是「C01 ≥ C00 且 C11 ≥ max(C10,C01)」。补底座后 C00 的训练目标变成
  「逐流 BCE ＋ 实体级 BCE」，故一阶不变量若仍以 `g_f` 为基准，保护的就不再是 C00 的目标，
  「不比 C00 差」这句话在形式上失去依据。合成结构的选择直接决定该主张能否成立。

Current evidence:
  - 现有控制器 ≡ PCGrad 式投影 ＋ 重归一化到 ‖g_f‖，即 CAGrad 的 c=1 边界【全文核验＋实测】
  - PCGrad 的逐任务顺序投影**有顺序依赖**，论文以随机洗牌换取「期望意义上的对称」，
    并在附录 H 做了固定顺序 vs 随机顺序的消融【全文核验】
  - CAGrad 定理 3.2 要求全部损失梯度 H-Lipschitz，而 L_rank（CVaR-pAUC）非光滑，前提本就不成立【全文核验】
  - IMTL-G 的尺度不变性（Theorem 2）成立，但全文无收敛速率保证，且其「无偏」是对称公理【全文核验】
  - 本课题优化器为 torch.optim.AdamW（本轮新核实，见 1.2 节），eps 与 betas 取默认值【本课题实测】

Missing evidence:
  - g_e 从未被单独计算：‖g_e‖ 的量级、cos(g_f,g_e)、cos(g_e,g_r) 全部空白
  - 实体级 BCE 与 BER 是否在优化同一方向（决定 §6.3 的「稀释风险」有多大）
  - 现有控制器落在饱和区（c<1）的步数占比——推论 P1、P2 只在该区间成立

What would support it（判据 S，四格开跑前冻结，不含任何自由常数）:
  S1（分组正确）：同一次运行内，cos(g_f,g_e)<0 的步数占比 **严格小于** cos(g_base,g_r)<0 的步数占比。
      解释：若两个粒度之间的冲突比「分类 vs 排序」还频繁，说明分组分错了对。
  S2（底座自洽）：s_f + s_e = 1 在每一步成立（数值容差内），其中
      s_f = ⟨g_base,g_f⟩/‖g_base‖²，s_e = ⟨g_base,w_e·g_e⟩/‖g_base‖²。
      这是恒等式（第六节 R4），不成立即实现有误。
  S3（退化）：w_e=0 时，同 θ、同批、同 RNG 下新旧合成逐位相等。

What would falsify it（判据 F）:
  F1：S1 的不等号反向或相等 → H1 被证伪，2+1 分组无依据，须改为对称三路方案（第三节给出其顺序无关形式）。
  F2：cos(g_e,g_r) 的运行均值 **高于** cos(g_f,g_r) 的运行均值 → 实体级 BCE 与 BER 在优化同一方向，
      §6.3 的稀释风险实现，预测 C01−C00 相对旧四格缩小。这是**预注册的预测**，不是通过门。
  F3：饱和区占比低到使 P1、P2 不再描述实际运行（占比须如实报告，正文的尺度不变性主张按该占比限定作用域）。
  F4：w_e=0 的逐位等价失败且排除 RNG 因素后仍失败 → 实现引入了副作用，回到原实现者修复。

Minimal next action:
  实现侧：把实体级 BCE 以求和形式挂进 _flow_phase_bare 与 _flow_phase_entity_memory，
  并把 combine_gradients 的第一实参由 g_flow 改为 g_base（函数体不改）；
  新增第七节的诊断埋点与第八节的机械断言。
  测量侧：在四格开跑前，用一次诊断反向单独取 g_e，落一份含 S1/S2/F2 所需量的收据。
  成本：无新增训练；诊断反向按第七节的节流频次计入，量级参考既有 stage_timing 的 diagnostic_backward 一项。

Decision: run experiment（结构已定，待实现与诊断数据裁决 H1、H2）
```

---

## 一、事实基础：先核对，不重推

### 1.1 现有控制器的形式与已核验行为

实现在 `thesis/experiments/llm_probe/tools/ch3_ft_gradient_controller.py` 第 45–92 行【本课题实测】：

$$
g_r^{+}=\begin{cases}
0, & \lVert g_f\rVert=0\\[2pt]
g_r-\dfrac{\langle g_f,g_r\rangle}{\lVert g_f\rVert^{2}}g_f, & \langle g_f,g_r\rangle<0\\[6pt]
g_r, & \text{否则}
\end{cases}
\qquad
c=\min\!\Big(1,\ \frac{\lVert g_f\rVert}{\lVert g_r^{+}\rVert}\Big),
\qquad
g=g_f+c\,g_r^{+}.
$$

两条一阶不变量（同文件第 95–137 行）：$\langle g,g_f\rangle\ge\lVert g_f\rVert^{2}$ 与 $\lVert c\,g_r^{+}\rVert\le\lVert g_f\rVert$。

调用点是 `ch3_ft_c00_dual_selection.py` 第 2764–2784 行 `_combine_and_step`，
由 `entity_ranking_training_step`（C01，第 2787 行）与 `combined_mechanism_training_step`（C11，第 2804 行）调用。
两者都先取 `g_flow`（`_flow_phase_bare` 或 `_flow_phase_entity_memory`），再取 `g_rank`（`_ranking_phase`），
合成后写回 `.grad` 并 `optimizer.step()`【本课题实测】。

文献定位已完成，不重做：该控制器 ≡「PCGrad 式投影 ＋ 重归一化到 $\lVert g_f\rVert$」，
即 CAGrad 的 $c=1$ 边界；`controlled_rank_step_norm` 与 `grad_norm_flow` 逐位相等
（epoch 2 两者同为 `1.2866296768188477`），故「排序梯度被压缩 628 倍」的读法已被推翻
【全文核验＋本课题实测，见[多目标梯度冲突文献调研](多目标梯度冲突文献调研.md) 1.7 节】。

### 1.2 三项损失的挂载现状与一处本轮新核实

| 项 | 符号 | 当前挂载位置 | 状态 |
| --- | --- | --- | --- |
| 逐流 BCE | $g_f$ | `training_step`（z2=0）与 `_flow_phase_bare`／`_flow_phase_entity_memory`（z2=1） | 既有 |
| 实体级 BCE | $g_e$ | **仅** `training_step`（提交 `c96bb74`，`mechanism.entity_bce` 开关后） | **z2=1 两格尚未挂载** |
| BER 排序 | $g_r$ | `_ranking_phase` | 既有 |

**两条必须先记清的实现事实**【本课题实测】：

1. **实体级 BCE 是以求和方式并入逐流损失后一次反传的**，不是第三条独立梯度。
   `training_step` 第 1564 行附近：`loss_sum = loss_sum + entity_bce_weight * entity_bce_value`，
   随后 `accumulator.backward(loss_sum, ...)`。提交信息与代码注释都写明「不新增第二次 `backward()`」。
2. **z2=1 的两格还没有这一项**：`_flow_phase_bare`（第 2141–2184 行）只算逐流 BCE。
   补底座的实现必须把同一项加进这两个函数，否则四格里只有 z2=0 的两格有底座，
   问题从「机制一只在 z2=1 有训练通道」变成「底座只在 z2=0 存在」，不对称没有消除，只是换了个方向。

**本轮新核实的一条**：优化器是 `torch.optim.AdamW`，由
`tools/ch3_ft_transformer_field_token_protocol_a.py` 第 3139 行构造，只传 `lr` 与 `weight_decay`，
故 `betas=(0.9, 0.999)`、`eps=1e-8` 取 PyTorch 默认值（默认值经
`npx ctx7@latest docs /websites/pytorch_2_13 "torch.optim.AdamW ..."` 核对，
文档同时写明 `eps` 是「term added to the **denominator**」、weight decay 与梯度更新解耦）【本课题实测＋官方文档核验】。

这条**关闭了文献调研 1.5 节列为限定第一条的未决项**（原文：「本课题使用的优化器未在本调研中核实。
若为 Adam 一类自适应方法，全局尺度 $\alpha$ 在很大程度上会被逐坐标的二阶矩归一化吸收」）。
后果见 5.3 节与 7.2 节。

### 1.3 三条先决数学性质（本文推导，可三行核验）

这三条决定了后面全部四问的答案，且**不依赖任何未做的实验**。

记饱和区为 $\lVert g_r^{+}\rVert\ge\lVert g_f\rVert$（即 $c<1$ 或 $c=1$ 的边界）。C01 前两轮实测范数比为 `22` 与 `641`，
深在该区间内【本课题实测】；但占比须按判据 F3 逐步记录。

**推论 P1（对排序梯度的正尺度不变性）**【本文推导】

对任意 $\kappa>0$，把 $g_r$ 换成 $\kappa g_r$：
分支判据 $\langle g_f,\kappa g_r\rangle<0$ 与 $\langle g_f,g_r\rangle<0$ 同号，落同一分支；
投影式对 $g_r$ 一次齐次，故 $g_r^{+}\mapsto\kappa g_r^{+}$；
受控步长 $c\lVert g_r^{+}\rVert=\min(\lVert g_r^{+}\rVert,\lVert g_f\rVert)\mapsto\min(\kappa\lVert g_r^{+}\rVert,\lVert g_f\rVert)$。
当 $\kappa\lVert g_r^{+}\rVert\ge\lVert g_f\rVert$ 且 $\lVert g_r^{+}\rVert\ge\lVert g_f\rVert$ 时两者同为 $\lVert g_f\rVert$，
而受控步的方向 $g_r^{+}/\lVert g_r^{+}\rVert$ 不随 $\kappa$ 变，故合成梯度 $g$ **完全不变**。

> **即：在饱和区内，给排序损失乘任何正常数都是空操作。** 本课题没有、也不需要「排序损失权重」这个超参。
> 不变性覆盖的 $\kappa$ 范围下界为当前的 $c$ 值，实测 `0.045` 与 `0.0016`，即向下约 `600` 倍仍在区间内。

**推论 P2（对底座梯度的一次正齐次性）**【本文推导】

把 $g_f$ 换成 $\kappa g_f$（$\kappa>0$）：投影方向只取决于 $g_f$ 的方向，故 $g_r^{+}$ **不变**；
在 $\kappa\lVert g_f\rVert\le\lVert g_r^{+}\rVert$ 时 $g\mapsto\kappa g_f+\kappa\lVert g_f\rVert\hat u_r^{+}=\kappa g$。
即合成算子在饱和区对第一实参一次正齐次。

**推论 P3（AdamW 对全局尺度的吸收）**【本文推导，依赖 1.2 节的官方文档核验】

AdamW 的一步为 $\theta\leftarrow\theta-\eta\big(\hat m/(\sqrt{\hat v}+\varepsilon)\big)-\eta\lambda\theta$，
其中 $\varepsilon$ 加在分母、权重衰减与梯度项解耦。梯度整体乘 $\kappa>0$ 使 $\hat m\mapsto\kappa\hat m$、
$\sqrt{\hat v}\mapsto\kappa\sqrt{\hat v}$，故梯度项变为 $\hat m/(\sqrt{\hat v}+\varepsilon/\kappa)$。
**在 $\kappa\sqrt{\hat v}\gg\varepsilon$ 时该项与 $\kappa$ 无关**，衰减项本就与梯度无关，
故参数更新对常数正缩放不敏感；$\varepsilon\to0$ 时为严格不变。

**两条不可忽略的限定**：其一，该结论对**随时间变化**的缩放只在 EMA 暂态之外成立，
而实测 $\lVert g_r\rVert$ 在两轮间变化 `8.72` 倍，暂态非平凡；
其二，$\kappa\sqrt{\hat v}\gg\varepsilon$ 是一个可测条件，不是可假定条件，须按第七节埋点核对。

**P1＋P2＋P3 合起来说明**：两个损失各自的正缩放，一个被控制器精确消去、一个被优化器近似吸收。
因此「`641` 倍范数失衡」这一现象**在更新方向上没有留下可调的自由量**。
新增第三项后仍要处理的，只有底座内部的**混合方向**，见第五节。

---

## 二、推荐方案的形式化定义

**方案名：底座—辅助分组合成**（结构上是 2+1 分组，不是三路对称协调）。

### 2.1 定义

设共享参数 $\theta$，三个损失 $L_{\text{flow}}$（逐流 BCE）、$L_{\text{ent}}$（实体级 BCE）、$L_{\text{rank}}$（BER）。
固定常数 $w_e\ge0$。定义

$$
L_{\text{base}}(\theta)\;=\;L_{\text{flow}}(\theta)\;+\;w_e\,L_{\text{ent}}(\theta),
\qquad
g_{\text{base}}=\nabla_\theta L_{\text{base}},\quad g_r=\nabla_\theta L_{\text{rank}} .
$$

合成算子**沿用现有 `combine_gradients` 不改一行**，只把第一实参由 $g_f$ 换成 $g_{\text{base}}$：

$$
g_r^{+}=\Pi_{g_{\text{base}}}(g_r),\qquad
c=\min\!\Big(1,\frac{\lVert g_{\text{base}}\rVert}{\lVert g_r^{+}\rVert}\Big),\qquad
g=g_{\text{base}}+c\,g_r^{+} .
$$

其中 $\Pi_{g_{\text{base}}}$ 是 1.1 节的条件投影（内积为负时投到 $g_{\text{base}}$ 的法平面，否则恒等）。

一阶不变量随之改写为

$$
\boxed{\ \langle g,\,g_{\text{base}}\rangle\ \ge\ \lVert g_{\text{base}}\rVert^{2}\ }
\qquad\text{与}\qquad
\boxed{\ \lVert c\,g_r^{+}\rVert\ \le\ \lVert g_{\text{base}}\rVert\ }.
$$

### 2.2 为什么保护的对象必须换成 $g_{\text{base}}$

这是本方案的核心论点，也是它与「原样保留 $g_f$ 作基准」的唯一实质分歧。

补底座之后，**C00 的训练目标就是 $L_{\text{base}}$**（[四格设计的不对称与改法](四格设计的不对称与改法.md) 第三节的四格表：
C00 = 逐流 BCE ＋ 实体级 BCE（max 聚合））。四格的预注册主张「C01 优于 C00」
是关于 C00 这个目标的主张。一阶不变量 $\langle g,g_{\text{base}}\rangle\ge\lVert g_{\text{base}}\rVert^{2}$
的含义正是「加入排序项后，更新方向在 C00 目标上的一阶下降量不小于纯 C00 更新」。

若仍以 $g_f$ 为基准，不变量保护的是**四格里已经没有任何一格在单独优化的那个目标**，
「不比 C00 差」这句话就没有形式依据。**这一步不是优化技巧的选择，是把保证对准科学主张的对象。**

### 2.3 四格的完整训练目标

| 格 | `z1'`（ETA） | `z2`（BER） | 训练目标 | 梯度合成 |
| --- | :-: | :-: | --- | --- |
| C00 | 0 | 0 | $L_{\text{flow}}+w_e L_{\text{ent}}^{\max}$ | 单一梯度，不进控制器 |
| C10 | 1 | 0 | $L_{\text{flow}}+w_e L_{\text{ent}}^{\text{tail}}$ | 单一梯度，不进控制器 |
| C01 | 0 | 1 | $L_{\text{flow}}+w_e L_{\text{ent}}^{\max}$ ⊕ $L_{\text{rank}}$ | $g_{\text{base}}$ 与 $g_r$ 经控制器 |
| C11 | 1 | 1 | $L_{\text{flow}}+w_e L_{\text{ent}}^{\text{tail}}$ ⊕ $L_{\text{rank}}$ | $g_{\text{base}}$ 与 $g_r$ 经控制器 |

$L_{\text{ent}}^{\max}$ 与 $L_{\text{ent}}^{\text{tail}}$ 分别对应
`ch3_ft_entity_bce.entity_bce_loss` 的 `aggregation="maximum"` 与 `"tail_mean"`，
后者即 ETA（$\alpha=0.5$ 已冻结）。`⊕` 表示经控制器合成而非求和。

**ETA 在四格两侧都有训练通道**这一点由此成立：$z2=0$ 时它经 $L_{\text{ent}}$ 进梯度，$z2=1$ 时同样如此。
交互项 $I=C11-C10-C01+C00$ 因此不再由构造决定——这正是补底座的全部目的。

### 2.4 与既有代码的差异面（供后续实施计划划边界）

| 文件 | 改动 | 是否改算术 |
| --- | --- | --- |
| `ch3_ft_gradient_controller.py` | **不改** | 否 |
| `ch3_ft_entity_bce.py` | **不改** | 否 |
| `ch3_ft_c00_dual_selection.py` `_flow_phase_bare` | 加入与 `training_step` **同一份**实体级 BCE 求和逻辑 | 是（新增项） |
| 同上 `_flow_phase_entity_memory` | 同上 | 是（新增项） |
| 同上 `_combine_and_step` | 形参更名与诊断键更名，函数体逻辑不变 | 否 |
| 收据模式 | 新增 `objective_grouping`、`schema_version` 与第七节诊断量 | 否 |

**求和逻辑必须只有一份实现**。`training_step` 里已有的那一段（微批内 `np.unique` 重编实体、
`np.maximum.at` 取实体标签、调 `entity_bce_loss`）应抽成一个共用函数供三处调用，
否则三处会各自漂移——这与 `ch3_ft_entity_bce.py` 文档字符串已经声明的「不重写聚合算术，避免两份实现漂移」同一理由。

<!-- 续写位置 -->
