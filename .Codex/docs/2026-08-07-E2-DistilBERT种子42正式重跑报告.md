# E2 DistilBERT 种子 42 正式重跑报告

## 结论

- 正式训练计算成功完成，未读取最终测试集，未修改冻结数据、划分、模型绑定、训练配置或评价协议。
- 训练完成 `6582` 个优化步，最佳源域校准宏平均 F1 为 `0.9998421032`。
- C 域宏平均 F1 为 `0.1465741544`，分组等权宏平均 F1 为 `0.2509197605`，最差组宏平均 F1 为 `0.1216`，恶意召回率为 `0`，良性误报率为 `0`，精确率－召回率曲线下面积为 `0.7101435145`。
- 结果说明编码器在源域拟合充分，但固定阈值下没有检出 C 域恶意样本；该现象是跨域泛化失败证据，不应描述为模型有效。

## 运行身份

- 服务器启动证据目录：`runs/launchers/e2-distilbert-seed42-abd-to-c-v1-20260807T055932Z-5606`
- 服务器输出目录：`runs/baselines/e2-distilbert-seed42-abd-to-c-v1-20260807T055932Z-5606`
- 本机启动证据目录：`thesis/experiments/llm_probe/runs/launchers/e2-distilbert-seed42-abd-to-c-v1-20260807T055932Z-5606`
- 本机输出目录：`thesis/experiments/llm_probe/runs/baselines/e2-distilbert-seed42-abd-to-c-v1-20260807T055932Z-5606`
- SwanLab 运行号：`j9wzx2ee`
- SwanLab 地址：`https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/j9wzx2ee`
- 基础模型绑定：`b8354b5403f233dc37819a9e083f86c30723437631a1988546a9ecc042cef69e`

## 训练设置

- 基座：`distilbert/distilbert-base-multilingual-cased`
- 面板：`A+B+D → C`
- 种子：`42`
- 训练轮数：`3`
- 训练批大小：`16`
- 梯度累积：`2`
- 最大序列长度：`128`
- 学习率：`2e-5`
- 精度：`bfloat16`
- 训练样本：`83099`
- 校准样本：`26216`
- C 域验证样本：`2952`

## 后置验收误判及修复

训练主命令与日志管道退出码均为 `0`，`run_state.json` 和 `artifact_manifest.json` 均为 `finished`。启动器最初仍将状态写为 `failed`，根因是 `runs/` 受到 Git 忽略规则影响，而启动器使用了 `rg --files --hidden` 枚举 SwanLab 文件，返回 `0` 行和退出码 `1`。

只修改了 `thesis/experiments/llm_probe/scripts/run_e2_distilbert_formal.sh` 的两个后置枚举：

- SwanLab 文件门禁改为 `rg --files --hidden --no-ignore`。
- 输出哈希枚举改为 `rg --files --hidden --no-ignore`。

没有重训，也没有修改任何实验输出。对既有制品重新执行相同后置门禁后，启动器状态已原子纠正为 `finished`，并生成含 `21` 项的 `output.sha256`。

## 验证情况

- 本机 Shell 语法检查通过：`bash -n scripts/run_e2_distilbert_formal.sh`。
- 本机与服务器修复后脚本哈希一致：`0552222ce2d14ce459127528cda0c5225b3c72f6e2eb543816f36758f9724116`。
- 主命令退出码：`0`。
- `tee` 退出码：`0`。
- 运行状态：`finished`。
- SwanLab 模式：`online`，上传 `780` 条记录。
- 六项核心制品均存在且非空：摘要、预测、训练摘要、指标、模型权重和检查点清单。
- 本机根据服务器 `output.sha256` 校验全部 `21` 项制品，全部通过。

## 局限性

- 本轮只有种子 `42`，不能据此报告均值、标准差或显著性。
- 宏平均 F1 与传统强基线接近，但恶意召回率为 `0`，不能作为主方法候选；其价值是证明 C 域具有真实跨域难度，并为 E2 方法变体建立编码器参照。
- 当前程序仅在运行开始和最终阶段写 SwanLab 指标，不能从网页查看逐批训练曲线；该缺陷不影响本轮最终结果，但后续正式多种子运行应增加周期级指标。
