# DRIFT 第三章全文与综述联合阅读：网页版预推送简报

## 当前状态

- **状态：正式出站包已生成，但尚未发送。** 本文件记录推送与生成过程；不得把包已生成写成网页版已收到。
- 仓库：`NatsuiroGinga/bilibili-note`。
- 分支：`exp/ch3-drift-20260908`。
- 固定证据提交：`1e2201f5576f9d78feabc3e042e9c65423b4ef4d`；远端分支已推送并经 `git ls-remote` 核验；新版网页版任务尚未实际读取，连接器访问情况待发送时核验。
- 当前白名单：16 个仓库相对 Markdown 路径，见同目录 `drift-ch3-full-evidence-review-paths.txt`。
- 原文入口：DRIFT 官方 arXiv v2：<https://arxiv.org/abs/2605.10436>。交接工具禁止将目录名含 `datasets/` 的路径列入白名单，因此仓库内 DRIFT 结构化笔记虽已进入本地证据提交，但正式交接改由官方原文入口承担全文阅读。

## 路径状态核对

17 份核心证据 Markdown 已进入并推送到固定提交 `1e2201f`，其中 16 份属于交接白名单；DRIFT 结构化笔记因目录门禁不进入白名单，原文改由官方 arXiv v2 入口提供。远端分支 `exp/ch3-drift-20260908` 已指向完整固定提交，16 份白名单文件可由网页版按提交读取。

## 交接目的与阅读范围

用户授权提交并推送后，正式交接包应让普通 ChatGPT 先阅读朱焱雷第三章全文 Markdown、DRIFT 官方 arXiv v2 原文及仓库结构化全文笔记、两份文献综述，再结合当前 F 方案、V4 报告、数据角色、正式攻击／抽样与评价裁决材料，做一次有界的全文与综述联合阅读。普通 ChatGPT 的输出只作**外部候选，待本地全文和实验复核**，不得裁决实验、修改合同或替代本地证据核验。

联合阅读应核对：

1. F 指标理论链的定义、推导前提、可观测量与适用边界。
2. 朱焱雷论文第三章的主表、消融结论、机制数量和创新声明，与当前第三章材料的对应关系。
3. DRIFT 原论文的任务、数据、时间漂移设定与方法证据，不把综述或摘要当作原文实验结论。
4. 当前方法来源台账、命题级原创性排重与科研裁决中，哪些属于已有文献事实、哪些只是待验证假设。
5. 正式指标提升空间与 F 指标空间的关系；只指出可核对的证据缺口，不提出新阈值、样本、超参数或候选存废决定。
6. `2024-Losch-ESAT选择性对抗训练.md` 与当前问题的直接相关性、差异和不能据此推出的结论。

## 任务边界

- 只读取路径白名单中的仓库相对 Markdown，以及任务中给出的 DRIFT arXiv 公开入口。
- 不读取或请求原始 PDF、日志、数据、模型、权重、检查点、凭据、服务器连接、绝对路径或目标期结果。
- 不运行代码，不验证实验有效性，不改写论文、路线合同、恢复卡或实验配置。
- 不把网页输出写回知识库或研究结论；回收后必须保留“外部候选，待本地全文和实验复核”标记。

## 推送后交接状态

1. 已完成：用户明确批准五个提交及其敏感内容范围，随后完成远端纯快进推送。
2. 已完成：远端固定提交已回填简报与正式出站包。
3. 已完成：以 `drift-ch3-full-evidence-review-paths.txt` 为唯一白名单生成正式包并通过预检。
4. 待完成：由独立对接子代理在浏览器控制可用后发送；网页确认接收后登记回执，完整回答回收后登记原始输出。

## 预期回收格式

要求普通 ChatGPT 返回 Markdown，逐项列出实际读取路径与固定提交、原文和综述证据、对应小节或页码、可证伪的最小本地核查以及无法由白名单判断的事项；同时报告 `actual_model`、`actual_effort`、`actual_mode` 和 `used_apps`。如果界面未暴露思考强度，必须写明“未核验”，不得猜测。

## 当前执行记录

- 实际模型：`gpt-5.6-luna`。
- 实际强度：`medium`（主代理派发参数已明确指定）。
- 已执行：读取仓库规则、ChatGPT 交接技能与既有 MP 交接回执；核对路径状态；在用户知情授权后将五个提交纯快进推送到远端；以 `git ls-remote` 核验远端固定提交；运行 `chatgpt_handoff.py create` 生成正式出站包；由独立 `gpt-5.6-terra`（`medium`）复核包内容。
- 推送结果：远端分支 `exp/ch3-drift-20260908` 已指向 `1e2201f5576f9d78feabc3e042e9c65423b4ef4d`；未改写历史、未创建替代分支。
- 未执行：新版浏览器发送与回收。独立代理的浏览器控制器列表为空，因此没有启动回执和原始回收件。
