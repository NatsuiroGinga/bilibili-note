# P0 AGENTS 上下文精简实施计划

## 目标

- 将进入 `llm_probe` 与 `scripts` 的累计规则分别压到 `31,768` 字节以下，保留至少 `1,000` 字节余量。
- 不提高 `project_doc_max_bytes`，不新增生命周期 Hook，不影响正在运行的实验。

## 边界

- 修改：`thesis/experiments/llm_probe/AGENTS.md`、`thesis/experiments/llm_probe/scripts/AGENTS.md`。
- 新建：全局精简候选、原文件指纹清单、故障防复发手册和最终精简报告。
- 用户级 `/Users/bilibili/.codex/AGENTS.md` 不直接修改；只生成 `.Codex/docs/agents-split/AGENTS.global.compact.md`，由用户手动备份并覆盖。
- 不修改：实验代码、配置、结果、运行制品、两份恢复文档、根 `AGENTS.md`、`.Codex/docs/AGENTS.md`、`thesis/AGENTS.md`。

## 实施步骤

- [x] 完整读取六层规则、相关技能与官方手册缓存。
- [x] 记录原文件字节数和 SHA-256。
- [x] 生成全局通用规则精简候选，保留语言、`rg`/`fd`、`apply_patch`、凭据和安全边界。
- [x] 将 `llm_probe` 故障案例迁入按需手册，常驻文件保留触发路由与核心断言。
- [x] 压缩技能、审查、代理、Git 与脚本远程规则的重复展开。
- [x] 用 `wc -c` 验收两条组合，用 `rg` 验证关键规则仍存在。
- [x] 写最终报告。

## 验收命令

- `wc -c`：计算全局、根、`thesis`、`llm_probe` 和 `scripts` 的单文件与组合字节数。
- `rg`：核对恢复协议、凭据、GPU 通知、SwanLab、结果写回、统一启动器、禁止 `--delete`、退出码与日志门禁仍存在。

## 决策与阻塞条件

- 详细案例只在出现匹配故障时读取，避免常驻上下文。
- 通用工作流只保留技能触发规则，实验局部覆盖继续常驻。
- 可机械检查且已有 Hook 或包装器覆盖的要求只保留一句，不新增 Hook。
- 用户级文件不直接写入；仓库内候选通过后只提供手动备份与覆盖命令。

## 状态

已完成。用户级原文件保持 `10,385` 字节；当前实际 `llm_probe` 与 `scripts` 累计分别为 `30,737` 和 `31,609` 字节，均低于 `31,768` 字节。全局候选安装后分别降至 `23,857` 和 `24,729` 字节。
