# AGENTS.md 渐进拆分 P0/P1 笔记

## 初始事实

- 运行标识：`20260730-130222`。
- Codex 版本：`codex-cli 0.144.5`。
- Git 提交：`acdf98ba5cc776f7c19495fc53b1164e916eb33c`。
- 当前项目规则字节数：根 `7,947`，`thesis` `6,202`，`llm_probe` `25,589`，三层合计 `39,738`。
- 三层合计超过默认 32,768 字节 `6,970` 字节。
- `scripts/AGENTS.md` 在 P0 时不存在。
- `llm_probe/AGENTS.md` 相对 `HEAD` 有 19 条既有新增规则；调研文档使用的 `25,589` 字节审计值与当前工作树完全一致，因此将当前工作树版本冻结为基线。

## 初始哈希

- 根：`7d066755da5676a7968b3276efb522fd5b574520643e7dd5384f596b5153e4c2`。
- `thesis`：`673e6c3b2ac8e35d88f6029de00f6bce630bc4aa76a4a3186782d7dfc1840e23`。
- `llm_probe`：`ead7c2027693e8a66aaf9d1c4fa8ed26bdaafcb3ce0686907e3c76cf17d99ce3`。

## 范围判断

- P1 迁移远程启动器、环境加载、`rsync`、`screen`、重启恢复、服务器命令能力、`fd`、`rg`、`uv`、Shell 引用、真实换行、`tee` 父目录、`pipefail` 与 `PIPESTATUS`、脚本语法门禁。
- 数据合同、训练配置、模型视图、生成式评测、SwanLab、测试策略和 Git 规则不迁入脚本层。

## P1 预算决策

- 父级完成约定迁移后为 16,456 字节；根、`thesis` 与父级合计 30,693 字节。
- 脚本自动注入层只能使用剩余 2,075 字节，无法容纳未经删减的 6,857 字节完整合同。
- 为避免提前实施 P2/P4，脚本层常驻 1,641 字节关键门禁，并强制动作前读取完整执行合同。
- 最终 `scripts` 项目链为 32,334 字节，低于默认预算 434 字节；完整语义通过强制按需读取保留。
