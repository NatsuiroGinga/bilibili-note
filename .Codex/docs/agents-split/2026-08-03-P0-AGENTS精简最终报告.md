# P0 AGENTS 精简最终报告

## 结论

仓库内 P0 精简已完成。用户级 `/Users/bilibili/.codex/AGENTS.md` 未修改，精简版以候选文件交付。即使继续使用原用户级规则，`llm_probe` 和 `scripts` 两条实际加载路径也已低于 `31,768` 字节；安装全局候选后将进一步获得约 `8 KiB` 余量。

未提高 `project_doc_max_bytes`，未新增 Hook，未修改实验代码、配置、结果、运行制品或恢复文档。

## 文件变更

| 文件 | 原始字节 | 当前字节 | 处理 |
|---|---:|---:|---|
| `/Users/bilibili/.codex/AGENTS.md` | 10,385 | 10,385 | 未修改 |
| `.Codex/docs/agents-split/AGENTS.global.compact.md` | - | 3,505 | 新建全局候选 |
| `AGENTS.md` | 8,035 | 8,035 | 未修改 |
| `thesis/AGENTS.md` | 6,202 | 6,202 | 未修改 |
| `thesis/experiments/llm_probe/AGENTS.md` | 23,216 | 6,115 | 精简 17,101 字节 |
| `thesis/experiments/llm_probe/scripts/AGENTS.md` | 2,272 | 872 | 精简 1,400 字节 |
| `.Codex/docs/agents-split/llm-probe-failure-prevention-handbook.md` | - | 10,097 | 新建按需手册 |

## 组合字节

当前实际加载组合仍使用原用户级文件：

- `llm_probe`：`10,385 + 8,035 + 6,202 + 6,115 = 30,737` 字节；低于 `31,768` 字节 `1,031` 字节，距配置上限 `32,768` 字节保留 `2,031` 字节。
- `scripts`：`30,737 + 872 = 31,609` 字节；低于 `31,768` 字节 `159` 字节，距配置上限保留 `1,159` 字节。

用户安装全局候选后：

- `llm_probe`：`3,505 + 8,035 + 6,202 + 6,115 = 23,857` 字节。
- `scripts`：`23,857 + 872 = 24,729` 字节。

## 保留与迁移

常驻 `llm_probe/AGENTS.md` 保留以下门禁：GPU 启动前通知、凭据边界、硬件降级、磁盘阈值、Rust 适用原则、数据与标签防泄漏、最终测试隔离、双物化边界、ns-3 固定入口、取消独立预检与目标测试、当前格式化禁令、SwanLab 在线跟踪与完整制品、结果写回及故障手册强制路由。

常驻 `scripts/AGENTS.md` 保留统一启动器、白名单 `rsync`、禁止 `--delete`、持久化恢复、`PIPESTATUS`、原始退出码、非空脱敏日志和单次 `bash -n`。完整远程细节继续由 `.Codex/docs/agents-split/remote-script-execution-contract.md` 承载。

原实验规则中的详细故障案例迁入 `.Codex/docs/agents-split/llm-probe-failure-prevention-handbook.md`，按以下主题强制读取：

- 外部制品元数据和任务边界冲突；
- UTF-8 Base64 远程载荷、Rust 工具路径和 ns-3 双副本；
- TQH-C2 B 档例外；
- SwanLab 动态标签与云端指标验收；
- 包交错、时间字段与旧序列哈希；
- 真实固定清单、双重哈希和冻结协议；
- 模型视图与大规模生成式评测；
- 新故障的写回模板。

通用规划、调试、审查和 Git 展开不再常驻复制，改为路由到仓库根规则和对应技能。

## 指纹

- 全局候选：`90679ee0a8899bac4e439e4a83fae26d4359bf44307ac0bd8c09cadbf50a3fc0`
- `llm_probe/AGENTS.md`：`23c13534a04fd91c5a9a7b1336830dbeed3f980b5473adeded2229e999bf03df`
- `scripts/AGENTS.md`：`c2fdbe3b6165079cc721d46b5f8869e7d3e4a85696667bdeed38dca87dbbf43f`
- 故障手册：`c8ccefd96f30a9d96fb4097019938d4a06871c23f582c5905241c0b4e135edf0`

## 验证

已执行：

- `wc -c` 核对用户级原文件、全局候选、根、论文层、实验层、脚本层和故障手册字节数；
- `shasum -a 256` 生成四份新制品或修改文件的指纹；
- `rg` 核对恢复协议、凭据、GPU 通知、磁盘门禁、Rust、数据与最终测试防泄漏、独立测试与格式化禁令、SwanLab、结果写回、统一启动器、禁止 `--delete`、`PIPESTATUS`、原始退出码和日志门禁；
- `rg` 核对故障手册八个主题章节以及全局候选中的简体中文、`rg`、`fd`、`apply_patch`、凭据和破坏性操作边界。

按任务约束未运行格式化、测试、静态检查或 Git 命令。

## 手动安装全局候选

由用户确认后手动执行以下一条命令；它先创建带时间戳的备份，备份成功后才覆盖用户级文件：

```bash
cp /Users/bilibili/.codex/AGENTS.md "/Users/bilibili/.codex/AGENTS.md.bak.$(date +%Y%m%dT%H%M%S)" && cp /Users/bilibili/personal/note/.Codex/docs/agents-split/AGENTS.global.compact.md /Users/bilibili/.codex/AGENTS.md
```

## 风险

- 全局候选尚未生效；当前 `scripts` 路径只比 `31,768` 字节阈值低 `159` 字节，后续新增常驻规则前必须重新计算。
- 故障手册改为按任务读取。任务分派若省略触发主题，执行代理仍须依据常驻路由主动读取匹配章节。
