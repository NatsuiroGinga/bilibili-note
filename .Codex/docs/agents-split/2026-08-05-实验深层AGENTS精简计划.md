# 实验深层 AGENTS 精简计划

## 目标

在不削弱恢复、凭据、数据防泄漏、GPU 通知、SwanLab、日志、结果写回、同步安全和实验非阻塞纪律的前提下，将用户级、仓库根及实验三层规则累计压缩到 32,768 字节以内，并预留至少 1,500 字节。

## 阶段

- [x] 读取根规则、三层目标规则和现有拆分文档。
- [x] 识别常驻规则与按需细节的边界。
- [x] 新建按需运行合同并精简三层目标规则。
- [x] 核算五层累计字节并检索关键哨兵、规则和链接。
- [x] 产出精简报告。

## 边界

- 只修改 `thesis/AGENTS.md`、`thesis/experiments/llm_probe/AGENTS.md`、`thesis/experiments/llm_probe/scripts/AGENTS.md` 和 `.Codex/docs/agents-split/`。
- 不修改用户级或仓库根 `AGENTS.md`，不修改实验代码。
- 不运行测试驱动开发、格式化器、全仓检查或 Git 操作。

## 当前状态

已完成。五层累计 31,225 字节，低于上限 1,543 字节。
