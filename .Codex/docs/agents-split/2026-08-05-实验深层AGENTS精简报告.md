# 实验深层 AGENTS 精简报告

## 结论

实验脚本最深路径的五层默认规则已由 37,826 字节降至 31,225 字节，减少 6,601 字节，低于 32,768 字节上限 1,543 字节。恢复、凭据、数据防泄漏、GPU 通知、SwanLab、本地日志、结果写回、白名单同步、安全删除、复审非阻塞和冻结实验纪律仍在有效加载链中；根规则中的 `brainstorming` 条件触发规则继续继承。

## 字节变化

| 文件 | 改前 | 改后 | 变化 |
|---|---:|---:|---:|
| 用户级 `AGENTS.md` | 10,385 | 10,385 | 0 |
| 仓库根 `AGENTS.md` | 10,258 | 10,258 | 0 |
| `thesis/AGENTS.md` | 6,202 | 4,608 | -1,594 |
| `thesis/experiments/llm_probe/AGENTS.md` | 10,109 | 5,168 | -4,941 |
| `thesis/experiments/llm_probe/scripts/AGENTS.md` | 872 | 806 | -66 |
| **五层累计** | **37,826** | **31,225** | **-6,601** |

## 迁移与保留

- 新建 `.Codex/docs/agents-split/llm-probe-operational-contract.md`，按章节承接服务器切换与硬件降级、镜像下载、资源监测、受控并行、Rust 数据热路径、确定性物化、生成式评测加速和工具检查细节。
- 远程入口、Shell 引用、环境、白名单 `rsync`、SHA-256、可恢复状态、管道日志和脚本门禁继续由 `remote-script-execution-contract.md` 承接。
- 具体故障案例、SwanLab 异常、TQH-C2、包时间与哈希、冻结合同、模型视图、PINN 指标、TCP 配对和 ns-3 故障继续由 `llm-probe-failure-prevention-handbook.md` 承接。
- 通用规划、Git、调试和审查流程不再在实验深层复述，继承仓库根规则及对应技能。
- `thesis/AGENTS.md` 仍常驻课题定位、第三四章边界、朱焱雷工作量参照、PINN 公式纪律、数据证据角色和正文图件纪律。
- `llm_probe/AGENTS.md` 仍常驻当前凭据变量、GPU 使用前通知、服务器回收边界、CPU/GPU 分工、磁盘通知、数据防泄漏、最终测试隔离、正式运行断言、复审非阻塞、白名单同步、安全删除、SwanLab 在线项目、本地原始日志和结果写回。
- `scripts/AGENTS.md` 仍常驻远程合同入口、统一启动器、禁止 `rsync --delete`、持久化状态和 `PIPESTATUS` 日志纪律。

## 验证

- 使用 `wc -c` 核算用户级、仓库根和三层目标文件，累计 31,225 字节。
- 使用 `rg` 核验三层规则加载哨兵、三份按需文档链接及 `brainstorming` 继承说明。
- 使用 `rg` 核验 `GPU_SSH_378`、数据防泄漏、最终测试隔离、SwanLab、本地原始日志、`review_pending`、严重问题作废、禁止 `rsync --delete`、安全删除和两份恢复文档写回规则。
- 未运行测试驱动开发、格式化器、全仓检查或 Git 操作；本任务未修改实验代码。

## 变更文件

- `thesis/AGENTS.md`
- `thesis/experiments/llm_probe/AGENTS.md`
- `thesis/experiments/llm_probe/scripts/AGENTS.md`
- `.Codex/docs/agents-split/llm-probe-operational-contract.md`
- `.Codex/docs/agents-split/2026-08-05-实验深层AGENTS精简计划.md`
- `.Codex/docs/agents-split/2026-08-05-实验深层AGENTS精简笔记.md`
- `.Codex/docs/agents-split/2026-08-05-实验深层AGENTS精简报告.md`

用户级与仓库根 `AGENTS.md` 未修改，实验代码、配置和运行制品未修改。

## 遗留风险

- 当前只剩 1,543 字节余量。用户级、仓库根或三层规则后续新增内容前必须重新运行五层字节核算。
- 按需合同不会自动加载；任务必须依照 `llm_probe/AGENTS.md` 的主题路由读取匹配章节。若未来新增新的实验故障类型，应优先写入按需文档，而不是扩充常驻规则。
