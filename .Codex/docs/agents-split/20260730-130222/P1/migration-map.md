# P1 迁移映射

行号以 P0 快照 `AGENTS.llm-probe.md` 为准。

| 父文件原规则              | 子文件目标章节                   | 处理                                                                                                            |
| ------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 12 的环境加载部分、13、14 | 环境与能力预检、同步与可恢复运行 | 凭据边界留在父级；Shell、PATH、`uv` 与 `screen` 迁出                                                            |
| 18 至 26                  | 同步与可恢复运行                 | 保留状态、检查点、`.partial`、关机和重启合同，压缩历史叙述                                                      |
| 30 至 33                  | 同步与可恢复运行                 | 保留 `rsync` 白名单、排除、校验、回收与不覆盖本地改动                                                           |
| 37 至 40                  | 环境与能力预检                   | 保留 `uv` 安全档位；与能力预检合并                                                                              |
| 84 的 `bash -n` 部分      | 脚本门禁                         | 其他格式化规则留在父级                                                                                          |
| 94                        | 同步与可恢复运行、脚本门禁       | 保留模式检查、`bash` 调用和语法门禁                                                                             |
| 97 至 104                 | 各对应章节                       | 迁移失败启动归档、能力探测、状态输出、本机缓存与 `screen`、引用、测试包装器和长回归规则；105 起的数据合同不迁移 |
| 109、111 至 114           | 环境与能力预检、启动与日志完整性 | 保留 `.bashrc` 顺序、远端父目录、资源采样、staging 与 `jq` 防复发合同                                           |
| 调研文档 208 至 218       | 环境与能力预检                   | 新增统一能力清单，替代易过时的“已安装”事实                                                                      |
| 调研文档 229 至 239       | 远程入口与引用                   | 新增真实换行和单一暂存脚本合同                                                                                  |
| 调研文档 241 至 253       | 启动与日志完整性                 | 新增 `tee` 父目录、`pipefail`、`PIPESTATUS` 和五类冒烟                                                          |

不迁移数据合同、模型视图、训练科学变量、生成式评测、SwanLab、Git、Python 测试选择或论文写回规则。

## 两级加载

父级迁出后，根、`thesis` 与实验根合计 30,693 字节，只给脚本层留下 2,075 字节。为避免提前压缩 P2/P4 范围或删除规则语义，`scripts/AGENTS.md` 常驻 1,641 字节关键门禁，并强制任何脚本修改、同步或运行前完整读取 `.Codex/docs/agents-split/remote-script-execution-contract.md`。完整合同 6,857 字节，保存上表全部执行语义；历史触发过程另存 `remote-script-rule-history.md`。
