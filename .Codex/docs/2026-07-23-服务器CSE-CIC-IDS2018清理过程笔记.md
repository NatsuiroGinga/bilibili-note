# 服务器 CSE-CIC-IDS2018 清理过程笔记

## 操作边界

- 服务器根目录：`/root/autodl-tmp/thesis/datasets/`
- 允许名称：`CSE-CIC-IDS2018`、`CSE-CIC-IDS2018-PCAP`，以及以这两个完整名称为主体的明确归档文件。
- 禁止范围：本机备份、服务器实验目录、运行制品、模型、代码和其他数据集。

## 审计发现

- 审计时间：2026-07-23 17:49:10（服务器时区 `+08:00`）。
- `bash -ic 'command -v fd; command -v fdfind'` 显示 `fd` 是 `fdfind` 的别名，实际可执行文件为 `/usr/bin/fdfind`。
- `/root/.cargo/bin/fd`、`/root/.local/bin/fd`、`/usr/local/bin/fd` 均不存在或不可执行。
- 指定根目录的顶层只有 `DEDALE-2.0`、`GeNIS-2025`、`HIKARI-2021` 三个目录。
- 递归精确匹配允许名称及常见归档扩展的候选数为 0。
- 递归、忽略大小写的 IDS2018 宽松名称匹配数也为 0。
- 因候选数为 0，没有候选类型或候选大小可供 `du` 汇总；整个数据集根目录的 `du -sh` 结果为 4.2G。

## 删除与验证

- 未运行 `rm` 或其他删除命令。
- 最终精确匹配数为 0，宽松匹配数为 0。
- 审计前：53,687,091,200 字节总量，41,055,526,912 字节已用，12,631,564,288 字节可用，使用率 77%。
- 最终验证：53,687,091,200 字节总量，41,056,534,528 字节已用，12,630,556,672 字节可用，使用率 77%。
- 本任务可归因的释放空间为 0 字节。两次检查之间已用空间增加 1,007,616 字节，属于服务器同期活动，不是本任务删除结果。
- 未操作本机 `raw/datasets/`，也未操作服务器实验目录、运行制品、模型、代码或其他数据集。
