# E2-A/S/P/X 种子 42 路线裁决计划

## 目标

在不读取最终测试集、不干预训练且不事后改变指标或阈值的前提下，依据完整 E2-A/S/P/X 制品裁决共享 PINN 是否进入 E3。

## 阶段

- [x] 阶段 1：恢复 PINN/R2 路线规则与实验合同。
- [x] 阶段 2：核验配置、指标实现、A 制品和运行目录结构，冻结裁决模板。
- [ ] 阶段 3：只读等待并核验 S/P/X 完整制品。
- [ ] 阶段 4：计算固定指标差值并给出唯一裁决。
- [x] 阶段 5：完成待证报告与最小文档验证。

## 固定问题

1. E2-P 是否在相同数据、编码器、训练预算和阈值协议下满足 `P>A` 且 `P>X`？
2. E2-P 相对 E2-S 是否产生守恒约束的独立增益，而不是仅来自状态辅助？
3. E2-P 是否在最差组、源域非退化、校准、物理误差和训练成本上没有出现会否决路线的问题？

## 固定裁决

- `支持继续 E3`：完整制品有效，核心门禁 `P>A` 且 `P>X` 成立，并通过源域非退化及结果有效性检查。
- `进入唯一一次 E4`：完整制品有效，但核心门禁或关键辅助门禁失败。
- `结果无效`：存在数据、划分、标签、指标、阈值、运行完整性或比较预算错误，不能用于方法裁决。

## 禁止事项

- 不读取最终测试集。
- 不修改训练代码、数据、阈值、指标或实验进程。
- 不按结果挑选指标、样本、运行或阈值。
- 单种子只作路线筛选，不作统计显著性或最终论文优越性主张。

## 已遇到的问题

- S/P/X 首次恢复在训练前被“完成态 A 优化器状态集合校验过严”拦截；A 未修改或重训，恢复代理正在执行最小修复。本问题属于运行恢复，不是科学结果。

## 当前状态

**暂停于阶段 3**：裁决模板和 A 锚点已经冻结；S/P/X 尚未启动。恢复校验修复已完成，但在本次记录截止时尚未重新启动。按主代理要求停止轮询，后续出现完整制品后再继续阶段 3、4。
