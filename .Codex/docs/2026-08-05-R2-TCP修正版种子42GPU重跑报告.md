# R2 TCP 修正版种子 42 GPU 重跑报告

## 当前结论

TCP 可辨识监督修正版、G3 逐簇缺测掩码修复和全新物理专家拟合均已完成。种子 42 的 `F-A/F-P/F-S` 已在独立运行目录中跑满 `711` 步并正常结束；最终测试保持不可见。

修正版 `F-P` 的开发验证宏平均 F1 为 `0.9090792054`，比 `F-S` 高 `0.0137455862`，但比 `F-A` 低 `0.0199344021`。等协议物理残差相对 `F-A` 改善 `9.47%`，未达到冻结合同要求的 `10%`；等协议状态误差反而恶化 `2.05%`。因此结论是：**TCP 修复与重跑在工程上完成，但当前修正版未通过方法冻结门槛，不能继续补跑种子 43、44 来宣称成功。**

## 服务器预检

- 服务器配置：`GPU_SSH_378`。
- GPU：NVIDIA GeForce RTX 5090，显存 `32607 MiB`，启动前占用 `0 MiB`。
- 数据盘：总量 `100 GiB`，已用 `58 GiB`，剩余 `43 GiB`。
- 依赖导入：`torch`、`transformers`、`swanlab`、`pandas` 通过，CUDA 可用。
- SwanLab：`ping` 和 `verify` 均通过，在线模式，账号为 `mortiswang`。
- 三份冻结物理数据和角色清单的远端摘要与配置逐项一致。

## 白名单同步

仅同步以下四个文件，未同步测试、QUIC 工具或其他生产文件：

| 文件 | 本地与远端 SHA-256 |
| --- | --- |
| `src/flow_probe/r2_final_physics_fit.py` | `0e509825bf79bb8c3d10296cf0b56828882a53dcf49f7bd1d614f355f196a2fb` |
| `src/flow_probe/r2_final_experts.py` | `3b1d53da100cb5d9a02f504da69dd9976c720af2cbfe68fe3b14e23c610f8465` |
| `src/flow_probe/r2_final_distilbert_probe.py` | `321a41dd5f1f38e9ef15ffbabef0fc5d35bb7012114be86470c57fccda0fbdff` |
| `configs/r2_final_physics_fit_seed42_identifiable_v2.yaml` | `6423c42334f71052e9b696c63f8e589a6d783f41429e474a04c0bdf418b7cc42` |

同步收据：`thesis/experiments/llm_probe/runs/sync-receipts/r2-tcp-identifiable-v2-20260805T120944Z/sha256-receipt.json`。

## 物理专家拟合

- screen 身份：`r2idphys-s42-20260805T120944Z`。
- 启动器目录：`runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42_identifiable_v2-20260805T155517Z/`。
- 配置：`configs/r2_final_physics_fit_seed42_identifiable_v2.yaml`。
- 输出根：`runs/r2-minimal-sidecar-physics-fit/tcp-udp-identifiable-v2-seed-42/`。
- SwanLab 运行名：`r2-id-physics-s42`。
- SwanLab 运行编号：`9ks07fe6`。
- SwanLab 地址：<https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/9ks07fe6/chart>。
- 创建时间：`2026-08-05T15:55:43Z`。
- 完成时间：`2026-08-05T15:56:11Z`。
- 状态：`FINISHED`。
- 训练步数：`300`，对应 `10` 个周期。

校准集末步关键指标：

| 指标 | 统一专家 | 路由专家 |
| --- | ---: | ---: |
| TCP 状态损失 | `0.7467964222` | `0.7466972234` |
| TCP 残差损失 | `0.0002596866` | `0.0004671489` |
| UDP 状态损失 | `0.0620225462` | `0.0662455757` |
| UDP 残差损失 | `0.0000153281` | `0.0000053850` |

以上仅证明新物理拟合完成，不替代后续 `F-A/F-P/F-S` 检测与物理联合裁决。

## 当前阻塞与恢复动作

- 阻塞：本机代理端点 `127.0.0.1:7890` 无监听，当前 SSH 与 rsync 连接被关闭。
- 已停止重复 SSH、rsync 和物理拟合；未切换旧服务器。
- 代理恢复后只执行一次：回收新检查点、绑定清单、结果、逐步指标和启动日志；核对 SHA-256；生成并同步三份全新独立探针配置；并行启动 `F-A/F-P/F-S` 各 `711` 步。
- 三组探针不得复用旧检查点、旧绑定、旧运行目录或旧结果；最终测试保持不可见。

## G3 逐簇缺测掩码修复

- 根因：部分 TCP 评价簇中 `ssthresh_start/end` 没有有效观测，旧统计实现错误地要求每个簇覆盖全部正式状态维度。
- 修复边界：只修改逐簇状态统计，使当前簇仅对实际可观测的正式维度取等权均值；完整逐维分子和分母仍保留，跨簇汇总仍要求每个正式维度的总分母大于零。
- 明确未做：没有插补缺测值，没有删除样本，没有改变数据、物理方程、G3 阈值或 QUIC 文件。
- 目标测试红灯：旧实现以“G3 逐簇状态维度缺少有效观测”失败。
- 同一目标测试绿灯：`1 passed in 0.29s`。

修复计划与测试证据见 [R2 G3 逐簇缺测掩码统计修复计划](2026-08-06-R2-G3逐簇缺测掩码统计修复计划.md)。

## v3 物理专家拟合

- 配置：`configs/r2_final_physics_fit_seed42_identifiable_g3mask_v3.yaml`。
- 配置 SHA-256：`a8f6464d2de67efaf3fcdcccbce942c49b1198d9116cd3c76f025878da830718`。
- 输出根：`runs/r2-minimal-sidecar-physics-fit/tcp-udp-identifiable-g3mask-v3-seed-42/`。
- 启动器：`runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42_identifiable_g3mask_v3-20260806T050026Z/`。
- 状态：`finished`，共 `300` 步，耗时 `25.4007` 秒。
- 检查点 SHA-256：`ce0f0be461f179b25f620bbca2c0abae8a2d372701386b3eec0c7d11a2afa188`。
- 绑定清单 SHA-256：`635c9316bd4d16a049fc203e83c08eb8063b08b1e4c1057d2449b2042a88d420`。
- SwanLab：<https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/o1a07ewu>。

## 种子 42 重跑结果

三组均使用新检查点、独立运行目录和独立 Python 进程；运行结束后服务器无残留训练进程，`screen` 为空。

| 组别 | 校准宏平均 F1 | 开发验证宏平均 F1 | 等协议状态误差 | 等协议物理残差 | 状态 |
| --- | ---: | ---: | ---: | ---: | --- |
| `F-A` | `0.9784164643` | `0.9290136074` | `2.3709683459` | `0.0002671317` | `finished` |
| `F-P` | `0.9891466336` | `0.9090792054` | `2.4196148250` | `0.0002418273` | `finished` |
| `F-S` | `0.9858721253` | `0.8953336192` | `2.4187464217` | `0.0002421213` | `finished` |

- 服务器结果根：`runs/r2-minimal-sidecar-probe/distilbert-identifiable-g3mask-v3/seed-42/`。
- `F-A` 启动器：`runs/launchers/r2-final-sidecar/r2_final_distilbert_f-a_seed42-20260806T051757Z/`。
- `F-P` 启动器：`runs/launchers/r2-final-sidecar/r2_final_distilbert_f-p_seed42-20260806T051757Z/`。
- `F-S` 启动器：`runs/launchers/r2-final-sidecar/r2_final_distilbert_f-s_seed42-20260806T051757Z/`。
- `F-A` SwanLab：<https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/ovrczbp1>。
- `F-P` SwanLab：<https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/wir92njk>。
- `F-S` SwanLab：<https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/uouy4hvh>。

## 科学裁决

1. `F-P > F-S` 表明真实物理旁路优于置换旁路，当前结构并非完全依赖容量或随机正则化。
2. `F-P < F-A` 表明真实物理旁路仍损害开发验证检测性能，不能冻结为当前第三章方法。
3. 物理残差改善仅为 `9.47%`，低于 `10%` 门槛；状态误差没有改善，因此 G3 明确失败。
4. 校准集上的优势没有迁移到开发验证集，不能用校准结果替代开发验证裁决。
5. 本轮应保留为有效失败证据。下一步若继续 PINN 路线，必须形成计算图实质不同的新合同，例如已登记的“发送者级特权物理教师到网络级学生蒸馏与零初始化门控残差”，不能只调权重或补种子。
