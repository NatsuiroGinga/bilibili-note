# DEDALE 2.0 解压过程笔记

## 输入

- 压缩包：`/root/autodl-tmp/thesis/datasets/DEDALE-2.0/zeek_orange_dmz.zip`
- 官方大小：415,796,741 字节。
- 官方 MD5：`c9eeda34bb1a1591fe1874718bdc192f`。
- 目标目录：`/root/autodl-tmp/thesis/datasets/DEDALE-2.0/extracted/zeek_orange_dmz/`

## 审计记录

### 压缩包预检

- 时间：2026-07-20 11:15:20。
- MD5：`c9eeda34bb1a1591fe1874718bdc192f`，与官方值一致。
- 成员总数：511；文件 482 个，目录 29 个。
- 全部成员解压总量：2,049,968,557 字节。
- 成员压缩总量：415,672,437 字节；膨胀比 4.931692。
- 唯一顶层目录：`orange_dmz/`。
- 文件类型：454 个 `.log`，28 个 `.csv`。
- 路径安全检查通过：没有绝对路径、`..`、反斜线路径、重复成员、符号链接、特殊文件或空文件名。
- `ZipFile.testzip()` 未发现损坏成员；`unzip -t` 返回全部成员正常。
- 目标目录不存在，满足不覆盖条件。

### 磁盘预检

- 解压前可用：27,310,256,128 字节。
- 预计解压后可用：25,260,287,571 字节，约 23.52 GiB。
- 强制保留下限：16,106,127,360 字节，即 15 GiB。
- 磁盘门槛通过；预计余量高于下限约 8.52 GiB。

### 标签入口

- 压缩包内包含 D1 至 D28 共 28 个逐日 `conn_labeled.csv`。
- 文件路径模式：`orange_dmz/D{日序}_{日期}_output_orange_dmz_Zeek/conn_labeled.csv`。
- 后续先读取字段和标签分布，不把原始标签文件直接混入平衡训练集。

### 首次解压失败

- 时间：2026-07-20 11:21:01。
- 原因：服务器没有 `/usr/bin/time`，解压命令退出码为 127。
- 失败发生在 `unzip` 启动前，没有写入任何 ZIP 成员。
- 原始压缩包未修改；只留下本次创建的空目标目录和 `zeek_orange_dmz.extract.log`。

### 解压与验收

- 空目标目录经 `fdfind` 确认为 0 个成员后使用 `rmdir` 删除。
- 重试使用独立日志 `zeek_orange_dmz.extract.retry1.log`，未覆盖首次失败日志。
- `unzip -n` 成功退出，实际解压耗时 11 秒。
- 重试后的首轮自检存在 NUL 字节计数转义错误，误报文件数为 0；文件字节总量已正确等于 2,049,968,557。
- 修正后的只读验收结果：482 个文件、29 个目录、454 个 `.log`、28 个 `.csv`，全部 CSV 均为 `conn_labeled.csv`。
- 原始 ZIP MD5 保持 `c9eeda34bb1a1591fe1874718bdc192f`。
- 解压后可用空间为 25,116,110,848 字节，约 23.39 GiB，高于 15 GiB 下限。
- 目标目录：`/root/autodl-tmp/thesis/datasets/DEDALE-2.0/extracted/zeek_orange_dmz/`。
- ZIP 顶层目录原样保留，因此数据实际位于目标目录下的 `orange_dmz/`。

### 字段与标签审计

- 数据包含 18 类 Zeek 日志和 1 类已标注 CSV；共 454 个 `.log` 和 28 个 `conn_labeled.csv`。
- 18 类 Zeek 日志在其各日文件中的 `#fields` 与 `#types` 均只有 1 个版本，没有发现跨日字段漂移。
- 28 个 `conn_labeled.csv` 的表头完全一致，每个文件与同目录 `conn.log` 的数据行数严格相等。
- 已标注连接流共 839,054 条，其中标签 0 为 839,052 条、标签 1 为 2 条、标签 2 为 0 条；不存在非法标签。
- 两条攻击流均位于 D15，分别是恶意邮件发送和感染邮件拉取，均属于 `initial_access`、`TA0001`、`T1566`。
- 数据时间范围为 2024-12-23 00:00:21.753051043 至 2025-01-19 23:59:51.335328102。
- 部分长连接的结束记录跨越目录日期边界，正式划分必须同时保留目录日序和 `ts`，不能仅按文件夹日期推断连接时间。
- 首轮字段审计的表名打印因 `printf` 格式串以连字符开头而报错，未影响统计。修正后已进行精简只读复核，表名与字段对应关系正常。

### 资源与运行边界

- 最终数据盘总量 53,687,091,200 字节，已用 28,590,817,280 字节，可用 25,096,273,920 字节，占用率 54%。
- 可用空间约 23.37 GiB，未触发 80% 或 10 GiB 告警门槛。
- 审计时服务器存在 1 个既有 GPU 计算进程；本任务仅使用 CPU 和磁盘读取，没有启动、停止或干扰该进程。
- 首次失败日志：`/root/autodl-tmp/thesis/datasets/DEDALE-2.0/zeek_orange_dmz.extract.log`。
- 成功重试日志：`/root/autodl-tmp/thesis/datasets/DEDALE-2.0/zeek_orange_dmz.extract.retry1.log`。
