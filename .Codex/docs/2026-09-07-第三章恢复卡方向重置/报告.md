# 第三章恢复卡方向重置报告

## 修改

- 仅修改 `RWKV第三章恢复卡.md:5-16`，新增“2026-09-07 方向重置”摘要和统一候选台账指针。
- 将旧 FT、ETA-BER、跨年和四格正文降为历史证据，不删除原始段落；旧“第三章定案”唯一答案页指针改为只读追溯用途。
- 登记 DRIFT revision `3b31077020cd1c013d0a75cad51042a2327c4521`、临时路径 `/tmp/drift-probe-data`、探针仅静态检查状态、机制审查与文献阶段结论。

## 未修改

- 未修改路线总控、论文、代码、配置、长 Goal、候选台账或历史正文。

## 验证

- `/tmp/drift-probe-data` 实测 `939M`，含八份 T17/T25 Parquet；摘要明确其临时性。
- 探针配置固定上述 revision 与输入根，正式抽样半宽为 `null`；脚本仅允许 `--pilot-domains` 测吞吐，未冻结半宽时机械拒绝正式抽样。
- 新摘要的链接、状态词和引用范围已检查：不含“实验完成”，DRIFT 引用仅写 `notes.md` 已落盘的 `A=0/B=0` 阶段结论。
- `git diff --check` 通过。

## 剩余风险

- 探针尚无真实结果，正式每分面精度/成本、数据资格和机制存废均待后续真实 `pilot` 与分析。
- `/tmp/drift-probe-data` 可被清理，后续运行前须重新核验八份输入与 revision，不能以本卡替代持久制品清单。
