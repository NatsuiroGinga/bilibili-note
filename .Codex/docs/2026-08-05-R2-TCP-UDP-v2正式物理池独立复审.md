# R2 TCP/UDP v2 正式物理池独立复审

## 裁决

**通过。严重问题 0，重要问题 0，次要问题 0。**

本次复审确认，`r2-final-tcp-udp-physics-v2` 已正确绑定固定 attempt4 输入，开发物理池未混入 `test` 或 `unseen-configuration` 的 CSV 行，TCP 发送者级真值、UDP 固定守恒、四窗口序列、独立运行统计簇以及模式、顺序、语义摘要均与发布清单一致。该制品可以进入模型角色清单、拟合配置和加载器绑定阶段。

本裁决只放行 TCP/UDP v2 正式物理池，不证明 R2 检测性能提升，不放行 QUIC 正式专家，也不替代后续 `G0`、模型运行时门禁和 GPU 正式实验裁决。

## 复审范围

- 配置：`thesis/experiments/llm_probe/configs/r2_final_tcp_udp_physics_v2_materialization.yaml`
- 入口：`thesis/experiments/llm_probe/scripts/r2_final_tcp_udp_physics_v2_materialize.py`
- 发布池：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-tcp-udp-physics-v2/`
- 固定输入：`thesis/experiments/llm_probe/runs/ns3-data/r2-protocol-dynamics-v2-formal-attempt4-event-pairing-v1/`
- 实施报告：`.Codex/docs/2026-08-05-R2-TCP-UDP-v2正式物理池物化报告.md`

复审全程只在本机执行，只读访问生产代码和数据；未连接服务器，未修改生产代码或数据，未运行 TDD、pytest、格式化、Prettier、Pyright、Git 差异或全仓检查。

## 证据

### 1. 来源锁与逐运行收据

- 物化配置 SHA-256 为 `d14f826fb9bb8d3c4ac0bdfcdf4eb6c92560fadce8c90668fa82a294bf69ab71`，与发布 `source-lock.json` 一致。
- 物化入口 SHA-256 为 `33a342feeadad8b276c18094f63b32daa4d0448336719ddbe2aa6068d0a64863`，与实施报告一致。
- 冻结 ns-3 清单、矩阵状态、矩阵摘要、attempt4 来源锁和 attempt4 制品清单的实际 SHA-256 均与配置逐项一致。
- 独立枚举确认 `receipt.json`、`main.csv`、`tcp-sender-windows.csv` 各 512 份，共 1,536 个输入文件。
- 独立重算全部 1,536 个文件摘要，并逐一对照 attempt4 制品清单；不一致数为 0。
- 独立解析 512 份收据，核对运行序号、协议、物理组、场景源码摘要、TCP 合同摘要和两个 CSV 摘要；不一致数为 0。
- 固定运行序号完整覆盖 `0000..0511`，没有缺失或重复。

### 2. 开发与保留划分隔离

冻结清单的运行分布为：

| 划分 | 运行数 | 发布池处理 |
| --- | ---: | --- |
| `train-fit` | 256 | 纳入 |
| `calibration` | 64 | 纳入 |
| `validation` | 64 | 纳入 |
| `test` | 64 | 仅核验文件摘要，不读取 CSV 行 |
| `unseen-configuration` | 64 | 仅核验文件摘要，不读取 CSV 行 |

入口在逐运行收据和文件摘要核验后、CSV 正文加载前跳过两个保留划分。独立读取全部发布行后确认：

- 发布运行数为 384，保留运行数为 128；
- 三份 Parquet 和序列清单仅包含 `train-fit`、`calibration`、`validation`；
- 发布行未出现保留运行序号；
- 所有发布行均为 `review_pending` 且 `final_test_visible=false`；
- 入口只绑定 ns-3 attempt4 和冻结 ns-3 清单，不含公开检测数据集或公开最终测试路径。

### 3. TCP 发送者级真值与掩码

- `tcp-sender-truth-v2.parquet` 共 184,320 行，与 192 个开发 TCP 运行、每运行 120 个窗口及冻结发送者数量计算结果一致。
- 复合主键 `(run_stable_order, source_window_index, sender_index)` 全部唯一，无缺失和重复。
- 每条 TCP 行均能回指同一运行的四窗口序列和独立运行簇。
- 拥塞窗口、慢启动阈值、在途字节、确认字节、确认报文段、丢失事件和超时事件的观察掩码均与空值状态一致。
- RTT 掩码、样本数和均值/最小值/最大值一致；未观察时样本数为 0 且三个统计量为空。
- 每条 TCP 行的源 CSV 摘要和源收据摘要均与对应 attempt4 运行一致。
- 动力学真值保持发送者级，没有在进入 TCP 方程前跨发送者平均。

### 4. UDP 固定守恒

- `udp-window-truth-v2.parquet` 共 23,040 行，全部来自 192 个开发 UDP 运行。
- 所有行都满足包守恒：`计划包数 = 实际发送包数 + 应用丢弃包数`。
- 所有行都满足字节守恒：`计划字节数 = 实际发送字节数 + 应用丢弃字节数`。
- 两个固定守恒残差在全部 23,040 行上均为 0。
- UDP 适用和观察掩码全部为真；积压不适用时，起止积压值均为空，掩码与值一致。
- 每条 UDP 行的源主 CSV 摘要和源收据摘要均与对应 attempt4 运行一致。

### 5. 四窗口序列与独立运行簇

- `physics-targets-ns3-v2.parquet` 共 46,080 行，384 个开发运行各 120 行。
- `(run_stable_order, source_window_index)` 主键全部唯一。
- `ns3-sequence-manifest-v2.jsonl` 共 11,520 条唯一序列，每条序列严格包含连续 4 个窗口。
- 统计簇共 384 个，每个簇对应一个独立仿真运行，并包含 30 条四窗口序列。
- `evaluation_cluster_id` 等于“物理组 + 协议族”的独立运行身份；同一物理条件下的 TCP 和 UDP 运行不会被误合并为一个独立重复。
- 序列清单中的 11,520 个 `sequence_id` 与窗口表完全一致。

### 6. 模式、顺序、语义摘要与原子发布

- 独立流式读取三份 Parquet，重算模式、逐行语义和稳定顺序 SHA-256，全部与 `input-summary.json` 和 `artifact-manifest.json` 一致。
- 独立重算序列 JSONL 的文件、语义和顺序 SHA-256，全部一致。
- 独立重算清单内全部发布文件 SHA-256，不一致数为 0。
- 独立重算非自引用制品列表的 Merkle 摘要，结果为 `cd55f6fb82c03f8b68f00b9e3224e6ff814b9ac7fe7ac7652cf29dd5a43a5a3e`，与发布清单一致。
- 正式目录不存在 `_INCOMPLETE.json`，活动 `.partial` 目录不存在。
- 入口先在同文件系统阶段目录写完全部制品、删除未完成标记，再通过不覆盖的 `os.rename` 发布；正式状态记录 `materialization_completed=true` 和 `same_filesystem_rename_no_overwrite`。

## 独立复算结果

| 项目 | 结果 |
| --- | ---: |
| 已核验收据 | 512 |
| 已核验输入文件 | 1,536 |
| 开发运行 | 384 |
| 保留运行 | 128 |
| 公共窗口 | 46,080 |
| TCP 发送者窗口 | 184,320 |
| UDP 窗口 | 23,040 |
| 四窗口序列 | 11,520 |
| 独立运行簇 | 384 |
| 独立审计错误 | 0 |

## 后续边界

1. 下一步可以把已经放行的检测开发六视图与本物理池绑定到模型角色清单、三个拟合配置和加载器。
2. 绑定时必须使用本报告复核的精确文件摘要，并显式加载发送者级 TCP 伴随表；不得回退旧聚合 TCP 字段或旧 attempt5 输入。
3. QUIC 仍保持非正式、训练权重为 0 和 `UNKNOWN` 回退，不能用本裁决宣称三协议专家已齐备。
4. 角色清单、参数预算、梯度隔离、最终测试读取前拒绝、检查点和单组单进程收据完成后，仍需独立复核并执行 `G0`。
