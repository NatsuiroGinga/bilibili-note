# 子代理派发 Hook 实施报告

## 目标

在 Codex 创建子代理前，以确定性规则核验名称、模型、推理强度和简报最小内容。

## 计划

- [x] 核验指定工作树、现有 Hook 配置和 Codex 原生工具匹配能力。
- [x] 实现 `agent_dispatch_guard.py` 并确认原生 Hook 注册边界。
- [x] 使用合法与非法 JSON 载荷验证，并写入收据。

## 文件范围

- `.Codex/hooks/agent_dispatch_guard.py`
- `.Codex/hooks/tests/test_agent_dispatch_guard.py`
- `.Codex/hooks.json`（只读核验，未改动）
- `.Codex/docs/2026-09-07-子代理派发Hook/报告.md`
- `.Codex/docs/agents-split/receipts/agent-dispatch-policy-hook.json`

## 已冻结决策

官方文档记载 `Agent` 可能匹配 `spawn_agent`，但本会话没有捕获一次真实派发的 Hook 输入，不能以文档替代本机实测。因此本项目采用主代理在派发前显式调用的单入口 `--preflight`，不注册或宣称自动拦截。

任务名称使用带版本的模型标识：`<domain>_<specific_duty>_<model-tag>_<effort>`。当前允许 `gpt6_sol`→`gpt-6-sol`、`gpt6_luna`→`gpt-6-luna`、`gpt6_astra`→`gpt-6-astra`，以及机械任务 `gpt5_6_terra`→`gpt-5.6-terra`。例如 `ch3_dual_risk_contract_gpt6_sol_high`。无版本后缀 `sol/terra/luna/astra` 明确拒绝；当前角色默认规则不允许把旧版 `gpt-5.6-sol/luna` 当作兼容派发选项，名称中的模型标识还必须与实际 `model` 参数完全对应。

## 实现内容

- `.Codex/hooks/agent_dispatch_guard.py --preflight` 从标准输入读取派发 JSON，以退出码 `0` 放行、`2` 拒绝，并输出机器可读 JSON。
- 对所有子代理硬校验名称、模型、推理强度、唯一绝对工作树、预期分支及首个 `pwd`/`git branch --show-current` 核验。
- 仅当 `agent_type=worker` 或简报明确为写入任务时，才额外校验文件所有权、责任所有权、非独占工作树和不得回滚他人改动。
- 子代理名称只接受当前角色模型的带版本标识，并校验该标识与实际模型参数的精确映射；保留推理强度后缀与 `reasoning_effort` 一致性校验。
- 不重复编码代理 Skill 收据合同：`required_skills` 与收据仍由既有收据验证器按生产代码任务处理。

## 验证

| 命令或场景 | 结果 |
| --- | --- |
| `/opt/miniconda3/envs/rwkv/bin/python -m py_compile .Codex/hooks/agent_dispatch_guard.py` | 通过 |
| `/opt/miniconda3/envs/rwkv/bin/python -m json.tool .Codex/hooks.json` | 通过 |
| `git diff --check -- .Codex/hooks/agent_dispatch_guard.py .Codex/hooks.json` | 通过 |
| 合法非写入 Sol `Agent` JSON | `--preflight` 返回 `valid=true`，无收据要求 |
| 合法写入 Terra `worker` JSON | `--preflight` 返回 `valid=true` |
| 缺唯一工作树且强度不一致的非法 JSON | 退出码 `2`，返回 `ADG-EFFORT`、`ADG-WORKTREE` |
| 带版本模型名与既有简报规则定向测试 | `/opt/miniconda3/envs/rwkv/bin/python -m unittest discover -s .Codex/hooks/tests -p 'test_agent_dispatch_guard.py' -v`，6 项通过；覆盖 GPT-6 三类角色、GPT-5.6 Terra 机械任务、旧名拒绝、模型错配、强度错配，以及 worker 的共享工作树和所有权要求 |
| 当前规则下的非默认 `gpt-5.6-sol/luna` | 定向测试确认其名称标识被拒绝，不作为当前默认派发规则的兼容项 |

首次语法检查曾在第 152 行报告未闭合括号；根因是收据校验分支缺少 `violations.append` 的闭合括号。已只补充该括号并重跑全部验证。

## 局限与启用条件

- 本会话未实测 Codex 是否把 `collaboration.spawn_agent` 送入 `Agent` 匹配器，故当前唯一启用方式是派发前显式调用 `--preflight`；不得报告为自动拦截。
- 此守卫校验派发参数和简报声明，不能替代派发后的收据验收，也不能证明代理实际执行了简报。
- `.Codex/hooks.json` 未保留本任务增量；其中的临时路径门禁属于另一代理，未被本任务暂存或提交。

## 状态

显式预检实现与验证已完成，收据见 `.Codex/docs/agents-split/receipts/agent-dispatch-policy-hook.json`。
