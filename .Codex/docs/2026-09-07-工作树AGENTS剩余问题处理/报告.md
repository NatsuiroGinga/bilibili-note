# 工作树 AGENTS 剩余问题处理报告

## 范围

- 工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- 修改：根 `AGENTS.md`；`thesis/experiments/llm_probe/AGENTS.md`。
- 未修改：根 `CLAUDE.md`、`thesis/AGENTS.md`、`thesis/experiments/llm_probe/scripts/AGENTS.md`。

## 处理

- 用职责分工替换旧固定分档：Sol 负责文献、计划和方案制品；Astra 基于 Sol 制品作科研裁决；Terra/Luna 负责非科研杂务与工程实现；`effort` 由主代理按任务裁定。
- 压缩根级本机资源说明和实验工程的重复合同、适配表、验证与环境说明，保留数据隔离、真实实验、凭据、运行身份、制品恢复和正式结果隔离边界。
- `CLAUDE.md` 已为薄导入且语义同步，无需编辑。

## 验证

- `wc -c` 统计：实验脚本链 `28,542` 字节（根 `13,812` + 论文 `2,684` + 实验 `10,372` + 脚本 `1,674`），较目标 `28,500` 余量 `42` 字节。
- `wc -c` 统计：RWKV 文档链 `24,644` 字节（根 `13,812` + RWKV 规则 `4,590` + 路线总控 `6,242`）；论文链 `16,496` 字节（根 + 论文规则）；知识库链 `15,515` 字节（根 + `wiki/AGENTS.md`）。
- 路由检索仅命中新的 Sol/Astra/Terra/Luna 规则；`初始分档`、固定 `low`/`medium`/`high`/`max`/`ultra` 档位均无命中。
- 根 `CLAUDE.md` 的 `@AGENTS.md` 薄导入和“唯一来源”声明仍存在；`git diff --check` 通过。
