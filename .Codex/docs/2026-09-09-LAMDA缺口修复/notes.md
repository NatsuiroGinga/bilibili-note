# LAMDA 缺口感知条件风险门控回放证据笔记

## 已有实验事实

- 阶段 C 的固定四角色记忆＋等单位风险损失在开发期将 FPR 从 `0.013148155` 升至 `0.089252353`，AP 从 `0.987242653` 降至 `0.936816545`；该具体实现已否决。
- 双风险梯度投影在开发期将 FPR 从 `0.013148155` 降至 `0.012231661`，旧恶意负向翻转率从 `0.052107226` 降至 `0.031983684`，但 FNR 从 `0.059478156` 升至 `0.070306038`、AP 从 `0.987242653` 降至 `0.985101162`。
- 翻转分解显示，始终投影减少了部分恶意负向翻转，但没有带来足够的恶意正向修复；这支持“仅在安全冲突时投影”的待验证假设。

## 文献来源边界

- A-GEM、回归感知持续学习、困难样本回放和不平衡持续学习已入库，提供方法家族来源与对照设计依据。
- 这些文献不是 LAMDA 结果；新组合在本项目中仍标记为外部候选，必须由源年真实实验验证。
- 当前网页深度研究任务 `WEB:40805147-727c-4418-ae67-b5a9044ad760` 继续在既有“🔎 LAMDA 深度研究”会话中运行；其返回内容不能替代本地实验。

## 2026-09-09 网页深度研究回收

- 报告已完成，列出 36 个公开来源；完整交接记录见 [LAMDA 定向漏判修复与 FPR 约束深度研究报告](../chatgpt-handoffs/inbox/LAMDA-2026-定向漏判修复-FPR约束深度研究报告.md)。
- 报告推荐外部组合为“定向恶意漏判回放＋PCT/Focal Distillation＋独立 FPR 约束／门控”，并把 Android 主动学习作为有人工复核预算时的后续层。
- 报告明确把 DGR、TERM、A-GEM、IWMS、DREAM 标为增强件或直接对照，不能提供 FPR 不增加的直接保证；所有外部论文数值均不得当作 LAMDA 结果。
- PCT 和 TFCO 原件尚未进入本工作树的 `raw/` 与 `wiki/`；在其全文和版本核验完成前，本计划仍先执行已批准的缺口修复／条件风险门控筛选，不将网页组合直接写入代码。

## 研究假设

1. 只按上一冻结模型的 margin 缺口加权当前恶意修复，能够减少对已正确恶意样本和良性样本的无差别推高。
2. 只在修复梯度与安全参考梯度冲突时投影，能够避免始终投影造成的过度保守。
3. 两者若分别有独立信号，联合版本可能形成可解释的统一算法；没有独立信号则不凑两个机制。

## 待验证项

- 缺口修复是否在来源／开发期改善 FNR 或当前恶意正向修复而不提高 FPR。
- 条件投影是否在保持阶段双风险投影安全信号的同时减少 AP/FNR 损失。
- 联合臂是否具有超出两个单臂的互补收益。
