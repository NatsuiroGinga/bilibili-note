# 任务十四 G0–G5 同预算裁决过程笔记

## 当前状态

- 日期：2026-07-22
- 阶段：G2/G4 已完成，G3 正式训练运行，G5 工程实现进行中
- 正式训练：G2、G4 完成；G3 已启动
- GPU：G3 单进程运行

## 已冻结决定

- G0/G1 优先审计复用任务十一 S1/S2，不无意义重复训练。
- G2/G3 分别采用对称与固定非对称合作议价。
- G4 采用物理可靠性、生成 0.95 一阶保护线和动态辅助参与。
- G5 必须先通过独立协调变量与非零反应项数学门禁。
- 正式预算为 202 次有效更新，探索性冒烟为 2 步，最多两个训练进程并行。

## 错误记录

- 子代理任务简报提取脚本只识别 `Task N` 英文标题，无法识别强制中文计划中的“任务 N”。处理方式是不修改中文计划标题，改用 `.Codex/docs/sdd/` 下的独立中文简报。
- 服务器首次最小目标测试为 25 通过、1 失败。失败节点是 `test_g4_can_keep_both_auxiliaries_when_all_directions_are_aligned`：完全同向 Gram 下候选效用数学并列，但协调器比较键直接使用原始浮点数，舍入噪声覆盖了确定性并列排序。修复范围限定为比较键的 `1e-12` 容差量化；不修改 G4 公式、0.95 生成底线或测试预期。修复后只重跑该精确失败节点。

## 下一检查点

1. G0/G1 协议复用审计结论。
2. G2–G4 实现文件与最小测试范围。
3. 服务器同步、目标测试和 2 步冒烟结果。

## 服务器预检

- GPU：无计算进程。
- `screen`：无会话。
- 数据盘：使用 73%，可用约 14 GiB。
- 环境：PyTorch `2.13.0+cu130`、Transformers `4.57.6`、SwanLab `0.9.0`，CUDA 可用。

## G2–G4 门禁与运行

- 五个实现文件通过白名单 `rsync` 同步，本机与服务器 SHA-256 逐项一致。
- 服务器首次最小目标测试为 25 通过、1 失败；修复浮点并列排序后，精确失败节点 1/1 通过，直接受影响的协调器测试 14/14 通过。未重复运行无关测试。
- G2 两步冒烟：退出码 0，优化器更新 2 次，逐步指标 2 行，SwanLab `9lp0u2hq`，制品状态 `finished`。
- G4 两步冒烟：退出码 0，优化器更新 2 次，逐步指标 2 行，SwanLab `lznkllel`，平均生成边际 `0.960288`，制品状态 `finished`。
- G2 正式目录：`runs/game-coordination/qwen3-1.7b-seed42-g2-nash202-v1/`。
- G4 正式目录：`runs/game-coordination/qwen3-1.7b-seed42-g4-reliability-protected202-v1/`。
- G2 与 G4 均完成 202/202 步并以退出码 0 正常结束；SwanLab 分别为 `o3d0kdgm` 和 `fx5bygz7`。
- G2 正式结果：生成验证损失 `0.060874`、未观测状态误差 `0.516429`、物理残差 `0.003763`；相对 G0 的生成门槛失败，未观测状态改善不足 10%。
- G4 正式结果：生成验证损失 `0.018361`、未观测状态误差 `0.516625`、物理残差 `0.000989`；生成和物理门槛通过，未观测状态相对 G0 只改善约 8.73%，尚未达到 10%。
- G4 在 202 步中触发生成否决 176 次，拒绝状态 157 次、拒绝物理 52 次；平均共享权重为生成 `0.9632`、状态 `0.0092`、物理 `0.0276`。
- G2/G4 已观测状态误差分别升至 `0.403531` 与 `0.509243`，需要核对私有状态头受共享目标拒绝控制是否造成锚点监督不足；当前结果不据此冻结算法。
- G3 正式目录：`runs/game-coordination/qwen3-1.7b-seed42-g3-asymmetric-nash202-v1/`，已启动。
- G5 数学门禁通过，工程实现已分派，与 G3 训练并行。

## G4R 修正版决定

- G3 已完成 202/202 步并正常退出；GPU 已空闲。
- 新增 `G4R`，不覆盖原始 G4。共享 LoRA 协调保持完全相同，只让私有状态头持续接受 `q0` 锚点梯度。
- 物理私有梯度仍服从原 G4 的物理接受状态与可靠性缩放。
- G4R 通过最小测试和 2 步冒烟后立即运行 202 步，用于判断锚点误差异常是否来自私有梯度停止。
