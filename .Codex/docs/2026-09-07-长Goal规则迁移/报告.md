# 长 Goal 规则迁移报告

## 改动

- 根 `AGENTS.md:34-38` 以数据可得、未饱和强基线、真实泛化轴、严格 `2×2`、单冻结种子和多保真止损替换固定第三章合同。
- 根 `AGENTS.md:37` 规定正式入文须两个单机制各较 C00 在主分布外指标正增益、C11 优于两者、协同有预冻结交互或活动证据，且不得降低训练域性能；联合臂无第三模块。
- 根 `AGENTS.md:38` 规定每级在看结果前冻结样本、泛化轴、指标、最小效应或无效区、停止条件和最大成本，只有过门者进入下级。
- 实验规则 `AGENTS.md:17-26,38,43` 移除 LSPR、83 字段、年度等候选或数据特例，改为主体与泛化轴隔离和数据集－模型适配通用门。
- 保留根 `AGENTS.md:87-88` 的 Astra Sol 制品加直接相关真实实验制品门。

## 验证

- `wc -c`：最长实验脚本链为 `27,740` 字节（根 `14,487` + 论文 `2,684` + 实验 `8,895` + 脚本 `1,674`），距 `28,500` 目标余 `760` 字节；RWKV 链 `21,466`，正文链 `20,660`，图件链 `18,075`，原件链 `17,320`，知识库链 `16,190`，高层交付链 `15,647`，过程文档链 `16,876`，均低于 `32,768`。
- 冲突检索未命中 LSPR、FT、多种子正式实验、时间漂移、83 字段、朱论文数值、年度隔离或固定第三章规则；命中的 C00/C11、预冻结条件和 Astra 条款均为本轮规则。
- `CLAUDE.md` 保持 `@AGENTS.md` 薄导入和“唯一来源”声明；`git diff --check` 通过。

## 剩余风险

- 朱式工作量和结构参照不在长 AGENTS 中恢复；其具体依据须继续由短 Goal、恢复卡或原件承担，且不得与本规则冲突。
