# R2 F-A 检查点绑定名称遮蔽修复报告

日期：2026-08-05

## 结论

`_physics_system` 已恢复对导入函数 `checkpoint_binding(...)` 的调用。检查点内同名载荷局部变量改为 `checkpoint_binding_payload`，绑定字段、比较、摘要算法和返回结构均未改变。

## 修改文件

- `thesis/experiments/llm_probe/src/flow_probe/r2_final_distilbert_probe.py`
  - 将后段检查点载荷局部变量及其三处直接引用从 `checkpoint_binding` 重命名为 `checkpoint_binding_payload`。
- `thesis/experiments/llm_probe/tests/test_r2_final_distilbert_probe.py`
  - 新增最小回归测试，仅验证前段能解析并调用导入的 `checkpoint_binding`。
- `.Codex/docs/2026-08-05-R2-F-A检查点绑定名称遮蔽修复报告.md`
  - 本报告。

## 红灯证据

执行命令：

```bash
uv run pytest tests/test_r2_final_distilbert_probe.py::test_physics_system_resolves_imported_checkpoint_binding_before_payload
```

结果：失败，1 项失败。

关键错误：

```text
src/flow_probe/r2_final_distilbert_probe.py:902: UnboundLocalError
local variable 'checkpoint_binding' referenced before assignment
```

该错误在测试替换后的导入绑定函数被调用前发生，符合“后段同名局部赋值导致整个函数内名称被判为局部”的根因。

## 绿灯证据

执行命令：

```bash
uv run pytest tests/test_r2_final_distilbert_probe.py::test_physics_system_resolves_imported_checkpoint_binding_before_payload
```

结果：通过，`1 passed in 0.89s`。

## 源码摘要

`thesis/experiments/llm_probe/src/flow_probe/r2_final_distilbert_probe.py`

```text
7da76322aa4918bbdb0f8c918566763fd5f906b5fe9cf25f335955a6edbf36cc
```

## 未改边界

- 未修改导入函数、检查点字段、绑定清单、配置、数据、包装器、物理公式或训练参数。
- 未运行完整测试集、格式化、静态检查、真实导入预检、服务器命令或实验。
- 未提交 Git。

## 风险

本次验证仅覆盖名称解析与导入绑定函数的到达路径，不替代真实检查点、数据和 GPU 正式运行的后续门禁验证。
