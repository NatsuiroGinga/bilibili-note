# 混合检索漏用防复发计划

## 范围

- 修改根 `AGENTS.md`、`literature-hybrid-search` 技能、检索 README、`scripts/literature_search/cli.py` 与本目录过程文档。
- 生产代码只增加陈旧索引拒绝门，不改检索、索引或嵌入逻辑。

## 步骤

1. 已完成：读取规则、技能、README、提交 `6b31183`、`a695368` 与全项目架构报告。
2. 已完成：定位根因，`query` 仅拒绝缺失索引，未检查 `stale=true`。
3. 已完成：在本地查询入口增加陈旧索引拒绝，并固化研究判断路由规则。
4. 进行中：运行语法、帮助与当前真实 CLI 状态检查，检查关键词、链接和 Git 差异；仅提交本任务文件。

## 验收

- 规则覆盖数据集适配、机制、路线、跨文档实验、项目综合与文献依据。
- 本地 `query` 在陈旧索引时拒绝并提供原因和构建命令；`online` 不依赖本地索引。
- 明确 `rg` 的精确核验边界、词法降级披露、最小收据、架构验证和全量物化的区别，并隔离 `real-user-query-v1`。
