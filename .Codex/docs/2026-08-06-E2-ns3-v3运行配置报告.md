# E2 ns-3 v3 运行配置报告

## 结论

- 五个冻结 TCP 字段闭合运行配置已经生成，可在生产源码与服务器双副本哈希均为 `0123538bb10eba6a69e5057fdd1d3c817a457ede019efc8b4111b9c614311617` 时启动。
- 一个冻结 UDP 配对行已经从原始 512 行清单按原位置选择，独立输出目录和运行参数已经生成。当前生产加载器仍只接受五个 TCP 闭合项，因此 UDP 配置须等待通用单项选择入口完成后启动。
- 512 组正式配置仅生成不可启动的审计模板。五个 TCP 真实运行尚未产生 `field-closure-summary.json`，因此没有伪造 `expected_field_closure_summary_sha256`。真实汇总哈希产生后，必须原子生成正式 JSON，再做一次加载验证。

## 新增文件

1. `thesis/experiments/llm_probe/configs/r2_ns3_tcp_truth_v2_directional_runtime_v3.json`
   - 选择：沿用冻结的五个 TCP 字段闭合项及其原顺序。
   - 输出：`runs/ns3-data/r2-tcp-truth-v3-directional-field-closure-attempt1`。
   - 续跑来源：`null`。
   - 配置 SHA-256：`fb34a4674a6e54db9a97b2814c1f1d4fa0d32a7ecc58f92d5bf94e0d35b6b8ef`。

2. `thesis/experiments/llm_probe/configs/r2_ns3_udp_truth_v2_directional_field_closure_v3.json`
   - 选择：冻结清单索引 `1` 的 UDP 行。
   - 物理组：`dd83746a955899c9b5c5db2d074e65e0059a73424803090b0cc83f932b550739`。
   - 场景：低丢包、低时延、稳定容量、`8+0` 发送者组成。
   - 配置 SHA-256：`922d90d34f5d7c9933e7636ca25f1c3ddfad0fce541712e381bdf639e7d6c5d5`。

3. `thesis/experiments/llm_probe/configs/r2_ns3_udp_truth_v2_directional_runtime_v3.json`
   - 输出：`runs/ns3-data/r2-udp-truth-v3-directional-field-closure-attempt1`。
   - 续跑来源：`null`。
   - 配置 SHA-256：`f58ce5550a47782a6747cf657a853e8ccfe139f5dac75e419dca1eff38ec93e7`。

4. `thesis/experiments/llm_probe/configs/r2_ns3_protocol_dynamics_v3_parallel_runtime.template.json`
   - 固定输出：`runs/ns3-data/r2-protocol-dynamics-v3-formal-attempt1`。
   - 并发数：`24`。
   - 续跑来源：空列表，禁止复用旧 v2 输出。
   - 冻结清单 SHA-256：`d867c05013ff9c566d965ec3115bcefe6c905cca69865b042eb81f56e012a111`。
   - 模板 SHA-256：`98549d209922b870d38ec37c9ff02b958888d745f1de26f9a53fb0a2882492bd`。
   - 该文件的模式明确标为模板，且缺少正式加载器要求的闭合汇总哈希，不能误启动。

## 验证

- 四份新增 JSON 均完成标准 JSON 解析。
- 冻结清单重新读取为恰好 `512` 行，未修改清单、种子、参数和顺序。
- 五个 TCP 选择项均能在冻结清单中找到同物理组 TCP 行。
- UDP 选择项与冻结清单索引 `1` 的完整原始行逐字段一致。
- TCP 配置的键集合、参数模式、输出目录、空续跑来源和全部本地输入哈希均与当前 v3 加载接口一致。
- 512 模板已核对固定输出目录、`24` 并发、空续跑来源及冻结清单哈希。
- 尝试直接导入现有加载器时，本机缺少 `PyYAML`，导入在运行任何配置逻辑前停止；没有安装依赖，也没有把环境缺依赖误记为配置失败。

## 待办与阻塞

1. 同步生产源码和 TCP v3 配置后，在服务器运行五个 TCP 字段闭合验收。
2. 生产加载器补齐“冻结清单单个 UDP 行”入口后，运行 UDP 六字段验收。
3. TCP 验收完成后读取 `field-closure-summary.json` 的 SHA-256，原子生成正式 512 配置；正式文件不得保留模板模式或任何占位值。
4. TCP 与 UDP 两类真实验收均通过后，才允许启动 512 组正式重跑。

## 未执行

- 未运行本机测试、格式化、SSH、远程同步或 ns-3 仿真。
- 未修改生产源码、旧配置、冻结清单或任何既有实验制品。

## 五个 TCP 通过后的正式配置

五个 TCP 真实字段闭合通过后，已经新增正式配置：

- 路径：`thesis/experiments/llm_probe/configs/r2_ns3_protocol_dynamics_v3_parallel_runtime.json`
- 配置 SHA-256：`71928a1f9eda97a3a1e2aa6a349953f771745e39464718647ddce675fb8da9c3`
- 真实闭合摘要 SHA-256：`fa3e87c84fe108ea19e305ae8dc96987d56c4eb742e03d77853a6713c8becc10`
- 冻结清单 SHA-256：`d867c05013ff9c566d965ec3115bcefe6c905cca69865b042eb81f56e012a111`
- 输出：`runs/ns3-data/r2-protocol-dynamics-v3-formal-attempt1`
- 并发：`24`
- 续跑来源：空列表

现有加载器的 JSON 读取函数、键集合和模式常量验证均通过；本地清单、场景源码和合同哈希也与配置一致。审计模板仍原样保留，没有被正式配置覆盖。本次只生成并验证配置，没有启动 512 组运行。
