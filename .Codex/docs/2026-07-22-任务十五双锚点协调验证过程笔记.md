# 任务十五：双锚点物理协调验证过程笔记

## 当前状态

- 日期：2026-07-22
- 阶段：202 步正式运行与制品验收均已完成；四项门槛通过两项、失败两项，机制裁决为失败
- GPU：正式运行正常结束，`screen` 已退出，GPU 显存已释放到 `0 MiB`
- 实验别名：`G4R-A2`
- 命令变体：`G4R`

## 已冻结决定

- 只把状态监督模式从 `anchor0_only` 改为 `anchor0_plus_one`。
- 除放开 `G4R` 双锚点输入契约并修正掩码制品元数据外，不修改训练算法、物理公式、协调器或评价协议。
- 以 S3 为同监督基线，执行四项联合门槛。
- 本轮整体失败，不命名最终算法，也不把未通过的博弈协调包装为支柱。

## 运行记录

### 配置同步与预检

- 本机与服务器配置 SHA-256：`d528f875977766731e51f2b53797c124c4d14e8f7011e230946126f2ced9be64`。
- 本轮同步的三个文件已逐项核验，本机与服务器 SHA-256 一致。
- 服务端解析结果：`state_supervision_mode=anchor0_plus_one`、`seed=42`、`tracking.mode=online`。
- 启动前服务器无 GPU 计算进程、无 `screen` 会话；数据盘使用率 74%，剩余约 14 GiB。

### 首次两步冒烟

- 计划输出：`runs/game-coordination/qwen3-1.7b-seed42-g4r-a2-dual-anchor-smoke2-v1`。
- 外部日志：服务端 `runs/game-coordination/verification-20260722-task15/g4r-a2-smoke2.log`。
- 退出码：`1`。
- 失败位置：`game_train.py::_build_settings`。
- 错误：协调训练入口将所有 G2 至 G5 变体统一限制为 `anchor0_only`，因此主动拒绝 `G4R + anchor0_plus_one`。
- 裁决：该运行在模型加载和训练前退出，未产生参数更新、验证指标或可比较实验结果，不占用正式运行编号。

### 最小契约修复

- 修复报告：[任务十五双锚点最小契约修复报告](sdd/task-15-dual-anchor-contract-fix-report.md)。
- 仅允许 `G4R` 使用 `anchor0_only` 或 `anchor0_plus_one`；其他协调变体继续固定为单锚点。
- `state_mask.json` 改为记录运行时实际监督模式。
- 未修改协调算法、物理公式、训练预算和评价协议。
- 最小修复已通过独立审查，未发现阻塞正式运行的问题。

### 服务器精确测试

- 服务端精确测试实际结果为 `8 passed in 1.25s`，退出码 `0`。
- 测试通过后才执行第二次两步冒烟，没有跳过契约门禁。

### 第二次两步冒烟

- 输出目录：`runs/game-coordination/qwen3-1.7b-seed42-g4r-a2-dual-anchor-smoke2-v2`。
- 退出码：`0`；完成 2 个训练步和 2 次模型更新。
- `private_state_anchor_forced` 累计值为 `2`。
- `state_mask.json` 共记录 807 个掩码；所有掩码均满足 `q0=true`，且每个掩码恰有两个真值位置，监督模式为 `anchor0_plus_one`。
- 运行摘要状态为 `finished`，峰值显存为 `3972.684 MiB`。
- SwanLab 运行号：`mnj6n2cv`。

### 202 步正式运行

- 执行报告：[任务十五 GPU 执行报告](sdd/task-15-gpu-execution-report.md)。
- 输出目录：`runs/game-coordination/qwen3-1.7b-seed42-g4r-a2-dual-anchor202-v1`。
- 验证目录：`runs/game-coordination/verification-20260722-task15/`。
- `screen` 名称为 `task15-g4r-a2-202`，进程号为 `321305`；GPU 训练进程号为 `321314`，运行结束后正常退出。
- SwanLab 运行号：`yv5wfs11`。
- SwanLab 云端状态为 `FINISHED`；正式运行状态为 `finished`，退出码为 `0`，完成 `202 / 202` 次更新。
- 运行时间为 `485.5015` 秒，峰值显存为 `5831.782 MiB`，私有状态锚点强制次数为 `202`。

正式验证指标与固定门槛：

| 指标           |      实际值 |          门槛 | 裁决 |
| -------------- | ----------: | ------------: | ---- |
| 生成验证损失   | 0.010701656 | `<= 0.030804` | 通过 |
| 物理残差       | 0.000989265 | `<= 0.097318` | 通过 |
| 未观测状态误差 | 0.517984573 | `<= 0.125748` | 失败 |
| 已观测状态误差 | 0.510903416 | `<= 0.101337` | 失败 |

- 总体状态误差为 `0.515381226`。
- 相对 S3，生成验证损失变化 `-63.521641%`，物理残差变化 `-99.085124%`，未观测状态误差变化 `+270.730442%`，已观测状态误差变化 `+429.373249%`。

### 正式制品验收

- `step_metrics.jsonl` 共 202 行，首尾步号为 1 和 202，优化更新与私有状态锚点强制次数均合计为 202。
- `state_mask.json` 共记录 807 个双锚点掩码，模式为 `anchor0_plus_one`；每个掩码第 0 位均为真，且恰有两个真值位置。
- 制品清单列出的 12 项路径全部存在。
- 外部日志未发现回溯、错误、异常、显存溢出或非数值信号。
- SwanLab 本地运行目录存在，云端能列出训练指标和五项验证指标序列，跟踪制品完整。

### 最终裁决

- 四项门槛必须同时满足；本轮仅通过生成与物理两项，未观测和已观测状态两项失败，因此 `G4R-A2` 机制整体失败。
- 停止锚点比例和博弈权重扫描，不运行随机种子 43、44。
- 第三章支柱 A 的双锚点有限队列 PINN 保留；支柱 B 的博弈协调方案淘汰。
- 下一阶段进入“物理状态显式连接生成表征”和“流形约束”候选的原始文献门禁与结构设计，不提前冻结新机制或算法名称。
