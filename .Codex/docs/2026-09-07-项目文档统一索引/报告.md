# 项目文档统一索引报告

## 处理

- 读取 `source-command-kb-index`、`source-command-sc-index`、`source-command-sc-index-repo` 三个技能。
- 根和 `Research/` 的 `02-Index.md` 均未绑定当前工作树，故不执行 `kb-index`，也不修改其自动区块。
- 依本仓库落点覆盖 `sc-index-repo` 的默认根输出，在 `.Codex/docs/` 创建唯一 `PROJECT_INDEX.md` 与 `PROJECT_INDEX.json`。

## 范围

- 索引包括当前权威入口、Goal 制品、进行中任务、实验代码/配置/结果、方法、文献、规则恢复和历史归档。
- 未修改 `raw/wiki` 的任何 `INDEX.md`，未读取凭据、`.env`、密钥、运行大数据或缓存。

## 验证

- `PROJECT_INDEX.md` 为 `4,702` 字节，`PROJECT_INDEX.json` 为 `4,211` 字节；Markdown 小于 `5KB` 导航目标。
- JSON 已用 `jq empty` 解析；Markdown 含 `GENERATED_START/END` 与独立“人工补充”区，后续刷新可只覆盖生成区块。
- 已核验恢复卡、统一候选台账、DRIFT 数据/机制/双探针/引用制品及探针脚本配置路径存在。
- 未发现 `.env`、`.pem`、`config.yaml`、临时数据路径或凭据键进入索引；`git diff --check` 通过。
