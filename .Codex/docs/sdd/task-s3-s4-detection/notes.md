# S3/S4 同协议正式检测验证过程笔记

## 审计事实

- S3 训练制品：`runs/physics-sparse/qwen3-1.7b-seed42-s3-anchor0-plus-one-fullphys202-v1/`，变体 M1，202 步，SwanLab `kg4roxi0`。
- S4 训练制品：`runs/physics-sparse/qwen3-1.7b-seed42-s4-anchor0-plus-one-fullphys202-v1/`，变体 M2，202 步，SwanLab `ud4aa0bm`。
- 两组 `input_sha256.json` 的 SHA-256 均为 `69855d63ddb066afc8529779e19c2b7d2cda7412c6366dbd9091baf4c24fcdb8`；`physics_sample_order.json` 均为 `fdabfc49300656b50c32205d056fe5b8a93455493ca115e785b286db1c1113ae`；状态掩码哈希均为 `6341e14d00d6e000a0fef279684c04aaa6494988233f7e8293f0552604e2ec5e`。
- 配置快照差异只有运行名、输出目录和 M1/M2 变体；两组均为种子 42、`anchor0_plus_one`、`lambda_state=1.0`、`lambda_physics=0.01`、202 步。
- S3 未观测状态误差为 `0.139720`、物理残差为 `0.108131`；S4 分别为 `0.041676` 和 `0.048182`，机制门槛已通过，但生成验证损失从 `0.029337` 恶化到 `0.042196`。
- 现有分层评估器可以直接通过 `PeftModel.from_pretrained` 加载两组 `final_adapter`；候选预测包含真实标签、候选概率、置信度和阈值，足以在评估后只读计算期望校准误差、Brier 分数、负对数似然和配对自助置信区间。因此未修改评估器。
- 既有单进程 `eval300` 峰值显存约 `3502 MiB`；双进程启动后实测每进程约 `7826 MiB`，总计约 `15.3 GiB`，低于 RTX 5090 的 32 GiB。

## 运行状态

- 启动时间：2026-07-22 19:09:50。
- S3 `screen`：`376752.s3-eval300`；进程 PID `376769`；SwanLab `6lpd9wt2`。
- S4 `screen`：`376755.s4-eval300`；进程 PID `376768`；SwanLab `rvts5pn4`。
- S3 输出：`runs/physics-detection-evaluation/qwen3-1.7b-seed42-s3-anchor0-plus-one-eval300-v1/`。
- S4 输出：`runs/physics-detection-evaluation/qwen3-1.7b-seed42-s4-anchor0-plus-one-eval300-v1/`。

## 指标与裁决

- `family_test` 自由生成首个完整结果：S3 宏平均 F1 `0.929208`、结构化输出合法率 `1.0`、良性误报率 `0.146667`；S4 分别为 `0.840976`、`1.0` 和 `0.373333`。
- S4-S3 家族自由生成宏平均 F1 点差为 `-0.088232`，95% 置信区间 `[-0.111387,-0.064937]`，低于预注册的 `-0.01` 门槛。
- 子类自由生成宏平均 F1 从 `0.852319` 提升到 `0.880598`，点差 `+0.028279`，95% 置信区间 `[0.017176,0.039615]`。
- 三个未知攻击场景的候选评分召回均值从 `0.375556` 提升到 `0.756667`，良性误报率均值从 `0.243333` 降到 `0.091111`；两个点差的 95% 置信区间均不跨 0。
- `subtype_test` 期望校准误差由 `0.410308` 升到 `0.492946`，增量 `0.082638`，超过预注册的 `0.02` 门槛。
- S4 未观测状态误差改善 `70.17%`，物理残差改善 `55.44%`，两个机制门槛通过；生成验证损失恶化 `43.84%`。
- 九项预注册门槛中，家族自由生成宏平均 F1 与子类测试校准误差失败，`overall_pass=false`。当前 S4 不能冻结为最终算法。

## 验收事实

- 两组均于 2026-07-22 19:19 左右正常完成，`screen` 与 GPU 进程均已退出。
- 两组各有 79 行 `metric_history.jsonl`、12 个预测文件和 12,600 行预测；全部分区结构化输出合法率为 `1.0`。
- SwanLab 官方只读接口返回 `FINISHED`、`show=true`；代表性家族宏平均 F1 云端数据点与本地一致。
- 本地已经回收 S3/S4 训练适配器、状态头、评估输出、比较结果和云端接口原始 JSON。
- 比较分析的 30/30 个输入文件哈希一致；比较目录制品清单全部通过；服务器与本地源码及核心 JSON 哈希一致。
- 数据盘使用率保持 `75%`，剩余约 `13 GiB`。
