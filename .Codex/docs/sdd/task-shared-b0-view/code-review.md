# 共享 B0 派生视图修复轮 1 独立复审

## 结论

**范围复审通过。** 上轮发现的 **严重问题 1 项、重要问题 2 项、建议 2 项均已解决**。修复差异未引入新的严重或重要问题，当前计数为 **严重 0、重要 0**。

代码可以进入服务器最小测试与真实数据双物化。由于本复审按任务边界未运行测试或训练，在服务器测试和真实物化门禁通过前，不得把共享视图标记为正式验收，也不得据此写入论文结果。

## 上轮问题复核

### 严重 1：缺少 GeNIS/TQH-C2 隔离验证视图

**状态：已解决。**

- 配置在 `configs/shared_b0_view_v1.yaml:37` 与 `configs/shared_b0_view_v1.yaml:47` 分别绑定 GeNIS、TQH-C2 验证清单、SHA-256、数量、套件、划分和分组字段。
- `_validation_contract` 在 `src/flow_probe/shared_b0_view.py:420` 检查验证清单哈希、数量、套件、划分、样本表绑定、唯一性和候选样本不相交。
- `_assert_group_disjoint` 在 `src/flow_probe/shared_b0_view.py:481` 检查候选训练样本与验证样本的分组不相交。
- 物化器在 `src/flow_probe/shared_b0_view.py:935` 生成分源公共特征、Qwen 和 BERT 验证视图，并在统计与冻结清单中记录数量、顺序哈希及隔离结果。

### 重要 1：`publication` 配置未被消费

**状态：已解决。**

- `src/flow_probe/shared_b0_view.py:718` 读取 `publication`，要求命令输出目录精确等于配置路径。
- 临时后缀固定为 `.partial`，拒绝覆盖与原子改名开关必须为 `true`。
- 运行状态和冻结清单记录规范化输出相对路径及临时后缀。
- `tests/test_shared_b0_view.py:464` 覆盖命令输出路径与配置不一致时的拒绝行为。

### 重要 2：测试未调用实际物化入口

**状态：已解决。**

- 测试由 8 项增加到 14 项。
- `tests/test_shared_b0_view.py:409` 运行真实最小三源端到端物化，检查候选输出、分源验证输出、成功原子发布和隔离统计。
- `tests/test_shared_b0_view.py:427` 覆盖哈希失配后保留失败 `.partial`、状态文件与 `_INCOMPLETE`。
- `tests/test_shared_b0_view.py:442` 覆盖更新全部关联哈希后仍拒绝重复候选样本。
- `tests/test_shared_b0_view.py:455` 覆盖已有正式输出拒绝覆盖。
- `tests/test_shared_b0_view.py:464` 覆盖发布路径合同失配。

### 建议 1：ns-3 物理监督缺少结构门禁

**状态：已解决。**

- `src/flow_probe/shared_b0_view.py:624` 验证物理数值列表长度、有限性和非缺失。
- `src/flow_probe/shared_b0_view.py:633` 要求状态监督与归一化物理映射非空，并验证各字段四窗口长度。
- `src/flow_probe/shared_b0_view.py:645` 验证五个队列边界锚点后才生成独立物理视图。

### 建议 2：候选顺序类型错误未统一为合同异常

**状态：已解决。**

- `src/flow_probe/shared_b0_view.py:395` 已复用 `_integer` 解析 `stable_order`，非法类型会转换为 `SharedB0ViewError`，不再从命令入口泄漏原始类型异常。

## 新增严重或重要问题

**未发现。**

## 非阻塞说明

- 当前测试已经真实覆盖成功物化和关键失败路径，但服务器依赖、真实固定清单、生产哈希以及 10,000 条真实顺序仍须由服务器最小测试和真实双物化验证。
- 分组重叠与畸形物理结构已有生产门禁，尚无专门的负向测试节点；这不阻塞本轮范围复审，可在后续回归中补充。
- 分源验证文件符合实施计划。B0 启动配置仍需显式记录训练时选用的验证文件及后续分源评价方式，禁止临时把候选训练文件复用为验证文件。

## 复审裁决

- 严重：0
- 重要：0
- 建议遗留：0 项阻塞，2 项非阻塞说明
- 裁决：**允许进入服务器最小测试与真实双物化。**
