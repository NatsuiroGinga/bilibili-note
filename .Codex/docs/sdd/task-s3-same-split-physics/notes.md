# S3 同分区物理评估记录

## 已有证据

- E1 与 E2 种子 42 已在同一 seed44 测试集完成物理评估。
- E1 未观测状态均方误差为 `3.261725551823061`，物理残差均方误差为 `6.076250563176`。
- E2 未观测状态均方误差为 `0.1197177195588403`，物理残差均方误差为 `0.04291048891094537`。
- E1→E2 的相对改善分别为 `96.3296%` 与 `99.2938%`，该比较同分区且有效。
- 服务器现有四个 `physics_test_summary.json` 仅覆盖 E1 种子 42 和 E2 种子 42、43、44，没有 S3。

## 待记录

- S3 三种状态掩码的指标、运行时间和峰值显存。
- S3/E2 每个种子的同分区相对改善。
- 输入文件、状态掩码、模型权重与状态头哈希。
- 服务器日志、SwanLab 运行号及本地归档位置。
