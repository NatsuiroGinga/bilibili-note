# TQH-C2 A/B/C 固定预算物化修复复审

## 结论

**通过，允许启动串行真实双物化。**

- 严重问题：0 项。
- 重要问题：0 项。
- 原一般问题：已关闭。
- 已知服务器最小回归：3/3 通过。本次复审未运行本地 `pytest`。

## 修复核验

1. 固定清单已重新冻结。域内 validation 和 test 均为 4 个 cell，并同时覆盖 A/B/C、30/300/1800/3600 四档 interval 与 0/30/70 三档 jitter。
2. 新清单仍包含 36 个 cell、303,599 条映射流；每 cell 上限 1,000 后仍为 35,235 条流，未改变预算数据规模。
3. 新固定清单 SHA-256 为 `4a274a0449147d4ef6f478471acf3514c4a3b474806e88403c7c7bf09e8274f7`。
4. `test_checked_in_assignment_satisfies_real_coverage_contract` 直接加载版本库中的真实固定 JSON，并核对 144 行分配、4 份审计及域内两组覆盖，不再只依赖合成清单。
5. 物化入口现已拒绝不一致的 `sampling_algorithm` 和 `source_mapped_sample_count`，对应冲突测试已经加入。
6. 冻结脚本按四档 interval 枚举组合，强制 A/B/C 与三档 jitter 覆盖；验证集和测试集必须互斥，再按样本比例偏差与确定性 SHA-256 裁决，输出采用排序 JSON，未读取模型输出或测试性能。

## 运行边界

本复审只关闭上一轮的重要与一般问题。真实物化仍必须串行执行两次，并继续核对 2,475,729 条目标包、峰值常驻内存、12 份清单加载、全部非自引用制品逐字节一致和失败目录隔离；这些是运行验收项，不再构成启动阻塞。
