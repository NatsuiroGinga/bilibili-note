# 三源统一预算双物化与发布验证

- **验证日期**：2026-07-29
- **数据状态**：`theory_selection/review_pending`
- **发布性质**：路线筛选阶段的受控数据制品，不是论文最终冻结数据

## 1. 验收结论

三源统一主索引和预算清单已经完成服务器真实规模双物化。`build-a` 与 `build-b` 均正常退出，11 个发布文件的相对路径、大小和 SHA-256 零差异；通过验收的构建已发布到服务器固定路径 `runs/data-frozen/dataset-v1/`，发布后复核仍为零哈希差异。

本轮只证明统一索引、抽样预算和开放集清单在当前输入与实现下具有确定性。制品继续保持 `theory_selection/review_pending`，不得写成 `frozen/final_tuning`，也不得直接进入论文最终主表。

## 2. 目标测试

- 服务器最小目标套件有效通过 `12` 项。
- 针对修正后的精确节点单独复核通过 `1` 项。
- 有效门禁合计 `13/13`；这里的口径是“12 项目标套件加 1 项精确复核”，不是伪称一次运行输出了 13 项。
- 覆盖范围包括输入哈希、身份冲突、划分泄漏、重复样本、配额与确定顺序、候选协议显式状态、输出目录与校验和合同。

测试证据位于服务器 `runs/verification/unified-budget-v1-minimal-20260729/`。

## 3. 独立复审

- 严重问题：`0` 项。
- 重要问题：`1` 项，属于重启恢复完整性缺口，不改变本次成功构建的数据内容。
- 剩余缺口：协议加载和大规模内存计算等早期阶段发生异常时，生成器内部状态可能仍停留在 `running`；外层启动器状态和保留的 `.partial` 目录可以识别未完成运行，但内部阶段标记尚不完整。
- 裁决：本次成功构建的计数、文件集合和哈希可以用于后续 `theory_selection` 基线；在修复恢复缺口并完成最终审查前，状态继续保持 `review_pending`。

历史问题、修复范围与复审依据见[独立代码审查](code-review.md)。

## 4. 双物化资源与退出状态

| 构建      | 服务器输出路径                                          | 退出码 | 墙钟耗时 | 峰值常驻内存    | 目录大小     |
| --------- | ------------------------------------------------------- | ------ | -------- | --------------- | ------------ |
| `build-a` | `runs/data-candidate-builds/unified-budget-v1/build-a/` | `0`    | `110` 秒 | `3,729,992` KiB | 约 `542` MiB |
| `build-b` | `runs/data-candidate-builds/unified-budget-v1/build-b/` | `0`    | `111` 秒 | `3,772,476` KiB | 约 `542` MiB |

两次运行的命令、输入哈希、标准输出、标准错误、资源采样和退出状态位于服务器 `runs/data-materialization/unified-budget-v1-20260729/`。

## 5. 计数验收

| 制品或预算           | 总数        | GeNIS       | TQH-C2   | ns-3    |
| -------------------- | ----------- | ----------- | -------- | ------- |
| 统一主索引           | `1,410,346` | -           | -        | -       |
| 正式训练预算         | `59,656`    | `30,000`    | `27,235` | `2,421` |
| 候选筛选预算         | `10,000`    | `3,973`     | `3,606`  | `2,421` |
| GeNIS 神经开放集主表 | `30,000`    | `30,000`    | -        | -       |
| GeNIS 开放集全量清单 | `1,179,168` | `1,179,168` | -        | -       |

正式预算与候选预算均满足固定来源配额；ns-3 的 `2,421` 条记录各使用一次，没有复制补足预算。

## 6. 哈希与发布验收

- 双构建文件数：各 `11` 个。
- 双构建逐文件 SHA-256 差异：`0`。
- `master_records.parquet` SHA-256：`16458a4a191c51eefc58717e16609790b9914936a2470ef29099bdfbc70af703`。
- `budget_checksums.json` SHA-256：`38883c7ef8b1e47ebd218514e71f95474cebaa8637df6b5203326802546a6c2e`。
- 服务器发布路径：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-frozen/dataset-v1/`。
- 发布目录与验收构建逐文件 SHA-256 差异：`0`。

## 7. 下一门禁

1. 基线和候选配置只引用发布后的路径、清单与 SHA-256，不再读取临时构建目录。
2. 从统一 `sample_id` 清单派生树模型、BERT、Qwen 和序列方法共享视图。
3. 启动同协议 B0 基线；当前制品只能支持路线筛选和开发裁决，不能作为最终论文冻结数据。
4. 后续修复早期异常内部状态标记，并用最小故障注入复核恢复行为。
