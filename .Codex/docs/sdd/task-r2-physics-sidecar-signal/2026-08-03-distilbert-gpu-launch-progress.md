# R2 DistilBERT 物理旁路正式探针启动记录

> **2026-08-04 最新状态（本节覆盖下方 2026-08-03 运行快照）：** 静态种子 42 四组已经完成；种子 43 完成 `T-A/T-P`，`T-S` 在第 0 步因 SwanLab `401 Unauthorized` 失败，`T-R` 未开始；种子 44 未开始。服务器当前以无卡模式运行，CUDA 不可用、无 `screen` 会话，数据盘约 `100 GiB`、已用 `41 GiB`、可用 `60 GiB`。当前仍为 `NOT_READY`。

## 0. 2026-08-04 已核验进度

### 0.1 静态旁路结果

| 种子 | 组别 | 宏平均 F1 | 良性误报率 | Brier 分数 | 期望校准误差 | 攻击召回率 | 状态 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 42 | `T-A` | 0.7927419192 | 0.1794258373 | 0.0986712411 | 0.0910825924 | 0.9468085106 | 完成 |
| 42 | `T-P` | 0.6127103249 | 0.1722488038 | 0.1718080005 | 0.1899855015 | 0.4148936170 | 完成 |
| 42 | `T-S` | 0.7844144907 | 0.1818181818 | 0.1040540278 | 0.1084081770 | 0.9255319149 | 完成 |
| 42 | `T-R` | 0.7875604671 | 0.1818181818 | 0.1032441246 | 0.0943381599 | 0.9361702128 | 完成 |
| 43 | `T-A` | 0.8010245540 | 0.1770334928 | 0.06702624545 | 0.08839407978 | 0.9680851064 | 完成 |
| 43 | `T-P` | 0.7989545883 | 0.1722488038 | 0.07688420815 | 0.1036052229 | 0.9468085106 | 完成 |
| 43 | `T-S` | - | - | - | - | - | SwanLab `401`，第 0 步失败 |
| 43 | `T-R` | - | - | - | - | - | 未开始 |
| 44 | 四组 | - | - | - | - | - | 未开始 |

种子 42 中，真实静态物理旁路 `T-P` 同时低于 `T-A/T-S/T-R`；种子 43 中 `T-P-T-A=-0.0020699657`。因此，静态旁路严格成功门槛已经因种子 42 的 `T-P<T-A` 而无法通过，但十二组尚未完成，完整多种子统计裁决仍为 `NOT_READY`。该结果只否决静态 18 维物理向量直接注入，不否决完整 R2。

### 0.2 SwanLab 根因与运行修复

SwanLab `0.9.0` 在同一 Python 进程第二次调用 `swanlab.init` 时复用已经失效的 HTTP 客户端，导致 `401 Unauthorized`。这不是用户未登录或项目权限失效。后续固定为**每个实验组一个独立 Python 进程和一个独立 SwanLab 运行**；失败证据保留，但归档、复审和文档更新不得阻塞训练。

### 0.3 唯一修复方向

唯一允许的结构修复是[因果多窗口物理旁路修复计划](../task-r2-multiwindow-sidecar-repair/implementation_plan.md)：使用四个严格因果窗口、部署可观测协议路由、未知协议走共享通路、TQH 与 ns-3 分离缩放、零初始化且最大幅度为 10% 的有界融合，并增加等信息量原始历史组 `T-H`。架构规格与数据可行性审计已完成，本地 `2,560` 个样本和 `173,196` 个包齐全；数据合同仍在编写，尚未冻结。

- **记录时间**：2026-08-03
- **实验性质**：TQH-C2 冻结开发集上的结构适配探针，不是完整 R2 或 Qwen 正式验证
- **服务器运行根**：`runs/r2-transformer-sidecar-probe/distilbert-v0/`
- **旧启动器目录**：`runs/launchers/r2-distilbert-sidecar-20260803T114736Z-1991/`
- **当前启动器目录**：`runs/launchers/r2-distilbert-sidecar-20260803T121538Z-4692/`
- **SwanLab 项目**：`mortiswang/malicious-traffic-llm`
- **当前总状态**：`NOT_READY`，在线鉴权已经恢复，种子 42 的 `T-P` 正在运行；十二组矩阵尚未完成，不能形成方法有效性裁决

## 1. 已完成部分

种子 42 的基础组 `T-A` 已完成全部 `192` 个优化步，`run_state.json` 满足：

- `status=finished`
- `failure=null`
- `current_step=192`
- `best_epoch=3`
- `latest_checkpoint=null`

服务器制品目录为：

`runs/r2-transformer-sidecar-probe/distilbert-v0/seed-42/t-a/`

SwanLab 运行号为 [`bbu5doo1`](https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/bbu5doo1)。在线接口已核验该运行状态为 `FINISHED`；`run summary` 能返回训练损失、学习率、三轮开发指标和最终验证指标，说明云端图表数据已经实际上传。

### 1.1 种子 42 `T-A` 开发验证结果

| 指标 | 数值 |
| --- | ---: |
| 样本数 | 512 |
| 宏平均 F1 | 0.792742 |
| 平衡准确率 | 0.883691 |
| 攻击召回率 | 0.946809 |
| 良性误报率 | 0.179426 |
| Brier 分数 | 0.098671 |
| 期望校准误差 | 0.091083 |
| TN / FP / FN / TP | 343 / 75 / 5 / 89 |

本组训练耗时 `88.532` 秒，总耗时 `92.642` 秒；峰值分配显存为 `2,784,222,720` 字节，约 `2.59 GiB`。这些数值只是后续 `T-P/T-S/T-R` 的同种子对照，不能单独判断物理旁路是否有效。

## 2. 已归档的中断事实

启动器在创建种子 42 的真实旁路组 `T-P` 时停止。该组尚未进入训练：

- `status=failed`
- `current_step=0`
- `best_model=null`
- `latest_checkpoint=null`
- 失败发生在 `swanlab.init` 阶段
- SwanLab 请求 `POST /api/projects/mortiswang` 返回 `401 Unauthorized`

原始失败制品已归档到：

`runs/r2-transformer-sidecar-probe/distilbert-v0/seed-42/failed-attempts/t-p-swanlab401-20260803T121504Z/`

归档清单 SHA-256 为 `c36571ffbe6a453b2ac77db34c4a52fc8f6ac7c612db8885f1c2e1cd9a032e08`。旧启动器最终状态为 `failed`、退出码为 `1`；失败证据未删除，也没有复用或覆盖失败目录。归档是一次性证据保存动作，已经完成且不再阻塞实验恢复，已完成的 `T-A` 也未重跑。

## 3. 鉴权恢复与当前运行

恢复运行使用新的启动器：

`runs/launchers/r2-distilbert-sidecar-20260803T121538Z-4692/`

新的种子 42 `T-P` 已成功建立 SwanLab 运行 `y6j9i2dj`，说明本次 `swanlab.init` 已通过，先前的 `401 Unauthorized` 不再是当前阻塞项。2026-08-03 20:16 核验状态为：

- `status=running`
- `current_step=60`
- GPU 显存占用 `3402 MiB`

该组仍在训练，尚无可用于旁路比较的最终指标。

## 4. 当前裁决

1. 本轮状态是 `NOT_READY`，不是科学意义上的 `NO_GO`。
2. `T-A` 已形成有效对照制品，不应重跑。
3. 旧 `T-P` 失败证据已经独立归档，归档不再阻塞实验；新的 `T-P` 已进入训练。
4. 在线鉴权恢复只表示基础设施门禁已经通过，不代表真实旁路有效。
5. `T-P/T-S/T-R` 以及种子 43、44 的矩阵尚未完成，不能计算 `P-A`、`P-S`、`P-R`，也不能进入多种子分析或形成 `GO/NO_GO`。

## 5. 下一步

1. 不再等待或追加归档动作，继续跟踪当前启动器中的种子 42 `T-P`。
2. `T-P` 完成后严格串行运行 `T-S`、`T-R`、种子 43 四组和种子 44 四组。
3. 十二组完成后运行冻结的三种子分析器，并按[结果验收与写回模板](distilbert_results_handoff_template.md)回收、验收和裁决。
