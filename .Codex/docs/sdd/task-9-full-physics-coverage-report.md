# 任务九：完整物理样本覆盖诊断报告

- **日期**：2026-07-21
- **随机种子**：42
- **训练步数**：202
- **物理批量**：4
- **严格对照**：M1 与原始 M2，`lambda_state=1.0`、`lambda_physics=0.01`
- **裁决**：不通过

## 1. 运行与追踪

两条运行在独立 `screen` 会话中并行执行，只用于机制诊断。根据任务约束，本次运行时间、吞吐量和功耗不得进入论文效率表。

| 变体 | 服务端输出目录                                                  | SwanLab 运行 | 状态       |
| ---- | --------------------------------------------------------------- | ------------ | ---------- |
| M1   | `runs/physics-m012/qwen3-1.7b-seed42-m1-fullphys202-v1/`        | `37v8dydw`   | `FINISHED` |
| M2   | `runs/physics-m012/qwen3-1.7b-seed42-m2-lp0p01-fullphys202-v1/` | `p49qkyyt`   | `FINISHED` |

两条运行均使用 SwanLab 在线项目 `mortiswang/malicious-traffic-llm`。训练结束后两个 `screen` 会话均正常退出。

服务端归档根目录：

```text
/root/autodl-tmp/thesis/experiments/llm_probe/runs/physics-m012/
```

本地归档根目录：

```text
thesis/experiments/llm_probe/runs/physics-m012/
```

运行地址：

- M1：`https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/37v8dydw/chart`
- M2：`https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/p49qkyyt/chart`

## 2. 严格对照结果

| 指标             |           M1 |     M2，0.01 |     M2 相对 M1 |
| ---------------- | -----------: | -----------: | -------------: |
| 生成验证损失     | 0.0239528552 | 0.0278116039 |  上升 16.1098% |
| 状态均方误差     | 0.0040767035 | 0.0128902028 | 上升 216.1918% |
| 物理残差均方误差 | 0.0036914204 | 0.0040591998 |   上升 9.9631% |

M2 在完整物理训练集至少曝光一次后，三项指标均劣于同条件 M1。完整覆盖没有逆转 50 步实验中的生成退化，且状态误差与物理残差同时恶化。

## 3. 一致性与制品核验

- 两组 `step_metrics.jsonl` 均为 **202 行**。
- 两组 `input_sha256.json` 的 SHA-256 均为 `69855d63ddb066afc8529779e19c2b7d2cda7412c6366dbd9091baf4c24fcdb8`。
- 两组 `physics_sample_order.json` 的 SHA-256 均为 `fdabfc49300656b50c32205d056fe5b8a93455493ca115e785b286db1c1113ae`。
- 输入清单、训练步数和物理样本顺序完全一致，差异可归因于是否加入原始物理损失。
- 两组配置快照、控制台日志、环境清单、输入哈希、物理样本顺序、状态头、逐步指标、训练摘要和制品清单均存在且非空。
- 两组最终适配器目录均约 **82 MiB**；SwanLab 原始日志目录分别约 **300 KiB** 和 **304 KiB**。
- 两组 `artifact_manifest.json` 均记录 `status=finished`、`tracking_mode=online`，并包含对应运行编号和运行地址。
- 使用不含 `--delete` 的 `rsync` 增量回收后，本地再次核验两组摘要、指标行数和哈希，结果与服务端一致。

## 4. 裁决与下一步

原始 M2 未满足“状态指标和物理指标同时改善，且生成验证损失无明显恶化”的门禁，因此：

1. 不进入多随机种子、完整正式训练或跨基座实验。
2. 不继续搜索物理损失权重，也不继续扩展梯度投影变体。
3. 下一步只做无训练审计：按场景与负载区间分解五个状态分量误差和物理残差，核对量纲、归一化、状态可辨识性、残差主导项和物理样本分布。
4. 只有审计定位到具体失配后，才重新设计状态目标、时序残差或公开任务耦合方式；不得用复杂模块掩盖当前失败。

## 5. 局限性

- 本次只使用随机种子 42，失败结论用于否定当前 M2 配置，不外推为所有物理约束形式均无效。
- 两条运行并行执行，因此运行时间与吞吐量不能用于单进程效率比较。
- SwanLab 清单与原始日志已核验；动态图表页面未完成浏览器可见性核验，不宣称网页图表已人工确认。
