# 检索与证据日志

## 固定检索式

1. `physics-informed neural network packet traffic network queue dynamics`
2. `physics-informed network traffic estimation packet inter-arrival`
3. `PINN TCP congestion control dynamics packet capture`
4. `physics-informed QUIC traffic dynamics encrypted traffic`
5. `neural point process packet arrivals physics informed`
6. `network calculus neural network differentiable traffic envelope`
7. `fluid model network traffic physics informed neural network`
8. `conservation law packet network traffic neural network`
9. `encrypted traffic detection packet sequence inter arrival time physics`
10. `PCAP network telemetry queue occupancy inference packet timing`

## 阶段检查点

- 阶段一：已完成。研究问题、纳入排除标准、支持和否决门槛已冻结。
- 阶段二：已完成一轮。共收集 17 篇可读全文，其中 15 篇新下载、2 篇复用本地基础全文。
- 阶段三：已完成。12 篇核心全文、5 篇补充全文，逐篇记录采用边界。
- 阶段四：已完成。核心全文均完成关键章节、方程、限制和页码证据核验。
- 阶段五：已完成。形成一个首选和两个备选，并明确排除四类伪 PINN 路线。
- 阶段六：进行中。研究制品和 15 篇阅读笔记已生成，待格式化、链接与 Zotero 状态核验。

## 收集结果

- 新下载目录：`raw/papers/pinn/pcap/`。
- 新 PDF 有效性：15/15 通过 `pdfinfo`。
- 转文本结果：17/17 可检索。
- 核心证据：12 篇均使用全文，不使用仅摘要公式论断。
- Zotero：本地 API 与连接器均可用；先检索去重，确认 Raissi、道路交通 PINN 和二维 TCP/AQM 已存在，再将其余 14 条记录导入当前可编辑的 `attack-detection` 集合，接口返回 `201`。
