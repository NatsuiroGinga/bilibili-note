# 任务十七 v5 域随机化阶段报告

## 当前结论

任务十七唯一一次 ns-3 覆盖扩展、四窗口数据构造和固定可辨识性诊断均已完成。工程运行全部成功，但三项预注册研究门槛只通过一项，因此任务十七总体裁决为失败。按事先冻结的停止规则，不进入 C1/C2，也不继续增加仿真、输入字段或估计器变体。

## 已完成

1. 新增独立 v5 场景，覆盖容量、时延、三种队列、队列上限、独立丢包、两种传输协议、发送端、包长、到达过程、起始抖动和公共测量噪声。
2. 新增冻结矩阵生成器，严格产生 `120/30/30/30` 共 210 个不重复环境参数元组。
3. 新增正式矩阵执行器，逐配置保存命令、标准输出、标准错误、CSV、状态、SHA-256、验证摘要、SwanLab 指标和制品清单。
4. 通过 FIFO/CoDel/RED、UDP/TCP、恒定/突发及容量变化的代表性冒烟；所有已验收冒烟的字节与包守恒残差均为零。
5. 在正式运行前修复噪声配置的字段前缀，保证公共观测与监督真值通过键空间隔离。
6. 正式矩阵 210 个配置全部完成，得到 25,200 个窗口，双守恒残差违规为零。
7. 构造 24,570 条四窗口公共观测序列，切分为 `14,040/3,510/3,510/3,510`。
8. 完成训练均值常数、单窗口与四窗口三个固定对照，并完成组级置信区间和范围外不确定性裁决。

## 正式运行

- 配置清单 SHA-256：`a9002d666720843eba9e55fef2f6ebdda4a8ef69c6e9255797dcad5480ef0880`。
- 服务端输出：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/ns3-data/ns3-domain-randomization-formal-20260722-v1`。
- `screen`：`task17-v5-210`。
- SwanLab：`mortiswang/malicious-traffic-llm/j4xqs5er`。
- 运行状态：`210/210` 完成，失败 `0`，SwanLab 状态 `finished`。

## 可辨识性结果

| 方法         | 未见配置状态均方误差 |
| ------------ | -------------------: |
| 训练均值常数 |            0.0172143 |
| 单窗口       |            0.0171397 |
| 四窗口       |            0.0166949 |

- 四窗口相对常数只改善 `3.017%`，未达到 `10%` 门槛。
- 四窗口相对单窗口的组级误差差为 `-0.000444865`，95% 置信区间为 `[-0.000626147,-0.000262251]`，该项通过。
- 四窗口范围外不确定性曲线下面积为 `0.08111`，95% 置信区间为 `[0.02442,0.16000]`，该项失败且方向反转。
- SwanLab 运行号：`lydyn38z`；预测记录 7,020 条。回收后核对 12 项哈希记录，其中模型、指标、预测、配置等 11 项一致；`console.log` 因清单生成后继续追加完整指标而不一致，不影响实验数值。

## 本地归档

- 原始 v5 矩阵：`thesis/experiments/llm_probe/runs/ns3-data/ns3-domain-randomization-formal-20260722-v1/`，210 份 CSV 哈希全部通过。
- 四窗口序列：`thesis/experiments/llm_probe/runs/ns3-data/ns3-observability-sequences-h4-20260722-v1/`，9 项制品哈希全部通过。
- 固定诊断：`thesis/experiments/llm_probe/runs/observability/task17-observability-seed42-20260722-v1/`，除上述运行期可变日志外，其余 11 项哈希全部通过。

## 最终裁决

任务十七的三项门槛只通过一项，`overall_pass=false`。四窗口虽显著优于单窗口，但增益不足以超过常数基线门槛，且不确定性不能识别范围外配置。因此不得把历史估计结果称为可辩护的物理状态，不得启动物理软令牌和不确定性门控训练。

第三章第二支柱按失败停止规则降级并冻结为**有限队列物理一致性**，与已冻结的**双锚点边界约束**组成最终理论框架。后续只执行降级方案的 S3/S4 同协议正式检测验证，不再改变理论结构。

## 下一步

1. 将任务十七失败裁决同步到第一创新点实验总控。
2. 跳过任务十八 C1/C2，按既定降级方案执行一次 S3/S4 同协议正式检测验证。
3. 验证结束后冻结第三章标题、方法名称、公式、算法和贡献边界。
