# R2 检测开发六视图最终独立复审

- 日期：2026-08-04
- 复审对象：GeNIS 与 TQH-C2 校准、开发验证的检测视图、共同历史视图和路由视图
- 本机制品根：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-candidate-v1-server/`
- 复审性质：全新只读独立复审
- 结论：**严重问题 0，重要问题 0，六视图数据门禁放行。**

## 1. 复审边界

本次复审没有修改生产数据、实验代码、配置或服务器状态，没有重新物化数据，也没有读取最终测试。复审依据为：

1. `configs/r2_final_sidecar_role_manifest_v1.json`，文件 SHA-256 为 `69cc3be2cc3668f0b90937496f02a37a5d6b6aa57bb346969f385e4e5e0f9d28`。
2. `configs/r2_final_detection_development_split_v1.yaml`，文件 SHA-256 为 `389de958126b25b91997f870d2cb4b21df090f826159c4e9ed45a897d2c3c968`。
3. 服务器生成并回收本机的 `artifact-manifest.json`，文件 SHA-256 为 `167cb69cb39504c6db0cd40127f9103542f44d38a5d15cac1ac4ee7b76f63dbb`。
4. `source-lock.json`、`materialization-contract.json`、`schema.json`、`development-split-assignments.jsonl` 与 `development-group-audit.jsonl`。
5. 本机六份 Parquet 原始制品。

复审没有调用物化器内部函数读取已有结论，而是独立实现规范化 JSON、模式哈希和逐行语义哈希计算，再与服务器收据及角色清单比较。

## 2. 六份制品独立重算

六份 Parquet 的文件 SHA-256、模式 SHA-256、逐行语义 SHA-256、文件大小和行数均与 `artifact-manifest.json` 完全相同；角色清单记录的文件哈希、模式哈希、行数和就绪状态也全部匹配。

| 制品 | 行数 | 文件 SHA-256 | 模式 SHA-256 | 语义 SHA-256 | 结果 |
| --- | ---: | --- | --- | --- | --- |
| `detection-view-calibration.parquet` | 7,200 | `5736e8930ebd7c23f01bd5112278db35b4018c24556cfe3a03b4a721725b66df` | `1ce1cdb73304b16f27c6f3ad95cf500470b10fd6c6d3c6dab12b17a27c8f1154` | `1c5efd25ce5ba6314dcdb8b5386c4cdb3f7597a9b545f071ef60bf9928010aae` | 通过 |
| `detection-view-development-validation.parquet` | 7,199 | `077a1faf35538481d2a63eb4d1c6417b6620582b50e2bf502dfde55c798c6239` | `1ce1cdb73304b16f27c6f3ad95cf500470b10fd6c6d3c6dab12b17a27c8f1154` | `030e0f73438351c5a6b934e82fe507cf844fff9137593fd4e736c5a57b0e5bc6` | 通过 |
| `common-history-calibration.parquet` | 28,800 | `fd128612e5906241ffeb4b7d7ea2596c4398bc8cc3a88ae95638ec9d32423b6a` | `cff15d736c582837428c0c6d4aee09a052df49bc6228f0d23c11bb8b23480439` | `5d6942eaf9c7a7f3b485129378afd035c7d1c7383fd460687474677b7d8eca93` | 通过 |
| `common-history-development-validation.parquet` | 28,796 | `c69b237bcabbef8bdef6df4d2977a68b2a2440b95760294d014370a67a2113a6` | `cff15d736c582837428c0c6d4aee09a052df49bc6228f0d23c11bb8b23480439` | `ca69f6ee809e591bdb53c44aca039888e35cbb5b98587cea7457f9f9e4711f89` | 通过 |
| `route-assignments-calibration.parquet` | 7,200 | `b28bdac163d2d4ede236e6e4154b7f56ae6a35bc8eaebf4605b439c23c7360ae` | `d39be92ee1950c03eeaa492b9931d91dbb9178279110bf4acb908e2f34f2af2e` | `8a5c508bcdc66edc5692f10d895a50336481c3ebad7a6574b2f0afa6f155b1b8` | 通过 |
| `route-assignments-development-validation.parquet` | 7,199 | `217b65d25d7be83dc57966d0de2d16f42a944c10b8002d8f2bd09c1b1e202838` | `d39be92ee1950c03eeaa492b9931d91dbb9178279110bf4acb908e2f34f2af2e` | `da5b5a8eb1ca0cf60950b5aea9a73719cf56c42cd6932111524cf74c500622de` | 通过 |

收据中完整制品列表的规范化载荷 Merkle 独立重算为 `7ee5fe8fae8ccd6ec5a83f829dbd38f5a1b587b7611c3c32a436540c4097146d`，与服务器收据及角色清单一致。

## 3. 样本、四窗和三视图绑定

### 3.1 校准分区

- 检测记录 7,200 条，共同历史记录 28,800 条，路由记录 7,200 条。
- GeNIS 5,200 条，TQH-C2 2,000 条。
- 检测与路由 `stable_order` 均严格为 0 至 7,199。
- 检测、历史与路由的 `sample_id`、`stable_order`、`source_dataset`、`split_id` 逐行一致。
- 每个检测样本恰有四条历史记录，`window_index` 严格为 0、1、2、3。
- 样本顺序摘要为 `6ce207f4291d3a48baf975318bf5068776256fabaa75ce0e32775d1cb5004136`，与原验收记录一致。

### 3.2 开发验证分区

- 检测记录 7,199 条，共同历史记录 28,796 条，路由记录 7,199 条。
- GeNIS 5,199 条，TQH-C2 2,000 条。
- 检测与路由 `stable_order` 均严格为 0 至 7,198。
- 检测、历史与路由的四项身份字段逐行一致。
- 每个检测样本恰有四条历史记录，`window_index` 严格为 0、1、2、3。
- 样本顺序摘要为 `6469172e408d9bcccdc06f33d1d842d0d0e4a82978763afd00422b5597f19d7f`，与原验收记录一致。

### 3.3 缺失掩码和路由

- GeNIS 的四窗历史均明确标记不可用，没有以填充值伪造历史。
- TQH-C2 校准集包含 1,993 个四窗有效样本和 7 个单窗有效样本。
- TQH-C2 开发验证集包含 1,909 个四窗有效样本、1 个双窗有效样本和 90 个单窗有效样本。
- 所有路由记录均为停止梯度；正式 QUIC 训练全部关闭。
- 所有 `UNKNOWN` 记录均未启用 UDP 或 QUIC 专家。

## 4. 分区互斥与 train-fit 不变性

独立读取 14,399 条 `development-split-assignments.jsonl` 和 4,222 条 `development-group-audit.jsonl` 后得到：

1. 校准与开发验证 `sample_id` 交叉数为 0。
2. 以 `source_dataset + group_id_sha256` 组成的独立组交叉数为 0。
3. 分组审计的每组行数与样本分配逐组精确一致，4,222 个组键全部唯一。
4. 分配记录只包含 `calibration` 与 `validation`，且分配样本集合分别与两份检测视图完全一致。
5. 所有分配记录均声明 `assignment_uses_label=false`；源码中的分组函数只消费样本身份、组映射、组大小、固定种子和确定性哈希，不消费标签或模型输出。
6. `train-fit` 允许清单文件 SHA-256 保持为 `a086968e972b92edac2fdb7f25a17cb9cb5ada67b9b0d03e443a3f03028caed1`，仍为 7,579 个唯一样本，全部标记 `split_id=train-fit`。
7. `train-fit` 允许清单与两个开发分区的样本交叉数为 0；开发分组函数只处理既有 GeNIS、TQH-C2 验证来源，训练视图由独立过滤函数按冻结顺序构造。因此，没有证据表明 `train-fit` 被重新切分。

## 5. 最终测试不可见性

- 六份 Parquet 的全部记录均严格为 `final_test_visible=false`。
- 14,399 条分配记录和 4,222 条分组审计记录全部为 `final_test_visible=false`。
- `artifact-manifest.json`、`materialization-contract.json`、`source-lock.json` 和 `run-state.json` 均声明最终测试不可见。
- `source-lock.json` 只列出训练允许清单、冻结训练视图、GeNIS/TQH-C2 既有验证来源、包级辅助源、固定配置和固定脚本，没有最终测试或未见配置输入角色。
- 实际物化配置与源码哈希分别为 `f2df5943a9b6a2c20529a3ae30944ec0d8a2060b735896d8abe8cc5f86ca5821` 和 `9855ba856c16c23e47bbe2bcb1a57504f68c918a17c8bc077ca4388e8de91d71`，与 `source-lock.json` 一致。

据此，本次制品证据与源码输入边界均未发现读取最终测试的迹象。

## 6. 问题与边界

### 6.1 严重问题

0 项。

### 6.2 重要问题

0 项。

### 6.3 非阻塞边界

1. 原始拆分合同仍保留物化前的 `status: blocked_pending_server_materialization`、空输出哈希和 `task5_ready: false`。该文件作为固定输入继续保持原哈希；当前就绪事实由角色清单中的 `development_materialization_manifest` 和服务器生成的 `artifact-manifest.json` 承载。直接消费拆分合同而绕过角色清单的程序仍应被禁止。
2. 本机复审对象是六份校准/开发验证 Parquet。本机没有回收 `detection-view-train-fit.parquet`，因此本次没有重新计算该服务器文件的逐行语义哈希；`train-fit` 未重新切分的结论来自冻结允许清单哈希、7,579 条唯一成员、与开发集零重叠、源码数据流隔离和服务器收据的一致证据。
3. 本次放行只覆盖六视图数据门禁，不代表 TCP/UDP v2 正式物理池、模型全部运行时门禁、`G0`、15 个候选运行或 R2 方法效果已经通过。

## 7. 最终裁决

**六视图数据门禁放行。严重问题 0，重要问题 0。**

模型任务可以只读消费角色清单绑定的六份固定制品；不得重新物化、重新切分 `train-fit`、改动样本顺序或读取最终测试。后续仍须按实验总控完成 TCP/UDP v2 物理池、模型运行时收据、总体验收和 `G0`，才能启动正式候选运行。

## 8. 实际验证

- 对六份 Parquet 逐文件重新计算文件 SHA-256、模式 SHA-256、逐行语义 SHA-256、大小和行数：6/6 通过。
- 重算服务器收据载荷 Merkle：通过。
- 校准和开发验证的检测、历史、路由三视图身份与稳定顺序核验：2/2 通过。
- 每样本四窗结构核验：14,399/14,399 通过。
- 样本与独立组跨分区核验：交叉数均为 0。
- `train-fit` 允许清单成员、哈希、角色与开发集零交叉核验：通过。
- 最终测试不可见性与来源锁角色审计：通过。

本次未执行 TDD、pytest、格式化、Pyright、Git 差异检查、服务器重物化或任何 GPU 操作。
