# ChatGPT GitHub 半自动交接与 Git 整理计划

## 目标

在不纳入秘密、实验大文件、数据、权重、日志或其他代理未验收文件的前提下，分批提交并推送当前可读制品；建立普通 ChatGPT 对话的只读半自动交接入口。

## 推送目标

- 本地分支：`exp/eta-ber-20260904`
- 现有上游：`origin/codex/ch4-dtep-pbc-20260819`
- 不合并主分支、不改默认分支、不强推、不改写历史。

## 排除项

- `runs/`、原始数据、权重、检查点、日志、缓存、`.env`、凭据、SSH 文件、原始论文 PDF。
- `raw/papers/**/*.pdf` 与所有不在本任务所有权内的未跟踪文件。
- 主代理正在实现的 `ch3_drift_bresnet_dual_condrisk_screen.py`、对应配置和启动器。

## 阶段

- [x] P0：建立计划与排除范围。
- [x] P1：只读审计已跟踪提交与工作树，识别可读原子批次及敏感/大对象风险。
- [ ] P2：以显式路径逐批暂存、审查、提交；每批记录 SHA 与验证。
- [ ] P3：推送并核对上游分支 SHA。
- [ ] P4：建立普通 ChatGPT 对话的半自动交接文档与最小生成工具。
- [ ] P5：合并根规则、验证并记录交接回执。

## 当前状态

**推送阻塞。** `@{upstream}..HEAD` 的 139 个既有本地提交包含 18 个已跟踪 `raw/papers/**.pdf` 大对象，大小约 0.18MB 至 7.9MB。它们不属于当前未跟踪工作树，直接推送仍会上传，违反本轮边界。未执行暂存、提交或推送；等待用户决定允许该历史、改用不含该历史的新分支，或明确授权历史重写方案。
