# Git 分批提交与推送实施计划

## 目标

将当前工作区的变更按独立、可审查的主题提交，并推送当前分支 `codex/ns3-pinn-m012-20260720`。

## 边界

- 不使用 `git add -A`，不混合无关的用户修改。
- 不改写既有提交、不执行强制推送，也不创建拉取请求。
- 提交前检查凭据、大型二进制文件、部分暂存差异与各批次的最小验证要求。
- 涉及 `thesis/experiments/llm_probe/` 的生产代码与配置，先完成独立审查和对应验证。

## 阶段

- [x] 阶段 1：盘点索引、工作区、文件类别和远程分支状态。
- [x] 阶段 2：提出批次边界与 Conventional Commits 提交信息，取得确认。
- [ ] 阶段 3：逐批暂存、核验、提交（五个非代码批次已完成；实验工程批次阻塞）。
- [x] 阶段 4：推送并核对远程分支（已推送完成批次）。

## 当前状态

**部分完成。** 五个非实验代码批次已推送并建立上游分支；实验工程批次因独立审查发现严重契约不一致而暂缓。剩余 396 项工作区变更（暂存 98、未暂存 32、未跟踪 271）均保持未改动。

## 已确认的索引快照批次

1. `chore(repo): 更新仓库基础配置`
2. `docs(research): 记录实验与证据进展`
3. `docs(reference): 整理 ns-3 文档与原始论文`
4. `docs(thesis): 更新研究章节框架`
5. `feat(experiments): 新增流量探针实验流程`
6. `docs(wiki): 补充物理约束知识库`

## 已创建提交

1. `3017a2a chore(repo): 更新仓库基础配置`
2. `4cb9711 docs(research): 记录实验与证据进展`
3. `f60c5d9 docs(reference): 整理 ns-3 文档与原始论文`
4. `9492406 docs(thesis): 更新研究章节框架`
5. `3d5a91c docs(wiki): 补充物理约束知识库`

## 推送结果

- 远程分支：`origin/codex/ns3-pinn-m012-20260720`。
- 推送状态：成功；远程已提示可创建拉取请求，但用户未要求创建，本轮未创建。

## 盘点错误

- 使用工作区文件大小审计暂存项时，三个已从工作区删除、但索引仍有内容的旧论文章节文件无法 `stat`。原因是部分暂存，不代表索引损坏；后续将用 Git 索引对象审计，不读取不存在的工作区路径。
- 实验工程独立审查发现严重问题，详见 `.Codex/docs/2026-07-24-索引快照-实验工程审查报告.md`：ns-3 C++ 生成器输出 v3 CSV 表头，而 Python 校验器只接受 v4；相应测试断言也与实现返回键不一致。该批次在修复、服务端最小回归和重新审查前不得提交或推送。
