# ns-3.48 安装任务计划

## 目标

在 GPU 服务器 `/root/autodl-tmp/thesis/ns3/` 完成 ns-3.48 的配置、编译和最小仿真验收，并保留完整日志。

## 阶段

- [x] 只读预检后台会话、进程、日志、目录和磁盘状态
- [x] 安全恢复或继续配置与编译
- [x] 运行 `scratch-simulator` 并核验版本、构建目录和磁盘
- [x] 写入安装验收记录并完成格式与差异检查

## 决策

- 不修改 ns-3 上游源码，仅为 `./ns3` 命令设置 `USER=ns3builder`。
- 使用 `screen`、独立日志和 `-j 8`，不操作现有 GPU 实验进程。
- 保留首次触发 root 防护的失败日志，不覆盖失败证据。

## 错误记录

- 首次安装被 ns-3 包装器的 root 防护拒绝，尚未进入 CMake；远端预检确认原日志仍只包含该异常。
- 此前重试未成功启动：预检时无 screen 会话，日志没有新增配置或编译内容。
- 绕过 root 防护后，配置因系统 CMake 3.22.1 低于 ns-3.48 要求的 3.25 而失败；保留独立失败日志，并改用隔离工具环境提供新版 CMake。
- Prettier 写回新验收记录时受本地文件属性影响返回 `EPERM`；按 Prettier 生成的精确差异修正表格，并用 `prettier --check` 复核。

## 当前状态

全部阶段已完成；正式记录、交接写回、格式检查和差异检查均已通过。
