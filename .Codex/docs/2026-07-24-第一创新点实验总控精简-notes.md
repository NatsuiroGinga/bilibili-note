# 第一创新点实验总控精简记录

## 已发现问题

- 当前总控共 312 行，已完成的任务十七、任务十八、D0、D1/D2/D3 在“结论”“当前状态”“下一步”中重复出现。
- `PreCompact` 快照仍指向任务十七和 E1/E2 初始实现阶段，已经过期。
- E2 仍写为种子 43、44 正在复现，未反映三随机种子同分区复核已完成。
- S3→E2 的 `14.32%` 与 `60.32%` 使用了不同数据分区，必须替换为同分区结果。
- 外部基线应先于新的结构探索冻结并执行。

## 待核验

- 三个随机种子的同分区物理指标和总门槛：已核验。
- 当前正式基线的执行顺序与未触碰最终测试集要求：已核验。

## 最新同分区裁决

| 种子 | 家族宏平均 F1 变化 | 子类宏平均 F1 变化 | 未观测状态误差改善 | 物理残差改善 | 总门槛 |
| --- | ---: | ---: | ---: | ---: | --- |
| 42 | `+0.005671` | `+0.000048` | `13.34%` | `60.06%` | 通过 |
| 43 | `+0.007955` | `-0.001452` | `-9.89%` | `82.41%` | 失败 |
| 44 | `+0.001066` | `+0.001047` | `-53.68%` | `8.59%` | 失败 |

- 三个种子的检测与校准门槛均通过，说明检测收益方向稳定。
- 状态与物理门槛只在种子 42 全部通过，说明结构没有形成稳定的物理收益。
- E2 不能冻结为最终算法；旧的跨分区 `14.32%`、`60.32%` 不再使用。

## 当前执行顺序

1. 从未使用的 GeNIS 完整数据中冻结会话、时间和场景隔离的最终测试清单。
2. 在统一输入、划分和调参预算下运行正式基线：传统强基线、判别式深度基线、普通监督 Qwen 和至少一种已发表流量方法。
3. 基线协议冻结后，只允许探索能够同时守住检测门槛并改善多种子物理稳定性的结构。
4. 方法冻结后再做消融、HIKARI/DEDALE、多拓扑、跨基座和效率验证。
