# DRIFT 逻辑路线隔离笔记

## 已知迁移事实

- DRIFT 当前状态集中在 `RWKV第三章恢复卡.md` 顶部 2026-09-07 摘要，但下方仍混有大量旧 LSPR/FT/ETA-BER/RWKV 历史。
- 新 DRIFT 恢复卡不得复制这些旧历史，只提炼当前事实与证据路径。
- 朱论文原文分析已确认：创新主体应是一个统一主方法；内部因素数量由证据决定；只有自然形成两个独立因素时才做严格 `2×2`。

## 待确认规范入口

- A 已确认：`raw/papers/datasets/2026-Lee-DRIFT-DGA-Temporal-Drift.pdf` 与 `wiki/papers/datasets/2026-Lee-DRIFT-DGA-Temporal-Drift.md`。本地论文作用域混合查询中结构化笔记排名第 1，`source_pdf` 明确指向该原件。
- B 已确认：`.Codex/docs/2026-09-07-DGA检测系统综述/文献综述.md` 与 `.Codex/docs/2026-09-07-DRIFT引用与方法前沿/文献综述.md`。前者已完成 12 份核心原件与严格笔记的综合，后者任务计划所有 P0/P1 项均标记已完成；默认不加载各自过程 `notes.md`。
- C 已确认：`thesis/methods/第三章-朱焱雷第三章机制构思与创新声明分析.md`。

## 本地检索收据

- 状态检查首次因 uv 缓存权限受沙箱限制失败；经授权读取本机缓存后成功。
- 初次索引状态：`stale=true`，原因是当前新增与修改文档。
- 增量重建后：`built_at=2026-09-08T03:00:19.544979+00:00`，`source_manifest_hash=31171de905ed7c7630d21560d15d42c4d69c63d8ca654a2b1ecad8c1ef5c641a`，`index_sha256=bb686317be9101b71009b69e182739561be59eae9c5bc28747968aa9ee9ce220`，`stale=false`。
- 查询模式：本地离线混合检索；论文入口使用 `scope=paper`，综述入口使用 `scope=project`。

## 当前事实提炼

- 数据协议唯一合同：`thesis/methods/第三章-DRIFT数据角色与评价协议.md`。T17–T19 只用于开发、选择和冻结；T20–T25 无后缀文件在全部设计冻结后整批一次正式评价；T25 必须作为最新年完整报告。
- 当前真实病灶：低误报下的未来良性域、跨 family/伪未见 family 与跨年新 eSLD 泛化；静态三支路有约 34% 至 36% 判断不一致和可纠错样本，但启发式选择会用更高漏报换较低默认误报，证明“存在纠错空间”，不证明当前融合机制有效。
- M1-v1 源期否决并禁止 T18；M2/J06 当前共享预算形式精确分解，创新身份与训练资格撤销；标准类别内硬最大资格探针只否决该近邻实现，不外推到所有双侧风险。
- M1-v2 只获复用冻结 P0 的 T17 32 步短窗资格，不批准完整 T17、T18 或正式实验。
- 第二方向只到 T17 伪未见 family 驱动的辅助任务路由零更新梯度诊断；family 不等于 generator，不能写成机制有效。
- 当前不应预设两个因素。创新主体是一个解决上述 DRIFT 病灶的统一主方法；因素数量由最小证伪结果决定，只有自然形成两个独立开关时才采用严格 `2×2`。

## Goal 工具状态

- 本子线程调用 `get_goal` 返回 `goal=null`，无法看到主线程所述 `usageLimited` Goal。
- 在确认目标工具作用域前不创建仅对子线程生效的新 Goal；若不能替换，将在本任务目录落盘完整 objective 供主代理或恢复后直接设置。
