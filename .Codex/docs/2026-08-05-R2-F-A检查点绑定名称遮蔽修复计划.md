# R2 F-A 检查点绑定名称遮蔽修复计划

> **执行要求：** 使用 `subagent-driven-development`，实现代理对本任务执行一次精确范围的测试驱动开发。

## 目标

修复 `r2_final_distilbert_probe._physics_system` 中导入函数 `checkpoint_binding` 被同名局部载荷变量遮蔽的问题，使种子 42 的 `F-A` 能通过检查点绑定构造并继续正式入口，同时不改变检查点内容、绑定语义、配置、数据或科学参数。

## 根因证据

- 正式启动器 `runs/launchers/r2-final-sidecar/r2_final_distilbert_f-a_seed42-20260805T072316Z/` 在进入 GPU 和 SwanLab 前失败。
- 栈追踪指向 `src/flow_probe/r2_final_distilbert_probe.py:902` 的 `checkpoint_binding(...)` 调用。
- 同一函数后部把 `checkpoint_binding` 赋值为检查点载荷，Python 因此将该名称解析为局部变量，导致调用前触发 `UnboundLocalError`。

## 文件边界

- 修改：`thesis/experiments/llm_probe/src/flow_probe/r2_final_distilbert_probe.py`
- 测试：优先在现有 R2 最终探针目标测试文件中增加一个最小回归测试；若没有合适文件，只新增一个精确测试文件。
- 报告：`.Codex/docs/2026-08-05-R2-F-A检查点绑定名称遮蔽修复报告.md`
- 禁止修改：检查点、配置、数据、Shell 包装器、物理公式、训练预算、其他源码。

## 单一任务

- [ ] 写一个直接覆盖 `_physics_system` 名称解析或等价真实调用路径的回归测试，并运行该唯一目标，确认以 `UnboundLocalError` 预期红灯失败。
- [ ] 仅把检查点载荷局部变量重命名为不与导入函数冲突的名称，并同步修改该局部变量的直接引用。
- [ ] 仅重跑同一测试目标，确认绿灯；不得运行完整测试集、重复测试、格式化、Prettier、Pyright、Git 差异或独立导入预检。
- [ ] 记录红灯、绿灯、修改行、源码新 SHA-256 和遗留风险。

## 验收与后续

- 唯一目标测试必须先红后绿，且红灯原因与服务器栈追踪一致。
- 生产改动不得改变任何绑定字段、摘要比较、检查点读取顺序或返回结构。
- 主代理同步唯一变更源码并核对摘要后，以新的唯一身份、`UV_NO_SYNC=1`、原包装器字节不变，仅重启 `F-A`。
- 失败启动保留，不计作科学实验失败；新的正式运行由运行时断言继续失败关闭。

## 状态

**进行中：** 根因已确认，等待实现代理完成一次最小测试驱动修复。
