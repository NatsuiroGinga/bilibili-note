# 各级 AGENTS 与强制恢复文档一致性审计

## 结论

**总体判断：需要修改后再依赖当前恢复链启动或分派 E2／服务器实验。** 本轮完整读取了 9 份指定文件，并补读 `raw/AGENTS.md`、`wiki/AGENTS.md`。按“一个可独立修复的问题计一项”，共发现：

- **直接冲突：6 项**，其中 P0 2 项、P1 3 项、P2 1 项。
- **过时但因近层规则覆盖而暂不构成当前冲突：1 项**。
- **缺失但应补充：1 项**。
- 其余审计维度一致，或已由恢复协议保证读取，无需把短期状态和数值重复写入长期规则。

最高优先级问题是 `thesis/AGENTS.md:14` 仍把 `thesis/README.md` 指定为章节名称权威来源，而该 README 仍冻结旧 Qwen／GeNIS 路线；同时两份强制恢复文档顶部的自动快照仍指向旧 R2 活动计划。代理即使按规则完成恢复，也可能在随后读取论文入口时重新采用已经退出的基座、数据集和章节边界。

## 审计范围与判定方法

### 完整读取的文件

1. `/Users/bilibili/.codex/AGENTS.md`
2. `AGENTS.md`
3. `output/AGENTS.md`
4. `output/开题改进交接文档.md`
5. `output/第一创新点实验总控.md`
6. `thesis/AGENTS.md`
7. `thesis/experiments/llm_probe/AGENTS.md`
8. `thesis/experiments/llm_probe/scripts/AGENTS.md`
9. `.Codex/docs/AGENTS.md`
10. `raw/AGENTS.md`
11. `wiki/AGENTS.md`

文件枚举先确认 `/opt/homebrew/bin/fd` 可用，未使用 `find`。审计前先检查当前差异；两份恢复文档和 `thesis/AGENTS.md` 已有用户修改，本轮未覆盖或回滚这些修改。

### 分类口径

- **直接冲突**：两个仍被声明为权威的规则或恢复事实会导向不同动作或研究结论。
- **过时但不冲突**：上层旧规则表述不当，但近层规则已明确覆盖，当前按优先级仍能得到唯一动作。
- **缺失但应补充**：规则要求从某一权威文件取得信息，但该文件未提供该信息，导致操作无法按规则完成。
- **通用规则无需重复**：细节已在两份强制恢复文档明确，并由根恢复协议强制读取；复制到长期 `AGENTS.md` 只会增加再次过时的风险。

## P0：阻断性直接冲突

### P0-1：论文局部规则把权威性指向仍冻结旧路线的 README

**AGENTS 位置：**

- `thesis/AGENTS.md:14`：规定“当前章节名称以 `thesis/README.md` 为准”。

**被引用文件中的冲突内容：**

- `thesis/README.md:20-21`：第三章仍是旧“队列物理约束与有界条件注意力”，第四章仍称“生成式训练方法”。
- `thesis/README.md:30-32`：仍把 `Qwen/Qwen3-0.6B` 分层生成基线写在主流水线中。
- `thesis/README.md:52-65`：仍宣称 Qwen3-0.6B 主实验基座已冻结，并把非 Qwen 迁移列为未冻结项。
- `thesis/README.md:73-77`：仍把 GeNIS、DEDALE、HIKARI、CICIoT2023、CSE-CIC-IDS2018 分配为正式证据。
- `thesis/README.md:95`：仍固定 Qwen3-0.6B 为主模型身份。

**恢复文档依据：**

- `output/第一创新点实验总控.md:22-24`：唯一公开检测数据集是 TQH-C2 v1.2.0，主基座尚未冻结，Qwen 后置。
- `output/第一创新点实验总控.md:73-76`：E2 使用共同七字段编码器，且不启用协议路由；Qwen 仅在方法冻结后跨基座验证。
- `output/开题改进交接文档.md:30-36`：第三章正在以 E2 冻结路线，第四章须等第三章冻结后承接。
- `output/开题改进交接文档.md:50-53`：TQH-C2 是唯一正式公开检测数据集，GeNIS 仅为历史档案。

**影响：** 这是当前最危险的冲突。进入 `thesis/` 的代理必须服从近层 `thesis/AGENTS.md`，会把旧 README 当作权威，重新采用已退出的 Qwen 主基座、GeNIS 正式证据和旧章节名称。

**建议最小修改：**

1. 先把 `thesis/AGENTS.md:14` 改为：章节工作标题与研究边界以两份 `output/` 恢复文档为准；`thesis/README.md` 只能在同步更新后作为目录索引。
2. 再单独更新 `thesis/README.md` 的章节表、主流水线、冻结边界、数据证据分工和服务器摘要；不要在 `AGENTS.md` 复制 E1 分数、下载百分比或运行路径。

### P0-2：两份强制恢复文档顶部自动快照仍是旧 R2 执行态

**冲突位置：**

- `output/开题改进交接文档.md:9,13`
- `output/第一创新点实验总控.md:9,13`

两处仍把 `.Codex/docs/2026-08-04-R2最终物理旁路正式实验计划.md` 标为当前活动计划，并声称“任务 3、4 正在收口、任务 5 尚未启动、QUIC 仅允许种子验证且正式权重为 0”。

**同文件恢复依据：**

- `output/开题改进交接文档.md:98-103`：E0/E1 已完成，E2 是当前路线冻结门禁，E3/E5 后置。
- `output/第一创新点实验总控.md:60-61`：QUIC 三轮及聚合门禁已通过，当前是 `pilot120`，且该问题不阻塞 E2。
- `output/第一创新点实验总控.md:112-126,145-148`：当前工作是 E2；TQH-C2 PCAP 下载与 QUIC 修复均不阻塞 E2。

**影响：** 根 `AGENTS.md:25` 要求按顺序完整读取两份恢复文档。代理最先遇到的快照与正文相反，可能恢复旧任务编号、旧 QUIC 权重和旧活动计划。

**建议最小修改：** 修复自动快照的生成来源，使其从当前总控正文或当前活动计划清单生成；一次性重写两个快照区块。不能只手工改正文，否则下一次 Hook 仍会恢复旧快照。

## P1：重要直接冲突

### P1-1：课题“固定题名”在根规则、论文规则和恢复文档之间不一致

**冲突位置：**

- `AGENTS.md:16`：把主线固定为《面向恶意流量检测的新型训练方法研究》。
- `thesis/AGENTS.md:9`：把课题固定为《面向恶意流量检测的新型大模型训练方法研究》。
- `output/开题改进交接文档.md:21-24`：固定的是“加密恶意流量检测的大模型方法及训练系统”研究对象，同时允许题目、章名和模型称谓随实验裁决修改。

**影响：** 一个代理会认为题名不可变，另一个会认为带“大模型”的另一题名不可变，而恢复文档又明确题名未冻结。

**建议最小修改：** 两级 `AGENTS.md` 只固定研究边界，不固定尚待 E2/E3 裁决的完整题名。可统一成“研究对象固定为加密恶意流量检测的训练方法及训练系统，不得转为流量生成或传统分类器论文；正式题名按两份恢复文档裁决”。

### P1-2：开题恢复文档同时允许生成式基座竞争，又把 Qwen 排除出 E2 主检测筛选

**冲突位置：**

- `output/开题改进交接文档.md:22`：生成式、编码式或双阶段系统均可竞争主路线。
- `output/开题改进交接文档.md:23,68-70`：E2 以双向编码器为主检测基座，Qwen 不参加 E2 主检测筛选，只做方法冻结后的跨基座验证。
- `output/第一创新点实验总控.md:23,82-85,112-114`：E2 只比较共同字段编码器与预注册强基线；Qwen 和 ET-BERT 均后置。

**影响：** “主基座尚未冻结”会被误读为 Qwen 仍可参加 E2，与已经冻结的比较角色冲突。

**建议最小修改：** 将 `output/开题改进交接文档.md:22` 收窄为“第三章固定保留 PINN；E2 在编码器候选与共同预算强基线中冻结主检测结构，双阶段分析与跨基座结构后置”。保留“最终主基座尚未由实验冻结”，不要写成某个具体编码器已经胜出。

### P1-3：PINN/R2 写回协议在根规则与 output 局部规则之间冲突

**冲突位置：**

- `AGENTS.md:28`：PINN/R2 裁决写回两份 `output/` 总控。
- `output/AGENTS.md:15-19`：开题交接只保存总体定位与长期纪律，实验总控保存实时状态；仅同一变化同时影响二者时同步更新。
- `.Codex/docs/AGENTS.md:24-25`：详细证据先写过程记录，再按根规则和 `output/AGENTS.md` 更新对应恢复文档。

**恢复文档依据：**

- `output/开题改进交接文档.md:16` 明确自身只保存长期定位，并把实时状态指向实验总控。
- `output/第一创新点实验总控.md:16` 明确自身只保存实时裁决、证据、任务和下一门禁。

**影响：** 根规则会促使代理把每个运行错误、短期进度或实验路径同步到两份文件，破坏 `output/AGENTS.md` 已建立的职责分离。

**建议最小修改：** 把 `AGENTS.md:28` 改成“PINN/R2 确定变化按 `output/AGENTS.md` 分流写回；同时影响课题定位和实验状态时才同步两份恢复文档”。

## P2：一般直接冲突

### P2-1：一次有界重构的触发范围不一致

**冲突位置：**

- `output/开题改进交接文档.md:102`：E2 或 E3 失败均可触发一次有界重构。
- `output/第一创新点实验总控.md:120-122`：只写 E3 失败时触发。

**影响：** 如果 `E2-P` 未通过门槛，代理无法唯一判断是直接收缩主张、进入一次重构，还是继续 E3。

**建议最小修改：** 依据当前路线裁决统一触发条件。若当前意图确为“E2/E3 任一关键门禁失败后最多一次”，只修正实验总控 `E4` 首句；无需在长期 `AGENTS.md` 复制具体阶段编号。

## 过时但因近层规则覆盖而暂不构成当前冲突

### G-1：全局格式化、静态检查和回滚规则过宽

**位置：**

- `/Users/bilibili/.codex/AGENTS.md:83-87`：任何修改在汇报前都必须格式化、静态检查，失败后立即执行 `git checkout -- <file>`。

**近层约束：**

- `AGENTS.md:45-47`：只读盘点和复审不补造测试；只有会使实验无效的问题才暂停受影响实验。
- `thesis/experiments/llm_probe/AGENTS.md:29`：精确目标测试通过后，不以无关完整测试、重复检查或格式化延迟实验。
- `thesis/experiments/llm_probe/scripts/AGENTS.md:11`：Shell 只在交付、审查或提交前集中一次 `bash -n`。

**判定：** 近层规则优先后，本仓库仍可得到“只运行最小相关验证”的唯一动作，因此未计入直接冲突。但全局规则中的 `git checkout -- <file>` 可能覆盖用户未提交修改，不应作为自动失败处理。

**建议最小修改：** 在全局规则中限定“仅格式化实际修改的源码；仅运行与变更相关的静态检查；Markdown 报告用 `git diff --check` 等文档检查即可”。删除自动 `git checkout`，改为停止并报告错误，待确认文件所有权后再处理。

## 缺失但应补充

### M-1：规则要求从路线总控读取服务器与凭据键，但总控没有该入口

**规则位置：**

- `AGENTS.md:33`：远程动作前从所属路线总控读取当前服务器与凭据键的规范来源。
- `thesis/experiments/llm_probe/AGENTS.md:15`：当前服务器和凭据键只从所属路线总控读取。

**缺失证据：**

- `output/第一创新点实验总控.md:18-25,139-148` 只有研究状态与活动任务，没有当前服务器别名、服务器项目根或凭据环境变量键的规范入口。
- 两份恢复文档中“服务器”只出现在自动快照提示，未给出可执行的规范来源。

**影响：** 按强制规则，代理不能安全确定该连接哪台服务器或从哪个环境变量键取凭据；这会阻塞后续需要 GPU 的 E2 工作。该缺失不应通过猜测旧服务器、读取历史命令或打印环境值解决。

**建议最小修改：** 在 `output/第一创新点实验总控.md` 增加一个短小的“当前计算环境”区块，只记录已核验的服务器别名、服务端项目根、凭据**键名**或密钥存储入口、GPU 当前是否需要，以及详细运行合同的相对链接；禁止写入地址解析值、密码、令牌或短期进程号。长期 `AGENTS.md` 继续只保留“从总控读取”的通用规则。

## 分维度一致性矩阵

| 审计维度 | 判定 | 主要依据 | 处理建议 |
| --- | --- | --- | --- |
| 课题定位 | **直接冲突** | `AGENTS.md:16`、`thesis/AGENTS.md:9`、开题交接 `21-24` | 只固定研究边界，题名留给恢复文档裁决 |
| 第三、四章关系 | 一致 | `thesis/AGENTS.md:10-12,33`；开题交接 `30-36` | 无需修改 |
| PINN 固定边界 | 一致 | `thesis/AGENTS.md:10,16-23`；实验总控 `21,73-78` | 无需修改 |
| 唯一公开数据集 | 一致，但被旧 README 反向引用污染 | `thesis/AGENTS.md:27-29`；开题交接 `46-55` | 修正 `thesis/AGENTS.md:14` 的权威指向 |
| GeNIS 历史角色 | 一致，但被旧 README 反向引用污染 | `thesis/AGENTS.md:28`；开题交接 `53` | 同上 |
| 主基座尚未冻结 | AGENTS 主体一致；恢复文档自身有冲突 | `thesis/AGENTS.md:10`；实验总控 `23`；开题交接 `22-23` | 收窄开题交接第 22 行 |
| E2 编码器与强基线 | 一致，**无需在长期规则复制模型清单** | `thesis/AGENTS.md:10`；实验总控 `82-85,112-114` | 具体清单继续只放实验总控 |
| Qwen 后置跨基座 | 一致，但被旧 README 反向引用污染 | `thesis/AGENTS.md:10`；实验总控 `23,85` | 修正 README 权威指向并同步 README |
| E2 A/S/P/X | 一致，**通用规则无需重复** | 开题交接 `100`；实验总控 `73-76,83,114`；根恢复协议 `25` | 保留在实时总控，不复制到长期 AGENTS |
| QUIC 后置且不阻塞 E2 | 正文一致；顶部快照直接冲突 | 开题交接 `76-77,101-103`；实验总控 `60-61,75-76,118,148` | 修复自动快照来源；不把 `pilot120` 数值写入 AGENTS |
| 最终测试隔离 | 一致 | `llm_probe/AGENTS.md:21-25`；开题交接 `89-94`；实验总控 `130-135` | 无需修改 |
| GPU／服务器键 | **缺失但应补充** | 根规则 `33`、`llm_probe/AGENTS.md:15-19`；实验总控未提供入口 | 只在实时总控补服务器别名和凭据键名 |
| 格式化／测试／复审边界 | 近层一致；全局规则过时 | 全局 `83-87`、根 `45-47`、`llm_probe/AGENTS.md:29` | 收窄全局规则，禁止自动覆盖回滚 |
| 恢复协议 | **直接冲突** | 根 `25-28`、`output/AGENTS.md:15-19`、`.Codex/docs/AGENTS.md:24-25` | 根规则改为按 output 局部职责分流 |

## 不建议写入长期 AGENTS 的短期内容

以下内容应继续留在 `output/第一创新点实验总控.md` 或详细记录中，不要复制到长期规则：

- B PCAP 下载百分比、QUIC `pilot120` 轮次和失败计数。
- E1 的具体 F1、召回率和字段删除变化数值。
- E2 的拟合次数上限、种子扩展进度、当前运行目录和短期状态。
- 当前服务器进程、GPU 利用率、磁盘瞬时值和运行身份。

长期规则只需保留：唯一数据集与证据角色、最终测试隔离、禁止快捷字段、PINN 的可微与守恒边界、恢复顺序、凭据不落盘、服务器信息从实时总控读取、最小相关验证和写回职责分离。

## 变更与验证记录

- **新增文件**：`.Codex/docs/2026-08-06-各级AGENTS与强制恢复文档一致性审计.md`。
- **未修改文件**：全部被审计的 `AGENTS.md`、两份强制恢复文档及 `thesis/README.md`。
- **格式化**：本轮只新增 Markdown 报告，没有适用的源码格式化器；已逐行回读全文。
- **结构检查**：`rg --count '^### P[012]-'` 返回 `6`，与“P0 2 项、P1 3 项、P2 1 项”一致。
- **空白检查**：`rg --line-number '[[:blank:]]+$'` 无输出；退出码 `1` 表示未匹配到尾随空白。
- **代码、静态检查与测试**：本轮没有修改源码、配置或脚本，因此未运行代码格式化、编译、静态检查或测试；这符合根规则对只读审计不补造测试的边界。

## 总体审查裁决

**请求修改。** 在 P0-1 与 P0-2 修复前，不应把当前规则链视为可安全恢复 E2 的唯一来源；尤其不应依据 `thesis/README.md` 启动 Qwen／GeNIS 旧路线，也不应依据顶部 PreCompact 快照恢复旧 R2 任务。P1、P2 问题不会立即破坏现有实验制品，但会造成后续分派、写回和一次重构门槛分歧，建议同一轮最小修订完成。
