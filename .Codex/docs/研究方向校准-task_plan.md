# 任务计划：研究方向与第三、四章编排校准

## 目标

将当前已确认的研究定位、第三与第四章递进关系、待实验决策项同步到论文索引和交接文档，同时保留旧方案素材。

## 阶段

- [x] 阶段1：核对现有论文索引、旧创新点草稿和交接文档
- [x] 阶段2：形成“已确定 / 暂定 / 待验证”三层决策记录
- [x] 阶段3：更新 `thesis/README.md` 与 `output/开题改进交接文档.md`
- [ ] 阶段4：检查术语、章节编号、旧方案状态和 Markdown 格式

## 关键问题

1. 如何确保论文始终围绕“新型恶意流量大模型训练方法”，而不是两级检测系统？
2. 如何在基座模型和 GRPO 尚未实验选定时，给出稳定但不过度承诺的第三、四章结构？
3. 如何保留已有 CI-PRD、PIC-GRPO、数据集和实验材料，避免误当作已验证贡献？

## 已作决策

- 研究对象固定为同一个恶意流量检测基座模型及其新型训练方法。
- 第三章解决检测效果、泛化或物理一致性问题；第四章解决第三章暴露的困难样本、长尾、未知攻击或训练优化问题。
- 第四章应沿用第三章的任务、数据与主基座，并以第三章模型为起点或直接解决其局限。
- 旧“CI-PRD 初筛 + PIC-GRPO 精判”两级部署方案降为历史候选，不再作为当前主线。
- 基座模型、输入模态、物理约束形式和第二创新点算法，均由后续竞速实验决定；`GRPO` 不是硬性要求。
- 构思第三、四章时参考朱焱雷的问题递进、同一基座、机制协同和完整论证方式，但不复用具体方法。
- 最终每个创新点必须形成有真实机制增量的新算法，正式名称在消融验证后确定。

## 遇到的错误

- 无法创建 `.Codex/docs/研究方向校准/` 子目录：沙箱返回 `Operation not permitted`。改为直接在 `.Codex/docs/` 下创建带主题前缀的规划文件。

## 状态

**当前处于阶段4**：正在执行术语一致性与 Markdown 格式检查。
