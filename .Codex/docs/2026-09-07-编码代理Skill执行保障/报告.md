# 编码代理 Skill 执行保障报告

## 实际读取

- `daily-coding`：`/Users/bilibili/.codex/skills/daily-coding/SKILL.md`，SHA256 `c550a289d2993a9d9c1e17fe2661cff3bc4022d8ed977a0bf8fea9e1ef485f01`。
- `agent-identifier`：`/Users/bilibili/.agents/skills/agent-identifier/SKILL.md`，SHA256 `e4b95d1aa05ed0edf1ddd126ced10998a914b98e8c2931ce788938552fa51a56`。
- `codex-hook-emulation`：`/Users/bilibili/.codex/skills/codex-hook-emulation/SKILL.md`，SHA256 `84011df1cfed6e231a29f4c410c3b57c37531add2db662af1742f7f173470be9`。
- `hook-development`：`/Users/bilibili/.agents/skills/hook-development/SKILL.md`，SHA256 `3db8b26f91d0023caeee6e6e22de896067312423f24f55fbc914260e0247ca06`。

## 实现

- 根规则要求生产代码子代理提供动态 Skill 收据，主代理只运行一次验证器。
- 详细合同、JSON 模板和只读验证器已新增；验证器不执行收据命令或网络访问。
- 当前 Hook/代理配置没有可靠的 spawn 前拦截，故未伪称自动注入；Hook 只能作提醒，不替代收据。

## 验证

- `/tmp` 有效收据通过；缺 Skill、Skill SHA256 错误、`actual_changed_files` 越权三类收据均被拒绝，临时文件已清理。
- `py_compile` 和 `--help` 通过；验证器不执行收据中的命令。
- 当前 Hook 仅匹配 Bash 或编辑工具；没有可靠 spawn 前简报拦截，故以根规则和收据验收为真实机制。
- 最长实验脚本链为 `28,477` 字节（根 `14,802` + 论文 `2,684` + 实验 `9,251` + 脚本 `1,740`），距 `28,500` 目标余 `23` 字节；`git diff --check` 通过。
