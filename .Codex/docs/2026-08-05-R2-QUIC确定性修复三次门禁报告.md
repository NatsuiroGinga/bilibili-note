# R2 QUIC 确定性修复三次门禁报告

## 结论

三次串行门禁均通过，机器审计器唯一状态为 `GO_TO_INDEPENDENT_REVIEW`，审计器退出码为 `0`。本结果只允许进入新的独立复审；**未恢复剩余 28 条或原 40 条字段矩阵**，QUIC 的正式训练权重、`formal` 状态及 `UNKNOWN` 回退不因本报告改变。

## 静态与同步门禁

- 有效 Python 编译：`PYTHONPYCACHEPREFIX=/tmp/r2-quic-pycache python3 -m py_compile ...`，退出码 `0`。此前一次未设置临时缓存的调用因本机沙箱禁止写入 macOS 全局字节码缓存失败，未产生源码语法诊断；控制器已裁定该环境失败不计为有效门禁。
- Shell 语法：`bash -n thesis/experiments/llm_probe/scripts/run_r2_quic_controlled_collection.sh`，退出码 `0`。
- 配置合同：`jq -e` 精确核验 240 秒上限、序号 12、固定端点和双向种子，退出码 `0`。
- 精确语义搜索：`rg` 核验固定调度模式、240 秒、门禁状态及端口，退出码 `0`。
- 以下五个白名单文件已通过 `/tmp/gpu-rsync-push-378.exp` 同步，服务器 SHA-256 与本机 5/5 一致：
  - `scripts/r2_quic_udp_proxy.py`：`a03861337393a4be634ea36e122df84a5de158a689c7b2c3662984bb2a255258`
  - `scripts/r2_quic_controlled_collect.py`：`ec281032ec05336b2edd666e0ccb9cfd94886448b0e5eba9d06c6d63607e5046`
  - `scripts/run_r2_quic_controlled_collection.sh`：`6b1768c15b0104fa78ed8e36df206279d3df788d9bbf7b85c1dfe7aa282dbc66`
  - `scripts/audit_r2_quic_determinism_gate.py`：`d58b1cd5bc2cf24deef20cbce1c1d2144e74b17037f85e86163efa3e8ec3b4d7`
  - `configs/r2_quic_controlled_determinism_repair_v2.json`：`185317d6ef27476c533a1f9a5797e09bc6c7f9d31a4627a7fc5247410176c02d`

## 三次门禁

| 运行 | 启动器退出码 | 四类连接退出码 | 结果根 |
| --- | --- | --- | --- |
| `run1` | `driver_exit=0`，`tee_exit=0` | 客户端、抓包器、代理、服务端均为 `0` | `/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-raw/r2-quic-determinism-repair-v2-run1/` |
| `run2` | `driver_exit=0`，`tee_exit=0` | 客户端、抓包器、代理、服务端均为 `0` | `/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-raw/r2-quic-determinism-repair-v2-run2/` |
| `run3` | `driver_exit=0`，`tee_exit=0` | 客户端、抓包器、代理、服务端均为 `0` | `/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-raw/r2-quic-determinism-repair-v2-run3/` |

三轮均使用 `aioquic/p03/seed43/bulk`、`fixed_sequence_epoch_v1` 与 240 秒客户端上限。每轮响应均为 `67,108,864` 字节，SHA-256 均为 `af2f4dcdb15a8bdad0dabda5228554cf497a05e4767408953c3759e423165da7`；`payload_decrypt_error=0`、随机丢弃/队列丢弃/发送错误/关停残留队列均为 `0`，四个固定端口在连接前后均可独占绑定。

## 审计证据

- 唯一门禁摘要：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-raw/r2-quic-determinism-repair-v2-gate-summary.json`
- 三轮共用连接标识：`b74dd9cdddca8667358671db`。
- 三轮共用四元组合同 SHA-256：`106d856a4127c1a9c64e5ec484193f782e57b0837d0dce9b3bc114862c00b6d4`。
- 三轮共用代理配置 SHA-256：`0cc60c6f53cac94ebc0474191a69fb91380e6bd4826c42e6153bbd620a82a9f1`。
- 三轮共用集合配置 SHA-256：`185317d6ef27476c533a1f9a5797e09bc6c7f9d31a4627a7fc5247410176c02d`。
- 审计器的逐轮 21 项检查与跨轮 8 项一致性检查均为真，未执行自动运行、重试或制品删除。

## 下一步

请求新的独立审查代理逐条复核本次确定性修复与三次门禁合同。在独立复审通过前，不启动或恢复任何其他 QUIC 字段矩阵运行。
