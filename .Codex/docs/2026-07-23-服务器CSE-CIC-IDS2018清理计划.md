# 服务器 CSE-CIC-IDS2018 清理计划

## 目标

仅删除 GPU 服务器 `/root/autodl-tmp/thesis/datasets/` 下名称明确匹配 `CSE-CIC-IDS2018` 或 `CSE-CIC-IDS2018-PCAP` 的原始目录或归档，并记录审计、删除和空间验证结果。

## 阶段

- [x] P0：确认授权、路径边界和服务器连接变量。
- [x] P0：使用 `fd` 与 `du` 只读审计精确候选路径、类型和大小。
- [x] P0：逐项处理已核实候选；候选数为 0，因此未运行删除命令。
- [x] P0：使用 `fd` 验证无匹配，并使用 `df -h` 核对空间变化。
- [x] P1：写入清理记录，运行 Prettier 与 `git diff --check`。

## 决策

- 用户已明确授权删除服务器原始数据，无需再次确认。
- 本机 `/Users/bilibili/personal/note/raw/datasets/` 及服务器实验、运行、模型、代码和其他数据集全部不在操作范围内。
- 候选路径不在指定根目录或名称存在歧义时立即停止。

## 错误记录

- 服务器非交互 shell 中直接调用 `fd` 首次报错。交互式核对确认 `fd` 是指向 `/usr/bin/fdfind` 的别名，后续审计使用该可执行文件完成。

## 状态

全部阶段已完成。
