# 第三章数学公式更新记录

## 输入

- 规范：`/Users/bilibili/Downloads/latex.md`
- 当前目标：`/Users/bilibili/personal/note/thesis/03-基于队列物理约束与有界条件注意力的恶意流量检测方法.md`

## 发现

- 规范使用 `$...$` 表示行内公式，使用 `$$...$$` 表示独立公式。
- 目标文档原有 25 个带编号的独立公式，其中式（3-1）已使用 `$$`，但公式内容外多了一对普通方括号；其余独立公式使用 `\[...\]`。
- 目标文档的行内公式统一使用了 `\(...\)`。
- 本次只更新公式定界符和式（3-1）的多余包裹符号，没有改动公式语义、编号、正文和章节结构。

## 验证

- 旧式定界符 `\(...\)`、`\[...]`：0 处。
- `$$` 定界符行：50 行，对应 25 个独立公式块。
- 公式编号：25 个，范围为式（3-1）至式（3-25）。
- 独立公式定界符配对：通过。
- 行内 `$` 定界符配对：通过。
- 孤立普通方括号行：0 处。
- `prettier --write`：通过。
- `git diff --check`：通过。
