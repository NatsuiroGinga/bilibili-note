# 第4章 CI-PRD：守恒不变量物理残差检测器（创新点1）

> **方向校准（2026-07-16）**：本文件是历史候选方案素材。当前课题研究同一个恶意流量检测基座模型的新型训练方法，不再把 CI-PRD 作为独立的第一级检测器。可复用内容仅限经重新验证的物理约束、公式和探索实验。当前第三、四章规划见 [[当前研究定位与第三四章编排]]。
>
> 状态：🟡 草稿中（4.1/4.2/4.3/4.4 已写，4.5-4.8 待写）｜ 术语见 [[开题术语体系]] ｜ 公式见 [[守恒不变量物理公式]] ｜ 模板参考朱焱雷 Ch3（仅结构）
> 命名：CI-PRD = Conservation-Invariant Physics Residual Detector（守恒不变量物理残差检测器），**非 TD-PRD**（TD-PRD 硬编码已否决的方案 A TCP 流体）。

## 4.1 引言

网络攻击检测长期面临两类困难。其一，传统基于特征匹配的入侵检测系统依赖已知攻击签名，难以应对零日漏洞与变种攻击 [cite:Hubballi2014-survey]。其二，近年来基于深度学习与大语言模型的数据驱动检测器虽具备语义理解能力，但其性能高度依赖大规模标注数据，在小样本与分布外（out-of-distribution, OOD）攻击场景下泛化能力显著下降——模型倾向于记忆训练集中的攻击特征，而非学习攻击应满足的底层规律。

物理信息神经网络（Physics-Informed Neural Networks, PINN）为缓解上述问题提供了新思路。PINN 通过自动微分将物理定律以残差形式嵌入损失函数，使模型在拟合数据的同时遵守先验物理约束，从而以更少数据学到更具泛化能力的表示 [cite:Raissi2019-PINN]。然而，现有 PINN 主要用于求解偏微分方程的正问题与逆问题，其在网络攻击检测中的专用化研究仍不充分：少数工作将物理信息引入工业控制、物联网等含真实物理过程的场景 [cite:PIGCRN; cite:INVARLLM]，但通用网络流量检测缺乏可计算的"物理"定义，且既有工作存在物理约束空泛、与检测目标耦合不足等问题（见 [[PPINN-FDIA-工业IoT]] 反例）。

本章针对这一空白，提出**守恒不变量物理残差检测器** CI-PRD（Conservation-Invariant Physics Residual Detector）。其核心思想是：将网络流量视作图上的连续流，以**流量连续性方程（质量守恒）**及其离散化形式（节点流量守恒、流双向守恒、TCP 状态守恒、熵平稳性）为物理约束，构造 PINN 检测器；模型在"物理一致"子空间预测，攻击打破守恒律则物理残差升高，残差幅值即异常得分。与纯数据驱动方法相比，CI-PRD 以物理约束减少对标注数据的依赖；与通用 PINN 相比，CI-PRD 将残差直接用作检测判据而非求解目标，是 PINN 在入侵检测场景的专用化设计。

本章贡献为：（1）定义一组可计算的网络流量守恒不变量，并明确其物理合法性与适用边界；（2）提出以守恒残差为软约束的 CI-PRD 检测框架与联合损失；（3）在真实 IDS2018 数据上验证守恒不变量对传输层洪泛/扫描攻击的可分性。

## 4.2 问题定义与威胁模型

### 4.2.1 检测任务数学描述

设网络流量观测为按时间窗 $\Delta$ 切分的流/包级特征序列 $X=\{x_t\}_{t=1}^{T}$，其中每条流 $f$ 含五元组、方向、字节数、包数、TCP 标志、时间戳等字段。检测任务为二分类（或多分类）映射：
$$\hat y = g_\theta(X), \quad y \in \{\text{Benign}, \text{Attack}\} \text{（或细粒度攻击类型）} \tag{4.8}$$
CI-PRD 作为两级架构的第一级，输出的是**异常得分** $s=\|\mathrm{res}(X)\|$（守恒残差幅值）而非直接分类：$s$ 低于安全阈值判为正常流量放行，高于高危阈值判为明确攻击，处于灰区则送第二级 PIC-GRPO 语义精判（见 [[05-PIC-GRPO-创新点2]]）。这一设计使第一级免于大模型调用、可毫秒级初筛。

### 4.2.2 威胁模型

本章针对以下攻击类型：

- **DDoS 洪泛**（如 HOIC、LOIC-UDP）：多对一单向洪泛，打破流双向守恒（I2）与节点流量守恒（I1）。
- **端口扫描**：一对多低流量探测，打破流双向守恒（I2）与目的熵平稳（I4）。
- **SYN flood / 慢速半连接**（如 Slowloris）：打破 TCP 状态守恒（I3）。
- **变种/OOD 攻击**：训练时未见过的协议变种与混淆载荷，用于检验泛化。

**适用边界（关键）**：守恒不变量对上述**传输层**攻击有效；对**应用层 DoS**（如 HTTP 洪水 GoldenEye，完成完整 TCP 握手且双向交互，不打破传输层守恒）则无效。这一边界已由真实 IDS2018 数据的跨攻击类型实验验证（见 [[experiments/README|实验 v3]]：单向 DDoS 召回 76–100%、误报 0.6%，而 HTTP 层 DoS 不可分）。应用层攻击的检测职责由第二级 PIC-GRPO 的语义推理承担，两级形成互补分工。

### 4.2.3 物理"方程"选型决策

CI-PRD 的可行性取决于"物理"是否可从数据计算。两个候选方案经 IDS2018 字段普查实证裁决（见 [[IDS2018字段普查报告]]）：

- **方案 A（TCP/AQM 流体方程）**：以 TCP 窗口 $W(t)$、队列 $q(t)$、RTT、丢包概率 $p(t)$ 的耦合 ODE 为物理约束。**否决**——IDS2018 流级 CSV 不含 $W$/$q$ 时序；即便改用原始 PCAP，$q(t)$、$p(t)$ 为路由器内部状态，端点抓包仍不可观测，残差无法算全。且 IDS2018 含约 19% UDP 流，不服从 TCP 流体方程。
- **方案 B（守恒不变量）**：以质量守恒（连续性方程）及协议/统计不变量为约束。**采纳**——I2/I3/I4 在 IDS2018 流级或包级可算，且实验证明对单向洪泛攻击可分。

故本章采用方案 B，守恒不变量定义见 4.3 节。方案 A 的 TCP 流体 ODE 保留为 I5，仅作消融对比（4.3.5 节）。

## 4.3 守恒不变量定义（核心）

CI-PRD 的"物理"由一组**守恒不变量**构成。按物理合法性分两层：I1 为基本物理律（质量守恒，连续性方程），I2–I4 为领域/协议不变量，I5 仅作消融对比。网络流量被建模为图 $G=(V,E)$ 上的连续流，对链路 $e$ 定义流量密度 $\rho_e(x,t)$、传播速度 $u_e(x,t)$、通量 $J_e=\rho_e u_e$。

### 4.3.1 I1 节点流量守恒（质量守恒，连续性方程）

流量连续性方程（质量守恒）为：
$$\frac{\partial \rho_e}{\partial t} + \frac{\partial J_e}{\partial x} = s_e(x,t) \tag{4.1}$$
其中 $s_e$ 为源/汇（流量产生/丢弃）。该式与流体力学中 PINN 所解的连续性方程同构，是 CI-PRD 物理合法性最强的约束。在节点 $v$ 处离散为 Kirchhoff 电流定律，对时间窗 $\Delta$：
$$\mathcal{R}_{I1}(v,\Delta) = \sum_{i \in \mathrm{in}(v)} B_i^{in}(\Delta) - \sum_{j \in \mathrm{out}(v)} B_j^{out}(\Delta) - B^{drop}(v,\Delta) - \Delta q(v,\Delta) \tag{4.2}$$
正常稳态转发节点 $\mathcal{R}_{I1}\approx 0$；DDoS 受害节点流入 $\gg$ 流出，$\mathcal{R}_{I1}$ 显著为正。**可算性**：需多节点 per-port 字节计数，自构造测试床可算，IDS2018 端点不可算。

### 4.3.2 I2 流双向守恒（协议交互不变量）

对客户端-服务端流 $f$，请求-响应字节应满足协议规定的有界比：
$$\mathcal{R}_{I2}(f) = \frac{B_{fwd}(f)}{B_{fwd}(f) + B_{bwd}(f) + \varepsilon} \in [r_{\min}, r_{\max}] \tag{4.3}$$
正常服务双向交互，$\mathcal{R}_{I2}\in[r_{\min},r_{\max}]$（协议相关，如 HTTP 约 $[0.3,0.7]$）；单向洪泛/扫描 $B_{bwd}\approx 0$，$\mathcal{R}_{I2}\to 1$，违反区间。残差 $\mathrm{res}_{I2}(f)=\max(0,\,r_{\min}-\mathcal{R}_{I2},\,\mathcal{R}_{I2}-r_{\max})$。**可算性**：`TotLen Fwd/Bwd Pkts`、`Down/Up Ratio`，IDS2018 流级可算。**实验已证**：对单向 DDoS 洪泛（HOIC/LOIC-UDP）召回 76–100%、误报 0.6%（见 [[experiments/README|实验 v3]]）。

### 4.3.3 I3 TCP 状态守恒（协议状态机不变量）

TCP 连接须遵循状态机：每个 SYN 应有匹配 SYN-ACK 完成握手。对时间窗 $\Delta$：
$$\mathcal{R}_{I3}(\Delta) = \frac{\#\mathrm{SYN}_{unanswered}(\Delta)}{\#\mathrm{SYN}(\Delta) + \varepsilon} \tag{4.4}$$
正常 $\mathcal{R}_{I3}\approx 0$；SYN flood / Slowloris 半开 $\mathcal{R}_{I3}\to 1$。**可算性**：严格形式需逐包 TCP 标志，PCAP 可算（已从 IDS2018 截断包恢复真实 pcap）；流级弱近似用 `SYN Flag Cnt` 与反向包数。

### 4.3.4 I4 熵平稳性（统计不变量）

对窗口 $\Delta$ 的目的端口/IP 分布熵：
$$H_{dst}(\Delta) = -\sum_{p} P(p|\Delta)\log P(p|\Delta), \quad \mathcal{R}_{I4}(\Delta) = \frac{|H_{dst}(\Delta) - \bar H_{dst}|}{\bar H_{dst} + \varepsilon} \tag{4.5}$$
正常 $H_{dst}$ 平稳；DDoS 目的集中→$H_{dst}$ 骤降，端口扫描目的分散→$H_{dst}$ 骤升，均使 $\mathcal{R}_{I4}$ 增大。**可算性**：`Timestamp`+`Dst Port`，IDS2018 可算。

### 4.3.5 I5 TCP/AQM 流体 ODE 残差（消融对比，方案 A）

Misra 型流体方程：
$$\dot W(t) = \frac{1}{R(t)} - \frac{W(t)W(t-R)}{2R(t-R)}p(t-R), \quad \dot q(t) = N\frac{W(t)}{R(t)} - C \tag{4.6}$$
残差为模型预测与该 ODE 的偏差（自动微分）。**可算性**：$W$ 可由 seq/ack 推断、$R$ 可观测，但 $q(t)$、$p(t)$ 为路由器内部状态，端点 PCAP 不可观测 → IDS2018 不可算，仅测试床消融，用于证明守恒不变量（I1–I4）优于 TCP 流体残差。

### 4.3.6 不变量→攻击映射与适用边界

| 不变量          | 物理定位             | 可算数据     | 有效攻击                  |
| --------------- | -------------------- | ------------ | ------------------------- |
| I1 节点守恒     | 基本物理（质量守恒） | 测试床       | DDoS（节点失衡）          |
| I2 流双向守恒   | 协议交互不变量       | IDS2018 流级 | 单向洪泛/扫描（实验背书） |
| I3 TCP 状态守恒 | 协议状态机不变量     | PCAP         | SYN flood/半开            |
| I4 熵平稳       | 统计平稳性           | IDS2018      | DDoS/扫描                 |
| I5 TCP 流体 ODE | 动力学律（TCP专属）  | 仅测试床     | （消融）                  |

**适用边界（诚实）**：守恒不变量对**传输层洪泛/扫描**有效；对**应用层 DoS**（如 HTTP 洪水 GoldenEye、慢速 Slowloris，完成握手、双向交互，不打破传输层守恒）无效——此类交由第二级 PIC-GRPO 语义精判（两级分工，见 [[05-PIC-GRPO-创新点2]]）。该边界已由 [[experiments/README|实验 v3]] 跨攻击类型对比验证。

## 4.4 CI-PRD 方法框架

### 4.4.1 总体结构

CI-PRD 以神经网络 $\hat\rho_\theta$ 逼近流量场，将 4.3 节可计算的不变量残差作为物理损失软约束，联合数据拟合损失训练。攻击流量打破守恒律，使模型预测的物理残差升高，残差幅值即异常得分。结构上分三路：流量场预测主干、不变量残差计算（自动微分）、异常得分输出。

### 4.4.2 物理残差计算（自动微分）

对每个可算不变量，由 $\hat\rho_\theta$ 的输出经自动微分求残差：I1 由时空导数按式 (4.1) 求；I2–I4 由流/窗口统计按式 (4.3)–(4.5) 求。残差汇总为物理损失项。

### 4.4.3 CI-PRD 联合损失

$$\mathcal{L}_{CI\text{-}PRD} = \mathcal{L}_{data} + \lambda_1 \mathrm{res}_{I1} + \lambda_2 \mathrm{res}_{I2} + \lambda_3 \mathrm{res}_{I3} + \lambda_4 \mathrm{res}_{I4} \tag{4.7}$$
测试床数据启用 I1（连续性方程，强物理）；IDS2018 启用 I2/I3/I4（协议/统计不变量）。$\lambda_1$–$\lambda_4$ 为平衡系数。异常得分 $s = \|\mathrm{res}\|$，超阈则判可疑并送第二级。

### 4.4.4 LLM 辅助不变量提取

借鉴 [[INVARLLM-物理不变量提取]]：用大模型从协议规范（RFC）、部署文档中提取候选守恒不变量的假设形式，再用历史数据做因果验证（PCMCI+ 启发的统计检验）剔除幻觉不变量，保留数据支持的不变量注入式 (4.7)。这使不变量集可随协议/场景自适应，而非全靠人工预设。

### 4.4.5 与通用 PINN 的区别

通用 PINN 解 PDE 的正/逆问题，目标是拟合解函数；CI-PRD 把 PINN 改造为**异常检测器**——以残差幅值直接作判定依据，物理律作软约束把模型限制在"物理一致"子空间，攻击打破物理 → 残差升高。这一改造参考 [[PIGCRN-化工过程攻击检测]]（PINN-IDS 专用化）与 [[INVARLLM-物理不变量提取]]（不变量残差检测）。

## 4.5 算法实现与模型适配

- 流级（CSV）与包级（PCAP）两种输入适配
- 不变量在不同数据形态下的可计算性（见普查报告）

## 4.6 理论分析

- 残差→异常得分的合理性
- 小样本优势论证（参考 [[PIGCRN-化工过程攻击检测]]、[[INVARLLM-物理不变量提取]]）

## 4.7 实验评估

- 4.7.1 实验设置（IDS2018 + 自构造测试床）
- 4.7.2 综合检测性能对比（vs 纯数据驱动基线）
- 4.7.3 消融（I1-I5 各不变量贡献；含 I5 TCP 流体作消融对比）
- 4.7.4 小样本/分布外泛化
- 4.7.5 不变量残差可视化
- 4.7.6 真实物理受限环境（PCAP）

## 4.8 本章小结

## 待办

- [x] I2/I3 在 IDS2018 上的可分性验证（实验 v3 完成：单向 DDoS 召回 76-100%、误报 0.6%；应用层 DoS 无效→交 LLM）
- [x] 方案 B 物理合法性补强（I1 连续性方程=质量守恒，4.3 节已形式化；I2-I4 如实标为协议/统计不变量）
- [ ] I3 严格形式在恢复的 pcap 上的逐包验证（未答 SYN 时间窗尖峰）
- [ ] I4 熵平稳可分性补测（未单独实验）
- [ ] 引用核验：[[INVARLLM-物理不变量提取]]、[[PIGCRN-化工过程攻击检测]] 等所引文献；4.1/4.2 中 `[cite:...]` 占位需在参考文献章统一赋 cite key
- [ ] 4.5-4.8 续写（算法实现/理论分析/实验/小结）
