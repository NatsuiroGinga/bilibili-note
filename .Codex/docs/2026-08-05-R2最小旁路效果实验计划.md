# R2 最小旁路效果实验计划

## 目标

用最少但可复用的 GPU 运行回答：真实 TCP/UDP 物理旁路是否比等容量基线和置换旁路改善恶意流量检测与物理一致性。

## 冻结范围

- 组别：`F-A`、`F-P`、`F-S`。
- 种子：`42`。
- `QUIC`：未放行，统一路由为 `UNKNOWN`。
- 数据：冻结 `train-fit`、`calibration`、`dev-validation` 和已通过独立复审的 TCP/UDP v2 正式物理池。
- 三组共享模型、参数预算、训练预算、优化器、早停、样本顺序和评价脚本。
- 最终测试不可见。

## 执行步骤

- [ ] 将 v2 物理池路径、模式、清单哈希和载荷 Merkle 绑定角色清单与模型加载器。
- [ ] 生成三组独立配置、运行目录、SwanLab 标识和参数预算收据。
- [ ] 同步 GPU 运行必需的最小制品，执行正式包装器运行时断言。
- [ ] 在新服务器运行三组种子 42 实验，保留日志、检查点、逐样本预测和资源指标。
- [ ] 比较宏平均 F1、状态误差、物理残差、时间和显存，给出继续或停止裁决。

## 复用规则

若后续不修改数据、代码、配置、训练预算、检查点选择和评价协议，这三次运行可直接作为正式消融的种子 42 结果；否则只作为探索证据。

## 停止条件

- 任一组读取最终测试、哈希不一致或参数预算不等，立即作废三组。
- `F-P` 未同时表现出检测非退化和物理指标改善时，不启动剩余 12 个完整候选运行。

## 当前状态

**正在准备模型角色绑定和三组启动包。**
