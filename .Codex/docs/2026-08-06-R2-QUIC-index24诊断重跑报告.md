# R2 QUIC 第 24 条诊断重跑报告

## 结论

本次唯一诊断重跑**没有复现**旧正式运行的 600 秒客户端超时。同一冻结样本 `aioquic / pilot03 / seed=43 / bulk` 在 120.644 秒内自然完成，客户端下载并校验了 67,108,864 字节响应。

该结果只说明第 24 条样本在相同代码、配置和科学参数下并非必然超时。单次成功不足以确定旧超时的根因，也不能把独立诊断结果自动并入旧的 120 条正式矩阵。按任务约束未进行第二次重跑。

## 执行边界

- 阶段固定为 `formal`。
- 目标固定为 `only_index=24`。
- 客户端硬超时保持 600 秒。
- 使用既有 v5 配置与采集器，不修改配置、样本、超时、工具或科学参数。
- 使用全新启动身份与输出目录，不覆盖旧的 23 条成功连接和第 24 条失败证据。
- 任务只使用服务器 CPU、网络和磁盘，不使用 GPU，不触碰 E2 或 ns-3 v3。

## 绑定核验

本地与服务器的 SHA-256 完全一致：

| 文件 | SHA-256 |
| --- | --- |
| `configs/r2_quic_stratified_pilot120_v5.json` | `7595d0b36b148f76e182b2fe6d173b53493d67216e32ddf4539a518ae1fe3c19` |
| `scripts/r2_quic_controlled_collect.py` | `f7f486cdf1ec40b87564681d96d89099441c407d75a46a11701a2b6b2a8d7ef4` |
| `scripts/run_r2_quic_controlled_collection.sh` | `5434616fec999563b9353d335794208a9a02175300d8511849a86e3770a8c377` |
| `scripts/r2_quic_udp_proxy.py` | `187950182f85e0e55e95b81542d2420c6a8418f3317dc1b25298f7330702a6a7` |

诊断绑定文件 SHA-256 为 `8585c82ece366dadf401e99a1297d4bb1928b06823a718315c3cf4efb75718fc`。

## 运行结果

- 运行状态：`finished`。
- 启动器退出：采集器 `0`，日志管道 `0`。
- 连接标识：`f48643ea2f99d01d72b1bd1e`，与旧失败连接相同。
- 实现与条件：`aioquic 1.3.0`、`pilot03`、种子 43、64 MiB 大载荷。
- 网络条件：50 毫秒时延、2% 随机丢包、100 Mbit/s、2 MiB 用户态队列容量。
- 客户端自然完成：是。
- 连接耗时：120.644 秒。
- 响应字节：67,108,864。
- 响应 SHA-256：`af2f4dcdb15a8bdad0dabda5228554cf497a05e4767408953c3759e423165da7`。
- 客户端、抓包器和代理退出码：均为 `0`。
- 服务端原始退出码：`-15`；状态中保存了控制器主动发送 `SIGTERM` 的收据。

## 传输与事件证据

### 客户端到服务端

- 代理接收：764,517 字节、14,421 个数据报。
- 随机丢弃：16,750 字节、316 个数据报。
- 转发：747,767 字节、14,105 个数据报。
- 容量丢弃：0 字节、0 个数据报。
- 发送错误：0 字节、0 个数据报。
- 端点节奏收据：14,421 条。

### 服务端到客户端

- 代理接收：70,644,786 字节、56,684 个数据报。
- 随机丢弃：1,396,192 字节、1,125 个数据报。
- 转发：69,248,594 字节、55,559 个数据报。
- 容量丢弃：0 字节、0 个数据报。
- 发送错误：0 字节、0 个数据报。
- 端点节奏收据：56,684 条。

### 账本与训练真值

- 代理事件账本：278,215 行、124,890,575 字节，SHA-256 为 `adb3edf1d54cfc5dd006548af2667ed55b72215b5b7cb9cd870724897c089a7f`。
- 部署抓包：74,071,975 字节，SHA-256 为 `77af1bea8366e1aebdea9770746414e8deccaf2e4ee00eb91553ac8b658e3b27`。
- 客户端 qlog：39,024,946 字节，SHA-256 为 `9841d2b63eb401591bd581fe32006cc94f00968715c426fa86ff6a171a626945`。
- qlog 记录 14,423 个发送包事件、55,381 个接收包事件、316 个丢包事件和 3,643 个恢复状态更新事件。
- 双向关机时等待、服务中、传播中和已调度字节均为 0，未发现容量丢包或发送错误。

## 证据路径

服务器启动目录：

`/root/autodl-tmp/thesis/experiments/llm_probe/runs/launchers/r2-quic-v8-1-index24-diagnostic-20260806T145516Z/`

服务器完整结果目录：

`/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-raw/r2-quic-v8-1-index24-diagnostic-20260806T145516Z/`

本机启动目录：

`thesis/experiments/llm_probe/runs/launchers/r2-quic-v8-1-index24-diagnostic-20260806T145516Z/`

本机回收目录：

`thesis/experiments/llm_probe/runs/data-raw/r2-quic-v8-1-index24-diagnostic-20260806T145516Z/`

本机已回收启动绑定、启动日志、启动状态、运行状态、冻结配置副本、工具版本、连接状态、退出收据、端点合同、代理统计以及完整客户端和服务端日志。大型响应、PCAP、qlog 与代理逐事件账本保留在服务器完整结果目录，状态文件记录了其大小和 SHA-256。

## 裁决与后续边界

1. 旧正式运行仍保持 `failed/23`，不得用本次诊断覆盖其状态。
2. 本次诊断证明第 24 条在冻结条件下可以完成，因此不能把旧超时解释为该样本或 QUIC 动力学的确定性失败。
3. 当前证据不足以区分旧超时来自瞬时调度、主机负载、端点状态或其他非确定性运行因素。
4. 是否恢复剩余正式矩阵必须建立新的续跑身份和显式接纳规则；本任务不作该决定。
5. 没有执行第二次诊断，没有修改超时，也没有重启 GPU 实验。
