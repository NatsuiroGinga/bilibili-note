# DEDALE 2.0 后台下载任务计划

## 目标

可靠完成 `zeek_orange_dmz.zip` 下载，按官方大小与校验值验收，并保留完整故障与续传证据。

## 阶段

- [x] 阶段一：恢复课题上下文并建立持久化计划。
- [x] 阶段二：审计服务器磁盘、现有进程、部分文件和官方端点行为。
- [x] 阶段三：保留旧制品并切换到 HTTP/1.1 单调续传外层循环。
- [x] 阶段四：至少两次间隔检查文件增长与后台存活状态。
- [x] 阶段五：达到完整大小后核对官方摘要并归档记录。

## 关键问题

1. 官方端点是否正确支持字节范围请求，完整大小和摘要是什么？
2. 如何避免单个下载进程内部重试回到旧偏移并截短文件？
3. 服务器是否存在其他大文件下载，磁盘空间是否接近阈值？

## 已作决定

- 旧分片和旧日志只归档，不删除。
- 新策略优先 HTTP/1.1，每次失败后由外层循环读取当前文件长度并重新请求。
- 文件未达到官方大小并通过官方摘要前，不解压、不进入实验。

## 错误记录

- 旧 curl 在约 5 分钟内从 100,212,168 字节回退到 97,164,744 字节，旧日志出现 `Throwing away 11198653 bytes`。已停止旧进程并完整归档。
- 首次远程命令传递因多层 Shell 处理反斜线而失真，后改为本地 Base64 编码、远端同一交互式 Shell 解码执行；凭据始终只从环境变量读取。
- 本地 Tcl 不支持 `binary encode base64`，改用系统 `base64` 命令后恢复。
- 全局 `prettier` 不在命令路径中，改用仓库已有的 `.Codex/tools/prettier/node_modules/prettier/bin/prettier.cjs`；目标文档需要变更时原地写入被沙箱拒绝，改为生成临时格式化结果后用 `apply_patch` 精确应用。
- 16:40 发起的一次只读远程查询直到次日才向代理返回，下载进程不受影响并于 20:42 完成，但 25%、50% 和 75% 里程碑未能实时发送；结构化日志保留了三个里程碑的准确时间。后续长下载应增加服务端主动通知或独立短轮询，不能依赖单个长时间工具调用。

## 状态

**任务完成**：文件于 2026-07-19 20:42:00 达到 415,796,741 字节并通过官方 MD5；下载会话和进程已正常退出，未解压文件。
