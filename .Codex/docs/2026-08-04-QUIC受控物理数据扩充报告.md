# QUIC 受控物理数据扩充报告

- **日期**：2026-08-04
- **当前状态**：`blocked_remote_channel_before_field_collection`
- **字段闭合进度**：`0/40`
- **正式队列进度**：`0/600`，尚未建立或启动
- **GPU**：未使用，本任务为 CPU 与网络采集任务

## 1. 当前结论

40 连接字段闭合矩阵、项目隔离工具链引导脚本、可恢复采集驱动和持久化启动包装器已经在本地落盘，并通过白名单同步到服务器。四个文件的本地与服务器 SHA-256 逐项一致。

服务器工具链引导会话已经启动过，但持久化状态为 `failed/bootstrap_exit_1`。读取失败日志前，远程 SSH 代理连续三次关闭连接，因此目前不能确认 `tc`、抓包工具、aioquic 或 quiche 中的任何一项已经安装完成。40 连接采集尚未启动，不能签发字段闭合结论。

## 2. 冻结字段闭合矩阵

### 实现与角色

- 被测客户端一：aioquic `1.3.0`。
- 被测客户端二：quiche `0.24.5`。
- 固定参考服务端：quiche `0.24.5`。
- 客户端 qlog 只作训练期物理真值。
- 双向 PCAP 只作部署可见观测。
- qlog、连接标识、网络配置和实现身份不得进入部署输入。

### 5 个网络配置

| 配置 | 时延 | 丢包率 | 速率 |
| --- | ---: | ---: | ---: |
| `p01` | 5 ms | 0% | 100 Mbit/s |
| `p02` | 20 ms | 0.5% | 100 Mbit/s |
| `p03` | 50 ms | 0% | 20 Mbit/s |
| `p04` | 100 ms | 2% | 100 Mbit/s |
| `p05` | 200 ms | 2% | 20 Mbit/s |

- 种子：42、43。
- 负载：4 MiB、64 MiB。
- 固定队列上限：1,000 个包。
- 双向应用相同 netem 合同，网络命名空间与 veth 隔离。
- 组合规模：`2 个实现 × 5 个配置 × 2 个种子 × 2 个负载 = 40`。

## 3. 正式 600 连接合同

- 配置网格：`delay=[5,20,50,100,200] ms × loss=[0,0.5,2]% × rate=[20,100] Mbit/s`，共 30 组。
- 种子：42、43、44、45、46。
- 负载：4 MiB、64 MiB。
- 组合规模：`2 × 30 × 5 × 2 = 600`。
- `group_id` 单位固定为“客户端实现与版本 × 固定服务端版本 × 网络配置”，角色与版本都进入标识。
- 按完整 `group_id` 整组切分为 400/100/100，不允许同组连接跨训练、校准和开发验证。
- 正式队列启动前可用空间必须不少于 20 GiB；低于该值立即停止新增连接并汇报。
- 固定队列上限与固定参考服务端不能被解释为跨队列或跨服务端泛化证据。

## 4. 已落盘文件

| 文件 | SHA-256 |
| --- | --- |
| `configs/r2_quic_controlled_collection_v1.json` | `2252a101883b826a7977503824e2b5a8d962bf6dbebf9b6d4c504ffea4b560e0` |
| `scripts/bootstrap_r2_quic_controlled_tools.sh` | `eb1a603eda4b9d05d3741c1afe66df10d5cb1da1e99efb7ad93019dae66bab5a` |
| `scripts/r2_quic_controlled_collect.py` | `b440a55be0ccab7eb734ff421829a65b31154eef0409de6c048b0a89c9d6b5cf` |
| `scripts/run_r2_quic_controlled_collection.sh` | `8208b53802d3640ec7ec06b8eaad5f26af00e6c351fafddd6edaf2010b490140` |

服务器同步目标根为 `/root/autodl-tmp/thesis/experiments/llm_probe/`。同步收据保存在本地 `thesis/experiments/llm_probe/runs/hook-receipts/`，没有使用 `--delete`，没有同步运行制品、凭据或最终测试数据。

## 5. 采集与恢复设计

- 每个连接使用确定性 `connection_id` 和实现/版本/角色/配置组成的 `group_id`。
- 每个连接独立保存 netem 配置、PCAP、客户端 qlog、客户端日志、抓包日志、退出码、文件大小和 SHA-256。
- 完成状态按连接原子写入；已有失败目录不会自动覆盖或复用。
- 客户端退出码与抓包退出码必须同时为 0，PCAP 和 qlog 必须非空，才可写 `finished`。
- qlog 必须包含传输包事件、`metrics_updated` 和 ACK 帧；PTO 只在出现显式证据时置观察掩码，否则保存 `null + mask=false`，禁止填零或用 ACK 延迟替代。
- 最终测试始终不可见，采集数据不含恶意标签。

## 6. 服务器事实

在远程通道中断前已核验：

- `/root/autodl-tmp` 共 100 GiB，已用约 41 GiB，可用约 60 GiB。
- `runs/` 约 19 GiB。
- `rg`、`fdfind`、`uv` 和 Python 可用。
- 系统包仓库提供 `iproute2`、`tcpdump`、`tshark` 和 Cargo，但未安装。
- 没有现存 QUIC 采集进程或 `screen` 会话。
- 工具链引导只解包到项目 `runs/tools/r2-quic-controlled-v1/`，设计上不执行系统级 `apt install`。

## 7. 已执行验证

| 验证 | 结果 |
| --- | --- |
| 两份 Shell 脚本本地 `bash -n` | 通过 |
| 采集驱动本地 Python 编译 | 通过 |
| 冻结配置 JSON 解析 | 通过 |
| 四文件白名单同步 | 通过 |
| 四文件本地与服务器 SHA-256 | 逐项一致 |
| 两份 Shell 脚本服务器 `bash -n` | 通过 |
| 工具链引导状态 | 失败：`bootstrap_exit_1` |
| 40 连接采集 | 未启动，`0/40` |

按任务边界未运行 pytest、格式化、Prettier、Git、全仓检查、GPU 操作或最终测试读取。

## 8. 当前阻塞与下一动作

唯一当前阻塞是远程通道持续返回“SSH 代理关闭连接”，导致无法读取 `runs/tools/r2-quic-controlled-v1/bootstrap.log` 的失败命令末尾。不能在没有日志证据时猜测依赖失败原因或重复安装。

远程通道恢复后的唯一顺序：

1. 只读获取 `bootstrap.log` 末尾，定位第一个真实失败命令。
2. 形成单一可证伪根因并做最小修复；使用新工具链会话身份，不覆盖失败状态。
3. 工具版本与路径全部闭合后启动 40 连接队列。
4. 首个连接完成时立即登记客户端与抓包退出码、PCAP/qlog 路径、哈希和 `1/40` 进度。
5. 只有 40/40 全部通过字段与角色门禁，才建立并启动 600 连接正式 CPU 队列。
