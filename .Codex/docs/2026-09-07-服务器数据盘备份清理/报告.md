# 服务器数据盘备份清理报告

日期：2026-09-07

## 范围与边界

- 远端盘点根：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/`。
- 本机备份目标：`thesis/experiments/llm_probe/runs/server-backups/seeta-16851-20260907/`；本次开始时不存在。
- 未修改生产代码、配置、数据合同或实验制品内容。
- 未触碰正在传输的正确目录 `runs/data-raw/drift-dga-2026-rev-3b31077020cd1c013d0a75cad51042a2327c4521/DRIFT_input_eSLD/`。

## 只读核验

- GPU 空闲（`1 MiB / 32760 MiB`，利用率 `0%`）；未发现训练进程或 `screen` 会话。
- 发现一条正在写入上述正确 DRIFT 目录的 `rsync` 接收进程，因此禁止与其竞争带宽或删除相关路径。
- 初始数据盘：总计 `107374182400` 字节，已用 `90776600576` 字节，可用 `16597581824` 字节（约 15.5 GiB，85%）。
- `runs` 总占用 `65418772480` 字节；最大对象为：
  - `runs/data-prepared/ch3-protocol-a-raw83-shared-v1`：`16954503168` 字节。
  - `runs/diagnostics/dijk-repro/cache`：`14456745984` 字节，含 `X23.npy`、`X24.npy` 等历史 NumPy 缓存。
  - `runs/data-prepared/lspr24-screen-wide-v1`：`5439668224` 字节。
  - `runs/candidates`：`6802354176` 字节。
  - `runs/baselines`：`6241325056` 字节。

上述对象均为数据、表示缓存、模型或检查点。尚未取得本机完整副本、文件数、总字节和 SHA-256 对账收据，故未删除。

## 已执行的精确清理

| 对象 | 删除前 | 依据 | 结果 |
| --- | ---: | --- | --- |
| `runs/data-raw/drift-dga-2026-rev-3b31077020cd1c013d0a75Jenn51042a2327c4521/DRIFT_input_eSLD/` 及空父目录 | 48 字节 | 目录仅含一个空子目录，且没有非本次核验命令的引用 | 已删除 |
| `runs/tools/rust-1.85.0-x86_64-unknown-linux-gnu-relay/rust-1.85.0-x86_64-unknown-linux-gnu.tar.xz` 及空父目录 | 182464985 字节 | 可再生 Rust 安装包缓存，未发现使用进程 | 已删除 |

删除命令完成时，数据盘读数为：已用 `90728611840` 字节、可用 `16645570560` 字节。正确 DRIFT 目录的上传同时持续写盘，因此该时点的净可用空间变化不等于缓存文件的逻辑大小。

## 待处理候选

1. `runs/diagnostics/dijk-repro/cache` 约 14.5 GB：历史缓存，最有释放空间价值，但必须先同步至本机备份根并核对文件数、总字节、逐文件 SHA-256，再删除。
2. `runs/data-prepared/lspr24-screen-wide-v1` 约 5.4 GB：历史准备数据，未确认是否有当前消费者或可重建收据，保持原样。
3. `runs/candidates` 与 `runs/baselines`：含模型与检查点，未建立备份收据，保持原样。

## 未完成备份

- 已尝试同步 `runs/diagnostics/dijk-repro/cache` 至 `server-backups/seeta-16851-20260907/dijk-repro/cache/`，使用可续传的 `rsync --partial`。
- 实测吞吐约为 `0.05–0.4 MB/s`；在本次受控执行窗口内仅开始传输，尚未形成完整副本、文件数/总字节对账或 SHA-256 收据。
- 本机备份根当前仅保留空目录结构，没有完整文件或可用收据，不能视为备份。远端 `dijk-repro/cache` 保持原样，严禁删除。

## 风险与后续条件

- 本机备份根尚不存在，因而本次没有可报告的备份文件数、总字节或 SHA-256 收据。
- 当前正确 DRIFT 上传完成且进程退出后，才能启动大对象的串行备份；备份期间应避免同时执行新的大规模传输。
- 对每个候选必须先确认无进程占用，再按“同步、文件数与总字节核对、SHA-256 核对、删除、删除后磁盘复核”的顺序执行。
