# DRIFT 跨用途异标签冲突最小合同记录

- **状态日期**：2026-09-23
- **性质**：数据合同裁决准备，不是方法结论

本次严格自定义跨用途互斥诊断完成 30 个角色、240,000 个已选候选。角色内非法标签及入选重复均为 0；跨用途异标签角色对计数为 1,277，其中源训练到源验证为 8、源训练到目标测试为 1,223、源验证到目标测试为 46。聚合制品状态为 `blocked`。

该 `blocked` 只表示严格自定义互斥门未通过。DRIFT 官方复现角色仍可用，不表示哈希选择算法、官方协议或训练方法被否决。最终论文合同仍待用户在官方复现、全局异标签排除并补足、全跨用途实体隔离三条路径中选择。

本地官方仓库 `raw/resources/2026-DSN-DRIFT` 已检出固定提交 `e20d1fdf56c623993966c6786f61c01f91dec6d2`。`utility/dataset.py` 的训练、验证和逐年测试入口均调用 `pl.read_parquet(files).unique()`，没有传入 `subset="domain"`。Polars 官方接口规定 `subset=None` 默认按所有列判断重复。因此官方加载器只能证明整行去重，不能写成“按 eSLD 去重”；同 domain 但标签或其他列不同的行不会因此删除。

未找到原论文或官方说明明确讨论同一 exact eSLD 跨角色异标签、标签随年份变更或跨角色／跨年实体互斥。不能断言论文作者知情，也不能把 1,277 直接解释成数据污染、概念漂移、攻击成果或官方协议违规。

本次审计只比较各角色已选候选集，不能证明全总体冲突率、1,277 个互异 eSLD 或任一修复已经可行。用户选择前，最终论文成员合同不冻结，不启动正式训练。
