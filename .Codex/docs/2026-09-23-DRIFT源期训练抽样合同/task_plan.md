# DRIFT 源期训练抽样合同任务计划

- **状态日期**：2026-09-23
- **任务性质**：`architectural`
- **当前结论**：旧行位置设计快照与现行哈希最小 `top-k` 语义不一致已修正；全量审计完成。1,277个跨用途异标签冲突只使额外严格互斥诊断不通过，不否决官方角色。用户随后要求在同一源成员上共同预算训练 F 与基线，因此本机 60,000／60,000 固定哈希成员可用于源期筛选；最终论文成员合同和正式低误报评价尚未冻结。
- **冻结边界**：正式论文数据集固定为 DRIFT。后续候选、基线、消融、两骨干训练与评价只使用 DRIFT，不混入 LAMDA、RWKV、PINN、R2、CPA-ELP 等旧路线数据。

## P0：已完成的证据恢复

- [x] 读取 DRIFT 路线规则、路线总控和第三章恢复卡。
- [x] 读取《第三章-DRIFT数据角色与评价协议》与《第三章-正式攻击与目标抽样合同裁决》全文。
- [x] 读取正式评价实施计划全文；确认全源约 8693 万行、禁止新增物化、无服务器时不能完成全源容量与覆盖审计。
- [x] 读取朱焱雷全文 §3.6.1；确认 CP-Mobile、CP-Desktop 与移动/桌面合并的 CP-Full 均按 8:1:1 分层随机划分训练、验证、测试。

## P0：写作与诊断设计

- [x] 完成研究问题卡的三类成员边界：源期训练抽样、源阈值良性成员、目标评价成员。
- [x] 比较全源、固定哈希年份×类别分层、沿用行序 6 万三种训练策略的允许与禁止主张。
- [x] 把真实数据覆盖诊断与本机 MPS 时长诊断写成任何合同变更前的硬前置，不预设抽样规模。
- [x] 单列旧方案中只有服务器才能完成的全源精确去重、完整总体分布、容量、吞吐和训练可行性诊断；该项不等同于后来单独授权的本机有界内存哈希最小 `top-k` 两遍全量只读审计。
- [x] 给出可观察量、对照、支持与否决条件、成本、数据权限及用户待决项。

## P1：恢复卡写回

- [x] 仅追加一段，记录朱文可迁移原则、DRIFT 数据集冻结、目标每年每类 200k 固定哈希不变、源期训练抽样待诊断，以及 2026-09-23 F 本机终止事实。
- [ ] 保留恢复卡已有未提交修改，不改实验合同。

## P0：全量审计启动门禁

以下四项已在本次审计启动中机械确认；它们只证明诊断成功启动，不冻结候选数或训练合同：

- [x] **候选语义唯一**：实施简报、实际入口与启动命令均为“按 exact eSLD 去重，以 `SHA256(length_prefix_encode(data_revision, corpus_namespace, exact_esld))` 选取每角色哈希最小 `count` 个唯一实体；第一遍选择，第二遍复核选中实体”，不存在行位置抽样或按选中位置读取。
- [x] **运行目录洁净**：启动前解析并记录唯一运行目录；目录不存在，或仅含本次重定向预建的普通非符号链接 `console.log`，不得含其他文件、目录、符号链接或既有制品。
- [x] **绝对路径已核对**：收据明确记录工作树、脚本、配置、候选规格和运行目录的绝对路径；传给命令行入口的仓库相对参数必须从该工作树解析到这些同一绝对对象，避免依赖含混的当前目录。
- [x] **启动后双重存活证据**：进程启动后立即回读 `audit-status.json` 为 `status=running`，并在一个心跳周期内确认角色进度与控制台心跳；审计随后正常终止并留下聚合收据。

## P0：全量审计结果与边界

- [x] 30个角色完成，240,000个请求候选均被选中；角色内非法标签和入选成员重复均为0。
- [x] 严格跨用途异标签互斥诊断计得1,277个角色对冲突，运行状态为`blocked`；收据见[跨用途异标签冲突分析与合同选项](跨用途异标签冲突分析与合同选项.md)。
- [ ] 用户选择最终语料合同：官方整行去重复现，或另行批准严格身份隔离规则。选择前不得冻结论文成员或把源期筛选称为正式训练。

## 验收

- [x] 三份新文档与恢复卡链接、状态和边界一致。
- [x] 未发明抽样量、覆盖率、时长或效果门槛；未冻结项明确交用户决定。
- [x] 用户授权的全量只读数据扫描已完成；未运行训练、MPS正式训练、远程命令或 Git 写操作。
- [x] 执行 `git diff --check` 并回读改动。

## 2026-09-23 P0 共同预算筛选运行器恢复计划

**目标**：让 `official_p2p3.py` 的 A→B→D→G→F 筛选臂在不改变冻结科学合同的前提下，留下可追踪、可恢复和可审计的本机 MPS 制品。

**范围与文件所有权**：只修改 `.Codex/docs/2026-09-10-DGA对抗鲁棒组相对方案/official_p2p3.py`；运行制品只写入新建的 `thesis/experiments/llm_probe/runs/diagnostics/` 唯一目录；本计划记录实施状态。

**冻结约束**：保持固定哈希源训练/验证成员、A/B/D/G/F 算子、3 个 epoch、batch 128、优化器、损失、0.5 筛选阈值和源期评价面板不变；不读取目标年、不物化成员/攻击集、不生成合成测试、不运行自动格式化。

**实施步骤**：

- [x] 增加原子 `run-status.json`：记录运行身份、代码/成员规格身份、每臂状态、最近 epoch/batch、异常摘要与完成时间。
- [x] 以每 20 批结构化心跳记录展示名、epoch/batch/总批、损失、已耗时、吞吐/ETA 与 RSS；每 epoch 把清洁 FPR、攻击 FNR/AP 写回状态。
- [x] 每 epoch 原子保存该臂模型/优化器/已完成轮次检查点，并为每个已完成臂单独持久化结果；入口只支持轮次边界恢复，批内中断重放该轮。真实恢复仍待首轮断点后核验。
- [x] 用真实入口完成 `py_compile`、`--help`；已启动同一源期成员入口的 A 臂，尚在源成员选择阶段，真实优化和断点验收待运行推进。

**验收命令**：`/opt/miniconda3/envs/rwkv/bin/python -m py_compile .../official_p2p3.py`、`.../official_p2p3.py --help`、`git diff --check`；真实 A 臂必须出现 `run-status.json`、结构化心跳、epoch 检查点和单臂结果。

**状态**：主代理已实现并启动 `screen` 会话 `ch3_drift_source_a_20260923`，运行目录 `thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-source-common-a-mps-20260923-v1/`。12 个源训练与 6 个源验证角色均已选满；18:17 CST 首个真实优化心跳为第1轮 `1/469`，损失 `0.085949`、RSS峰值约612MiB，`run-status.json` 同步为 `running`。脚本、选择器、模型加载器、特征适配器、候选规格与权重的 SHA-256 均已写入运行状态。第一轮指标和断点待产生；0.5 阈值源期筛选不能冒充正式低误报评价。

**后续同预算机制核查**：A/B 完整结果出现后，在 D/F 启动前补只读式计数埋点：每批记录候选生成数、模型评分数、入损失数、`M=0..4` 组的入选数及零优势并列数；G/F 记录真良性触发数与加权损失质量。埋点不得更改候选字符串、排序、损失或优化更新。若代码身份因埋点改变，单列差异并证明训练算子未变；轮次断点恢复仍须按各自代码哈希严格校验。此项用于检验网页审查与既有理论指出的 `M=4` 零优势作用面，不能先于 A/B 结果改写主判据。

**首轮真实验收（18:32 CST）**：A 第1轮 469批完成；源期固定0.5干净FPR `0.0235667`、二位攻击FNR `0.3084667`。正类单一攻击面板的AP=`1.0`无区分力。277MiB断点的 SHA-256 为 `69ab4213f33c2f4397eebca151f87502c2fc6e3e6c5edc70715f6553e305ee58`，以 `torch.load(..., map_location="cpu", weights_only=True)` 真实读取确认模型296张量、优化器296状态、轮次1和随机状态齐全；第2轮已出现首批心跳。未做中断重启实验，不能把安全读取等同于恢复轨迹等价。

**第二轮真实验收（18:47 CST）**：A 第2轮469批及源期验证完成，固定0.5干净FPR `0.0234667`、二位攻击FNR `0.3070667`，与首轮差值均很小；277MiB断点已原子替换、CPU安全读取为轮次2，包含两轮曲线和296项优化器状态。第三轮首批已出现；A 尚未完成，不能启动 B 或报告 A/B 差异。

**第三轮断点（19:02 CST）**：A 第3轮469批及源期验证完成，固定0.5干净FPR `0.0224000`、二位攻击FNR `0.3179667`；与第2轮呈现误报下降、攻击漏检上升。真实277MiB断点已安全读取为 `completed_epoch=3`，三轮轨迹齐全。最终单臂评分与结果清单仍在计算；当前不能把 `checkpointed` 写为整臂 `completed`。

**A 完整完成、B 接续（19:13 CST）**：A 的 `run-status.json` 已为 `completed`，单臂与总结果文件均存在，原 PID 和 `screen` 均退出。A 三轮终点源期固定0.5干净 FPR `672/30000`、干净 DGA FNR `781/30000`、二位编辑 FNR `9539/30000`、半替换近似 FNR `18869/30000`；纯 DGA AP 不作比较。随后以同一脚本/候选规格/模型身份启动 B，运行目录 `runs/diagnostics/ch3-drift-source-common-b-mps-20260923-v1/`、PID `19465`，初次状态 `loading_source` 并已出现源角色读取日志；B 训练和指标尚未产生。
