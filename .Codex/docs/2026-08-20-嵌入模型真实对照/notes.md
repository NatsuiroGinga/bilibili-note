# 嵌入模型真实对照笔记

## 2026-08-20 初始检查点

- 工作树：`.worktrees/ch4-dtep-pbc-20260819`。
- 当前暂存区为空，但工作树存在大量其他代理的未提交改动。
- 独占修改范围：`.Codex/docs/2026-08-20-嵌入模型真实对照/`。
- 已读取根规则、`.Codex/docs/AGENTS.md`、`planning-with-files`、`huggingface-best`、`hf-cli` 与 `pytorch-patterns`。
- 待核对提交：`6b31183`、`a695368`、`eaea961`。
- 关键防污染顺序：先依据仓库真实文档冻结查询与相关性标注，再加载或运行任何候选模型结果。
- 当前尚无模型间实证结论；在完成提交历史和现有评测核对前，不把现用模型称为最优。

## 风险

- 当前生产检索模块存在并发未提交修改，评测必须避免依赖未经归属确认的新行为。
- 候选模型的公开基准只能用于筛选，不能替代本仓库真实查询集上的对照。
- 下载量和热度不作为效果证据。

## 下一检查点

1. 通过 Hugging Face 官方接口读取候选模型卡、许可证、参数量和固定修订。
2. 在共同分块和共同融合参数下实现独占评测驱动。
3. 执行本机中央处理器真实构建和查询，并记录 MPS 不可用的回退收据。

## 2026-08-20 相关性冻结检查点

- 冻结文件：`frozen_qrels.json`。
- 冻结时间：`2026-08-20T20:26:09+08:00`。
- 冻结时提交：`606f8d0610299306632fb6cc7d01b177390c6c2e`。
- 源 `real-user-query-v1` 哈希：`871e2486f51e989177a42908d6aabb20172ce03fc760177192964aef81a76835`。
- 既有 `curated-regression-v1` 哈希：`bd1539ecee99ad7c8dc5b1dcd0108d28307d80bcee7a61e23bb07cb2c01bdefe`。
- 8 条真实用户查询已独立标注；其中 `RUQ07` 因“新方案”缺少可解析指代，冻结为上下文不足拒答项。
- 新增 6 条跨作用域验收查询，覆盖 `recovery`、`experiment-receipts`、`thesis-chapters`、`papers`、跨 collection 多相关项和域外拒答。
- 冻结前没有运行候选模型、没有查看候选排名，也没有从检索输出提取答案词。
- MPS 探测：`torch==2.13.0` 报告 `mps_built=true`、`mps_available=false`；真实评测按合同记录并回退中央处理器。

## 2026-08-20 设备核验修正

- 沙箱内 MPS 探测为假阴性；沙箱外同一 uv 锁定环境报告 `mps_built=true`、`mps_available=true`。
- E5-small 最小真实编码在 `mps:0` 生成 `2×384` 有限向量。
- 正式三模型构建与查询均使用 `mps:0`，没有触发中央处理器回退。

## 2026-08-20 最终检查点

- 语料：1711 份文档、34,630 个共同分块；三模型词法结果逐查询完全一致。
- E5-small：构建 390.7 秒，索引 210,337,792 字节。
- E5-base：构建 1193.4 秒，索引 256,344,064 字节。
- BGE-M3：构建 4148.6 秒，索引 263,532,544 字节。
- `real-user-query-v1` 向量模式中，E5-small 的 `Recall@10/MRR/nDCG` 为 `0.4857/0.2649/0.2068`；E5-base 为 `0.2714/0.0603/0.0940`；BGE-M3 为 `0.4857/0.1184/0.1774`。
- `cross-scope-query-v1` 混合模式中，E5-small 的 `Recall@10/MRR/nDCG` 为 `1.0000/0.3158/0.4308`，高于 E5-base 的 `0.9000/0.1444/0.3082`，也高于 BGE-M3 的 `1.0000/0.1544/0.3524`。
- 三模型负例拒答率均为 0；真实用户和跨作用域查询的错 collection 率为 0.80 至 0.88，说明主要瓶颈是拒答与跨 collection 融合，而不是嵌入模型容量。
- 裁定：两个新候选均未相对 E5-small 形成明确非支配优势，保留当前默认且不修改生产配置。支持范围仅为本轮三个候选和两套预注册独立验收集，不宣称全局最优。
