# QUIC-MedNetCom 下载固定过程笔记

## 已核对事实

- 官方仓库：`https://github.com/Mohmoulay/QUIC-MedNetCom.git`
- 固定提交：`f237a20360b83868a197488c5b557f53e4b7e53c`
- 下载前本机未发现同名完整或临时副本。
- 下载前数据卷可用空间约 48 GiB，足以容纳预估不足 1 GiB 的仓库。
- H23Q 已降为条件备选，不在本任务下载范围内。

## 待核验

- 已确认官方提交可获取且当前 `main`、`HEAD` 均指向固定提交。
- 已确认 89 个 qlog 文件中有 81 个非空可解析对象，另有 8 个官方零字节占位文件。
- 已确认 79 个 qlog 含拥塞窗口、在途字节、往返时延和确认范围；另有 2 个非空短 qlog 缺少这些核心状态。
- 已确认仓库没有根许可证文件，README 没有数据再分发条款，当前按不可公开再分发处理。

## 完整性结果

- `git fsck --full`：退出码 0。
- 固定提交：`f237a20360b83868a197488c5b557f53e4b7e53c`。
- 上游文件：200 个，共 695,847,525 字节。
- qlog：89 个，共 630,227,334 字节。
- 非空可解析 qlog：81 个，合计 81 条 trace、2,852,760 个事件。
- 逐文件清单：200 行，全量 `shasum -a 256 -c` 通过。
- 清单 SHA-256：`ad4924be38968925ea0532d32e643709b40f7f852c863108cbd165addf4ecd61`。

## 查询错误说明

- 初次结构统计查询错误地把 `keys_unsorted` 生成的数组继续送入对象字段查询，产生“不能用字符串索引数组”的提示。这不是 qlog 解析失败。改为先按顶层类型分支后确认：81 个非空文件均为标准 qlog 对象，8 个文件没有内容。
