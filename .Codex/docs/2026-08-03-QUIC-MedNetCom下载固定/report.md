# QUIC-MedNetCom 下载固定回执

## 结论

QUIC-MedNetCom 已从官方仓库完整下载并固定到指定提交，可以立即进入 R2 的 QUIC 物理字段与单位审计。H23Q 未下载。

## 本地制品

- 数据根目录：`raw/datasets/QUIC-MedNetCom/`
- 来源记录：`raw/datasets/QUIC-MedNetCom/SOURCE.codex.md`
- 逐文件清单：`raw/datasets/QUIC-MedNetCom/SHA256SUMS.codex.txt`
- 固定提交：`f237a20360b83868a197488c5b557f53e4b7e53c`
- 数据目录实际占用：约 715 MiB，其中 `.git` 约 51 MiB。

## 数量与字节

| 项目 | 数量 | 字节数 |
|---|---:|---:|
| 上游非 Git 文件 | 200 | 695,847,525 |
| `.qlog` 文件 | 89 | 630,227,334 |
| 非空且可解析 qlog | 81 | 包含在上项中 |
| 零字节上游 qlog | 8 | 0 |
| 含核心物理状态的 qlog | 79 | 包含在上项中 |

81 个非空 qlog 共包含 81 条 trace 和 2,852,760 个事件。79 个文件可检出拥塞窗口、在途字节、往返时延和确认范围；59 个文件出现明确丢包事件。剩余 2 个非空文件虽能解析，但内容过短且缺少完整物理状态，后续不能直接作为完整动力学监督。

## 验证

- `git rev-parse HEAD` 与指定提交完全一致。
- `git fsck --full` 退出码为 0。
- 81 个非空 qlog 均为合法 JSON，顶层为对象，版本均为 `draft-02-wip`，`traces` 均为单元素数组。
- 8 个零字节 qlog 同样存在于固定提交的 Git 树中，不是网络下载或检出损坏。
- `SHA256SUMS.codex.txt` 覆盖 200 个上游文件；全量校验均为 `OK`。
- 清单自身 SHA-256 为 `ad4924be38968925ea0532d32e643709b40f7f852c863108cbd165addf4ecd61`。

## 限制

- 上游没有明确的数据再分发许可证。当前允许本课题内部实验使用，但禁止公开镜像或上传原始数据。
- 该数据没有恶意流量标签，只能作为 QUIC 物理辅助池，不能替代 GeNIS、TQH-C2 或未来带标签 QUIC 数据的检测监督。
- 下载完成不等于数据已冻结。下一步还需冻结可用文件白名单、字段语义、单位、事件时序、专家适用掩码和训练/验证划分。
