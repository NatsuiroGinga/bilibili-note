# RWKV 长度与连续状态最小因果消融任务计划

## 目标

在不访问服务器、不修改代码、不读取 LSPR24 选点标签的边界内，基于现有证据设计协议 A 下 RWKV 计算切片长度、有效历史长度与跨片段状态继承的最小因果消融，并明确是否需要补做长度消融。

## 文件所有权

- `.Codex/docs/RWKV/2026-08-21-RWKV长度与连续状态消融设计/task_plan.md`
- `.Codex/docs/RWKV/2026-08-21-RWKV长度与连续状态消融设计/notes.md`
- `.Codex/docs/RWKV/2026-08-21-RWKV长度与连续状态消融设计/实施方案.md`

除上述三个新增文件外，不修改、暂存或提交其他文件。

## 阶段

- [x] 阶段 1：读取根规则、过程文档规则、RWKV 局部规则与适用技能。
- [x] 阶段 2：检查目标目录 Git 快照和共享暂存区。
- [ ] 阶段 3：恢复 RWKV 总控、第三章恢复卡和冻结目标合同。
- [ ] 阶段 4：核对连续状态候选、第三章既有长度结果和 BF16 实施证据。
- [ ] 阶段 5：形成最小实验臂、训练单元、指标、停止门、资源与断点恢复接口。
- [ ] 阶段 6：自检三份文档，暂存并提交唯一任务范围。

## 核心问题

1. 既有 `L=16/32/64/128` 三种子 E5 结果是否足以回答当前长度问题？
2. 如何分别操纵计算切片长度、有效历史长度和跨片段状态继承，避免混杂？
3. 在固定协议 A、固定 R2 与胜出 K、统一 BF16 下，最少需要多少训练单元？
4. 哪些指标和停止门能在不接触 LSPR24 选点标签的前提下支持继续、否决或升级正式验证？

## 验收

- 三份文件均位于指定目录，且 Git 提交仅包含这三份文件。
- 方案不使用三折，不读取 LSPR24 选点标签，协议 A 固定 LSPR23 实体不相交开发划分。
- 方案显式区分计算切片长度、有效历史长度与跨片段状态继承。
- 既有三种子 E5 只作为历史证据，不冒充当前 BF16 因果消融。
- 方案列出最小实验臂、训练单元数、主要指标、护栏指标、资源指标、停止门、预计资源和断点恢复接口。

## 决策记录

- 任务属于实验设计而非运行：本轮只产出“设计可行/实验待证”方案，不访问服务器。
- 目标目录与共享暂存区在修改前均为空：不创建空快照提交。

## 错误与阻塞

- 暂无。

## 当前状态

正在执行阶段 3：恢复路线合同并提取可追溯实验约束。
