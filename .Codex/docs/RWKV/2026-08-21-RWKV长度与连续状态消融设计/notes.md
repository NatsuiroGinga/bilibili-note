# RWKV 长度与连续状态消融证据笔记

## 任务边界

- 只读核对现有文档与论文正文。
- 不访问服务器，不启动实验，不修改代码。
- 不读取 LSPR24 选点标签。
- 输出仅限本目录的 `task_plan.md`、`notes.md` 与 `实施方案.md`。

## 证据分级

- 冻结合同：决定数据、切分、精度、候选和运行边界。
- 原始或实施证据：支持既有运行事实、资源事实和接口现状。
- 历史筛选证据：只用于确定待检验因素，不能替代当前因果消融。
- 设计推论：必须标为“实验待证”，并给出可证伪实验。

## 待提取证据

- RWKV 路线总控与第三章恢复卡中的当前数据、协议和候选状态。
- 可微神经骨干替代 XGBoost 目标合同中的 R2、胜出 K、BF16 与开发选点约束。
- 跨片段实体连续状态候选目录中的机制定义、风险与已有接口。
- 第三章正文 `L=16/32/64/128` 三种子 E5 结果及其适用边界。
- 当前 RWKV BF16 实施报告中的资源测量、恢复字段和未关闭风险。

## 已确认事实

- 目标目录修改前不存在未提交文件，共享暂存区为空，当前 `HEAD` 可作为修改前快照。
