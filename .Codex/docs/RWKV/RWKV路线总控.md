# RWKV 路线总控

<!-- RESEARCH_ROUTE=RWKV -->

- **状态日期**：2026-08-26
- **用途**：RWKV 路线公共索引与跨章稳定合同；第三、四章活动状态只写入对应章节恢复卡。
- **路线性质**：论文主线。研究对象固定为网络攻击状态检测；从 RWKV 及生态工作提取可迁移机制，原生 RWKV-7 只作机制来源或必要对照，不预设唯一骨干。

## 一、恢复顺序

1. 所有 RWKV 任务先完整读取同目录[路线规则](AGENTS.md)与本文件。
2. 第三章任务只追加[第三章恢复卡](RWKV第三章恢复卡.md)；第四章任务只追加[第四章恢复卡](RWKV第四章恢复卡.md)；跨章任务才读取两张卡。
3. 只有追溯第三章候选存废时读取[第三章候选方案登记册](2026-08-08-第三章候选方案登记册.md)。
4. 只有事实冲突、制品追溯或用户明确要求时，经[历史交接入口](RWKV历史交接入口.md)选择历史材料。
5. [当前恢复卡](RWKV当前恢复卡.md)只作旧链接兼容页，不是默认入口。

默认不读取 PINN/R2 的 `output/` 恢复文档。只有用户要求跨路线比较、返回历史路线，或正式决定替换论文主线时才切换，禁止混写。

## 二、公共研究边界

- 第三、四章共享同一 LSPR 数据体系、合法字段、标签权限、最终测试隔离和评价协议。LSPR23 是源年开发成员；LSPR24 已被历史实验访问，后续评价必须标明 `target_informed=true`，不得反向选择机制、骨干、阈值或公式。
- 字段预算为 Dijk 附录 A 的 83 字段：原始 IP 只作分组键，演习身份、既有 IDS 输出和标签派生字段禁止入模。逐项依据见[Dijk 协议审计](archive/文献与审计/2026-08-13-Dijk2026协议逐项审计.md)。
- RWKV 唯一活动训练路径是独立 CUDA 融合实现，在合法 Raw83 数据门通过后使用独立身份从头训练；不得复用历史实现的权重、优化器、断点、代码锚或对照门。
- 文献、设计、审查和代理实验只证明可试性。支持、否决和论文结论必须回指绑定数据、配置、共同预算基线、日志、指标和制品。
- `16.1%` 是本课题早期自定门槛，不来自朱焱雷论文。朱表 3-7 的 `+9.30/+12.30` 是 PGD-20 鲁棒准确率绝对百分点，只作工作量和量级参照。

## 三、公共数据与实验合同

- 跨年度字段、切分、身份和清单以[共同数据合同](archive/路线选型与数据基础设施/2026-08-11-LSPR23-LSPR24共同字段审计与跨年度数据合同.md)为准；缓存成员数、服务器状态和运行数量等易变事实必须从当前清单或原始状态实测。
- 不得查看最终评价后修改样本、来源比例、字段、基线能力、指标、阈值或超参数；所有比较共享截止时刻、历史预算、拆分、调参次数和统计程序。
- 单种子 Q0、零训练筛选和描述性目标年评价只能决定是否增加计算，不能进入正文正式结果。每个命名机制必须有无机制、单机制和组合消融。
- 第三章按单次运行报告且不评冠军；第四章的单种子事实不得升级为论文支持。公开主比较只纳入有论文来源并在冻结共同协议下重跑的基线。
- 新实验不持久化逐流概率、标签数组、折成员表或拆分缓存；长期制品限于配置、模型、日志、状态、哈希、清单和聚合指标，专项合同另有更严格规定时从其规定。

## 四、规范路径、服务器与凭据

- 当前工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`。
- 本地实验根：`thesis/experiments/llm_probe/`；远端项目根：`/root/autodl-tmp/thesis/experiments/llm_probe/`；运行根通常为 `<项目根>/runs/diagnostics/<运行名>`。
- 当前服务器身份为 B76，凭据键只允许 `GPU_SSH_B76` 与 `GPU_PWD_B76`；禁止写入值、解析后的连接信息或私有参数。服务器可能回收，任何远程动作前重新核验身份、根目录、磁盘、依赖、GPU、运行与制品。
- 远程 Shell、同步、资源、神经训练和故障细节分别读取[运行合同](../agents-split/llm-probe-operational-contract.md)、[远程脚本合同](../agents-split/remote-script-execution-contract.md)、[神经训练合同](../agents-split/llm-probe-neural-training-contract.md)和[故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)；本文件不复制易漂移命令与阈值。

## 五、章节与当前证据索引

- [第三章恢复卡](RWKV第三章恢复卡.md)：当前 Goal、有效结果、活动任务、状态待核运行、阻断与下一动作。
- [第四章恢复卡](RWKV第四章恢复卡.md)：DTEP、PBC、E1、长实体诊断和 Q0 载荷。
- [第三章有效 2×2 实验计划](archive/第三章-MLP线/2026-08-26-第三章有效2x2机制优化计划/有效2x2实验计划.md)：提交 `a3a4cd4` 冻结的当前 S0 合同。
- [四篇漂移文献审计结论](archive/第三章-MLP线/2026-08-26-四篇漂移文献机制优化审计/审计结论.md)：提交 `5a9bc59` 完成；只支持一轮零训练 S0，不放行训练层多算子。
- [规则链字节审计](../2026-08-26-AGENTS规则链字节精简审计/审计报告.md)：提交 `f960bf42` 完成 P0。
- [恢复文档精简审计](archive/基础设施/2026-08-26-恢复文档精简/审计报告.md)：本轮前后行数、字节、链接和恢复能力收据。
- [公共数据合同](archive/路线选型与数据基础设施/2026-08-11-LSPR23-LSPR24共同字段审计与跨年度数据合同.md)、[LSPR 基线登记册](archive/路线选型与数据基础设施/2026-08-12-LSPR完整基线登记册.md)、[第三章全量进度快照](archive/基础设施/2026-08-25-第三章实验全量进度快照.md)。
