# 任务计划：因果实体记忆交叉注意力与低误报实体排序

- 日期：2026-08-28
- 代理：`ft_causal_memory_lowfpr_theory_sol_max`
- 模型：`gpt-5.6-sol`
- 推理强度：`max`
- 状态：进行中

## 目标

为第三章 FT-Transformer 重锚候选形成可审查的双机制理论、数学推导、全文文献边界和实验计划。所有候选结论仅标为“设计可行／实验待证”，不写代码、不启动实验、不修改恢复卡、研究问题卡、正文、`raw/`、`wiki/`、Zotero 或服务器。

## 文件所有权

本任务独占修改以下文件：

- `task_plan.md`
- `notes.md`
- `机制设计与实验计划.md`
- `数学推导与近邻边界.md`

## 阶段

- [x] 阶段 1：读取规则、技能、路线总控、第三章恢复卡、全局快照和当前研究合同。
- [x] 阶段 2：读取 FT 代码接口审计、基线与消融编排、五篇文献审计检查点。
- [x] 阶段 3：运行本地混合检索并建立全文证据矩阵。
- [x] 阶段 4：冻结问题定义、可观测量、严格因果接口和两个机制的数学形式。
- [x] 阶段 5：推导退化性质、梯度路径、偏差边界、复杂度和极端条件。
- [x] 阶段 6：设计 C00/C01/C10/C11、训练与在线推理算法、检查点恢复和实验门。
- [x] 阶段 7：完成朱式改造表、魔法数字依据表和近邻边界。
- [x] 阶段 8：自审公式维度、因果性、退化、梯度、引文、占位词和 Git 差异。
- [x] 阶段 9：只提交本任务四份文档并回报提交 SHA。

## 固定裁决

- 章级机制一为结构级“因果实体记忆交叉注意力”。
- 章级机制二为训练级“低误报实体排序训练”。
- tokenizer、历史 token 构造和类型嵌入只属于 FT 骨干适配或机制一实现细节，不单列为创新机制。
- 普通实体 AP 选轮只属于协议对照，不得包装为训练机制。
- LSPR23 用于选择与冻结；LSPR24 仅在封印后作 `target_informed=true` 描述性评价。

## 阻塞与错误

- 当前工作树存在大量其他代理改动；本任务只暂存和提交独占目录。
- 当前 Goal 工具返回空；第三章 Goal 状态与职责以恢复卡及 `2026-08-26-第三章Goal-规则-研究契约三层职责.md` 为准。
- 初次 `uv` 状态查询因沙箱不能读取用户缓存失败；提权后按仓库入口完成。
- 并发代理持续修改非论文过程文档，索引在部分后续查询前再次显示 `source_changed`；核对论文集合为 522 篇且 `papers` 差异为零。失败查询改用精确 `rg` 与原始 PDF／官方 PDF，不把索引摘要当承重证据。
- 五篇文献审计仍在扩展检索阶段；已吸收其 TabFormer、FATA-Trans、Fieldy、Stein 自回归表格 Transformer、Zhu 2022 深度 pAUC 与 Hu 2023 多示例 TPAUC 检查点，不把检查点升级为饱和检索结论。
- 用户最终要求理论逐项绑定真实实验；两份主文档已新增“病灶制品→可观测量→单变量实验→通过／否决门→状态”矩阵。统一严格过去接口保持硬阻断，所有机制与公式均标为实验待证。
- `prettier --check` 在执行前被仓库 `P0-NO-PRETTIER` 钩子拒绝；未绕过。Markdown 改用占位词、公式分隔符、代码围栏、标题结构与 `git diff --check` 静态检查。

## 验证收据

- `git diff --check`：通过。
- 占位词扫描：`TODO/TBD/待补全/PLACEHOLDER/CITATION NEEDED/FIXME` 零命中。
- 数学块分隔符：`124` 个，为偶数；代码围栏两组，起止完整。
- 状态与措辞扫描：所有机制、公式和原创性结论保持“设计可行／实验待证”；严格过去接口标为硬阻断。
- 提交范围：只包含本目录四份文档；未暂存或提交其他代理与用户文件。

## 当前检查点

**完成**：公式、因果、退化、梯度、采样偏差、训练／运营并列语义、引文、占位词、实验绑定和 Git 范围自审已完成；等待最终独占提交并回报 SHA。
