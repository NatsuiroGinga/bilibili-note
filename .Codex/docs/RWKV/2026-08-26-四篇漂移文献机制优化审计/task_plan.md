# 四篇漂移文献机制优化审计任务计划

## 目标

核验提交 `3fad1f87d44f803504ecaa804e835236e380f840` 中四篇漂移论文全文，并结合最多四篇既有构件近邻，裁定它们对第三章两个优化方向的机制来源、放置层和最小实验能够提供多强的文献支持：

1. `Lp + max` 多算子实体聚合。
2. O11 逐流分数上的决策层因果前缀统计。

文献只证明机制来源与可试性；候选效果统一标为“实验待证”。

## 文件所有权

- 只新增本目录的 `task_plan.md`、`notes.md`、`证据矩阵.md`、`审计结论.md`。
- 不修改研究问题卡、实验代码、恢复卡、路线总控、看板、原始 PDF 或既有全文笔记。
- 不访问服务器，不启动实验，不扩展在线候选。

## 范围合同

| 项 | 冻结内容 |
| --- | --- |
| 研究对象 | 加密恶意流量中的跨时漂移、实体聚合、表示对齐、测试时适应、自适应重训和因果在线打分 |
| 核心文献 | 提交 `3fad1f8` 新增的 4 篇 2025－2026 全文 |
| 构件近邻 | Pevný、Ilse、MIDAM、ForkMerge，最多 4 篇 |
| 纳入 | 全文可读，方法或实验直接涉及漂移适应、实体或节点聚合、在线决策；关键论断可定位到物理 PDF 页、表或公式 |
| 排除 | 只做漂移检测且不能支撑聚合、放置或实验设计者仅作边界；测试集预言机调参数字不得作本项目基线 |
| 全文要求 | 核心论文 4/4 回原始 PDF；构件近邻以既有全文笔记定位，关键构件事实按需要回原件 |
| 停止条件 | 四篇逐篇页码核验、构件矩阵、正文允许/禁止措辞和最小实验裁决完成 |

## 检索问题表

| 编号 | 问题 | 所需证据 |
| --- | --- | --- |
| Q1 | MalMoE 表 II 的 `AVG 0.9710→0.4822` 与 `DEG 0.9817→0.2811` 分别属于什么任务、漂移列和指标方向？ | 原始 PDF 表 II、表头、正文解释 |
| Q2 | 四篇是否真的推出 `Lp + max` 并联，而非仅支持“多算子可能互补”？ | 原始公式、消融和构件近邻的聚合定义 |
| Q3 | 三篇非 MalMoE 漂移论文是否支持在 O11 逐流分数上做决策层因果前缀统计？ | 方法流程、统计量输入、时间因果性和决策用途 |
| Q4 | 文献能否允许训练层多算子；若允许，强度是直接机制来源、类比启发还是仅实验动机？ | 放置层、是否参与梯度、是否依赖目标期标签 |
| Q5 | 本轮最低成本且可证伪的实验应是零训练四臂、冻结骨干还是完整训练四格？ | 文献边界与既有快速实验分级合同 |

## 证据等级

- `D1`：原始 PDF 直接支持具体机制、公式、放置层或实验事实。
- `D2`：多篇全文之间的受限综合，可支持设计动机或比较假设，但不能证明本项目机制有效。
- `D3`：跨任务或跨层迁移推论，只能作为待验证假设。
- `X`：本课题实验制品；本任务不新增或复核服务器制品，仅按既有合同识别其边界。

## 六阶段

- [x] 阶段一：定义范围。完成范围、纳入排除、全文要求、检索问题与停止条件冻结。
- [ ] 阶段二：检索与收集。核对提交文件、PDF 完整性、本地混合检索状态和构件近邻路径。
- [ ] 阶段三：筛选与分类。完成 4 篇核心、最多 4 篇构件近邻的纳入理由和作用分类。
- [ ] 阶段四：全文分析。逐篇回 PDF 核验页码、表格、公式、方法层和不可外推边界。
- [ ] 阶段五：综合与研究空白。完成两个方向的支持强度、冲突、缺口及最小可证伪实验。
- [ ] 阶段六：生成与验证制品。完成证据矩阵、审计结论、引用与路径核验。

## 决策记录

- 本轮不做泛化综述，不联网新增论文。
- MalMoE 数字必须按表头和行列方向重核，不能沿用既有笔记的二次解释。
- “多算子互补”与“指定 `Lp + max` 并联”分开裁定。
- “漂移统计用于触发重训”与“在逐流分数上进行因果前缀在线打分”分开裁定。

## 错误与阻塞

- 暂无。

## 状态

当前处于阶段二：盘点核心 PDF、全文笔记和构件近邻，并运行项目规定的本地文献检索状态检查。
