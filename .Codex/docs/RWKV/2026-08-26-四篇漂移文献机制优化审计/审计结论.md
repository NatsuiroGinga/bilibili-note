# 四篇漂移全文对第三章机制优化的审计结论

## 结论

四篇全文**足以支持只开一轮零训练 S0，也足以否决立即进入训练层多算子**；它们**不足以证明** `Lp + max` 并联或 O11 逐流分数上的决策层因果前缀有效。

1. MalMoE 提供同域直接证据：AVG 与 DEG 对两类图漂移表现互补，单算子错配会严重退化。但其成功结构是**分专家＋硬选择门**，而不是在同一表示中直接拼接；`Lp` 与 `max` 又都读取同一逐流分数，不能把 AVG/DEG 的互补性直接搬过来。
2. Pevný 2016 只支持按袋语义选“均值或最大”和把池化放在网络内部，**不支持“均值＋最大双摘要拼接”**。现有文档把它标为该拼接的 `D1` 来源，证据等级过高，应降为 `D3` 任务化构造。
3. 四篇核心论文都没有 O11 分数层因果前缀。MalMoE 用整图均值路由专家，Berruz 用连续批特征统计触发重训，MADCAT 在测试时更新编码器，Wasswa 做潜空间对齐且删除时间戳。它们只能约束“在线量无标签、不可读未来”，不能支撑具体前缀公式或效果。
4. 旧《第三章 MLP 四格消融加固实施计划》的 `C01p=C11p` 与 CPA 无关闭格问题已由提交 `ab9ed32` 撤销。提交 `a3a4cd4` 的新冻结矩阵以既有 CPA `B00/B10` 和实体算子 `max/0.5×mean+0.5×max` 构成有效 2×2，可进入一轮 LSPR23 零训练 S0。
5. 新 S0 是多算子方向的**较弱代理**：它没有 `Lp`，也没有新建分数层 CPA。因此即使四格出现正信号，也不能写成原两个更强方向已验证。

最终证据状态：**设计可行、实验待证**。

## 对两个方向的强度裁决

| 方向 | 机制来源 | 放置层 | 最小实验 | 当前上限 |
| --- | --- | --- | --- | --- |
| `Lp + max` 多算子实体聚合 | MalMoE 直接支持“不同信息统计可能互补”；Pevný/Ilse 支持池化选择与嵌入级放置 | 训练层暂缓 | 当前 S0 只以固定 mean-max 混合作代理；阳性后再决定是否另立 S1 | 精确并联为 `D3`，当前 S0 不验证它 |
| O11 分数层因果前缀统计 | MalMoE 的整图门控与 Berruz 的顺序批统计只作类比 | 决策层候选，不回传 O11 | 当前 S0 不新建分数层 CPA；另行提出须重走研究合同 | 直接来源为 `0`，当前 S0 不验证它 |

## 已冻结的唯一下一轮 S0

### 目的

只回答一个可证伪问题：在保持既有 `B00/B10` 逐流模型不变时，固定 `0.5×mean+0.5×max` 相对 `max` 是否在 LSPR23 实体运营指标上产生源年信号，并与既有 CPA 形成可识别交互。

### 四臂约束

- 因子一：既有 CPA 检查点 `B00/B10`。
- 因子二：实体算子 `max` 与固定 `0.5×mean+0.5×max`。
- `C00/C01'` 共享 B00 逐流输出，`C10/C11'` 共享 B10 逐流输出；逐流同一性为硬门。
- 混合算子在每个严格前缀和终端使用同一 `0.5/0.5`，不搜索、不拟合、不修改权重。
- 只读 LSPR23；LSPR24 特征和标签读取计数必须为 `0`。
- 固定等权无直接文献依据，只能称任务化无参数对照。它不是 `Lp + max`，也不是新分数层 CPA。

### 机械有效性门

- B00 配对格共享 B00 逐流分数，B10 配对格共享 B10 逐流分数；配对格的 logits/probability SHA、实体键、曝光顺序和有效样本必须逐位一致。
- 每个前缀只使用当前及严格过去的流；终端实体标签不得进入前缀特征、排序或阈值构造。
- 四格机制签名必须分别反映 `B00/B10` 和 `max/mix` 两因子，不能回退到已撤销的 `C01p=C11p`。
- 全程目标年特征和标签读取计数均为 `0`。
- 不拆完整并列组、不插值；名义 FPR 与实际 FPR 分列。

### 可观测量与停止门

- 必报逐流 AP、实体 AP、最大实体 AP、完整可达假阳性预算曲线、六档实际 FPR、首次告警实际 FPR、未告警率、1 基曝光序阶梯、并列组大小和资源。
- 只有源年出现真实非支配信号、至少一处严格改善且低预算无新退化，才允许 S1。
- 固定混合没有信号或产生低预算新退化，停止该代理方向，不进入训练层。
- S0 无论正负都只是筛选证据，不进入正文效果表。

## 训练层多算子裁决

**当前不允许直接进入训练层。** 文献只给出两种条件路线：

1. MalMoE 路线：不同算子放在不同专家中，使用图级上下文硬选择。其原文还说明直接 AVG-DEG 拼接受尺度偏置与漂移分量污染影响。
2. 构件路线：先在嵌入级形成单算子摘要，再由袋级头分类。Pevný、Ilse 都没有验证 `Lp + max` 在加密流量漂移下联合训练。

只有新 S0 在 LSPR23 证明固定混合与既有 CPA 形成真实非支配信号后，才允许另立**冻结骨干 S1**。S1 若要回答原始 `Lp + max`，必须把 Lp 单头、max 单头、联合头和同容量对照分开；不能把 mean-max S0 的信号直接升级。若需要辅助损失进入共享骨干，ForkMerge 只提供更新过滤的训练合同，仍须另做 S2，不得由文献直接放行。

## 正文措辞

### 现在允许

- “MalMoE 在合成图漂移实验中观察到不同节点统计对不同漂移形态具有互补稳健性。”
- “本研究据此把多算子互补登记为待验证假设，并先以固定 mean-max 无参数算子进行源年零训练消融。”
- “本次核验的四篇近期漂移论文分别采用表示层鲁棒化、潜空间对齐、测试时训练或漂移触发重训，未直接评估逐流分数的实体内因果前缀。”
- “决策层候选只使用无标签、当前及历史可得量；其有效性由本项目实验裁决。”

### 实验前禁止

- “MalMoE 证明了 `Lp + max` 并联有效。”
- “Pevný 提出了均值＋最大或 `Lp + max` 双摘要拼接。”
- “三篇/四篇漂移论文支持分数层 CPA。”
- “因果前缀是近期漂移方法的主流、首创或已验证机制。”
- “训练层多算子提高了 LSPR23→LSPR24 稳健性。”
- “本方法对任意时间跨度均可免重训。”
- 使用 Berruz 的绝对准确率作本项目基线，或使用 Wasswa 的召回率作精确效果参照。

## 研究空白与最佳方向

1. **算子互补缺证**：尚无本轮全文证明同一逐流分数上的有限 `Lp` 与 `max` 对跨年漂移互补。当前 mean-max S0 只是最低成本代理筛选，不能关闭精确 `Lp + max` 的证据缺口。
2. **融合规则缺证**：若两个算子确有互补，仍不知道直接拼接、晚期分数融合还是硬门控更合适。MalMoE 的证据更偏向分支隔离与门控，不支持裸拼接。
3. **分数层因果缺证**：现有工作统计图、特征批或更新损失，没有把实体内逐流分数变成严格前缀告警。该空白只能写成“本次四篇未覆盖”，不能写成全领域空白。
4. **评价必须先于扩模**：首先在共同整数假阳性预算、完整并列组和首次告警轴上验证；没有源年信号时停止，不以训练层扩模替代实验裁决。

## 主代理动作

1. 接受“一轮 S0、训练层暂缓、正文只写待验证假设”的总裁决。
2. 采用提交 `a3a4cd4` 的有效 2×2，禁止恢复提交 `ab9ed32` 已撤销的旧 Task 2。
3. 把新 S0 的论文定位写成“受多算子互补现象启发的固定无参数代理，实验待证”，不得称 `Lp + max` 或分数层 CPA。
4. 将 Pevný 的证据映射改为“均值或最大选择＋嵌入级池化”，删除其对双摘要拼接的 `D1` 归属。
5. S0 通过后才单独审批冻结骨干 S1；S0 失败即停止训练层扩展，不再用文献数量救回。

## 限制

- 核心四篇原始 PDF 均已核验，缺失全文为 `0`。
- Pevný、Ilse、MIDAM 的原件指针在当前工作树不可读，本轮按任务合同使用其既有全文笔记；ForkMerge 已回原件。
- 本轮未联网新增候选，也未重新查询或修改 Zotero。
- 未运行任何本项目实验，因此没有“实验支持”结论。
