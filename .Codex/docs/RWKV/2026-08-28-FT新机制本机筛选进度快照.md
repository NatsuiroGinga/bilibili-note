# FT 新机制与本机筛选进度快照

<!-- RESEARCH_ROUTE=RWKV -->

- **快照时间**：2026-08-28 14:18 CST
- **用途**：周额度不足前的精确恢复点；只记录当前 FT 单基座 Goal、机制裁决、代码与实验状态、未提交文件和唯一下一步。
- **上位规则**：先读[路线规则](AGENTS.md)、[路线总控](RWKV路线总控.md)和[第三章恢复卡](RWKV第三章恢复卡.md)。

## 一、当前 Goal 与方法裁决

当前 Goal 已重设并处于 `active`：第三章以 FT-Transformer 为唯一主基座，寻找两个经过朱式任务化改造的可微机制，完成正式 C00/C01/C10/C11、LSPR23 选择封印、LSPR24 同批描述、表图正文与自审。

当前章级机制方向：

1. **结构级：因果实体记忆交叉注意力**。字段/历史 Token、数值 tokenizer 和类型嵌入只作 FT 适配与该机制的实现细节，不单列创新。
2. **训练级：低误报实体排序训练**。普通实体 AP 选轮只作实验协议，不计创新。

两机制均为“设计可行／实验待证”。任何有效性与创新性主张须等待 LSPR23 单臂和正式 2×2；LSPR24 不得参与筛选。

## 二、子代理状态

| 代理 | 模型与强度 | 当前状态 | 已有结果 |
| --- | --- | --- | --- |
| `ft_code_interfaces_audit_sol_high` | `gpt-5.6-sol/high` | 完成 | 代码接口审计完成；跨设备单代码合同提交 `0f360c1`，454 行、7/7 链接存在 |
| `ft_candidate_evidence_miner_terra_medium` | `gpt-5.6-terra/medium` | 完成 | 基线与消融编排提交 `a3053da`；冻结已发表基线、FT C00、原组件三层基线 |
| `ft_new_mechanisms_five_papers_sol_max` | `gpt-5.6-sol/max` | 运行中 | 五篇核心已读 5/5；扩展本地/联网饱和检索尚未完成。普通历史 Token 为构件，M-B 为协议，通用 M-E 已占用 |
| `ft_causal_memory_lowfpr_theory_sol_max` | `gpt-5.6-sol/max` | 收尾中 | 四文档 4/4、1086 行；21 篇承重近邻；12/12 机制验证矩阵、13/13 公式验证矩阵完成，尚待最终提交 |

理论代理确认的硬阻断：正式严格过去接口仍缺 `available_ns`、`source_record_id`、`previous_segment_row`、`role_id`；未补齐前不得实现或运行正式因果实体记忆机制。

## 三、本机环境与规则更新

- 本机 Apple M1、16 GB 统一内存。
- 唯一已验证本机实验解释器：`/opt/miniconda3/envs/rwkv/bin/python`。
- PyTorch `2.12.0`，沙箱外 MPS 可用。
- 已只向该环境安装 scikit-learn `1.9.0`、SciPy `1.18.1`、joblib `1.5.3`、threadpoolctl `3.6.0` 和必要依赖；项目 `pyproject.toml`、`uv.lock`、`.venv` 未修改。
- 项目 `.venv` 缺 NumPy/PyTorch，不得用于真实数据扫描、训练或评价。
- 上述分流规则已写入 `thesis/experiments/llm_probe/AGENTS.md`，当前尚未提交。

## 四、已完成真实数据前置

LSPR23 训练区字段基数扫描已完成 `7/7`：

| 字段 | `unique_count` |
| --- | ---: |
| SrcPort | 61057 |
| DstPort | 6041 |
| Protocol | 7 |
| L3/L4 Protocol | 5 |
| Int/Ext Dst IP | 2 |
| External_src | 2 |
| External_dst | 2 |

- 收据：`thesis/experiments/llm_probe/runs/diagnostics/ch3-lspr23-field-cardinality-receipt-v1/field-cardinality-receipt.json`
- SHA-256：`f56d949a27d15d2fc26c26116a1023d494cc5a873f5493e2e9cdd46ae86a9350`
- 目标年读取：0。
- 83 字段未减少：78 个分位数字段＋3 个二值直通字段＋2 个协议词表字段；加 `[CLS]` 共 84 Token。

## 五、C00 双选轮实现状态

已新增、尚未提交：

- `tools/ch3_ft_c00_dual_selection.py`
- `configs/ch3-ft-c00-dual-selection-mps-screening-v1.json`
- `configs/ch3-ft-c00-dual-selection-cuda-formal-v1.json`
- `configs/ch3-lspr23-field-cardinality-receipt-local-v1.json`
- `scripts/local_launchers/run_ch3_ft_c00_dual_selection_mps_screening_v1.sh`
- `scripts/remote_launchers/run_ch3_ft_c00_dual_selection_cuda_formal_v1.sh`
- `.Codex/docs/RWKV/2026-08-28-FT-C00双选轮本机筛选实现/`
- `thesis/experiments/llm_probe/AGENTS.md` 修改。

静态检查已通过：Python 语法、MPS 配置、CUDA 配置、两个 `bash -n`、限定 `git diff --check`。两配置的科学身份均为：

`97fb18ab72419634e55f51b8f4042eb57899a8fc4c457fea2de46541b87e09f8`

模型是 924,283 参数裸 FT，不经过旧 CPA 融合层或 `p_log`。同一训练轨迹维护逐流 AP 与实体 AP 两份最优检查点；MPS 权重禁止续训正式 CUDA。

## 六、MPS 校准结果与当前运行

第一次校准（微批 `1×64`、验证批 1）已完成：

- 完整优化步：`15.0128958225` 秒。
- 完整源验证：`1344.5833699703` 秒。
- 一步后逐流 AP：`0.0710751050`。
- 一步后实体 AP：`0.0016371343`。
- 旧配置投影完整 `20×1000` 预算：约 `90.8749` 小时。
- 这只是随机初始化后一步的工程校准，不是模型效果结论。

第二次校准复用已拟合输入变换，运行配置改为微批 `4×16`、验证批 8；科学身份不变。当前：

- 运行身份：`ch3-ft-c00-dual-selection-mps-screening-v1`
- 状态：`running/probe-runtime`
- 完整优化步已完成。
- 完整源验证：`8976/22444`（40.0%）。
- 吞吐：约 `12.6` 序列/秒。
- 最近预计剩余：约 `1070.2` 秒。
- LSPR24 读取：0。

输出根：`thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c00-dual-selection-mps-screening-v1/`。

## 七、指标边界

C00 双选轮只用逐流 AP 与实体 AP 选检查点。正式机制评价仍必须实现并报告：最大实体 AP、六档共同实际 FP 预算检测率、完整实际 FP 曲线及面积、首次告警未检出率与曝光位置、逐流护栏、参数量、训练/推理时间、内存、检查点与恢复。名义 `dr_at_fpr` 不得替代实际完整并列组曲线。

## 八、恢复后的唯一下一步

1. 只读检查第二次校准的 `status.json` 与 `budget-receipt.json`；不要重复启动。
2. 校准成功后，用新实测冻结本机 `epochs`、`steps_per_epoch` 和预算收据哈希。
3. 提交上述 C00 工具、两配置、两启动器、本机基数配置、AGENTS 规则与实施文档。
4. 启动本机 C00 双选轮筛选；服务器可用后以同代码、独立 CUDA BF16 身份从头正式训练。
5. 等扩展文献与理论代理提交后，执行 brainstorming，冻结两个正式机制及单臂实验；因果数据接口未补齐前不得启动机制一。

