# XGBoost 与相近树模型结构改造文献审计任务计划

- **日期**：2026-08-19
- **研究路线**：`RESEARCH_ROUTE=RWKV`
- **代理映射**：`xgboost_tree_adaptation_literature_sol_max → gpt-5.6-sol → effort=max`
- **任务性质**：为第四章基座设计核验树模型的序列、实体、多尺度、校准、漂移与预算改造谱系；不写论文正文，不运行实验，不裁决方法有效。
- **独占写入范围**：本目录下 `task_plan.md`、`notes.md`、`直接近邻证据矩阵.md`。除非取得新增合法全文且仓库流程强制要求，否则不改 `raw/`、`wiki/`、总控、恢复卡、正文、代码或实验配置。

## 一、目标

回答以下五个问题，并把答案限定在可逐页核验的全文证据内：

1. XGBoost、梯度提升决策树与随机森林如何接入序列上下文、实体历史或多尺度统计？
2. 树模型如何完成窗口／尺度选择、实体聚合、分数校准、标签漂移适应或预算约束？
3. 是否已有与“固定二进多尺度实体特征＋XGBoost”或“树分数＋目标先导段预算／先验校正与回退”直接同构的方法？
4. 哪些方法可作为同一冻结合同下的强基线，哪些只能作为公式、特征器、校准器或评价部件？
5. 新证据分别如何压缩或保留 DTEP 与 PBC 的原创边界？

## 二、检索对象与操作化定义

### 2.1 树模型上下文改造

- 原始逐流或逐事件行仍由 XGBoost／梯度提升树／随机森林分类；额外输入必须来自同一实体的历史。
- 序列上下文包括严格历史前缀、滑动窗口、指数衰减统计、事件间隔、趋势、差分与频次。
- 多尺度包括固定二进窗、任意多窗、随机区间、多分辨率变换或多个时间衰减尺度；须区分“固定并列输入”“自动选窗”“自适应窗口”。

### 2.2 实体与袋级改造

- 实体键可以是主机、地址对、用户、设备、服务器或多示例袋。
- 区分三种路径：先聚合特征再训练树、逐实例树分数再聚合、直接训练袋级树／提升模型。

### 2.3 决策层适应

- 分数校准：逻辑校准、等渗回归、温度或其他后处理；必须记录校准数据来源。
- 先验／标签漂移：只改变类别先验、重加权或后验校正；记录条件假设、诊断与回退。
- 预算约束：固定假阳率、告警数量、代价或计算预算；区分训练目标、阈值选择与部署后处理。

### 2.4 两个直接同构判定对象

- **T1：固定二进多尺度实体特征＋XGBoost**：同一实体严格因果历史；窗口长度为二进序列或等价固定多分辨率；每尺度统计作为 XGBoost 输入；尺度合同在源域冻结。
- **T2：树分数＋目标先导段预算／先验校正与回退**：源域树模型冻结；只读目标时间先导段；据无标签预测或合法小标签估计预算／先验；条件失配时回退；冻结后在不相交后段评价。

## 三、纳入、排除与证据等级

### 纳入标准

- 原论文、出版社全文、作者稿、PMLR、arXiv、会议开放论文或官方技术报告；题录有 DOI、arXiv 或稳定出版标识。
- 至少直接支撑序列／实体／尺度／校准／漂移／预算中的一个结构关系。
- 强结论必须能给出物理页码、章节、公式、算法或表格位置。
- 经典奠基工作不限年份；直接近邻重点覆盖 2016—2026，近期查重覆盖 2025—2026。

### 排除标准

- 仅把 XGBoost 用作普通静态表格分类器，没有历史、实体、尺度或决策适应关系。
- 只有摘要、搜索摘要、二手综述或产品说明；最多作为后续检索线索。
- 使用目标后段标签事后选窗、选阈值或选模型，且不能为冻结先导协议提供正面证据。
- 仅模型内部多层／多尺度卷积，未把尺度关系映射到树输入、树结构或树分数决策。

### 证据等级

| 等级 | 要求 | 可支持结论 |
| --- | --- | --- |
| E3 | 正式或作者全文；题录核验；页码／公式／算法锚点完整 | 机制占用、直接差异、强基线资格 |
| E2 | 可核验预印本全文；版本与页码完整 | 同上，但须注明预印本 |
| E1 | 正式元数据或摘要，全文缺失 | 仅候选与人工补件 |
| E0 | 二手或未核验线索 | 只用于扩展查询，不进结论矩阵 |

## 四、直接同构等级

| 等级 | 判定 |
| --- | --- |
| D3：完整同构 | T1 或 T2 的全部关键关系在同一工作中出现 |
| D2：强近邻 | 同现至少两个关键关系，但缺因果、实体、冻结、回退或源域选择中的一个决定性环节 |
| D1：基础部件 | 只占用一种算子、特征器、校准器、漂移估计器或预算机制 |
| D0：边界／反例 | 表面相似但信息权限、时间顺序、评价单元或模型角色不同 |

## 五、六阶段计划

- [x] **阶段一：恢复与范围冻结**——完整读取仓库与 RWKV 规则、路线总控、恢复卡、登记册、既有 XGBoost–CPA–ELP 审计和第四章双机制直接近邻查重材料；定义 T1／T2 与证据等级。
- [x] **阶段二：本地与 Zotero 盘点**——按稳定标识去重；当前未发现 TSF／CIF／DrCIF、gcForest、Adaptive XGBoost／Random Forest、MIForests、GPBoost、经典树分数校准与 Saerens 先验修正的精确题名原件或条目；Zotero 9.0.6 只读接口正常。
- [x] **阶段三：联网扩展检索**——完成 4 组谱系检索与 13 组直接同构窄查询，共 17 组；T1 与 T2 均在最新 `D2` 后连续两轮未新增 `D2／D3`，满足停止条件。
- [x] **阶段四：全文核验**——累计 21／21 篇完成全文定位、题录核验和页码／公式／章节证据摘录，其中 E3 10 篇、E2 11 篇。
- [x] **阶段五：跨论文综合**——已区分可借鉴改造、强基线、条件性基线、基础部件，并裁定 DTEP／PBC 的保留边界；所有性能主张保持“实验待证”。
- [x] **阶段六：引用核验与交付**——已核对元数据、稳定标识、全文入口、矩阵计数、人工补件与三份制品边界。

## 六、停止条件

同时满足以下条件后停止扩展：

1. 树模型上下文、多尺度、实体聚合、校准／先验、漂移／预算五类各有至少一篇 E2／E3 强证据与一篇边界证据；
2. T1 与 T2 各完成至少两轮窄查询，最后新增 D2／D3 后连续两轮无新增；
3. 2016—2026 与经典谱系均覆盖，且 2025—2026 近期查询已执行；
4. 所有最终提及的论文均有本地原件、合法全文位置或精确人工补件条目；
5. 强基线与基础部件已分开，DTEP／PBC 只保留有边界的任务化表述。

## 七、已作决策

- 不重复既有 CPA–ELP 审计已经逐页核验的 Lee 与 Stolfo、Bilge、Gülçehre、Revaud、Gehri、Dijk 等；仅在综合矩阵中交叉引用其已有证据。
- 不重复第四章双机制审计已经逐页核验的 BBSE、DFM、PseudoCal、Haiba、NIDES、Slips、El-Hajj、CBAM、Choi 等；本轮只补树模型特有结构和尚缺的直接同构关系。
- “树模型结构改造”包括树前的历史／多尺度特征器、树本身的在线／漂移改造、树后的实体聚合与校准；三层必须分栏，不能混写成一种创新。
- 文献只能证明来源与可比性；任何在 LSPR23→LSPR24 上的有效性均为实验待证。

## 八、错误与阻塞

- 2026-08-19：首次在指定隐藏目录创建任务文件时，沙箱返回 `Operation not permitted`；已通过受控授权只创建用户指定目录，未改其他路径。
- 2026-08-19：MDPI 正文入口返回 `429`，PMC PDF 入口返回下载挑战页，PMC 开放包地址暂时返回 `404`；已改用 Europe PMC 官方全文 XML 与 PMC 正式元数据核验 Hung（2026）的第 3.4、3.6、4.4、4.5 节，因此不构成人工下载阻塞。该文无连续物理页码，证据锚点按正式章节记录。
- 当前没有新增付费墙阻塞；若后续命中影响 T1／T2 裁决且无合法全文，将立即登记人工下载清单并通知根代理。

## 九、当前状态

**任务完成。** 累计核验 21 篇合法公开全文，完成 17 组查询；T1 与 T2 最高均为 `D2`，未发现 `D3`。两条直接同构路线均已满足最新 `D2` 后连续两轮无新增的停止条件。结论只表示本轮定义、数据库与查询范围内未命中完整同构，不构成全球首创证明；DTEP、PBC 的实际效果均为实验待证。

### 2026-08-19 全文检查点

- **已核验全文**：15 篇；工作副本及抽取文本位于 `/tmp/manifold-pdf-extract/xgb-tree-audit/`，公开入口和物理页码已写入 `notes.md` 与证据矩阵。
- **T1 当前最高近邻**：`D2`。TSF 的 `interRF` 已在同一论文中明确出现“长度为 2 的幂的区间特征＋500 棵随机森林”；Kitsune 已出现“网络实体／通道＋严格在线的五尺度衰减统计”。两者均未同现 T1 的全部关系。
- **T2 当前最高近邻**：`D2`。Saerens 等已出现“冻结分类器＋无标签新数据估计类别先验＋后验修正＋显著性检验决定是否执行修正”，但未定义因果先导段、实体告警预算和不相交后段冻结评价。
- **人工补件**：无。首批候选不存在付费全文阻塞；本轮遵守独占写入边界，未改 `raw/`、`wiki/` 或 Zotero。

### 2026-08-19 增量全文检查点

- **累计核验全文**：19 篇；新增 Pinchuk（2026）、TreeText-CTS（2026）、TAP-GPPS（2026）与 Hung（2026）。前三篇为 arXiv 可核验预印本，后一篇为 *Sensors*／PMC 正式全文。
- **T1 最新 `D2`**：Pinchuk 已把“实体严格过去信息＋多组尾随窗统计＋单一 XGBoost”放在同一方法中；TreeText-CTS 已把患者实体、`{1,2,4,8,16,32,48}` 小时多尺度统计、仅训练集拟合并冻结的 XGBoost 放在同一方法中。两者均未同时满足网络逐流、纯二进冻结尺度合同、源实体折外尺度选择和单一 XGBoost 最终分类器。
- **T2 最新 `D2`**：TAP-GPPS 已把校准源模型、无标签目标先验估计、后验修正和目标约束阈值选择联合；Hung 已把 XGBoost／随机森林分数、验证集固定假阳率阈值和隔离测试联合。两者均未形成“目标因果先导＋实体告警预算＋失配诊断／回退＋冻结后段评价”的完整合同。
- **人工补件**：无。Hung 的正式全文以 Europe PMC JATS／PMC 元数据核验；PDF 防自动化不影响当前技术裁决。

### 2026-08-19 停止条件增量检查点

- **累计核验全文**：20 篇；新增 Anctil、Hauguel 与 Noel（2026）medRxiv 预印本。
- **T1**：TreeText-CTS 后两轮不同查询均只重复命中既有近邻或 D0／D1 边界，未新增 `D2／D3`，扩展检索停止。
- **T2**：Hung 后第一轮无新增；第二轮命中 Anctil 等的批次隔离固定特异度 XGBoost，裁为 `D2`，连续无新增计数重置。
- **人工补件**：无；Anctil 等官方 PDF 已合法取得并完成物理页码核验。

### 2026-08-19 PBC 强近邻检查点

- **累计核验全文**：21 篇；新增 Li 等（2026）arXiv v2。
- **T2 最新 `D2`**：Li 等已在同一方法中固定源模型、用互不重叠的目标 `C1／C2／T` 分别完成带标签先验匹配、保形阈值和留出评价，并以阳性标签不足触发全审查安全回退。
- **仍非 `D3`**：基座仅称 Clinical／Radiomics 风险模型，没有树模型实例；目标三分区为回顾性随机拆分，不是按到达时间冻结的因果先导；回退是全审查／延迟，不是回到源阈值。
- **人工补件**：无；arXiv v2 共 20 页，题录、公式、表格与限制均已核验。

### 2026-08-19 最终可恢复检查点

- **完成量**：21 篇全文、17 组查询；E3 10 篇、E2 11 篇。逐篇证据、页码／公式、支持点与不能支持点均已写入 `notes.md` 和证据矩阵。
- **终止依据**：T1 在 TreeText-CTS 后的 Q11、Q12 连续无新增 `D2／D3`；T2 在 Li 等后新增执行 Q16、Q17，连续无新增 `D2／D3`。两条路线均满足预注册停止条件。
- **直接近邻裁定**：T1、T2 均为最高 `D2`、无 `D3`。T1 最接近 Pinchuk、TreeText-CTS、TSF／`interRF` 与 Kitsune；T2 最接近 Saerens、TAP-GPPS、Hung、Anctil 和 Li 等。
- **原创边界**：DTEP 仅保留“网络实体严格前缀＋源实体折外尺度选择并冻结＋同预算单一 XGBoost＋跨时间评价＋预注册失败回退”的任务化组合；PBC 仅保留“冻结树分数＋目标时间因果先导＋明确标签预算＋先验失配诊断＋实体告警／假阳率预算＋源策略回退＋冻结后段评价”的任务化组合。
- **人工补件**：无；没有最终裁决依赖付费墙、仅摘要来源或无法定位的全文。
- **范围确认**：只更新本目录三份指定文档；未改 `raw/`、`wiki/`、Zotero、RWKV 总控／恢复卡、论文正文、代码或实验制品。
