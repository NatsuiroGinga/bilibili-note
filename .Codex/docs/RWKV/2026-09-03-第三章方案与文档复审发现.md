# 第三章方案与文档复审发现（2026-09-03）

- **来源**：两个 sonnet 复审工作流的第一阶段产出。
- **状态**：**核验阶段全部因会话额度触顶失败**，故下列发现**均未经对抗核验**，
  等级为复审代理自评，**不得直接当作定论**。核验补做后再更新本文件。

## 四格实验方案复审

### ?（自评总裁定：可启动，无阻断项——新四格内部（C00/C10/C01/C11共享同一骨干与同一Baseline）的比较是公平、自洽、可执行的，符合"单种子先导/screening_only"这一实验阶段的定位。但归因论证与文档一致性均须先补：(1) 骨干缩容与补底座两个变量同时改变，任何与既有half档读数(0.650418/0.886216/0.711063)或全容量CUDA读数的跨代比较目前都无法单一归因，若C01_new−C00_new明显小于+0.235798，需另跑约10.5小时的最小解耦实验才能定因（发现1）；(2) 新C10(ETA真实训练)分别与旧C10-half(零训练重聚合)、全容量线旧C10(CEM机制)是两种不同的"同名不同物"，必须在结果文档中显式标注估计程序/机制名，否则会重蹈本文档自己记录过两次的历史误判(发现2、3)；(3) 第三章定案.md§4.2的执行步骤表仍是补底座决定之前的旧三训练方案，与已裁决的四次训练新定义矛盾，建议启动前先修订，避免按旧步骤表误操作(发现6)；(4) 单种子交互项I的解读需附加噪声边界声明(发现7)。以上均为文档/解读层面的最小补课，不构成暂停21小时实验本身的理由。）

#### [披露] 骨干缩容（d=96/3层→d=32/1层，参数量240,955→14,261，约17倍）与补底座（新增实体级BCE共同底座）在同一次实验中一起引入，跨代读数无法单一归因

- **理由**：四格设计的不对称与改法.md 第157-175行（§6.5用户裁决）要求给四格补上共同底座“实体级损失”；第三章定案.md 第122-154行（§4.1）同时把骨干从既有half档（d=96、3层、240,955参数，见恢复卡第81-100行〇之二点五表）进一步缩到d=32、1层、14,261参数（第三章定案.md第135行，已用`.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md`第26行、第32-35行核验为闭式精确值）。既有可比较基准（C00-half=0.650418、C01-half=0.886216、C10-half=0.711063）全部是旧骨干＋旧Baseline（无实体级BCE）测得，与新计划的骨干规模、Baseline定义同时不同，两个自变量在这一次21小时实验里被捆绑引入，不存在只变一个变量的对照臂。
- **失败形式**：21小时跑完后，若C01_new−C00_new的量级明显小于既有+0.235798（36.25%），撰写结论时极易直接写成“补底座稀释了BER增益”（四格设计的不对称与改法.md第132-143行§6.3已预期这一解释方向），而实际上骨干参数量骤降约17倍本身就足以独立压低任何机制的可学习信号——把两种可能原因中的一种直接当成结论写入文档，属于恢复卡〇之二点八开篇总结的“用推算/单一原因代替实测”同型错误，一旦写入将被下一轮任务（如权重再标定、候选取舍）直接引用而放大误差。
- **修法**：跑完新四格后，若观察到C01_new−C00_new显著小于+0.235798，再补一组最小解耦实验：在同一新骨干（d=32、1层）上另跑两条“旧Baseline（无实体级BCE）”对照臂——C00-oldbase-d32与C01-oldbase-d32。按本机S0=0.120步/秒、S1=0.043步/秒（第三章定案.md第57-58行）乘以8.47×缩容加速比（第131行）反推：C00类臂约2.78小时、C01类臂约7.72小时，合计约10.5小时（与任务简报“四次训练合计约21小时”的分臂结构一致，可交叉核验）。比较(C01−C00)_newbase_d32 与 (C01−C00)_oldbase_d32：接近则主因是骨干，明显更小则主因是补底座；该实验同时把“缩容档效应可外推”证据链在d=96→d=32这一步补上一个真实数据点（见发现5）。不建议在结果尚未出来前预先执行——先跑完21小时新四格，按其读数决定是否需要这10.5小时的补充实验。
- **自称证据**：读取恢复卡RWKV第三章恢复卡.md第81-100行（〇之二点五三格half读数与C10零训练重聚合口径）、第三章定案.md第44-58行（§2.2/§2.3）与第122-154行（§4.1新骨干配置与参数核验）、四格设计的不对称与改法.md第132-143行（§6.3稀释疑虑）；参数量14,261经`.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md`第20-35行独立核验（分词器5728+块8372+LayerNorm128+头33=14261）。

#### [披露] 新C10（ETA，端到端真实训练）与旧C10-half（0.711063，由C00权重零训练重聚合得到）是不同估计程序，不可直接比较

- **理由**：恢复卡RWKV第三章恢复卡.md第90-96行（〇之二点五）明确记载旧C10-half=0.711063是“由C00的tail检查点零训练重聚合算得”；四格设计的不对称与改法.md第157-175行（§6.5）明确裁决“C10因此变成真正的训练臂，不再是零训练重聚合”。同名“C10”在旧四格与新四格里对应两种结构不同的计算程序（一个是对已训练权重的推理期重聚合，一个是联合优化实体级BCE(ETA聚合)的独立端到端训练），数值接近或不接近都不能证明或证伪“骨干缩容/补底座对该读数的影响”，因为估计程序本身已经换了。
- **失败形式**：若新C10读数恰好接近0.711063或呈相近的相对增幅，容易被直接写成“缩容/补底座后ETA效应量级保持一致”，把两种不同实现的数字放进同一句比较——这正是恢复卡〇之二点八开篇“今天主代理已犯的错误类型”第一类错误在同一议题上再次发生的具体路径。
- **修法**：结果文档中，凡引用旧C10-half=0.711063处必须显式标注“零训练重聚合估计”；凡引用新C10处标注“端到端训练估计”；两者只能作为独立事实分别陈述，不得相减或计算比例。零额外实验代价，属写作纪律问题。
- **自称证据**：恢复卡RWKV第三章恢复卡.md第90-96行、第785-788行（第七节“C10用零训练重聚合”裁决原文）；四格设计的不对称与改法.md第157-175行（§6.5裁决原文）。

#### [披露（按历史两次误判记录，处理优先级应从严）] 新C10（ETA机制）与全容量CUDA线旧C10（CEM机制，0.6454，−0.0390，已否决）是完全不同的机制，文档自认这是“最容易混淆之处”，风险尚未消除

- **理由**：第三章定案.md第27-29行原文：“已废止：CEM（因果实体记忆）是旧机制一……它与ETA同占C10格位，是本页最容易混淆之处——凡见「C10负增益、机制一否决」，那说的是CEM，不是ETA。”该警示是针对已发生过两次的真实误判写的（定案.md第3-4行自述“主代理本人因其结构而两次误判：把CEM的否决当成ETA被否决”），但现有文档结构里CEM读数（0.6454，第39行）与ETA读数（0.711063，第50行）仍然分列在同一“C10”标签下的不同小节，混淆源并未从结构上消除。
- **失败形式**：结果报告若在表格或正文中裸写“C10”而省略机制名限定词，读者会把“C10目标年负增益、正式候选已否决”这一结论错误施加到新的ETA机制上，导致对ETA方向性下达一个基于错误机制归属的否定判断——与已发生两次的历史误判同型。
- **修法**：强制格式规则：任何表格中的“C10”单元格必须在同一格内附注机制名（CEM或ETA），不得裸写“C10”；正文首次出现C10读数时必须完整点名机制。零额外实验代价，属表格/写作纪律。
- **自称证据**：第三章定案.md第1-4行、27-29行、37-40行（全容量表，C10标注为CEM）；恢复卡RWKV第三章恢复卡.md第81-100行（半容量表，C10标注为ETA，读数0.711063）。

#### [披露] 全容量CUDA目标年读数（C00=0.6844/C01=0.7410/C10(CEM)=0.6454）与新计划的源年小骨干读数不具备直接比较条件

- **理由**：全容量读数（第三章定案.md第37-40行）是目标年（LSPR24）、零训练零参数更新的描述性评价、骨干d=192/3层924,283参数、旧Baseline（无实体级BCE）；新计划读数是源年（LSPR23）、正常端到端训练、骨干d=32/1层14,261参数（约65倍参数差）、新Baseline（含实体级BCE）。定案.md第111-120行（§三“还缺什么”第2项）明确写“ETA的目标年评价……从未做过”，说明两组数字连评价年份轴都不同，加上机制归属仍有发现3的CEM/ETA混淆风险，至少四个变量同时不同。
- **失败形式**：若结果文档把新源年小骨干读数与全容量目标年读数并列成表或计算差值，会把年份效应、骨干规模效应（约65倍）、Baseline定义效应、机制归属混淆四种因素叠加进一次比较，产出的数字无法解释也无法证伪。
- **修法**：全容量目标年读数只作为独立的“历史目标年基线”陈述，不与新源年小骨干结果同表并列；如需说明两者关系，只用文字分别陈述各自证据边界，不做数值运算。零额外实验代价。
- **自称证据**：第三章定案.md第33-42行（§2.1全容量表）、第111-120行（§三缺口清单）、第122-154行（§4.1新计划配置）。

#### [披露] “缩容档效应方向可外推”假设目前只有一个已验证数据点（d=192→d=96，仅BER单机制），新计划把宽度轴再推进约3倍、层数轴推进到此前零证据区间，且同时叠加补底座这一独立未验证变量——现有披露不完整

- **理由**：唯一已验证点：恢复卡RWKV第三章恢复卡.md第110-112行记载，全容量C01源年实体AP 0.9537 vs 缩容档half第10轮0.9260，“接近”，被称为“缩容档机制效应方向可外推的第一个正面证据……但只是单机制单方向”。该比较本身还部分依赖“旧编译路径全容量”对“新即时执行half”的跨口径比较，与恢复卡第673行“口径隔离（硬边界）……跨口径比较一律无效”的规则存在张力（文档称其为“旁证”而非正式判据，减轻但不消除张力）。新计划把宽度从96进一步压到32（第三章定案.md第131行，约3倍）、层数从3压到1（此前“零证据”，第153行原话），且同时新增补底座变量；ETA机制方向、C10(ETA)读数、交互项I在任何缩容规模下都从未验证过一次。定案.md第153-154行现有的“代价须写明”披露只提到“宽度再减半、层数轴零证据”两点，未提及补底座是叠加的第三个独立未验证变量。
- **失败形式**：结果文档若只援引第153行现成措辞（“本机结果只用于排序与止损，不进正文”），读者容易误解该限定只关于骨干规模，从而把本次首次在该缩容规模上测出的C10/C01/I读数当作比“骨干效应”更广泛意义的正式验证证据使用（例如用于第四章立项或候选取舍决策）。
- **修法**：在第三章定案.md §4.1“代价须写明”处补一句，明确列出本次实验同时叠加的独立变量清单（骨干宽度、层数、补底座三项），并注明“ETA方向、C10读数、交互项I在此前从未在任何缩容规模上验证过，本次是该证据链的第一个数据点，不是第二个”。零额外实验代价，纯文档修订，建议在21小时实验产出结果前后同步完成。
- **自称证据**：恢复卡RWKV第三章恢复卡.md第110-112行、第673行；第三章定案.md第131-132行（8.47×/层数比1.00:0.65:0.33，均为速度实测非效果实测）、第153-154行（现有披露范围）。

#### [披露（建议列为启动前必须完成的文档修订，不建议在文档未同步前按§4.2字面操作）] 第三章定案.md §4.2（执行顺序与决策点）仍描述补底座之前的旧三训练/C10零训练重聚合方案，与已裁决的四次训练新四格定义及已更新的§4.1骨干配置不一致——文档内部自相矛盾

- **理由**：第三章定案.md第156-163行（§4.2）仍写“段1: C00训练（双聚合，同时产出C00与C10）……合计≈25h”，这是补底座决定之前的旧方案；而四格设计的不对称与改法.md第157-175行（§6.5，2026-09-03用户裁决）已明确“C10因此变成真正的训练臂，不再是零训练重聚合，四格需四次训练”，第三章定案.md自身§4.1也已更新为d=32/14,261参数/8.47×的新骨干（第126-154行）。在新Baseline下，C00用实体级BCE(max聚合)、C10用实体级BCE(ETA聚合)，两者训练损失函数不同、梯度不同，“一次训练同时产出C00与C10”这一操作在新定义下已不成立，§4.2与§4.1/§6.5三处互相矛盾。
- **失败形式**：若执行者直接按§4.2的旧步骤表操作（只训练一次“双聚合C00”后零训练重聚合出C10），得到的C10读数会是发现2所述的错误估计程序产物，而不是新四格定义要求的端到端训练C10；21小时（实际会少算，因为省略了C10的独立训练）跑完后才发现C10臂未被真正训练，需要额外补跑约2.78小时（按发现1反推的分臂时长），且若未察觉直接把重聚合读数当新C10使用，会把发现2的估计程序混淆问题引入本应是新四格“干净”的内部自洽比较中。
- **修法**：启动21小时训练前，先把§4.2改写为四次独立训练（C00/C10/C01/C11），并用发现1反推的分臂小时数（C00≈2.78h、C10≈2.78h、C01≈7.72h、C11≈7.72h，合计≈21h）替换现有三段25h表格。零额外实验代价，纯文档修订，但应在启动前完成。
- **自称证据**：第三章定案.md第156-163行（§4.2现状）与第122-154行（§4.1新配置）对照；四格设计的不对称与改法.md第157-175行（§6.5裁决原文）；`.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md`第118-126行（“任务2：暂停，零产出……四格定义已改为共同底座实体级BCE＋C10变真实训练臂，四次训练而非三次；配置写法待主代理给出新简报”），印证按新定义的实际配置文件尚未建立。

#### [披露] 交互项I=C11−C10−C01+C00是四个各自带噪声的单种子读数的线性组合，其可信度低于任一单项delta，须显式加噪声边界说明，不得单独据以判定联动存在与否

- **理由**：四格设计的不对称与改法.md第196-203行（§六“证据等级”）自称“第三节的改法、第四节的四条待验证：设计推理，零实验验证”——即“补底座恢复I判别力”这一论证本身尚未经实验验证。已知C00-half后10轮的轮间噪声“相邻轮差中位0.0705/最大0.3280”（恢复卡第557-559行），说明单个格读数的波动幅度本身不小；I作为四项之差（独立同分布假设下方差近似为四项之和），噪声传播不小于任一单项。新计划为单种子（任务简报未提及多seed），不足以从统计上区分“真实交互”与“单种子噪声巧合为正/为负”。
- **失败形式**：单种子跑出的I若为正（哪怕量级很小），容易被直接写成“交互存在、联动创新点得证”；若为负或接近零，容易被直接写成“联动主张证伪”——两种情形都可能只是单种子噪声而非真实信号，与恢复卡〇之二点八记录的EAS候选“用单步/单轮快照外推全程”错误同构（本次风险点是“用单种子外推交互是否存在”）。
- **修法**：结果文档中，I的读数只能标注为“单种子观测值”，并同时报告作为噪声基准的参考尺度（可复用C00-half已实测的轮间波动幅度）；不得单独给出“I>0”或“I≤0”的正式判定。如需正式判定交互存在与否，按仓库证据顺序应留给“多种子正式实验”阶段处理，不在本次21小时screening范围内强制执行。
- **自称证据**：四格设计的不对称与改法.md第196-203行；恢复卡RWKV第三章恢复卡.md第557-559行（C00-half轮间噪声实测）、第346行（EAS候选单轮外推全程的历史错误记录，同构风险来源）。

### ?（自评总裁定：须先修阻断项。核心结论：实体级 BCE 目前只挂载在 z2=0 训练路径（training_step，服务 C00/C10），tools/ch3_ft_c00_dual_selection.py 的 _flow_phase_bare（C01 流阶段）与 _flow_phase_entity_memory（C11 流阶段）逐行读遍均无一处引用 mechanism.entity_bce，validate_config 也不拦截“entity_bce 开启但走 z2=1 路径”这一组合。若不先完成 thesis/methods/三目标梯度合成设计.md 第九节列出的 P0（7 项用户裁决）到 P3（挂载实现）——目前全部未完成，且该文档未被执行计划或恢复卡引用一次——直接按 thesis/methods/第三章定案.md §4.2 启动 C00→C01→C11 的 14-25 小时训练，C01/C11 将静默复现补底座之前的旧机制，21 小时后得到的读数无法支撑或证伪新四格的核心主张（“共同底座恢复交互项 I 的判别力”），必须先修这一阻断项才能启动。次要问题（entity_bce.weight=8192.0 为自述未验证占位值、恢复卡“1/128”数字已过时、entity_bce 与 entity_memory 同开被静默忽略、z1' 的双重编码缺一致性校验、c_equal_one_step_count 方向未预注册）均为披露或严重级别，不单独构成阻断，但建议与阻断项修复一并处理——尤其 w_e 标定诊断本身零训练成本，不应等 21 小时跑完后才发现权重不可解释。）

#### [阻断] C01/C11 训练路径完全未挂载实体级 BCE，新四格的“共同底座”实际只存在于 C00/C10 两格

- **理由**：新四格定义（thesis/methods/第三章定案.md 表格、thesis/methods/四格设计的不对称与改法.md §6.5）要求全部四格共享“逐流 BCE ＋ 实体级 BCE”底座，C01 在此基础上加 BER，C11 在 ETA 底座上加 BER。但逐行读取 tools/ch3_ft_c00_dual_selection.py 第 2331-2374 行（_flow_phase_bare，由 entity_ranking_training_step 调用于 C01）与第 2377-2450 行（_flow_phase_entity_memory，由 combined_mechanism_training_step 调用于 C11）后确认：两个函数从头到尾没有一处引用 mechanism.entity_bce / entity_ids / entity_bce_module，其 loss_sum 只由纯逐流 BCE 构成。validate_config（第 336-462 行）里 entity_bce 的校验块（399-413 行）不含任何与 entity_ranking_enabled / entity_memory_enabled 的互斥或依赖校验；训练分派（3060-3153 行）完全按 (entity_memory_enabled, entity_ranking_enabled) 二元组路由，不读取 entity_bce_enabled。全仓 rg 检索“g_base”与“entity_bce”均确认零处 z2=1 路径下的挂载。项目自身 2026-09-03 的设计文档 thesis/methods/三目标梯度合成设计.md §1.2、§9（P3 阶段）独立得出同一结论，并把修复列为尚未执行的“Minimal next action”；该文档 §10 列出的 7 项待用户裁决事项与 §9 的 P0-P6 实施顺序全部未完成，且 rg 全仓核验显示该文档未被 第三章定案.md、四格设计的不对称与改法.md 或 RWKV第三章恢复卡.md 引用一次。
- **失败形式**：若按第三章定案.md §4.2 的执行顺序（C00→C01→C11 训练，缩容档约 14-25 小时）直接为 C01/C11 创建 entity_bce.enabled=true 的配置并启动，代码会静默通过全部 require() 断言、正常跑满预定轮次、产出结构完整的收据（entity_bce.weight 等字段原样写入配置快照），但训练时 g_flow 只由纯逐流 BCE 产生——C01、C11 实际训练的目标与补底座之前的旧 C01/C11 逐位相同（除 RNG 外）。21 小时后得到的 C01/C11 读数不会体现“共同底座”任何效应，交互项 I 会继续呈现四格设计文档最初诊断出的不对称，只是换了个方向（三目标梯度合成设计.md 原话：“底座只在 z2=0 存在，不对称没有消除”）。此时 C00-vs-C01、C10-vs-C11 的差值无法被解释为 BER 的边际贡献，因为四格已经不在同一训练目标族内比较；若据此写入路线总控或正文，会把一个从未真正执行的实验当作已验证结论。
- **修法**：启动前至少完成 三目标梯度合成设计.md 第九节的 P0-P3：(a) 用户对 §10 七项裁决拍板（核心是把一阶不变量保护对象从 g_flow 换成 g_base 这一科学合同变更、w_e 标定路径、归一化口径二选一、埋点方案 A/B/C）；(b) 按 §4.5 修正累加器归一化口径；(c) 按 §5.2 用一次零训练诊断反传测 ‖g_flow‖、‖g_entity‖ 定 w_e 并登记；(d) 把 training_step 里已有的实体级 BCE 求和逻辑抽成共用函数接入 _flow_phase_bare 与 _flow_phase_entity_memory，_combine_and_step 第一实参由 g_flow 改为 g_base（该文档已给出 w_e=0 时的严格退化证明 D1，可作为回归验收）；(e) 补 V6/V7 两项配置校验。这些是常规实现工作（数百行改动＋若干零训练诊断步），可在半天到一天内完成并用一次 probe_runtime 核验（design doc §8 V1-V8），不需要动用 21 小时预算。在此之前不应创建或启动任何 C01/C11 的 entity_bce 正式配置。
- **自称证据**：直接读取 tools/ch3_ft_c00_dual_selection.py 第 2331-2374 行、2377-2450 行、336-462 行、3060-3153 行；`rg -n "g_base|entity_bce" tools/ch3_ft_c00_dual_selection.py` 确认 entity_bce 仅出现在 225-412、1678-1756 行（均属 training_step / 配置校验，非 z2=1 路径）；`rg -n "三目标梯度合成设计|口径推导" thesis/methods/第三章定案.md thesis/methods/四格设计的不对称与改法.md .Codex/docs/RWKV/RWKV第三章恢复卡.md` 零命中；通读 thesis/methods/三目标梯度合成设计.md 全文（972 行），§0/§1.2/§2.4/§9/§10 逐节确认。

#### [严重] entity_bce.weight = 8192.0 是自述“未验证”的占位值，validate_config 未强制其登记依据

- **理由**：thesis/methods/实体级BCE权重的口径推导.md 第七节明确自述该值“未验证（占位值）”，取自 effective_batch_size×sequence_length 的理论上界，而非该文件自己推荐的 ‖g_flow‖/‖g_entity‖ 实测比值标定程序（第五节）。已部署配置 configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json 第 107 行确认 weight: 8192.0。对照 tools/ch3_ft_c00_dual_selection.py：entity_aggregation（ETA）校验块（361-389 行）强制要求 numeric_provenance 字典与 touches_target_labels 字段，而 entity_bce 校验块（399-413 行）只要求 weight>0.0 与 aggregation 取值合法，代码层面不阻止一个无登记依据的权重进入训练，与仓库根 AGENTS.md“科研数值……缺项标'未验证'，不得进入实验”的门禁要求不符。
- **失败形式**：在完成 Finding 1 的挂载修复后，若沿用该占位权重跑满 21 小时四格，得到的差值实际反映一个从未校准的混合比 ρ=w_e‖g_e‖/‖g_f‖，可能远离设计文档建议的 ρ≈1 记账单位——ρ 过小会使实体级 BCE 的方向影响可忽略，复现“底座形同虚设”的原问题；ρ 过大则可能使 C00 偏离“裸 FT＋温和实体正则”，削弱其作为四格比较基准的意义。由于从未记录 ‖g_flow‖、‖g_entity‖ 的实测值，事后无法区分“机制无效”与“权重标定错误”两种解释，21 小时的信号可能只是权重伪影。
- **修法**：启动前先执行 三目标梯度合成设计.md §5.2/§7.4 方案 B 的一次诊断：同一批真实 LSPR23 数据，对 L_flow 与 L_entity 各反传一次（不训练、不更新参数），记录 ‖g_flow‖、‖g_entity‖，按修正后的归一化口径取 w_e=‖g_flow‖/‖g_entity‖ 并按根 AGENTS.md 六项登记依据。成本为一次前向＋两次反传，分钟级，不占用 21 小时预算。建议同时在 validate_config 的 entity_bce 校验块补充与 entity_aggregation 同等的 numeric_provenance 强制要求。
- **自称证据**：thesis/methods/实体级BCE权重的口径推导.md 全文（158 行），尤其第七节；configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json 第 107 行；对比 tools/ch3_ft_c00_dual_selection.py 第 361-389 行与第 399-413 行。

#### [披露] 恢复卡 L4 条“w_e=1.0 被压到 1/128”的具体数字已被项目自身文档更正，尚未回写

- **理由**：本任务简报与 .Codex/docs/RWKV/RWKV第三章恢复卡.md L4 条均称“实体项本身已是逐实体均值……故写 w_e=1.0 实际被压到约 1/128”。但 thesis/methods/实体级BCE权重的口径推导.md 第三节（同日、时间戳更晚）已自我更正：代码中 entity_bce_value 是 .mean()（tools/ch3_ft_entity_bce.py 第 141 行 `per_entity_bce[scored_mask].mean()`），不是 .sum()，故压缩倍数是 1/N_flow 本身，量级在 1/8192 到 1/几百之间随本步真实有效流数波动，而非固定的 1/128；“1/128”隐含“实体项传入的是逐实体损失之和”的假设，与实现不符。thesis/methods/三目标梯度合成设计.md 的 R6 条（§4.5）同样采用更正后的 M/N_flow 表述。我独立读取 tools/neural_precision_runtime.py 第 461 行（normalized_loss = loss_sum / self.total_valid_units）与上述 141 行确认该更正成立。
- **失败形式**：若后续子代理或正文写作继续摘抄恢复卡当前文字直接引用“1/128”这一具体数字，会低估实际压缩幅度（真实上界更接近 1/8192，比 1/128 还弱约 64 倍），从而误判需要多大的 w_e 才能抵消归一化；已作废数字会像本任务的简报一样被反复传递给下一轮子代理。
- **修法**：按仓库“结论作废时必须同时作废引用它的所有位置”的既有纪律，把恢复卡 L4 条更新为引用口径推导.md 第三节的更正版本（压缩因子为 M/N_flow，量级 1/8192 到 1/几百），并注明“1/128 已被 2026-09-03 同日文档取代，不再单独引用”。纯文档同步，零实验成本。
- **自称证据**：.Codex/docs/RWKV/RWKV第三章恢复卡.md 第 594-603 行 L4 条；thesis/methods/实体级BCE权重的口径推导.md 第 73-83 行第三节；tools/neural_precision_runtime.py 第 461 行；tools/ch3_ft_entity_bce.py 第 141 行。

#### [披露] 补底座后 c_equal_one_step_count 的预期方向未预注册——按 P1/P2 给出预测

- **理由**：任务要求在看到结果前给出方向性预测并作为预注册，但第三章定案.md、四格设计的不对称与改法.md、三目标梯度合成设计.md 与恢复卡均未对“补底座后 c_equal_one_step_count 如何变化”给出预注册方向（三目标梯度合成设计.md §7.2/§8.2 的 D 组诊断量含 ρ 漂移、饱和占比 D3 等，但未专门预注册 c=1 步数占比的变化方向），不预注册会使事后解释失去证伪意义。
- **失败形式**：无预注册时，结果出来后无论方向如何都可能被事后解释为“符合预期”，违反根 AGENTS.md“证伪判据须在看到数据前冻结”的要求，削弱该观测量作为机制证据的说服力。
- **修法**：基于三目标梯度合成设计.md §1.3 已核验的代数性质 P1（饱和区内合成对 g_rank 的正缩放不变）与 P2（饱和区内合成对 g_base 一次正齐次）：补底座后 g_base=g_flow+w_e·g_entity，只要 g_entity 不与 g_flow 强烈反向（cos(g_f,g_e) 目前从未测量），‖g_base‖ 一般不小于 ‖g_flow‖，而 c=min(1,‖g_base‖/‖g_rank+‖) 的分子变大会使 c 更容易被限制在上限——**预测方向为 c=1 的步数占比相对当前 C01-half 第 10 轮基线（72.4%）增大，而非减小**。该预测以 cos(g_f,g_e) 不强烈为负为前提；一旦 design doc §7.2 的 cosine_flow_entity 埋点显示夹角为强负值，应视为预测不成立，转而支持“两个粒度冲突”这一替代解释（对应判据 S1 的反面）。建议在实现 Finding 1 的挂载修复时把这条预测与 D4/cosine_flow_entity 埋点一并写入验收清单。
- **自称证据**：thesis/methods/三目标梯度合成设计.md 第 174-190 行推论 P1、P2 的代数推导（逐行核验成立）；.Codex/docs/RWKV/RWKV第三章恢复卡.md〇之二点七点五节“C01-half 第 10 轮 c_equal_one_step_count=276，72.4% 的步排序梯度被压到 ‖g_flow‖”的既有读数作为基线参照。

#### [披露] entity_bce 与 entity_memory(CEM) 同开被静默忽略；z1' 的双重编码缺一致性校验

- **理由**：恢复卡 L3.1 条已记录“entity_bce 与 entity_memory 同时启用会被静默忽略，未加互斥校验”。核实：entity_memory_training_step（z1=1,z2=0）与 _flow_phase_entity_memory（z1=1,z2=1 下 C11 的流阶段）均不读取 mechanism.entity_bce（rg 确认 entity_bce 仅出现在 225-412、1678-1756 行，不落在这两个函数的行范围内），故该组合确实会被静默忽略，与 三目标梯度合成设计.md §8.1 V7 条“entity_bce 与 entity_memory 同开须报错”的要求不符，该校验目前不存在。另外 z1'（ETA）开关编码在两处独立字段：mechanism.entity_aggregation.enabled/kind（驱动评价口径与 BER 内层聚合，367-396 行）与 mechanism.entity_bce.aggregation（驱动底座聚合，410-412 行），二者之间没有一致性校验——同一份 C10/C11 配置理论上可以让 entity_aggregation.enabled=false 同时 entity_bce.aggregation="tail_mean"，得到一个“评价口径是 max 但训练底座是 ETA”的从未设计过的格，这正是 design doc §8.1 V6 条已列出但未实现的检查。
- **失败形式**：这组 latent 缺陷不影响本次四格计划本身（新四格不使用 CEM，V6/V7 的字段冲突尚未被任何现存配置触发），但若后续扩展本机制时创建了触发这两个组合的配置，代码会静默通过并训出一个身份不明的模型，且没有报错提示——按本任务简报所述的静默失败模式，这类缺陷往往在数周后才被发现，届时相关运行的算力已浪费。
- **修法**：在 validate_config 中补两条 require：(1) entity_bce_enabled 与 entity_memory_enabled 同时为真时 raise；(2) entity_bce_enabled 且 aggregation=="tail_mean" 时要求 entity_tail_aggregation_enabled 为真（aggregation=="maximum" 时要求其为假），确保两处 z1' 编码一致。改动约 10-15 行，零训练成本，可与 Finding 1 的挂载修复一并实现验收。
- **自称证据**：`rg -n "entity_bce" tools/ch3_ft_c00_dual_selection.py` 全量结果（仅 225-412、1678-1756 两段）；.Codex/docs/RWKV/RWKV第三章恢复卡.md 第 585-589 行 L3.1 条；thesis/methods/三目标梯度合成设计.md 第 801-802 行 V6/V7 条。

### ?（自评总裁定：须先修阻断项。逐条回答复审要求的五个问题：

【Q1：nonargmax_grad_share 能否替代旧交互项 I 作为联动判据】不能单独替代，二者是互补而非替代关系——I 是结果层面的交互效应，nonargmax_grad_share 是机制层面「ETA 是否把 BER 梯度铺开到非 argmax 流」的过程观测量，第三章定案.md 二之二节把两者并列作为『联动』主张的共同支撑。该观测量本身机制清晰、代码实现可信（旧 C11 已有非零实测 0.15082），但其『接近 0 即证伪』的判据未给数值阈值（Finding 4），当前只能算『可证伪的观测量＋未冻结的判据』，不算『可机械判定的判据』。建议阈值须在 C11 训练前冻结，不得等看到结果后讨论。

【Q2：新四格是否恢复了 I 的判别力，四格设计文档 13–16 行的论证是否成立】设计层面的论证（三目标梯度合成设计.md §2.3：『ETA 在 z2=0 与 z2=1 两侧都有训练通道，I 不再由构造决定』）在数学上是自洽的，前提是 C10 被独立训练、且 C01/C11 也真正带上了实体级 BCE。但逐步核对当前代码与执行计划：(a) C01/C11 的训练路径（_flow_phase_bare / _flow_phase_entity_memory）代码层面完全没有接入 entity_bce，四格实际上不共享底座（Finding 1）；(b) 第三章定案.md §4.2 的执行表仍按旧的零训练重聚合产出 C10，C10-C00 差值大概率仍主要是评价口径差而非独立训练效应（Finding 2）。这两条任一成立，13–16 行揭示的旧退化模式（I 近乎必然为正、无判别力）就会在新标签下原样复现。**结论：设计可行，但实验尚未真正开始验证——当前执行计划本身尚未正确落实该设计，须先修好这两处才能让 I 的判别力真正得到检验。**

【Q3：四格设计文档 6.6 节判据 3 未冻结是否阻断启动】不直接阻断（§6.5 用户裁决已明确将其从『启动闸门』降级为『结果解释依据』），但若要用它来解释或支持任何『两机制是否联动』的结论，必须在看到 C11 结果前把『量级是否有说服力』的数值标准写死（同 Finding 4）。

【Q4：8–10 轮均值口径在 10 轮预算下是否合理】合理，且有数据支持：J 条的依据是旧基线 C00-half 实测『后 10 轮噪声是前 10 轮的 2.8–7 倍且无新信息』，据此把训练直接截断在第 10 轮、只读 8–10 轮均值，是同一份证据的自然推论，不是『从 20 轮里选窗口』式的事后挑选。唯一的披露项是该证据来自旧基线，新基线下的训练动态尚待新 C00 跑出后并行复核（Finding 6），但这不影响当前口径的预注册有效性。

【Q5：有哪些判据是要看到结果才能定的，逐条列出】(1) nonargmax_grad_share『接近 0』的具体数值——未定（Finding 4）；(2) 四格设计文档 6.6 判据 3『量级小到没有说服力』的机械化定义——文档自认未冻结（Finding 4，已降级为非阻断但仍需在引用前补齐）；(3) 段 1 决策点『C00 源年 AP 是否落在与 0.650418 可比的区间』——既没有独立于已被自我否定的旧基准的替代对象，也没有数值容差（Finding 3）。除此之外，判据一『C01>C00』、C11≥max(C10,C01) 的点估计比较、以及第 8–10 轮读数窗口本身，均已在看到结果前给出明确的可机械判定形式，不在此列。

**总体建议**：不要按第三章定案.md 现有 §4.2 直接启动 21 小时训练。先完成 Finding 1（entity_bce 接入 z2=1 两格的代码改动＋回归）与 Finding 2（把执行计划改写为四段独立训练并重算预算，预计约 28h 而非 21h），再补上 Finding 3、4 的两处判据；C00/C01 部分（判据一 C01>C00）设计本身自洽、可执行、判据可机械判定，可以在完成上述代码修复后作为第一优先级先跑；C10/C11 部分（涉及交互项 I 与联动主张）在完成 Finding 1、2 前不应投入训练预算，因为其核心科学结论目前无法被当前执行计划有效检验。）

#### [阻断] z2=1 两格（C01/C11）尚未接入实体级 BCE，当前代码无法产出「共同底座」四格

- **理由**：训练目标补底座的全部前提是四格共享 L_flow+w_e·L_ent 这一底座。代码实测：`training_step`（z2=0，服务 C00/C10）第 42-92 行已挂 `entity_bce_module.entity_bce_loss` 并按 `entity_bce_weight` 并入 `loss_sum`；但 `_flow_phase_bare`（z2=1/z1'=0，C01 第一步，2331-2374 行）与 `_flow_phase_entity_memory`（z2=1/z1'=1，C11 第一步，2377 行起）通篇不出现 `entity_bce` 字样，只计算逐流 BCE。`thesis/methods/三目标梯度合成设计.md` 1.2 节表格与文字同样确认「z2=1 的两格还没有这一项」，2.4 节把「给 _flow_phase_bare / _flow_phase_entity_memory 加入同一份实体级 BCE 求和逻辑」列为尚待完成的改动。
- **失败形式**：若照 `第三章定案.md` §4 字面启动 21～25 小时训练，C00/C10 训练目标含实体级 BCE，C01/C11 训练目标仍是「逐流 BCE+BER」（旧定义），四格不再共享底座。四格设计的不对称与改法.md 第一节已证明的『I 近乎必然为正、无判别力』的病灶不会被修复，只是从『z1′训练通道只在 z2=1 侧存在』换成『实体 BCE 只在 z2=0 侧存在』，同型问题原样复现，21 小时预算白耗。
- **修法**：启动前先完成 `三目标梯度合成设计.md` §2.4 的代码改动：把 training_step 中已有的求和逻辑抽成共用函数，接入 `_flow_phase_bare` 与 `_flow_phase_entity_memory`；随后按恢复卡 L2 条方法（固定 RNG）做一次 `w_e=0`/`entity_bce.enabled=false` 时的逐位等价回归，确认未破坏 C01/C11 现有关闭路径。工作量为数小时级实现＋回归，不计入 21 小时训练预算，但必须在写新四格配置文件之前完成。
- **自称证据**：thesis/experiments/llm_probe/tools/ch3_ft_c00_dual_selection.py:1665-1900（training_step 内 entity_bce 挂载，行 42-92）；同文件 2331-2374 行（_flow_phase_bare 无 entity_bce）；2377 行起（_flow_phase_entity_memory 无 entity_bce）；thesis/methods/三目标梯度合成设计.md:140-155、277-279

#### [阻断] 第三章定案.md §4.2 仍按『双聚合零训练重聚合』产出 C10，与『C10 须独立训练』的最新裁决直接矛盾

- **理由**：`第三章定案.md:160` 段 1 写「C00 训练（双聚合，同时产出 C00 与 C10）」——这是旧机制（z2=0 时 ETA 聚合不进训练图，C10 复用 C00 权重、只换评价口径）的原样沿用。但 `RWKV第三章恢复卡.md:671` 明写「C10 因此变成真正的训练臂，不再是零训练重聚合，四格需四次训练」；`三目标梯度合成设计.md` §2.3 的四格损失表把 C10 定义为独立的 `L_flow+w_e·L_ent^tail`『单一梯度，不进控制器』，即须有自己的反传轨迹；该文并明确点出「ETA 在四格两侧都有训练通道…交互项 I 因此不再由构造决定——这正是补底座的全部目的」，这句话的成立前提正是 C10 被独立训练，而不是零训练重聚合。同一天产出的 `.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md:120-124` 也明确记录「四次训练而非三次；配置写法待主代理给出新简报」，即当前尚无任何按新定义写的 C10/C01/C11 配置文件。
- **失败形式**：若按 §4.2 字面执行（3 段训练，C10 靠 C00 权重零训练重聚合读出），C10 的权重与 C00 完全相同，只是评价口径不同——这恰是『四格设计的不对称与改法.md』第一节已证明的旧退化（I=(C11-C01)-(C10-C00)，C10-C00 因无独立训练信号而趋近于纯评价口径差）在新标签下的复现。跑完后计算出的 I（无论正负）都不能回答『两机制是否联动』这一实验的核心问题，21 小时预算中约 20.4 小时（C01+C11 段）产出的结论会被下一轮复审直接推翻。
- **修法**：写配置文件前，把 §4.2 的执行表改写为四段独立训练：C00、C10、C01、C11 各自独立完整训练，损失定义按三目标梯度合成设计.md §2.3 的表逐格落实。预算需按四次独立训练重新估算——C00/C10 无 BER，预计与当前 C00-half 同量级速度（各约 3.7 h）；C01/C11 有 BER，预计各约 10.4 h；合计约 28 h，既不是 §4.2 现表的『≈25h』，也不是任务简报里的『约 21 小时』（该 21h 数字来自同一份报告按旧 3 段方案的估算，其 123 行已自我更正为『四次训练而非三次』）。
- **自称证据**：thesis/methods/第三章定案.md:156-163（§4.2 执行表）；.Codex/docs/RWKV/RWKV第三章恢复卡.md:671；thesis/methods/三目标梯度合成设计.md:255-269（§2.3 表与结论句）；.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md:120-126

#### [严重] 段 1 决策点用『旧四格（w_e=0）C00 的 0.650418』作新 C00（w_e>0）的可比性基准，作者自己的分析已否定这种比较

- **理由**：`第三章定案.md:160` 段 1 的决策点写「C00 源年 AP 是否落在与 `0.650418` 可比的区间；塌陷则退回 d=48 2 层」——`0.650418` 正是旧四格（未补底座、`w_e=0`）C00-half 的读数（见 `RWKV第三章恢复卡.md` 〇之二点五）。但同一批文档中 `三目标梯度合成设计.md` §4.3「旧四格能不能作对照：不能」明确论证：「新四格的每一格都取 `w_e>0`……旧 C00 与新 C00 是两个不同的训练目标，其最优解、选轮轨迹与实体分数分布都没有理由相同」，并指出 `mechanism.entity_bce.weight` 一变即改变 `science_identity_sha256`，制度上已经承认这是两个不同的科学身份族。段 1 决策点没有采纳这条已有结论，仍用被自己否定的对象做数值比较，且『可比区间』本身也从未给出容差（搜索全仓库对『可比区间』『塌陷』相关阈值均无命中）。
- **失败形式**：新 C00 因多了一项实体级 BCE 损失，其 AP 系统性偏离 0.650418 是预期之内的正常现象，不代表机制『塌陷』；反之真正的塌陷（如权重配置错误、梯度爆炸）也可能被误判为『和旧 C00 不可比很正常』而被放过。无论哪个方向的误判，都会影响是否继续后续约 20 小时训练的决策，且该决策目前既无独立于旧 C00 的基准，也无数值容差，等于『看结果现场拍板』。
- **修法**：启动前重新定义一个不依赖旧 C00(w_e=0) 数值的合理性检查（例如：损失是否单调下降、AP 是否显著高于源年正例率对应的随机基线、或诊断埋点如 ρ=w_e‖g_e‖/‖g_f‖ 是否落在预期区间），并给出明确数值容差，写入文档冻结，不得沿用 0.650418 作直接比较。
- **自称证据**：thesis/methods/第三章定案.md:160；thesis/methods/三目标梯度合成设计.md:457-465（§4.3）

#### [严重] nonargmax_grad_share『接近 0』证伪判据与四格设计文档 6.6 判据 3『量级小到没有说服力』均无冻结数值阈值

- **理由**：`第三章定案.md:96-98` 的证伪判据原文是「C11 运行中若 `nonargmax_grad_share` 的轮级均值接近 `0`，则……联动主张被证伪」——『接近』未给数值。`四格设计的不对称与改法.md:191-192` 自己也承认「判据 3 中『量级小到没有说服力』的机械化定义须在 C11-half 出数前冻结……当前未冻结，记为待补」。用户 §6.5 裁决已把 6.6 判据 3 从『启动闸门』降级为『结果解释依据』，不阻断启动本身，但只要后续正文或过程文档要引用这两条来支持或否定『两机制联动』，就必须在看到本次 C11 结果前把数值定下来，否则违反仓库『预注册判据不得在看到结果后修改』的强规则（第三章定案.md 五节自己也这样要求 4.2 的决策点）。已有一个可参考的量级锚点：旧基线（w_e=0）C11 第 6 轮实测 `nonargmax_grad_share_epoch_mean=0.15082043792523847`（非零，见运行收据），但这是旧基线下的观测，不能直接套用为新基线的阈值。
- **失败形式**：21 小时后 C11 产出具体数值，若无预先冻结阈值，『是否证伪』『量级是否有说服力』的判断会退化为看到数字后现场定标准——即使结论本身可能正确，也会被下一轮复审以『事后定门槛』为由要求重新走一遍证据链，拖慢定稿。
- **修法**：在 C11 训练启动前，写定 nonargmax_grad_share 的数值下限判据（可参考旧 C11 第 6 轮 0.1508 作为量级锚点，但需注明只作参考、不可直接挪用）与 I 相对量级的判定方式（例如仿照『C01>C00 判据一』已采用的符号检验/相邻轮方向一致性方法），由用户或预先落盘文档确定，不得等结果出来后讨论。
- **自称证据**：thesis/methods/第三章定案.md:96-98；thesis/methods/四格设计的不对称与改法.md:191-192；thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c11-halfwidth-screening-v1/receipts/entity-ranking-diagnostics-6.json（nonargmax_grad_share_epoch_mean=0.15082043792523847）

#### [披露] 任务简报所引『参数量 11,265』与『21 小时』预算已与当前权威文档不一致

- **理由**：本次复审过程中 `第三章定案.md` 被并发代理更新：参数量已从错误的 `23,041` 修正为闭式实测值 `14,261`（第 135 行，并附更正说明），与任务简报给出的 `11,265` 不一致；`21 小时` 总预算数字出自 `.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md:29`，是按旧『3 段训练』估算得出，该报告自己在 123 行已注明该估算方式已过时（『四次训练而非三次』），且与本审查 Finding 2 所述的四次独立训练量级（约 28h）不符。
- **失败形式**：若配置文件按『11,265 参数』写入，会与代码闭式计算值（14,261）不符，触发运行时参数量断言失败（不可运行）；若仍按『21 小时』规划资源/日程，会与实际需要的约 28 小时产生较大落差，打乱后续任务排期与守护脚本的轮询预期。
- **修法**：启动前重新读取 `thesis/methods/第三章定案.md` 与 `2026-09-03-本机筛选缩容档实现与回归报告.md` 的最新状态，以文件当前内容为准配置与排期，不沿用简报中的旧数字。
- **自称证据**：thesis/methods/第三章定案.md:135, 138-141；.Codex/docs/RWKV/2026-09-03-本机筛选缩容档实现与回归报告.md:29, 96-103, 120-124

#### [披露] 8–10 轮读数窗口的依据完全来自旧基线（w_e=0）C00-half，新基线下训练动态尚未复核

- **理由**：`RWKV第三章恢复卡.md` J 条冻结『第 8–10 轮均值』口径的依据，是对旧基线 C00-half（无实体级 BCE）相邻轮差中位数与最大值的实测（前 10 轮 0.0253/0.0454，后 10 轮 0.0705/0.3280）。新四格因新增实体级 BCE 损失项，训练动态（收敛速度、震荡幅度）可能与旧基线不同，该窗口选择尚未在新基线下重新验证。该口径本身满足『看到任何 C01/C10/C11 结果前冻结』的预注册要求，且 J 条『后期噪声更大、无新信息』的一般性论证具有合理性，故不构成阻断。
- **失败形式**：若新基线下训练动态显著不同（例如实体级 BCE 导致更早饱和或更强震荡），8–10 轮窗口可能不再是信噪比最优窗口，读数精度打折扣，但不会造成数据泄漏或指标本身失效，只是统计功效可能非最优。
- **修法**：无需阻断启动；可在 C00（新基线）训练产出逐轮曲线后，作为并行诊断步骤复核相邻轮差是否仍呈现同样的噪声结构，以确认窗口是否仍是本次运行下的合理选择。若明显不同，留作后续改进依据，不得回头修改本次已冻结口径。
- **自称证据**：thesis/methods/RWKV第三章恢复卡.md:555-559（J 条）

### ?（自评总裁定：须先修阻断项：本轮复审的两条阻断项都不属于“数据/指标/因果”这类经典失效，而是这份复审自身被赋予的判准——“21小时四臂在本机能否真的跑完”——下的可执行性阻断。第一条（完整源验证耗时完全未纳入21/25小时预算，且本机今日在同构width_profile=half配置下实测已跑≥25分钟仍未完成、对照服务器CUDA同配置仅需44.2秒）有直接的本机日志与status文件为证，不是推演；第二条（第三章定案.md §4.2仍写“三段/25h、C00双聚合同时产出C10”，与四格设计的不对称与改法.md §6.5“四格需四次训练”的用户裁决冲突）同样有两份文档原文互相矛盾为证。这两条不解决，要么实际墙钟时间会远超21小时且中途可能挂起在没有报错的验证阶段，要么产出的C10不满足补底座设计初衷、要等十轮跑完才能发现问题。此外还有两条“严重”级证据缺口（8.47×训练加速比未覆盖entity_bce；d=32下的显存峰值从未直接测量，且注意力矩阵不随宽度线性收缩）值得在启动前补一次小规模探测（几分钟量级），代价远低于盲跑21小时后才发现预算失真。其余四条为“披露”级，其中两条（检查点续训机制、降低validation_batch_sequences不改指标）是确认性发现——设计本身可行，不构成障碍；另两条（参数量文档在复审期间被并发修订、四臂必须且只能串行）是提醒事项。建议：启动前先跑一次“d=32/1层+entity_bce（对C01/C11再加entity_ranking）的单epoch完整流程小样探测”，同时测出该配置下的one_optimizer_step_seconds、完整源验证秒数与显存峰值三个数字，把21/25小时预算和两份计划文档一并更新、并明确本次到底是三段还是四段执行，再提交完整10轮×4臂的无人值守任务。）

#### [阻断] 「完整源验证」耗时未纳入21/25小时预算，且本机同构配置今日实测已跑≥25分钟未完成

- **理由**：四臂每轮训练后都会调用 validation_metrics()（tools/ch3_ft_c00_dual_selection.py:1768-1884，日志行1799-1802打印“开始完整源验证：序列=22444，验证批=128”），该函数与训练循环内触发验证的调用是同一份代码（line 3495-3498起）。这不是可选诊断，是拿“第8–10轮均值”读数的必经步骤。而“21/25小时”的全部现有版本——任务简报的“10轮×1000步…四次训练合计约21小时”、第三章定案.md §4.2（≈3.7h+10.4h+10.4h≈25h）、四格设计的不对称与改法.md §6.2（≈13.6h）——逐一核对都只按“训练步速×步数×轮数”折算，未见任何一处把验证耗时计入总预算。
- **失败形式**：本机今天（2026-09-03）在完全相同的宽度档位（width_profile=half，d=96/3层，与已冻结的服务器口径一致）跑过一次 probe_runtime 计时探针：日志 runs/diagnostics/LOCAL-s0-timing.log 最后一行停在 19:09:39“开始完整源验证：序列=22444，验证批=128，设备=mps”，此后再无任何进度行；对应目录 runs/diagnostics/ch3-ft-LOCAL-mps-s0-timing-probe-v1/status.stale-killed-1935.json 的文件名时间戳显示该进程在19:35仍处于 state=running 并被判定为需要清理的僵尸/挂起状态——即这一次“完整源验证”至少跑了25分26秒仍未产出一行进度日志，随后被中止。对照 RWKV第三章恢复卡.md 第561-571行（K条）：同一 width_profile=half 配置在服务器 CUDA 上验证只需 44.2 秒/轮。44.2秒→≥1500秒是34倍以上的放大，明显超出训练步速本身的放大倍数（0.120 vs 3.352 步/秒，约28倍）。若这一模式在 d=32/1层 下不能成比例改善（第三章定案.md §4.1 已记录“固定开销”现象：d=96→d=192 缩容只快40%，因为“固定开销265秒占64%”，即收窄宽度对“非计算类”耗时收益有限），则10轮×4臂=40次验证可能额外叠加数小时到十余小时墙钟时间，且失败模式不是抛异常退出，而是进度日志停止推进、进程长期占用不响应——这正是会在“跑完21小时后才暴露、且暴露时已经浪费掉大半天算力”的那类缺陷，符合本轮复审要找的失败形态。
- **修法**：启动四臂正式训练前，先用与最终配置完全一致的 d=32/1层 + entity_bce 配置单独跑一次“1个epoch的完整源验证”，实测其墙钟耗时；再决定：(a) 若耗时可接受（如<3分钟/次），把 40 次验证的总时长显式计入预算并更新两份计划文档；(b) 若耗时仍不成比例地长，考虑把 validation_every_epochs 从1改为“仅在第8-10轮验证、其余轮跳过”（不影响读数口径，因为口径本就只取第8-10轮均值），可省去约70%的验证调用次数。该探测本身只需几分钟，成本远低于盲跑21小时后才发现验证拖垮进度。
- **自称证据**：tools/ch3_ft_c00_dual_selection.py:1768-1884（validation_metrics定义与日志文案）、3495-3498（训练循环内调用点）；runs/diagnostics/LOCAL-s0-timing.log（最后一行“开始完整源验证”，无后续）；runs/diagnostics/ch3-ft-LOCAL-mps-s0-timing-probe-v1/status.stale-killed-1935.json（文件名时间戳19:35）与同目录config.json（width_profile=half, steps_per_epoch=20, validation_every_epochs=1, validation_batch_sequences=128）；.Codex/docs/RWKV/RWKV第三章恢复卡.md:561-571（K条：服务器CUDA同配置验证44.2秒/轮）；thesis/methods/第三章定案.md:24-33（“缩容只快40%，固定开销占64%”原始出处见恢复卡〇条，定案.md§4.1第131-132行沿用同一경验）。

#### [严重] 8.47×训练加速比与由此推出的21/25小时训练时长，测量口径不含实体级BCE——而新四格的全部四臂训练损失都含它

- **理由**：新四格的共同底座是“逐流BCE + 实体级BCE”（四格设计的不对称与改法.md §3表格；第三章定案.md §一“四格”一节亦确认已按此裁决），entity_bce 已实际接入训练循环（tools/ch3_ft_c00_dual_selection.py:1706-1756，`loss_sum = loss_sum + entity_bce_weight * entity_bce_value`），每个训练步都会多算一次 tail_aggregate + BCE。但本机今天两次唯一的timing-probe实测（S0/S1，任务简报引用的0.120/0.043步/秒正是它们）的 config.json 里 mechanism.entity_bce 键均不存在——S0的mechanism整体是空字典`{}`，S1只含entity_ranking、不含entity_bce（已核对 runs/diagnostics/ch3-ft-LOCAL-mps-s0-timing-probe-v1/config.json 与 …s1-timing-probe-v1/config.json 的mechanism字段）。也就是说，作为21/25小时预算基础的两个步速数字，测的是“旧损失（无实体级BCE）”，不是新四格实际要跑的损失。
- **失败形式**：若entity_bce的额外前向/反传开销在d=32、微批极小（micro_batch_sequences=8，一微批内实体数≤8）的条件下确实很小，那么21小时估算大致仍然可用；但这只是推测，没有一次真实测量同时覆盖“d=32”与“含entity_bce”两个条件——本机唯一涉及entity_bce的运行是服务器CUDA上的ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1（d=96/3层），据RWKV第三章恢复卡.md〇之二点六节记载“刚起跑数分钟，进度可弃”，从未产出稳定的step_seconds读数。第三章定案.md §4.1自己也承认8.47×这个数字“出自主代理为测速写的基准复刻件”（第138-141行），复刻件是否忠实复现了entity_bce+tail_aggregate的真实计算路径未见交代。一旦entity_bce的实际开销比预期大（例如tail_aggregate里含有排序/gather操作在MPS上效率不佳），21/25小时的训练部分本身就会失真，叠加finding1的验证开销缺口，总墙钟时间可能远超预期，而这要等真正跑起来才能发现。
- **修法**：在正式启动四臂前，用最终真实配置（d=32/1层 + entity_bce enabled + 对应aggregation）跑一次几十步的probe_runtime，直接读取one_optimizer_step_seconds，与不含entity_bce的0.120/0.043步/秒对比，确认额外开销量级后再据此更新21/25小时预算，而不是延用旧测速数字。
- **自称证据**：runs/diagnostics/ch3-ft-LOCAL-mps-s0-timing-probe-v1/config.json 与 …s1-timing-probe-v1/config.json（mechanism字段均无entity_bce）；tools/ch3_ft_c00_dual_selection.py:1706-1756（entity_bce训练侧调用）；thesis/methods/第三章定案.md:138-141（“基准复刻件”注记）；.Codex/docs/RWKV/RWKV第三章恢复卡.md〇之二点六节（entitybce-newbase运行“刚起跑数分钟，进度可弃”）。

#### [阻断] 两份权威依据文档就“C10要不要独立训练”直接冲突，若沿用旧的零训练重聚合执行，会产出不满足补底座设计初衷的C10

- **理由**：四格设计的不对称与改法.md §6.5（用户裁决）明确写“C10因此变成真正的训练臂，不再是零训练重聚合，四格需四次训练”，理由是补底座后C00（实体级BCE用max聚合）与C10（实体级BCE用ETA聚合）优化的是两个不同的损失函数，不再是“同一份权重、评价时换聚合算子”的关系。但第三章定案.md §4.2（截至我核查时的最新版本）仍写“段1：C00训练（双聚合，同时产出C00与C10）≈3.7h”，这是延续旧四格“C10零训练重聚合、四格只需三次训练”的做法（该做法的合法性前提——z2=0时实体聚合根本不进训练图——恰恰是补底座要修复的那个不对称本身，见四格设计的不对称与改法.md §二）。RWKV第三章恢复卡.md 第六节第11条已单独记录过这个矛盾并明写“未经用户裁决前，既不要启动该C10训练”，但该矛盾在定案.md里至今没有被订正。
- **失败形式**：若实际执行照抄定案.md §4.2（只训练C00、零训练重聚合出“C10”），得到的“C10”读数只是C00权重用tail_aggregate重新打分的结果，ETA聚合从未真正参与反传——这不会报错、不会中断，读数在数值上仍然“看起来合理”，但它不是补底座设计想要验证的对象（一个真正在实体级BCE(ETA)上训练出来的模型），会让判据一、交互项I的论证基础重新失效，而这要等四臂全部十轮跑完、去核对代码路径时才会被发现。
- **修法**：启动前先明确“本次到底跑三段（延用定案.md §4.2）还是四段（按四格设计.md §6.5）”，并把结论回写进两份文档中过时的一份，消除歧义；按当前证据链（用户已就“立即修复Baseline缺一层”做过裁决，四格设计.md §6.5是更新、更具体的裁决），应执行四段方案，定案.md §4.2需要更新为四段并重新估算耗时。
- **自称证据**：thesis/methods/四格设计的不对称与改法.md:157-193（§6.4“C10因此变成真正的训练臂…四格需四次训练”）；thesis/methods/第三章定案.md:126-163（§4.1已按新sixth_width档位更新，但§4.2执行计划仍是旧的三段“C00双聚合同时产出C10”表述）；.Codex/docs/RWKV/RWKV第三章恢复卡.md 第六节第11条（“交用户裁决前不要启动该C10训练”）。

#### [严重] d=32/1层在本机（16GB统一内存）的显存峰值从未直接测量，不能用训练步速的8.47×倒推显存也降到约1/8

- **理由**：现有唯一显存-批量曲线（训练峰值批64/512/4096→1088/2200/13221 MB）是针对width_profile=half（d=96/3层）测的（.Codex/docs/RWKV/2026-09-03-第三章恢复卡二次精简归档.md:302-316“模型单步（批64,d=96）59ms”一行同表内），没有任何一次对d=32/1层做过同样的批量-显存扫描。Transformer的注意力分数矩阵大小是O(batch×heads×length²)，不随d_token收缩（n_heads=8固定不变，只是每个头的宽度从96/8=12缩到32/8=4，见第三章定案.md:134行“n_heads=8（不变）”），只有层数从3降到1能按层数比例减少反传要保留的激活份数；也就是说显存的收缩幅度大概率明显小于训练步速8.47×那样的收缩幅度，不能把“计算更快”直接等价成“占用更少”。
- **失败形式**：任务简报本身给出的事实（“今日一次标定占到14.33GB时进程进入不可中断等待，整机内存剩17%”）说明这个失败模式在本机是真实发生过的，且不是普通的OOM报错退出，而是进程挂起、系统资源逼近耗尽——这种状态通常需要手动介入甚至硬重启才能恢复，一旦发生在四臂串行队列的中段（例如C01或C11训练中，本机并发上限为1、无法在旁路诊断），会打断整条21小时排队且不产生可用于诊断的干净错误日志。验证批（128序列=16384行）比已测的批64（8192行）大2倍，是当前唯一已知会在一次forward里处理这么多行的地方，其显存占用同样没有单独测过。
- **修法**：正式四臂启动前，先用d=32/1层 + entity_bce（以及对C01/C11再加entity_ranking）的真实配置，跑一次单步/单验证批的显存探测（torch.mps.current_allocated_memory()读数，恢复卡8.2节已给出方法），确认峰值明显低于安全线（例如<8GB，留出系统与其他进程余量）后再提交完整10轮任务；同时确认entity_ranking在预算档位9706（C01/C11六档中最大档）下的批构成不会显著推高峰值。
- **自称证据**：.Codex/docs/RWKV/2026-09-03-第三章恢复卡二次精简归档.md:292-330（§八，d=96显存曲线与“瓶颈是磁盘IO不是内存”的旧结论）；thesis/methods/第三章定案.md:126-141（§4.1，d=32/n_layers=1/n_heads=8不变）；任务简报给出的“14.33GB…不可中断等待，整机内存剩17%”事实（未见对应书面收据，见下一条发现）。

#### [披露] 任务简报所引用的参数量（11,265）与耗时依据已经过时——第三章定案.md在本次复审期间被并发编辑，更正为14,261

- **理由**：本次复审开始时首次读取thesis/methods/第三章定案.md，§4.1骨干缩容档表格记载参数量为23,041（d_token=48/1层口径）；复审过程中二次读取同一文件，内容已变为d_token=32/1层、参数量“14,261”，并且文中明确标注“本表初稿的参数量`23,041`是错的，已更正……那个数出自主代理为测速写的**基准复刻件**”。而任务简报给我的数字是“参数11,265”，与这两个版本都不一致。三个数字（11,265 / 23,041 / 14,261）互不相同，说明该文档目前处于连续修订状态（工作树规则本身也提示“另有代理在改代码”）。
- **失败形式**：若据本报告或任务简报里的具体参数量、耗时数字直接去启动实验或写入正文，可能引用的已经是被后续修订推翻的版本，造成又一次“凭旧上下文断言”的误判（恢复卡〇之二点八已登记多例同型错误）。
- **修法**：正式启动前重新读取thesis/methods/第三章定案.md的最新版本，以其为准，不沿用本报告或任务简报中出现的具体参数量/耗时数字；若后续还要引用参数量，直接从代码闭式`base.expected_parameter_count(81,2,14,d_token=32,n_layers=1)`的实测输出取值，不再手抄文档里的字面数字。
- **自称证据**：thesis/methods/第三章定案.md（复审期间前后两次读取内容不同：初次含“d_token 48…参数量23,041”，二次含“d_token 32…参数量14,261，本表初稿的参数量23,041是错的”）；任务简报原文“参数 11,265”。

#### [披露] 断点续训机制基本安全，但若崩溃恰好发生在“完整源验证”挂起阶段，该轮训练与验证都不会被保存（与finding1相关联）

- **理由**：已读代码核实两点：(1) mechanism_state的显式校验（restore_mechanism_state，tools/ch3_ft_c00_dual_selection.py:3212-3244）只比对entity_memory_enabled与entity_ranking_enabled，没有显式比对entity_bce_enabled，但science_projection()（同文件629-654行，第642行`"mechanism": config.get("mechanism", DEFAULT_MECHANISM)`）把整个mechanism字典（含entity_bce）纳入了science_identity_sha256，而resume路径在3390行有`require(checkpoint["science_identity"] == science_receipt, "在途科学身份漂移")`——所以用不同entity_bce配置续训一份在途检查点会被这道门拒绝，不存在“静默用错配置续训”的风险；entity_bce本身也是无状态纯函数（tools/ch3_ft_entity_bce.py全文未见任何持久化状态），不需要额外的检查点字段。(2) 检查点写入发生在“本轮训练1000步全部完成 + 完整源验证跑完”之后（3477-3568行，先算metrics再调用checkpoint_payload/atomic_torch），即按“整轮”而非“按步”的粒度落盘，与配置里`checkpoint.atomic_boundary: "complete_optimizer_step_and_epoch"`的字面承诺一致。
- **失败形式**：常规崩溃（如断电、进程被杀）下，最多损失“当前正在进行、尚未完成的那一轮”训练+验证，不是整臂7.6小时——这点比任务简报担心的更乐观。但finding1已证实“完整源验证”存在挂起且长时间不报错的先例：如果崩溃/强杀恰好发生在这个挂起阶段，本轮1000步训练的成果与已经消耗的挂起时间会一起被丢弃（因为checkpoint要等validation_metrics()返回后才写），需要从上一轮的检查点重跑整轮。
- **修法**：无需改动续训逻辑本身；把finding1的验证耗时问题解决后，这条风险自然收敛。若finding1一时无法根治，可考虑把校验点提前（先写“本轮训练已完成、验证未完成”的中间检查点），但这是额外工程量，优先级低于先弄清验证耗时本身。
- **自称证据**：tools/ch3_ft_c00_dual_selection.py:629-654（science_projection含mechanism全字典）、3212-3244（restore_mechanism_state显式校验范围）、3390（在途科学身份漂移门）、3477-3568（先validation_metrics()再checkpoint_payload/atomic_torch的顺序）；tools/ch3_ft_entity_bce.py（全文无持久状态）。

#### [披露] 降低validation_batch_sequences不改变任何指标定义，可作为缓解finding1/finding4的安全手段

- **理由**：已读validation_metrics()全文（tools/ch3_ft_c00_dual_selection.py:1768-1884）：函数把所有批次的预测/标签/实体id先拼接进`predictions`/`targets`/`entity_ids`三个列表，循环结束后才一次性对拼接后的完整数组计算flow_ap（average_precision_score）、entity_ap_max（np.maximum.at聚合）、entity_ap_tail（tail_aggregate聚合）。validation_batch_sequences只决定forward阶段每次处理多少行、循环多少次，不参与任何指标公式，最终AP值与批量切分方式无关。
- **失败形式**：N/A——这是一条确认性发现，不是缺陷。
- **修法**：若finding1（验证耗时）或finding4（验证批显存）需要缓解，可以放心把validation_batch_sequences从128调小（或调大，视MPS上小算子调度开销与显存哪个是瓶颈而定），不会引入任何“改指标口径救结果”的问题，四臂之间也应统一使用同一验证批量以保证可比性（批量本身不影响数值，但统一配置更利于审计）。
- **自称证据**：tools/ch3_ft_c00_dual_selection.py:1790-1884（validation_metrics函数体，尤其1831-1868行的拼接后一次性计算逻辑）。

#### [披露] 四臂只能串行执行，这与仓库“本机并发上限1”规则一致，21/25小时预算的串行前提本身没有设计问题

- **理由**：本机是单一Apple Silicon/MPS加速器（一块统一内存芯片，不像服务器有“三卡并行”的物理条件——四格设计的不对称与改法.md §6.2曾提到服务器场景“若三卡并行则约5-6小时”，但那是服务器口径，不适用于本机）。根AGENTS.md“本机资源保护”一节明文“并发上限为1：本机同一时刻只允许一个重任务（训练、批量转换、大规模扫描）”，且给出了具体事故依据（多重任务并发导致死机）。已核对任务简报与两份计划文档，21/25小时的估算方式都是把四臂耗时直接相加（串行），没有假设并行，设计上与该硬约束一致。
- **失败形式**：N/A——这是一条确认性发现，不是缺陷。列出仅为正面回答任务简报第5问，避免遗漏。
- **修法**：维持串行执行即可，无需改动；只需注意恢复卡E条记录过的教训——手动启动与后台守护轮询可能撞车导致两个训练挤一张卡，起新一段训练前先pgrep确认没有同类进程在跑。
- **自称证据**：根AGENTS.md「本机资源保护」节（“并发上限为1”及其事故依据）；thesis/methods/四格设计的不对称与改法.md §6.2（“若三卡并行则约5-6小时”仅针对服务器）；.Codex/docs/RWKV/RWKV第三章恢复卡.md 操作事实速查E条（手动启动与守护轮询撞车导致OOM警告的先例）。

### ?（自评总裁定：本维度（数据合同/泄漏/最终测试隔离/运行身份）总体裁定：**无阻断项，但须先处理两条严重项再启动**（可在数十分钟到1小时内完成，无需新实验）。\n\n已核实为设计可行、不构成风险的两点（供参考，未放入 findings）：(1) 年度隔离——`evaluation.target_reads==0` 在 tools/ch3_ft_c00_dual_selection.py 的 validate_config 里被 require() 强制（第620行），entity_subsample（同文件 select_entity_subsample，第897-941行）只读 arrays['E23']，全链路未发现任何触碰 LSPR24 的路径，新四格的年度隔离成立。(2) `data.reuse_input_transform_from` 在新骨干 d=32/1层下依然合法——已通过读代码证实（非仅采信恢复卡断言）：fit_input_transform 写入 state_hash 的 token_count 字段实为 numeric_field_count+categorical_field_count+1（tools/ch3_ft_transformer_field_token_protocol_a.py 第767行 parameter_count_decomposition），与 d_token/n_layers 无关；architecture 相关字段只用于参数量预算断言，不写入 state，故跨宽度复用输入变换在结构上必然成立，不依赖\"实测发现两次state_hash恰好相同\"这一巧合式证据。\n\n须先修的两条严重项：①代码身份哈希（code_sha256/tool_sha256）只覆盖 base 模块 ch3_ft_transformer_field_token_protocol_a.py，完全不覆盖编排脚本 ch3_ft_c00_dual_selection.py 本身，也不覆盖 BER/ETA 核心 ch3_ft_entity_ranking_loss.py（今日提交5a9943a刚改过）、梯度控制器 ch3_ft_gradient_controller.py、新增的 ch3_ft_entity_bce.py——断点续训跨越这四个文件的代码编辑不会被检测，而21小时四臂长跑历史上已多次中断，风险非假设性；②同一新机制（实体级BCE共同底座）目前存在两个都自称\"预注册/定死\"但互相冲突的读数窗口（本方案沿用未经真实数据验证的\"读8-10轮\"，而已实际生成过的同机制服务器配置里有一份基于真实C00/C01-half十轮曲线做出、更严谨的\"读4-6轮\"裁决），未reconcile就启动等于在两个预注册版本间悬而未决，21小时后可能被迫二选一而丧失预注册效力。\n\n另有三条披露级问题建议知悉但不阻塞启动：实体级BCE的\"实体袋\"口径与BER的因果截断袋不一致、且在多数实体流数稀少的情况下ETA大概率在该层退化为max（建议启动前用一次零训练前向诊断确认，成本极低）；本次审查期间实测代码库正被并发代理编辑，方案的骨干宽度/参数量数字已在审查窗口内变化三次（d=48/23041→d=32/14261→简报里的11265三者互不相同），启动实际配置前须重新只读核验而非信任本报告或简报的静态数字；方案目标措辞\"确立机制\"超出 run_tier=screening_only 允许支撑的结论强度，与仓库自身\"本机结果只用于排序与止损\"的边界不符，建议改措辞。）

#### [严重] 代码身份哈希未覆盖 BER/ETA/梯度控制器/实体级BCE 与编排脚本本身，断点续训无法检测这四个文件的代码漂移

- **理由**：四格唯一的代码身份校验是 `build_run_identity`（thesis/experiments/llm_probe/tools/ch3_ft_transformer_field_token_protocol_a.py:3840-3852）里的 `code_sha256 = _sha256_file(Path(__file__).resolve())`——`__file__` 在该函数定义所在模块内恒指向该 base 模块自身。编排器 `ch3_ft_c00_dual_selection.py` 从未调用 `build_run_identity`（全文件搜索零命中），只在 `validate_config`（同文件:609-626）里核对 `sha256_file(base_tool_path) == config['base']['tool_sha256']`，而 `base_tool_path` 同样指向 `tools/ch3_ft_transformer_field_token_protocol_a.py`（见 `configs/ch3-ft-c00-halfwidth-screening-v1.json:10-13`）。逐一 grep 确认 `ch3_ft_c00_dual_selection.py`（第24行 `__file__` 只用于 `TOOL_DIR` 路径解析）、`ch3_ft_entity_ranking_loss.py`、`ch3_ft_gradient_controller.py`、`ch3_ft_entity_bce.py` 四个文件均无任何自哈希写入身份或收据。断点续训的一致性校验（同文件:3390-3391 `require(checkpoint['science_identity']==science_receipt)` / `runtime_identity`）只比对 `science_projection`/`runtime_projection` 两个配置投影字典，其 `mechanism` 字段是配置值而非代码哈希，同样查不出这四个文件的代码变化。AGENTS.md 提到的"training-identities.json 训练身份漂移检查"（三类不可废弃之一）经 `rg -ln training-identities tools/ src/` 核实只在 `src/flow_probe/cuda_rwkv_training.py` 实现，FT 四格管线完全未接入。
- **失败形式**：今日提交 `5a9943a` 已实际修改过 `ch3_ft_entity_ranking_loss.py`（MPS fp32 定位修复，声称对 CUDA 逐位不变但未被任何身份哈希记录）；本机四臂 21 小时长跑历史上已多次中途中断（服务器到期关机、MPS 僵尸 status.json 等，见恢复卡 D/E 条与〇之二点六）。若某臂断点续训恰好跨越了对这四个文件之一的编辑，系统会把新旧代码状态判定为"同一身份"而放行续训，产出前后半程实际执行不同机制代码却共享同一份收据身份的检查点，而收据里没有任何字段能揭示这一点——四格赖以成立的"同一代码提交、全即时执行"前提因此无法被验证，且发现窗口极可能是 21 小时跑完之后。
- **修法**：仿照现有 `tool_sha256` 的模式（`ch3_ft_c00_dual_selection.py:625-626`），为 `ch3_ft_c00_dual_selection.py`、`ch3_ft_entity_ranking_loss.py`、`ch3_ft_gradient_controller.py`、`ch3_ft_entity_bce.py` 各增加一个 `_sha256_file(Path(...))` 字段，写入运行身份（`science_projection`/`runtime_projection` 或单独一个 `mechanism_code` 分支）并在配置里冻结期望值、`require()` 校验，续训时按同一逻辑比对。改动量小（可复用既有 26 行内的校验模式），预计 1 小时内可完成；应在启动 21 小时四臂运行前补齐，事后发现只能整臂重跑。
- **自称证据**：已读代码：tools/ch3_ft_transformer_field_token_protocol_a.py:3840-3852（build_run_identity）；tools/ch3_ft_c00_dual_selection.py:24、609-626、3390-3391；configs/ch3-ft-c00-halfwidth-screening-v1.json:10-13；对 tools/ch3_ft_entity_ranking_loss.py、tools/ch3_ft_gradient_controller.py、tools/ch3_ft_entity_bce.py 执行 `rg -n "__file__|sha256"` 均无自哈希命中；`rg -ln training-identities tools/ src/` 只命中 src/flow_probe/cuda_rwkv_training.py；今日提交 `5a9943a` 的 `git show` 差异确认改动了 ch3_ft_entity_ranking_loss.py 的 `prefix_scores`。

#### [严重] 同一新机制（实体级BCE共同底座）已存在两个互相冲突、都自称"预注册/定死"的读数窗口，本方案沿用的是未经真实数据验证的那一个

- **理由**：待审方案（及 thesis/methods/第三章定案.md 现行版本第132行"轮数10"）沿用"10轮×1000步，读第8-10轮均值"，这是从旧（无实体级BCE）四格的信噪比分析（恢复卡 J 条：前10轮相邻轮差中位0.0253/后10轮0.0705）继承来的口径。但已生成并试跑过一次（后放弃）的同机制服务器配置 `thesis/experiments/llm_probe/configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json` 的 `budget.basis` 字段记录了**同日、基于C00-half/C01-half真实十轮曲线**做的另一次专门分析："轮数预算由10轮改为6轮...读数口径相应改为第4-6轮均值（原第8-10轮），该口径变更在看到本臂任何结果前定死"，并给出具体数字（截断3轮为-0.0259假阴性，4轮+0.0091小于噪声0.0306，6轮+0.1635为噪声5倍）。两个"定死"版本口径不同（8-10 对 4-6），且没有文档说明为什么本方案的新档位（d=32、1层，比该分析所用的d=96更小、可能更噪）应该沿用未经真实数据验证的8-10而不是已用真实曲线验证过的4-6，也未对d=32重做该分析。
- **失败形式**：四格判据本身增量接近噪声量级（该 basis 字段自陈4轮增量0.0091小于噪声0.0306），选窗不同足以翻转判据方向。若21小时跑完后才发现应当用4-6而非8-10（或反之），等同于事后改口径，违反根AGENTS.md"不得看过最终测试后选择...候选、超参数或门槛"的门禁精神，且无法诉诸"预注册"来仲裁，结论的可信度会被质疑。
- **修法**：启动前显式二选一并写入方案：要么说明为何d=32档沿用8-10（需要论据，例如该档信噪比特性与d=96接近的证据），要么直接采用已用真实曲线验证过的4-6窗口（对应把训练轮数也从10改为6，节省约40%时间，与21小时预算的重新核算一并处理）。这是纯决策工作，不需要新实验，可在启动前完成。
- **自称证据**：thesis/methods/第三章定案.md 当前第130-160行（本次审查期间发现该文件被并发代理从 d_token=48/参数23,041 修订为 d_token=32/参数14,261，第138行文档自称"本表初稿的参数量23,041是错的，已更正"）；thesis/experiments/llm_probe/configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json 的 budget.basis 字段原文（已用 python3 json.tool 读取确认）。

#### [披露] 实体级BCE共同底座的"实体袋"既非BER的因果截断袋、也非评价的完整袋，是第三种未登记口径，ETA一侧可能大概率退化为max

- **理由**：entity_bce 挂载在 `training_step`（thesis/experiments/llm_probe/tools/ch3_ft_c00_dual_selection.py:1706-1756）内，对每个微批用 `base.sample_distinct_positions` 随机采样的 `effective_batch_size` 条序列内出现的实体聚合，每实体在该微批内能聚合到几条流完全取决于随机采样命中率，不受 `truncate_length` 或专门的实体级抽样控制。BER 的 `_ranking_phase`（同文件:2664-2751）用完全独立的 `EntityStratifiedSampler` 抽实体批，并显式按 `max_segments=ceil(truncate_length/sequence_length)` 逐实体截断（`_cap_segments_per_entity_rows`）。C01/C11 里这两项损失在同一步反传，各自看到的"实体证据集合"来自两套不同抽样与截断规则，entity_bce一侧的隐含袋特性从未登记或诊断。恢复卡记载"52.05%实体只拥有≤2条流"，随机序列采样下同一实体在单个微批内出现≥2条流的概率通常更低，entity_bce_loss（tools/ch3_ft_entity_bce.py:129-134）在 `m_per_entity=1` 时 `k_e=⌈0.5×1⌉=1`，tail_mean（ETA）退化为等价于maximum。
- **失败形式**：共同底座本意是让ETA聚合算子"在这一层持续产生梯度"（四格设计的不对称与改法.md第三节），若多数微批下entity_bce的tail_mean实际退化为max，则C10（ETA共同底座）与C00（max共同底座）的训练梯度大部分时候等价，稀释了补底座这一改法本应恢复的"C10−C00"训练效应。21小时跑完后若观察到C10-C00增量远小于预期，容易被误判为"ETA机制本身弱"，而根因可能是共同底座层面ETA没有真正生效。
- **修法**：启动前用真实微批配置跑一次零训练前向诊断，统计 entity_bce 内 `aggregated['m_per_entity']`（已在 tools/ch3_ft_entity_bce.py:132-134 计算，当前被就地丢弃未落收据）在实际微批下的分布，确认 `m_per_entity≥2` 的实体占比是否足以让tail_mean与maximum产生可观测差异；成本极低（复用现有前向，不需要训练），可作为启动前的一次快速自检，不阻塞已满足门禁的其余部分。
- **自称证据**：thesis/experiments/llm_probe/tools/ch3_ft_c00_dual_selection.py:1706-1756（entity_bce挂载）与:2664-2751（_ranking_phase独立抽样/截断）；tools/ch3_ft_entity_bce.py:60-155（entity_bce_loss实现，含m_per_entity计算但未落收据）；.Codex/docs/RWKV/RWKV第三章恢复卡.md第698行"52.05%实体拥有两条以上的流"。

#### [披露] 审查期间codebase被并发代理实时编辑，本方案关键数值（骨干宽度/参数量）已在审查窗口内变化，任务简报给出的数字与当前代码/文档均不一致

- **理由**：任务硬约束已声明"另有代理在改代码"。本次审查过程中，thesis/methods/第三章定案.md 的骨干缩容参数被实时修订：本会话第一次读取时为 d_token=48、参数23,041；随后重新grep该文件时已变为 d_token=32、参数14,261，且文档自认此前的23,041"是错的，已更正"。而任务简报给出的又是第三个数字（参数11,265）。三个数字均指向同一个 `local_screening_sixth_width_single_layer` 档位（该档位代码常量 `SIXTH_WIDTH_D_TOKEN = base.D_TOKEN // 6` 已在 tools/ch3_ft_c00_dual_selection.py:89 落地，等于32，与文档最新版一致但与简报不一致）。
- **失败形式**：本审查关于transform复用、entity_bce挂载、代码身份等结论均锚定在某一时刻读到的具体代码/文档快照上；若并发代理在本审查之后进一步修改了 SIXTH_WIDTH_D_TOKEN 的分母、entity_bce默认权重或其他冻结常量，实际用于启动21小时运行的配置可能与本报告核验过的状态不同，导致本报告的具体行号与数值核验过期，需要在真正创建正式运行配置那一刻重新核验而非直接信任本报告。
- **修法**：启动前对将要实际使用的配置文件重新跑一次 `--validate-config`（或等价 dry-run）只读核验，重新核对 `bare_ft_expected_parameter_count` 的实际返回值与配置里 `expected_parameter_count` 是否一致，不依赖本报告或任务简报里的静态数字。
- **自称证据**：本会话内前后两次读取 thesis/methods/第三章定案.md 得到不同数值（d_token 48→32，参数23,041→14,261，文档自认前者"是错的"）；任务简报另给出参数"11,265"，与两次读取均不同；tools/ch3_ft_c00_dual_selection.py:89 `SIXTH_WIDTH_D_TOKEN = base.D_TOKEN // 6`。

#### [披露] 方案目标措辞"确立"两个机制，超出 run_tier=screening_only 允许支撑的结论强度

- **理由**：thesis/experiments/llm_probe/AGENTS.md"数据合同与最终测试隔离"节明确：`screening_only` 运行"其结果不得进入论文正式结果、表格、帕累托比较或模型与机制优劣主张，拟晋级正文的结论须以独立正式运行身份按冻结公平合同重跑"；thesis/experiments/llm_probe/configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json 的 `checkpoint.resume_note` 也明写"筛选臂检查点...不得作为正式续训起点"；thesis/methods/第三章定案.md 自身"本页的边界"节同样写"本机结果只用于排序与止损，不进正文"。而待审方案的目标陈述是"在本机确立第三章两个机制...及其联动"，"确立"一词与上述边界不符。
- **失败形式**：若21小时跑完后团队把本机四格读数直接当作"机制已确立"写入正文、或据此砍掉/保留候选，将违反本仓库自己的门禁，需要在结果出来后二次纠偏，且容易在后续交接中被误读为已有正式结论。
- **修法**：仅需把方案目标陈述里的"确立"改为"筛选/排序与止损用途的信号收集"，与第三章定案.md自身边界节措辞对齐；不改变实验设计与执行内容，零成本。
- **自称证据**：thesis/experiments/llm_probe/AGENTS.md"数据合同与最终测试隔离"节原文；thesis/methods/第三章定案.md第155-160行"本页的边界"；configs/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1.json 的 checkpoint.resume_note 字段。

## 文档一致性复审

### ?

#### [自相矛盾] RWKV第三章恢复卡.md 自身内部矛盾：C10训练裁决在「研究边界」节写已定，在「当前阻断」节仍列为待裁决阻断

- **位置**：两处均在 .Codex/docs/RWKV/RWKV第三章恢复卡.md：第669-672行（二、当前Goal与研究边界）与第761-764行（六、当前阻断 第11条）
- **文档说**：第671行：「四格定义已于2026-09-03由用户裁决更改（该文件第6.5节）……C10因此变成真正的训练臂，不再是零训练重聚合，四格需四次训练」，并在第672行说明这正是当天主代理误判的直接根源。但同一份文件第761-764行「六、当前阻断」第11条仍写：「C10缩容档的路线自相矛盾……交用户裁决前不要启动该C10训练」，把同一件事列为尚未解决的活跃阻断项。
- **实际**：对照 thesis/methods/四格设计的不对称与改法.md 第157-164行（§6.5）：用户在读过代价分析后已于2026-09-03「明确指令『立刻修复Baseline缺一层的问题』。本改法即刻启动，不等四格数据」，即裁决已经作出且已启动执行。第669-672行的表述与之一致；第761-764行的「待裁决、不要启动」表述是写于裁决之前、未随后续更新回写的残留内容。
- **风险**：「六、当前阻断」是恢复卡里代理最先查、最权威的「是否还有活阻断」清单。若代理只读该节（而非先读研究边界节的最新裁决），会得出「C10训练仍待用户裁决、不能启动」的错误结论，与文档同一天已记载的用户裁决直接相反——这正是恢复卡自己反复告诫的「结论作废时未作废引用它的所有位置」病灶在其自身内部的实例。
- **修法**：删除或改写第761-764行第11条，标注该阻断已由2026-09-03裁决解除，指向第671行与四格设计的不对称与改法.md §6.5作为依据；或在该条前加「已解除」标记并说明解除时间与依据。

#### [缺失] 第三章定案.md 完全未提及2026-09-03当天『四格加入共同底座实体级BCE』的裁决，其执行计划与该裁决脱节

- **位置**：thesis/methods/第三章定案.md 全文（「一、方法」第15-25行、「四、怎么补」第122-169行均无相关内容）；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:671 与 thesis/methods/四格设计的不对称与改法.md 第157-193行（§6.5/6.6）
- **文档说**：定案.md「一、方法」表格描述的ETA/BER定义中不含共同底座；「四、怎么补」提出的本机执行计划（4.1节本机缩容档d=32/1层、4.2节C00/C01/C11共约25h训练）同样未提实体级BCE、未说明C00基线定义是否改变、未提及原C10零训练重聚合裁决是否作废。全文对『实体级』『entity_bce』『共同底座』检索零命中。
- **实际**：用户已于2026-09-03裁决『立刻修复Baseline缺一层的问题』（四格设计的不对称与改法.md §6.5），四格改为均含共同底座实体级BCE（C00=逐流BCE+实体级BCE(max)，C10=逐流BCE+实体级BCE(ETA)，C01/C11在各自底座上再加BER），且C10因此『变成真正的训练臂，不再是零训练重聚合，四格需四次训练』（恢复卡第671行）。恢复卡『进行中工作索引』（第318行）记载『补底座（实体级BCE）：任务1、2完成；权重与埋点在实测中』，是当前实际进行的工作；且已有制品佐证——服务器端已生成运行 ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1（status.json state=prepared，2026-09-03 16:39拉取，width_profile=half），与定案.md提出的d=32本机档位完全是两回事。
- **风险**：定案.md自称『本页是第三章状态的唯一答案页』，若后续代理或用户仅信任该文件，会完全不知道共同底座实体级BCE这一改变四格全部定义的关键裁决已经发生并已进入实施；若照定案.md『四、怎么补』的计划执行本机d=32档位四格，产出的结果仍是旧定义（无共同底座），既不满足用户已裁决的新方案，也会与恢复卡记载的『补底座任务1、2已完成』相互矛盾，造成方向错误或重复劳动。
- **修法**：在定案.md『一、方法』补入共同底座实体级BCE的定义与C10训练臂身份变更；在『四、怎么补』中说明该计划（本机d=32档位）与服务器端已启动的entitybce-newbase(half)训练之间的关系——是取代、并行还是待用户进一步裁决，不应留白。

#### [与制品不符] 第三章定案.md 断言『服务器不再使用（成本裁决）』缺乏独立记载支持，且与同日服务器端在跑制品冲突

- **位置**：thesis/methods/第三章定案.md:124；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md 全文（无该表述，只有『到期关机，可重启，数据未丢』）与制品 thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1/status.json、同目录 .pull.log
- **文档说**：定案.md第124行：『服务器不再使用（成本裁决）。本机唯一算力，MPS阻塞已修复。』
- **实际**：恢复卡全文核查（含专节〇之二点六『服务器到期关机』）只记载服务器『到期关机，可重启，数据未丢』，性质是被动到期而非主动『不再使用』的成本裁决，且给出了『重启后的动作顺序』，暗示仍会再用。更关键的是，本地制品 status.json（state=prepared，未加入.gitignore白名单、需 --no-ignore 才能看到）与其 .pull.log 显示：2026-09-03 16:39（早于定案.md 20:12的最后提交时间约3.5小时）刚从服务器 root@116.172.66.188:36810（恢复卡B0条确认为『实际在用』的正确服务器）拉取了entitybce-newbase运行的身份文件，说明该时刻服务器仍在被用于准备共同底座训练任务。
- **风险**：若『服务器不再使用』只是定案.md写作时未核实最新状态的想当然判断，会导致刚准备好（prepared态）的服务器端共同底座训练任务被无声放弃，且没有任何文档记录这一放弃决定；若该断言确有依据（例如16:39之后用户又作出了新裁决），定案.md也未说明依据、时间点，以及entitybce-newbase运行该如何处置。
- **修法**：核实『服务器不再使用』这一决定的真实来源与时间；属实则需在恢复卡/相关文档同步记载，并明确交代该在跑的服务器端entitybce-newbase运行的处置方式；不属实则删除该断言，按恢复卡的『到期关机、可重启』表述改写。

#### [过期] ETA-BER联合框架进度卡.md 第70、85行『两项门槛待冻结』『改底座/用观测量二选一』均已过期

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:70,85；对照 thesis/methods/四格设计的不对称与改法.md:99-102（§五）与 .Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md:69,117
- **文档说**：进度卡第70行：『独立耦合证据｜三个观测量已预注册，但两项门槛待冻结，须在C11启动前补齐』；第85行仍把『改四格补实体级底座（重跑）』与『改用耦合观测量（零代价）』列为并列、尚待选择的『二选一』。
- **实际**：机制耦合观测预注册.md第69、117行明确『✅已冻结（2026-09-02，C11启动前）』，给出具体数值（观测量二：n_high≥12，P=0.017578；观测量三′：预期方向τ<1），该冻结经git log核实对应提交6c2125c（『freeze the two pending coupling-observation thresholds』）确实存在。且『二选一』本身已被用户于2026-09-03裁决为『改四格补实体级底座』并立即启动（四格设计的不对称与改法.md §6.5），不再是待选项。
- **风险**：后续代理据进度卡会误判耦合观测量门槛仍未定、且『补底座』与『用观测量』仍是两个平级候选方案，从而重新发起已被用户裁决关闭的讨论，或未意识到C10已经变为训练臂这一影响四格全部读数解读的关键前提。
- **修法**：更新进度卡第70、85行，写明两项门槛已冻结的具体数值，并注明『补底座』已由用户裁决启动、不再是待选项。

#### [过期] ETA-BER联合框架进度卡.md 记载的缩容档四格进度（C01在跑/C10配置已补齐/C11待跑）整体滞后约9小时，已被后续进展推翻

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:17-19,30；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:81-101（〇之二点五）与 thesis/methods/第三章定案.md:44-53
- **文档说**：『缩容档四格进度（2026-09-03）：C00-half已跑满20轮……；C01-half在跑；C10-half配置已补齐（身份dfed8add18f7…7005）；C11-half待跑。』总进度对表同样写『C00完成、C01中断、C10有、C11未启动』。
- **实际**：经git log核实，进度卡最后提交于2026-09-03T11:13:24，而恢复卡与定案.md分别于当日20:07:35与20:12:09提交，进度卡滞后约9小时，未反映期间进展：C01-half实际已完成（提前停在第10轮，判据一C01>C00通过，+36.25%，符号检验P=0.0078），并非『在跑』；C11-half实际已运行到第6轮，因服务器到期关机而中断（有1检查点、10份收据），并非『待跑』/『未启动』；C10-half已有一个读数0.711063，但其计算方法（零训练重聚合）与用户当日裁决存在待厘清的口径问题，并非单纯『配置已补齐』。
- **风险**：依据进度卡会得出『判据一尚未验证、四格还差三格』的错误进度判断，实际是『三格均已产出读数（一格有方法学争议待厘清），判据一已通过』，可能导致重复安排已完成的工作或漏报已通过的判据。
- **修法**：用恢复卡〇之二点五节（第81-101行）的最新状态表覆盖进度卡第17-19、30行。

#### [自相矛盾] 机制耦合观测预注册.md 正文『✅已冻结』标注与末尾『五、边界』章节自相矛盾

- **位置**：.Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md:69,117（vs 177-179）
- **文档说**：第69行、第117行分别标注『✅已冻结（2026-09-02，C11启动前）』并给出具体门槛值（n_high≥12；τ<1方向）；但文档末尾『五、边界』第177-179行仍写：『观测量二在15组配对下的临界值、观测量三′的方向声明，两项待补……补齐前该项记为「未预注册」，其结果不得用于支撑耦合主张。』
- **实际**：两处针对的是完全相同的一对门槛。第69/117行是2026-09-02对原文（写于2026-09-01）的追加更新，而第177-179行的『五、边界』总结章节属于原文，未随该次更新同步修订，是残留的过时表述。
- **风险**：读者若习惯先读文档末尾的『边界/限制』总结（一种常见的快速核验方式），会得出与正文相反的结论——误判这组观测量仍『未预注册』、不可用于支撑耦合主张，从而低估或错误处理已有的C11耦合证据。
- **修法**：删除或改写第177-179行，使其与第69、117行的已冻结状态一致。

#### [表述含糊] 第三章定案.md 把方法学存在争议的C10-half读数(0.711063)平铺列入『已经测出了什么』，未附恢复卡记载的警告

- **位置**：thesis/methods/第三章定案.md:50；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:97-100,761-764
- **文档说**：定案.md『二、已经测出了什么』2.2节表格将C10-half『0.711063｜+0.060645(+9.3%)｜由C00的tail检查点零训练重聚合算得』与C00-half、C01-half的读数并列呈现，并在小节导语称『全部为已完成运行的实测』。
- **实际**：恢复卡对同一数字明确警告：『⚠️与此并存的矛盾……未经用户裁决前，既不要启动该C10训练，也不要把上面的0.711063写入正文』（第97-100行）——因为该数字所依赖的计算方法（零训练重聚合）与用户2026-09-03裁决的『C10应变为真正训练臂』直接冲突（见前述发现1、2），即该读数对应的旧口径实质上已被取代。
- **风险**：后续代理或撰写正文时可能把0.711063当作C10-half的权威、无争议读数直接引用（例如用于核验判据C10>C00），而实际上其方法论已被2026-09-03的裁决取代，正确的C10-half读数应来自新的训练臂（含实体级BCE+ETA聚合）且尚未产出。
- **修法**：在定案.md为该行加注与恢复卡一致的警告，或将其移出『已经测出了什么』章节，改标为『旧口径读数，待新裁决下的训练臂重新产出』。

#### [过期] ETA-BER联合框架进度卡.md『缩容档实测参数』表沿用已被恢复卡废弃的窗口采样步速/单轮时长口径

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:100-103；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:21-33,561-571
- **文档说**：『步速：全容量2.418步/秒，half 3.309步/秒，只快37%；单轮（含验证）：全容量约7.7分钟，half约5.8分钟』。
- **实际**：恢复卡已核实这组数字属窗口采样，与收据wall_seconds全程平均口径不一致，现已统一为『全容量2.387步/秒、half 3.352步/秒，只快40%；单轮：全容量7.92分钟、half 5.71分钟』（第21-33行『K条』），并将此类混淆专门记入误判登记表（第363行：『C00-half步速在卡上同时写着3.322与3.403…』）。
- **风险**：数值误差本身不大（<2%），但两份文档并列引用会造成『同一个量出现不同数字』的困惑，且违反恢复卡自己确立的『同一量在卡上出现过三个值时，一律以收据全程平均为准』的规范。
- **修法**：用恢复卡K条的全程平均数字覆盖进度卡第100-103行，并注明口径为『收据全程平均』。

### ?

#### [缺失] 进度卡未反映2026-09-03已裁决的"补齐共同底座（实体级BCE）"重大决策，全文仍按旧四格设计撰写

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 全文，尤其第7-19行（实验档位）、第60-74行（四、联合框架）、第78-86行（五、当前阻塞与下一步）
- **文档说**：第8-19行只描述缩容档half（d=96、3层、参数240955）四格C00/C01/C10/C11的进度；第72-74行把"C00是纯逐流BCE、ETA无处附着"当作尚待解决的"根源诊断"呈现；阻塞表第4条仍把"交互项无判别力"列为待二选一处理的开放问题。
- **实际**：thesis/methods/四格设计的不对称与改法.md 第6.5节记录用户已于2026-09-03明确裁决"立刻修复Baseline缺一层的问题"，四格全部改为共同底座（实体级BCE）+ 各自机制，C00不再是裸FT；thesis/methods/第三章定案.md 第4节据此给出新的"sixth-width"缩容档位（d_token=32、1层、参数14261，与half的d=96/3层不同）与执行顺序；仓库已有真实运行制品 thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c00-entitybce-newbase-halfwidth-screening-v1/status.json（state=prepared）与代码提交 712495a（16:09）。git提交时间线证实：进度卡最后一次修改为62ab1f8（2026-09-03T11:13:24+08:00），而第三章定案.md的关键提交693af68/a263b3d/58572e5与RWKV第三章恢复卡.md的89717ff均发生在20:06-20:12，即全部晚于进度卡9小时以上，进度卡从未纳入这批决策。
- **风险**：下一轮若只读进度卡（它自称"每次问进度即更新本卡并照此回答"），会继续按旧half四格（无实体级BCE）规划、汇报或复用其C10/C11训练配置与梯度控制器结论，而实际上四格定义已变、需要四次全新训练、梯度合成从两目标变三目标（现有CAGrad式控制器需重新裁决）、C00不再可与已发表方法池的"裸FT"口径直接比较——这正是背景说明中列出的"结论作废未回写"同型事故的第四次重演。
- **修法**：在进度卡开头新增一节，注明"2026-09-03 用户已裁决补齐共同底座（实体级BCE），四格定义变更，旧half四格（C00/C01/C10/C11-half，无实体级BCE）数据仅作历史止损/排序参考，不再是当前四格"，链接第三章定案.md第4节与四格设计的不对称与改法.md第6.5节；同步删除或标注阻塞表中已被新决策取代的条目（尤其第4条"交互项无判别力"的二选一处置，已被'补底座'方案实质性回答）。

#### [与制品不符] "C11-half 待跑"/"C11 未启动"与运行制品不符：C11-half 实际已跑到第6轮，因服务器到期关机而中断

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第19行（"C11-half 待跑"）与第30行（"C11 未启动"）
- **文档说**：第19行："C11-half 待跑"；第30行总进度对表："C00 完成、C01 中断、C10 有、C11 未启动"。
- **实际**：实测 thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c11-halfwidth-screening-v1/：status.json 记 state="running"（但该状态已被RWKV第三章恢复卡〇之二点六节判定为服务器到期关机后的僵尸状态，非真实运行中）；receipts/ 目录下有 entity-ranking-diagnostics-1.json 至 -6.json 共6份逐轮诊断收据，checkpoints/ 下有1个 inflight.pt 检查点，无 selection.json。即C11-half确已开始训练并完整跑完6轮，只是未到8-10轮读数窗口、且随服务器关机中断，而非从未启动。RWKV第三章恢复卡〇之二点五节已如实记录"只到第6轮即随服务器关机中断"。
- **风险**：下一轮可能误判"C11从未跑过"，从零规划启动动作（如重新排队GPU、重新准备配置），既浪费已投入的6轮训练成本，也可能与恢复卡中已经存在的中断态检查点/进度产生管理冲突（例如误删或误覆盖 inflight.pt）。
- **修法**：改为"C11-half 已开始训练，因服务器到期关机于第6轮中断（服务器可重启、数据未丢），尚未到第8-10轮冻结读数窗口"，并注明是否延用该次训练续跑或按新的实体级BCE底座重新起跑（取决于发现1的裁决）。

#### [与制品不符] "C01 停在 7/20"与运行制品不符，且"C01-half 在跑"与其真实finished状态矛盾

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第82行（"C01 停在 7/20"）与第18行（"C01-half 在跑"）；对照 thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c01-halfwidth-screening-v1/status.json
- **文档说**：第18行："C01-half 在跑"；第82行阻塞表："四格未完成（C01 停在 7/20，C11 未启动）| 恢复 C01 → C11 → 裁决"。
- **实际**：status.json 原文：{"state": "finished", "exit_code": 0, "early_stopped_at_epoch": 10, "configured_epochs": 20, "stop_reason": "2026-09-03 用户裁定:缩容档只用前 10 轮,后 10 轮不提供关于机制是否生效的新信息"}。即C01-half已完成（非在跑），提前停止的轮次是10（非7），且是用户主动裁定的停止（有明确stop_reason），不是训练中断。RWKV第三章恢复卡〇之二点五节与第三章定案.md 2.2节均一致记录为"完成10轮（提前停）"。
- **风险**：若按"7/20"理解，下一轮可能误判还需补跑3轮才能凑够冻结口径要求的第8-10轮窗口；若按"在跑"理解，可能误判该训练仍占用资源、去查询或等待一个实际已经结束的进程；阻塞表"恢复 C01"的处置指令本身就建立在错误前提上，可能触发不必要的重启操作。
- **修法**：改为"C01-half 已完成（用户主动提前停止于第10/20轮，收据完整，非中断）"；阻塞表处置列删除"恢复 C01"，只保留C11（及发现1裁决后的新四格）的续跑安排。

#### [与制品不符] ETA源年增益"+0.0714"全仓无出处，与恢复卡/定案记录的+0.060645（+0.0607）不符

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第46行；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md 〇之二点五节与 thesis/methods/第三章定案.md 第50行
- **文档说**：第46行："效果 | **未验**。源年 `C10 > C00`（`+0.0714`）已过判据第一条，目标年门未测"。
- **实际**：对全仓（含thesis、.Codex/docs、runs收据）做rg检索，字符串"0.0714"仅出现在进度卡本行自身（其余命中均为不相关的FPR阈值0.07142857…或arXiv编号），无任何计算路径或收据能推出0.0714。权威读数为：RWKV第三章恢复卡〇之二点五节精确给出 C10-half第8-10轮均值0.711063 减 C00-half同口径0.650418 = +0.060645（+9.3%），第三章定案.md第50行圆整为"+0.060645（+9.3%）"，与恢复卡完全一致。
- **风险**：若该数字被抄入正文或跨文档比较，会与恢复卡/定案的权威读数（+0.060645）不一致，且无法回溯来源，一旦流入论文将构成不可追溯的实验数值断言，直接违反根AGENTS.md的"每个实验裁决必须关联原始日志"魔法数字门禁纪律。
- **修法**：改为"+0.060645（+9.3%）"（或引用定案圆整值"+0.0607"），并注明口径：C10-half第8-10轮均值（0.711063，由C00收据零训练重聚合算得）减C00-half同口径（0.650418）。

#### [过期] "耦合观测量两项门槛待冻结"过期：两项门槛已于2026-09-02冻结，且冻结时间早于进度卡本次写入时间

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第83行；对照 thesis/methods/四格设计的不对称与改法.md 第99-102行与 .Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md 第69、117行
- **文档说**：第83行阻塞表第2条："耦合观测量两项门槛待冻结 | **须在 C11 启动前补齐**，否则该轮数据作废"。
- **实际**：四格设计的不对称与改法.md第99-102行："✅ 两项待定已于 2026-09-02 C11 启动前冻结（提交 `6c2125c`）：观测量二的15组配对临界值 n_high ≥ 12（P=0.017578）...观测量三′的方向预期 τ < 1"；机制耦合观测预注册.md第69行"✅ 已冻结（2026-09-02，C11 启动前）：15 组配对的临界值为 n_high ≥ 12"、第117行"✅ 方向已冻结（2026-09-02，C11 启动前）：预期 τ < 1"均独立确认。git记录冻结提交6c2125c时间为2026-09-02T17:39:22+08:00，早于进度卡最后一次修改（62ab1f8，2026-09-03T11:13:24+08:00）近18小时——即进度卡撰写时该信息本已可得却未采纳。
- **风险**：下一轮可能误判两项判据仍未定，重新去"补齐"已经冻结的门槛（如重算n_high临界值或重新声明τ方向），这不仅浪费精力，若在已看到C11初步数据后"顺手"重新定门槛，将直接违反预注册纪律（"冻结后不得按结果修改定义或门槛"），构成结果无效的高危操作。
- **修法**：第83行改为"两项门槛已冻结（观测量二 n_high≥12；观测量三′ 方向 τ<1，提交6c2125c），无须补齐，C11出结果后直接按此判读"。

#### [自相矛盾] 机制耦合观测预注册.md自身自相矛盾：正文已标"✅已冻结"，但结尾"边界"节仍写"两项待补"

- **位置**：.Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md 第69/117行（"✅已冻结"）与第177行（"两项**待补**"）
- **文档说**：第二节观测量二（第69行）："✅ 已冻结（2026-09-02，C11 启动前）：`15` 组配对的临界值为 `n_high ≥ 12`"；3.2节观测量三′（第117行）："✅ 方向已冻结（2026-09-02，C11 启动前）：预期 `τ < 1`"。但第五节"边界"（第177行）仍写："观测量二在 `15` 组配对下的临界值、观测量三′的方向声明，两项**待补**；相应候选采纳后须在其实施计划中补齐并回写本文件，补齐前该项记为「未预注册」"。
- **实际**：两处表述直接矛盾：正文中间在2026-09-02追加了冻结标注（并给出精确计算依据），但文件末尾的"边界"总结段落是原始2026-09-01写法，未随之更新。此外该"边界"节还写"四格判据仍为...`I > 0`"，这也已被RWKV第三章恢复卡2026-09-03的修订判定为"无判别力，不得再作为联动或创新点的判据"，同样是该文件未同步的过期内容。
- **风险**：该文件是耦合观测量的权威预注册文档，若读者只看结尾"边界"节（通常作为快速核对入口），会得出与正文相反的结论（门槛未定/I>0仍是判据），这正是进度卡第83行错误的直接源头之一——即便主代理只读进度卡就已经错，但根源文档自身的自相矛盾会让任何后续核对者继续踩坑。
- **修法**：在机制耦合观测预注册.md第五节"边界"重写第177行为"两项门槛已于2026-09-02冻结（提交6c2125c），详见第二、3.2节"，并同步更新"四格判据仍为...I>0"一句，标注I>0已作废，指向RWKV第三章恢复卡2026-09-03的修订。

#### [过期] "状态日期：2026-09-02"与文档实际内容及最后提交时间不符

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第7行
- **文档说**：第7行："状态日期：2026-09-02"。
- **实际**：文档正文多处显式标注"2026-09-03"（如第17行"缩容档四格进度（2026-09-03）"），git记录该文件最后一次修改提交（62ab1f8）时间为2026-09-03T11:13:24+08:00。
- **风险**：读者可能按字段值误判全卡内容截至09-02（低估09-03新增内容的时效性），也可能反过来过度信任"状态日期"字段本身，忽略卡内09-03上午写入的内容其实已经落后于当天下午/晚上的最新裁决（见发现1）。
- **修法**：更新为"状态日期：2026-09-03"，并建立"每次编辑本卡后随手同步该字段"的机械习惯。

#### [与制品不符] BER目标年增益"+0.0567"与精确制品数值+0.0566469647904972的四舍五入不符

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md 第56行；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md 第694行
- **文档说**：第56行："效果 | 旧编译路径下源年实体 AP `0.9537` 对 C00 `0.7888`、目标年 `+0.0567`"。
- **实际**：RWKV第三章恢复卡3.1节精确记录C01对C00目标年实体AP正增益为 `+0.0566469647904972`，四舍五入到四位小数应为 `+0.0566`，而非 `+0.0567`（第五位小数为4，应舍不入）。0.9537/0.7888两个数字本身经核实准确（对应更精确值0.9537273846736761与0.7887951742121221）。
- **风险**：属末位舍入误差，风险较低，但若被复制传播且与恢复卡的精确值并列引用，会造成同一量在不同文档呈现两个不同末位数字，违反"同一量出现多值须统一口径"的纪律（恢复卡误判登记表已记录过同类错误的教训）。
- **修法**：改为 `+0.0566`（或直接引用全精度 `0.0566469647904972`）。

### ?

#### [自相矛盾] 第三章定案.md §4执行计划仍用旧「C10零训练重聚合」三训练方案，与同日恢复卡记录的「四格已改共同底座实体级BCE、需四次训练」直接冲突

- **位置**：thesis/methods/第三章定案.md:1-13（承认四格定义已改判）与:122-163（§4.2执行顺序，三段式排期，未提实体级BCE）；对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:671（提交89717ff，09-03 20:07）
- **文档说**：第三章定案.md开篇写「四格定义已在2026-09-03由用户裁决更改」，但其§4.2『执行顺序与决策点』仍排三段训练：『C00训练（双聚合，同时产出C00与C10）→C01训练→C11训练』，合计约25小时，全程未出现实体级BCE、未提『四次训练』。
- **实际**：RWKV第三章恢复卡.md第671行（当天20:07提交，比第三章定案.md最后一次提交58572e5的20:12早5分钟）明确记载：『四格定义已于2026-09-03由用户裁决更改（四格设计的不对称与改法.md第6.5节）：四格全部加入共同底座实体级BCE...C10因此变成真正的训练臂，不再是零训练重聚合，四格需四次训练』。当天12:24-16:30的三目标梯度合成设计.md、实体级BCE权重的口径推导.md、IMTL-G两任务退化验证.md等一系列文档，以及代码提交c96bb74（11:58『mount entity BCE in training_step behind a flag』）证明『补底座』是贯穿全天的实质在建工作而非已放弃方案；恢复卡〇之二点七『进行中工作索引』第318行也登记『补底座（实体级BCE）：任务1、2完成；权重与埋点在实测中』。
- **风险**：若按第三章定案.md§4执行，会在d=32缩容档上重跑一次仍用旧C10零训练重聚合的三训练四格，其C11数据仍无法为交互项/联动主张提供判别力——这正是当初决定改四格定义的原因——造成又一轮约14-25小时的无效实验，且重复恢复卡第672行明确点名的『结论作废未同步引用位置』错误模式。
- **修法**：动工前需用户或主代理明确二选一：(a) 按恢复卡671行的新定义补齐§4计划——C00用max聚合＋实体级BCE、C10用ETA聚合＋实体级BCE（真训练非零训练重聚合）、C01/C11在各自底座上再加BER，并重算四次训练的工时预算；或(b) 若坚持先跑旧定义验证d=32档位可行性，须在文档中显式声明『本轮为screening、不满足新四格定义、不得作为联动创新点的正式证据』，避免被当作定稿四格误用。

#### [缺失] 进度卡完全未记录2026-09-03的四格重新定义（共同底座实体级BCE、C10转为真训练臂），仍呈现I>0为待验证联动证据

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:27-30（总进度对表）、:60-74（四、联合框架）
- **文档说**：进度卡把四格仍描述为『z1'（ETA开关）×z2（BER开关）→C00/C10/C01/C11』的旧结构；『交互项判别力』一行写『无。z1'训练通道按构造只在z2=1存在，I>0近乎必然...』『独立耦合证据｜三个观测量已预注册，但两项门槛待冻结』，把I>0问题当作『已知弱点』陈列，未标注该判据已被正式作废。
- **实际**：.Codex/docs/RWKV/RWKV第三章恢复卡.md:670-672明确写『I>0一条已被证明无判别力，不得再作为联动或创新点的判据』且『四格定义已于2026-09-03由用户裁决更改...四格需四次训练』。该决定记录于四格设计的不对称与改法.md第99行（提交a1006e9，09-03 11:04），早于进度卡最后一次编辑（提交62ab1f8，09-03 11:13）——即进度卡在决定已作出之后被编辑，却仍未同步『四、联合框架』整节。
- **风险**：依赖进度卡的读者会认为当前四格仍是三训练结构、I>0仍是待用观测量佐证的『弱耦合』证据，从而重复恢复卡第672行明确指出的『主代理据此把I>0当创新点判据、建议不加实体级BCE，与用户裁决相反』这一具体误判。
- **修法**：『四、联合框架』整节按恢复卡670-672行重写：标注I>0判据已作废及理由，四格已改为四训练结构（C00/C10各自训练＋实体级BCE），并链接四格设计的不对称与改法.md§6.5作为裁决出处。

#### [过期] 进度卡「两项门槛待冻结，须在C11启动前补齐」已过期一天以上

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:70,83
- **文档说**：『独立耦合证据｜三个观测量已预注册，但两项门槛待冻结，须在C11启动前补齐』；阻塞表第2行『耦合观测量两项门槛待冻结｜须在C11启动前补齐，否则该轮数据作废』。
- **实际**：两项门槛（观测量二15组配对临界值n_high≥12、观测量三′方向τ<1预期）已于2026-09-02 17:39冻结（提交6c2125c，见.Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md:69-77,117-131），该冻结甚至早于进度卡本身最后一次编辑（09-03 11:13）。四格设计的不对称与改法.md第99-102行也已记录『✅两项待定已于2026-09-02...冻结』。
- **风险**：下一轮代理可能误判C11尚不能启动，或误以为该轮已有数据需要作废，阻塞已可执行的工作；这正是本次任务背景所述『第四处』已知冲突，现已核实确认为真。
- **修法**：删除或更新进度卡第70、83行，改为引用已冻结的具体阈值（n_high≥12、τ<1）及冻结提交号6c2125c。

#### [自相矛盾] 进度卡仍用已被恢复卡明令禁止的「嵌套CVaR」框架解释耦合强度弱

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:67 对照 .Codex/docs/RWKV/RWKV第三章恢复卡.md:664-665、thesis/methods/第三章定案.md:100-104
- **文档说**：进度卡『四、联合框架』表格：『耦合强度｜弱。「嵌套CVaR」只在负实体分支成立（∂L/∂S_p≤0是MIL语义）；对照组内层已是CVaR』——把『嵌套CVaR』当作仍部分有效、用来解释弱耦合的分析框架。
- **实际**：恢复卡明确写『两机制共享嵌套CVaR框架』这一主张已被后续分析推翻（本方法不是FCCO实例），不得据此论证，且命名规则明文『不含嵌套、双层字样』。第三章定案.md§二之二也把『与BER构成嵌套CVaR共享框架』列为『已被推翻的旧主张』（三个组成部分有两个撑不住），并与当前论证（机械依赖＋可证伪观测量nonargmax_grad_share）明确切割。
- **风险**：下一轮代理若沿用进度卡措辞解释『为什么耦合弱』，等于复用一个已被正式否决的理论框架做设计依据，违反仓库『被否决实现/主张的读数或框架只能用于说明其为何被否决，不得作为当前设计依据』的纪律。
- **修法**：进度卡该行改为仅陈述可独立成立的机械性事实（如『C01内层已用max聚合，属性质上退化的α→0经验CVaR』『ETA仅在负实体分支的MIL语义下对损失有单调影响』），删除『嵌套CVaR』标签，并加一句指向恢复卡664-665的否决记录。

#### [与制品不符] 三个预注册耦合观测量（CV_K、Jaccard、τ）从未被实际产出；已产出的诊断量与其数学定义不一致，代码注释存在误导性等同

- **位置**：.Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md:29-131(定义)、:144(声明落盘路径receipts/mechanism-coupling-<epoch>.json)；实际制品见thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-c11-halfwidth-screening-v1/receipts/entity-ranking-diagnostics-*.json；代码注释thesis/experiments/llm_probe/tools/ch3_ft_c00_dual_selection.py:2808
- **文档说**：预注册文档定义三个观测量——观测量一CV_K=std(ξ)/mean(ξ)（轮内ξ逐步序列的变异系数）、观测量二J_K（相邻轮活动负实体集合的Jaccard相似度）、观测量三′τ（活跃/非活跃实体的内层尾部占比之比）——并声明写入『receipts/mechanism-coupling-<epoch>.json』。ch3_ft_c00_dual_selection.py:2808注释把nonargmax_grad_share称作『观测量一』。
- **实际**：全仓库rg搜索`mechanism-coupling`/`mechanism_coupling`零命中（tools/、src/及全部receipts目录，用--no-ignore核实）；直接fd列出C11-half实际10份收据文件，无该文件。已产出的entity-ranking-diagnostics-1.json中falsifiable_observables字段实测为{nonargmax_grad_share_epoch_mean, per_budget_active_rate_epoch_mean, inner_tail_frac_epoch_mean}等**轮级标量均值**：既不是ξ的逐步序列（无法算CV_K）、也不是活动负实体的成员集合（只有比例，无法算Jaccard）、也不是按活动集membership拆分的τ（inner_tail_frac只有全轮池化均值）。nonargmax_grad_share（非argmax流上梯度能量占比）与预注册文档定义的CV_K（ξ的变异系数）在数学上是两个不同的量，代码注释『是观测量一』的等同关系未见任何文档正式论证。
- **风险**：若误以为『两项门槛已冻结』等同于『观测量已可用于产出联动证据』，会误判C11-half现有收据已能支撑或证伪耦合主张；实际上要拿到可比对预注册门槛的读数，仍需新增代码实现。
- **修法**：明确区分『门槛已冻结』与『观测量已实现』两件事。若采用nonargmax_grad_share作为替代观测量（第三章定案.md§二之二实际做法），应在预注册文档中正式记录替代关系及原三观测量被搁置的理由。若要实现原三个观测量，需要：(a)在ch3_ft_entity_ranking_loss.py的ranking loss函数内（约505-510行附近的xi_k计算处）把逐步xi_k累积进列表而非只算标量均值，供轮末计算CV_K；(b)把pairwise>xi_k.unsqueeze(1)算出的布尔掩码映射回负实体全局ID集合（当前只保留比例），并按预注册文档§四『不得写出实体ID列表』的要求，只落盘相邻轮集合的Jaccard值或哈希摘要；(c)把entity_tail_aggregation模块的逐实体inner_tail_frac按(b)得到的活动/非活动二分重新聚合为两个条件均值以得到τ。

#### [自相矛盾] 机制耦合观测预注册.md自身首尾矛盾：正文已冻结两项门槛，末尾§五边界仍写「两项待补/未预注册」

- **位置**：.Codex/docs/RWKV/2026-09-01-机制耦合观测预注册.md:69-77,117-131（正文，已由提交6c2125c更新）与:176-179（§五边界，未被6c2125c触及）
- **文档说**：正文：『✅已冻结（2026-09-02，C11启动前）：15组配对的临界值为n_high≥12』『✅方向已冻结...预期τ<1』；文末§五写『观测量二在15组配对下的临界值、观测量三′的方向声明，两项待补...补齐前该项记为「未预注册」，其结果不得用于支撑耦合主张』。
- **实际**：用`git show 6c2125c`核实该提交diff，只修改了正文两处冻结段落（原文件行66-68和105-112附近），从未触碰§五边界小节，遗留了冻结前的旧措辞。
- **风险**：读者若只读文末『边界』总结（常见的快速核对习惯），会得出『两项仍未预注册、不可用于支撑耦合主张』的错误结论，与正文相反——与本次任务背景所述『进度卡』问题同型的错误会在这份被大量引用的预注册文档内部再发生一次。
- **修法**：同步修订§五对应两行，改为『两项已于2026-09-02冻结，见第二、三节，可用于支撑或证伪耦合主张』，或直接删除该段。

#### [缺失] 进度卡与第三章定案.md的「还缺什么」清单显著低估参照论文第三章的理论与结构篇幅，且遗漏第二骨干通用性验证这一结构决策点

- **位置**：thesis/methods/ETA-BER联合框架进度卡.md:32(『定理1（一阶泰勒，2行）』)；thesis/methods/第三章定案.md:111-120(§三『还缺什么』只列3项)；对照wiki/papers/attack-detection/朱焱雷-全文.md:1174-1326(§3.5理论分析全文，需rg -a读取因含\0字节)、:1023-1080(算法3.1)、:1080-1173(§3.4.2/3.4.3两骨干实现细节)
- **文档说**：进度卡把理论缺口压缩为单一一条『定理1（一阶泰勒，2行）』且注明『本章已声明不主张收敛保证』；第三章定案.md的『还缺什么』清单只有3项（C11读数、ETA目标年评价、四格统一重跑），完全未提算法伪代码、理论分析、8个实验小节这三项进度卡自己在对表里列出的缺口，也未提第二骨干问题。
- **实际**：原件核实：参照论文『3.5理论分析』实际是四个独立子节（3.5.1课程学习对非凸损失曲面的平滑作用；3.5.2 Stackelberg均衡收敛稳定性，正文标题实为『定理2：Stackelberg梯度的稳定性』而非『定理1』，含三段式证明分析（抑制高敏感度方向/平滑优化轨迹/免虚假鲁棒性），远非『2行』；3.5.3基于平坦极小值理论的泛化能力解析；3.5.4连续空间博弈的物理映射与理论上限分析），而非单一『定理1』。算法3.1（3.4.1节）是约30行的完整训练流程伪代码。3.4节还含3.4.2/3.4.3两个子节，分别针对ET-BERT与BUPT-CNN两个骨干给实现细节，对应参照论文四格消融是『两骨干各一遍』（进度卡对表里写了这句话）；而本课题目前全部四格计划（第三章定案.md§4、恢复卡J条身份表）均只覆盖FT-Transformer单一骨干，未见任何文档把『是否需要第二骨干通用性验证』列为已裁决或待裁决事项。
- **风险**：若仅以第三章定案.md（自称『唯一答案页』）或进度卡的缺口清单规划后续工作，会严重低估补齐『确立联合框架』所需的理论论证篇幅（把一个多节理论体系当成一个2行定理，且定理编号本身都对不上），并遗漏『是否需要第二骨干验证泛化性』这一参照论文明确存在、本课题从未讨论过的结构决策点。
- **修法**：把『还缺什么』扩展为至少5类：(1)C11读数，(2)ETA目标年评价，(3)四格按新定义统一重跑，(4)算法伪代码+理论分析（先裁决本方法可主张哪几类理论性质——收敛保证已声明不主张，需另找可主张的性质如稳定性或泛化的描述性论证，量级对标参照论文的4个子节而非1个定理），(5)是否补第二骨干通用性验证；若不补，需仿照参照论文『主动缩小研究对象』的体例在正文写明理由，而非默认略过不提。

---

**发现总数：54**（未经核验）