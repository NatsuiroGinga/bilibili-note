# RWKV 当前恢复卡

<!-- RESEARCH_ROUTE=RWKV -->

- **状态日期**：2026-08-12
- **用途**：RWKV 当前恢复入口；只保留可执行状态、不可变边界和证据索引。更新时覆盖可变状态，不叠加历史交接。
- **恢复顺序**：本卡须在 `RWKV路线总控.md` 后、`2026-08-08-第三章候选方案登记册.md` 前完整读取。旧[路线跨会话交接](2026-08-06-RWKV路线跨会话交接.md)是历史档案，不得作为当前任务入口。

## 当前状态与唯一下一步

- C12“漂移触发有界状态控制与可信双参照校正”与 R1 双层尾部风险的种子 42 Q0 均为**不晋级／弱证据不利**，且均为 `screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`；不得写成正式证伪、论文结果或方法有效。
- 不重跑 C12/R1，不依据目标开发标签追参；状态和指标以[第三章候选方案登记册](2026-08-08-第三章候选方案登记册.md)及下列报告为准。
- **唯一下一步**：候选 B“连续时间频谱状态与有界信任融合”。按既有文献边界收窄独立机制，再做最小可证伪快速消融；当前[实施计划](2026-08-12-候选B角色分离物理时间RWKV-Q0实施计划.md)。结果先写专属报告和登记册，再更新总控。
- **2026-08-12 11:27 运行检查点**：C56 上完整基线矩阵 `runs/baselines/lspr-baseline-dual-track-q0-seed42-v1` 已完成 `17/17` 个单元，`state=finished`、`exit_code=0`、`final_accessed=false`。候选 B 的 `candidate-b-time-q0-s42-v1` 持久会话仍在运行，首批三个变体训练结束后正在封存概率，运行根为 `runs/candidates/candidate-b-physical-time-rwkv-q0-seed42-v1`。C12-R 的 `c12-r-reference-replay-q0-s42` 持久会话仍在运行，`DREF` 已完成，两个后续变体并行回放，其余按队列继续，运行根为 `runs/candidates/c12-r-nondestructive-dual-reference-replay-q0-seed42-v1`。两项运行均为快速筛选证据且 `final_accessed=false`；新会话只读检查状态与日志，禁止重复启动。

## 数据与泄漏边界

- 冻结跨年度合同：LSPR23 是有标签源年度；LSPR24 依时间为前 `10%` 无标签目标前缀、随后 `70%` 可见开发区、最后 `20%` 最终封存区。字段、样本身份、历史预算、困难面板和评价协议以[数据合同](2026-08-11-LSPR23-LSPR24共同字段审计与跨年度数据合同.md)为准。
- 最终 `20%` 在方法、字段、阈值、困难面板、超参数、代码、种子和统计模板全部冻结前，禁止访问、持久化、统计、哈希、评价或作为模型输入；不得依最终结果改样本、字段、比例、基线能力、指标或阈值。
- 单种子 Q0 只用于筛选；冻结合同、共同预算基线、机制消融和多种子正式实验支持后，才可升级为论文证据。

## 服务器与运行前预检

- 当前远程连接只从环境变量 `GPU_SSH_C56` 与 `GPU_PWD_C56` 读取，严禁记录或回显值。A44 仅是历史制品来源。
- C56 开机、进程和磁盘均是可变事实。同步、物化或训练前先查询代理状态，再只读核验服务器身份、项目目录、数据、模型、依赖、GPU、磁盘、持久会话和原始日志；不得沿用旧 PID、运行目录或资源假设。

## 关键证据索引

- 当前路线状态、不可变研究边界和服务器纪律：[RWKV 路线总控](RWKV路线总控.md)。
- 候选状态、优先级和下一动作：[第三章候选方案登记册](2026-08-08-第三章候选方案登记册.md)；机制详情：[C1-C12 候选详情与历史证据索引](candidates/2026-08-11-C1-C12候选详情与历史证据索引.md)。
- 候选 B 设计与快速消融：[方案设计](candidates/候选B-连续时间频谱状态与有界信任融合-方案设计.md)、[快速消融](candidates/候选B-连续时间频谱状态与有界信任融合-快速消融.md)。
- 历史 C12 证据：[阶段 A 运行报告](2026-08-11-C12阶段A神经基线实现与运行报告.md)、[阶段 B 运行报告](2026-08-11-C12阶段B主机制实现与运行报告.md)。历史 R1 证据：[双层尾部风险运行报告](2026-08-11-R1双层尾部风险重构Q0实现与运行报告.md)。
