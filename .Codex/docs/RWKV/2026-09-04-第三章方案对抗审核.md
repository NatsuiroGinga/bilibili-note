# 第三章方案对抗审核：袋尺度不变聚合 与 跨时段分布鲁棒训练

<!-- RESEARCH_ROUTE=RWKV -->

- **日期**：2026-09-04（夜）
- **审核对象**：`thesis/methods/第三章-袋尺度不变聚合与跨时段分布鲁棒训练.md`（提交 `6d57a80`，含主代理自审 §五之二）
- **立场**：证伪主代理的自审结论，不复述它
- **硬约束执行情况**：服务器已下线，**未尝试连接**；**未在本机运行任何训练、张量运算或大规模扫描**；
  **未读取任何 LSPR24 数据**；唯一的计算类动作是本地文献混合索引的增量构建（`33.7` 秒，检索工具自动触发）
- **证据分级**：`【实测】`本课题真实运行制品；`【实测·转述】`真实运行，但制品在已回收服务器上、本机不可达，数字取自仓库报告或恢复卡；
  `【全文】`已读原件全文并核对页码或公式；`【摘要】`仅摘要或题录级，**不支撑任何论断**；`【推导】`本审核的数学或逻辑推理
- **分阶段落盘**：每答完一问即提交一次；未完成的节留显式占位，不空着也不编造

---

## TL;DR

> **放弃本方案。** 不进入 `writing-plans`，不写正文，不为任何一个构件创建运行身份；其中可复用的两件东西——分辨率门重定、训练量阶梯——降级为诊断，
> 按「B-1 配对 CI → B-2 实体键审计 → H3 条件漂移探针」的顺序重新立项，机制家族由 H3 的结果决定，不预设。

三条最强理由：

1. **两个核心构件都是再发明，且都带着不利实测躺在恢复链指向的文档里。** 机制一 = R1-M1「源时间环境 Group DRO」
   （2026-08-11 设计、实现、运行，`Q0不晋级`，GroupDRO 目标年 AP `0.012878` 对 ERM `0.012218`，环境探针 AUC 未降反升）；
   改造二 = GeNIS 线 `sagawa-small-group-adjusted`（以恶意稳定键数代入 Sagawa 式 (5)，2026-08-13 已跑、完整候选不晋级）。
   主代理自审的「零实验」「非重复」两条裁定都是错的。
2. **机制一的前提证据方向相反，且在协议下不可实施。** 「年内 KS ≥ 跨年 KS」在同一份文档里既用来否决 H1（这种漂移无害）又用来支持 DRO（这种漂移值得最坏化）；
   时间尾部逐流 AP `0.9999` 直接证明时段不是任务意义上的域，Group DRO 在该区退化为 ERM（Sagawa §3.1）；
   组调整在 `239` 个正实体上没有可用区间（`n_g⁺ = 0` 发散，不等则主导，相等则惰性）；
   DRO 必需的早停在封印协议下没有合法选轮信号。这不是「实验待证」，是「不宜立项」。
3. **方案赖以自审的分辨率门 `0.063` 量错了对象。** 它是 `n⁺ = 20` 外推 `37.6` 倍得到的**边际** CI 宽，却被用来判两个模型在同一批实体上的**配对**差。
   在配对自助 CI 算出来之前（CPU 分钟级，用已有分数），本方案 §5 的三条判据、以及今日「C10／C11 与 C00 不可分辨」的判读全部悬置。
   这一条不修，任何后续四格都判不了。

**附带一条正面结论**：崩塌发生在逐流打分器（源年 `0.9999` → LSPR24 `0.12–0.40`），实体级 `max` 聚合掩盖了它。
第二机制若不落在逐流表示或训练目标上，按构造过不了以实体 AP 为判据的门；聚合、排序、阈值三层都在下游。

---

## 〇、与主代理的分歧汇总（**这是重点**）

| # | 主代理的说法 | 本审核的说法 | 证据 | 见 |
| --- | --- | --- | --- | --- |
| 1 | 主张 5「机制一……本课题零实验」 | **错**。R1-M1 源时间环境 Group DRO 已设计、实现、运行，`Q0不晋级`，环境探针 AUC 未降 | `【实测·转述】`R1 报告 §9 | Q2 2.1、Q3 3.2 |
| 2 | 主张 6「仓库既有 Sagawa 笔记未覆盖式 (5)，此项非重复」 | **错**。GeNIS 证据卡 2026-08-12 逐式核验式 (5) 并判「已占」；GeNIS Q0 实现了以恶意稳定键数为支持量的 `sagawa-small-group-adjusted` | `【实测·转述】`证据卡 §4.1、Q0 计划第 20、40 行 | Q3 3.3 |
| 3 | 主张 2「改造二被扫描否决」 | 扫描只否决打分侧；训练侧比例 `k` 已测（C10 vs C00 `+0.025`）；固定 `k` 未测。状态应为「弱化」 | `【实测·转述】`〇之二点二 B、E2 | Q1 1.2 |
| 4 | 判据 1「差异 `< 0.063` 一律不可分辨」 | **量错对象**：`0.063` 是 `n⁺=20` 外推的**边际** CI 宽，被拿来判**配对**差；且是 `37.6` 倍外推非实测 | `【推导】` | Q1 1.4 |
| 5 | 「年内 KS `0.3869` ≥ 跨年 `0.3562` ⇒ Group DRO 有作用面」 | 同一证据在 §一 用于否决 H1，含义是「这种漂移无害」；片对片 vs 年对年粒度不同；时间尾部逐流 AP `0.9999` 直接证明时段不是域 | `【实测·转述】`2.0 节事实 2；`【全文】`Sagawa §3.1 | Q2 2.1 |
| 6 | 「评价方差由正例数决定 ⇒ 组调整用 `n⁺`，且该更换有自己的证据」 | 混同了指标抽样方差与训练损失泛化差距；至多是启发式 | `【全文】`Sagawa §3.3 | Q2 2.5 |
| 7 | B2「`m` 未定」 | 不止未定：`n_g⁺ = 0` 发散、不等则调整项主导、相等则调整项惰性；可行 `m ∈ {2,3,4}` 且 R1 已用 `4`；组按时段而风险按实体袋，单位冲突 | `【推导】`＋`【全文】`Dijk 2024 演习相位 | Q2 2.2、2.3 |
| 8 | B7「选轮规则未定」 | 不是待办，是阻断：封印协议下没有合法早停信号；训练量阶梯读 LSPR24 即 oracle | 主代理自己的 2.0 边界 | Q2 2.4 |
| 9 | 「`p = 0.0128`，本日唯一带统计功效的结论」 | `33` 个算子是同一批分数的参数化变体，非可交换样本；`p` 不能按字面读 | `【推导】` | Q1 1.4 附 |
| 10 | 方案默默弃掉 BER | 与〇之二点三「BER 不判死」和 08-13「单一机制可以不构成创新点」冲突，未记录裁决 | 三份文档 | Q4 4.1 |
| 11 | 「先有失效面再找家族」 | 失效面只能在 LSPR24 上观测，与 2.0「禁止在 LSPR24 上搜机制」互斥；且已有一条失效面（逐流 AP 崩塌）没被使用 | `【实测·转述】` | Q5 5.1、5.4 |
| 12 | 候选家族表含保形／阈值类 | 按构造改不了 AP，R1-M2 已实测；不能作第三章 AP 机制 | `【实测·转述】`R1 报告 §9 | Q5 5.5 |

**没有分歧的地方**：主张 1、3、7、8 的删除；主张 4 的弱化；「打分侧聚合不是杠杆」；「本方案不得进入 `writing-plans`」。

---

## 一、Q1｜自审是否删多了

**总裁定：结果上没有删多，但两处裁定的依据写错，一处判据本身有方法学错误；后者影响的不止本方案，而是此后所有四格。**

### 1.1 八条裁定逐条复核

| # | 主代理裁定 | 本审核裁定 | 依据 |
| --- | --- | --- | --- |
| 1 | 改造一（softmax 温度插值）删除 | **同意** | `【全文】`AutoPool 式 (8) 与 Prop. 1（`wiki/papers/methodology/multiple-instance/2018-McFee-AutoPool自适应池化.md`），且袋中位 `2` 落在 Prop. 1 的退化区 |
| 2 | 改造二（固定 `k` vs 比例 `k`）删除，依据是「扫描否决」 | **结果可维持，依据须改写**（见 1.2） | 扫描只测打分侧；训练侧比例 `k` 已有 LSPR24 读数；训练侧固定 `k` 未测 |
| 3 | 改造三（`τ` 由源年定）删除 | **同意** | 随 1 作废 |
| 4 | 袋尺度是失效面 → 弱化为「现象成立、机理未证」 | **同意，且须再加一层**：H4 的「正实体袋 `112→15`」在 H6（两年实体键口径）未审前，连「现象成立」都不能定 | 见 Q5 |
| 5 | 机制一（跨时段 Group DRO）实验待证，「本课题零实验」 | **不同意「零实验」**：本仓库已跑过同类候选并判不晋级 | 见 Q2、Q3 |
| 6 | 改造二（`C/√n⁺`）设计可行、非重复 | **不同意「非重复」**：本仓库 GeNIS 线已实现「正例键数作支持量」的 Sagawa 式 (5) 单元 | 见 Q3 |
| 7 | §4.2 联合论证删除 | **同意**；两种可能的「机制一内含两部件」表述都救不回来 | 见 1.3 |
| 8 | §5 四格失效 | **同意** | 维度之一已空 |

### 1.2 主张 2：否决的范围被说宽了

主代理自己在 §2.4 承认「本实验只测『给 `max` 训练出的模型换算子』，未测『用不同算子训练』」，
但自审表仍把主张 2 记为「`【实测】`扫描否决」。三条事实：

1. `【实测·转述】`打分侧：`33` 个算子目标年跨度 `0.0069`（`runs/diagnostics/_pool_sweep/pool_sweep.json`）。**这一层否决成立。**
2. `【实测·转述】`训练侧、比例 `k`：ETA 本身就是训练侧的比例 top-`k`（`ch3_ft_entity_ranking_loss.py:245`，`k_e = ⌈α·m_e⌉`）。
   新四格 C10-entitybce 对 C00 的跨年差为 `+0.025`（恢复卡〇之二点二 B 节），主代理判「落在噪声内」。
   **即训练侧比例 `k` 已经测过，结论是不可分辨，不是「未测」。**
3. 训练侧、固定 `k`：**未测**。先验弱（`【实测】`袋 P90 `= 9`，Peters 的 `k = 12` 对 `≥ 90%` 实体退化为全袋均值），
   且 Peters 的 `12` 取自**其**数据集平均袋大小，无迁移依据。

正确写法：「打分侧实测否决；训练侧比例 `k` 已测、与 C00 不可分辨（分辨率门本身待重定，见 1.4）；训练侧固定 `k` 未测、先验弱、不列为本章机制」。
状态是**弱化为低优先级待证**，不是「被扫描否决」。这处差异不改变本方案的命运，但改变台账的真实性。

### 1.3 主张 7：机制一内部能否拆出两个可分离部件

审题要求核查「组权重更新」与「组调整」能否构成机制一内部的两部件，使联合论证不依赖机制二。两种拆法都不成立：

- **拆法 A：`q` 指数上升（DRO 本体）× `C/√n_g⁺` 组调整。** `【全文】`Sagawa 原文 §3.3 明确写道：
  该 `1/√n` 代理「只在 group DRO 设定下起作用……在 ERM 设定下不适用；若最小化平均训练损失，`1/√n` 项只是常数，不影响优化」
  （`wiki/papers/methodology/2020-Sagawa-GroupDRO-全文.md` 第 133 行）。
  即组调整在「DRO 关」的两格里**按构造惰性**。这与本仓库 2026-09-02 已诊断的四格不对称问题同型
  （`thesis/methods/四格设计的不对称与改法.md` 第一节：一个机制只在另一机制开启时才有训练通道，交互项必然为正、无判别力）。
  **主代理若走这条路，等于把刚被 2026-09-03 用户裁决修掉的结构缺陷再造一次。**
- **拆法 B：强正则／早停 × DRO。** 两者在 ERM 上各自独立有效，交互项也真实存在——
  `【全文】`但这正是 Sagawa 论文的**中心结论**（表 1：无强正则时 DRO 与 ERM 最坏组准确率相同，强正则下 DRO 高出 `21.3→84.6`、`37.8→86.7`）。
  以此作第三章的两部件，是在新数据上复现 Sagawa 表 1，不构成差量；且早停需要选轮信号，封印协议下没有（见 Q2 2.4）。

故删除 §4.2 正确。**没有「删多了」。**

### 1.4 分辨率门 `0.063`：一个影响此后所有四格的方法学错误（**这是本节最重要的发现**）

主代理把「LSPR24 实体 AP 的分辨率 `0.063`」当作判据 1，凡差异小于它一律「不可分辨」。该数的来路与用法各有一处问题：

**来路**：`【实测·转述】`源年验证 `20` 个正实体的自助 `95%` CI 宽 `0.386`（`_h5_ci/h5_ci.json`），
按 `1/√n⁺` 外推 `0.386 × √(20/752) = 0.0629`（`【推导】`本审核复算，与卡上一致）。
它是从 `n⁺ = 20` 到 `752` 的 **`37.6` 倍外推**，不是 LSPR24 上的实测；AP 的 CI 宽并不严格按 `1/√n⁺` 缩放（依赖分数分布形状与负例数）。

**用法**：`0.386` 是**单个模型 AP 的边际 CI 宽**；判据 1 拿它去判**两个模型在同一批实体上的配对差**。
`【推导】``Var(AP_A − AP_B) = Var(AP_A) + Var(AP_B) − 2·Cov(AP_A, AP_B)`；同骨干、只换聚合或只加一项损失的两个模型，
其实体排序高度相关，协方差项大，配对差的 CI 通常远窄于边际 CI。**用边际宽度当配对差的门槛，既不是保守也不是激进，是量错了对象。**

**后果**：

- 「C11 vs C00 `−0.039` 不可分辨」「C10 vs C00 `+0.025` 不可分辨」两条判读可能错，方向未知；
- 「`33` 个算子跨度 `0.0069` 比分辨率低一个数量级」的论证形式不成立（`0.0069` 很可能仍在配对噪声内，但**必须测出来**，不能用外推数判）；
- 更严重的是前瞻效应：`C00 = 0.761`，判据 2 要求单机制跨年增益 `≥ 0.063`，即 `≥ 0.824`。**这个门槛把一切现实量级的机制预先判死**，
  不是因为机制无效，而是因为门槛量错了对象。

**修法零成本且不碰 LSPR24 前向**：`_pool_sweep/scores.npz` 与四格评价收据里已有 LSPR24 逐流分数，
对实体做配对自助（或实体级置换检验）直接得到 `ΔAP` 的 CI——CPU 分钟级，服务器恢复后第一件事。
**在此之前，本方案 §5 的判据 1、2、3 全部悬置**；这条进阻断项清单。

**附带一条**：§2.3「结论二」的置换检验 `p = 0.0128` 建立在 `n = 33` 个算子上，
而这 `33` 个算子是同一批分数上的参数化变体（六档比例 `k`、十档固定 `k`、十个温度、五个分位），彼此高度相依，
不是可交换样本。有效样本量远小于 `33`，`p` 值不能按字面读。它仍是本日最有信息量的观察，但「本日唯一带统计功效的结论」这句要收回。

---

## 二、Q2｜自审是否删少了

**总裁定：删少了。机制一的前提证据方向相反、既有筛选证据不利、组调整在本数据上没有可用区间、早停在封印协议下无合法信号——
这四条中任一条都足以把「实验待证」改成「先验不利、不宜立项」。主代理把它们全部漏了。**

### 2.1 前提「年内 KS `0.3869` ≥ 跨年 `0.3562`」不支持「时段是有意义的域划分」，反而否定它

主代理在 §一 用这条否决 H1（边缘漂移不是崩塌成因），又在 §3.2 改造一用**同一条**证明「年内变异充足，Group DRO 有作用面」。
两用是矛盾的，理由如下：

1. **逻辑**：H1 的否决式是「年内边缘漂移与跨年一样大，而年内性能不崩」。这句话的直接含义是：**这种量级的边缘漂移是模型已经容忍的那类变化**。
   Group DRO 的不确定集是各组分布的凸包（`【全文】`Sagawa 式 (2)–(3)），它保护的正是「组间那种差异」。
   组间差异既然已被证明不致害，对它做最坏组优化就是在保护一个不需要保护的方向。
   **同一份证据不能既证明「时段差异无害」，又证明「时段差异值得作最坏情形优化」。**
2. **粒度**：`【推导】`「年内相邻时段 KS」是**片对片**，「跨年 KS」是**整年对整年**——后者是两个混合分布之间的距离，混合会机械地拉低 KS。
   两个数不在同一粒度上，`1.086` 的比值是苹果比橘子。公平的比法是同相位片对片（`slice_i(2023)` vs `slice_i(2024)`），制品本机不可达，无法复核，
   但**判据的构造本身已经决定了它偏向「年内 ≥ 跨年」**。
3. **直接证据**：`【实测·转述】`恢复卡 2.0 节事实 2——LSPR23 时间尾部（`time_tail_fraction = 0.15`，四个臂从未见过）上
   **逐流 AP 全为 `0.9999`**，而 LSPR24 仅 `0.12–0.40`；主代理自己的结论是「尾部不是漂移代理，是同分布的更多数据」。
   这就是「时段不是任务意义上的域」的直接实测：**在最晚的时段上，早期时段训练的模型没有任何泛化间隙。**
   `【全文】`Sagawa §3.1：平均训练损失趋零则最坏组训练损失也趋零，DRO 与 ERM 无差别——本课题 C00 源年逐流 AP `0.9999` 正处于该区。
4. **既有实测**：`【实测·转述】`R1 Q0（2026-08-11，`runs/candidates/r1-source-time-environment-tail-risk-q0-seed42-v1`）：
   四个源时间环境（按 2-IP 父组首次出现时刻的四分位切、父组不跨环境）上，GroupDRO 目标年 AP `0.012878` 对 ERM `0.012218`，
   环境探针宏 AUC `0.7818 → 0.7929`（**未降反升**，门槛要求降 `0.05`），裁决 `Q0不晋级`
   （`.Codex/docs/RWKV/archive/路线选型与数据基础设施/2026-08-11-R1双层尾部风险重构Q0实现与运行报告.md` §9）。
   它是筛选级、RWKV 式状态头、逐流单位，与本方案不同骨干不同单位，**但它是本仓库唯一一次真实的「按时段 Group DRO」，方向不利**。

**什么才算「有意义的域划分」**：(a) 组间差异必须落在跨年失效的那条机理上；(b) 最坏组训练损失必须对目标损失有信息量。
本仓库一个月前就写好了对应的最小证伪设计——`wiki/papers/methodology/2020-Sagawa-GroupDRO群体移位.md`「最小证伪实验」：
**「另加『打乱时间环境标签』对照；若真实时间群体不优于打乱群体……则否决该环境映射」**。
本方案 §4.3 的证伪判据（`q` 与袋大小的秩相关）根本不检验「时段分组是否有意义」这一前提；
**打乱时段对照**才是该问的第一个问题，而它已经在库里，主代理没用。

### 2.2 `C/√n_g⁺` 的发散与临界组数

`【实测·转述】`完整源区 `239` 个正实体（`.Codex/docs/RWKV/2026-08-27-D0验收与D1D2病灶诊断报告.md` 第 62 行：三折 `80+80+79`），
源年验证占 `20`（或 `25`，两份文档不一致，见恢复卡与定案页），训练侧约 `215`。

`【推导】`

- **发散点**：`n_g⁺ = 0 ⇒ δ̂_g = +∞`。该组永远是 `argmax`，`q` 坍缩到一个**没有正实体的时段**上，
  训练信号退化为「该时段全良性」。`【全文】`Dijk 2024（`wiki/papers/datasets/LSPR24/Dijk-2024-LSPR23数据集与随机森林复现.md`）：
  LSPR23 净跨度 `28.4` 小时，且「演习关闭约 `6` 小时期间未采集，但该时段没有攻击活动」——Locked Shields 是分相位的两天演习，
  攻击在时间上成簇。**等时长切分只要 `m ≥ 5`（每片 `≈ 5.7` 小时）就有相当概率出现零正实体片**；
  R1 之所以没炸，是因为它按父组首次出现时刻的**四分位**（等计数）切，并在代码里显式 `raise`「时间块划分产生空环境」
  （`src/flow_probe/r1_source_environment.py:347`）。本方案对此只字未提。
- **非发散但失效的区间**：`n_g⁺ ∈ [1, 100]` 时 `δ̂_g` 跨 `10` 倍。`argmax` 由调整项而非损失决定，除非 `C` 小到调整项惰性。
  两头都没有机制作用面。唯一能让 `δ̂_g` 处于「既不主导也不惰性」的办法是**各组 `n_g⁺` 大致相等**——
  而那意味着按正实体分位切组，此时 `δ̂_g` 对所有组相同，**调整项退化为常数，改造二自我抵消**。
- **临界组数**（均匀正实体、训练侧 `215`）：按恢复卡自己的噪声底 `n⁺ ≥ 20`，`m ≤ 10`；
  按 Sagawa 最小组 `56`（Waterbirds），`m ≤ 3`。**可行区间 `m ∈ {2, 3, 4}`，而 R1 已经用过 `m = 4`。**
  真实的逐时段正实体数**本审核未算**（不在本机做数据扫描）；R1 的 `environment-manifest.json` 与
  `environment-label-sequence-sampling` 收据记录了每环境×标签的池大小（`r1_source_environment.py:448-459`），服务器恢复后直接读，零计算。

### 2.3 组单位与风险单位冲突（方案未提的结构缺陷）

改造四说「风险的单位是实体袋」，改造一说「组按时段定义」。`【实测·转述】`恶意实体流链中位 `112` 条、最大袋 `2,475,228` 条，
一个正实体跨越多个时段是常态。Group DRO 要求训练单位被组**划分**；实体属于哪个时段组是未定义的。
R1 的处理是整个 2-IP 父组归入首次出现的时间块（候选 C 方案设计 §3）；本方案 B2 只写「`m` 未定」，没意识到单位冲突。
且承载 `94.4%` 正例流的头部 `0.1%` 实体无论归哪个组都会主导该组损失——组结构会被少数巨袋决定，与「时段」无关。

### 2.4 「正则或早停是前提」在封印协议下不可实施——B7 不是待办，是阻断

改造三承认 DRO 必须配强正则或早停；`【全文】`Sagawa 表 3 的所有 DRO 结果都按**最坏组验证准确率**选轮与选 `λ`。
本课题：`【实测·转述】`源年验证只有 `20` 个正实体（CI 宽 `0.386`），四格上源年与跨年的秩相关 `ρ = −1`，
主代理 2.0 节自定边界「禁止再用源年读数选机制」「禁止把机制搜索建立在 LSPR24 上」。
于是：用源年选早停点＝噪声；用 LSPR24 选＝Gulrajani 所说的 oracle。**两条路都被主代理自己封了。**
正在跑的「训练量阶梯」（§三之二）判据是「跨年实体 AP 随轮数单调下降」——它读的是 LSPR24，
据其选出的轮数就是 oracle 选择，与 2.0 节边界直接冲突。**这一冲突在方案里没有被指出。**

### 2.5 「评价方差由正例数决定 ⇒ 组调整用 `n⁺`」混同了两个量

`【全文】`Sagawa 式 (5) 的 `δ_g` 是**组训练损失的泛化差距**，`1/√n_g` 来自复杂度型泛化界对样本量的依赖（原文第 131 行引 Cao 2019）。
主代理的支撑证据是**实体 AP 这个评价指标**的自助 CI 随 `n⁺` 缩放。前者关于损失（BCE／排序损失，负例同样贡献），
后者关于指标的抽样方差。两者的 `1/√n` 只是形式相同。**「该更换有自己的证据」这句不成立**；它至多是一个可试的启发式。

### 2.6 Q2 裁定

主张 5 应改为：「**实验待证；前提证据方向相反（2.1）；既有筛选证据不利（R1 Q0）；在封印协议下缺少可实施的早停信号（2.4）**」。
主张 6 应改为：「**本仓库已实现近似变体（见 Q3），证据链混同评价方差与泛化差距（2.5），在本数据上没有可用区间（2.2）**」。
两条都不是「删多了」，是「删少了」。

---

## 三、Q3｜是否还有第三次重复发明

**总裁定：有，而且不止一次。机制一是本仓库 R1-M1「源时间环境 Group DRO」的再发明；改造二「用正例数做组调整」是 GeNIS 线
`sagawa-small-group-adjusted` 单元的再发明。两者都在恢复链指向的文档里，且都带着不利的实测结果。**

### 3.1 检索记录（按强制顺序）

| 层 | 工具与查询 | 结果 |
| --- | --- | --- |
| 本地混合索引 | `literature_search query "按时间环境分组的 group DRO 分布鲁棒优化 跨年度 入侵检测 恶意流量" --scope all --mode hybrid`（索引触发增量构建 `33.7` 秒后 `stale=false`，向量与词法通道均运行） | 前 10：Duchi 2021 笔记（「不替代 GroupDRO 的显式时间环境」）、**Sagawa 笔记「最小证伪实验：打乱时间环境标签」**、DORO、**FOIL 笔记「候选 C 将时间环境推断迁移到跨年度 NIDS」**、SOPA、Levy 2020；`reports`／`plans` 集合命中 E2「仅源域 GroupDRO」实施计划 |
| 本地混合索引 | `"Group DRO 组调整 C/sqrt(n_g) 正例数 恶意稳定键 支持量收缩 小组泛化差距 Sagawa 式(5)" --scope all` | 前 2：Sagawa 笔记 2026-09-04 补读的式 (5) 段、Sagawa 全文 §3.3；`research-notes` 命中 **GeNIS 核心理论公式证据卡**（2026-08-12） |
| 仓库 `rg` | `group ?dro\|候选C\|时间环境\|时段` | R1 方案设计、R1 两份实现报告、`r1_source_environment.py`、`r1_tail_risk.py`、两份 R1 配置与启动器、2026-08-11 理论融合综述、2026-08-17 归档第 8 条、GeNIS 三份文档 |
| Zotero 语义检索 | 两条查询（时段 Group DRO × NIDS；正例数组调整） | 无直接命中；最近的是 Cantone 2024 跨数据集泛化、Luay 2025 NetFlow 时间分析（`CUX69W2P`）、DIVERSIFY；`Wild-Time`、`TDRO` **均不在库** |
| 在线 | `discover_papers` ×2、`WebSearch` ×2 | `【摘要】`Wild-Time（Yao 等，NeurIPS 2022 D&B）定义了 **GroupDRO-T：把时间子流当作组**；`【摘要】`TDRO（arXiv:2312.09901）把时间段 × Group DRO 用于推荐冷启动；`【摘要】`Per-GDRO／组级不确定 GDRO（arXiv:2509.08942）；**未检索到 NIDS 或恶意流量上按时段分组的 Group DRO**；**未检索到任何「按正例数缩放组调整」的工作**（WebSearch 明确零命中） |

### 3.2 第一问：「按时段分组的 Group DRO 用于跨年泛化」

**已存在于本仓库，且已实测**（`【实测·转述】`）：

- **设计**：`.Codex/docs/RWKV/archive/路线选型与数据基础设施/candidates/候选C-源域时间环境稳健状态与最坏环境告警校准-方案设计.md`
  §3「在 LSPR23 训练区按时间连续划分 `E ≥ 4` 个环境……2-IP 父组不跨边界」；§10.1「外层 GroupDRO 最小化最坏源时间环境……环境内 CVaR」。
  `2026-08-11-跨年度任务非RWKV理论融合文献综述.md` §3.1 同。
- **实现**：`src/flow_probe/r1_source_environment.py`（`_time_blocks`、环境×标签均衡采样、空环境 `raise`）、`src/flow_probe/r1_tail_risk.py`
  （`ERM/GROUPDRO/VREX/CVAR/TAILRISK` 五变体）。
- **运行与裁决**：见 2.1 第 4 条；`R1 Q0不晋级并转向候选B`。同一时期 E2 线（2026-08-06）也跑过「仅源域 GroupDRO（捕获组）」，
  宏 F1 与最差组 F1 均低于 HGB／XGBoost／RF／MLP（`.Codex/docs/2026-08-06-E2种子42强基线首轮结果.md` 第 25 行）。
- **文献侧**：Sagawa 笔记首句即「本文是候选 C 训练期 M1 的直接理论来源」。

**已发表通用先例**（`【摘要】`，须下载全文并入库后才能写进任何文档）：Wild-Time 的 GroupDRO-T 就是「时间子流 = 组」。
本方案的「时段组」不是新划分，是 Wild-Time 已命名的基线变体搬到 LSPR。NIDS 特化版本未检索到——**这是「未发现」，不是「不存在」**。

**裁定**：机制一 = R1-M1 第三次再发明（本日第六次同型事故）。差异只有骨干（FT vs RWKV 式状态头）、单位（实体 vs 流）与调整项。
若要复活，须先解释为什么这三处差异会翻转 R1 的方向——方案里没有这一段。

### 3.3 第二问：「用正例数而非样本数做组调整」

**已存在于本仓库**（`【实测·转述】`）：

- `.Codex/docs/2026-08-12-GeNIS跨场景泛化与固定告警预算/子代理-核心理论公式证据卡.md` §4.1 逐式核验了 Sagawa 式 (5)
  （物理页 6），并裁定「`C/√n_g` 支持量修正已被该论文占用」「若直接增加 `C/√n_g`，就回到 Sagawa 已占机制」（第 81、405 行）。
  **主代理自审说「仓库既有 Sagawa 笔记未覆盖式 (5)」——`wiki/` 那份笔记确实没覆盖，但证据卡覆盖了，且早了三周。**
- `.Codex/docs/2026-08-13-GeNIS支持量收缩跨场景极端尾部排序-Q0实施计划.md`：「独立支持量 `n_g` 是该场景去重后的**恶意稳定键数**」（第 20 行）；
  方法 4 `sagawa-small-group-adjusted`：「按 `R_g + C/sqrt(n_g)` 选择最坏场景，`C = sqrt(median_g(n_g))`」（第 40 行）。
  **这就是「用正例单元数代入式 (5)」**——与本方案改造二只差「正实体」与「恶意稳定键」的命名。
  另有 `support-shrunk`（`w_g = n_g/(n_g+λ_s)`）用同一正例支持量做收缩。
- 运行：`genis-60s-support-shrunk-extreme-tail-ranking-q0-seed42-v1`，`112/112` 完成，完整候选 `candidate_advanced=false`
  （`2026-08-13-GeNIS支持量收缩跨场景极端尾部排序-Q0实验结果报告.md`）。
  **占位**：`sagawa-small-group-adjusted` 单元的逐场景读数未写入该报告；本机存有
  `thesis/experiments/genis/runs/q0/genis-60s-support-shrunk-extreme-tail-ranking-q0-seed42-v1/outer_units/scene-{7,8}/*/cell-sagawa-small-group-adjusted.json`，
  本审核未读取（对裁定非必要）。若要引用其数字，先读这些文件。

**文献侧**：Zotero 与在线均零命中。**不能据此断言文献中不存在**；只能说「本轮三级检索未发现」。

**裁定**：改造二是 GeNIS 线的再发明；且 GeNIS 证据卡在 2026-08-12 就写明了它不能作原创点。

### 3.4 顺带核出的一处近邻（非重复）

`thesis/methods/机制一候选-尺度自适应软聚合与c课程调度.md`（2026-09-02）提出「尺度自适应软聚合」（`τ_e = c₀/σ_e` 的 LSE，正仿射等变）。
它的「尺度」是 logit 尺度，本方案的「尺度」是袋大小，**不是同一件事**，不算重复；但两者都属「连续可调聚合」，
且都已在本仓库被否决或搁置，写方案时应互相引用。

---

## 四、Q4｜单机制方案能否达到对标标准

**总裁定：结构不是阻断，证据才是。「单机制 ＋ 多骨干横向 ＋ 消融」在工作量与理论深度上可以做到与参照论文同级，
但它与一条仍然有效的用户裁决冲突，且当前没有任何一个机制在跨年上为正——没有正结果的单机制章不是「单机制」问题，是「无结果」问题。**

### 4.1 仓库里关于对标的既有裁决（按时间）

| 日期 | 裁决 | 出处 | 对单机制路线的含义 |
| --- | --- | --- | --- |
| 2026-08-13 | 用户：「**第三章的创新点由两个机制结合构成，单一机制可以不构成创新点**」「不用追求完全的机制新颖」 | `.Codex/docs/RWKV/archive/第三章-MLP线/2026-08-13-第三章双机制组合创新的论证结构.md` 头部 | **直接反对**；该裁决未被撤销 |
| 长期 | 范文只可提取「单章单问题、**章内机制协同**、理论与实验共同支撑」等组织模式及工作量尺度 | `thesis/AGENTS.md` 论文边界 | 「章内机制协同」被列为要提取的组织模式 |
| 长期 | 朱论文对位表须逐项列「研究问题拆分、**双模块或互补维度组织**、骨干专属输入适配、模块挂载、对照与消融工作量、指标、结果表述量级、不可迁移边界」 | `thesis/experiments/llm_probe/AGENTS.md`「朱焱雷第三章第一参照」 | 八项里两项（双模块、模块挂载）在单机制下无对位对象 |
| 长期 | 「第三、四章须在同一主线、任务和评价协议下形成**可由对照与消融验证的机制增量**」 | 根 `AGENTS.md` | 只要求增量可验证，**不要求两个机制** |
| 记忆 | 「朱论文是标准上限：定稿只按参照论文同级标准，不加码；**对标等级不照抄结构**」 | 用户记忆 `zhu-thesis-is-the-standard-ceiling` | 允许结构不同、等级相同 |
| 2026-09-01 | 写作定位：与参照论文同一种做法，**组合已有方法，差量落在任务化组织**（四项差量） | `2026-09-01-第三章公式来源与改造实质盘点.md`（`codex/ch3-cpa-elp-20260820` 分支，`git show` 只读核对） | 差量不在公式，在组织；两机制或一机制都可承载 |
| 2026-09-04 | 对位表如实列出差距：**骨干 `1` vs `2`、本地重跑外部基线 `0` vs `6`、消融变体运行 `4` vs `8`**；「若要类比朱的表述量级，必须先补足同等量级的外部基线复现或至少一个额外骨干」 | `thesis/methods/第三章新四格-朱论文对位表.md` §五、§九 | 差距主要在横向（骨干与基线），不在纵向 |
| 2026-09-04 | 叙事定位：「本章不讲『两机制协同』……若不通过：本章为 **BER 单机制**，ETA 作为消融中的一个对照条目」 | `thesis/methods/第三章-叙事定位与创新性论证.md` §四 | 主代理已在同日另一份文档里预设了单机制退路，但没引 08-13 裁决 |

**三份同日文档互不一致**：叙事定位保留 BER 作单机制；恢复卡 〇之二点三「BER 不判死」；被审方案把 BER 整个拿掉、换成两个新机制；
2.0 GOAL 又说「不是给某个已有机制找联合伙伴」。这四句话不能同时成立，**需要用户一句话定案**，进阻断项。

### 4.2 支持「单机制＋多骨干横向＋消融」的理由

参照论文的工作量是 `2` 骨干 × `2×2` 变体 `= 8` 次运行，外加 `6` 个本地重跑基线；它的「异构骨干普适性」维度（表 3-5、3-6）是本课题目前完全缺失的一块。
单机制路线恰好把工作量补在这块：`3` 个骨干 × {开, 关} `= 6` 次运行，外加已发表方法池的 `5` 个目标年读数（`【实测·转述】`XGBoost `0.5129`、CNN `0.4096`、全注意力 `0.3536`、RF `0.2605`、GRU `0.1600`）。
运行数量级与朱相当，横向覆盖反而更接近朱。理论深度方面，叙事定位 §五记录 3.2–3.5 四节已按单机制改写完（提交 `9afd179→dbbd478`），
3.5 的四小节（CVaR-pAUC 性质、`α ≤ 1/2` 的充要论证、六档预算投影、封印协议）与朱 3.5 的四小节在体量上对位；
公式来源盘点已把「可以写／不可以写」钉死，单机制不会比两机制少任何一条可写主张。
根 `AGENTS.md` 的硬要求只是「可由对照与消融验证的机制增量」，单机制 × 骨干 × 开关的消融矩阵完全满足。
最后一条是诚实性：两机制联合在本课题已被 `7` 个方案的实测排除（`thesis/methods/第三章-联合框架提案台账.md`），
为凑第二个机制，主代理一天之内六次重新发明已有成果——**继续凑的边际代价已经高于收益**。

### 4.3 反对的理由

用户 08-13 的裁决没有被撤销，主代理无权单方面改为单机制；任何单机制路线都必须先拿到一句新的用户裁决并写进恢复卡，否则违反「用户知情后的最终决定仍应执行」。
体例上，朱的章级骨架是「问题拆两个子问题 → 两个模块 → `2×2` 超可加」，对位表八项里「双模块或互补维度组织」「模块挂载」在单机制下没有对位对象，
而第四章（EAS／AIS 两机制）仍按双机制写，两章体例不一致会被评审直接看出。
更实质的是：多骨干横向的前提是各骨干在 LSPR24 上的读数可分辨，按现行分辨率门几乎全部不可分辨（见 1.4）；
方法池 `5` 个成员的目标年读数（`0.16–0.51`）远低于 FT C00 的 `0.761`，横向表会变成「骨干差异」表而非「机制差异」表。
而最致命的一条与结构无关：**当前没有任何机制在跨年上为正**——BER `−0.336`（可分辨）、ETA `+0.025`（待配对 CI 复判）、时段 DRO 未测且先验不利。
一个单机制章需要那个机制有正结果；两机制章需要两个。**眼下的短缺是一个都没有。**

### 4.4 Q4 裁定

- 「单机制 ＋ 多骨干横向 ＋ 消融」**在工作量与理论深度上可达对标标准**（横向补纵向），**前提**是：
  (a) 用户撤销或修订 08-13 裁决并记入恢复卡；(b) 有一个跨年为正、且在配对自助 CI 下可分辨的机制；(c) 分辨率门先重定。
- (b) 现在不成立。所以 Q4 的答案不由 Q4 决定，由 Q1.4（门）与 Q5（失效面）决定。
- 主代理自审给出的「路一／路二」二选一是假选项：路一缺正结果，路二缺失效面。**两条路共同的前置是 Q5。**

---

## 五、Q5｜有没有更好的第二机制方向

**总裁定：H6 是有效性门，必须先关，但它给不出失效面；H3 是最可能给出**可区分家族**的失效面的假设。
另外有一条已实测、主代理没当失效面用的证据：崩塌发生在逐流打分器，实体级 `max` 聚合把它掩盖了——
这直接排除了一切作用在聚合、排序、阈值层的第二机制，把搜索范围压到逐流表示与训练目标上。**

### 5.1 已有的失效面证据（主代理放着没用）

`【实测·转述】`恢复卡 2.0 节事实 2：源年逐流 AP `0.9999`，时间尾部（从未训练）仍 `0.9999`；LSPR24 逐流 AP 仅 `0.12–0.40`；
而 C00 的 LSPR24 **实体** AP 为 `0.761`。`【全文】`Dijk 2026 表 3：LSPR24 **同年** XGBoost 逐流 AP `0.9923`
（`wiki/papers/datasets/LSPR24/Dijk-2026-LSPR23到LSPR25序列构造跨年评估.md`）。

三条推论（`【推导】`）：

1. LSPR24 的 `P(Y|X)` 用同一批 `83` 字段是可学的（同年 `0.99`），失效不是特征不够，是**映射或支撑变了**。
2. 逐流打分器在 LSPR24 上接近失效（`0.12–0.40`），实体 AP `0.76` 靠的是 `max` 聚合加上正实体流多——只要袋里有一条流被打高，实体就被救回。
   **聚合层是在给一个已坏的打分器补漏，不是失效面本身。** `33` 个算子跨度 `0.0069` 是这一事实的必然结果，不是新发现。
3. 于是 ETA、BER、袋尺度不变聚合、保形阈值（恢复卡〇之二点三候选表里的 Transcendent 家族）全部作用在打分器**下游**。
   `【实测·转述】`R1-M2 早已给出同型教训：「NP 哨兵的 AP 和严格全局前 K 召回与原始概率逐项一致」——阈值类机制按构造改不了 AP。
   **第二机制若不落在逐流打分器上，按构造过不了以实体 AP 为判据的门。**

### 5.2 H6：先关的有效性门（不是失效面）

**代码事实**（本审核读码）：

- LSPR24 实体在评价阶段**内联构造**：`ch3_ft_transformer_field_token_protocol_a.py:4931-4938` 以 `s24|d24` 字符串取词典序无向对再 `np.unique`；
  `ch3_ft_lspr24_descriptive_eval.py:497-515` 逐字复制同一逻辑。
- LSPR23 实体直接读**预物化**的 `E23.npy`（同文件 `:3013`），源年缓存只有 `X23/y23/I23/M23/E23/T23`，**没有地址数组**；
  `E23` 的写出代码在本工作树 `tools/dijk2026_replication/` 内**找不到**（`rg 'np\.save|\.npy'` 零命中）。
- 结论：**两年实体键出自两条代码路径，口径同一性无法从代码证明。** H6 存疑成立。

**旁证**：`【全文】`Dijk 2026 表 8 的 LSPR24 2-IP 训练序列数 `160,660`（每序列 `≤ 128` 流）与 `47,115` 实体、均袋 `429` 相容
（`≈ 3.4` 序列／实体）。LSPR24 一侧的计数与论文自己的 2-IP 构造一致，**LSPR23 一侧无此旁证**。

**最小实验（服务器 CPU，无 GPU，不读任何标签）**：

| 项 | 内容 |
| --- | --- |
| 输入 | `raw/datasets/LSPR23/ls23pr_flows.zip`（主工作树，`1.79 GB`）的源／目的地址列；缓存 `E23.npy`；LSPR24 的 `s24/d24`（只取地址格式统计，评价工具已读过） |
| 动作 | 用与 `:4931-4938` **逐字相同**的函数重建 `E23′`；检查 `E23 ↔ E23′` 是否一一对应（每个 `E23` 编号恰映射到一个 `E23′` 编号，反之亦然）；统计两年地址词表：唯一地址数、IPv6 占比、空值／`nan` 占比 |
| 通过 | 双射成立且实体数 `= 150,680`；两年地址格式分布同型 |
| 失败 | 任一不成立 ⇒ H4 的「`112 → 15`」与全部实体级跨年读数在统一键函数下**重算**，在此之前不引用 |
| 成本 | 解析 `16.4M` 行一次，CPU 十分钟量级；零训练、零 LSPR24 标签读取 |

H6 的两种结果都不是失效面：一致则无新信息，不一致则是勘误。它排第一只因为便宜且是所有实体级数字的前提。

### 5.3 H3：最可能给出可区分家族的失效面

H3 问的是：LSPR24 的正例落在 LSPR23 有支撑的区域里但标签翻了（概念漂移），还是落在 LSPR23 没见过的区域（支撑漂移），
还是落在 LSPR23 见过且同为恶意的区域但模型仍漏掉（模型依赖）。三种答案指向三个不同家族，这正是「先失效面后家族」要的判别力。

**可观测量**（分析单位 = LSPR24 正实体，尊重实体级指标）：

1. 在**封印的 LSPR23 输入变换空间**（`input_transform_applied_only_never_refitted_on_target=true`）中，
   对每条 LSPR24 正例流求其到 LSPR23 训练流分层样本（`1–2M` 行）的 `k = 50` 近邻距离；以 LSPR23 验证集自身近邻距离的 `95%` 分位为界，判「在支撑内／外」。
2. 支撑内的流：近邻中恶意标签占比（LSPR23 训练标签，合法）→「近邻多良性」或「近邻多恶意」。
3. 该流在 C00 上的分数（**直接取 `runs/diagnostics/_pool_sweep/scores.npz`，零新增前向**），按仓库既有 `DR@4%FPR` 工作点判「检出／漏检」。
4. 逐实体汇总各格的流质量份额，得 `{检出, 漏检} × {支撑外, 支撑内·近邻多良性, 支撑内·近邻多恶意}` 的 `2×3` 表。

**对照**：(a) LSPR24 负例抽样走同一流程，给出基线格分布；(b) **LSPR23 时间尾部正例**走同一流程——
它从未被训练，按 5.1 应几乎全部落在「支撑内·近邻多恶意·检出」；**若不是，探针本身坏了，停**（探针有效性门：该格 `≥ 90%`）。

**先声明的判读**（只分类失效面，不判任何机制成败）：

| 漏检正实体质量的主格 | 失效面 | 与 DRO 不同源的家族 | 封印协议下的合法迭代场 |
| --- | --- | --- | --- |
| `≥ 60%` 支撑外 | **支撑漂移**（新颖） | 开集／异常暴露、距离型不确定性打分、表示紧致性约束 | LSPR23 内**留一攻击家族**（攻击家族是有意义的域划分，时段不是）；需先确认 LSPR23 侧有攻击叙事文件——LSPR24 有 `attack_narratives.json`，LSPR23 的对应物本审核未查 |
| `≥ 60%` 支撑内·近邻多良性 | **概念漂移**（同区异标） | **不存在只用源年就能解的训练目标**（任何在该区判恶意的函数在 LSPR23 上都会被罚）；家族是标签漂移／目标域信息适应，与封印冲突 | 无；本章须改写为「诚实负结果 ＋ 声明式重训边界」，交用户裁决 |
| `≥ 60%` 支撑内·近邻多恶意但漏检 | **模型依赖**（映射局部保持，模型押在漂移字段上） | 输入稳健正则：随机字段遮蔽／特征装袋／mixout 类（**不用**最坏字段子集，那是 DRO 同源） | 以 LSPR23 **年内时段间 KS** 标定的边缘扰动模型——此处「年内 KS ≥ 跨年 KS」这条证据**第一次变得有用**：年内片间移动与跨年同量级，可作合法扰动幅度 |
| 无主格 | 混合 | 不选单一家族 | 如实报告 |

**成本**：`83` 维近邻，查询 `≤ 10⁵–5×10⁵` 条 LSPR24 正例流（逐实体封顶），GPU 分批 `cdist` 分钟级或 CPU 数十分钟；零训练；零新增 LSPR24 前向。
**代价**：读 LSPR24 特征、标签与实体键——是目标域知情的诊断，须登记（见 B-5）。

### 5.4 为什么是 H3 而不是 H6，以及两者都绕不开的结构困境

H6 只能「一致／勘误」二选一；H3 把漏检质量切成三个可操作的面，其中两个有源年内的合法代理（攻击家族留一、年内片间扰动），
第三个直接判定本章能否在封印协议下成立。**它是这条线上第一个能在家族之间做选择、而不是确认某一家族的探针。**

结构困境必须写明：**失效面只能在 LSPR24 上观测**（LSPR23 内部没有代理——时间尾部是同分布），而主代理 2.0 节边界「禁止把机制搜索建立在 LSPR24 上」。
两者不能都满足。可行的折中（交用户裁决）：

1. 建 **LSPR24 读取台账**：四格评价、`_pool_sweep`、H1/H2/H4/H5 探针、oracle 重拟合、训练量阶梯——逐条登记读了什么；
2. **现在**按实体把 LSPR24 切成「诊断半」与「封存半」——不完美（全年四格 AP 与逐流分数已在手），但能止住对终评那一半的继续污染；
3. 正文如实写「机制家族的选择受一轮 LSPR24 诊断信息影响」，与恢复卡已把 oracle 重拟合定为「上界诊断」的做法一致。

### 5.5 不要再走的方向（已死或按构造排除）

- 阈值／保形／工作点类：改不了 AP（R1-M2）。可作第四章式工作点内容，不能作第三章 AP 机制。
- 聚合算子：打分侧已扫描否决；训练侧比例 `k` 已测不可分辨。
- 时段 Group DRO：Q2、Q3。
- 时段域不变表示（DI-NIDS／DANN 家族）：与时段 DRO 同一前提缺陷（时段不是域，尾部 AP `0.9999`），R1 的环境探针 AUC 也没动；
  除非域划分改为攻击家族，否则不立项。

---

## 六、阻断项清单（**不解决就不能开工**）

| # | 阻断项 | 性质 | 不解决的后果 | 解法与成本 |
| --- | --- | --- | --- | --- |
| **B-1** | **分辨率门重定**：用 LSPR24 实体级配对自助（或置换）得到 `ΔAP` 的 CI，替换外推的边际宽度 `0.063` | 实验（CPU 分钟级，用已有分数，零前向） | 本方案 §5 判据 1–3 全部悬置；今日「C10／C11 与 C00 不可分辨」判读不可信；任何后续四格都判不了 | 服务器恢复后第一件事；输入 `_pool_sweep/scores.npz` 与四格评价收据 |
| **B-2** | **H6 实体键审计**：同函数重建 `E23′` 与缓存 `E23` 比对 | 实验（CPU 十分钟级，无标签） | H4「`112 → 15`」及全部实体级跨年读数身份未定 | 见 5.2 |
| **B-3** | **时段分组有效性**：打乱时段标签对照 ＋ 从 R1 收据读每时段正实体数 | 实验（前者一次训练；后者零计算） | 机制一不得立项——前提证据现为反向（2.1），既有筛选证据不利（R1） | R1 `environment-manifest.json`、`environment-label-sequence-sampling` 收据；打乱对照设计已在 Sagawa 笔记 |
| **B-4** | **早停／选轮的合法信号**：在封印协议下给出可用于选轮的源侧信号，或明确记录为 oracle 选择 | 用户裁决 ＋ 设计 | 机制一「必须配早停」在协议下不可实施（2.4）；训练量阶梯选出的轮数是 oracle 选择 | 现无候选信号（源验证噪声、尾部同分布、`ρ = −1`）；诚实选项是登记为 oracle 诊断 |
| **B-5** | **LSPR24 读取台账 ＋ 诊断半／封存半划分** | 用户裁决 | 「先失效面后家族」与「禁止在 LSPR24 上搜机制」互斥；不裁决则每个新探针都在无声消耗终评 | 见 5.4；台账零成本，划分一次性 |
| **B-6** | **BER 存废与章结构的一句话定案**：〇之二点三「BER 不判死」／本方案弃 BER／08-13「单一机制可以不构成创新点」／2.0 GOAL 四者不相容 | 用户裁决 | 单机制或双机制都无法立项；主代理会继续在四句话之间摆动 | 写进恢复卡二节 |
| **B-7** | **重复发明防线**：新方案文档必须附「已死方案台账 ＋ `archive/` 与 GeNIS 线检索记录」 | 流程 | 本方案两个核心构件都是再发明（Q3），一天六次 | 机械化：方案模板加「检索记录」必填节，缺则不得进入 `writing-plans` |
| **B-8** | **在线候选入库**：Wild-Time（GroupDRO-T）、TDRO 现为 `【摘要】`，任何方案文档引用前须全文入库（`raw/` ＋ `wiki/`） | 文献 | 摘要级来源不得支撑论断 | 按 `raw/AGENTS.md`、`wiki/AGENTS.md` |
| **B-9** | **训练量阶梯的身份**：其判据读 LSPR24，且用 `0.063` 门 | 用户裁决 | 结果出来也不能按现判据读；若据其选轮数则触 B-4 | 降级为 oracle 诊断；判据改配对 CI |

**顺序**：B-1 → B-2 → B-3 并行可做；B-4／B-5／B-6／B-9 是用户裁决，不依赖服务器；B-7 立即生效；B-8 与 Q5 探针并行。
**在 B-1、B-2、B-5、B-6 关闭之前，不得为任何第二机制创建实施计划或正式运行身份。**
