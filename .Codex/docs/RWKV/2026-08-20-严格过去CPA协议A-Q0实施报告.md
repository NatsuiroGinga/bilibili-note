# 严格过去 CPA 协议 A Q0 实施报告

## 结论

严格过去 CPA 的协议 A Q0 入口已实现并通过本机静态验收。实现只新增 S10/S11 两个训练单元；父运行 `ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1` 的 B10/O11 只读复用，不重训、不复制检查点、不改变父事实。

本轮按用户裁决未连接服务器、未启动实验、未运行测试或冒烟，因此当前状态是**实现完成、实验待证**。父运行制品只能在后续正式入口中执行配置、代码、选择收据、检查点、状态和源数据身份的机械核验；本报告不宣称远端父制品已在本轮复核通过。

## 变更内容

### 1. 严格过去 CPA 工具

新增 `thesis/experiments/llm_probe/tools/ch3_full_mlp_strict_past_cpa_protocol_a_q0.py`。

- S10：严格过去 CPA 加实体最大池化，只训练逐流主损失。
- S11：严格过去 CPA 加旧 ELP，沿用逐流主损失与旧片段辅助损失。
- `ctx_t^-` 通过前缀和右移实现；第一个位置的和与计数均为精确零，后续位置只含严格早于当前位置的隐藏表示。
- 沿用父工具的协议 A 训练、最早最大逐流 AP 选轮次、逐轮权重、完整在途状态、优化器与随机状态恢复、完整实体指标、曲线和原子制品写入。
- 模型结构维度不变，参数量仍为 `2,144,258`；在线状态仍是一份前缀和与计数，时间与空间复杂度和旧 CPA 同阶。
- 不排序、不重排、不读取最终实体长度；时间戳并列时继续使用冻结序列中的稳定原始行序。

### 2. 父制品只读门

工具在训练前机械核验：

- 父配置 SHA-256 为 `f13ddd96d8a09b122004fb2c281cedb4411f03420a0eddc04cfc03b46f943f34`。
- 父代码 SHA-256 为 `45ad7d1be85832e4b235a8942615e4aad7b94c154ec2cef7ad00ec2510feb2da`。
- 父状态为 `complete` 且退出码为 0。
- 父选择封印包含 B10/O11，配置与代码身份一致。
- 每个父单元的选择收据、封印记录、检查点相对路径和检查点 SHA-256 一致。
- 检查点内身份与选择收据一致，并可被旧自包含 CPA 模型严格加载。
- 当前 LSPR23 数据清单身份与父选择封印一致。

任一条件失败即在加载 LSPR24 前停止。父检查点只从父运行根读取；新运行只保存父身份和摘要收据。

### 3. 源年门与目标年隔离

LSPR23 实体不相交验证集同时评价 B10、O11、S10、S11。以下条件必须全部成立：

1. S10 对 B10、S11 对 O11 的实体 AP 均严格增加。
2. 两组 `0%–8% FPR` 完整预算曲线归一化面积均严格增加。
3. 两组逐流 AP 均不下降。
4. 两组 DR@4%FPR 与 DR@8%FPR 均不下降。
5. 两组各至少两个有效实体长度桶的实体 AP 增加。

失败时正常封印源年否决结果并以 `target_year_arrays_read=0` 结束。通过后才一次加载 LSPR24 七个数组，在同一阶段分别对 B10、O11、S10、S11 前向一次；目标结果不参与检查点、机制、指数、门槛或运行身份修改。

### 4. 配置与启动器

新增冻结配置与唯一启动器：

- `thesis/experiments/llm_probe/configs/ch3-full-mlp-strict-past-cpa-protocol-a-q0-seed42-v1.json`
- `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_full_mlp_strict_past_cpa_protocol_a_q0_seed42_v1.sh`

启动器保留同名锁、`screen` 持久入口、同 `RUN_ID` 的 `--resume`、静态合同、父输入存在性、源年六数组、依赖、GPU/主存/磁盘准入、控制台与文件日志、五秒资源采样、主命令与 `tee` 双退出码、SwanLab 目的地授权和结果制品验收。启动前不枚举或加载 LSPR24。

## 文件范围

本任务共新增五个文件：

1. `.Codex/docs/RWKV/2026-08-20-严格过去CPA-协议A-Q0实施计划.md`
2. `thesis/experiments/llm_probe/tools/ch3_full_mlp_strict_past_cpa_protocol_a_q0.py`
3. `thesis/experiments/llm_probe/configs/ch3-full-mlp-strict-past-cpa-protocol-a-q0-seed42-v1.json`
4. `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_full_mlp_strict_past_cpa_protocol_a_q0_seed42_v1.sh`
5. `.Codex/docs/RWKV/2026-08-20-严格过去CPA协议A-Q0实施报告.md`

未修改父工具、父配置、父运行目录、恢复卡、路线总控、实验台账、知识库和实验制品。暂停的全项目混合检索架构审计只保留未提交计划与笔记，不属于本提交。

## 静态验收

按用户裁决，各项只执行一次：

| 项目 | 命令摘要 | 结果 |
| --- | --- | --- |
| Python 语法编译 | 读取工具源码并调用内建 `compile`，不写字节码 | 通过，退出码 0 |
| Python 导入 | `PYTHONDONTWRITEBYTECODE=1` 导入新模块 | 通过，退出码 0 |
| 帮助入口 | 新工具 `--help` | 通过，退出码 0 |
| 配置核验 | 新工具 `--config ... --validate-config` | 通过，退出码 0，输出“配置核验通过” |
| Shell 语法 | `bash -n` 新启动器 | 通过，退出码 0 |

三个生产文件对“三折”、`OOF`、`nested`、“嵌套”、`/tmp`、`black` 和测试入口扫描均为零命中。未运行自动格式化器；仓库规则禁止 `black`，本任务也未授权其他 Python 格式化。

## 输入 SHA-256

| 文件 | SHA-256 |
| --- | --- |
| `tools/ch3_full_mlp_strict_past_cpa_protocol_a_q0.py` | `688139f80d7aa7ab0553d8012b0ca1cd843dd255983cd456852db7ac22e7f90d` |
| `configs/ch3-full-mlp-strict-past-cpa-protocol-a-q0-seed42-v1.json` | `feef122f6a35b5b1fcfcd31cb29151139f866a09e07b6508ca163ac2caf9da77` |
| `scripts/remote_launchers/run_ch3_full_mlp_strict_past_cpa_protocol_a_q0_seed42_v1.sh` | `8739dbaef4abbab683789f0ddb765539019948285f80c90296f56d81a792a84e` |

## 资源合同

- 新训练单元：2 个，S10 与 S11 串行。
- 预计训练墙钟：8–12 分钟；该值为父同构实验基础上的工程估算，须由真实收据校准。
- 参数量：每个模型 `2,144,258`。
- 启动门：GPU 空闲显存至少 12,288 MiB，控制组可用主存至少 30 GiB，可用磁盘至少 10 GiB，同名并发为 0。
- 每 5 秒记录 GPU 已用显存与控制组主存；真实 GPU 小时、峰值显存和主存写入运行收据。

## 第三方接口

未引入新依赖或新接口形态。实现只复用父工具中已经完成真实运行或既有核验的 PyTorch、NumPy、scikit-learn 与 SwanLab 调用，因此未触发新的第三方 API 文档查询。

## 风险与阻塞

1. 本轮禁止服务器，父 B10/O11 的远端状态、收据与检查点尚未由本实现实际执行核验；正式入口会在任何训练和目标读取前机械阻断不一致。
2. LSPR24 已被历史实验读取，本 Q0 即使通过也只能形成 `target-informed` 描述性直接比较，不能写成独立最终测试或正式论文证据。
3. 同一 LSPR23 验证集承担选轮次和源年方向门，存在选择偏差；本任务按用户裁决禁止三折、折外预测和嵌套确认，不做显著性或泛化主张。

## 唯一后续入口

只有主代理在当轮重新核验服务器、输入、依赖、SwanLab 目的地、GPU 时长和费用后，才允许执行：

```bash
bash scripts/remote_launchers/run_ch3_full_mlp_strict_past_cpa_protocol_a_q0_seed42_v1.sh
```

本任务实现代理为 `project_hybrid_search_scope_audit_sol_high`，实际执行模型为 `gpt-5.6-sol`，推理强度为 `high`。
