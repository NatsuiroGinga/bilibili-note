# RWKV-7 首轮资源门根因与修订实施报告

## 结论

本地生产入口已达到 **READY**。`4.5 GPU 小时`已裁定为撤销的人为工程预算，不是科学门；`rerun1` 不设置任何运行时长或 GPU 小时上限，也没有替代上限。训练仍为每单元 `20` 轮、每轮 `1000` 步，候选、数据、模型、优化器、精度、评价和目标年隔离均未删减。

旧运行目录保持只读。新运行首次启动时必须把旧运行 `adapter-R0-K0-C00` 的完整第 1 轮检查点迁移到独立 `rerun1` 输出根，并从第 2 轮继续；任何非白名单差异都会使入口失败，不会静默从头训练。

## 根因裁决

- 原投影把当前完整单元与后续五个等价单元合计为六单元，公式没有重复计数。
- 首轮投影 `4.844 GPU 小时`比旧 `4.5 GPU 小时`预算高约 `7.6%`。
- 旧预算来自历史硬件线性外推，不是模型有效性、训练稳定性或数据质量门。
- 本次旧运行停止属于工程合同停止，不是 RWKV-7 科学失败。

## 变更文件

- `tools/ch3_rwkv7_field_aware_protocol_a_2x2.py`
  - 支持旧身份和新 `rerun1` 身份。
  - 删除训练、容量筛选和目标评价中的全部累计时长投影停止。
  - 删除容量候选的时长资格筛选，K0/K1/K2 均按原顺序完整执行。
  - 新增一次性旧检查点迁移门与 `checkpoint-migration-receipt.json`。
- `configs/ch3-rwkv7-field-aware-protocol-a-2x2-seed42-v1-rerun1.json`
  - 使用独立运行身份、输出根和 SwanLab 分组。
  - 删除 `maximum_gpu_hours` 和首轮预算重校准字段。
- `scripts/remote_launchers/run_ch3_rwkv7_field_aware_protocol_a_2x2_seed42_v1.sh`
  - 删除 `timeout` 硬停止和最长秒数收据字段。
  - 保留显存、控制组主存、磁盘和并发准入。
  - 非零退出时把启动器状态写为 `failed/runtime`，不再遗留陈旧 `running`。
  - 支持新身份固定的一次性迁移参数。
- `scripts/remote_launchers/run_ch3_rwkv7_field_aware_protocol_a_2x2_seed42_v1_rerun1.sh`
  - 固定新运行身份、会话名、旧迁移源和新启动器身份。

旧配置、旧失败输出目录、轮数、步数、批量、学习率、权重衰减、模型结构、适配器、容量、四格、字段、切分、指标和评价次数均未修改。

## 迁移门

迁移前机械核验：

- 旧冻结配置摘要与旧在途身份一致。
- 旧、新配置归一化后完全相同；只允许运行身份及其派生输出路径和 SwanLab 分组、删除运行时长字段不同。
- 当前六个源数组清单摘要与旧在途身份一致。
- 任务固定为 `adapter-R0-K0-C00`，模型状态键、形状和精度与新模型完全一致。
- 优化器参数组、参数槽数与全部超参数和新配置一致。
- 在途状态是完整第 `1` 轮、第 `1000` 步，历史只有第 1 轮，最佳轮为第 1 轮。
- 模型、优化器、采样生成器、Python/NumPy/PyTorch CPU/CUDA 随机状态、历史、最佳状态和梯度门全部存在。
- 旧最佳权重摘要与旧第 1 轮检查点一致。

迁移后原子写入新身份的第 1 轮检查点、在途检查点和梯度收据，并记录源/目标 SHA-256、允许差异、保留状态与 `scientific_contract_equal=true`。重复启动只接受摘要仍匹配的既有迁移收据。

## 本地验证

以下检查各执行一次，均通过：

1. Python 语法检查。
2. 模块导入检查，读出新身份 `ch3-rwkv7-field-aware-protocol-a-2x2-seed42-v1-rerun1`。
3. `--help` 检查。
4. `rerun1 --validate-config`，输出“配置核验通过”。
5. 旧公共启动器与新 `rerun1` 启动器 `bash -n`。

未访问服务器，未读取真实缓存，未执行检查点迁移，未启动训练或评价。因此迁移收据与第 2 轮继续运行仍须由 B76 真实入口验证。

## 唯一启动命令

在远程项目根执行：

```bash
bash scripts/remote_launchers/run_ch3_rwkv7_field_aware_protocol_a_2x2_seed42_v1_rerun1.sh
```

入口自动检测旧第 1 轮在途检查点，迁移到新输出根并以 `--resume` 从第 2 轮继续。不得另行手工复制检查点或添加时长限制。
