# TabM32 骨干专属论文配方协议 A 实施报告

任务 `N-12`，执行方 Claude Code，日期 `2026-08-21`。
实施计划见 [TabM32 骨干专属论文配方协议 A 实施计划](2026-08-21-TabM32骨干专属论文配方协议A实施计划.md)。

## 一、运行边界声明

本方**未启动任何实验、未同步、未修改任何活动运行根**。服务器动作仅有一次：经用户当轮明确授权，只读取回字段基数收据的数值（见第四节），未写入、未启动、未改动服务器上任何文件。

未触碰 GRANDE、RWKV-7、严格过去 CPA、既有 TabM4 工具、任何共享框架。所有提交只暂存本任务文件，工作树中其他代理的未提交改动一律未动。

## 二、交付文件

| 角色 | 路径 | 状态 |
| --- | --- | --- |
| 实施计划 | `.Codex/docs/RWKV/2026-08-21-TabM32骨干专属论文配方协议A实施计划.md` | 已交付 |
| 工具 | `thesis/experiments/llm_probe/tools/ch3_tabm32_paper_recipe_protocol_a.py` | 见第七节 |
| 配置 | `thesis/experiments/llm_probe/configs/ch3-tabm32-paper-recipe-protocol-a-seed42-v1.json` | 已交付 |
| 启动器 | `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_tabm32_paper_recipe_protocol_a_seed42_v1.sh` | 已交付 |
| 实施报告 | 本文件 | 已交付 |
| 看板 | `.Codex/docs/RWKV/2026-08-20-第三章神经骨干跨进程协作看板.md` | 只改 N-12 行与消息日志 |

提交链：

```
43f380c  实施计划
e20eb57  任务1 配置与骨架
af5bf6e  计划对齐重写后的 llm_probe 规则
85aa718  任务1修复 批量闭合检查前移
aa8a937  登记精度合同冲突为冲突五
a7624d7  任务2 收据消费与输入变换
41c1e9a  采纳 Codex 精度裁决，记录跨 profile 限制
9ae3ed5  看板：提出精度可比性与收据可读性两项阻塞
d8f1930  任务7 远程启动器
```

## 三、参数量闭式：关闭冻结配方的「待实现复核」

冻结配方 `骨干专属论文配方冻结.md` 第 156 行把可训练参数量记为 `995,457`，标注「数学闭合，**待实现复核**」。本方按第 170 行的 `BE(a,b,k)=a×b+k×a+k×b+k×b` 逐层展开核验：

```
BE(83,512,32)      =  77,920
BE(512,512,32)     = 311,296
BE(1024,512,32)    = 589,824
32 × Linear(512,1) =  16,416
共享 p_log         =       1
合计               = 995,457      与冻结值一致
```

由此解出闭式：

```
总参数量 = 544 × d_in + 950,305
```

`d_in = 83` 时恰为 `995,457`。**该复核由此关闭。**

同时得到一个此前未被记载的事实：**冻结的 `995,457` 只在输入维为 `83` 时成立**。候选二一旦入选，参数量必须按闭式重算并单独封印，不能沿用冻结值。

## 四、字段基数收据的实测值

经用户授权只读取回，来源 `runs/diagnostics/ch3-lspr23-field-cardinality-receipt-v1/field-cardinality-receipt.json`，`complete = true`。

| 字段 | 训练区基数 | 整数性 | 缺失率 | top-1 | top-10 | top-1000 |
| --- | --- | --- | --- | --- | --- | --- |
| `SrcPort` | `61,057` | `False` | `0` | `1.89%` | `2.46%` | `8.26%` |
| `DstPort` | `6,041` | `False` | `0` | `36.91%` | `92.08%` | `99.95%` |
| `Protocol` | `7` | `False` | `0` | — | — | — |
| `L3/L4 Protocol` | `5` | `False` | `0` | — | — | — |
| `Int/Ext Dst IP` | `2` | `False` | `0` | — | — | — |
| `External_src` | `2` | `False` | `0` | — | — | — |
| `External_dst` | `2` | `False` | `0` | — | — | — |

`training_effective_flows.effective_flows = 11,991,315`。
`protocol_a_source_split.statistics = entity_count 150680 / train_sequences 208598 / validation_sequences 22444 / train_validation_row_intersection 0`，与本运行的切分身份一致。

该收据关闭了三项待定：

1. **存储视图**：七字段 `integer_like` 全为 `False`，取值域形如 `-3.92 ~ 0.94`。**缓存中确为标准化浮点，不是原始端口号**。本方先前依据 `dijk_ingest.py` 只做 `float32` 强制转换而推断「无统计量变换」是错的——那只覆盖入库环节，`X23.npy` 由别的管线产出且已标准化。Codex 在看板的提醒正确。所幸实现不依赖该解释：词表键用 `float32` 位模式，只要求映射单射且确定。
2. **缺失指示位**：七字段 `missing_fraction` 全为 `0`，故两候选一律不引入指示位，与冻结的 `83` 维无冲突。
3. **候选二规模**：`d_in = 76 + 2 + Σ(c+1) = 67,195`，参数量 `544 × 67,195 + 950,305 = 37,504,385`。

## 五、冲突登记与裁决

计划第二节共登记六项冲突。截至本报告：

| 编号 | 冲突 | 状态 |
| --- | --- | --- |
| 一 | 冻结配方「耐心 16 无轮数上限」与协议 A「20 轮跑满不早停」不可兼得 | 本方取协议 A，待 Codex 确认 |
| 二 | 协议 A 的 `weight_decay=0.01` 与两个优化器候选（`0.0003`、`0`）都不同 | 本方取候选值、其余协议项照抄，待确认 |
| 三 | 冻结的 `995,457` 只在 `d_in=83` 成立 | 参数量断言按候选分支，待确认 |
| 四 | 既有配置 `observed_tabm_single_job_peak_gpu_memory_mib=8244` 已被 D-1 实测 `13646.41` 推翻 | **未裁决**，`minimum_free_gpu_memory_mib` 仍为 `null`，启动器门禁 6 会拒绝启动 |
| 五 | 共享精度合同与协议 A 同精度可比性 | **已由 Codex 提交 `c758763` 裁决**，见下 |
| 六 | 候选二的容量混杂 | **未裁决**，见下 |

### 冲突五的裁决与后果

Codex 裁定：B76 上新神经训练默认 `cuda-bf16-amp-fp32-sensitive-v1`，经 `tools/neural_precision_runtime.py` 接入；`FP32` 只能作有收据的显式例外；同族比较须同 profile，跨 profile 只作完整系统或工程 Pareto。

本方原建议取 `FP32`（依据：三个已完成协议 A 骨干实测均为纯 `FP32`，而 `BF16` 在本运行只省激活 `0.17 GiB`、占实测峰值 `1.3%`）被否决，已按裁决执行。

**必须记入选择封印的后果**：TabM32 在 `BF16` 下的四格结果，与 ResMLP2、TabM4、全容量 MLP、严格过去 CPA 这四个纯 `FP32` 骨干的比较属跨 profile。`neural_precision_runtime.py` 的常量 `CROSS_PROFILE_CLAIMS` 仅允许 `complete_system_pareto` 与 `engineering_observation`。**第三章全模型指标总表并列这两类结果时必须标注 profile 差异，不得据此声称某骨干「全面领先」。** 这是裁决的直接推论，不是本方保留意见。

### 冲突六：候选二的容量混杂（待 Codex 裁决）

由第四节实测值导出：

```
SrcPort  独热 61,058 列  占候选二总宽 90.9%
DstPort  独热  6,042 列  占  9.0%
其余三个离散字段合计        17 列  占  0.0%

候选一  d_in=83      参数量    995,457
候选二  d_in=67,195  参数量 37,504,385      输入维 810×，参数量 37.7×
```

显存不是瓶颈：选择阶段 `9.51 GiB`、评价阶段 `15.76 GiB`，单卡 `31.84 GiB`。

问题在科学层面：**两个候选相差 37.7 倍参数量，在 C00 上比验证 AP 不是在比输入接口，而是在比容量。** 冻结配方对两个优化器候选明确要求「结构、成员数、训练批、随机失活、停止和样本曝光相同，只改变优化器锚」，但对输入候选没有对应条款，而输入候选恰是唯一会改变容量的一维。

分布形态进一步支持这一判断：`DstPort` top-10 覆盖 `92.08%`，是真正的类别量；`SrcPort` 基数 `61,057`、top-1000 仅覆盖 `8.26%`，是临时源端口的典型形态，近乎每流一随机值。把它独热成 `61,058` 列，等于给模型六万个近乎无信号的维度，并贡献了候选二 `90.9%` 的宽度与绝大部分参数差。

本方不自行处置，建议 Codex 三选一：按字段类型分治但 `SrcPort` 保持数值；两候选做同参数量对照以剥离容量混杂；或维持现设计但在封印中明确记载该比较混杂了容量差异，不能解释为输入接口的效应。

## 六、审查结果

每个任务均经独立审查代理复核，审查档位不低于被审任务。

**任务 1**（配置与骨架）：规格 5 步全通过，无 Critical，1 项 Important、3 项 Minor。Important 为「批量闭合检查排在 `candidate` 兜底之后成为死码」——审查用实验证明改坏 `gradient_accumulation_steps` 会被笼统兜底拦截，掩盖真根因。修复后经定向复审判定 `ADDRESSED`，复审独立跑七组用例，含两组类型检查（字符串与 `None` 均抛带中文原因的 `ValueError` 而非 `TypeError`）与一组兜底回归。

**任务 2**（收据消费与输入变换）：规格 6 步中 5 项通过、1 项部分通过（参数量三方比对需模型，属后续任务）。1 项 Critical、1 项 Important、6 项 Minor。

审查执行了一个本方未设计的关键实验：**只扰动留出区数据，`state_hash` 与 `output_dimension` 完全不变**——这是本地能取得的最强泄漏证据，直接证明拟合只用训练区。审查另在 sklearn `1.9.0`（实现报告只测了 `1.7.2`，而 `uv.lock` 钉了两支）复测组装式变换器逐位等价，并实测 `subsample=1_000_000_000` 确实关闭子采样（换 `random_state` 结果逐位相同，默认 `10000` 则不同）。

**任务 7**（启动器）：实现代理自审时发现两处 `bash -n` 无法检出的 `errexit` 缺陷并修复——一是 `--stage` 参数解析用裸 `[[ ]] && var=1`，会导致只要传 `--stage` 脚本必崩；二是 `finalize_resources` 失败会在写最终状态前中止 worker，使运行永远卡在 `running`。

本方独立复核启动器：`bash -n` 通过；`rsync --delete`、`/dev/null`、`/tmp/`、凭据类字面量、主机与地址类字面量**五类禁令全部零命中**（该脚本刻意不用 `/dev/null`，改用变量捕获）；九道门禁齐备。

## 七、本方发现并纠正的三处缺陷

**其一，实施计划中的收据键路径错误。** 计划示例代码写 `receipt["protocol_a_source_split"]["entity_count"]`，而生产端把三个计数嵌在 `statistics` 之下（`ch3_lspr23_field_cardinality_receipt.py` 第 336 至 343 行）。照抄会在服务器上直接 `KeyError`。由任务 2 实现代理发现并修正，键路径与三个精确值 `150680 / 208598 / 22444` 均已核对。

**其二，独热词表的数据源错误。** 计划原写「按收据 `top_values` 建词表」，而收据只输出前二十个取值（第 113 行 `top_k_values=20`、第 263 行 `ordered[:top_k]`），`unique_count` 可达 `61,057`。照此实现会静默丢失绝大多数取值，产生看似可运行但语义错误的输入。已改为运行时从 LSPR23 训练区拟合，收据只作断言与容量核算。

**其三，资源预算漏乘成员维。** `project_input_dimension` 原写 `dense_bytes = micro_batch × sequence_length × output_dimension × 4`，缺 `× ensemble_members`。BatchEnsemble 的输入在进第一层前必须展开为 `N×k×T×F`（既有实现第 375 行断言、`LinearBatchEnsemble.forward` 的 `r.unsqueeze(0).unsqueeze(2)` 要求成员维在第 1 轴）。该式在候选二下把 `4.10 GiB` 报成 `131.3 MiB`，差 32 倍，后果是服务器上据此判「可行」后 OOM。由本方独立算术发现，审查代理复算确认并评为 `Critical`。

## 八、显存构成核算

按协议 A 的形状断言核算，用于替代已被推翻的 `8244 MiB`：

| 项 | 值 | 占 D-1 实测峰值 `13646.41 MiB` |
| --- | --- | --- |
| `X23` `(16353511, 83)` fp32 | `5.06 GiB` | — |
| `X24` `(20227356, 83)` fp32 | `6.25 GiB` | — |
| `I23` `(271815, 128)` int64 | `0.26 GiB` | — |
| 双年数据常驻合计 | `11.66 GiB` | `87.5%` |
| TabM32 激活上界 | `0.34 GiB` | `2.6%` |
| 参数加梯度加 Adam 两态 | `0.015 GiB` | 可忽略 |

**峰值由数据常驻主导，模型与激活占比很小。** 单个 fp32 隐藏张量为 `4 × 128 × 32 × 512 × 4 = 32 MiB`、CPA 拼接 `64 MiB`，与冻结配方第 150 行的资源推导逐字一致，该推导由此获得独立复核。

## 九、规模与时长估算

- 每样本乘加：TabM4 为 `4 × (83×186 + 372×186) = 338,520`；TabM32 为 `32 × (83×512 + 512×512 + 1024×512) = 26,525,696`，比值 `78.4×`。
- 以 N-02 实测单格 `160.0 s`、单步 `8.00 ms` 为基线外推：

| 情形 | 单格 | 选择 3 次加四格共 6 次 |
| --- | --- | --- |
| 上界，算力线性放大 | `3.48 h` | `20.89 h` |
| 中性，利用率提升后摊薄 | `1.22 h` | `7.31 h` |
| 下界，大矩阵接近峰值算力 | `0.52 h` | `3.13 h` |

N-02 快照时 GPU 利用率仅 `9%`，故线性外推是上界。该区间是**估算**，不是承诺；工具在首个 epoch 结束时用实测吞吐替换并写入日志。无运行时长上限。

## 十、遗留风险与阻塞

1. **`minimum_free_gpu_memory_mib` 仍为 `null`**，启动器门禁 6 会拒绝启动。待 Codex 依第八节核算裁定。这是当前唯一的启动硬阻塞。
2. **冲突六未裁决**，候选二的容量混杂若不处置，即使它胜出也无法归因于输入接口，输入封印失去意义。
3. **跨 profile 限制须传递到正文**：写第三章全模型指标总表时必须标注 profile 差异，不得据 BF16 结果与 FP32 骨干直接比较而声称领先。
4. **工具文件规模超出全局 800 行上限**。审查同意不中途拆分（任务 1 已冻结模块契约），留待启动前统一处理。
5. **端到端未验证**：门禁 6 恒失败加上阶段函数在任务 3+4 完成前为骨架，启动器目前只经静态审查与逻辑推演，未跑通真实数据路径。这是预期的上游未就绪状态。
6. **本地无 GPU 与 torch**，前向形状、参数量三方比对、显存四量采集只能在服务器首次运行的入口阶段完成。本报告中标为「待服务器验证」的项不得与实测项混读。

## 十一、待补：任务 3 与任务 4 的交付

三层 BatchEnsemble 骨干、训练循环、梯度累积、协议 A 检查点选择、同身份断点恢复、选择封印与 LSPR24 单次评价的实现与验收结果，待对应实现代理交付后补入本节，并同步更新第二节的提交链与第七节的缺陷清单。
