# 第三章神经骨干跨进程协作看板

## 一、用途与唯一真源

- 本文是 Codex 与 Claude Code 协作执行第三章神经骨干实验的唯一进程间通信入口。
- 当前工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`。
- 当前分支：`codex/ch4-dtep-pbc-20260819`；建立本文时基准提交为 `75f5992fba33a5939875fe720a5cb943b6421033`。
- 研究合同以 [ResMLP2 与 TabM 协议 A 四格重跑实施计划](2026-08-20-ResMLP2与TabM协议A四格重跑实施计划.md) 为准，当前状态以 [RWKV 第三章恢复卡](RWKV第三章恢复卡.md) 为准；本文只负责所有权、运行调度和消息交接，不改研究口径。
- 任何进程不得依据聊天记录覆盖本文和原始运行制品。运行事实以服务器 `status.json`、`run.log`、检查点和聚合结果为准。

## 二、任务看板

状态只使用 `待领取`、`已领取`、`运行中`、`阻塞`、`完成`、`否决`。

| 编号 | 任务 | 所有者 | 状态 | 运行身份或制品 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| N-01 | 最初 ResMLP2 协议 A 四格 | Codex 主进程 | 运行中 | `ch3-resmlp2-cpa-elp-protocol-a-2x2-seed42-v1` | Codex 独占监控、断点恢复和结果回收；Claude Code 不得修改或重启 |
| N-02 | TabM4 协议 A 四格 | Claude Code | 待领取 | `ch3-tabm4-cpa-elp-protocol-a-2x2-seed42-v1` | Claude Code 按第四节验收、同步并启动 |
| N-03 | RWKV-7 源年资格对照 | Claude Code 候选队列 | 待领取 | 实现提交 `72212c0`，资源说明 `80d83d9` | N-01/N-02 完成后再领取；预计约 4.5 GPU 小时，不与短实验争抢 GPU |
| N-04 | FT-Transformer | 未分配 | 阻塞 | 尚无工具、配置、启动器或真实结果 | 先冻结实现计划，不得直接启动 |
| N-05 | ResMLP2/TabM 骨干专属论文配方 | Codex 文献代理 | 运行中 | `.Codex/docs/RWKV/2026-08-20-骨干专属论文配方冻结/` | Claude Code 不修改；候选冻结前禁止据此实现或运行 |

## 三、当前运行快照

快照时间：`2026-08-20 16:26 CST`。该快照会过期，任何启动动作前必须重新查询服务器。

- 服务器：B76，单张 RTX 5090，显存总量 `32607 MiB`。
- N-01：`screen=ch3-resmlp2-protocol-a-s42-v1`，启动器与运行状态均为 `running`；阶段为仅加载 LSPR23 的源年选择。
- N-01 进度：`C00 epoch=18/20`；当时 GPU 使用 `7474 MiB`、空闲 `24638 MiB`、利用率 `9%`。
- N-01 输出根：`runs/diagnostics/ch3-resmlp2-cpa-elp-protocol-a-2x2-seed42-v1`。
- N-01 启动器根：`runs/launchers/ch3-resmlp2-cpa-elp-protocol-a-2x2-seed42-v1`。
- 当前目标年已经被历史实验访问，N-01/N-02 都是直接比较实验，不重新获得不可见最终测试身份。

## 四、Claude Code 的立即任务：领取 N-02

### 4.1 只读恢复

1. 完整读取根 `AGENTS.md`，随后读取 `thesis/AGENTS.md`、`thesis/experiments/llm_probe/AGENTS.md`、`thesis/experiments/llm_probe/scripts/AGENTS.md`、RWKV 路线规则、路线总控、第三章恢复卡和本看板。
2. 不读取 PINN/R2 恢复文档，不修改 N-01、N-05 及第四章机制代码。
3. 将 N-02 所有者改为 `Claude Code`、状态改为 `已领取`，并在消息日志追加领取记录后再执行。

### 4.2 本地文件与固定身份

N-02 只使用以下三个生产文件：

| 文件 | SHA-256 |
| --- | --- |
| `thesis/experiments/llm_probe/tools/ch3_resmlp2_tabm_protocol_a_2x2.py` | `c6f42f5d97119290ca10d71fc7d83c4c4e4d42aa1745b8559c9aa1f655adddee` |
| `thesis/experiments/llm_probe/configs/ch3-tabm4-protocol-a-2x2-seed42-v1.json` | `0e56ebd6245147fe7257da503e6ee96bf4d09669540809e52fc2c174922d11c7` |
| `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_tabm4_protocol_a_2x2_seed42_v1.sh` | `21631bd42fdfe7e3d0d3c41d999fcc3c909a5ef6dcec07c9c960db30e7f36146` |

- 运行身份：`ch3-tabm4-cpa-elp-protocol-a-2x2-seed42-v1`。
- 持久会话：`ch3-tabm4-protocol-a-s42-v1`。
- Claude Code 不得修改共享工具；如果本地或远端共享工具 SHA 不等于表中值，停止并在消息日志请求协调。
- 首次同步前只执行一次以下最小入口检查；禁止格式化、单元测试、集成测试和独立冒烟实验：

```bash
cd thesis/experiments/llm_probe
source tools/env/activate.sh
UV_CACHE_DIR=.cache/uv PYTHONPYCACHEPREFIX=.cache/codex-pycache uv run --no-sync python -m py_compile tools/ch3_resmlp2_tabm_protocol_a_2x2.py
UV_CACHE_DIR=.cache/uv PYTHONPYCACHEPREFIX=.cache/codex-pycache uv run --no-sync python -c 'import importlib.util; p="tools/ch3_resmlp2_tabm_protocol_a_2x2.py"; s=importlib.util.spec_from_file_location("ch3_protocol_a", p); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print("IMPORT_OK")'
UV_CACHE_DIR=.cache/uv PYTHONPYCACHEPREFIX=.cache/codex-pycache uv run --no-sync python tools/ch3_resmlp2_tabm_protocol_a_2x2.py --validate-config --config configs/ch3-tabm4-protocol-a-2x2-seed42-v1.json
bash -n scripts/remote_launchers/run_ch3_tabm4_protocol_a_2x2_seed42_v1.sh
```

### 4.3 并行资源准入

- 用户已在本会话明确授权第三章神经骨干实验、确认 B76 开机，并要求利用空闲 GPU 并行和支持断点恢复。N-02 预计使用一张 RTX 5090、约 `8–12 GiB` 显存和 `10–25` 分钟；实例或计费状态发生变化时必须重新询问，未变化时不重复打断用户。
- N-01 与 N-02 输出、锁和 SwanLab 身份独立，可以并行；各自内部的 `C00/C01/C10/C11` 仍必须顺序执行。
- 启动 N-02 前重新查询 N-01 状态、GPU 空闲显存、主存、磁盘和同名进程。只有 N-01 正常运行、无同名 N-02、GPU 空闲显存不少于 `12 GiB` 时才并行启动。
- `12 GiB` 不是任意数字：既有 TabM 真实运行峰值为 `8244 MiB`，该门保留约 `46%` 显存余量。未满足时等待 N-01 完成，不得放宽门槛。
- 并行运行的墙钟、吞吐和 GPU 小时必须标记资源争用，只作工程记录；论文效率比较另用空闲 GPU 串行复测冻结代表单元。
- 启动器自身执行依赖导入、GPU 可用性、30 GiB 主存门、SwanLab 目的地与登录、运行锁和制品后验收。Claude Code 不在启动前重复拆成多轮人工检查；若同一 B76 实例未重启，只补远端三文件 SHA、`bash -n`、当前 GPU/磁盘/进程快照后直接进入启动器。服务器重启或实例变化时才重做完整能力与数据门。

### 4.4 同步、启动与恢复

- 凭据仅从路线总控规定的 B76 环境变量读取；禁止打印或写入本文。远程执行使用项目内 `tools/remote_exec/gpu_env_quiet.exp`，同步使用 `tools/remote_exec/gpu_rsync_push.exp`，禁止创建 `/tmp` 辅助脚本。
- 只同步第四节列出的三个文件，禁止 `rsync --delete`；同步后核对远端 SHA。
- 同步后在远端项目根对 TabM 启动器执行一次 `bash -n`；工具、配置和依赖的运行时核验由同一正式启动器完成，不另建实验身份。
- 唯一首次启动命令：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
bash scripts/remote_launchers/run_ch3_tabm4_protocol_a_2x2_seed42_v1.sh
```

- 中断后必须先核对配置、工具、输入和检查点身份，再使用同一运行身份恢复：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
bash scripts/remote_launchers/run_ch3_tabm4_protocol_a_2x2_seed42_v1.sh --resume
```

- 不得删除失败目录或换新身份伪装恢复。每轮检查点、已完成单元、目标评价收据和最终状态均由启动器幂等处理。

### 4.5 完成回报

Claude Code 完成 N-02 后必须在消息日志写入：

1. 远端三文件 SHA、启动时间、运行身份和会话名。
2. 每格选择轮次、LSPR23 验证逐流 AP。
3. LSPR24 逐流 AP、实体 AP、最大池化实体 AP、六档检测率和完整预算曲线制品路径。
4. 峰值显存、峰值主存、训练/评价墙钟，以及是否存在资源争用。
5. `status.json`、`aggregate-results.json`、清单、日志和 SwanLab 收据路径；工程失败与科学结果分开说明。

## 五、Codex 的独占任务

- 继续 N-01，不启动第二个 ResMLP2，不修改 N-02 运行目录。
- 每个单元完成后核对检查点与选择收据；中断时只用原身份 `--resume`。
- N-01 完成后回收并分析完整指标；不得只比较实体 AP。
- 继续 N-05 文献配方冻结；不因 N-01/N-02 的 LSPR24 结果修改论文配方候选。
- Claude Code 已领取 N-02 后，Codex 不再同步、启动或修复 N-02，除非消息日志明确移交所有权。

## 六、跨进程写入协议

### 6.1 写锁

任何进程修改本文前执行：

```bash
mkdir .Codex/docs/RWKV/.第三章神经骨干跨进程协作看板.lock
```

- 创建成功才可写；失败表示另一进程正在写，等待后重新读取本文。
- 写入、`git diff --check` 和仅本文的原子提交完成后，用 `rmdir` 释放空锁目录。
- 禁止用 `rm` 强行删除锁。锁长期存在时先在聊天中确认持有者，不得猜测为陈旧锁。

### 6.2 文件所有权

- 任务所有者只修改看板登记的文件和运行根。
- 共享工具在任一远端运行进行时冻结。需要改动时先让所有使用者确认并迁移到新版本身份。
- Git 暂存和提交只能包含本任务文件；禁止 `git reset --hard`、`git checkout --` 或覆盖其他进程未提交改动。
- 发现目标文件已有未提交修改时停止并发编辑，在消息日志请求移交。

### 6.3 消息格式

每条消息追加一行，不改写历史：

| 时间 | 发送方 | 接收方 | 类型 | 任务 | 内容 | 是否需确认 |
| --- | --- | --- | --- | --- | --- | --- |
| `YYYY-MM-DD HH:MM CST` | `Codex` 或 `Claude Code` | 对方 | `领取/进度/阻塞/移交/完成` | `N-xx` | 精确路径、状态、数字或下一动作 | `是/否` |

## 七、消息日志

| 时间 | 发送方 | 接收方 | 类型 | 任务 | 内容 | 是否需确认 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-20 16:26 CST | Codex | Claude Code | 移交 | N-02 | TabM4 协议 A 四格开放领取；先按第四节核验，满足资源门后可与 N-01 并行 | 是 |
| 2026-08-20 16:26 CST | Codex | Claude Code | 进度 | N-01 | ResMLP2 正在 C00 第 18/20 轮；Codex 独占运行与恢复 | 否 |

## 八、给 Claude Code 的启动指令

用户只需在 Claude Code 中发送：

> 完整读取 `.Codex/docs/RWKV/2026-08-20-第三章神经骨干跨进程协作看板.md`，领取并执行 N-02。严格遵守文件所有权、GPU 准入、断点恢复和消息日志协议；不要修改或重启 N-01/N-05。
