# 第三章神经骨干跨进程协作看板

- **状态日期**：2026-08-26
- **用途**：当前任务、文件所有权、状态待核运行和直接接手条件。本文不再保存代理聊天、逐分钟进度或已关闭事故。
- **工作树**：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- **当前分支**：`codex/ch4-dtep-pbc-20260819`

## 一、恢复与事实源

1. 依次读取根 `AGENTS.md`、`.Codex/docs/AGENTS.md`、[RWKV 路线规则](AGENTS.md)、[路线总控](RWKV路线总控.md)和[第三章恢复卡](RWKV第三章恢复卡.md)。
2. 本看板只决定所有权和接手动作，不改变研究公式、数据合同或科学裁决。
3. 运行事实只认原始 `status.json`、日志、检查点、清单和聚合制品；提交、文件存在和代理自报不是完成证据。
4. 精简前消息日志固定在基线 `5a9bc596f80b9b6c8dec9dc29e361f10ac13ef1d`，需要事故或对话追溯时用 Git 读取，不回填本看板。

## 二、当前任务表

| 编号 | 任务 | 所有者 | 状态 | 当前事实 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| G-CH3 | 第三章统一资格 Goal | 主控 | `active` | 有效 `2×2` S0 已完成；源年仅 B00 侧非支配，B10 侧被支配，固定混合与 CPA 开启状态不兼容 | 先冻结并审批 S1，不扩展固定混合、公式或目标年选择 |
| I-2X2 | 有效 `2×2` S0 | 已完成 | `完成` | 源年与目标均 `finished/exit0`；源 `9/9`、目标 `7/7` 清单零差异；资源并发 | 结果见[实施与结果报告](2026-08-26-第三章有效2x2机制优化计划/实施与结果报告.md)；不得重复运行或改写父制品 |
| A-LIT | 四篇漂移文献机制审计 | 已完成 | `完成` | `5a9bc59`：只放行固定 mean-max 零训练 S0，不支持训练层多算子 | 不再扩展检索，按允许/禁止措辞约束 S0 与正文 |
| A-XGB | XGBoost 同数据集论文配方来源审计 | 已完成 | `完成` | `8ca6e0b` 已提交四层来源矩阵 | 后续引用[审计报告](2026-08-26-XGBoost同数据集论文配方来源审计/审计报告.md)，不得把当前配置称为论文配方复现 |
| P-S1 | 冻结骨干 S1 规划 | 未分派 | `待规划／未授权实现` | S0 源年有非支配信号，但固定混合在 CPA 开启状态失效 | 另立 `[mean,max]` 线性实体头与 `[mean,mean]` 同参数控制计划，重新审批后才能实现 |
| E-S1 | 独立实体头 S1（覆盖 P-S1 旧状态） | 已完成 | `源年拒绝／停止` | 候选 AP `0.706044`，高于控制 `0.683671`、低于 B10 max `0.868404`，`low_budget_ok=false`，`target_reads=0` | 保留 O11；不跑 LSPR24、不追调；内部负筛选不进入论文正文、主表或小结；证据见[结果报告](2026-08-26-全容量MLP独立实体头S1/结果报告.md) |
| R-RULES | AGENTS 规则链 P0 | 已完成 | `完成` | `f960bf42`：远程脚本链余量 `4,977` 字节 | 不重开 P1/P2；后续规则修改继续执行组合字节门 |
| E-CUDA | 独立 CUDA-RWKV 正式源年训练 | 运行身份所有者待当前状态确认 | `状态待核／禁止重复启动` | 最近记录为 `A-R2-small-C00` 已有 step `900` 完整断点，step `914` 诊断有限且非零 | 远端动作前只读查询；若未完成，仅同身份原子恢复 |
| E-TABM | TabM32 论文配方 | 运行身份所有者待当前状态确认 | `状态待核／禁止重复启动` | 唯一身份 `ch3-tabm32-paper-recipe-protocol-a-seed42-v1`；8 月 25 日结束状态未核 | 先读运行交接并只读监控，不手工调用 worker |
| E-FT | FT-Transformer | 运行身份所有者待当前状态确认 | `状态待核／禁止重复启动` | 8 月 25 日最后口述约 `epoch 10/20`，无当前原始状态 | 先定位正式身份与原始状态，不按口述判完成或失败 |
| D-TABLE | 统一总表 | 未分派 | `阻塞` | 名义键 DR 与实际 FPR 重算值混比；两条证据线共用帕累托池 | S0 后另立修复任务；修复前不更新优劣结论 |
| D-RESNET | 表格 ResNet v2 LSPR24 同源重评 | 未分派 | `阻塞` | 现有目标评价读取 dijk 标准化缓存，与 Raw83 源年训练不同源 | 用 Raw83 同源路径重跑前，现有目标读数无效 |

## 三、Claude 直接接手表

Claude 只能在“接手条件”满足后领取；领取前重新读取本表并执行目标路径 `git status --short`。状态仍为“保留所有权”时不得接管。

| 任务 | 接手条件 | 可修改文件 | 必须完成 | 禁止事项 |
| --- | --- | --- | --- | --- |
| P-S1 计划 | 主控明确分派且目标目录无他人修改 | 只限新建的 S1 计划、证据记录和后续获批文件 | 以源年 B00 正信号、B10 负信号和强负交互为病灶，冻结 `[mean,max]` 线性头、`[mean,mean]` 同参数控制、LSPR23 划分、训练预算、配对重抽样与停止门 | 不实现、不运行、不读取 LSPR24；不得把 S0 目标描述结果用于选结构、权重或阈值 |
| E-CUDA／E-TABM／E-FT 状态核对 | 用户当轮授权服务器可用，主控确认无其他监控者 | 只读远端状态；结果写入获批的独占运行报告，不直接写本表 | 核对运行身份、`status.json`、日志、进程、持久会话、检查点和清单，给出完成、可恢复、失败或不存在之一 | 不重启、不恢复、不删除、不回显凭据；状态未核前不得创建新身份 |
| D-TABLE／D-RESNET | I-2X2 完成且主控另行分派 | 由新计划精确声明 | 一次只修一个已证缺陷并用原始制品验证 | 不与当前实现、审计或运行共享写状态 |

## 四、当前冻结合同

- 有效四格：`C00=B00+max`、`C01'=B00+mix`、`C10=B10+max`、`C11'=B10+mix`，其中 `mix=0.5×mean+0.5×max`。
- 数据：当前 S0 只读 LSPR23，目标数组读取计数必须为 `0`；逐流概率只驻内存，不写逐样本或实体身份明细。
- 裁决：实体 AP、最大实体 AP、共同整数 FP 预算终端和首次告警曲线；不拆并列、不插值，名义与实际 FPR 分列。
- 停止：两个混合格均无信号或被对应 max 格支配时停止，不进入 S1，不读取 LSPR24。
- 训练：当前 S0 不训练、不更新参数、不写新检查点；父 `B00/B10` 只读且先核哈希。
- 科学表述：只可写“受多算子互补现象启发的固定无参数代理，实验待证”，不得称 `Lp+max`、分数层 CPA、首创或已有效。
- S0 结果：源年 B00 侧为非支配信号，B10 侧混合被支配，实体 AP 交互 `-0.154773`；目标描述两侧 AP 增益为正，但 `independent_test=false` 且四格最佳低于旧 O11。
- S1 边界：本表只开放规划，不授权实现；S1 必须用独立计划重新冻结并获批。

## 五、远端防重复与证据边界

- 本轮恢复文档精简未访问服务器；表中远端状态均须重新核验。
- [TabM32 运行交接](2026-08-25-TabM32运行交接.md)保存唯一身份和恢复边界；[第三章全量进度快照](2026-08-25-第三章实验全量进度快照.md)保存 8 月 25 日最后核验状态。
- GRANDE 零重训发布修复已完成，不再列为活动运行。旧剩余时间、瞬时 GPU、旧连接失败和已关闭探针事故只在专项报告追溯。
- 服务器离线、回收或资源不足不算科学失败。任何恢复都必须保持原配置、代码、数据、种子、检查点和运行身份一致，否则保留旧目录并停止。

## 六、所有权与写回协议

1. 同一任务同一时间只允许一个文件所有者；接管前必须由当前所有者或主控明确释放。
2. 修改文档前执行目标路径状态检查；目标有他人修改时停止，不覆盖、不暂存、不提交。
3. 提交只含任务表声明的文件；禁止 `git reset --hard`、`git checkout --`、清空共享暂存区或删除唯一制品。
4. 状态变化直接更新任务行、证据链接和下一动作，不追加聊天记录、预计时间或逐分钟心跳。
5. 完成必须链接提交、原始状态或制品；工程完成、运行完成与科学裁决分列，不以代理自报替代验收。

## 七、当前证据索引

- [第三章恢复卡](RWKV第三章恢复卡.md)
- [有效 2×2 实验计划](2026-08-26-第三章有效2x2机制优化计划/有效2x2实验计划.md)
- [有效 2×2 实施与结果报告](2026-08-26-第三章有效2x2机制优化计划/实施与结果报告.md)
- [四篇漂移文献审计](2026-08-26-四篇漂移文献机制优化审计/审计结论.md)
- [XGBoost 配方来源审计报告](2026-08-26-XGBoost同数据集论文配方来源审计/审计报告.md)
- [第三章全量进度快照](2026-08-25-第三章实验全量进度快照.md)
- [故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)
- [恢复文档精简审计](2026-08-26-恢复文档精简/审计报告.md)

## 八、历史追溯

- 精简前 `719` 行看板固定在基线 `5a9bc596f80b9b6c8dec9dc29e361f10ac13ef1d`：`git show 5a9bc59:.Codex/docs/RWKV/2026-08-20-第三章神经骨干跨进程协作看板.md`。
- 已关闭事故分别见[第三章全量进度快照](2026-08-25-第三章实验全量进度快照.md)、[门禁与探针缺陷记录](2026-08-25-门禁精简与探针缺陷记录.md)和[故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)。
- 历史消息只用于制品追溯，不得覆盖本表、章节恢复卡或更新的原始状态。
