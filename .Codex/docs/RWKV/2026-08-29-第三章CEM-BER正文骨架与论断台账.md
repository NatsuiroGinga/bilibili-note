# 第三章 CEM-BER 正文骨架与论断台账

<!-- RESEARCH_ROUTE=RWKV -->

- 日期：2026-08-29
- 状态：**骨架与台账，非正文**。按 `thesis/chapters/AGENTS.md` 的 `W1 论断台账` 要求，
  每条主张先登记证据状态，四格出结果后才按 `W4 结果接入` 填数。
- 现有 `thesis/chapters/第三章-CPA-ELP/` 是 CPA-ELP 的完整定稿（107 KB），
  重锚后大部分需重写；**在四格判据未出前不得改动它**。

## 一、为什么现在只做骨架

`thesis/AGENTS.md`：正式章节只写「实验支持」的核验结论。当前四格状态（2026-08-29 实测）：

| 格 | 状态 | 源年实体 AP | 最佳轮 |
| --- | --- | ---: | ---: |
| C00 | 完成（`finished`、`exit_code=0`） | `0.7887951742121221` | 17 |
| C10 | 完成（`finished`、`exit_code=0`） | `0.8150313407035376` | 17 |
| C01 | 训练中 | — | — |
| C11 | 未启动 | — | — |

四格判据（两单机制均超 C00、C11 最佳、交互为正）**只有第一条 `C10 > C00` 成立**
（增量 `+0.0262361665`）；`C01 > C00`、`C11 ≥ max` 与交互 `I > 0` 三条仍不可评估，
目标年读数亦未取得。故本文件仍不含任何方法有效性表述。

## 二、章节骨架（对标朱焱雷第三章的 22 个子节结构）

沿用既有写作规范的节标题风格（带限定语、子节以动词性名词收尾），
按 CEM-BER 的机制内容重排：

```
3.1 引言
3.2 问题定义与跨年度漂移建模
    3.2.1 实体级检测任务的数学描述
    3.2.2 逐流判别与实体级部署口径的错位分析
    3.2.3 严格过去可用信息的形式化界定
3.3 CEM-BER 方法框架设计
    3.3.1 总体结构
    3.3.2 因果实体记忆的读写机制设计
    3.3.3 预算感知实体排序的训练目标构造
3.4 算法实现与模型适配
    3.4.1 CEM-BER 训练算法流程描述
    3.4.2 严格过去接口与实体链调度的实现细节
    3.4.3 复杂度分析与在线推理适配
    3.4.4 检查点与断点恢复的状态完整性设计
3.5 理论分析
    3.5.1 因果记忆读写顺序的严格过去性证明
    3.5.2 零门退化的逐位等价性分析
    3.5.3 多预算 CVaR 目标的无偏估计条件探讨
    3.5.4 主任务优先梯度合成的一阶不变量分析
3.6 实验评估与分析
    3.6.1 实验设置
    3.6.2 跨年度综合检测性能对比
    3.6.3 双选轮协议的选择敏感性分析
    3.6.4 双机制消融实验
    3.6.5 记忆槽数与预算网格的敏感性分析
    3.6.6 实体链长度分层的检测效果分析
    3.6.7 受限观测下的首次告警能力验证
    3.6.8 训练与推理资源开销分析
3.7 本章小结
```

**与 CPA-ELP 旧结构的差异**：3.4.4 由「逐骨干挂载点」改为「检查点与断点恢复」
（单基座后不再有逐骨干问题，而状态完整性是 CEM 的实质工程内容）；
3.5 四个子节全部换成 CEM-BER 的理论对象；3.6.3 新增双选轮协议分析。

## 三、论断台账（`W1`）

每条主张登记：允许表述、禁止强化、证据状态、制品路径。

### 已有实验支持的主张

| # | 主张 | 允许表述 | 证据 |
| ---: | --- | --- | --- |
| 1 | 逐流指标不适合作选轮依据 | 「逐流平均精确率自第三轮起即饱和于 99.8% 以上，在训练末段几乎不再具有区分度；同期实体平均精确率仍在大幅变动，其后十轮的均值为 0.6073、标准差为 0.0843、极差达 0.3380」 | C00 运行日志与 `receipts/selection.json`，20 轮逐轮读数 |
| 2 | 两条选轮规则选中不同检查点 | 「按实体指标择优在两个格上均选中第十七轮；按逐流指标择优则在裸模型上选中第十六轮、在引入因果实体记忆后选中第二十轮。改用逐流指标选轮，裸模型的实体平均精确率下降 13.49 个百分点，引入记忆后下降 4.03 个百分点」 | C00 与 C10 的 `receipts/selection.json` 的 `history` |
| 2b | 逐流选轮在不同配置间不稳定 | 「逐流指标选出的轮次随配置改变（第十六轮与第二十轮），而实体指标在两种配置下一致地选中第十七轮」 | 同上 |
| 3 | 实体历史在数据中普遍存在 | 「52.05% 的实体拥有两条以上的流，这些实体承载 99.56% 的流量；多片段实体占 2.22% 却覆盖 96.09% 的流」 | `ch3-lspr23-entity-history-availability-v1` 收据 |
| 4 | 攻击实体的流链显著更长 | 「恶意实体流链长度中位数为 112，良性实体为 2」 | 同上 |
| 5 | 零门退化逐位成立 | 「关闭记忆开关后，模型输出与裸主干在探测片段上逐位相等」 | C00 运行日志的零门断言 |

### 待四格结果裁决的主张（**当前一律不得写入正文**）

| # | 待验证主张 | 裁决条件 | 若不成立的处置 |
| ---: | --- | --- | --- |
| 6 | 因果实体记忆提升跨年度实体检测 | `C10 > C00` 严格成立——**源年已成立**（`0.8150313407035376` 对 `0.7887951742121221`，增量 `+0.0262361665`） | **仍不得写入正文。** 源年成立只完成裁决条件的一半：本主张含「跨年度」，而目标年读数尚未取得，须等 LSPR24 封印后描述性评价。在此之前只能记为源年单条判据成立，不得表述为机制有效 |
| 7 | 预算感知实体排序提升跨年度实体检测 | `C01 > C00` 严格成立 | 同上，机制二降级 |
| 8 | 两机制互补 | `C11 ≥ max(C10, C01)` | 撤回「联合算法」称谓，改写为「两机制组合」 |
| 9 | 两机制超可加 | 交互 `I = C11 − C10 − C01 + C00 > 0` | 只写「联合最佳」，不写「协同」或「超可加」 |
| 10 | 方法在同池实体运营向量上非支配 | 目标年封印后评价 ＋ 帕累托核验 | 按落后读数四步法如实交代 |

### 禁止表述（沿用既有写作规范）

- 反引号包数字、加粗、句中夹页码、脚本名与路径、局限类词；
- 「全面优于」「全面领先」「全面超越」——有落后按四步法写出来；
- 「首次把实体历史用于 Transformer」「首次做多示例部分 AUC」——
  文献审计已裁定这两类主张的边界已被占用；
- 把参数量差距当作落后读数的辩解。

## 四、四格结果落位表（待填）

四格齐备后按此表填数，再进 `results-analysis` → `results-report`：

| 格 | 源年实体 AP | 目标年实体 AP | 目标年逐流 AP | 4% 预算检出率 | 参数量 |
| --- | ---: | ---: | ---: | ---: | ---: |
| C00 裸 FT | 0.7887951742121221 | 待填 | 待填 | 待填 | 924,283 |
| C10 ＋因果实体记忆 | 0.8150313407035376 | 待填 | 待填 | 待填 | 1,075,196 |
| C01 ＋预算感知实体排序 | 待填 | 待填 | 待填 | 待填 | 924,283 |
| C11 因果状态共享 | 待填 | 待填 | 待填 | 待填 | 1,075,196 |

源年实体 AP 两格均取实体规则选中的第 17 轮，`target_reads = 0`，compile 路径口径。

## 五、动作序列

1. 等 C01 与 C11 跑完（守护脚本幂等接力，C00 与 C10 已完成会被自动跳过）；
2. `ch3_ft_lspr24_descriptive_eval.py --dry-run` 核验封印与判据；
3. 接线目标年前向，产出 LSPR24 描述性读数。该工具的目标年前向仍未实现，
   两项已查明的口径事实见[四格进度](2026-08-28-CEM-BER四格实验进度.md)第七节：
   原 docstring 里「不能读 `X24.npy`」的警告对 FT 不适用，且 `E24.npy`／`T24.npy`
   不存在、实体链须从 `s24`／`d24`／`t24` 推导并先断言序列不跨实体；
4. `ch3_build_metrics_table.py` 出统一总表（`ft_dual_selection_v1` 适配器已就位）；
5. `ch3_common_first_alert_fp_budget_envelope.py` 出误报预算曲线与首次告警；
6. `results-analysis` → `results-report` → 按本台账填正文 → `citation-verification`
   → `writing-anti-ai` → `paper-self-review`。
