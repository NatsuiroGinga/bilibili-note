# 方案 A/B 数学阻断项二次修订记录

- **日期**：2026-08-06
- **输入复审**：[方案 A/B 数学阻断项最终复审](2026-08-06-方案AB数学阻断项最终复审.md)
- **修订对象**：[方案 A](2026-08-05-方案A第三章-受序动态记忆与正常状态生成.md)、[方案 B](2026-08-05-方案B第三章-字段交互与动态状态演化.md)
- **任务边界**：只修订数学、状态、消融、预算、边界与待核验措辞；未迁移数据合同，未实现代码，未运行训练、测试驱动开发或 Prettier

## 1. 修订结论

方案 A 的 1 项重要问题与 2 项次要问题均已在文档层关闭。方案 B 的完整状态数学阻断、官方公式方向、主消融配对和零生成目标分支均已在文档层关闭；`B-I3` 的过强措辞与放行边界已关闭，但定向一级文献核验属于尚未产生的外部证据，继续作为章级贡献冻结的硬阻塞。

本次修订不改变最终实验许可：两份方案都仍是数学修订候选，必须经过新的独立复审；在数据 G0、公式与代码映射、状态适配器及运行期收据完成前，均不允许实现或训练。

## 2. 方案 A 逐项关闭情况

| 编号 | 级别 | 修订内容 | 二次状态 |
| --- | --- | --- | --- |
| `A-P1` | 重要 | 固定单头列向量形状；按官方提交写成 \(\mathbf G_t=\operatorname{diag}(\mathbf w_t)-\widehat{\boldsymbol\kappa}_t(\mathbf a_t\odot\widehat{\boldsymbol\kappa}_t)^{\mathsf T}\)、\(\mathbf B_t=\mathbf v_t\widetilde{\mathbf k}_t^{\mathsf T}\)、\(\mathbf S_t=\mathbf S_{t-1}\mathbf G_t+\mathbf B_t\)；放行收据要求非对称、不可交换矩阵样例 | **文档层关闭** |
| `A-M1` | 次要 | 官方归一化改为 \(\max(\|\boldsymbol\kappa\|_2,10^{-12})\)，只主张 \(\|\widehat{\boldsymbol\kappa}\|_2\leq1\)；冻结 FP32、绝对／相对容差、最小可表示区间和五类故障注入 | **文档层关闭** |
| `A-M2` | 次要 | 定义 \(N_{\mathrm{gen}}\)；零合格目标时生成项不可用、禁止除法与零损失填充，联合目标退化为仅检测损失，并记录目标计数与可用性 | **文档层关闭** |

方案 A 另补齐了与官方状态接口一致的完整跨块状态：每层共享一份 TimeMix 移位状态、一份 ChannelMix 移位状态，每头维护快、慢两份 WKV 矩阵状态；三类状态必须同步携带、停止梯度和重置。第 13 节已改为七项可验收放行条件，不再从数学文档直接跳到训练。

## 3. 方案 B 逐项关闭情况

| 编号 | 级别 | 修订内容 | 二次状态 |
| --- | --- | --- | --- |
| `B-S1` | 严重 | 定义 \(\mathcal X_t=\{\mathbf x^{\mathrm{tmix}}_{t,\ell},\mathbf S_{t,\ell,h},\mathbf x^{\mathrm{cmix}}_{t,\ell}\}_{\ell,h}\)，以完整 \(\mathcal F_7\) 覆盖逐层矩阵、两类移位状态、多层残差和值残差依赖；用时变 \(\Lambda_t,\Gamma_t\) 给出可离线复算的有限时域乘积递推 | **文档层关闭** |
| `B-P1` | 重要 | 与方案 A 使用同一冻结官方提交和同一列向量、右乘、秩一外积约定，删除原错误转置方向 | **文档层关闭** |
| `B-I1` | 重要 | 为 `B10-state/B10-current`、`B11-state/B11-current` 冻结相同 RWKV 结构与状态字节、参数 ±3%、总每批计算 ±5%；收据必须含参数、总计算、状态摘要计算和同硬件吞吐 | **文档层关闭** |
| `B-I3` | 重要 | 明确候选章名仅为工作标签，一级文献核验是独立硬阻塞；收据完成前禁止冻结章级贡献、最终方法名或标题中的强贡献措辞 | **修订责任关闭，外部证据未关闭** |
| `B-M1` | 次要 | 定义 \(N_{\mathrm{gen}}\)；零合格目标时生成项不可用，联合目标退化为仅检测损失，禁止 `NaN` 与缺失目标零损失填充 | **文档层关闭** |

原已关闭的 `B-I4` 保持不变：`B-GR0/B-GR1` 继续按对应生成设置、相同 RWKV-7 状态核、参数 ±3% 和计算 ±5% 作为强基线；无法匹配时只能作描述性比较。

方案 B 删除了用单个 WKV 矩阵代表完整闭环的主张。新的有限时域界覆盖全部持久状态，但不主张全局收缩、渐近稳定或几何衰减；即使观测轨迹上 \(\Lambda_t<1\)，也不得外推为全局定理。单矩阵三项差分仅保留为给定层输入时的局部诊断。

## 4. 未迁移与未执行事项

- 未把尚未最终放行的 LSPR24 或 UGR'16 数据合同迁移到两份方案。
- 未创建或修改任何实现代码、配置、测试、启动器或运行目录。
- 未运行训练、基线、消融、数据下载、测试驱动开发或 Prettier。
- 未完成方案 B 定向一级文献核验，因此不产生新颖性通过结论。
- 未生成参数、计算量、状态字节、吞吐或显存实测收据。

## 5. 剩余阻塞

1. 新的独立数学复审尚未执行；本记录不能替代复审裁决。
2. 两份方案的官方公式与代码映射收据、非对称矩阵方向测试及显式状态输入／输出适配器尚未实现。
3. 方案 A 的 FP32 门界故障注入、`C0-P/C0-C` 预算收据尚未产生。
4. 方案 B 的完整状态跨块携带测试、逐步有限时域常数收据、两组当前／状态配对预算和 `B-GR0/B-GR1` 强基线预算尚未产生。
5. 方案 B 的定向一级文献核验仍是章级贡献、新颖性、最终方法名和论文标题冻结的硬阻塞。
6. UGR'16 G0 与后续数据合同仍需独立审计；本次未迁移数据合同。
7. 在以上阻塞关闭前，不允许实现方案候选或运行训练。

## 6. 验证记录

- 使用 `rg` 核对两份方案均采用列向量、旧状态右乘和正确的两个秩一外积方向。
- 使用 `rg` 确认方案 B 不再含旧 \(L_{\mathrm{cl}}\) 收缩式，并显式含三类聚合状态、逐步 \(\Lambda_t,\Gamma_t\) 与有限时域乘积递推。
- 使用 `rg` 确认两份方案均含 \(N_{\mathrm{gen}}=0\) 的仅检测分支、预算失败降级边界和不实现／不训练边界。
- 统计三份文档的 `\[` 与 `\]` 数量并逐文件核对相等；检查无行尾空白、制表符或控制字符。
- 本任务是纯 Markdown 数学文档修订，按任务边界未运行 Prettier。

## 7. 变更文件

- 已修改：[方案 A](2026-08-05-方案A第三章-受序动态记忆与正常状态生成.md)
- 已修改：[方案 B](2026-08-05-方案B第三章-字段交互与动态状态演化.md)
- 已新建：[本二次修订记录](2026-08-06-方案AB数学阻断项二次修订记录.md)
- 其他文件：未修改
