# Claude Code `RWKV路线跨会话交接 (fork 2)` 接管计划

## 目标

核清 Claude 会话终止时所有未完成工作，优先验收已启动实验，再完成缺失方案、必要修正和第三章收尾；禁止重复启动或依据中间状态下结论。

## P0：先核结果

- [x] 卷积等总参数量 2×2：已确认失败，无科学结果
- [x] 跨基座 RWKV-7 四格：已确认未启动
- [x] 等参数基线：已确认完成并回收本机
- [x] 排序键事实审计：已完成；`available_ns` 消融由用户暂停，不实施、不启动、不作为选型前置门禁

## P1：补缺方案与修正

- [ ] 完成 XGBoost 适配 CPA 与 ELP 的 GPU 冻结方案并提交用户审阅
- [ ] 修正 `xgb_reference` 中借用 Lp 口径的误导性命名，保留历史计算语义
- [ ] 将所有新结果写入第三章实验台账与当前恢复文档

## P2：第三章收尾

- [ ] 按已冻结写法重写 3.6.2
- [ ] 重绘失效图件并更新 `图件清单.json`
- [ ] 执行第三章 `paper-self-review` 六项门禁

## 已确认完成，不重复执行

- 四骨干结果判读：感知机、门控循环、全注意力、一维卷积已有合法结果
- 告警预算曲线分析及指标复现
- 序列排序键事实审计
- XGBoost `51.29%/69.28%` 与借用 Lp 的 `53.19%/81.65%` 口径辨析
- 现有 XGBoost 基线不重跑：主表固定使用自身 `max` 聚合口径；借用 C11 的 `p` 仅作敏感性分析

## 状态

**P0 状态核验已完成，当前执行 P1**：编写 XGBoost 双机制方案与共同基座选择合同；卷积失败修复与 RWKV-7 尚未启动；排序键消融已暂停且不再列入待办。
