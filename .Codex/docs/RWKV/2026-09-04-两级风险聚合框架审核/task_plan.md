# Task Plan：第三章「两级风险聚合框架」证伪式审核

## Goal
对 `thesis/methods/第三章-两级风险聚合框架.md`（提交 `3f7d36d`）做文献＋代码＋实验三环审核，
给出「相容点存在」与「软 top-k 新颖性」的裁断、阻断项清单、一句总裁定。
立场是证伪；任一环把方案打死即停。

## Phases
- [x] Phase 0: 建计划与笔记，读生产代码（tail_aggregate / _combine_and_step / gradient_controller）
- [x] Phase 1（最便宜证伪点）: 真实袋分布 → **未能证伪**，作用面 32.19%，不是「极低」
- [x] Phase 2（文献）: literature-reviewer / opus 已回；Q1 结论经主代理独立数值复核通过
- [x] Phase 3（代码）: §四 四项核验完成；三条自测逐位复现
- [x] Phase 4: 交付文档 `.Codex/docs/RWKV/2026-09-04-两级风险聚合框架审核.md` 已落盘提交

## 最终裁定
机制未被打死；**新颖性主张整体撤回**（算子 = RU 2002 式 (25)，独立复核最大相对误差 `1.247e−15`）。
阻断项 `5` 条（A1/A2/A3/A9/A11）、须改 `5` 条（A5/A6/A7/A10/A12）、`0` 条致命。
简报预设的两个杀点（A8 作用面极低、A7 c 缩放致命）**均未成立**，已如实记录。

## 已测结论（Phase 1/3）
- E1 `bag_alpha_reach.py`：α∈(0,1/2] 内行为改变面上确界 = `0.3219`（训练侧）/`0.3163`（验证侧）。
  **未证伪**。m=1 占 `46.22%`、m=2 占 `21.59%`。n_neg=64 下每步约 `20.6` 个负实体向 α 回传。
- E2 `alpha_controller_interaction.py`：`combined[α] == c·g_rank[α]` **逐位成立**；c=0 步上精确 0。
  `∂S_e/∂α = (s_⌊t⌋ − S_e)/α ≤ 0` 恒成立（200/200）。边界处 clamp 打平点梯度泄漏。
  α 不注册进 model.parameters() 时位移精确 0。
- E3 `alpha_param_bitwise.py`：多一个恒零坐标不改 c（四格安全）；α 须常量初始化否则 RNG 流平移；
  Adam 在 90% 零梯度占空比下 50 步仍移动 `+0.1619` ⟹ **c=0 是减速不是阻断**。

## Key Questions
1. α 在 (0, 1/2] 内移动时，实际改变行为的实体占比是多少？若极低 ⇒ 判据 2 结构上不可能成立。
2. 插值式软 top-k 是否早有成熟先例？若有，本方案「新颖性」主张须收窄。
3. α 的梯度是否被梯度控制器 c 缩放？若是 ⇒ 与已证伪方案同型。
4. 主代理三条自测是否可复现？

## Decisions Made
- （待填）

## Errors Encountered
- （待填）

## Status
**Phase 0** — 建档中
