# B76 磁盘释放只读审计笔记

## 已知输入

- B76 当前可用空间：`21,235,464 KiB`；已用比例：`80%`。
- CUDA 资格入口要求：可用空间不少于 `25 GiB`，且精确已用比例严格低于 `80%`。
- 精确读数为已用 `83,622,136 KiB`、总量 `104,857,600 KiB`、可用 `21,235,464 KiB`，已用比例为 `79.7483%`；比例门当前通过。
- `25 GiB` 等于 `26,843,545,600` 字节，当前可用空间等于 `21,745,115,136` 字节，故仅空间门还需释放 `5,098,430,464` 字节，约 `4.7487 GiB`。
- Raw83 P5 产品约 `16.56 GiB`，必须保留。

## 规则性结论

- B76 使用 `GPU_SSH_ACTIVE` 显式关闭 `ProxyCommand` 与 `ProxyJump` 后，经项目内 `gpu_env_quiet.exp` 直连。
- 盘点限定为七类目录；不得把原始数据、必要 Dijk 缓存、未回收检查点或论文证据列为可直接删除对象。
- 本报告只给出候选与删除前置核验，不执行清理。

## 待补充证据

- 远端各目录实际字节数、完成状态和清单线索。
- 本地是否已有完整回收副本及可核验哈希。
- 是否存在明显的可再生缓存或临时审计目录。

## 远端只读盘点结果

- 文件系统为 `104,857,600 KiB`，已用 `83,622,144 KiB`，可用 `21,235,456 KiB`，占用 `80%`。
- 项目根为 `83,175,798,783` 字节；各受限范围均以 `du -sb` 读取。
- 最大一级子目录中，Raw83 共享产品为 `16,952,934,536` 字节，Dijk 复现缓存为 `14,456,764,859` 字节；二者均为强制保留项。
- 虚拟环境为 `6,988,954,589` 字节，其中 `lib` 为 `6,960,682,139` 字节；它是 CUDA 训练依赖，不属于可删候选。
- 同名本机基线目录仅约 `3,862,528` 字节，远小于远端 `2,730,007,405` 字节，因此不是完整回收副本。
- 基线历史运行制品 A 状态为 `finished`，存在最新检查点和最佳模型；基线历史运行制品 B、C 状态均为 `completed`，均存在最新检查点。
- 候选历史运行制品 A 状态为 `finished`、退出码 `0`、决策为不晋级、仅用于筛选且无检查点；候选历史运行制品 B 状态为 `running`。
- 诊断历史运行制品 A 状态为 `running`。另有三项大历史目录无状态文件，不能判定完成。
- 在诊断、候选、基线范围内，未发现目录名明确为临时、缓存或审计的可再生目录。
- 当前 CUDA Raw83 与 N14 入口及配置未引用宽视图数据产品；仅历史筛选入口引用它。这不能证明其可删，仍须由自身数据清单和回收状态裁决。

## 证据限制

- 未识别到可证明已在本机完整回收且可按清单逐项 SHA-256 比对的远端大制品。唯一同名本机目录明显不完整。
- 大目录中未发现可用于逐项哈希核验的清单文件；后续回收必须先生成或取得正式清单，再执行逐项 SHA-256 比对。
