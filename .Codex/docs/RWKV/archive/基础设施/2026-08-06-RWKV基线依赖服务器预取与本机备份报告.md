# RWKV 基线依赖服务器预取与本机备份报告

- **日期**：2026-08-06
- **状态**：预取与本机回收完成；未安装、未编译、未启动训练
- **服务器**：仅使用旧服务器 `GPU_SSH/GPU_PWD`；未访问已关机的新服务器
- **服务端根**：`/root/autodl-tmp/thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/`
- **本机根**：`/Users/bilibili/personal/note/thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/`
- **成功运行**：`rwkv-baseline-deps-prefetch-20260806t030118z-attempt3`

## 1. 结论

本次已完成 4 个固定提交的官方源码快照、21 个 Linux 离线依赖制品、环境与许可证审计、制品清单、压缩包、本机回收和双端 SHA-256 核验。没有下载 `torch`、预训练权重、原始数据、运行结果、`.venv`、检查点或临时编译缓存，也没有安装依赖或启动训练。

依赖就绪度必须分开判断：

1. HGB 与 XGBoost 已存在于正式 `.venv`，依赖层面可直接进入后续基线实现预检。
2. GRU、TCN 和因果 Transformer 已有 `torch 2.13.0+cu130` 等核心依赖；本任务未实现或启动模型。
3. TimesNet 的官方源码已冻结，缺失顶层包已预取，但必须先执行离线试装并核对传递依赖，不能直接宣布可运行。
4. Mamba、`causal-conv1d` 和官方 RWKV CUDA 核只保存源码与构建依赖。服务器 `nvcc` 为 `null`，因此当前不能编译 CUDA 扩展。

## 2. 环境审计

### 2.1 服务器与工具链

| 项目 | 核验值 |
| --- | --- |
| 架构 | `x86_64` |
| 系统 | Ubuntu 系，Linux 5.15，glibc 2.35 |
| Python | `3.10.12` |
| uv | `0.11.29` |
| GPU | NVIDIA GeForce RTX 5090，32607 MiB |
| 驱动 | `580.105.08` |
| GCC | `11.4.0` |
| nvcc | `null`，没有可核验输出 |

### 2.2 Python 口径差异

受管启动器调用的系统 `python3` 没有加载项目 `.venv`，其审计显示 `torch` 和主要数值包未安装。随后对正式环境 `/root/autodl-tmp/thesis/experiments/llm_probe/.venv/bin/python` 做了只读复核：

| 包 | 正式 `.venv` 版本 |
| --- | --- |
| torch | `2.13.0+cu130`，`torch.version.cuda=13.0` |
| numpy | `2.2.6` |
| pandas | `2.3.3` |
| pyarrow | `25.0.0` |
| scikit-learn | `1.7.2` |
| xgboost | `3.2.0` |
| scipy | `1.15.3` |
| einops | `0.8.2` |
| sympy | `1.14.0` |
| transformers | `4.57.6` |
| triton | `3.7.1` |
| ninja | `1.13.0` |
| packaging | `26.2` |
| setuptools | `83.0.0` |

正式 `.venv` 缺少 `PyWavelets`、`matplotlib`、`sktime`、`local-attention`、`reformer-pytorch`、`lightning`、`pytorch-lightning`、`deepspeed` 和 `wheel`。系统 Python 的 `wheel 0.37.1` 没有进入正式 `.venv`，且本次因系统口径被判为已安装而没有重复下载。由此，当前离线包目录是面向现有服务器的顶层依赖备份，不是可独立重建全新环境的完整轮子仓库。

## 3. 官方源码锁

| 源码 | 官方仓库 | 提交 | 许可证 | 大小 | SHA-256 |
| --- | --- | --- | --- | ---: | --- |
| RWKV-LM | `BlinkDL/RWKV-LM` | `952102498e9ed367ea0a59ee64106916d474d30f` | Apache-2.0 | 23,022,290 B | `f9213d56ce1b1903717287ddbac92872088119315b65ce431a44a92df151d66a` |
| TimesNet 官方实现所在仓库 | `THUML/Time-Series-Library` | `4e938a1767106324dd753b2a44832bf870a0252e` | MIT | 1,681,438 B | `9e081377ab359cc31145a7e91d0d00018c2c3f1be7495dc6c50f393d9703de91` |
| Mamba | `state-spaces/mamba` | `e9594ce1c732d97440f0332fdc43170a2294dbfa` | Apache-2.0 | 1,732,630 B | `e2384aacc82612f33cae5a52929f2c7ac300cebf3d2d45a042f03befa0f3e8a6` |
| causal-conv1d | `Dao-AILab/causal-conv1d` | `4f6ae4e26ae5fe8af9372f8d312ab25cc4595223` | BSD-3-Clause | 36,350 B | `16f99e95be40fbcf38f8e1d604a3e7df2ed48b2fec8ad23b1accfda49a8a9c5d` |

四项均通过 GitHub 官方 `codeload` 的固定提交 URL 下载，采用断点续传和有限退避重试；归档顶层目录均核对包含完整 40 位提交。它们是提交归档，不描述为 Git 工作树。

## 4. 依赖制品

清单共记录 23 项依赖决策：下载 21 个制品，其中 20 个轮子、1 个 `deepspeed-0.19.3.tar.gz` 源代码包，总计 529,070,759 B；系统 Python 已有的 `setuptools 59.6.0` 与 `wheel 0.37.1` 被跳过。完整文件名、大小和 SHA-256 见本机制品目录中的 `manifest.json` 与 `manifest.sha256`。

关键规则已经满足：

- `torch` 没有下载，正式 `.venv` 保留 `2.13.0+cu130`。
- 没有下载 Mamba、RWKV 或 Transformer 预训练权重。
- 没有强装 `mamba-ssm` 或 `causal-conv1d` 轮子。
- Mamba 与 `causal-conv1d` 固定为 `server_source_build`，但 `nvcc=null` 使当前编译门禁失败。
- 下载包使用服务器当前 Linux x86_64/Python 3.10 解析；通用纯 Python 包除外。

需要谨慎处理的版本差异：离线目录中的 `transformers 5.14.1`、`tqdm 4.70.0` 和 `packaging 26.3` 高于正式 `.venv` 的 `4.57.6`、`4.68.4` 和 `26.2`。后续不得批量升级现有正式环境，必须只安装明确缺失且通过离线试装的包。

## 5. 空间与完整性

| 项目 | 值 |
| --- | ---: |
| attempt3 恢复开始时可用空间 | 50,256,941,056 B |
| 有效载荷完成后可用空间 | 49,701,031,936 B |
| 服务端载荷增量 | 555,909,120 B |
| 回收压缩包大小 | 561,117,646 B |
| 压缩包 SHA-256 | `aaaf24fc30b286607316cf7aeee055e86d23e0ee6d1b9cd414b007c70f28b873` |
| 内部哈希条目 | 60 |

白名单 `rsync` 未使用 `--delete`。本机压缩包通过服务端提供的 `.sha256` 核验；解包后 `manifest.sha256` 的 60 个条目全部通过。本机禁止目录核验没有发现 `.venv`、`runs`、数据、检查点或权重目录。

本机关键路径：

- 压缩包：`thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/rwkv-baseline-deps-prefetch-20260806t030118z-attempt3.tar.gz`
- 解包目录：`thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/rwkv-baseline-deps-prefetch-20260806t030118z-attempt3/`
- 主清单：`thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/rwkv-baseline-deps-prefetch-20260806t030118z-attempt3/manifest.json`
- 哈希清单：`thesis/experiments/llm_probe/artifacts/rwkv-baseline-deps/rwkv-baseline-deps-prefetch-20260806t030118z-attempt3/manifest.sha256`

## 6. 失败与恢复记录

1. 本机首次调用未加载旧服务器环境变量，受管入口在同步前拒绝；未产生远程改动。
2. 沙箱网络拒绝 SSH；获批后才在沙箱外通过同一受管入口执行。
3. 首次远程脚本在制品父目录不存在时提前调用磁盘统计，下载前失败。增加“先创建并验证固定输出根可写”的回归测试后恢复。
4. attempt2 在审计缺失的 `nvcc` 时抛出 `FileNotFoundError`，下载前失败。修复为缺失可选命令记录 `null`。
5. attempt3 的 `git fetch --depth 1` 在约 16 MiB 后因 GitHub RPC `curl 18`、`early EOF` 失败。随后在同一提交身份下切换到官方 `codeload` 可续传归档并成功。

失败的 Git 部分下载保存在成功运行目录的 `failed-git-fetch.partial/`，相关状态保存在 `state-before-codeload-resume.json`，日志保存在 `prefetch.log` 和服务端受管包装器目录。没有隐藏或删除失败证据。

## 7. 后续 uv 使用方式

先在服务器执行纯计划检查，不安装：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
uv pip install \
  --python .venv/bin/python \
  --no-index \
  --find-links artifacts/rwkv-baseline-deps/rwkv-baseline-deps-prefetch-20260806t030118z-attempt3/packages \
  --dry-run \
  PyWavelets==1.8.0 matplotlib==3.10.9 sktime==1.1.0 \
  local-attention==1.11.2 reformer-pytorch==1.4.4
```

仅当离线试装没有拟移除或升级 `torch`、`transformers`、`triton`、`numpy`、`pandas`、`pyarrow` 等正式依赖，才允许实际安装 TimesNet 缺失项。`lightning`、`pytorch-lightning` 与 `deepspeed` 应按后续 RWKV 实现计划的真实导入边界另行筛选，不能因为已经下载就全部安装。

Mamba 与 `causal-conv1d` 后续只能从冻结源码构建。当前必须先补齐并核验 CUDA 13.0 对应 `nvcc`、正式 `.venv` 的 `wheel` 与构建隔离依赖；在此之前不提供可执行构建命令，避免把不完整工具链误写为可安装状态。

## 8. 验证与未做事项

已执行：

- 最小测试先红后绿：`tests/test_prefetch_rwkv_baseline_deps.py`，最终 `2 passed`。
- 修改脚本的 `bash -n` 语法检查通过。
- 参数 JSON 合同校验通过。
- 四个官方提交归档的顶层提交名、许可证、大小和 SHA-256 核验通过。
- 服务端压缩包与本机压缩包 SHA-256 一致。
- 本机 `manifest.sha256` 60/60 通过。
- 本机禁止目录核验无命中。

未执行：

- 未安装任何包，未修改正式 `.venv`。
- 未编译 Mamba、`causal-conv1d` 或 RWKV CUDA 核。
- 未运行基线、训练、调参、GPU 冒烟或全仓测试。
- 未访问新服务器，未下载预训练权重。

## 9. 阻塞与下一步

1. `P0`：Mamba、`causal-conv1d` 和官方 RWKV CUDA 核缺少 `nvcc`，不能进入编译。
2. `P1`：正式 `.venv` 缺少 `wheel`，本次系统口径又将其误判为已安装，离线构建链仍不完整。
3. `P1`：21 个制品使用 `--no-deps` 下载，适合作为现有正式环境的顶层补充，不足以证明可从空环境完全离线重建。
4. `P1`：后续安装前必须用正式 `.venv` 执行离线 `uv --dry-run`，拒绝任何核心包升级或移除。

首批无 CUDA 扩展基线可继续实施计划；Mamba 与官方 RWKV CUDA 训练必须等待工具链补齐和独立构建门禁。
