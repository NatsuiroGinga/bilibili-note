# 第三章图件修复报告

- 代理：`ch3_figure_repair_opus`（Claude Opus 5，`effort: high`）
- 日期：2026-08-26
- 工单：`.Codex/docs/RWKV/2026-08-26-第三章Claude图表独立审查/审查报告.md`
- 修改前快照：`ef8c3a64420702f572a806eb74bace49d7b59917`（本轮开始时 `thesis/figures/第三章/` 工作树干净，未创建空快照提交）
- 提交：`e8053f4`（图件与脚本）、`42c80ff`（README）
- 范围：只改 `thesis/figures/第三章/` 下的文件与本机 `runs/` 数据副本；**未改动 `thesis/chapters/` 任何正文文件**

结论先给：审查报告列出的七条阻断已关闭六条，第七条（图 3-10）按「无权威数据源不得造图」撤下并登记恢复条件。
十一幅图全部内嵌简体 `SimHei`，全部实测 `400 ppi`、宽 `130–165 mm`、高 `80–95 mm`，
灰度模拟全部可读。图内每个数字都能指回制品字段，检出率口径已对统一指标总表逐位自校一致。

---

## 一、逐图处置

| 图 | 处置 | 内容变化 | 尺寸 | 实测 |
| --- | --- | --- | --- | --- |
| 图 3-1 ~ 3-4 | 重生成 | 内容不变；字体由 `STHeitiSC-Medium` 统一为 `SimHei`，补齐题注行 | 150×95、150×80、140×95、140×95 mm | `SimHei` + `Times New Roman` + `Cambria Math`，400 ppi |
| 图 3-5 | 换底＋改名 | 文件名由「跨年度主性能对比」改为「六个方法在三个指标上的相对位置」，与 `00-目录.md:66` 的 R5 改名一致；改为三面板横条（逐流 AP／实体 AP／4% 预算检出率），六个方法＝五个已发表配置基线＋本章方法（全容量 MLP，BF16） | 165×95 mm | 400 ppi |
| 图 3-6 | 换底 | 四格改用正式 CPA×ELP 四格 BF16 目标年读数；三面板＝实体 AP／逐流 AP／实际假阳率 2% 检出率；**图题与图内文字已删去「超可加」表述**，改为「两个单机制对基线均为正贡献，并用格在目标年四格中最高；交互项只作描述性读数」 | 150×90 mm | 400 ppi |
| 图 3-7 | 换底重定义 | 旧图为三种子逐流 AP 离散度，新协议为单种子（U4），无多种子源。改为四格在源年验证集上的**逐轮**表现与选中轮次 | 140×90 mm | 400 ppi |
| 图 3-8 | 换底 | 旧图只能画终值（旧日志无逐轮轨迹）。新制品 `selection_frozen.json` 有逐轮 `p`，改为真实收敛轨迹 | 130×85 mm | 400 ppi |
| 图 3-9 | 换底重定义＋改名 | 旧图「序列长度敏感性」自陈实验有三处缺陷且源已作废；改为「实体流数分桶敏感性」，取自主运行自身的 `length_buckets` 字段 | 150×90 mm | 400 ppi |
| 图 3-10 | **撤下** | 当前权威制品无任何按攻击类别的分面字段，无法换底重绘；三份文件移入 `作废/`，理由与恢复条件写入 `图件清单.json` 的 `withdrawn` 段与 `作废/README.md` | — | — |
| 图 3-11 | 换底重定义 | 旧图为 k-截断曲线（源已作废）。改为首次告警及时检出阶梯，取自 npz 的 `O11__first_alert__*` | 140×90 mm | 400 ppi |
| 图 3-12 | **新建** | 四条完整告警预算曲线（本章方法＋全注意力／一维卷积／门控循环网络），标出六档实际可达工作点与本章方法 3.72% 可达上限 | 150×95 mm | 400 ppi |
| `图件清单.json` | 重写 | 按生成器分段登记环境、字体、数据源哈希；逐图登记 `data_keys`、`png_ppi_measured`；新增 `withdrawn` 段 | — | — |
| `README.md` | 重写 | 删去已判废的 `180 mm` 与 `Hiragino Sans GB`；补三脚本分工、制品清单、检出率口径、撤下说明 | — | — |
| `占位数据/`、`预览-占位/` | 删除 | 工单授权；其 `placeholder_notice` 自陈「数值全部为人为构造，禁止进入正文」 | — | — |

---

## 二、数据字段映射表

### 制品与哈希（本机副本，已与服务器逐一核对一致）

| 角色 | 路径（相对 `runs/diagnostics/`） | SHA-256 前 16 位 |
| --- | --- | --- |
| 正式 CPA×ELP 四格聚合结果 | `ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1-bf16-v1/aggregate-results.json` | `f9c20e03893aeae7` |
| 正式四格完整告警预算曲线收据 | `…-bf16-v1/complete-alert-budget-curves-receipt.json` | `98e435ff4bec9cde` |
| 正式四格完整告警预算曲线 | `…-bf16-v1/complete-alert-budget-curves.npz` | `3820e783d1b594bc` |
| 正式 CPA×ELP 四格选轮冻结 | `…-bf16-v1/selection_frozen.json` | `025072897b213246` |
| 统一指标总表 LSPR24 评价 | `ch3-metrics-table-20260825b/lspr24-evaluation.json` | `8c60893dc8955548` |
| 统一指标总表溯源 | `ch3-metrics-table-20260825b/provenance.json` | `3355060147b0518a` |
| 已发表神经基线运行结果 | `ch3-published-neural-operational-backfill-v1/aggregate-results.json` | `5dd66aacce33eeaa` |
| 已发表神经基线完整告警预算曲线 | `ch3-published-neural-operational-backfill-v1/complete-alert-budget-curves.npz` | `eb5e91d44029c47e` |

`aggregate-results.json` 的 `f9c20e03…` 与统一总表 `provenance.json` 登记的哈希一致，
也与服务器 `sha256sum` 一致；`complete-alert-budget-curves.npz` 的 `3820e783…` 同样三方一致。

### 逐图键路径

| 图 | 数据来源键路径 |
| --- | --- |
| 图 3-5 | `lspr24-evaluation.json[].{flow_ap, entity_ap, dr_fpr_0.04, actual_fpr_fpr_0.04}` |
| 图 3-6 | `target_year_table.{B00,B10,O01,O11}.{entity_average_precision, flow_average_precision}`；npz `{B00,B10,O01,O11}__{realized_fpr, detection_rate}` |
| 图 3-7 | `selection_frozen.json cells.{B00,B10,O01,O11}.history[].{epoch, validation_flow_ap}` 与 `.selected_epoch` |
| 图 3-8 | `selection_frozen.json cells.{O01,O11}.history[].p`；`target_year_table.{O01,O11}.p` |
| 图 3-9 | `target_year_table.{B00,B10,O01,O11}.length_buckets[].{bucket, entity_average_precision, positive_entities}` |
| 图 3-11 | npz `O11__first_alert__fpr_*__{exposure_index, on_time_detection_rate}`；`target_year_table.O11.first_alert.*.realized_first_alert_fpr` |
| 图 3-12 | npz `O11__{realized_fpr, detection_rate}`；已发表基线 npz `{transformer,cnn,gru}__{actual_fpr, detection_rate}`；`lspr24-evaluation.json[].dr_fpr_*`（自校基准） |

### 位语义与四格数字（进图值）

`B00`＝双关、`B10`＝仅 CPA、`O01`＝仅 ELP、`O11`＝双开，源码核验见
`tools/ch3_full_mlp_complete_entity_lp_protocol_a_q0.py:36-41`。

| 格 | 目标年实体 AP | 目标年逐流 AP | 实际假阳率 2% 检出率 | 实际假阳率 |
| --- | ---: | ---: | ---: | ---: |
| B00 无机制基线 | `0.36463918` | `0.23239890` | `0.46675532` | `0.01992969` |
| B10 仅因果前缀聚合 | `0.44752344` | `0.13996656` | `0.56781915` | `0.01999439` |
| O01 仅实体级幂平均池化 | `0.46408056` | `0.14001613` | `0.56117021` | `0.01999439` |
| O11 双机制并用 | `0.58548960` | `0.13271059` | `0.75000000` | `0.01999439` |

主效应与交互（实体 AP）：A 单开 `+0.0829`、B 单开 `+0.0994`、和 `+0.1823`、并用 `+0.2209`、交互 `+0.0385`。

---

## 三、实测验证输出

### 1. 检出率口径自校（脚本内断言，失败即停止）

```
自校　全容量多层感知机＋完整实体幂平均（BF16）：六档读数与统一指标总表逐位一致
自校　全注意力网络：六档读数与统一指标总表逐位一致
自校　一维卷积网络（主干重建）：六档读数与统一指标总表逐位一致
自校　门控循环网络：六档读数与统一指标总表逐位一致
```

规则：在完整可达曲线上取实际假阳率不超过名义预算的最后一个点。四个方法 × 六档共 24 个
（检出率，实际假阳率）对与统一总表在 `1e-12` 容差内逐位相等。制品里 `dr_at_fpr` 的名义秩位键未使用。

### 2. 尺寸与分辨率（`sips -g pixelWidth -g pixelHeight -g dpiWidth`）

```
图3-1-整体方法框架                 2362x1496  dpi=400.000  印刷 150.0x95.0 mm
图3-2-二IP无向对序列构造           2362x1259  dpi=400.000  印刷 150.0x79.9 mm
图3-3-因果前缀跨流聚合             2204x1496  dpi=400.000  印刷 140.0x95.0 mm
图3-4-实体级可学Lp池化             2204x1496  dpi=400.000  印刷 140.0x95.0 mm
图3-5-六个方法在三个指标上的相对位置  2598x1496  dpi=400.000  印刷 165.0x95.0 mm
图3-6-双机制消融交互               2362x1417  dpi=400.000  印刷 150.0x90.0 mm
图3-7-训练稳定性                   2204x1417  dpi=400.000  印刷 140.0x90.0 mm
图3-8-Lp指数收敛轨迹               2047x1338  dpi=400.000  印刷 130.0x85.0 mm
图3-9-实体流数分桶敏感性           2362x1417  dpi=400.000  印刷 150.0x90.0 mm
图3-11-受限观测下的检测能力        2204x1417  dpi=400.000  印刷 140.0x90.0 mm
图3-12-检出率随告警预算的变化      2362x1496  dpi=400.000  印刷 150.0x95.0 mm
```

全部 `400 ppi`（合同下限 300、推荐 400）；最宽 `165.0 mm`（合同上限 165）；最高 `95.0 mm`（合同上限 140）。

### 3. 内嵌字体（`pdffonts`）

```
图3-1-整体方法框架                HJMCWU+CambriaMath ERUYXO+SimHei HHGBJV+TimesNewRomanPSMT HEKPJH+Cmex10
图3-2-二IP无向对序列构造          BHCQQK+CambriaMath FEAPLY+SimHei EDPINY+TimesNewRomanPSMT
图3-3-因果前缀跨流聚合            DGIJNB+CambriaMath ELWFSG+SimHei CSQWKD+TimesNewRomanPSMT
图3-4-实体级可学Lp池化            GPIUCE+CambriaMath CZMUKS+SimHei CXFFAZ+TimesNewRomanPSMT HEKPJH+Cmex10
图3-5-六个方法在三个指标上的相对位置 EBFDSL+SimHei HEECDI+TimesNewRomanPSMT
图3-6-双机制消融交互              HJALBO+SimHei CGCFMN+TimesNewRomanPSMT
图3-7-训练稳定性                  FYVYHY+SimHei BNHMQP+TimesNewRomanPSMT
图3-8-Lp指数收敛轨迹              BEMWEG+SimHei VCOUVC+TimesNewRomanPSMT
图3-9-实体流数分桶敏感性          HBBUPB+SimHei BHVIUS+TimesNewRomanPSMT
图3-11-受限观测下的检测能力       CVRMLG+SimHei GVFMSJ+TimesNewRomanPSMT
图3-12-检出率随告警预算的变化     FXCQXA+SimHei GHRYSM+TimesNewRomanPSMT
```

十一幅全部 `SimHei`（合同要求的简体黑体），`STHeitiTC-Medium` 零命中。西文 `Times New Roman`，
数学 `Cambria Math`（机制图）。字体根因：原 `绘制第三章结果图.py:92-107` 的 `register_fonts()`
对 `.ttc` 只调 `addfont()` 未指定 face_index，解析到繁体字面；现统一改用
`figstyle.resolve_cjk_font()`，该函数逐字面 `_iter_faces()` 校验并逐字核对码位覆盖。
该脚本原先残留的 `Hiragino Sans GB` 候选一并删除。

### 4. 灰度可读

十一幅图转 8 位灰度并缩到 1400 px 宽后逐幅查看，全部可读：四格与各方法先靠填充图案
（无、`//`、`..`、`xx`、`///`、`\\`）与线型（实线、长虚线、点线、点划线）区分，颜色只作辅助。
图 3-8 的 `p = 1` 参考线与图 3-12 的可达上限竖线在灰度下仍与其他线型可分。

### 5. 运行入口

```
uv run --project thesis/figures/第三章 python thesis/figures/第三章/绘制第三章机制图.py
uv run --project thesis/figures/第三章 python thesis/figures/第三章/绘制第三章结果图.py
```

两个脚本均可重复执行，产物幂等；`resultdata.py` 在制品缺失时报出精确路径并提示远程拉取，
不静默回退旧数据。

### 6. 制品拉取

从服务器只读拉取到本机 `runs/diagnostics/` 的目录：`ch3-metrics-table-20260825b`、
`ch3-common-first-alert-fp-budget-envelope-v1`、`ch3-published-neural-operational-backfill-v1`、
`ch3-xgb-cpa-elp-c11-operational-backfill-v1-rerun2`（后两者部分文件）。
未使用 `rsync --delete`。凭据只经环境变量传入，未打印、未落盘。

---

## 四、需正文联动的交接清单（主进程转交正文修订代理）

按优先级排列。前三条会造成正文与图件对不上，必须处理。

1. **图 3-10 已撤下，正文 3.6.7 攻击类别分面失去图件支撑。**
   当前权威制品无任何按攻击类别的分面字段（已在 bf16 聚合结果、统一总表、包络三处 `rg -i 'category|attack_type|label_name'` 零命中）。
   正文需二选一：删去 3.6.7 与图 3-10 引用并把图 3-11、3-12 顺次改为 3-10、3-11（目录同步）；
   或保留该节并新增一次按 `Category` 字段分面的目标年评价运行。**在补齐之前，正文不得引用图 3-10。**

2. **图 3-7 的内容已由「三种子稳定性」改为「逐轮选轮稳定性」。**
   正文 `3.6-实验评估.md:151` 现写「无机制参照的三次逐流平均精确率为 23.34%、20.83% 与 25.47%……」，
   这组数字来自已作废运行且新协议为单种子（U4 裁决），无对应制品。
   新图可支撑的表述是：四格二十轮验证取值的极差为 `0.0056 / 0.0075 / 0.0077 / 0.1158`，
   双机制并用格约为其余三格的 15 至 21 倍，且在第 7、12 两轮跌到 `0.89` 以下。

3. **图 3-9 的内容已由「序列长度 L 扫描」改为「实体流数分桶」。**
   正文 `3.6-实验评估.md:255` 现写四个长度档（16/32/64/128）；新图为五个实体流数桶
   （`1～2 / 3～10 / 11～100 / 101～1000 / 1001 条以上`）。节标题「序列长度敏感性」宜改为
   「实体流数敏感性」或等义表述；正文数字须整体替换。

4. **正文图 3-12 题注中的「假阳率 2.50% 处的交叉点」不成立。**
   `3.6-实验评估.md:135` 现写「标出假阳率 2.50% 处的交叉点」，并在正文引用交叉点相关数字。
   实测：在本章方法可达区间（实际假阳率 ≤ `3.72%`）内的 `1,721` 个共同预算点上，
   本章方法检出率**全部**高于全注意力网络，两条曲线**不相交**；两者的首个交叉点在实际假阳率 `10.12%`，
   已越过本章方法的可达上限。与一维卷积网络只在实际假阳率 `0.0539%` 处相交一次，交点之后本章方法在 `98.84%` 的点上更高。
   题注与相关论断须按上述实测重写。

5. **图 3-5 图题可沿用，但方法集合已变。**
   六个方法为随机森林、XGBoost、一维卷积网络、门控循环网络、全注意力网络、本章方法（全容量 MLP，BF16）。
   旧的「多层感知机＋CPA-ELP」（`ch3-2x2-fairsel`，实体 AP `0.5184`）已按恢复卡「历史强基线，不得再进正文」剔除。
   另需注明：随机森林与已发表配置 XGBoost 只持久化名义 4% 单点，没有实际假阳率收据，图内已用不同填充图案标出。

6. **图 3-6 的「超可加」表述已删除。**
   图题与图内文字改为中性描述。正文若仍有「超可加」「组合增益 18.34 个百分点」「交互 5.89 个百分点」
   等旧四格表述，须一并按 bf16 四格重写：A 单开 `+0.0829`、B 单开 `+0.0994`、并用 `+0.2209`、交互 `+0.0385`。
   **源年四格为 `0.6702 / 0.8412 / 0.7046 / 0.8305`，`B10 > O11`，任何图与任何正文表述都不得写成源年也联合最优。**

7. **十一幅图的题注行已全部备好。**
   `图件清单.json` 每个 `figures[].caption` 都以「图3-N　」开头，可直接粘进正文作为独立题注行，
   覆盖审查报告指出的十幅缺口（图 3-1~3-4、图 3-6~3-11）。图 3-5 的现有题注需按第 5 条更新方法集合。

8. **文件名变更需同步目录登记。**
   `图3-5-跨年度主性能对比` → `图3-5-六个方法在三个指标上的相对位置`；
   `图3-9-序列长度敏感性` → `图3-9-实体流数分桶敏感性`。

---

## 五、遗留风险与未处理项

1. `thesis/figures/第三章/绘制第三章骨干对照图.py` 的文档串仍自称产出「图3-12：骨干 × 有无机制」，
   与本轮新建的图 3-12（检出率随告警预算的变化）**图号冲突**。其占位输入已删除，脚本当前跑不出东西，
   但若有人补上数据就会覆盖真图 3-12。删除或改名需授权，本轮未动，仅在此登记。
2. `thesis/figures/第三章/__pycache__/绘制第三章机制图.cpython-313.pyc` 是历史遗留的被跟踪字节码；
   本轮新产生的 `.pyc` 未提交。清理需单独授权。
3. 图 3-12 的四条曲线只覆盖有完整告警预算曲线的方法。随机森林与已发表配置 XGBoost
   只持久化名义 4% 单点，无法进入该图；若正文要求它们出现在预算曲线上，需要补跑运行化回填。
4. 图 3-11 只画四档预算。名义 4% 与 8% 两档的首次告警阈值落进并列块，
   实际首次告警假阳率达 `79.05%`，预算不成立，已在图面与题注写明。
5. 本章全部结果图为单种子 `seed42` 的单次运行，目标年读数属封印后描述性评价，
   图内不写多种子误差。若后续要求多种子，图件须重做。
