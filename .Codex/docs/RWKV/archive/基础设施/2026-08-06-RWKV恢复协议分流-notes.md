# RWKV 恢复协议分流记录

## 已核验事实

- 项目根恢复协议当前无条件要求读取两份 PINN/R2 `output/` 文档。
- RWKV 局部规则同样把两份 PINN/R2 文档列入默认先读路径，导致路线交叉污染。
- `.Codex/docs/RWKV/` 已有方案、数据和基线过程文档，但缺少一份只保存实时状态的精简恢复总控。
- 用户已明确当前会话专门研究 RWKV 替代路线，并批准采用显式路线分流方案。

## 决策

- 不修改两份 PINN/R2 总控正文。
- 不使用全仓活动路线文件覆盖其他并行会话。
- 以会话显式绑定和摘要哨兵 `RESEARCH_ROUTE=RWKV` 选择 RWKV 恢复路径。
- RWKV 总控引用详细证据，不复制长篇设计和审查内容。

## 验证记录

- 首次整批补丁因根规则已被并行写入而未能匹配预期旧行；重新读取后确认根恢复协议实际已经落盘，随后按文件拆分完成其余变更，未覆盖并行内容。
- 根 `AGENTS.md`、RWKV 局部 `AGENTS.md` 与 `RWKV路线总控.md` 均能检出 `RESEARCH_ROUTE=RWKV` 和互斥路线规则。
- RWKV 局部文件中两份 `output/` 文档只出现在“默认不读取”的否定性边界，没有进入默认先读清单。
- 三个核心文件未发现行尾空白。
- 三个核心文件合计 21,350 字节，未触及项目声明的 32,768 字节组合上限。
