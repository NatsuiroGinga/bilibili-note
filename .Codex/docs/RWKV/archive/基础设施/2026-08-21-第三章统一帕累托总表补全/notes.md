# 第三章统一帕累托总表补全笔记

## 已核事实

- 现有来源清单共 12 个模型登记项；GRANDE 是源年专用运行，因此不进入 LSPR24 描述性评价表。
- ResMLP2 与 TabM4 的 C11 完整预算曲线均含 46,363 个可达负实体预算点，实际数组字段为 `n_false_positive_entity`、`nominal_fpr`、`realized_fpr`、`detection_rate`。
- 当前所有来源都没有首次告警制品。未告警率、按时检出曲线、时延摘要和起点/阈值定义必须保持缺失，不能由实体 DR 或 AP 推导。
- 新机械证据确认 RWKV 使用的 `T23/t24` 是 `mTimestampStart` 微秒，不是完整流可用时刻；按该值构造 `available_ns` 会出现大量实体内逆序，因此不得报告真实秒首次告警时延。
- 当前工程收据没有同时覆盖模型权重、选模、纯推理、吞吐、恢复和重复计算等完整向量，因此不能形成完整工程前沿。

## 实现裁决

- 保留 `lspr23-selection` 作为开发选择证据，新增 `lspr23-performance` 作为源年完整性能表；两者不复用指标列。
- 性能支配仅在同一 `evaluation_pool` 和同一 `first_alert_axis` 内判断，要求九个性能标量、完整预算曲线实际数组、六档未告警率和六档按时检出曲线全部齐全。
- 完整预算曲线以共同负实体计数为横轴逐点比较；只持久化路径、摘要或六个预设点的来源不参加完整支配。
- 首次告警摘要只作完整性证据，不临时选取均值、中位数或分位数作为支配轴；支配使用未告警率与按时检出累计曲线。
- 首次告警收据必须声明 `axis=exposure_index` 或合法的 `axis=elapsed_seconds`，并显式登记 `time_delay_available`。RWKV BF16 只允许写 1 基实体内曝光序号和 `time_delay_available=false`；不写伪秒时延。
- `exposure_index` 与 `elapsed_seconds` 不得混比。缺少真实秒不阻止曝光轴候选之间的同轴比较，但输出必须保留 `time_delay_available=false`。
- 工程支配单独计算。神经参数量与树数使用不同规模单位分池，不机械换算，不与性能前沿合并。
- RWKV-7、GRANDE、TabM32 通过显式清单路径和独立模式名接入；生成器不扫描 `runs/`。

## 接入前结果（已由下节更新）

- LSPR23 选择表 12 行。
- LSPR23 完整性能表 12 行。
- LSPR24 描述性评价表 11 行，按合同排除没有目标年评价的 GRANDE。
- 资源表 12 行。
- 三类帕累托结果当前全部为“证据不完整”；没有输出任何完整前沿或完整支配关系。

## 2026-08-21 全容量多层感知机 BF16 接入

- 受控来源新增“全容量多层感知机＋完整实体幂平均（BF16）”，只注册完整系统 O11；B/N/O 其余分支继续留在聚合结果作消融，不重复生成总表行。
- 运行 `ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1-bf16-v1` 已达到 `complete/finished/0`。生成器读取聚合结果、目标曲线收据、源年与目标年 NPZ、运行清单、资源收据和状态收据，共使溯源文件数由 12 增至 19。
- 源年 O11：逐流 AP `0.999693506366`、实体 AP `0.830501823003`、最大实体 AP `0.894679908412`；完整曲线按实际可达 FPR 重算的六档 DR 为 `0.85/0.95/1/1/1/1`。
- 目标年 O11：逐流 AP `0.132710594268`、实体 AP `0.585489598597`、最大实体 AP `0.571376613139`；按完整曲线实际可达点重算的六档 DR 为 `0.349734042553/0.533244680851/0.667553191489/0.75/0.789893617021/0.789893617021`。
- 目标年聚合表中的名义 `DR@4%=1` 与 `DR@8%=1` 不进入总表。实际曲线在不超过 4% 和 8% 的最接近可达点均停在 `FPR=0.037206393029`，对应 DR `0.789893617021`。
- 首次告警按实际可达 FPR 参与。目标年名义 4% 和 8% 阈值的实际首次告警 FPR 均为 `0.790501046093`，不能冒充 4% 或 8%；该映射保存在首次告警摘要和内部比较键中。
- 源年性能、目标年性能和工程表的机械状态均为“帕累托前沿”，`dominated_by=[]`、`dominates=[]`。原因是该行是当前唯一性能与工程必需分量齐全的同池行；这不等于已经形成公平效率优势或论文效果结论。
- 资源收据记录外部资源测量有并发影响、`fair_efficiency_evidence=false`。工程前沿只表示当前完整数值向量未被另一完整同单位行支配，不支持公平效率主张。
