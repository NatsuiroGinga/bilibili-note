# 第三章神经骨干跨进程协作看板

- **状态日期**：2026-08-31
- **用途**：当前任务、文件所有权、状态待核运行和直接接手条件。本文不保存代理聊天、逐分钟进度或已关闭事故。
- **工作树**：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- **当前分支**：`codex/ch4-dtep-pbc-20260819`
- **主线**：FT-Transformer 基座的 CEM-BER 四格。2026-08-27 由 MLP／CPA-ELP 线重锚而来，
  旧线结果真实但不再是主线，见第九节历史追溯。

## 一、恢复与事实源

1. 依次读取根 `AGENTS.md`、`.Codex/docs/AGENTS.md`、[RWKV 路线规则](../../AGENTS.md)、
   [路线总控](../../RWKV路线总控.md)和[第三章恢复卡](../../RWKV第三章恢复卡.md)。
2. 当前事实源是[2026-08-31 进度与错误记录](../../2026-08-31-第三章FT四格进度与错误记录.md)；
   代码与制品导航见[制品与代码索引](../../CEM-BER制品与代码索引.md)。
3. 本看板只决定所有权和接手动作，不改变研究公式、数据合同或科学裁决。
4. 运行事实只认原始 `status.json`、日志、检查点、清单和聚合制品；提交、文件存在和代理自报不是完成证据。

## 二、当前任务表

| 编号 | 任务 | 所有者 | 状态 | 当前事实 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| F-C00 | 裸 FT 基线 | 已完成 | `finished/exit 0` | 源年实体 AP `0.7887951742121221`，第 17 轮，墙钟 `50.5` 分 | 不得重跑；它是三个机制臂的唯一门槛 |
| F-C10 | ＋因果实体记忆 | 已完成 | `finished/exit 0` | `0.8150313407035376`，第 17 轮，对 C00 `+0.0262361665` | 不得重跑 |
| F-C01 | ＋预算感知实体排序 | 已完成 | `finished/exit 0` | `0.9537273846736761`，第 17 轮，对 C00 `+0.1649322105`，墙钟 `123.8` 分 | 不得重跑 |
| F-C11 | 两机制联合 | 未分派 | **`须整臂重跑`** | 关机中断，`state=running` 但无 `inflight.pt`，无任何读数 | 先推 OOM 修复再重跑，顺序见第三节 |
| F-EVAL | LSPR24 目标年描述性评价 | 已实现待运行 | `阻塞于四格未齐` | 目标年前向已实现（`68e2197`，14 项实现验证通过）；预检正确拒绝执行 | C11 完成后运行，不得绕过预检 |
| F-SPEC | 两机制形式化与复杂度规约 | 已完成 | `完成` | `6c73985`＋`0aae4fd`，226 行；含动机、公式、因果边界、挂载位置、在线算法与复杂度 | 正文 3.3／3.4／3.5 直接取用，不重写公式 |
| F-FIG | 第三章机制图件 | 已完成 | `完成` | `d1c5aaf`：CPA-ELP 旧图移入 `thesis/figures/第三章/作废/`，CEM-BER 新图已绘 | 结果图待四格齐备后补 |
| F-TABLE | 统一总表注册 | 已完成 | `完成` | `94face9`：四格已注册进 `ch3-metrics-table-sources-v1.json` | 四格齐备后由程序生成总表 |
| F-BASE | 已发表基线缺口 | 未分派 | **`阻断`** | 两个已发表基线缺失，已登记（`625e4e6`） | 见恢复卡；正文主比较表在补齐前不得定稿 |
| F-VERIFY | 制品可复核性核验 | 已完成 | `完成` | `72641b1`＋`4a93c99`：三格判定可复核；配置一致性与开关正交性已核 | C11 完成后对其重跑一次核验 |

## 三、新会话直接接手表

接手前重新读本表并执行目标路径 `git status --short`。**动作顺序不可颠倒。**

| 序 | 动作 | 前置条件 | 命令或入口 | 失败处置 |
| ---: | --- | --- | --- | --- |
| 1 | ~~核验服务器可达与身份~~ **已完成 08-31** | — | 实测同一容器 `autodl-container-tx6z363f86-9aa94f1e`，项目根在，磁盘余 `28G` | 端口每次开机变化；不通即关机，不重试、不计科学失败 |
| 2 | ~~推送 OOM 修复~~ **已完成 08-31** | — | 三个文件已推并双端校验 `sha256` 前 16 位一致：`ch3_ft_c00_dual_selection.py`＝`68cc0559a8dd8f67`（含两处 checkpoint 导入）、`run_ch3_ft_four_cell_serial.sh`＝`2e7d24e65704bd27`、`resume_ch3_ft_after_boot.sh`＝`1bfc11a9116a0a3b`、`tools/ch3_ft_emit_four_cell_summary.py`＝`d78b152d4f0ee8ac` | 已核验，无需重推 |
| 3 | 单入口续跑 | **需有卡模式**；无卡模式跑不了训练 | `bash scripts/remote_launchers/resume_ch3_ft_after_boot.sh`（`b77c233`，120 行，幂等） | 非零退出即停，保留日志 |
| 4 | 本机回传守护 | 第 3 步已启动 | `nohup bash scripts/remote_launchers/watch_and_pull_ch3_ft.sh &`，凭据经环境变量传入 | 轻量轮实测 6 秒；失败看 stderr，不吞错误 |
| 5 | C11 完成后核验 | C11 `finished/exit 0` | `tools/ch3_ft_verify_four_cell_artifacts.py` | 判定不可复核时先查判据口径再疑制品 |
| 6 | 目标年评价 | 四格齐备 | `tools/ch3_ft_lspr24_descriptive_eval.py --dry-run` 后全量 | 预检拒绝即四格未齐，不得绕过 |
| 7 | 结果分析 | 第 6 步产出制品 | `results-analysis` → `results-report` | 未产出分析制品前结论只能标「实验待证」 |
| 8 | 正文 | 第 7 步完成且 F-BASE 阻断关闭 | 按[论断台账](../../2026-08-29-第三章CEM-BER正文骨架与论断台账.md)填写 → `citation-verification` → `writing-anti-ai` → `paper-self-review` | 四格判据未全成立时按台账降级或删除对应主张 |

**C11 墙钟预计**：按 C01 的 `123.8` 分起算，叠加 CEM 后可能更长。

## 四、本会话（2026-08-31）代码与改动清单

### 4.1 新增代码

| 文件 | 提交 | 规模 | 用途 | 验证状态 |
| --- | --- | ---: | --- | --- |
| `tools/ch3_ft_emit_four_cell_summary.py` | `199c6dd` | 185 行 | 每臂结束自动把四格读数与判据写入固定路径 JSON，接入守护脚本 | 真跑两场景：四格全缺不崩、构造真值交互算得 `0.04` 与手算一致 |
| `tools/ch3_ft_verify_four_cell_artifacts.py` | `72641b1`＋`4a93c99` | 约 260 行 | 只读核验制品齐全性、检查点可加载性、身份自洽、参数量一致、配置一致性与开关正交性 | 真跑，三格判定可复核 |
| `scripts/remote_launchers/pull_ch3_ft_four_cell_artifacts.sh` | `199c6dd`／`be4b17d`／`ca80b60` | 97 行＋两次修复 | 回传四格制品到本机，`--light` 只拉收据日志 | 真跑，轻量轮 6 秒 |
| `scripts/remote_launchers/watch_and_pull_ch3_ft.sh` | `ca80b60` | 79 行 | 周期回传守护，臂完成自动全量备份并打标记 | 真跑，已回传 C00／C10／C01 |
| `scripts/remote_launchers/resume_ch3_ft_after_boot.sh` | `b77c233` | 120 行 | 开机后单入口续跑，幂等 | 见提交内说明 |
| `tools/ch3_ft_lspr24_descriptive_eval.py` | `68e2197` | ＋1147 −38 行 | LSPR24 目标年前向（子代理交付） | 14 项实现验证通过；四格未齐故未真跑 |

### 4.2 缺陷修复

| 提交 | 缺陷 | 修法 |
| --- | --- | --- |
| `3344e0f` | C01 首步 CUDA 显存溢出：排序阶段虽分微批前向，但把 100 个微批的完整反向图累积到最后 `cat`，需 `30.97 GiB` | 对每微批纯计算段套 `torch.utils.checkpoint(..., use_reentrant=False)`；峰值降至 `15,175 MiB`，代价是步速 `6.9 → 2.6` 步/秒。C11 记忆路径同样处理，但把状态读写留在检查点外 |
| `be4b17d` | 回传脚本用 `bash` 运行 expect 脚本、stderr 被吞、退出码被管道掩盖，三重掩盖使必然失败显示为「远端尚无该项」 | 改 `expect -f`、保留 stderr、区分「远端不存在」与「传输失败」 |
| `ca80b60` | 轻量轮逐项拉 36 次 SSH，单轮超 2 分钟，周期守护每轮超时 | 改整目录一次 rsync 加 `--max-size=5m`，降到 6 秒 |
| `0aae4fd` | 形式化规约中记忆粒度与写入顺序表述有误 | 已更正，见该提交 |

### 4.3 文档与规约

| 提交 | 内容 |
| --- | --- |
| `6c73985`＋`0aae4fd` | `2026-08-31-CEM-BER机制形式化与复杂度规约.md`，226 行 |
| `5f870b4`＋`460a8d6` | 恢复卡与四格进度同步到 08-31 |
| `c562c5c` | 制品核验记录与 BER 参数交叉印证 |
| `e436282` | 跨关机续训实验证据 |
| `625e4e6` | 两个缺失的已发表基线登记为阻断 |
| `d1c5aaf` | 第三章机制图件重绘，旧图移入作废目录 |
| `94face9` | 四格注册进统一总表来源 |

### 4.4 仓库基础设施（主工作树）

检索工具入口此前三处并存且互相矛盾，导致本会话连续用错三个环境。已统一：
根 `AGENTS.md`／`CLAUDE.md` 的检索路由改为唯一 uv 入口并列出禁用清单；
`scripts/literature_search/README.md` 同步；`embeddings.py` 的报错信息改为直接给出正确命令；
`pyproject.toml` 与 `uv.lock` 从未跟踪转为入库（此前新克隆的仓库跑不起该入口）。

## 五、本会话实验记录

| 实验 | 结论 | 证据 |
| --- | --- | --- |
| C01 训练 | 完成，源年实体 AP `0.9537273846736761` | `runs/diagnostics/ch3-ft-c01-entity-ranking-cuda-formal-v1/receipts/selection.json` |
| 跨关机续训 | 断点续训在真实关机后有效，C10 从断点接续完成 | `e436282` |
| 制品可复核性 | C00／C10／C01 三格可复核；C01 与 C00 的 `state_dict` 条目数与元素总数逐位相同（`54` / `924,285`），**独立佐证 BER 不引入推理期参数** | `runs/diagnostics/ch3-ft-four-cell-artifact-verification.json` |
| 四格配置一致性与开关正交性 | 已核 | `4a93c99` |
| `torch.compile` 覆盖范围 | 服务器实测 `c.encode.__self__ is m == True`，只编译 `forward`；C10／C11 走 `encode/inject/predict` 实际未编译，四格数值路径不一致，差异量级 `8.94e-07` | 用户 2026-08-30 裁决：如实记录偏离，不重跑 |
| 检索三模式对比（非主线） | 25 条真实查询集：`lexical` 命中率 `1.0000`／MRR `1.0000`；`vector` 命中率 `1.0000`／MRR `0.9067`；`hybrid` 与 `lexical` **逐条全部持平**（更好 0、更差 0、持平 25） | `scripts/literature_search/evaluation_queries_real.json`；用户已裁定检索优化不再推进 |

## 六、当前冻结合同

- 四格判据：`C10 > C00` 且 `C01 > C00` 且 `C11 ≥ max`，交互 `I = C11 − C10 − C01 + C00 > 0`。
  指标为源年 LSPR23 验证集实体 AP，`target_reads = 0`。
  **前两条已成立，后两条不可评估**；四格齐备前不得宣称 CEM 有效或 CEM-BER 成立。
- 选轮规则：源年实体 AP 择优，四格统一，冻结于 `cf2cf65`（实验之前）。
- 参数量：C00 `924,283`／C10 `1,075,196`／C01 `924,283`／C11 `1,075,196`。
- 口径隔离：旧路径源年 `0.7380`、compile 路径源年 `0.7888`、目标年 `0.7508` 三者互不通用；
  XGBoost 侧无源年读数（其训练不留源年验证集），全部为目标年，不可与 FT 源年并比。
- 第三章按单次运行报告且不评冠军，对标朱焱雷论文体例，不加码多种子或噪声分析。

## 七、远端防重复与证据边界

- **2026-08-31 服务器以无卡模式开机**：`nvidia-smi` 返回 `Permission denied`，
  该模式只能传文件、装依赖与只读核验，**不能跑训练**。代码同步已在该模式下完成（第三节第 2 步），
  切回有卡模式后可直接执行第 3 步，不必重推。
- 同步时清掉一个 8 月 29 日就存在的错误副本：`scripts/remote_launchers/ch3_ft_emit_four_cell_summary.py`
  （该文件属于 `tools/`，当时推送目标目录写错，一直残留）。`tools/` 下的正版未受影响。
- 服务器按周期开关机，端口每次开机变化。远端动作前先只读核验身份、根目录、磁盘、GPU、进程与制品。
- C00／C10／C01 已 `finished/exit 0`，**任何情况下不得重跑**；守护脚本会自动跳过。
- C11 无 `inflight.pt`，只能整臂重跑，不存在可续断点。
- 制品副本：本机 `runs/diagnostics/` 下有 C00（26 MB）、C10（824 MB）、C01（26 MB）；
  C00 另存私有 HuggingFace 仓库 `Heehobino/cember-ft-lspr23-ch3` 的 `c00-bare-ft/`（私有，不得按公开制品引用）。
- 服务器离线、回收或资源不足不算科学失败。

## 八、所有权与写回协议

1. 同一任务同一时间只允许一个文件所有者；接管前必须由当前所有者或主控明确释放。
2. 修改文档前执行目标路径状态检查；目标有他人修改时停止，不覆盖、不暂存、不提交。
3. 提交只含任务表声明的文件；禁止 `git reset --hard`、`git checkout --`、清空共享暂存区或删除唯一制品。
4. 状态变化直接更新任务行、证据链接和下一动作，不追加聊天记录、预计时间或逐分钟心跳。
5. 完成必须链接提交、原始状态或制品；工程完成、运行完成与科学裁决分列，不以代理自报替代验收。
6. 本工作树长期存在 110 余处其他会话的未提交改动，提交一律用 `git commit -- <精确路径>` 的 pathspec 形式。

## 九、证据索引与历史追溯

### 当前线（FT／CEM-BER）

- [2026-08-31 进度与错误记录](../../2026-08-31-第三章FT四格进度与错误记录.md)：当前事实源
- [制品与代码索引](../../CEM-BER制品与代码索引.md)：代码、制品、文档的单一导航入口
- [第三章恢复卡](../../RWKV第三章恢复卡.md)
- [CEM-BER 四格实验进度](../../2026-08-28-CEM-BER四格实验进度.md)：完整时间线
- [机制形式化与复杂度规约](../../2026-08-31-CEM-BER机制形式化与复杂度规约.md)
- [正文骨架与论断台账](../../2026-08-29-第三章CEM-BER正文骨架与论断台账.md)
- [故障预防手册](../../../agents-split/llm-probe-failure-prevention-handbook.md)

### 历史线（MLP／CPA-ELP，2026-08-27 重锚前）

结果真实但不再是主线，只作追溯：
[有效 2×2 实施与结果报告](../第三章-MLP线/2026-08-26-第三章有效2x2机制优化计划/实施与结果报告.md)、
[XGBoost 配方来源审计](../../2026-08-26-XGBoost同数据集论文配方来源审计/审计报告.md)、
[第三章全量进度快照](2026-08-25-第三章实验全量进度快照.md)。

精简前 `719` 行看板固定在基线 `5a9bc596f80b9b6c8dec9dc29e361f10ac13ef1d`：
`git show 5a9bc59:.Codex/docs/RWKV/2026-08-20-第三章神经骨干跨进程协作看板.md`。
历史消息只用于制品追溯，不得覆盖本表、章节恢复卡或更新的原始状态。
