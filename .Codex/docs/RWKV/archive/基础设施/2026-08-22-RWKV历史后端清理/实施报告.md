# RWKV 历史后端文档清理实施报告

## 结论

活动文档已收敛为单一事实：RWKV 唯一活动训练路径是独立 CUDA 融合实现；它须在合法 Raw83 数据门通过后使用独立身份从头训练，不复用任何历史训练实现的权重、优化器、断点、代码锚或对照门。当前状态为“设计可行、实验待证”。

## 变更内容

- 删除 21 份专门描述旧入口、资源门、恢复流程或工程比较的文档，其中 16 份已跟踪、5 份为无活动所有者的未跟踪遗留。
- 修改 22 份活动文档，覆盖路线规则、路线总控、第三章恢复卡、协作看板、独立 CUDA 计划与报告、混合登记文档、连续状态候选和四张派生 Markdown 总表。
- 四张派生 Markdown 总表删除无真实结果的 RWKV 占位行；活动表保留 12 个模型，LSPR24 保留 11 行。独立 CUDA RWKV 只有合法 Raw83 数据上的真实结果产生后才能重新登记。
- 新增本任务计划、笔记和实施报告三份过程文档。

## 验证情况

- 指定的七组历史模式在 `.Codex/docs`、`thesis`、`output`、`wiki` 活动 Markdown 文档中零命中。
- 所有已删文件名和目录名在活动 Markdown 文档中零引用。
- 旧 RWKV 工具路径、运行身份、速度比与兼容替换表述在活动 Markdown 文档中零命中。
- `git diff --check -- .Codex/docs/RWKV` 通过。
- 四张派生表结构核验通过：源年选择 12 行、源年性能 12 行、目标年评价 11 行、资源 12 行，均不计表头。
- 精确暂存前发现共享暂存区被 Raw83 预处理任务占用，本任务未清空或夹带；其所有者提交 `c581a12` 后释放。
- 已精确暂存 41 个本任务路径，排除全容量 MLP 既有改动和两个并发 CUDA Raw83 文档；`git diff --cached --check` 通过。
- 提交标识由当前提交产生，在最终汇报中记录。

## 保留边界

- 不修改代码、配置、运行制品、服务器或 Git 历史。
- 不删除其他论文与通用 PyTorch 内容；DeltaNet 与门控 DeltaNet 的通用参考说明保留。
- 不覆盖或暂存任务开始前已有的全容量 MLP 文档改动及其他代理、用户改动。

## 遗留风险

- 代码、配置、启动器和原始运行制品按用户边界保留，可能仍含历史实现标识；它们不是活动文档入口，也不纳入本次提交。
- 独立 CUDA 入口尚未在合法 Raw83 数据上真实运行，不能形成效果结论。
