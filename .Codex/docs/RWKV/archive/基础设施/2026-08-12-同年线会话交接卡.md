# 2026-08-12 同年线会话交接卡

<!-- RESEARCH_ROUTE=RWKV -->

- **性质**：断网前交接。恢复时先读本卡，再按需展开引用文档。
- **本卡负责范围**：LSPR24 同年线 + 数据集调研 + 磁盘治理。跨年度线由另一主进程写回，本卡不覆盖。

## 1. 回来后第一件事

读 workflow `wf_a87a7b13-f42` 的结果：

```
/private/tmp/claude-501/-Users-bilibili-personal-note/068bbd7d-1e28-42ae-9da2-046f106e4118/tasks/wwcrz93qm.output
```

若该文件不存在或为空，读 journal 逐条取回已完成代理的返回值：

```
/Users/bilibili/.claude/projects/-Users-bilibili-personal-note/068bbd7d-1e28-42ae-9da2-046f106e4118/subagents/workflows/wf_a87a7b13-f42/journal.jsonl
```

断网时状态：9 个代理中 **6 个已交结果**（5 个数据集调研 + 1 个横向综合），2 个对抗校验在跑，产出文档未启动。
**已完成代理的返回值已固化在 journal.jsonl，不会因断网丢失。**

脚本路径（可编辑后 `resumeFromRunId` 续跑，已完成代理走缓存）：
`/Users/bilibili/.claude/projects/-Users-bilibili-personal-note/068bbd7d-1e28-42ae-9da2-046f106e4118/workflows/scripts/dataset-citation-task-survey-wf_a87a7b13-f42.js`

**用户指示：AET-2026 的调研结果不采纳**，最终文档中剔除该行，不据其做任何裁决。

## 2. 今天已确定的结论（全部有制品支撑）

### 2.1 四个 P0 全部不晋级

C12、R1、候选 B、跨年度 TL1。TL1 `rerun2` 最高 AP=`0.03840115350812821`，晋级门 `0.0439`，恶意召回 `1/2020`。
**跨年度线关闭。**

### 2.2 同年线五实验

详见[同年线五实验结论汇总](2026-08-12-LSPR24同年线五实验结论汇总.md)。要点：

- A1 验证 AP=`0.9807915803958434`，但 **0.01 FP 预算下事件召回仅 `0.5326`**（漏 710/1519）
- 早期预警门禁**任一口径均未通过**（方案册是合取且召回项从未测量；脚本执行的是另一套 OR 逻辑）
- **「单窗事件历史贫瘠」已被两次独立实验证伪**（lag1..4 非空中位数 `3.0`）
- **事件召回 `0.5326` 不能作主指标基线**：端点簇自助 95% 半宽 `0.2346`，有效独立样本量约 `113` 而非 `1519`
- **M1 证据累积当场否决**（漏检多窗事件成员窗分数 p75=`0.0194`，24 个规则 oracle 上界全为负）
- **M2 预算分配空心**（增益 95% 来自阈值标定；冻结迁移 fp 超标 `309` 倍）
- **决策规则层无可支撑一章的余量**

### 2.3 切分键偏移已定性

`split_name` 字段本身正确（`wide.rs:227` 用全量 100% 活动键算）。**A 线不受影响，A1/E5 全部数字无需重算。**
C 线受影响：它从只含 80% 的已物化宽表重建活动键，`0.60 × 0.80 = 0.48`。
`b40_new`/`b50_new` 具体秒数不可信，不得写入冻结合同。

### 2.4 唯一尚未测量的方向

**单窗事件子集上的排序质量。** AP=`0.9808` 是被 `18902` 个平均分 `0.9838` 的 middle 窗主导的全局值；单窗事件平均分仅 `0.5414` 且历史充足。
最小实验（服务器恢复后，约 10 分钟）：在 1089 个单窗事件 + 匹配负样本子集上测 A1 的 AP/ROC-AUC。
子集 AP >`0.95` → 饱和，该方向亦否决；落在 `0.6`–`0.8` → 有空间。

## 3. 磁盘治理已完成

- TQH-C2 v1.2.0 四道门槛全过（11 项齐全、10 个官方哈希 OK、9 个 ZIP OK、收据落盘），见[验收收据](../2026-08-12-TQH-C2-v1.2.0验收收据.md)
- 旧版 v1.0.1 按方案 A 分级删除，**释放 66 GiB**（可用磁盘 35 → 101 GiB）
- 保留 78 MB 元数据作证据锚点（`evidence-table.md` 的 D01 按行号引用旧版 README）

## 4. 阻塞项

| 项 | 状态 |
| --- | --- |
| **C56 服务器** | **不可达**，relay 端点未监听。恢复后须补推 `tools/memory_admission_gate.sh`（本机已修并发检测正则，服务器仍是旧版） |
| E2 网格扫描 | 脚本已就绪 `scripts/lspr24_pilot/e2_split_key_offset_grid_scan.py`，等服务器 |
| 单窗子集排序实验 | 未实现，等服务器 |
| `download_tqh_c2_pcap.sh:6` | 默认路径仍指旧目录，改指 v1.2.0 还是废弃待裁决 |

## 5. 恢复后的建议顺序

1. 取回 workflow 结果，剔除 AET-2026，给出候选方向表与**需用户手动下载的论文清单**
2. 查 C56 是否恢复；恢复则先补推内存门禁，再跑单窗子集排序实验
3. 依据 1 与 2 的结果决定第三章方向；在此之前不冻结任何方案
