# 2026-08-11 PINN/R2 历史模型 Hugging Face 私有备份

## 任务边界

- 目标仓库：当前账号下的 `pinn-r2-historical-checkpoints-20260811`（私有模型仓库）。
- 候选来源：C56 上指定的 13 个历史 PINN/R2 运行目录。
- 上传范围：实际存在的模型权重、LoRA/适配器、检查点、训练配置、核心指标和必要日志。
- 排除范围：预测结果、原始数据、缓存、虚拟环境。
- 安全边界：不修改本机生产代码，不删除任何服务器文件，不记录服务器地址、密码或令牌。

## 执行记录

- 日期：2026-08-11。
- 代理：`/root/pinn_model_hf_backup_terra_low`。
- 模型：`gpt-5.6-terra`。
- 推理强度：`low`。
- 已读取：项目根规则、`thesis/` 与 `llm_probe/` 局部规则、`.Codex/docs/` 与 RWKV 局部规则、RWKV 路线总控、Hugging Face CLI 技能。
- 初次误用本机认证状态：首次任务按本机执行 `hf auth whoami`，受控联网重试返回本机令牌无效。该路径不符合“优先在 C56 远端认证”的合同，已停止。
- 根因修正：通过 C56 既有凭据连接远端，先执行 `source ~/.bashrc`；远端家目录未发现 `hf`，项目根目录已确认可用，但远端 `hf auth whoami` 未返回有效身份。
- 本机回退认证：经任务负责人授权后重新执行 `hf auth whoami`，本机账户认证成功。未显示或记录令牌。
- 远端目录核验：13 个指定运行目录均存在；样例目录已只读确认包含 `.pt` 检查点。
- 清单规则修正：首次空清单的根因是远端 `rg` 的 glob 参数位置错误；已修正为 glob 在路径参数之前。
- 当前执行环境阻塞：完整清单回传（相对路径、大小、SHA-256）被环境安全审查拦截，要求用户明确批准权重、配置和日志派生元数据从 C56 回传本机。未尝试替代绕过方式。
- 恢复尝试：任务负责人转达用户已批准回传、私有上传及核验后删除；执行环境仍要求该批准由用户在当前对话直接作出，不能以转发记录作为授权证据。因此未传输、上传或删除。

## 当前状态

`BLOCKED_DIRECT_USER_AUTHORIZATION_REQUIRED`

远端 Hugging Face 认证不可用，本机认证可用，但完整远端制品清单回传尚未获得执行环境所需的用户当前对话直接授权。未创建或修改 Hugging Face 仓库，未上传或删除文件，亦未生成上传前后哈希清单或成功收据。

## 解锁动作

1. 用户在当前对话直接批准将指定运行目录中允许范围文件的相对路径、大小和 SHA-256 从 C56 回传本机，用于私有备份清单；批准同时涵盖将实际允许范围制品上传至 Hugging Face 私有仓库，以及仅在完整双端核验成功后删除 13 个精确运行目录。
2. 生成并核验远端清单，安全回收允许范围的文件到本机临时目录。
3. 使用已认证本机账户创建或复用私有仓库，采用可恢复上传方式上传，并核验仓库远端列表与本地清单闭合。

在双端清单闭合前，严禁标记 `BACKUP_READY_FOR_DELETE`，也不得删除服务器文件。
