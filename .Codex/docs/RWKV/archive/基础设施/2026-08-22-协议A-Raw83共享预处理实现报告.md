# 协议 A Raw83 共享预处理实现报告

## 一、结论

- `IMPLEMENTATION=READY_FOR_REAL_DATA_GATE`：冻结的 P0–P5 源年生产代码、配置、启动器、消费者接口与官方快照已实现。
- `EXPERIMENT=NOT_RUN`：本任务不访问服务器，不打开真实 ZIP、旧 `I/M/E/T` 或 LSPR24 Parquet，因而没有 Raw83 全量哈希、状态哈希、吞吐或模型效果证据。
- 默认启动器只执行 `P0–P5`；目标年只有一个单独的“先验完整源资格封印，后构造 `ParquetFile`”入口，本轮未调用。

## 二、已实现文件

| 文件 | 职责 |
| --- | --- |
| `src/flow_probe/protocol_a_raw83.py` | `DIJK_FEATURES` 唯一顺序、模式 SHA、PyArrow ZIP CSV 流式批、小端 `float32` Raw83、实体/时间侧车、标签内容哈希与聚合、`I/M/E/T` 绑定、切分、能力分离加载器、目标年封印门 |
| `src/flow_probe/protocol_a_preprocessing.py` | 训练有效流均值填补、`StandardScaler.partial_fit`、A 臂裁剪、与官方一次 C 行主序逐位等价的连续行块随机流、B 臂 76 列分位数地标、七特殊列 A 臂复制、共享视图 |
| `tools/ch3_protocol_a_raw83_prepare.py` | P0–P5 阶段状态机、同身份恢复、cgroup 批量反解、动态磁盘门、唯一清单发布 |
| `configs/ch3-protocol-a-raw83-shared-v1.json` | 源输入、83 字段、旧序列根、A/B 精确参数、资源公式常量、官方快照与目标禁用合同 |
| `scripts/remote_launchers/run_ch3_protocol_a_raw83_prepare_v1.sh` | 生产文件门、动态磁盘准入、独占锁、资源采样、持久会话、失败退出码保留和同身份续跑 |
| `vendor/rtdl_revisiting_models/LICENSE` | 官方固定提交的 Apache-2.0 许可证原文 |
| `vendor/rtdl_revisiting_models/lib/data.py` | 官方固定提交的预处理源码原文，未修改 |
| `vendor/rtdl_revisiting_models/manifest.json` | 仓库、完整提交、URL、字节数、SHA-256 和许可证登记 |

## 三、关键实现裁决

1. 源 CSV 仅通过 `pyarrow.csv.open_csv` 逐批解析，`ConvertOptions.include_columns` 按字段名投影，83 字段显式为 `float64`，不使用全量 `read_csv`。
2. Raw83 只将非有限值和有全文数据定义的结构非法值变为规范 NaN。普通字段的有限负值保留并单独计数。端口要求整数且在 `[0,65535]`，两个协议字段要求整数，三个粗拓扑字段要求 `{0,1}`。
3. 原始 IP 只在批内计算无向对 SHA-256，不落入模型数据根。流侧车只保存 `uint32` 实体编号，实体表只保存不可逆摘要。
4. 标签只在流式解析进程内归一为二元值，记录内容 SHA-256 与正负聚合，不写任何逐样本标签文件。`LabelResolver` 还会依照 `train/validate` 序列行反算允许流位图，拒绝跨用途读标签。
5. P2 对旧 `I/M/E/T` 每个文件计算 SHA-256，复制后再验哈希，并重算冻结的 `208598/22444` 序列划分。同时验证掩码尾置、流全覆盖无重复、实体一一映射、序列内时间单调和 `T23` 首流身份。
6. A 臂两遍扫描训练有效流：先得均值，后按均值填补并分批 `StandardScaler.partial_fit`。保存 `mean_/var_/scale_`，常量列 `scale=1`，全年视图裁剪到 `[-10,10]`。
7. B 臂先生成 76 列训练均值填补 `float32` 映射，逐列用 `np.std(...,ddof=0)` 得标准差。同一 `default_rng(42)` 按训练有效流顺序对连续行块调用 `standard_normal((batch,76),dtype=float64)`，立即按官方 `1e-3/max(std,1e-3)` 加入当前批并写回唯一 `float32` 映射。该流程已由目标 NumPy `2.2.6` 证明与一次 C 行主序输出逐位一致，不再落盘独立 `float64` 噪声。
8. P1、P4 和 P5 分别保存批/列收据。源 ZIP 续跑时从头重放，已完成批重算内容、累计统计与标签哈希后才跳过写入。最终文件改名与阶段状态封印之间中断时，续跑直接重验最终文件，不会新建空部分文件。

## 四、第三方接口核验

### 4.1 环境实测

本工作树独立 `.venv` 没有 NumPy、PyArrow 和 scikit-learn。本轮只读复用主工作区既有环境，实测为：

- NumPy `2.2.6`；
- PyArrow `25.0.0`；
- scikit-learn `1.7.2`。

该组版本与当前 `uv.lock` 在 Python 3.10 的解析一致，但它不能代替 B76 运行前的真实版本门。

### 4.2 实测签名

```text
pyarrow.csv.open_csv(input_file, read_options=None, parse_options=None, convert_options=None, memory_pool=None)
numpy.random.Generator.standard_normal(size=None, dtype=float64, out=None)
StandardScaler.partial_fit(self, X, y=None, sample_weight=None)
QuantileTransformer(*, n_quantiles=1000, output_distribution='uniform', ignore_implicit_zeros=False, subsample=10000, random_state=None, copy=True)
QuantileTransformer.fit(self, X, y=None)
```

Context7 的首次 PyArrow 查询返回空输出，因此未把它冒充为已核证来源。随后使用官方文档核对：

- Apache Arrow 25 文档确认 `open_csv` 为单线程流式读取，`ConvertOptions` 支持 `include_columns` 和 `column_types`。
- NumPy 官方文档确认 `standard_normal(size=None,dtype=np.float64,out=None)`，`out` 必须与 `size` 和 dtype 匹配。
- scikit-learn 官方文档确认 `StandardScaler.partial_fit`、总体方差 `ddof=0`、`QuantileTransformer` 参数及 `quantiles_/references_` 属性。

## 五、官方快照

- 固定完整提交：`e3ed46cac38568785289d8fa16b8cfa585bde27e`。
- `LICENSE`：`11339` 字节，SHA-256 `bd39fcc730a79d256b14fcdccbfc8ac9f0c7fad4a2e8d01fc513def67980fe35`。
- `lib/data.py`：`9448` 字节，SHA-256 `cabfcf8b5e86af243dd4b6a90eca1015a97e27e3a8013bb57b27db70058e297f`。
- 两个文件均从 GitHub 原始内容 URL 下载，快照不做翻译、格式化或注释修改。

## 六、已执行验证

| 验证 | 结果 |
| --- | --- |
| 一次 `py_compile` | 通过；之后的精确小修改由模块导入与配置入口解析覆盖，未重复运行 `py_compile` |
| Raw83 与预处理模块导入 | 通过；输出模式 v1、83 字段和 76 个分位列 |
| 工具 `--help` | 通过 |
| 工具 `--validate-config` | 通过；输出配置 SHA-256、P0–P5 默认阶段、目标默认禁用与快照清单 SHA-256 |
| 启动器 `--help` | 通过；明示只执行源年 P0–P5 |
| 启动器 `bash -n` | 交付前执行 |
| 官方快照字节数与 SHA-256 | 通过 |
| 目标路径零打开静态门 | `ParquetFile` 只出现在先调用 `validate_source_qualification_seal` 的 P6 专用函数之后；P0–P5 工具不读取 `config["target"]` 路径 |
| 真实数据与服务器 | 未运行，符合实现任务边界 |

## 七、真实运行前未关闭风险

1. B76 必须先运行同一 `--validate-config`，并核对实际 NumPy、PyArrow 和 scikit-learn 版本等于锁定环境。
2. 目标 NumPy `2.2.6` 已实测确认：同一 `default_rng(42)` 对 `(n,76)` 按任意连续行块依次调用与一次输出 `BITWISE_EQUAL=True` 且 `SHA_EQUAL=True`。真实 P4 仍须核对运行版本和批连续收据。
3. 旧 `I/M/E/T` 的完整 SHA-256、行全覆盖、实体映射、冻结计数和时间顺序要在 P2 真实运行后才能确认。
4. P4 只保留约 `4.63 GiB` 加噪训练映射；地标和状态收据封印后立即精确清理，P5 不再依赖它。
5. 当前只是数据产品实现，不证明 A/B 输入臂、表格 ResNet、FT-Transformer 或任何第三章机制有效。

## 八、下一步

1. 主进程独立审核本提交的差异、配置、身份、年度门和恢复窄窗。
2. 审核通过后再同步到 B76，先只运行 `--validate-config`、版本收据和资源门。
3. 资源门通过后以唯一启动器执行真实 P0–P5；任何失败保留原退出码、批/列收据和部分映射，不更换运行身份。

## 九、`c581a12` 唯一修订复审

### 9.1 已关闭阻断项

1. 源配置已冻结 LSPR23 ZIP、`I/M/E/T`、`train_rows`、`validation_rows`、训练/验证实体成员、训练/验证流位图和时间切点的期望 SHA 及哈希口径。P2 对旧数组比对 NPY 文件 SHA，对派生 `int64/uint8` 制品比对小端连续原始字节 SHA；任何一项不同即失败。
2. P2 每批先对有效行号做 `unique` 检查，再验全局覆盖位图；累计有效位和位图非零数都必须恰为 `16353511`。
3. P1 和 P5 断点命中时会重读已写目标切片并与收据 SHA 比对。全部源制品先写入单一 `source-v1.partial.<run-id>` 世代，P5 完成后只用一次目录改名发布，再原子写 `current-source.json`。同时存在 partial/sealed 世代时硬失败。
4. 源配置、源工具和源启动器对 `LSPR24` / `target` / `parquet` 零命中。P6 使用独立配置和启动器；当前 `winning_arm=null`、`qualification_status=blocked-until-source-arm-seal`，在打开源清单或年度 Parquet 前必然失败。
5. 阶段状态哈希只包含逻辑配置、内容 SHA、模式和算法，不包含绝对路径。P2、A 状态和 B 状态的承重哈希同样排除制品路径。
6. 源与年度世代均采用严格文件允许集：实际文件集必须恰等于清单制品加 `dataset-manifest.json`，不再依赖有限黑名单。
7. 消费者构造时自验清单内容 SHA，对全部制品各做一次文件 SHA，同进程后续访问按文件身份命中缓存。源/年度标签解析在同进程最多顺序读取一次，只缓存内存 `int8`，不落盘。
8. P2 新增训练逐流与训练序列的正/负计数、正类权重、序列有效位和标签内容 SHA 聚合收据，仍不写逐样本标签。
9. `float64 -> float32` 后重新检查有限性，溢出转为规范 NaN 并与原始非有限、结构非法分开计数。P4 地标、状态和收据成功后按精确文件名删除唯一 B 臂临时映射；失败前保留。

## 十一、P4 单变量磁盘修订

- 独立 `float64` 噪声映射已删除；状态和收据不再记录该文件 SHA。
- 随机流身份固定为 `chunked-row-major-default_rng-bitwise-equivalent-v1`，记录 NumPy 版本、连续 `[start,end)` 批、批形状、批内容 SHA、批数和最终 filled-noisy 无头内容 SHA。不按列生成，不重置种子。
- 以全部源产品尚未存在作保守上界，除三份特征主体外还纳入 `I/M/E/T`、切分、位图和实体摘要等 `0.4419 GiB` 辅助制品，新资源公式为 `35.4807 GiB`。B76 实测可用 `36.0432 GiB`，准入后仍保留 `10 GiB` 低水位，理论余量约 `0.5625 GiB`。

### 9.2 独立 P6 状态

- 已固定 Parquet 字节数 `2775972952`、SHA-256 `1d96f0a...63057a`、PyArrow 25 物理模式 SHA-256 `540be446...143c1`、`20227356` 行、`21` 行组和 `101` 字段。
- 已固定 `I24/M24` 文件 SHA，实现封印后单胜出臂视图、Raw83、实体/时间侧车、序列清单与运行内标签能力。
- 由于尚无合法源年胜出臂，P6 只能标为 `IMPLEMENTED_BUT_QUALIFICATION_BLOCKED`，不得同步后启动。

### 9.3 唯一修订静态验收

- 一次 `py_compile`：通过。
- 一次模块导入：通过，输出源/年度模式、`83` 字段和 `76` 个分位列。
- 工具帮助、源配置、年度配置、两个 `bash -n`：全部通过。
- 年度配置静态输出 `runtime_admitted=false`、`winning_arm=null`，证明没有预设胜者。
- 未访问服务器、ZIP、Parquet 或真实数组；修订后仍是“设计可执行，真实数据待证”。

## 十、最后一次针对性修复

1. P1 的 Raw83/实体/时间文件和 P5 的 A/B 视图分别检查 `final` 与 `partial`。每个候选文件逐批对照组件收据 SHA：有效 partial 优先补完并替换 final；partial 无效而 final 有效时保留 final 并清理精确 partial；两者都无效时从源流重建 partial。不再要求全部文件同时为 final 或 partial。
2. 源/年度世代已存在时，普通入口会幂等验制品，并补建或修正当前指针、P5/P6 阶段收据、`sealed` 状态和完成状态。两个启动器只有在普通入口补建后再通过产品验证，才返回“已完成”。
3. P6 配置的阻断态同时保持源清单文件 SHA、输入臂收据路径/SHA、输入臂内容 SHA、胜出变换状态 SHA、胜出源视图内容 SHA、资格封印 SHA 和胜出臂全为 `null`。合法选择后必须由外部冻结值注入；P6 依次比对配置、收据、源清单和资格封印，不再从封印读值后与自身比较。
4. 公开的任意 Parquet 路径打开函数已变为只消费已核年度配置的私有入口。阶段身份不再哈希含绝对路径的上游收据文件，而是递归删除路径后哈希规范语义摘要。
5. 源 `dataset-manifest.json` 正式发布 P2 `training_weight_aggregate`；`train` 消费者只从该字段读取权重，并校验其标签 SHA，不使用全年 `label_aggregate`。年度清单新增 Raw83 和胜出视图的无 NPY 头内容 SHA，同时记录源变换状态 SHA 和源视图内容 SHA。
