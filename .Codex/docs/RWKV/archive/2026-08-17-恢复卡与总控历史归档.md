# RWKV 恢复文档历史归档

- **归档日期**：2026-08-17
- **来源**：`RWKV当前恢复卡.md` 与 `RWKV路线总控.md` 精简时切出的历史段落，**原样保留未作改写**。
- **用途**：只读核验既往事实。当前状态一律以两份主文档为准；本文件与主文档冲突时，**以主文档为准**。
- **不得**：不得把本文件内容复制回主文档，不得作为当前任务入口，不得据此启动实验。

---

## 一、原恢复卡「更早的历史状态（2026-08-13 及以前）」与「2026-08-12 内存事故与并发边界」

## 更早的历史状态（2026-08-13 及以前，与上节冲突时以上节为准）

- C12、C12-R、R1 与候选 B 的种子 42 Q0 均已完成且**不晋级／弱证据不利**；全部为 `screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`，不得写成正式证伪、论文结果或方法有效。不得重跑这些身份，也不得依据目标开发标签追参。候选 B 详见[实现与运行报告](2026-08-12-候选B角色分离物理时间RWKV-Q0实现与运行报告.md)。
- **第四章实体流数分桶诊断已完成**：`ch4-entity-length-bucket-diagnostic-seed42-v1` 为 `finished/complete/exit=0`；四格冻结指标逐项零差，`47,115` 实体 / `752` 正实体，20 个检查点且均 `seen_all=true`，不保存逐样本分数。该运行仅为 `screening_only=true`、`formal_paper_evidence=false`、`independent_test=false`。`C11-C00` 五桶实体 AP 增量为 `+0.123948/+0.239138/+0.137851/-0.322915/-0.302212`，短中实体有益而长实体显著有害，禁止宣称全局提升。新增 Q0「实体均匀训练与直接实体 AP 优化」仅用于提高 C11 实体 AP，与 BBSE 决策层分开且不替换当前 BBSE 早停方案；首次失败时为**工程失败待修复／实验待证**，不构成机制失败。[方案册](2026-08-13-第四章实体均匀训练与直接实体AP优化方案册.md)（399 行）及 [Q0 实验实施计划](2026-08-13-第四章实体均匀训练与直接实体AP-Q0实验实施计划.md)（396 行）已冻结并终检。接口勘察已确认现有缓存缺 LSPR23 实体成员索引，真实实现必须从 LSPR23 原始 ZIP 流式内存构建，禁止以 I23 序列假冒实体均匀测度，且不持久化逐样本制品。原创审计已完成，最终[报告](2026-08-13-第四章实体矩AP原创性与数学边界.md)为 351 行，8 篇核心／直接近邻全文已逐式核验；Yao 2023 Wiley 正式 PDF 已迁入 `raw/papers/methodology/ranking/2023-Yao-GeM-SmoothAP-IET.pdf`。可学习 Lp/GeM 加直接 AP 已被占用，不能作为原创。随机袋 p 阶矩及其导数在严格条件下无偏，不推出 SOAP/SOX 非线性 AP 复合梯度无偏；C11 先抽 K 再编码改变分数总体，更不能套简单抽样无偏结论。R1 为待门禁插值估计，新增双独立 `K=128` 袋、`K=32/64/128` 一致性、完整实体端到端梯度对照；门禁失败则禁止 M0R1/M1R1，仅运行 M0R0/M1R0 实体均匀加 BCE 诊断。三份生产文件已实现并静态验收，工具／配置／启动器 SHA-256 依次为 `564d6bc18e2442f25c532e624886aababbcf75e3ec022c60e0d58f4f13f1f7d3`、`d9cb3a6d1ef886b44797b0c0138f61a877044f49cc8735de0f9060486a7f97f3`、`33ae46f502d53261c357a55f61840df26057dc529b2f912e2e656f5f3846cdb9`；已同步 C56，远端哈希一致。首次 Q0 已停止，失败根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1` 必须保留，禁止覆盖。P2 索引完成 `16,353,511` 条流／`150,680` 实体／`239` 正实体，4 个 `p` 门全通过；M0R0 完成 `20000` 步、累计编码 `50,627,995` 条流。其后同一进程第二次 `swanlab.init` 向项目 POST 返回 `401 Unauthorized`；旧代码错误写为 `EVALUATION_FAILED`，实际为训练跟踪生命周期失败，准确状态为 `TRAINING_TRACKING_FAILED`。`target_labels_read=false`，无评价、无科学指标、无研究裁决。rerun1 尚未修复或重跑；后续唯一动作以紧随本段的“Q0 失败状态更正”中的防复发门禁为准。先前 SSH 不可用判断已被同一仓库入口真实连接推翻，不得作为服务器事实。代理状态：`plan gpt-5.6-sol/max`、`interface gpt-5.6-terra/medium`、`novelty gpt-5.6-sol/max` 均完成。模型私有归档与细节见[实施与运行报告](2026-08-13-第四章实体流数分桶诊断实施与运行报告.md)。
- **Q0 rerun2 最终机械状态（以本段为准）**：身份／运行根为 `ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`／`runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`，`screen=ch4-entity-ap-q0-s42-rerun2`；`status=COMPLETED_Q0_REJECTED/COMPLETE/exit=0`，启动器为 `finished/complete/exit=0`。`strict_2x2_complete=true`、`candidate_advanced=false`、`target_labels_read_after_selection_seal=true`、`failed_run_artifacts_reused=false`、`random_lp_gate=PASSED`；逐流／实体分数与源索引均未持久化。四格实体 AP（M0R0/M1R0/M0R1/M1R1）=`0.4563850872/0.2892240420/0.3158986812/0.4234940164`；`DR@4%FPR=0.6502659574/0.4747340426/0.4853723404/0.6954787234`；逐流 AP=`0.3013773042/0.2240005305/0.3201337105/0.1592074936`；`interaction=0.2747563804`；尾部实体 AP 增量=`-0.1070404121/-0.0718340368`；`entity_uniform_measure_supported=false`、`direct_entity_ap_supported=false`；历史 M0R0 差=`0.0066029128`。峰值 `RSS=12658.45 MiB`、GPU=`1893.14 MiB`；所有逐格与聚合 SwanLab 运行均完成。仅属 `screening_only=true`、`formal=false`、`formal_paper_evidence=false`、`independent_test=false`；单种子 Q0 不进入论文正式表。详细科学解释待结果分析代理复核。
- **Q0 失败状态更正（覆盖上段有关 rerun1 已进行的过期表述）**：首次 Q0 的 M0R0 在 `20000` 步、累计编码 `50,627,995` 条流后，因同一 Python 进程第二次 `swanlab.init` 向项目 POST 返回 `401 Unauthorized` 而停止。P2 源索引已完成 `16,353,511` 条流、`150,680` 实体、`239` 正实体，4 个 `p` 门均通过；它们只是工程运行事实。该问题违反既有[故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)，为工程回归而非科学失败；准确状态 `TRAINING_TRACKING_FAILED` 覆盖旧代码误记的 `EVALUATION_FAILED`。`target_labels_read=false`，无评价结果、科学指标或研究裁决。失败根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1` 和原工具／配置／启动器 SHA 必须保留；因缺完整恢复合同，M0R0 不得复用。当前状态为**工程失败待修复／实验待证**，不是机制失败。**唯一下一步（尚未修复或重跑）**：以 rerun1 新身份从头训练，每格独立 Python 进程和独立 SwanLab run，每次 `init` 前保存 `ping/verify` 收据，`401` 仅新进程有界重试一次。主代理负责启动前核验防复发门禁、失败根保留和新身份；原实现代理负责修复训练跟踪生命周期并交付 rerun1 启动收据，不得复用未经完整恢复合同独立核验的 M0R0。
- **第三章四格归档收据（非新实体 AP Q0）**：重跑 C00/C01/C10/C11 后，逐流 AP、实体 AP(Lp)、`DR@4%FPR` 相对各自冻结值的绝对差均为 `0`，证据为 `runs/diagnostics/ch4-entity-length-bucket-diagnostic-seed42-v1/aggregate-results.json` 与上述实施报告。模型已完成私有 Hugging Face 上传至 `Heehobino/lspr23-24-causal-prefix-lp-pooling-ch3`：提交 `9081f8b028e6c7a63cce0a64d2dd894958163ab8`、`private=true`，含 `20` 个 `safetensors` 检查点和 `7` 个配套文件；`manifest` SHA-256=`024daf41044a0a61476515e46edca8140c3e5cea0aef8a7e7d2d5213964790fb`，推理入口 SHA-256=`12766a81f05d81cef2574158d220b24ad343a77316191c1adf1c4671ed7694e5`。
- **Q0 当前状态覆写**：前述 v1／rerun1 失败记录仅作历史保留；当前 Q0 以本节“Q0 rerun2 最终机械状态”为准：`COMPLETED_Q0_REJECTED/COMPLETE/exit=0`、`strict_2x2_complete=true`、`candidate_advanced=false`、`target_labels_read_after_selection_seal=true`、`failed_run_artifacts_reused=false`、`random_lp_gate=PASSED`。逐流／实体分数与源索引均未持久化，所有逐格与聚合 SwanLab 运行均完成。仅属 `screening_only=true`、`formal=false`、`formal_paper_evidence=false`、`independent_test=false`；单种子 Q0 不进入论文正式表。详细科学解释待结果分析代理复核。
- **跨年度 P0 `rerun2` 已完成**：运行根为 `runs/candidates/crossyear-rare-mass-protected-asymmetric-pot-q0-seed42-v1-rerun2`，启动器为 `finished/complete/exit=0`；无进程、无 GPU 占用，五个聚合 SwanLab 运行已上传。`rerun1` 死于容器内存事故，`v1` 为 `worker_failed/exit_code=1`，均不是本次完成结果。
- **已核验 Q0 指标与边界**：全程 `screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`。B0 GPU 锚 AP=`0.03746366118798747`；M1 保护 POT AP=`0.03840115350812821`；U2 无质量下限与 M1 相同；U1 全量 OT AP=`0.039087019199325335`；M1+M2 安全回退与 B0 相同。共享 CPU 锚 AP=`0.038902589823969644`，U1 相对其仅 `+0.000184429375355691`；预算 `100/1000/10000` 的增量均为 `0`，所有变体恶意召回均为 `0.0004950495049504951`（`1/2020`）。
- **机械裁决**：质量保护独立机制不通过，回退组合无增益，`Q0不晋级/弱证据不利`，不是正式证伪；耗时 `6122.57` 秒，`peak_rss_mib=1978.949`。冻结合同见[跨年度泛化方案册](candidates/任务方案册-跨年度泛化-LSPR23到LSPR24.md)，实现记录见[Q0 实施与运行报告](2026-08-12-跨年度恶意质量保护部分传输-Q0实施与运行报告.md)。
- **GeNIS 跨场景协议无效已确认，候选不可裁决**：先前 Q0 资格失败事实不变；最新 C56 历史运行根为 `/root/autodl-tmp/thesis/experiments/genis/runs/baselines/genis-60s-scenario-holdout-safe-core-baselines-seed42-v1`，`finished/complete/exit=0`、`16/16`、`screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`、`official_test_accessed=false`。无特征矩阵、标签、折成员、逐样本概率或稳定键持久化。原 `safe-core68` 68 字段下 XGBoost/RF 八折四项指标均为 `1.0`，但该字段集不得再称为安全。归档来源与标签完全共线外，决定性只读诊断确认 `IdleTime` 在同源 `3-scenarios` 的 `203,179` 良性与 `341,406` 恶意间 ROC-AUC=`0.9959853720258349`，八场景约 `0.985220–0.997547`，其绝对时间量级编码演习阶段。科学结果无效；全 `1.0` 仅保留为工程/故障证据，禁止表述为强基线饱和或方法结果。详见[GeNIS Q0 实施与运行报告](../2026-08-12-GeNIS跨场景泛化与固定告警预算-Q0实施与运行报告.md)。
- **最新不可变执行边界**：后续新实验禁止持久化预处理特征矩阵、标签数组、折成员表、逐样本概率或拆分缓存；训练入口直接读取原始 ZIP，只在内存中构造特征、标签和折。允许保存原始数据副本、配置、模型、日志、状态和聚合指标。历史物化制品只保留为既有运行证据，不得复用为新实验入口，也不得未经确认删除。
- **GeNIS `corrected-v2` 已完成**：实际运行 `genis-60s-same-source-stable-key-corrected-v2-parallel-baselines-seed42` 完成 `16/16`，`finished/complete/exit=0`、`final_accessed=false`、`official_test_accessed=false`、无派生物化、SwanLab 成功。`IdleTime` 已排除，67 字段逐折单变量门禁通过、`blocked_fields=[]`；最高 `dMinPktSz` AUC=`0.913129218`，其次 `SrcPkts=0.901382`、`sMinPktSz=0.899666`、`DstJitAct=0.896266`。当前没有第二个达到 `0.98` 的单字段捷径，但多字段组合、攻击脚本模板或场景身份指纹尚未排除，禁止把高 AP 直接称为真实泛化饱和。旧全 `1.0` 消失；XGBoost/RF 的 AP 均值为 `0.9994556532/0.9993629431`，但宏 F1 均值仅 `0.6852240419/0.7338587603`，最差均为 `0.0994139345`；场景 4 两模型阈值召回 `0.0295276`，证明跨折决策不稳。仍为 Q0 筛选，不是论文证据。
- **GeNIS 支持量收缩跨场景极端尾部排序 Q0 已完成**：运行根 `/root/autodl-tmp/thesis/experiments/genis/runs/q0/genis-60s-support-shrunk-extreme-tail-ranking-q0-seed42-v1`，`112/112`、失败 `0`、`FINISHED`、`exit=0`、耗时 `1283.6032036095858` 秒、峰值 RSS `1.9923820495605469 GiB`；全程 `final_accessed=false`、`official_test_accessed=false`、`screening_only=true`、`formal_paper_evidence=false`。完整候选 `support-shrunk-scene-tail` 不晋级，`candidate_advanced=false`，仅是筛选否决，不是正式证伪或论文结果。为对齐朱焱雷论文，本 GeNIS 路线只使用 `seed=42`，不追加 `seed=43/44`，不追调。随机森林 `sample-cvar-tail` 的最坏场景 `recall@budget` 从 `0.029527617` 至 `0.408825707`、最大每万良性误报 `9.250694`，但主要由场景 4 驱动且 XGBoost 不复现，只是已发表机制强基线诊断，不能作为创新或晋级候选。GeNIS 分支唯一下一步是基于当前结果选择新候选；详见[Q0 实验结果报告](../2026-08-13-GeNIS支持量收缩跨场景极端尾部排序-Q0实验结果报告.md)。

## 并行候选调研检查点（不改变 RWKV/LSPR 主线）

- **GeNIS**：官方 `0-info`、`2-flows.zip`、`3-scenarios.zip`、`4-preprocessed.zip` 已迁入 `raw/datasets/GeNIS-2025/`，官方 MD5 与 ZIP 完整性通过；`1-packets.zip` 缺失，只阻塞逐包或首若干包任务，不阻塞流级实验。旧来源共线与 `IdleTime` 捷径已由 `corrected-v2` 修复。支持量收缩跨场景极端尾部排序 Q0 已完成且完整候选不晋级；本路线只使用 `seed=42`，不得追加 `seed=43/44` 或追调。GeNIS 分支下一步仅为根据当前结果选择新候选。GeNIS 仍是并行旁路，不改变 RWKV/LSPR 主线；跨数据集轨另行冻结，同域随机轨仅作发表复现。
- **新增论文边界**：Shukla 等已经覆盖多头自注意力、残差密集块、SMOTE 与随机欠采样，故这些不能作为新方案创新；其四个时间粒度表头与官方归档样本数存在错配，逐时间粒度数字在代码或映射未澄清前不得作严格复现门槛。Ikni、Belfedhal 只在背景中引用 GeNIS，不使用该数据实验。
- **用户最新裁决**：候选不要求新场景。GeNIS 同域随机轨只保留为发表复现；原完整场景留出因归档来源与标签完全共线而无效，后续必须改用同一来源内同时含良恶样本的场景数据或跨数据集共同字段协议。不得把既有动态采样、注意力或残差结构重新声称为原创。
- **TQH-C2**：官方五版本差异与任务机会矩阵已落盘；当前直接引用候选 `A=0/B=0`，不能解释为全球不存在引用。相似数据与方法文献仍在分批全文核验，未形成最终方案、未启动实验。
- 恢复这些调研时先读各自 `task_plan.md` 与 `notes.md`；不得仅凭聊天摘要继续检索或启动实验。

## 2026-08-12 内存事故与并发边界（强制先读）

- **C56 曾因三个实验并发内存耗尽而整机重启**。根因是启动器资源门禁只检查磁盘与显存，且余量读宿主机 `free`（报 `754 GiB`）而非容器 cgroup 上限（实为 `90 GiB`，`/sys/fs/cgroup/memory.max=96,636,764,160`），相差 `8.4` 倍。三个 `resource-gate.json` 只含 `disk_free_gib`、`disk_usage_fraction`、`gpu_free_gib` 且全部 `passed=true`。**这是工程缺陷，不改变任何候选的证据等级，不得记为科学失败。** 完整取证见[C56 三进程并发内存耗尽事故取证](2026-08-12-C56三进程并发内存耗尽事故取证.md)，可复用规则见[故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)「容器内存上限、并发准入与大矩阵载入」。
- **重启任何实验前必须先过 `tools/memory_admission_gate.sh <预计峰值GiB> <运行名>`**：该门禁读 cgroup 而非 `free`，默认拒绝并发；退出码 `10` 内存不足、`11` 已有并发实验、`12` 读不到 cgroup 上限。
- 内存足迹（A/C 为按行列数与 dtype 计算，TL1 为 `/proc` 实测）：

| 运行 | 修复前峰值 | 修复后峰值 | 说明 |
| --- | --- | --- | --- |
| A 线 | `36–40 GiB` | `29–33 GiB` | 阶段一 Arrow 整表 `+F`（`24.16` 或 `36.24`，取决于列 dtype）；阶段二 `F+X_tr+X_va = 36.04`。修复后阶段二降为 `F+X_tr = 28.98` |
| C 线 | `24–36 GiB` | `约 16 GiB` | 死亡时正处阶段一峰值；修复后 Arrow 侧降到单个 row group |
| TL1 | `VmHWM=1.93 GiB` | 不需修复 | `VmPeak=22.92 GiB` 全是 `mmap_mode="r"` 的虚拟映射 |

- **`mmap_mode="r"` 是正确做法**（TL1 常驻仅 `1.93 GiB`）；A/C 两线原来的全量 `pq.read_table` 加同时驻留 numpy 副本是错误做法，**已修复并本机复核**：语义常量逐项比对未变，`QuantileDMatrix` 的 `ref` 链未破坏，`py_compile` 退出码均为 `0`。
- **未核实量**：408 个特征列在 parquet 内的物理 dtype（float32 或 float64）决定阶段一峰值落在 `24` 还是 `36 GiB`，是内存核算中最大的未定量，重启前应在服务器读 schema 确认。
- **重启纪律**：A 线与 C 线均须先过 `tools/memory_admission_gate.sh`；修复后两者合计约 `45–49 GiB`，加 TL1 仍在 `90 GiB` 内，但**首次重启建议仍串行**，用新加的 `log_memory_state` 阶段日志实测真实曲线后再决定是否并发。
- 事故中幸存的有效结论（C 线 `run.log`）：修正活动键边界后**早期预警 B 任务硬门禁通过**（`gate_b pass=True`，三段严格正锚点 `2883/1968/356`、独立前体簇 `1025/753/158`），**推翻此前「B 门濒临不过」的判断**；同时查出共享筛选视图**切分键核验失败**（按去重活动窗起始推导的 60% 边界 `1709758690000000000` ≠ 视图实际 `validation_min=1709774230000000000`），需独立复核视图切分口径。
- **已完成比较证据**：跨年度完整基线矩阵 `17/17` 完成，最强 `dijk2026_xgboost` AP=`0.036855901673935376`；候选 B 最佳离散变体 AP=`0.019205959409695748`，角色分离变体 AP=`0.010123021167351991`，机制门失败并停止；C12-R 六格最高为 DREF AP=`0.009274477710716156`，其余门控/裁剪/预算/回退和随机匹配对照均未超过 DREF，恶意类 F1 与召回均为 `0`；R1 最佳 GroupDRO AP=`0.012877901366876719`，低于共享 XGBoost AP=`0.038902589823969644`。
- **单年度旁路事实**：LSPR24 同年当前窗 XGBoost 验证 AP=`0.9690348794483452`；筛选宽表 `label=-1` 占 `60.4075%`，训练与评价必须先只保留 `0/1`。该事实服务其他单年度任务，不改变当前 LSPR23→LSPR24 合同。


---

## 二、原路线总控「第 2 节 已完成事实」

## 2. 已完成事实

### 2.1 数据

- LSPR24 核心流表 `lspr24_v2.parquet` 已下载本机：2,775,972,952 字节，20,227,356 行，官方 MD5 与本地 SHA-256 均已核验。
- `attack_narratives.json` 已下载并按官方大小与 MD5 验收；在时间封存清单发布前继续禁止解析内容。
- `eve.log` 与约 5 GB 主机日志不阻塞第三章流量基线，只有第四章需要真实动作起点证据时才按需下载。
- LSPR24 G0 数据与切分合同已按“开发区先行、最终冻结后封存并一次评价”拆分为 `G0-D/G0-F`，合同版本 `lspr24-g0-v5-staged`，SHA-256 为 `877488f3529c6c862b060a74782d1904aae81512ea1169c713ba20d8c3f31a0e`。这只允许进入 `G0-D` 实现，不代表任一门禁已通过。
- A44 候选筛选视图运行根为 `runs/data-prepared/lspr24-screen-wide-v1`：`192/192` 分区完成，`development_window_count=7,946,899`（训练 `5,633,517`、验证 `2,313,382`），最终区丢弃窗口 `2,296,152`；`non_finite_flow_count=588,676`、`timestamp_order_corrected_flow_count=1,945,836`。最终占用 `5.1 GiB`，工作目录已清理；`screen_ready=true`、`formal_evidence=false`、`final_accessed=false`，仅可作为候选筛选视图完成事实。
- LSPR23 官方流表压缩包 `raw/datasets/LSPR23/ls23pr_flows.zip` 已完成验收：大小 `1,925,103,715` 字节，MD5 `f3f5bf9f7cecf2186511eabb38f7accc` 与官方一致，`unzip -t` 无错误，内部仅 `ls23pr_v1.csv`；未全量解压。
- LSPR23 与 LSPR24 原始字段名集合均为 101 列；跨年度首轮共同输入冻结为 77 个合法因果字段，采用不入模的密钥化无向 `2-IP` 侧车分组、最长 128 的非重叠序列和严格年度/分区断开。开发合同状态为 `CONTRACT=READY`，物化仍需服务器只读预检与真实收据。
- C56 的 Q0 `bounded_quick_cache` 已成功：`source-train/source-validation/target-prefix/target-development` 分别为 `142069/262136/262144/261839` 条流，分别含 `2926/6575/3820/2877` 个完整序列，总计 `928188` 条流和 `16198` 个序列；输出根为 `runs/data-prepared/lspr23-lspr24-bounded-quick-q0-v1`，成功收据为 `final_accessed=false`。Python 只读缓存已发布 40 个制品；Arrow 固定列表批次偏移和暂存路径发布错误均已修复并真实重建，不构成科学失败。

### 2.2 候选方案

- 原方案 A/B 文档只作为候选来源，不能代表第三章已经冻结；完整候选池与证据状态见第三章候选方案登记册。
- 用户已确定候选生成方向：从 RWKV 论文与生态应用中提取机制，移植到普通基座模型用于安全领域；优先复用现有数据集，必要时重新物化合法开发视图。
- **第四章实体 AP 候选 Q0**：「实体均匀训练与直接实体 AP 优化」仅面向提高 C11 实体 AP，不替换当前 BBSE 早停方案；首次失败时为**工程失败待修复／实验待证**，首次失败不是机制失败，也没有实验结果。[方案册](2026-08-13-第四章实体均匀训练与直接实体AP优化方案册.md)（399 行）与 [Q0 实验实施计划](2026-08-13-第四章实体均匀训练与直接实体AP-Q0实验实施计划.md)（396 行）已冻结并终检。接口勘察确认现有缓存缺 LSPR23 实体成员索引，真实实现必须从 LSPR23 原始 ZIP 流式内存构建，禁止以 I23 序列假冒实体均匀测度，并禁止持久化逐样本制品。原创审计已完成，最终[报告](2026-08-13-第四章实体矩AP原创性与数学边界.md)共 351 行，8 篇核心／直接近邻全文均已逐式核验；Yao 2023 Wiley 正式 PDF 已迁入 `raw/papers/methodology/ranking/2023-Yao-GeM-SmoothAP-IET.pdf`。可学习 Lp/GeM 加直接 AP 已被占用，不能作为原创。随机袋 p 阶矩及其导数的严格条件无偏性不推出 SOAP/SOX 非线性 AP 复合梯度无偏；C11 先抽 K 再编码会改变因果上下文的分数总体，亦不得套用简单抽样无偏结论。R1 已改为待门禁插值估计，设有双独立 `K=128` 袋、`K=32/64/128` 一致性和完整实体端到端梯度对照；门禁失败时禁止 M0R1/M1R1，只运行 M0R0/M1R0 实体均匀加 BCE 诊断。三份生产文件已实现且通过静态验收：工具 SHA-256=`564d6bc18e2442f25c532e624886aababbcf75e3ec022c60e0d58f4f13f1f7d3`，配置 SHA-256=`d9cb3a6d1ef886b44797b0c0138f61a877044f49cc8735de0f9060486a7f97f3`，启动器 SHA-256=`33ae46f502d53261c357a55f61840df26057dc529b2f912e2e656f5f3846cdb9`；已同步 C56，远端哈希逐项一致。首次 Q0 已停止，失败根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1` 必须保留，不得覆盖。P2 已完成流式源实体索引：`16,353,511` 条流、`150,680` 个实体、`239` 个正实体，4 个 `p` 门均通过；M0R0 完成 `20000` 步、累计编码 `50,627,995` 条流。随后同一进程第二次 `swanlab.init` 向项目 POST 时返回 `401 Unauthorized`。旧代码写入的 `EVALUATION_FAILED` 不准确，实际是训练跟踪生命周期失败，准确状态为 `TRAINING_TRACKING_FAILED`。`target_labels_read=false`，没有评价、科学指标或研究裁决。rerun1 尚未修复或重跑；后续唯一动作以紧随本段的“Q0 失败状态更正”中的防复发门禁为准。先前 SSH 不可用判断已由同一仓库入口的真实连接推翻，不得保留为 C56 服务器事实。代理状态为 `plan gpt-5.6-sol/max`、`interface gpt-5.6-terra/medium`、`novelty gpt-5.6-sol/max` 均已完成。
- **Q0 rerun2 最终机械状态（以本段为准）**：身份／运行根为 `ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`／`runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`，`screen=ch4-entity-ap-q0-s42-rerun2`；`status=COMPLETED_Q0_REJECTED/COMPLETE/exit=0`，启动器为 `finished/complete/exit=0`。`strict_2x2_complete=true`、`candidate_advanced=false`、`target_labels_read_after_selection_seal=true`、`failed_run_artifacts_reused=false`、`random_lp_gate=PASSED`；逐流／实体分数与源索引均未持久化。四格实体 AP（M0R0/M1R0/M0R1/M1R1）=`0.4563850872/0.2892240420/0.3158986812/0.4234940164`；`DR@4%FPR=0.6502659574/0.4747340426/0.4853723404/0.6954787234`；逐流 AP=`0.3013773042/0.2240005305/0.3201337105/0.1592074936`；`interaction=0.2747563804`；尾部实体 AP 增量=`-0.1070404121/-0.0718340368`；`entity_uniform_measure_supported=false`、`direct_entity_ap_supported=false`；历史 M0R0 差=`0.0066029128`。峰值 `RSS=12658.45 MiB`、GPU=`1893.14 MiB`；所有逐格与聚合 SwanLab 运行均完成。仅属 `screening_only=true`、`formal=false`、`formal_paper_evidence=false`、`independent_test=false`；单种子 Q0 不进入论文正式表。详细科学解释待结果分析代理复核。
- **Q0 失败状态更正（覆盖上段有关 rerun1 已进行的过期表述）**：首次 Q0 在 M0R0 训练完成 `20000` 步、累计编码 `50,627,995` 条流后，因同一 Python 进程第二次 `swanlab.init` 向项目 POST 返回 `401 Unauthorized` 而停止。P2 已完成源索引 `16,353,511` 条流、`150,680` 实体、`239` 正实体，4 个 `p` 门均通过；这些不能构成机制证据。该错误违反既有[故障预防手册](../agents-split/llm-probe-failure-prevention-handbook.md)，是工程回归而非科学失败；准确状态为 `TRAINING_TRACKING_FAILED`，不是旧代码误记的 `EVALUATION_FAILED`。`target_labels_read=false`，没有评价结果、科学指标或研究裁决。失败根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1` 及原工具／配置／启动器 SHA 必须保留；因无完整恢复合同，不复用 M0R0。当前为**工程失败待修复／实验待证**，不是机制失败。**唯一下一步（尚未修复或重跑）**：以 rerun1 新身份从头训练，每格独立 Python 进程、独立 SwanLab run；每次 `init` 前保存 `ping/verify` 收据；`401` 仅新进程有界重试一次。主代理负责启动前核验全部防复发门禁、失败根保留与新身份；原实现代理负责修复训练跟踪生命周期、交付 rerun1 启动收据，且不得复用未经完整恢复合同独立核验的 M0R0。
- **第三章四格归档收据（不属于新实体 AP Q0）**：C00/C01/C10/C11 重跑后，逐流 AP、实体 AP(Lp)、`DR@4%FPR` 对各自冻结值的绝对差全部为 `0`，原始证据为 `runs/diagnostics/ch4-entity-length-bucket-diagnostic-seed42-v1/aggregate-results.json` 与[实施报告](2026-08-13-第四章实体流数分桶诊断实施与运行报告.md)。第三章四格模型已完成私有 Hugging Face 上传：`Heehobino/lspr23-24-causal-prefix-lp-pooling-ch3`，提交 `9081f8b028e6c7a63cce0a64d2dd894958163ab8`，`private=true`，含 `20` 个 `safetensors` 检查点和 `7` 个配套文件；`manifest` SHA-256=`024daf41044a0a61476515e46edca8140c3e5cea0aef8a7e7d2d5213964790fb`，推理入口 SHA-256=`12766a81f05d81cef2574158d220b24ad343a77316191c1adf1c4671ed7694e5`。
- **Q0 当前状态覆写**：前述 v1／rerun1 失败记录仅作历史保留；当前 Q0 以本节“Q0 rerun2 最终机械状态”为准：`COMPLETED_Q0_REJECTED/COMPLETE/exit=0`、`strict_2x2_complete=true`、`candidate_advanced=false`、`target_labels_read_after_selection_seal=true`、`failed_run_artifacts_reused=false`、`random_lp_gate=PASSED`。逐流／实体分数与源索引均未持久化，所有逐格与聚合 SwanLab 运行均完成。仅属 `screening_only=true`、`formal=false`、`formal_paper_evidence=false`、`independent_test=false`；单种子 Q0 不进入论文正式表。详细科学解释待结果分析代理复核。
- RWKV-7 官方列向量更新方向、方案 A 投影范数边界、方案 B 完整多层状态边界、零合格生成目标和参数/计算匹配条件已经完成文档修订。
- 上述状态只能标为“设计可行、实验待证”，不能写成实验支持或论文确定贡献。
- LSPR24 开发区原子筛选中 B1F/A2 的 PR-AUC 分别为 `0.9997750317365747` 与 `0.9997778729511367`。A2 仅增加 `2.84×10^-6`，只能作为当前原子代理的弱不利证据，不构成创新。
- Leoste（2025）全文已核验：在 LSPR23 训练、LSPR24 零重训测试下，无包到达间隔的一维卷积神经网络与随机森林的准确率/精确率/召回率/F1 分别为 `84.66/8.05/47.69/13.78` 与 `97.30/15.65/1.18/2.20`；有包到达间隔时分别为 `91.98/12.47/35.23/18.42` 与 `97.25/38.86/12.06/18.41`。这支持跨年度任务存在显著难度，不证明任一 RWKV 机制有效。
- Dijk（2026）全文 `raw/papers/datasets/LSPR24/ssrn-6597680.pdf` 已核验并形成结构化笔记。其 LSPR23→LSPR24 跨年 AP 明显下降，且 `2-IP` 在八种构造中的跨年中位退化最小；这些结果用于冻结任务与序列构造，不证明 C12 有效，也不能代替本课题实验。

### 2.3 基线依赖

- DeltaNet 官方仓库、固定提交和本机快照已核验归档；正式环境尚未导入，真实前向、反向与训练均未运行。详见[DeltaNet 官方源码核验与本机归档记录](2026-08-11-DeltaNet官方源码核验与本机归档记录.md)。
- 旧服务器已冻结并回收 RWKV-LM、Time-Series-Library、Mamba 和 `causal-conv1d` 四份官方源码快照。
- 21 个离线制品、源码清单和压缩包已经回收本机；内部 `manifest.sha256` 共 60/60 项通过。
- HGB、XGBoost 和普通 PyTorch 序列基线具备核心依赖。TimesNet 仍需正式环境试装。
- 旧服务器已预装 CUDA 13.0.3 编译组件。项目级环境收据已固定 `/usr/local/cuda-13.0` 与正式 `.venv` 的 `ninja 1.13.0`；`sm_120` 独立 CUDA 编译、GPU 运行和 `torch.utils.cpp_extension.CUDA_HOME` 验证通过。系统级 NVIDIA 源、profile 和多余 `ninja-build` 均未保留。

---

## 三、原路线总控「3.1 更早的活动任务」与「第 4 节 唯一下一顺序」

### 3.1 更早的活动任务（2026-08-13 及以前）

**最新完成：第四章实体流数分桶诊断与模型归档**。运行根
`runs/diagnostics/ch4-entity-length-bucket-diagnostic-seed42-v1/` 为 `finished/complete/exit=0`。四格
`C00/C01/C10/C11` 对冻结逐流 AP、实体 AP 与 `DR@4%FPR` 逐项零差；覆盖 `47,115` 实体、`752` 正实体，
保存 `20` 个检查点，全部 `seen_all=true`，未持久化逐样本分数。全程 `screening_only=true`、
`formal_paper_evidence=false`、`independent_test=false`。`C11-C00` 的实体 AP 在五桶
`1-2/3-10/11-100/101-1000/1001+` 依次为 `+0.123948/+0.239138/+0.137851/-0.322915/-0.302212`：短中实体有益、
长实体显著有害，不能写成全局普适提升。下一 AP 候选为「实体均匀训练测度×直接实体 AP 目标」，状态为设计可行、
实验待证，与 BBSE 决策层分开。模型包已备份至私有 Hugging Face 仓库
`https://huggingface.co/Heehobino/lspr23-24-causal-prefix-lp-pooling-ch3`，提交
`9081f8b028e6c7a63cce0a64d2dd894958163ab8`，20 个检查点加 7 个配套文件，`private=true`；manifest
SHA-256=`024daf41044a0a61476515e46edca8140c3e5cea0aef8a7e7d2d5213964790fb`，推理
SHA-256=`12766a81f05d81cef2574158d220b24ad343a77316191c1adf1c4671ed7694e5`。详见
[实施与运行报告](2026-08-13-第四章实体流数分桶诊断实施与运行报告.md)。

1. **Rust `G0-D` 基础实现**：任务 1 至任务 5、正式 Parquet 两阶段入口和发布回滚目标测试已经通过；独立复核确认旧的最终区注入、发布残留和虚假 READY 问题已经处理。
2. **完整数据视图状态**：A44 上的 5 秒候选筛选视图已完成，但 `formal_evidence=false`，尚不能认定为完整开发视图或正式结果。仍须核验合同第 6.1/11.2 节要求的 5 秒窗口、完整字段清单、历史索引、推断锚点、评价簇和全部开发收据；不得用单个原始整数列或候选筛选视图冒充 G0-D 数据集。
3. **窗口粒度敏感性**：已登记 [窗口粒度敏感性实验计划](2026-08-08-LSPR24窗口粒度敏感性实验计划.md)。该实验仅在 5 秒主流程冻结后，使用开发区共同锚点比较 `G1=1秒×H20` 与 `G5=5秒×H4`；不宣称 5 秒最优，不触碰当前物化，也不以敏感性结果更换主方法。
4. **表格基线适配**：Q0 v2 已在 `/root/autodl-tmp/thesis/experiments/llm_probe/runs/baselines/lspr-crossyear-tabular-q0-seed42-v2` 完成，收据为 `final_accessed=false`。目标开发恶意基率为 `0.007714664354813454`；HGB 的 PR-AUC/宏平均 F1/恶意类 F1 为 `0.037147851122101574/0.5014316332078297/0.00936768149882904`（训练 `3.657` 秒、推理 `0.724` 秒），随机森林为 `0.015572256018043917/0.5020144520534988/0.011176157530601383`（`8.150/0.551` 秒），XGBoost 为 `0.038902589823969644/0.49740450223002863/0.0016769144773616546`（`4.344/0.286` 秒）。三者源验证 PR-AUC 约为 `0.99999/0.99993/0.99999`，构成严重跨年度退化的首条真实证据，但单种子 Q0 不构成候选裁决。旧 v1 因阈值扫描为 `O(n²)` 被主动中止；修为精确 `O(n log n)` 后 v2 成功，v1 不得用于结果。SwanLab 曾将 workspace/project 错拼为含斜杠的项目名，历史指标未上传；本地指标完整，代码已改为 `workspace=mortiswang`、`project=malicious-traffic-llm`，补传须经用户明确授权。
5. **跨年度 C12 实现**：旧 C12 的广义 Delta 公式已否决为原创主张，但跨年度数据工程继续复用。新 C12 为“漂移触发有界状态控制与可信双参照校正”：既有状态核仅生成状态提议，原创候选限定为核后的单步／累计影响预算、冻结／回退，以及冻结源锚与隔离候选参照的可信限量晋升。新设计含 `36` 个实质公式和 `1` 个算法。阶段 A v1 的五模型结果因等量序列池未落实、BF16 并列概率阈值错误和源验证概率收据缺失而整体无效，不得引用其排序或指标；v2 与阶段 B 三变体完成且 `final_accessed=false`。目标开发 PR-AUC 为 `C12-DREF=0.00927447771`、`C12-CTRL=C12-FULL=0.00835439568`，三者恶意类 F1／召回均为 `0`；最强既有核 `K-DELTA=0.016977277`，`K-GDELTA=0.010194771`。`C12-FULL` 未超过最强既有核或最佳单机制，按快速合同停止 C12 继续扩展；严格结论为 `Q0快速筛选不晋升/弱证据不利`、`formal_paper_evidence=false`，不是正式证伪或论文结论。完整 RWKV-7 官方基线仍未完成，现有短键只能称为 RWKV-7 式任务适配矩阵状态核。
6. **LSPR 文献查全影响**：截至 2026-08-11 的公开检索范围内确认 4 个直接处理 LSPR 的工作，其中 3 个含模型实验。跨年度任务继续成立，但 `LSPR23→LSPR24`、`2-IP` 序列构造及 RF／XGBoost／GRU／Transformer 比较均已有先例，不能作为创新；详细证据见 [LSPR 直接使用论文查全审计](2026-08-11-LSPR直接使用论文查全审计.md)。
7. **跨年度理论融合文献制品**：新增20份有效PDF与20篇逐篇结构化笔记已完成；Zotero 20条均已入库，其中15条已确认进入专属集合，5条本机Connector条目等待网络同步。首选为候选 C 的 M1双层训练风险与M2上下文预算风险；备选为密度比加权风险控制，以及OT状态适配加BBSE/JCPOT门禁。FOIL、DIVERSIFY和非平稳域泛化已占用潜在时间环境发现与通用不变学习空间，C12已占用目标状态更新与可信新正常晋升空间。详见[跨年度理论融合综述](2026-08-11-跨年度任务非RWKV理论融合文献综述.md)。
8. **R1 双层尾部风险重构 Q0**：五个变体均完成 `732` 步、概率封存、NP/经验/GPD 风险拟合和独立评价，启动器退出码 `0`。原始 AP 为 ERM `0.0122180649394674`、GroupDRO `0.012877901366876719`、V-REx `0.012054515654151374`、CVaR `0.011156523682837203`、双层尾部风险 `0.012246728127212125`。GroupDRO 没有达到共享 XGBoost `0.038902589823969644`，环境探针 AUC 也未下降；M2 只在大告警预算出现局部增益，完整组合低于最佳单机制。裁决为 `Q0不晋级/弱证据不利`，转向候选 B。
9. **候选 B 与 C12-R 最终 Q0 状态**：候选 B 五变体全部完成，最佳离散变体 AP=`0.019205959409695748`，角色分离物理时间变体 AP=`0.010123021167351991`，增量为 `-0.009082938242343757`；机制门与晋级门均失败，按合同停止且不追加频谱支路。C12-R 六格全部完成，最高仍为原始 DREF AP=`0.009274477710716156`，其余资格门、裁剪、预算、回退及随机匹配对照均未超过 DREF，恶意类 F1 与召回均为 `0`。两者均为 `Q0不晋级/弱证据不利`、`formal_paper_evidence=false`、`final_accessed=false`，不是正式证伪。
10. **2026-08-12 跨年度 P0 `rerun2` 已完成**：运行根为 `runs/candidates/crossyear-rare-mass-protected-asymmetric-pot-q0-seed42-v1-rerun2`，启动器 `finished/complete/exit=0`，无进程、无 GPU 占用，五个聚合 SwanLab 运行已上传。全程 `screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`。B0 GPU 锚 AP=`0.03746366118798747`；M1 保护 POT AP=`0.03840115350812821`；U2 无质量下限与 M1 相同；U1 全量 OT AP=`0.039087019199325335`；M1+M2 安全回退与 B0 相同。共享 CPU 锚 AP=`0.038902589823969644`，U1 相对其仅 `+0.000184429375355691`；预算 `100/1000/10000` 的增量均为 `0`，所有变体恶意召回均为 `0.0004950495049504951`（`1/2020`）。质量保护独立机制不通过，回退组合无增益，机械裁决为 `Q0不晋级/弱证据不利`，不是正式证伪；耗时 `6122.57` 秒，`peak_rss_mib=1978.949`。
11. **GeNIS 支持量收缩跨场景极端尾部排序 Q0 已完成且完整候选不晋级**：运行根为 `/root/autodl-tmp/thesis/experiments/genis/runs/q0/genis-60s-support-shrunk-extreme-tail-ranking-q0-seed42-v1`，`112/112`、失败 `0`、`FINISHED`、`exit=0`、耗时 `1283.6032036095858` 秒、峰值 RSS `1.9923820495605469 GiB`；`final_accessed=false`、`official_test_accessed=false`、`screening_only=true`、`formal_paper_evidence=false`。完整候选 `support-shrunk-scene-tail` 的 `candidate_advanced=false`，仅构成筛选否决，不是正式论文证据或正式证伪。为对齐朱焱雷论文的单次运行口径，本 GeNIS 路线冻结为仅 `seed=42`，不追加 `seed=43/44`，不追调完整候选。随机森林 `sample-cvar-tail` 仅为已发表机制强基线诊断：最坏场景 `recall@budget` 从 `0.029527617` 至 `0.408825707`，最大每万良性误报 `9.250694`，但主要由场景 4 驱动且 XGBoost 不复现，不能作为本课题创新或晋级候选。GeNIS 分支唯一下一步是基于当前结果选择新候选。详见[Q0 实验结果报告](../2026-08-13-GeNIS支持量收缩跨场景极端尾部排序-Q0实验结果报告.md)。
12. **GeNIS 后续工程合同已修正为无物化**：禁止持久化预处理特征矩阵、标签数组、折成员表、逐样本概率或拆分缓存；训练入口直接读取原始 ZIP，只在内存中构造特征、标签和折。允许保存原始数据副本、配置、模型、日志、状态和聚合指标。历史物化制品仅保留为既有实验事实，不得复用为新入口，也不得未经用户确认删除。

13. **2026-08-13 Dijk 2026 全量复现与机制受控消融（当前主线）**：运行根 `runs/diagnostics/dijk-repro/`。全部 `screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`。（a）83 字段全量逐流复现 LSPR23→LSPR24 零样本，XGBoost AP=`0.2244424863`、ROC-AUC=`0.8807393901`，对位 Dijk 表 5 的 `0.2416`。（b）实体级评价（2-IP 无向对）`47,115` 实体 / `752` 正例，实体级 AP `0.523326`（n=1）–`0.547484`（n=100），DR@4%FPR `0.7207`–`0.8324`。（c）数据量消融：`142,069` 行 vs `16,353,511` 行、同 `6000` 步，AP 比 `0.93×`——数据量不是主因。（d）序列表示消融：逐流 MLP `0.1590128242` < 序列+因果前缀均值 `0.2878764332`，但同配置重跑得 `0.1848481203`，**单种子方差跨度 `0.103`，所有神经比较需三种子重做**。（e）累加式机制阶梯（单种子）B=`0.1848481203`、D1 标量衰减=`0.2490874595`、D2 逐通道衰减=`0.2210798271`、D3 数据依赖衰减=`0.2224845029`、D4 外积状态与查询读出=`0.0674930736`、D5 加 delta 移除键=`0.2173232994`。**D4 是唯一既无归一化也无 delta 移除键的档，也是唯一崩塌的档**，形成待验假设「状态必须有界」；D5 的 `+0.1498` 是补 D4 的坑，不是独立增益，累加式只能读相邻差。（f）单因子消融（6 档 × 3 种子）运行中，S2「只去归一化」与 S3「只换外积但保留归一化」为预注册决定性对照。
14. **2026-08-13 第三章双机制组合论证结构已裁决**：详见[第三章双机制组合创新的论证结构](2026-08-13-第三章双机制组合创新的论证结构.md)。要点：（a）朱焱雷第三章两机制均非首创，`[61]` Bengio 课程学习、`[62]` Cai CAT、`[60]` Roth 展开优化，其增量在于调度的三维具体化、落到加密流量、以及表 3-7（p.42）显式证明超可加（ET-BERT `+12.30%` > 单模块之和 `9.00%`）；（b）其两机制结构正交（外层控制 / 内层计算），本课题原配对「表示层 2-IP 聚合 × 决策层 2-IP 聚合」同键同信息，不正交，已废弃；（c）改用「因果前缀聚合（计算）× 有界性与在线实例归一化（控制）」，实体级聚合降级为评价协议；（d）**「因果前缀跨流聚合无先例」的主张作废**——Lee & Stolfo 2000 TISSEC Table VIII p.243 `past two seconds same-host count` 已系统化提出，本课题差异只在无界前缀、可微表示与 2-IP 边三处工程操作化；（e）RevIN 只支持「全局统计量不能消除训练测试分布差异」的诊断（§4.2.2 p.8 Table 3），不支持把整窗静态统计量改成在线扩展统计量的搬法，只能写成受启发的新构造；（f）不采用朱的单次运行无误差棒与「充要条件」断言，Jacobs CCS 2022 §8 用 50 次重复运行测稳定性，本课题今晚已犯其第 6、8 两项。

15. **2026-08-13 会话交接**：完整状态见[会话交接文档](2026-08-13-会话交接文档.md)，数据更正以[实验结论更正台账](2026-08-13-实验结论更正台账.md)为准，两者优先于本目录更早文档。要点：（a）第三章双机制已定（表示层因果前缀跨流聚合 + 决策层实体级可学 Lp 池化），全套实验完成，制品在 `runs/diagnostics/ch3-full/`；2×2 判据在实体 AP(Lp) 与 DR@4%FPR 两个口径成立（交互 `+0.091269` / `+0.0173`），在逐流 AP 口径不成立（交互 `-0.182777`），三口径须并列写出。（b）正文 `thesis/chapters/03-基于因果前缀聚合与实体级可学池化的跨年度加密恶意流量检测方法.md` 已产出 `1183` 行、图 `图3-1`~`图3-11` 三格式齐全，均未终校。（c）PINN 版第三章已归档至 `.Codex/docs/RWKV/archive-pinn-ch3/`，路线替换经用户明确裁决。（d）跨年度评价采用 fair-selection、LSPR24 全量、不设封存区；LSPR25 因申请不到已放弃，改预注册单配置。（e）第四章候选定为「跨年度漂移下的实体级约束早停告警」，不含 RL；证伪实验 v2 完成，漂移问题真实存在（源留出阈值迁移后 FPR `0.0153`、DR 损失 `0.0652`、阈值比 `9.60×`），BBSE 在源 FPR=0.05 工作点可用（对前缀真值误差 `4.54%`），但 9 个工作点中 6 个失效，工作点选择规则未解决。v1 结论全部作废。（f）关键数据事实：LSPR23 前 `0.1%` 实体占流 `76.40%`、占正例 `94.43%`，含恶意实体仅 `239` 个；训练按流数正比抽样使含恶意实体过采样 `30` 倍，与实体级评价分布错配，尚未处理。

## 4. 唯一下一顺序

1. Q0 表格强基线 v2 已完成；保留本地日志、概率、指标和资源收据。历史 SwanLab 指标仅在用户明确授权后补传。
2. C12 阶段 A v2 与阶段 B 三变体已完成；`C12-FULL` 未超过最强既有核和最佳单机制，按快速合同停止继续扩展，旧 v1 全部排除，Q0 不冒充论文正式结果。
3. R1 双层尾部风险重构 Q0 已完成且不晋级；保留全部概率、风险、评价和运行收据，不重复运行或依据目标标签追改参数。
4. 跨年度 P0 `rerun2` 已完成且不晋级；保留完整负结果，等待候选重排裁决，禁止追调 TL1。
5. GeNIS 攻击场景留出运行工程已完成，但协议因归档来源与标签完全共线而无效；不追调 S1/ST，也不启动候选。
6. GeNIS 支持量收缩跨场景极端尾部排序 Q0 已完成：完整候选 `support-shrunk-scene-tail` 不晋级，且仅为 `seed=42` 的筛选证据；不追加 `seed=43/44`，不追调。随机森林 `sample-cvar-tail` 的场景 4 主导诊断不能升级为创新或候选。GeNIS 分支唯一下一步是根据当前结果选择新候选，仍不得产生论文结论。
8. 只有候选重排后出现预注册独立机制信号并需要多种子或追加消融时，才评估额外计算加速；不得以既有 Q0 结果追参。
9. 对胜出任务使用冻结开发验证区确定困难面板。每个候选必须比较接收基座、接收基座加移植机制和必要对照；不按方案结果挑数据或样本。
10. 依据开发实验完成方案裁决与必要的种子 42/43/44 正式矩阵，然后冻结方法、字段、阈值、困难面板、超参数、代码、种子、指标和统计模板，签发 `final-freeze.json`。
11. 只有冻结收据通过后才执行最终评价；不得根据最终结果重训或改配置。
12. **第三章当前唯一实验顺序（2026-08-13 起）**：先完成单因子消融 `6` 档 × 种子 `42/43/44`，用 S2「只去归一化」与 S3「只换外积但保留归一化」判定「状态必须有界」假设；再据其结果确定「有界性控制」的具体实现（归一化或 delta 约束），然后运行 2×2 交互实验 × 三种子，判据为 `(A11−A10) > (A01−A00)` 且差值超过合并标准差，四格必须同一评价口径。交互项为零或为负时照实记录，不得改判据凑超可加。其后按性价比排 Lp 池化扫描（Gulcehre 2014）与顺序随机化消融（后者只作离线诊断，不作机制迁移）。所有神经结果单种子只作筛选线索，不得进入论文比较。
