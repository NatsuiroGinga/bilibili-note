# 第三章 RWKV 双机制正文与图件 实施计划

<!-- RESEARCH_ROUTE=RWKV -->

> **给执行代理：** 必需子技能——用 `superpowers:subagent-driven-development` 逐任务实施本计划。步骤用 `- [ ]` 复选框跟踪。

**目标：** 把今晚 LSPR23→LSPR24 跨年度双机制的全套实验结果，写成学位论文第三章正文与图件，正式替换现有 PINN 版本。

**架构：** 数据源是 `runs/diagnostics/ch3-full/ch3_full_results.json`（服务器产出后回传）。图件由单一 Python 脚本从该 JSON 生成三种格式；正文按 W0–W5 门禁逐节撰写，每个数字必须能回溯到 JSON 键路径或日志行。

**技术栈：** Python 3.12 + matplotlib 3.11（沿用 `thesis/figures/第三章/pyproject.toml`）、Hiragino Sans GB 中文字体、STIX 数学字体、Markdown 正文。

**技能链路（按根 `AGENTS.md` 技能编排节）：** 实验制品 → `results-analysis` → `results-report` → 图件走 `publication-chart-skill` 定型后落既有绘图合同 → 正文走 `paper-miner` → `ml-paper-writing` → `citation-verification` → `writing-anti-ai` → `paper-self-review`。

## 全局约束

- **路线迁移已获用户明确授权**（2026-08-13）：RWKV 双机制正式替换第三章主线。现有 PINN 正文与 5 张图件必须先归档再覆盖，不得直接删除。
- 图件按根 `AGENTS.md`「学位论文图件合同」（2026-08-13 重定）：最大宽 `155 mm`（半栏 `75 mm`）、高 ≤ `110 mm`、中文黑体、西文 Times New Roman、图内最小 `8 pt` 主标注 `9 pt`、输出 `PDF` + `PNG` 600 dpi、**灰度可读**（线型/标记/填充区分，颜色仅辅助）、主线 ≥ `1.0 pt`。
- **历史 `180 mm` 与 Hiragino Sans GB 已判定为缺陷**（A4 版心不足 180 mm、字体非中文学位论文常规），归档的 PINN 图件不作风格参照，只参照其脚本的三格式导出与清单写出逻辑。
- 落点 `thesis/figures/第三章/`，命名 `图3-M-中文说明`，重写 `图件清单.json` 含数据来源键路径。
- 正文只写 `实验支持` 或 `实验待证`，二者必须显式区分。**单种子结果一律标注单种子并给出实测种子标准差。**
- 每张图和每个结果段必须写明：数据版本、切分、比较对象、指标口径、样本量、适用边界。
- **禁止运行 `black`**（仓库规则）。Python 修改只做语法与导入检查。
- 实体键只作分组，绝不入模。LSPR24 最终封存区不访问。
- 正文不引用 `runs/`、`output/` 或临时目录的图片。

## 冻结的实验事实（写作时逐条回查，不得凭记忆）

| 量 | 值 | 来源 |
| --- | --- | --- |
| LSPR23 流数 / 正类先验 | `16,353,511` / `0.1005655006` | `run.log` |
| LSPR24 流数 / 正类先验 | `20,227,356` / `0.0257073138` | `run.log` |
| LSPR24 实体数 / 正例实体 / 先验 | `47,115` / `752` / `0.0159609467` | `run.log` |
| **实体流数中位数** | **`2`** | `run.log`，E7 解释的关键 |
| 训练序列 / 评价序列 | `271,815` / `200,825` | `run.log` |
| 序列级正类先验 / pos_weight | `0.0479186211` / `19.8687` | `ch3_main.log` |
| XGBoost 全量逐流 AP | `0.2244424863` | `run2.log` |
| XGBoost 分数实体聚合 AP | `0.523326`(n=1) / `0.547484`(n=100) | `entity.log` |
| Dijk 2026 表 5 XGBoost / GRU+SesH / Transformer+2-H | `0.2416` / `0.1758` / `0.0742` | 原件 p.30 |
| 逐流 MLP | `0.1590128242` | `seq_ablation.log` |
| 同配置换种子实测 σ | `0.07~0.23` | `mech_ablate.log` |

## 文件结构

| 文件 | 责任 |
| --- | --- |
| `.Codex/docs/RWKV/archive-pinn-ch3/` | 归档旧 PINN 正文与 5 张图件（新建） |
| `thesis/figures/第三章/绘制第三章机制图.py` | **改写**：RWKV 双机制的机制示意图（图3-1~3-4） |
| `thesis/figures/第三章/绘制第三章结果图.py` | **新建**：从 JSON 生成结果图（图3-5~3-11） |
| `thesis/figures/第三章/图件清单.json` | 重生成 |
| `thesis/chapters/03-基于因果前缀聚合与实体级可学池化的跨年度加密恶意流量检测方法.md` | **新建正文**，旧文件删除 |
| `thesis/README.md` | 第 25 行章节表改为新题名 |
| `output/开题改进交接文档.md`、`output/第一创新点实验总控.md` | 记录路线替换决定 |

---

### 任务 1：归档 PINN 第三章

**文件：**
- 创建：`.Codex/docs/RWKV/archive-pinn-ch3/`
- 移动：`thesis/chapters/03-基于队列物理约束与有界条件注意力的恶意流量检测方法.md`
- 移动：`thesis/figures/第三章/图3-{1..5}-*.{svg,pdf,png}`、`绘制第三章机制图.py`、`图件清单.json`

- [ ] **步骤 1：建归档目录并复制（不是移动，先留双份）**

```bash
cd /Users/bilibili/personal/note
mkdir -p .Codex/docs/RWKV/archive-pinn-ch3/figures
cp thesis/chapters/03-基于队列物理约束与有界条件注意力的恶意流量检测方法.md .Codex/docs/RWKV/archive-pinn-ch3/
cp thesis/figures/第三章/图3-*.{svg,pdf,png} .Codex/docs/RWKV/archive-pinn-ch3/figures/
cp thesis/figures/第三章/绘制第三章机制图.py .Codex/docs/RWKV/archive-pinn-ch3/绘制第三章机制图-PINN版.py
cp thesis/figures/第三章/图件清单.json .Codex/docs/RWKV/archive-pinn-ch3/图件清单-PINN版.json
```

- [ ] **步骤 2：核对归档完整性**

```bash
ls -1 .Codex/docs/RWKV/archive-pinn-ch3/figures/ | wc -l   # 期望 15（5 图 × 3 格式）
wc -l .Codex/docs/RWKV/archive-pinn-ch3/03-*.md            # 期望 820
```

- [ ] **步骤 3：写归档说明**

在 `.Codex/docs/RWKV/archive-pinn-ch3/README.md` 写明：归档日期 `2026-08-13`、原因（用户明确裁决 RWKV 双机制替换第三章主线）、原文件路径、恢复方法。

- [ ] **步骤 4：确认归档后再删除原图件与原正文**

```bash
rm thesis/figures/第三章/图3-{1,2,3,4,5}-*.{svg,pdf,png}
rm thesis/chapters/03-基于队列物理约束与有界条件注意力的恶意流量检测方法.md
```

- [ ] **步骤 5：提交**

```bash
git add -A .Codex/docs/RWKV/archive-pinn-ch3 thesis/
git commit -m "chore(thesis): archive pinn chapter3 before rwkv route migration"
```

---

### 任务 2：回传实验结果并建立数据契约

**文件：**
- 创建：`thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ch3_full_results.json`
- 创建：`thesis/figures/第三章/结果数据契约.md`

**接口：**
- 产出：JSON 顶层键 `cells`（四格指标）、`curve`（延迟曲线）、`stability`（三种子）、`length`（长度敏感性）、`facet`（类别分面）

- [ ] **步骤 1：从服务器回传**

```bash
zsh -lc 'source ~/.zshrc; GPU_SSH="$GPU_SSH_C56" GPU_PWD="$GPU_PWD_C56" expect /tmp/gpu-rsync-pull.exp \
  /root/autodl-tmp/thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ch3_full_results.json \
  thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ch3_full_results.json'
```

若 `gpu-rsync-pull.exp` 不存在，用 `gpu-exec.exp` 执行 `cat` 后本地重定向写入，并核对 `python3 -c "import json;json.load(open(...))"` 通过。

- [ ] **步骤 2：校验 JSON 结构**

```python
import json
d = json.load(open("thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ch3_full_results.json"))
assert set(d) >= {"cells","curve","stability","length","facet"}
assert set(d["cells"]) == {"C00","C01","C10","C11"}
for c in d["cells"]:
    assert {"fap","fauc","e_max","e_lp","dr","p","tr","ev","npar"} <= set(d["cells"][c])
assert all(len(r) == 5 for r in d["curve"]["C11"])   # (k, ap, dr, n_ent, cov)
print("结构校验通过")
```

- [ ] **步骤 3：写数据契约文档**

`thesis/figures/第三章/结果数据契约.md` 逐键说明含义、单位、口径，以及每个图引用哪些键。

- [ ] **步骤 4：提交**

```bash
git add thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ thesis/figures/第三章/结果数据契约.md
git commit -m "feat(thesis): add chapter3 full experiment results and data contract"
```

---

### 任务 3：机制示意图（图3-1 ~ 图3-4）

**文件：**
- 创建：`thesis/figures/第三章/绘制第三章机制图.py`（全新内容，同名覆盖已归档的 PINN 版）

**接口：**
- 消费：无实验数据（**机制图不得使用实验结果**，遵循既有 `evidence_mode: method_schematic_without_experimental_results`）
- 产出：`图3-1`~`图3-4` 的 svg/pdf/png

四张图：

| 图 | 内容 | 尺寸 mm |
| --- | --- | --- |
| 图3-1-整体方法框架 | 逐流特征 → 2-IP 分组序列化 → 表示层因果前缀聚合 → 决策层实体级 Lp 池化 → 实体告警 | 180×110 |
| 图3-2-二IP无向对序列构造 | `χ(a,b)=sortlex{srcIP,dstIP}`、按时间排序、L=128 非重叠切分 | 180×85 |
| 图3-3-因果前缀聚合 | `ctx_t=Σ_{i≤t}h_i/Σ_{i≤t}m_i` 的因果掩码与归一化，对比非因果整窗聚合 | 180×95 |
| 图3-4-实体级可学Lp池化 | `S_e=(Σs_f^p/n)^{1/p}`，标出 p=1 均值、p→∞ 上界两个特例位置 | 180×95 |

- [ ] **步骤 0：先调用 `publication-chart-skill` 定图型与可读性规范**

对四张机制示意图，用该技能确认：图型选择、信息密度、图例与标注规范、是否需要配套表。技能给出的规范与下述既有绘图合同冲突时，以合同为准并记录冲突点。

- [ ] **步骤 1：读既有绘图脚本的样式约定**

```bash
sed -n '1,80p' .Codex/docs/RWKV/archive-pinn-ch3/绘制第三章机制图-PINN版.py
```
只抄可复用的工程部分：`mm→inch` 换算、多格式导出函数、`图件清单.json` 写出逻辑。**字体、宽度、配色一律不抄**——按新合同重设为黑体、`155 mm`、灰度可读。

- [ ] **步骤 2：写图3-3（先写最核心的一张，验证样式管线）**

因果前缀聚合示意：横轴为序列位置 `t=1..8`，画下三角掩码矩阵，右侧标注 `ctx_t` 公式，底部对照非因果整窗聚合的全矩阵。

- [ ] **步骤 3：生成并肉眼核验**

```bash
uv run --project thesis/figures/第三章 python thesis/figures/第三章/绘制第三章机制图.py
```
检查：中文无豆腐块、公式用 STIX、宽度 180 mm、三格式齐全。

- [ ] **步骤 4：补齐图3-1、图3-2、图3-4**

- [ ] **步骤 5：重生成图件清单并提交**

```bash
git add thesis/figures/第三章/
git commit -m "feat(thesis): add chapter3 mechanism schematic figures"
```

---

### 任务 4：结果图（图3-5 ~ 图3-11）

**文件：**
- 创建：`thesis/figures/第三章/绘制第三章结果图.py`

**接口：**
- 消费：任务 2 的 `ch3_full_results.json`
- 产出：`图3-5`~`图3-11`

七张图：

| 图 | 内容 | JSON 键 | 对位朱 §3.6 |
| --- | --- | --- | --- |
| 图3-5-跨年度主性能对比 | 四格 + XGBoost/Dijk表5/逐流MLP 横向条形，标随机先验基准线 | `cells.*.fap` | 3.6.2 |
| 图3-6-双机制消融交互 | 2×2 四格柱状 + A/B 主效应与交互项标注 | `cells.*.fap`、`cells.*.e_lp` | 3.6.4 |
| 图3-7-训练稳定性 | 三种子逐流 AP 散点 + 均值与标准差区间 | `stability` | 3.6.3 |
| 图3-8-Lp指数收敛轨迹 | p 随 epoch 变化，横线标 p=1 均值 | 从 `run.log` 解析 | 3.6.5 |
| 图3-9-序列长度敏感性 | L∈{16,32,64,128} 的逐流与实体级 AP 双轴 | `length` | 3.6.6 |
| 图3-10-攻击类别分面 | 各类别 AP 水平条 + 正例数标注 | `facet` | 3.6.7 |
| 图3-11-延迟准确率曲线 | 实体 AP 与 DR@4%FPR 随「前 k 条流」变化，标注实体流数中位=2 | `curve` | 3.6.8 |

- [ ] **步骤 0：先调用 `results-analysis` 产出严格分析制品**

对 `ch3_full_results.json` 执行 blocker-first 门禁：锁定分析单元（流级/实体级）、主指标（AP）、运行数（单种子 + 三种子子集）、来源（冻结日志路径）、比较族（四格 + 外部基线）。产出分析制品后才可画结果图与写 §3.6。

- [ ] **步骤 0b：调用 `publication-chart-skill` 为七张结果图定型**

重点让技能裁定：延迟曲线用双轴还是双子图、类别分面条形图的排序与截断规则、消融交互如何在一张图里同时呈现主效应与交互项。

- [ ] **步骤 1：写图3-11（延迟曲线，最关键的一张）**

双 y 轴：左轴实体 AP，右轴 DR@4%FPR；横轴 k∈{1,2,3,5,10,20,50,100,全部}，对数刻度。
**必须在图上标注竖直参考线 `实体流数中位=2`**，并在图题写明"半数实体仅含 2 条流，故 k>2 的收益仅作用于长尾实体"。

- [ ] **步骤 2：生成并核验数值与 JSON 一致**

```python
import json
d = json.load(open("thesis/experiments/llm_probe/runs/diagnostics/ch3-full/ch3_full_results.json"))
for k, ap, dr, n, cov in d["curve"]["C11"]:
    print(f"k={k} AP={ap:.6f} DR={dr:.4f} 实体={n} 覆盖={cov:.4f}")
```
把打印值与图上读数逐点比对。

- [ ] **步骤 3：补齐图3-5 ~ 图3-10**

- [ ] **步骤 4：全部重生成，更新图件清单，提交**

```bash
uv run --project thesis/figures/第三章 python thesis/figures/第三章/绘制第三章结果图.py
git add thesis/figures/第三章/
git commit -m "feat(thesis): add chapter3 result figures from frozen experiment json"
```

---

### 任务 5：正文 3.1–3.3（引言、问题定义、方法框架）

**文件：**
- 创建：`thesis/chapters/03-基于因果前缀聚合与实体级可学池化的跨年度加密恶意流量检测方法.md`

- [ ] **步骤 1：执行 W0 范围恢复**

读 `.Codex/docs/RWKV/RWKV路线总控.md`、`2026-08-08-第三章候选方案登记册.md`、`2026-08-13-第三章双机制组合创新的论证结构.md`。把每条待写内容标为四态之一。

- [ ] **步骤 2：写 3.1 引言**

结构照朱 §3.1：先讲跨年度迁移的实际困难（引 Dijk 2026 表 5 的退化数字、Leoste 2025 的跨年结果），再指出现有工作在表示层与决策层各自为战，最后引出本章两机制。**不得声称本章方法有效**，只写要解决什么。

- [ ] **步骤 3：写 3.2 问题定义**

三小节：3.2.1 跨年度检测任务的数学描述（流集合、实体划分 `χ(a,b)=sortlex{srcIP,dstIP}`、标签、评价指标 AP 与 DR@FPR 的定义式）；3.2.2 分布漂移的形式化；3.2.3 表示层与决策层的分工形式化。**编号公式从 (3.1) 开始连续编号。**

- [ ] **步骤 4：写 3.3 方法框架**

3.3.1 总体结构（引图3-1）；3.3.2 因果前缀跨流聚合（引图3-2、图3-3，给出 `ctx_t` 定义式与因果性证明的引理陈述）；3.3.3 实体级可学 Lp 池化（引图3-4，给出 `S_e` 定义式、`p` 的参数化 `p=exp(p_log)`、序列级辅助损失与其 `pos_weight=19.8687`）。

**谱系必须照实写**（这是硬要求，不得回避）：
- 因果前缀聚合上溯 Lee & Stolfo 2000 TISSEC Table VIII p.243 的 `past two seconds same-host count`，本章改造为无界因果前缀、可微表示、2-IP 边
- 归一化聚合形式上溯 Vision-RWKV ICLR 2025 式 (5)，本章限制为严格因果
- 实体级聚合上溯 Gehri 2023 CyCon 主机级聚合，Lp 算子上溯 Gulcehre 2014 ECML

- [ ] **步骤 5：语法与链接检查后提交**

```bash
rg -n "图3-[0-9]+" thesis/chapters/03-*.md | while read -r l; do echo "$l"; done   # 逐条核对图件存在
git add thesis/chapters/
git commit -m "docs(thesis): add chapter3 sections 3.1 to 3.3"
```

---

### 任务 6：正文 3.4–3.5（算法与理论分析）

- [ ] **步骤 1：写 3.4 算法实现**

3.4.1 训练算法流程（**算法 3-1**，伪代码含：序列构造、前缀聚合、Lp 池化、双损失加权、检查点平均）；3.4.2 复杂度分析（前缀聚合 `O(TD)`、Lp 池化 `O(n)`，与自注意力 `O(T²D)` 对比）。

- [ ] **步骤 2：写 3.5 理论分析**

**引理 3-1**：因果前缀聚合的在线可计算性——`ctx_t` 可由 `ctx_{t-1}` 与 `h_t` 以 `O(D)` 增量更新，不需重扫历史。给出递推式与证明。

**定理 3-1**：Lp 池化的单调性与特例覆盖——`M_p(s)` 关于 `p` 单调不减，且 `M_1` 为算术平均、`M_p→∞` 为上界、`M_p→0` 为几何平均。给出证明（幂平均不等式）。**这解释了为何可学 `p` 覆盖传统 max/mean 聚合。**

**定理 3-2**：跨年度模型选择的不可行性——在同年度验证分布与目标年度分布满足特定散度条件下，同年度验证指标对目标年度性能不具选择力。**用本章实测数据作为该定理的经验验证**（三种验证划分动态范围 `2.8e-5~1.2e-2`，与跨年度 AP 相关 `−0.40~+0.42`）。

- [ ] **步骤 3：核对公式编号连续、定理引用正确**

- [ ] **步骤 4：提交**

---

### 任务 7：正文 3.6 实验评估（八小节）

- [ ] **步骤 1：写 3.6.1 实验设置**

数据版本与哈希、切分、字段预算 `101−18=83`、训练预算 `20000` 步、检查点平均 `末5个`、评价指标口径、硬件与耗时。

**必须写入协议声明**：本章不使用基于验证集的早停或 epoch 选择，依据是实测三种同年度验证划分无选择力（引定理 3-2 与图表）。

- [ ] **步骤 2：写 3.6.2 综合性能对比**（表 3-1，引图3-5）

四格 + XGBoost + Dijk 表 5 三行 + 逐流 MLP + 随机先验。**Dijk 的数字标注为"论文原始数字，协议不完全等同，仅作背景参照"**。

- [ ] **步骤 3：写 3.6.3 训练稳定性与泛化**（表 3-2，引图3-7）

三种子结果、均值、标准差。**必须写明：同配置换种子的实测 σ 与机制效应同量级，故本章结论按单种子报告并显式标注该限制。**

- [ ] **步骤 4：写 3.6.4 消融实验**（表 3-3，引图3-6）

2×2 四格，A/B 主效应、单机制之和、组合、交互项，两条判据分别判定。**逐流与实体级两个口径的结论若不一致，照实并列，不择优呈现。**

- [ ] **步骤 5：写 3.6.5 机理分析**（引图3-8）

Lp 指数收敛轨迹；`p` 的取值与 Pevny & Somol 2016 关于 mean/max 池化泛化差异的观察对位。

- [ ] **步骤 6：写 3.6.6 序列长度敏感性**（表 3-4，引图3-9）

- [ ] **步骤 7：写 3.6.7 攻击类别细粒度**（表 3-5，引图3-10）

每类正例数必须同表列出；正例数 `<50` 的类别不单独下结论。

- [ ] **步骤 8：写 3.6.8 受限观测下的检测能力**（表 3-6，引图3-11）

延迟-准确率曲线。**必须写明实体流数中位数为 `2`**，并据此限定结论适用范围。

- [ ] **步骤 8b：调用 `results-report` 产出决策导向的实验报告**

把 §3.6 各小节的结论、局限与下一步动作固化为报告制品，作为 3.7 与第四章承接的输入。

- [ ] **步骤 9：写 3.7 本章小结**

明确区分"实验支持"与"实验待证"两类结论，并列出本章遗留问题（供第四章承接）。

- [ ] **步骤 10：提交**

---

### 任务 8：路线迁移同步与全章核验

- [ ] **步骤 1：更新 `thesis/README.md` 第 25 行章节表**为新题名与新状态。

- [ ] **步骤 2：按 `output/AGENTS.md` 职责分流，把路线替换决定写入 `output/开题改进交接文档.md` 与 `output/第一创新点实验总控.md`。**

- [ ] **步骤 3：更新 `.Codex/docs/RWKV/RWKV路线总控.md`**，记录第三章正文与图件已产出、路线已替换主线。

- [ ] **步骤 4：全章数字回查**

对正文中每一个数字，在 `ch3_full_results.json` 或冻结日志中定位来源。写一份核查表 `.Codex/docs/RWKV/2026-08-13-第三章数字回查表.md`，逐条列出：正文位置、数值、来源文件、来源键或行号。**任何找不到来源的数字必须删除或改为待证占位。**

- [ ] **步骤 5：图件引用核验**

```bash
rg -o "图3-[0-9]+-[^\s）)]+" thesis/chapters/03-*.md | sort -u | while read -r f; do
  ls "thesis/figures/第三章/${f}.pdf" >/dev/null 2>&1 || echo "缺图: $f"
done
```

- [ ] **步骤 6：篇幅与工作量核对**

```bash
wc -l thesis/chapters/03-*.md
rg -c "^\\(3\\.[0-9]+\\)|^\\$\\$" thesis/chapters/03-*.md    # 公式数
rg -c "^\\| " thesis/chapters/03-*.md                        # 表格行
ls thesis/figures/第三章/*.pdf | wc -l                        # 图件数
```
目标：约 26 页正文、公式 30 条以上、1 个算法、2 个定理、1 个引理、11 幅图、6~7 张表。**未达标时补论证深度，不得靠拆分段落或重复表格凑数。**

- [ ] **步骤 6b：调用 `paper-self-review` 做交付前审计**

审结构、逻辑、引用、图表与过度声称。**重点审：单种子结论是否在 3.6.3 与 3.7 两处都显式声明了限制；每个「实验支持」是否真有制品支撑。**

- [ ] **步骤 6c：调用 `citation-verification` 核验全部引用**

逐条核验书目信息与该句主张的原文支撑位置。未核验的引用改为占位，不从记忆生成条目。

- [ ] **步骤 6d：调用 `writing-anti-ai` 做最后语言修订**

只删冗余、改句法，**不得改动术语、数值、引用、限制或论断状态**。

- [ ] **步骤 7：最终提交**

```bash
git add -A thesis/ output/ .Codex/docs/RWKV/
git commit -m "feat(thesis): replace chapter3 with rwkv dual-mechanism route"
```

---

## 自查

**规格覆盖**：归档（任务1）、数据（任务2）、机制图（任务3）、结果图（任务4）、正文 3.1–3.3（任务5）、3.4–3.5（任务6）、3.6–3.7（任务7）、迁移同步与核验（任务8）。朱第三章的八个实验小节全部有对位任务。

**占位扫描**：无 TBD。所有命令给出完整可执行形式；所有图件给出内容、尺寸、JSON 键与对位小节。

**类型一致**：JSON 键名 `cells`/`curve`/`stability`/`length`/`facet` 在任务 2 定义，任务 4 引用一致；图件命名 `图3-M-中文说明` 在任务 3、4、8 三处一致。

## 已知风险

1. **实验尚未跑完**。任务 2 之前不能启动任务 4 与任务 7。E1 首格 C00 已出（逐流 AP `0.233366`），全套预计 60–70 分钟。
2. **单种子限制**。用户已裁决按朱的单次运行格式报告；实测 σ `0.07~0.23` 与机制效应同量级，该限制必须在 3.6.3 与 3.7 两处显式声明，不得只在脚注提及。
3. **定理 3-2 尚未形式化**。目前只有经验测量，需要在任务 6 补形式化条件；若无法给出严格条件，降级为"观察 3-1"并如实说明。
