# 第三章有效 2×2 机制优化任务计划

- **STATUS=APPROVED_FROZEN**
- **批准日期**：2026-08-26
- **批准范围**：方案 A 的 LSPR23 源年零训练四格运行
- **当前任务边界**：只冻结文档；本次文档代理禁止实现、同步和运行，未访问服务器
- **实现门禁**：后续实现只能逐字执行[有效 2×2 实验计划](有效2x2实验计划.md)；改变公式、检查点、数据年度、裁决指标或制品合同必须重新请求用户批准

## 目标

用 `B00/B10` 两个已封印检查点分别表示 CPA 关闭和开启，用固定实体算子 `max` 与 `0.5×mean+0.5×max` 表示实体算子两级，形成真正独立的 `2×2`。本轮只用 LSPR23 判断固定混合算子是否产生源年信号；无信号即保留现有 O11 并停止。

## 文件所有权

本任务只拥有：

- `task_plan.md`
- `证据与决策记录.md`
- `有效2x2实验计划.md`
- 旧计划标题后的撤销状态块；旧计划其余正文保持原样

本任务不修改研究问题卡、路线总控、第三章恢复卡、实验代码、配置、启动器、既有制品或 `AGENTS.md`。

## 阶段

- [x] 阶段一：读取规则、路线总控、第三章恢复卡、三层职责、研究契约、旧计划与文献审计检查点。
- [x] 阶段二：重建旧 Task 2 的两项 Critical 缺陷并撤销旧计划。
- [x] 阶段三：比较 A/B/C 三种修订路线，冻结方案 A。
- [x] 阶段四：冻结四格公式、年度隔离、指标、制品、恢复、并发与停止条件。
- [x] 阶段五：完成链接、占位词、公式唯一性和 Git 差异检查。
- [ ] 阶段六：由后续实现代理在独立任务中实现最小三件套。
- [ ] 阶段七：按用户已批准范围仅运行 LSPR23，生成源年裁决制品。
- [ ] 阶段八：若 S0 有信号，另立 S1 计划并再次请求批准；若无信号，保留 O11 并停止。

## 已冻结决策

1. 旧 Task 2 已撤销，不得实现、同步或运行。
2. 本轮采用方案 A：现有 CPA 检查点加固定 `mean/max` 实体算子 S0。
3. 四格为 `C00=B00+max`、`C01'=B00+0.5×mean+0.5×max`、`C10=B10+max`、`C11'=B10+0.5×mean+0.5×max`。
4. 在线分数使用同一算子的因果前缀形式；终端分数是最后一个前缀值。
5. 同一检查点的逐流输出必须逐位相同且哈希相同；逐流指标只作同一性护栏。
6. 本轮只允许读取 LSPR23，所有 LSPR24 数组读取计数必须为 `0`。
7. 不实现新的分数层 CPA，不训练新模型或新检查点，不搜索混合权重。
8. 不使用旧判据 `1×SE=0.036`。将来正式不确定性只允许配对实体重抽样，并须另行冻结重抽样合同。

## 阻塞与边界

- 四篇全文审计尚未完成，文献措辞状态为 `PENDING`；它不改变用户已批准的四格公式，但在审计完成前不得冻结论文机制来源、原创性或效果措辞。
- LSPR24 已被历史访问，任何将来的整批评价都必须标记 `target_informed=true`，只能在源年封印后一次性描述，且不属于本轮授权。
- 方案 B、方案 C 和 S1 均未获实现授权。

## 状态

**APPROVED_FROZEN**：方案 A 的 LSPR23 源年公式和实验合同已冻结；文档任务完成，尚未实现、同步或运行。
