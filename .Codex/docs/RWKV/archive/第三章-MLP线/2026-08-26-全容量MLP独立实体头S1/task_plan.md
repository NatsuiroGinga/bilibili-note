# 全容量 MLP 独立实体头 S1 任务计划

- **STATUS=APPROVED_FROZEN**
- **模型代理**：`xgb_lspr_config_provenance_sol_high` → `gpt-5.6-sol`，`effort=high`
- **任务性质**：只冻结文档；本任务不实现、不运行、不访问服务器

## 目标

依据提交 `a3a4cd4`、`5a9bc59` 与源年 S0 制品，冻结以 B10 为父检查点的独立三参数实体头 S1 合同，供主进程随后按文档实现。

## 文件所有权

仅修改本目录：

- `task_plan.md`
- `notes.md`
- `S1实验计划.md`

不修改恢复卡、路线总控、看板、实验代码、配置、启动器、运行制品或服务器文件；不覆盖、回滚或暂存其他代理改动。

## 阶段

- [x] P0：读取活动规则、技能与用户已批准合同，核对新目录快照和共享暂存区。
- [x] P0：核验 `a3a4cd4`、`5a9bc59` 与 S0 原始制品的身份、指标和信号裁决。
- [x] P0：核验仓库既有 LBFGS 用法、目标 PyTorch 版本与官方构造器／终止接口。
- [x] P0：冻结数据集－模型适配表、朱论文对位表、三参数候选／控制头与在线前缀接口。
- [x] P0：冻结全批量加权 BCE、FP64 LBFGS、源年隔离、共同整数 FP 曲线与拒绝条件。
- [x] P1：执行论断、占位、路径、差异和 Markdown 检查。
- [x] P1：仅提交本目录三份文档并报告 SHA。

## 已批准且不得改写的边界

1. 父检查点固定 B10，逐流概率逐位不改。
2. 候选输入 `[terminal_mean, terminal_max]`；参数匹配控制输入 `[terminal_mean, terminal_mean]`。
3. 两头均为线性 logit 加 bias，恰 `3` 个 FP64 参数；在线阶段把同一头作用于 prefix mean/max。
4. 训练只用 LSPR23 训练实体；验证实体零参与拟合；LSPR24 读取为 `0`。
5. 确定性全批量加权 BCE＋PyTorch LBFGS；无随机批次、无容量／学习率网格。
6. 原 max 是零参数回退；旧 O11 是只读强基线。
7. 通过条件同时要求候选胜参数匹配控制，并相对 B10 max 至少一项严格改善且不被支配；否则拒绝 S1 并保留 O11。
8. LSPR23 源验证只有 `20` 个正实体，S1 只作筛选，不作正式显著性证据。

## 错误记录

- 暂无。

## 当前状态

**APPROVED_FROZEN：合同已冻结，等待主进程按本文实现。**
