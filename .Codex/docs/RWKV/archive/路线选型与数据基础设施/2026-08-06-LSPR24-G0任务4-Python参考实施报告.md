# LSPR24 G0-A 任务 4 Python 参考实施报告

- **日期**：2026-08-06
- **任务**：人工开发小样本 Python 参考与单一命令入口
- **状态**：两条精确目标测试通过，等待独立复审
- **合同 SHA-256**：`17a86b71e2858b420a73759437a5301e6efd3ba6179f949ea9d4d87adeeb24af`

## 1. 实施边界

本任务只使用测试内人工开发 JSON 和只读共享规范向量。未读取真实 LSPR24 Parquet、标签、攻击叙事、最终测试或任何正式运行制品；未修改 Rust、正式配置、数据、总控、`scripts/` 或现有流程。

Python 入口只接受 `scope=development-visible-first-80-percent`。其他范围一律失败，未提供最终区命令参数或隐式回退。输出使用排他创建，已有路径拒绝覆盖。

## 2. 修改文件

- `thesis/experiments/llm_probe/src/flow_probe/lspr24_g0_reference.py`
  - 新增显式 `materialize_reference` 和 `compare_reference`。
  - 覆盖 Rust 冻结的空值、布尔、`i64/u8/u16/u64`、UTF-8、字节、规范 IP、SHA-256 与十二位小数定点类型标记和四字节大端长度前缀。
  - 十二位小数只接受十进制字符串，使用半偶舍入并输出有符号 `i128` 定点整数；拒绝 NaN、无穷和越界值。
  - 按 `protected_endpoint_id,last_ns,start_ns,raw_flow_id,endpoint_role_order,source_row_index` 完整键严格升序排列，重复完整键直接失败。
  - 逐行计算 SHA-256，再按四字节长度前缀串联有序行哈希计算数据集语义 SHA-256。
  - 比较前复算稳定顺序、规范字节、行哈希和数据集哈希，再对规范 JSON 字段和值类型执行逐字节硬比较；未实现浮点容差。
  - 提供 `materialize` 与 `compare` 两个子命令，共用同一开发参考入口。
- `thesis/experiments/llm_probe/tests/test_lspr24_g0_reference.py`
  - 覆盖共享规范向量、完整稳定键每个字段、输入乱序、定点量化、语义哈希、硬不一致、拒绝覆盖和最终区拒绝。
- `thesis/experiments/llm_probe/tests/test_lspr24_g0_reference_cli.py`
  - 覆盖唯一命令登记、物化、比较、重复输出失败关闭和最终区失败关闭。
- `thesis/experiments/llm_probe/pyproject.toml`
  - 只新增 `flow-probe-lspr24-g0-reference = "flow_probe.lspr24_g0_reference:main"`。
- `.Codex/docs/RWKV/2026-08-06-LSPR24-G0任务4-Python参考实施报告.md`
  - 记录本任务边界、红绿灯、文件与遗留风险。

共享文件 `thesis/experiments/llm_probe/tests/fixtures/lspr24_g0/canonical_vectors.json` 保持只读，未修改。

## 3. 测试驱动开发证据

### 3.1 库级目标红灯

命令：

```bash
uv run --no-sync pytest -q tests/test_lspr24_g0_reference.py
```

有效红灯结果：退出码 `2`。测试收集只因 `flow_probe.lspr24_g0_reference` 尚不存在而失败，符合生产接口缺失的预期原因。

### 3.2 命令入口目标红灯

命令：

```bash
uv run --no-sync pytest -q tests/test_lspr24_g0_reference_cli.py
```

第一次尝试发现测试错误使用 Python 3.11 才提供的 `tomllib`，而项目运行时为 Python 3.10；该次结果不计作有效红灯。只修正测试入口断言后重跑同一命令，退出码 `1`，唯一失败原因为 `pyproject.toml` 尚无冻结命令入口，符合预期红灯原因。

### 3.3 库级目标绿灯

同一精确命令结果：退出码 `0`，`1 passed in 0.03s`。

### 3.4 命令入口目标绿灯

同一精确命令结果：退出码 `0`，`1 passed in 0.19s`。

## 4. 格式化与静态检查

依照任务指令，未运行 Black、Ruff、Pyright、Prettier、全仓测试或其他额外测试。两条指定 pytest 目标均以退出码 `0` 通过。

## 5. 制品

测试只在 pytest 临时目录生成并清理人工输入和参考 JSON。未生成正式 G0-A 运行目录、收据、Parquet 输出、最终区参考或模型结果。

## 6. 遗留风险

- 当前证据只覆盖人工开发 JSON 与任务 2 共享规范向量，不能替代真实 LSPR24 受限封存、开发区全量物化或正式 G0-A。
- 当前入口复现任务 2 已冻结的规范编码、完整稳定键与语义哈希；Rust 任务 5 尚未提供正式时间窗口、历史样本和评价簇制品，因此这些正式制品的 Rust/Python 逐项核对仍需在任务 5 冻结小样本生成后执行。
- 排他创建保证已有输出不被覆盖，但本任务不承担任务 3 正式运行目录、`.partial` 发布和机器收据协议。
- 未读取最终区内容；最终区 Python 参考必须继续推迟到合同允许的一次性解封之后。
