# C1-C12 候选详情与历史证据索引

- **归档日期**：2026-08-11
- **用途**：保存活动登记册精简前的完整候选机制、证据、风险、实验要求和共用筛选约束；活动入口见[第三章候选方案登记册](../2026-08-08-第三章候选方案登记册.md)。
- **边界**：本索引不改变任何候选状态、排序或科学裁决。C12 的最新结论保持为 `Q0快速筛选不晋升/弱证据不利`、`formal_paper_evidence=false`、`final_accessed=false`，不是正式证伪或论文结论。

## 工作流与候选边界

1. 从 RWKV、RWKV-7、RWKV-8 及其生态应用论文中提取可独立描述和实现的机制，不把“直接换成 RWKV”当作唯一方案。
2. 每个候选必须明确写出机制来源、接收基座、最小移植接口、合法数据字段、快速消融、正式对照和可证伪条件。普通 Transformer、BERT、Mamba 或其他合格模型均可作为接收基座。
3. 候选先接受理论深度与章级工作量审查：必须能够形成单章单问题、章内机制协同、核心公式与算法、必要的性质分析以及足量消融和效率实验；只增加一个常规模块或只改损失权重不能单独撑起第三章。
4. 理论工作量参照朱焱雷第三章约 26 页、33 个编号公式、1 个算法、2 个定理、1 个引理、11 幅图和 7 张表的论证完整度。参照的是功能与深度，不机械凑数量，也不得照抄其公式或方法。
5. 通过理论门槛的候选使用同一冻结开发数据、同一接收基座、同参数与训练预算进行快速消融。快速消融只决定优先级和是否进入正式训练，不能直接写成论文结论，也不能单独把候选改成“已支持”或“已证伪”。
6. 正式候选必须与最强匹配基线比较。错误相对下降 16.1% 仅作为朱焱雷第三章的历史效果量级参照，不是硬门槛；最终综合主指标绝对值与增量、错误相对下降、效率、显存、多种子一致性、置信区间、独立消融和困难分面裁决。最终测试不参与候选选择、数据物化、字段选择或超参数调整。
7. 最终论文只能使用一个数据集。候选筛选前先统一审计本地已有数据的年代、官方来源、标签层级、序列连续性、身份泄漏、类别不平衡、基线饱和度、机制所需字段、算力成本和可复现性，选出唯一数据集；不得依据某个候选的成绩选择数据集。
8. 唯一数据集冻结后，在其内部定义一个第三章主任务，以及必要的辅助任务、时间外推、开放集和困难分面。第三章和第四章共享同一冻结数据合同，不得为提高结果追加第二数据集或改变样本来源。
9. 候选需要新的时间历史、字段组或训练目标时，可以从同一原始数据重新物化开发视图，但所有候选必须共享样本身份、拆分、标签、最终测试和公共字段预算。
10. 目标效果提升在公平基线完成后冻结：以主任务最强匹配基线为参照，预先确定主指标、辅助指标、效率和困难分面判据；快速消融只检查是否存在稳定、可解释且方向一致的增量，不得用最终测试反复挑选候选。

## 历史调度优先级（2026-08-11）

排序采用七个维度，其中理论深度与章级工作量合计占 35%，属于第三章候选的硬门槛：

| 维度 | 权重 | 判据 |
| --- | ---: | --- |
| 理论深度 | 20% | 是否包含两个解决不同子问题且可独立消融的机制、统一目标、核心公式族、算法和有条件的性质分析；禁止用常规模块堆叠或凑公式代替理论 |
| 章级工作量 | 15% | 是否能形成约 26 页或更多的完整论证，并承担约 30 个实质公式、1 个算法、引理/命题、约 10 幅图、7 张以上表及主结果、消融、效率、困难分面和失败分析；参照朱焱雷第三章但不机械复制 |
| 当前原创空间 | 15% | 近年全文与官方源码查重后，核心机制是否仍存在实质区别，而不是换名或简单拼接 |
| LSPR 跨年度任务匹配 | 15% | 所需字段、时间语义、无标签目标前缀和序列结构是否由冻结数据合同真实支持 |
| 正向效果潜力 | 15% | 是否针对已观测的跨年度退化与困难分面，且相对共同强基线存在合理但尚待实验的提升空间 |
| 可证伪性与消融 | 10% | 是否有接收基座、两个单机制、完整组合和关键置换/污染对照，失败时能够明确淘汰或重构 |
| 实现与单卡成本 | 10% | 是否能在冻结数据、单卡 GPU 和可接受时间内完成快速筛选与后续三种子验证 |

当前 1–5 分临时评分只用于调度，不构成有效性裁决：

| 优先级 | 章级候选 | 理论 | 工作量 | 原创 | 数据匹配 | 效果潜力 | 可证伪 | 可实施 | 加权分 | 当前动作 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | C12 漂移触发有界状态控制与可信双参照校正 | 5 | 5 | 3 | 5 | 5 | 5 | 4 | 92 | `P0`：Q0 阶段 A v2 与阶段 B 已完成，`Q0快速筛选不晋升/弱证据不利`；等待文献协议桥接基线 Q0 与候选重排 |
| 2 | B 连续时间频谱状态与有界信任融合 | 5 | 5 | 2 | 4 | 3 | 4 | 3 | 76 | `P1`：先根据 JSSM、DyG-Mamba 等全文收窄为 RWKV 广义 Delta 连续化与非扩张因果融合，再做最小消融 |
| 3 | D 行为后缀记忆与正常预测残差协同检测 | 4 | 5 | 3 | 3 | 4 | 4 | 2 | 73 | `P1`：先做重复后缀可观测性诊断；只有 LSPR 序列存在有效重复信号才训练 ROSA 组合 |
| 4 | C 源域时间环境稳健状态与最坏环境告警校准 | 4 | 4 | 2 | 5 | 3 | 4 | 3 | 72 | `P2`：FOIL 已覆盖时间环境推断与不变学习，需把原创核心收窄到最坏源环境告警校准后再决定是否训练 |
| 5 | A 源域解耦掩码预训练与可信字段图 | 4 | 4 | 2 | 3 | 3 | 4 | 2 | 64 | `P2`：联合掩码预训练与字段图近邻较多且训练成本高，优先重构，不按原方案直接投入 GPU |

章级候选池暂设上限为 6。文献调研产生的新方案优先替换同构风险高的候选，而不是无限追加；只有理论深度、章级工作量、数据可观测量和最小可证伪实验均明确时，才可占用第六个席位。C12 的原创分仍待专项全文核验，若广义 Delta 漂移状态与可信正常校正的组合也被完整覆盖，其优先级必须下调，但正在进行的数据与公共缓存工程仍可供其他跨年度候选复用。

**2026-08-11 文献闭合后的新调度证据**：候选 C 已从“稳定—私有状态＋最坏源阈值”收窄并重构为“源时间环境—样本尾部双层风险状态学习＋NP约束因果EVT上下文尾部风险”。该结论使 C 成为 C12 停止扩展后的首选快速实验方向，但仍是“设计可行、实验待证”，不追改 R1 冻结 Q0，也不把文献排序当作实验支持。详见[主综述](../2026-08-11-跨年度任务非RWKV理论融合文献综述.md)与[候选 C 方案设计](候选C-源域时间环境稳健状态与最坏环境告警校准-方案设计.md)。

## C1-C12 候选详情

### C1 受序双尺度动态记忆（原方案 A 机制一）

- **内容**：RWKV-7 状态更新外增加每头可证明受序的快、慢保留门，两条状态共享移除/写入/值内容，只在保留尺度上不同。
- **来源**：`2026-08-05-方案A第三章-受序动态记忆与正常状态生成.md`
- **证据状态**：`弱证据不利`
- **证据**：LSPR24 窗口级 HGB 代理，快（lag1-4）0.9601 / 慢（lag8-32）0.9560 / 快+慢 0.9624，双尺度相对最佳单尺度仅 **+0.2%**。
- **为什么不能据此淘汰**：这是**免训练滞后特征**的代理，不是学习型双状态门；HGB 的两组滞后特征天然冗余，学习型快慢门可能表现不同。且只在 LSPR24 一个数据集、一个尺度上测过。
- **待做实验**：在 TQH-C2 的 `interval × jitter` 网格上重测（多尺度动机在信标场景可能成立）；正式实验需学习型双状态。

### C2 正常状态生成（原方案 A 机制二，方案 B 亦含）

- **内容**：生成头只在经审计的连续正常历史与正常目标上拟合，用预测似然辅助检测。
- **证据状态**：`未筛选`
- **说明**：HGB 代理无法代理生成式辅助目标，至今**一次实验都没做过**。不得因方案 A 其他机制不利而连坐。
- **待做实验**：单变量消融（同骨干 + 有/无生成头），需真实训练。

### C3 状态条件字段交互（原方案 B 机制一）

- **内容**：语义字段组表示为当前窗字段标记，由上一时刻 RWKV 状态摘要调节字段交互。
- **证据状态**：`弱证据不利`
- **既有旁证**：LSPR24 窗口级 HGB 代理中，无历史时纯可加 0.6978 → 交互 0.9078（错误相对下降 **69.5%**）；给足 lag1-4 后 0.9240 → 0.9601（**47.5%**）；强交互 leaf=127 再降 31.8%。
- **新增直接证据**：开发区种子 42 的四变体神经快速消融中，字段注意力 Transformer（B1F）PR-AUC 为 0.9997750317365747、宏平均 F1 为 0.9948583299922971；RWKV 状态条件字段 Transformer 原子代理（A2）为 0.9997778729511367、0.9960671008288451。A2 相对 B1F 的 PR-AUC 仅 `+0.000002841214562`，低于 `0.005` 快速筛选门槛。详见[原子机制快速消融结果](../2026-08-08-LSPR24-RWKV原子机制快速消融结果.md)。
- **旁证**：三篇独立工作显示包头字段是主信号、载荷贡献接近零（NetMamba 表VI 去头部降 15.5–48.8%、去载荷仅降 0.16–0.84%）。
- **裁决与局限**：HGB 树分裂交互 ≠ 状态条件字段注意力；当前 A2 也只是上一状态的有界加性字段调制，不等同于完整 RWKV-7 状态更新。此次实验只对该原子代理给出弱不利证据，不证伪方案 B 的完整双机制方法。远端 A2 展示名中的“主候选”为历史标签，现已废止。
- **待做实验**：若保留方案 B，必须先提出第二个独立机制及两者的组合理论，再以接收基座、无条件字段交互、状态条件机制、完整双机制的共同预算消融验证组合增益。

### C4 时间尺度课程训练

- **内容**：难度 = `(interval_s/窗长, jitter_pct)` 二维外生网格，从短周期零抖动推进到 3600 秒 70% 抖动，同时调度衰减谱下限 `w_min(t)` 使每阶段最小可积累周期 ≤ 有效记忆长度 `L_eff = 1/(1−w_min)`。
- **证据状态**：`未筛选`
- **说明**：训练方法层面的创新点，需真实训练才能测，**至今无实验**。
- **待做实验**：同骨干同注入下，课程 vs 常规混合训练，多种子。

### C5 端点窗口序列维的 delta 规则状态注入

- **内容**：注入不发生在窗内 token 维，而在跨窗/跨连接的端点序列维；LLaMA-Adapter 式零初始化门控保证「注入前 = 原骨干」。
- **证据状态**：`实验无效`
- **证据**：E-INJ（TQH-C2 跨 testbed）B0 骨干only 0.9522 / B1+EMA 0.6406 / B2+对角 0.8864 / B3+delta 0.3430。
- **为什么无效**：**泄漏审计 delta 状态特征反预测 testbed AUC = 0.9998**——随机投影状态累积端点专属统计量，而每端点只属一个 testbed，状态退化为 testbed 指纹。这是实验设计缺陷，不是注入无效的证据。
- **待做实验**：重做，需 (a) 端点内标准化去除端点身份信息 (b) 同 testbed 内划分或多 testbed 交叉 (c) 学习型而非随机投影注入。

### C6 时间感知（连续时间）衰减

- **内容**：把 RWKV-7 的 per-step 衰减改为壁钟时间感知 `w_k = exp(-Δt_k · softplus(κ(x_k)))`，使状态半衰期以秒计而非步数计。
- **证据状态**：`未筛选`
- **动机（实测）**：TQH-C2 testbed 背景率仅 12 事件/小时，而真实 soho 端点连接率中位 **67/h**、p90 **198/h**；同样的 3600 秒信标，真实背景下要跨 ~200 步（p90 593 步）而 testbed 只跨 12 步。步数尺度与壁钟尺度在此严重脱钩。
- **待做实验**：E1 背景率扫描（soho 混入），测 K_min 随 λ_bg 的关系。

### C7 ROSA / RWKV-8 后缀自动机注入 ★新增

- **内容**：RWKV-8 "Heron" 的 **ROSA（Rapid Online Suffix Automaton）**——可微、在线、常数内存的后缀自动机，做精确模式匹配与复制。注入基础模型用于加密流量的结构重复模式发现。
- **来源**：本机 `BlinkDL/RWKV-LM` 快照 `952102498e...`（Apache-2.0）的 `RWKV-v8/` 与 `RWKV-8.md`
- **证据状态**：`未筛选`
- **官方实测能力**：`251105_reverse_run.py` RWKV7+ROSA **4 万参数**（L2-D32）反转 1–60 位数字 **99.8% 位准确率**；`251024_rosaQKV_run.py` **100 万参数** 40 位加减法 **99% 位准确率且无需 CoT**；`260212_rosa1bitLM_L12.py` 纯 ROSA1bit+FFN（无 RWKV）仅 minipile 1.5B token 已能 copy & count。实现核心：`matched 1 => e, matched 0 => -e, unmatched => 0`。
- **为什么与本课题契合**：GoldFinch 实测纯 Finch 在 MQAR 多查询联想召回上随长度失效——**精确模式匹配是 RWKV-7 的能力缺口**，而 ROSA 正是补这个缺口的。恶意流量检测的本质之一就是模式匹配（重复 C2 载荷结构、周期信标签名、协议指纹序列），签名检测干的就是这件事。
- **风险**：RWKV-8 **无正式论文**，只有官方仓库实现与设计文档；一切效果主张须本课题独立验证。安全领域 247 篇 RWKV 生态论文中**零篇涉及 ROSA**。
- **待做实验**：ROSA 在流量序列上能否发现重复结构；相对普通 RWKV-7 的增量。

### C8 骨干替换：NetMamba 的 Mamba → RWKV-7

- **内容**：NetMamba 是 Mamba(SSM) 骨干、约 6.5M 参数、权重 26.2 MB 可下载。把 Mamba 块换成 RWKV-7 time-mixing，数据管线、MAE 目标、参数规模全不变——**变量最干净的对照实验**。
- **证据状态**：`未筛选`，但有**强不利旁证**
- **不利旁证（实测，来自 NetMamba 论文表VI 与 NetMamba+ 表V）**：序列骨干本身收益很小——vanilla Transformer vs Mamba 差距仅 **0.5–2.6 个百分点**，且 CICIoT2022 与 ISCXTor 上 Transformer 反而更好；**Linear Transformer 直接崩溃**（0.6413 / 0.4226 / 0.3754）。
- **含义**：换骨干的天花板很低，而线性递归有真实崩溃风险。若做此实验，须正面回应"过度压缩注意力的代价"。
- **风险**：NetMamba 许可证未标注；仓库钉死 torch 2.1.1+cu121，RTX 5090（sm_120 需 CUDA 12.8+）有兼容风险。

### C9 骨干替换：netFound 的 flow 级注意力 → RWKV 状态递归

- **内容**：netFound 层次化结构中，flow 编码器**只在 ≤12 个 burst 的 CLS token 间做注意力**——序列长度仅 12，是替换为固定状态递归的天然位置。
- **证据状态**：`未筛选`
- **优势**：netFound 是唯一「权重可下（HF `snlucsb/netFound-small` 53.4M）+ 许可证 MIT + 2026-03 仍维护 + 抗捷径实测过关」的骨干。netFound 表2 显示删掉捷径字段后 ET-BERT 99.82→32.83、YaTC 99.69→63.48，而 **netFound-large 保持 99.98 不变**。
- **输入形态**：netFound 是唯一「匿名化包头协议字段 + 数值型运营元数据（IAT/burst 字节数/包数/方向）」输入，与本课题字段+统计数据最同构。
- **风险**：flow 级序列只有 12 步，替换成状态递归的收益空间可能很小（与 C1/C6 的"长历史无用"证据一致）。

### C10 表格形态的小规模自监督预训练

- **内容**：在 Annotated-Encrypted-Traffic-2026 的 **140.5 万条 TLS 连接**上做掩码重建预训练（`tls.rec` 长度序列 + 元数据字段），骨干用 RWKV/Transformer 混合小模型从头训。
- **证据状态**：`未筛选`
- **动机（实测）**：流量领域**没有任何权重可下载的预训练骨干原生支持表格形态**，而我们三类数据里两类是表格/元数据形态。这是真实空位，FlowSem-MAE（2026-03）刚开始填但未开源。
- **优势**：注入即架构本体而非补丁；数据量足够；完全绕开形态不匹配。
- **风险**：`tls.rec` 固定截断 20 条，无长序列。

### C11 跨加密协议不变表示

- **内容**：利用 TQH-C2 的 **B(Merlin/QUIC/HTTP3) 与 D(Merlin/HTTP2/TLS) 受控对照对**——同框架同 kill-chain 只差传输加密层——学习协议变换下守恒的行为表示。
- **证据状态**：`弱证据有利（短间隔）／不利（长间隔）`
- **证据**：A→D 跨协议仅时序特征，i=300 子集 err 1.000→0.227（错误相对下降 **+77.3%**），pooled 最优 +21.8%（超 16.1% 门槛）；但 i=1800 最多 +22.5%、**i=3600 仅 +5.3%**。
- **已知障碍（实测）**：D 组 HTTP/2 复用把连接级信标周期完全抹掉（c2 连接 gap 中位 ≈10.5 秒，与 `interval_s` 完全脱钩，CV 2.7–4.4）。长间隔跨协议主张在连接级不成立，须下到 TLS 记录级。
- **B 组统计功效不足**：每格正例仅 24–30。

### C12 漂移触发有界状态控制与可信双参照校正 ★跨年度完整候选

- **详细方案**：[C12 新方案设计](C12-漂移触发有界状态控制与可信双参照校正-方案设计.md)
- **快速实验合同**：[C12 新快速消融](C12-漂移触发有界状态控制与可信双参照校正-快速消融.md)
- **原创边界**：[C12 原创边界重构报告](../2026-08-11-C12原创边界重构报告.md)
- **数据合同**：[LSPR23-LSPR24 共同字段审计与跨年度数据合同](../2026-08-11-LSPR23-LSPR24共同字段审计与跨年度数据合同.md)
- **LSPR 文献影响**：[LSPR 直接使用论文查全审计](../2026-08-11-LSPR直接使用论文查全审计.md)。查全范围内确认 4 个直接使用工作，其中 3 个有模型实验；它们强化跨年度任务动机，但使 `LSPR23→LSPR24` 任务、`2-IP` 构造以及 RF／XGBoost／GRU／Transformer 对比均不能作为原创。
- **候选章节名**：基于漂移触发有界状态控制与可信双参照校正的网络攻击状态检测方法。
- **要解决的问题**：上一年度有标签流量训练的检测器在下一年度面对业务与攻击漂移。既有状态核的更新公式不作为本候选的原创；本候选检验如何约束状态提议的实际影响，以及如何在不改写源锚的前提下限量吸收可信新正常。
- **接收基座与既有核**：参数匹配的因果字段 Transformer 接收 DeltaNet、RWKV-7 或门控 DeltaNet 的状态提议。三种更新核均为强基线和承载核，不能计为 C12 创新。
- **公开强基线门禁**：在状态核公平矩阵外，至少补同一 77 字段与样本合同下的随机森林、XGBoost、GRU、因果 Transformer 和普通 RWKV；一维卷积神经网络作为 `P1` 外部对照。Dijk 2024 的同年 RF `F1=0.997` 因字段、随机种子和隔离细节不完整，只作外部参照。
- **机制一：漂移触发有界状态控制**。漂移证据只控制核后状态提议的单步／累计影响预算、冻结和检查点回退，而不重写 Delta 或 RWKV 状态更新公式。
- **机制二：可信双参照校正**。冻结源锚永不被目标更新改写；隔离候选参照只在有效质量、跨块稳定性和影响约束同时满足时保留、拒绝或限量晋升，且首次晋升前对检测头零影响。
- **理论与篇幅可行性**：新设计为 `36` 个实质公式和 `1` 个算法，性质仅约束状态影响、冻结回退、源锚隔离与标签不可达，不保证性能或污染识别正确性。
- **证据状态**：`Q0快速筛选不晋升/弱证据不利`，`formal_paper_evidence=false`；不是正式证伪或论文结论。旧广义 Delta 公式已否决为原创主张；跨年度数据工程可继续复用。
- **快速消融**：在冻结 `LSPR23→LSPR24` 协议下比较 `RX-TR`、三种既有更新核、`C12-CTRL`、`C12-DREF` 与 `C12-FULL`，并执行预算、回退、单参照、有效质量、低熵污染、连续突发和困难新正常消融。目标前缀无标签，最终 20% 始终不可见。
- **可证伪条件**：完整方法不超过最强既有核或最佳单机制、控制器不优于漂移门值或仅单步预算、双参照不优于单参照，或污染状态后果无可观测上限时，淘汰对应主张。
- **当前门禁**：C56 的 Q0 有界共同缓存与 Python 只读缓存均已成功，四角色合计 928,188 条流、16,198 个完整 `2-IP` 序列；阶段 A v2 与阶段 B 的远端制品根为 `/root/autodl-tmp/thesis/experiments/llm_probe/runs/candidates/{c12-phase-a-balanced-sequence-pools-q0-seed42-v2-threshold-sentinel-rerun1,c12-phase-b-drift-bounded-control-dual-reference-q0-seed42-v1}`，所有收据为 `final_accessed=false`。阶段 B 目标开发 PR-AUC 为 `C12-DREF=0.00927447771`、`C12-CTRL=C12-FULL=0.00835439568`，三者恶意类 F1／召回均为 `0`；既有核 `K-DELTA=0.016977277`、`K-GDELTA=0.010194771`。`C12-FULL` 未超过最强既有核和最佳单机制，按快速合同停止 C12 继续扩展；本次仅为 `Q0快速筛选不晋升/弱证据不利`，`formal_paper_evidence=false`，不得写成正式证伪、论文结论或效果支持。下一步为文献协议桥接基线 Q0 与候选重排，并等待是否重构 C12 或转向其他候选。

### 候选 C 文献闭合重构：源时间双层尾部风险与上下文告警风险 ★当前首选待实验

- **方案与消融**：[候选 C 设计](候选C-源域时间环境稳健状态与最坏环境告警校准-方案设计.md)、[候选 C 快速消融](候选C-源域时间环境稳健状态与最坏环境告警校准-快速消融.md)。
- **机制一 M1**：只用 LSPR23 因果时间环境，外层以 GroupDRO 保护最坏环境，环境内以 CVaR 保护高损失尾部；V-REx 是同预算竞争目标。M1 作用于 RWKV 状态训练，主指标为目标平均精确率/PR-AUC、`AP/π`和最坏源环境风险。
- **机制二 M2**：LSPR23 良性校准集提供 NP 源锚；LSPR24 无标签前缀或因果历史拟合 EVT 尾部并输出上下文超越风险。纯阈值版只检查预算偏差和假阳性；只有风险分数版能改变跨时间块排序并检验固定总预算召回。
- **数学哨兵**：同一连续分数只改阈值时，PR-AUC和严格全局前K召回必须不变。若变化属于实现/样本清单错误，不是M2收益。
- **原创边界**：FOIL、DIVERSIFY和非平稳域泛化已占用潜在环境发现与不变学习；GroupDRO、REx、CVaR、NP和EVT均为既有理论。候选只保留“冻结LSPR合同下的RWKV状态双层训练风险＋跨块预算风险”窄组合主张，且效果待证。
- **与 C12 的区别**：不更新目标模型/状态，不维护可信新正常或双参照，不执行目标样本晋升；只对冻结分数做因果尾部统计。
- **证据链**：20篇新增全文笔记、30篇候选矩阵及三条路线比较见[跨年度非RWKV理论融合综述](../2026-08-11-跨年度任务非RWKV理论融合文献综述.md)。
- **R1 边界**：当前 R1 Q0 的稳定—私有状态和最坏源环境纯阈值配置保持冻结，只作历史信号筛选；文献重构是其后的新实验，不反向解释 R1 结果。
- **最小门槛**：M1 相对 ERM 目标平均精确率 `≥+0.010` 且不低于 GroupDRO `-0.002`；M2-risk 在预注册预算上固定预算召回 `≥+3` 个百分点；组合必须超过最佳单机制。任一门槛失败则否决对应主张。

## 共用数据缺陷与实验陷阱

1. **TQH-C2 A 组单元格内含流级捷径**：5 个流特征即 `err = 0`。任何有意义的实验必须跨 testbed 或只用时序特征。
2. **jitter 疑似在长间隔失效**：A 组 j70 单元格实测信标间隔 CV 仅 0.006–0.058，理论均匀 ±70% 应为 **0.404**（i=30 时实测 0.305 正常）。需从 pcap 层核实，否则"抖动鲁棒性"主张会被数据反杀。
3. **端点身份泄漏**：跨连接状态会累积端点专属统计量 → 退化为 testbed 指纹（E-INJ 实测 AUC 0.9998）。必须做端点内标准化 + 泄漏审计。
4. **输入模态 >> 骨干**：NetMamba+ 表VII，CipherSpectrum 上 Byte Only 0.8779 / Size Only 0.8906 / Interval Only 0.7556 / All 0.9652（**+71.5%**）。**任何骨干替换实验必须先固定输入模态作为混淆控制变量**，否则测的是模态不是骨干。
5. **二手数字必须回原文核验**：本会话已发生一次事故——把 FlowSem-MAE §1.1 的「冻结编码器 >90%→<47%」误记为 Demystifying (arXiv:2509.23089) 的结论，而后者全文无此说法（其表 6 线性探针 Macro-F1 为 YaTC 0.47 / ET-BERT 0.46 / NetMamba 0.62 / netFound 0.97，是不同任务不同量纲）。

## 筛选原则与关联文档

- **一次实验只淘汰它真正测过的东西。** 代理实验（免训练 HGB、随机投影、合成数据）只能改优先级，不能判死。
- **实验设计崩了（泄漏、任务无效、数据缺失）时，结论作废，候选状态回退到实验前**，不得顺势淘汰。
- 淘汰必须写明：用了什么实验、什么设计、什么门槛、精确数字。
- 提升幅度必须同时报告绝对指标与增量、错误相对下降 `(err_base − err_method)/err_base`、多种子离散性和置信区间；朱焱雷第三章 **16.1%** 只作历史参照，不单独决定候选存废。
- [窗口粒度敏感性实验计划](../2026-08-08-LSPR24窗口粒度敏感性实验计划.md) 是冻结后开发区的补充稳健性检查：固定比较 `G1=1秒×H20` 与 `G5=5秒×H4` 的共同锚点增量，不以其结果选择候选、替换 5 秒主结果或读取最终测试。
- [第三章选题策略与预选方案](../2026-08-07-第三章选题策略与预选方案.md)
- [LSPR24 先导实验证据记录](../2026-08-07-LSPR24先导实验证据记录.md)
- [A/B 先导实验执行错误记录](../2026-08-07-A-B先导实验执行错误记录.md)
- [RWKV 路线总控](../RWKV路线总控.md)
- [LSPR24 RWKV 原子机制快速消融结果](../2026-08-08-LSPR24-RWKV原子机制快速消融结果.md)
- 实验脚本：`thesis/experiments/llm_probe/scripts/lspr24_pilot/`
