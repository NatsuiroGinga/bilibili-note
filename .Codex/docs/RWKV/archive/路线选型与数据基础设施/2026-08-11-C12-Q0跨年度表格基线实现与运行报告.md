# C12 Q0 跨年度表格基线实现与运行报告

- 日期：2026-08-11
- 路线：`RESEARCH_ROUTE=RWKV`
- 实现代理：`c12_q0_tabular_baselines_terra_medium`
- 模型与强度：`gpt-5.6-terra`，`effort=medium`
- 状态：`REMOTE_SYNC_BLOCKED_BY_APPROVAL`

## 实现范围

- 新增 `src/flow_probe/lspr_crossyear_tabular_baselines.py`。
- 新增 `scripts/remote_launchers/run_lspr_crossyear_tabular_q0_seed42_v1.sh`。
- 仅使用缓存清单 `arrays` 中已发布路径；不使用因发布前原子替换而仍含临时目录的 `roles` 路径。
- 共同输入固定为 `x_value(77)+x_missing(77)+log1p(delta_t_us)`，共 155 维；不把样本、序列或分组标识输入模型。
- 模型为 HGB、随机森林（128 树、最大深度 10、bootstrap）和可用时的 XGBoost。所有模型在源训练拟合、源验证冻结阈值，并输出 0.5 与源验证宏 F1 最优阈值。
- 目标开发标签旁车仅先校验路径与哈希；概率写入并哈希封存后，才打开标签并校验形状、类型和行数后评价。目标前缀标签与最终区均未读取。

## C56 只读核验

- 缓存根：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-prepared/lspr23-lspr24-bounded-quick-q0-v1/experiments/c12-seed42-q0-2ip-v1/python-cache-v1`。
- 行数：source-train=142069、source-validation=262136、target-prefix=262144、target-development=261839。
- 缓存清单声明 `final_accessed=false`；C56 无活跃 `screen` 会话；可用磁盘约 53 GiB。

## 预期制品

- 运行根：`runs/baselines/lspr-crossyear-tabular-q0-seed42-v1/`。
- 每模型输出 `metrics.json`、`target_development_probability.npy`、`probability_seal.json` 和 `run_state.json`。
- 指标含目标开发 PR-AUC、宏 F1、恶意 F1、精确率、召回率、混淆矩阵、Brier、ECE、训练/推理耗时与峰值 RSS。
- SwanLab 固定项目 `mortiswang/malicious-traffic-llm`。在线失败会记录状态，但不影响本地制品。

## 风险

- 源验证良性样本少于 10000 时，每百万 100 误报阈值显式标为诊断不可裁决，不能作为模型选择结果。
- 本报告尚未填写真实指标；种子 42 结果只能用于快速筛选，不能作为论文效果结论。
- 2026-08-11：对 C56 的只读缓存、状态和磁盘核验成功；新增模块与启动器的精确 `rsync` 被安全审批拒绝，未尝试任何替代传输或远端启动。
