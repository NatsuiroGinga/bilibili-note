# LSPR24 Rust G0-A 任务 2 实施报告

- **日期**：2026-08-06
- **任务**：规范编码、语义 SHA-256 与确定性外部排序
- **状态**：两条精确目标测试通过，等待独立复审
- **合同 SHA-256**：`17a86b71e2858b420a73759437a5301e6efd3ba6179f949ea9d4d87adeeb24af`

## 1. 实施边界

本任务只使用人工规范向量和人工排序记录。未读取 LSPR24 Parquet、标签、攻击叙事、最终测试或任何全量数据；未修改任务 1 的 `config.rs`、`input.rs`、`parquet_scan.rs` 和 `parquet_time_scan.rs`，也未修改 Python、配置、数据、总控或既有 R2 Rust 包。

## 2. 修改文件

- `thesis/experiments/llm_probe/tools/lspr24_g0/src/types.rs`
  - 新增 `SourceRowIndex`、`EndpointRoleOrder`、`StableSortKey` 和 `SortableRecord`。
  - 稳定键字段顺序固定为端点摘要、`last_ns`、`start_ns`、`raw_flow_id`、端点角色和源行号。
  - 源行号递增和排序行数使用受检查整数运算。
- `thesis/experiments/llm_probe/tools/lspr24_g0/src/canonical.rs`
  - 新增单字节类型标记、四字节无符号大端长度和值字节组成的 `tuple_encode`。
  - 区分空值、空字符串、整数位宽、布尔、字节、规范 IP、SHA-256 和已量化十二位小数定点整数。
  - 新增行 SHA-256、有序行哈希长度前缀串联的数据集 SHA-256 和流式数据集哈希器。
- `thesis/experiments/llm_probe/tools/lspr24_g0/src/external_sort.rs`
  - 新增有界内存排序段、带记录数的确定性二进制段格式和最小堆多路归并。
  - 工作目录使用排他创建；成功或失败均清理本次排序段。
  - 完整稳定键重复、非法段内容、长度或计数溢出和输出失败均返回结构化错误。
- `thesis/experiments/llm_probe/tools/lspr24_g0/src/lib.rs`
  - 仅导出任务 2 的公共类型和函数；保留任务 1 导出。
- `thesis/experiments/llm_probe/tools/lspr24_g0/Cargo.toml`
  - 固定增加 `sha2 = "0.10.9"`。
- `thesis/experiments/llm_probe/tools/lspr24_g0/Cargo.lock`
  - 使用离线 Cargo 解析一次性重新生成，未手工编辑。
- `thesis/experiments/llm_probe/tools/lspr24_g0/tests/canonical_hash.rs`
  - 覆盖共享规范向量、空值与空字符串、整数位宽、行顺序和源行号溢出。
- `thesis/experiments/llm_probe/tools/lspr24_g0/tests/stable_external_sort.rs`
  - 覆盖完整稳定键每个字段、输入乱序、跨段归并、段大小 `1/3/65536`、数据集哈希、重复完整键失败和临时目录清理。
- `thesis/experiments/llm_probe/tests/fixtures/lspr24_g0/canonical_vectors.json`
  - 保存 Rust 与后续 Python 参考共同使用的逐字节编码和 SHA-256 人工向量。

## 3. 测试驱动开发证据

### 3.1 规范编码红灯

命令：

```bash
cargo test --manifest-path tools/lspr24_g0/Cargo.toml --locked --test canonical_hash
```

首次结果：退出码 `101`。编译器只报告 `CanonicalValue`、`SourceRowIndex`、`tuple_encode`、`sha256` 和 `dataset_semantic_hash` 等任务 2 接口尚不存在，符合预期红灯原因。

### 3.2 外部排序红灯

命令：

```bash
cargo test --manifest-path tools/lspr24_g0/Cargo.toml --locked --test stable_external_sort
```

首次结果：退出码 `101`。编译器只报告 `StableSortKey`、`ExternalSortConfig`、`stable_external_sort` 等任务 2 接口尚不存在，符合预期红灯原因。

### 3.3 锁文件生成

新增固定依赖后只执行一次：

```bash
cargo generate-lockfile --manifest-path tools/lspr24_g0/Cargo.toml --offline
```

结果：退出码 `0`，锁定 95 个与 Rust 1.85 兼容的包。

### 3.4 规范编码绿灯

同一精确命令结果：退出码 `0`，`3 passed; 0 failed`。

### 3.5 外部排序绿灯

同一精确命令结果：退出码 `0`，`2 passed; 0 failed`。

## 4. 格式化与静态检查

依照实施计划和任务边界，未运行 `cargo fmt`、全仓测试、Clippy 或其他无关静态检查。两条精确 `cargo test --locked` 均重新编译本库和对应集成测试并通过，未出现编译警告。

## 5. 缓存与制品

- Cargo 测试缓存：`thesis/experiments/llm_probe/tools/lspr24_g0/target/`，核对时总大小约 `526M`；该目录包含任务 1 与任务 2 的可再生缓存，不是正式实验制品。
- 外部排序人工测试工作目录在成功和预期失败后均由断言确认不存在。
- 未生成正式 G0-A 运行目录、收据、Parquet 输出或模型结果。

## 6. 遗留风险

- 当前只由人工向量证明 Rust 语义；与 Python 的逐项硬一致留待任务 4。
- `FixedDecimal12` 接受已经完成十二位小数半偶舍入的 `i128` 定点值；源浮点解析、非有限值隔离和量化入口不在任务 2 范围。
- 当前多路归并同时打开全部排序段。正式物化配置必须让排序段数量低于主机文件描述符上限；若资源预检不满足，需要在不改变稳定键和语义哈希的前提下增加固定扇入的确定性多轮归并。
- 未运行真实 LSPR24，因此本报告不能证明 G0-A 通过，也不能启动任何基线或方案 A/B 训练。
