# 独立 CUDA-RWKV Raw83 资格方案任务计划

## 目标

形成一份可直接转交实现代理的独立 CUDA-RWKV Raw83 资格方案。本轮只设计、核验并提交本目录，不修改生产代码，不访问服务器，不启动数据物化或训练。

## 代理映射

- 代理名：`rwkv_cuda_standalone_raw83_design_sol_high`
- 模型：`gpt-5.6-sol`
- 推理强度：`high`

## 文件所有权

- 本任务只拥有本目录中的 `task_plan.md`、`notes.md` 与 `实施方案.md`。
- 仓库中其他已修改、未跟踪或已暂存文件均不修改、不暂存、不提交。
- 后续生产实现的文件所有权只在 `实施方案.md` 中预注册，本轮不创建这些文件。

## 阶段

| 优先级 | 阶段 | 状态 |
| --- | --- | --- |
| P0 | 完整读取根、过程文档、RWKV、论文和实验规则，恢复第三章路线 | 已完成 |
| P0 | 读取统一 Goal、共享 Raw83 合同与当前独立 CUDA 资格方案 | 已完成 |
| P0 | 读取官方 CUDA 供应商快照、后端、统一精度与跟踪实现 | 已完成 |
| P0 | 读取 RWKV 数据适配文档与朱焱雷论文对位证据 | 已完成 |
| P1 | 冻结独立实现依赖闭包、数据集－模型适配表和参数公式 | 已完成 |
| P1 | 冻结 A/B、R1/R2、容量包的分阶段最小源年选择 | 已完成 |
| P1 | 冻结 CUDA 编译、数值稳定、六梯度、首步更新与资源门 | 已完成 |
| P1 | 冻结四格、源年封印、目标年描述性评价与完整指标 | 已完成 |
| P1 | 冻结 SwanLab 单运行恢复、20 步断点和活动路径迁移 | 已完成 |
| P0 | 执行禁词、链接、差异、提交范围检查并只提交本目录 | 已完成 |

## 验收条件

1. 正文只用“独立 CUDA 融合实现”或“CUDA-RWKV”指称活动训练实现。
2. 活动入口的依赖闭包只包含当前方案登记的独立 CUDA、Raw83、统一精度与统一跟踪模块。
3. 共享 Raw83 的 LSPR23 源产品是唯一输入事实源；已知全年度标准化缓存不得参与拟合、选择或训练。
4. 输入视图、语义适配和容量依次单因素选择，禁止 A/B、R1/R2 与容量包的笛卡尔展开；所有选择只用 LSPR23 固定验证逐流 AP。
5. 数据集－模型适配表覆盖 A/B、12 语义组、端口、协议、二元字段、`I/M`、长度 128、开始时刻、位置因果、块间状态、标签权重、输入输出状态梯度和参数哈希。
6. 朱论文对位表准确记录模型专属输入与双模块消融，只借组织方式，不迁移对抗任务、模态、协议或数值。
7. CUDA 资格只由官方固定源码身份、目标环境编译、自身数值稳定、六梯度和首个完整优化步裁决。
8. 每 20 个完整优化步原子保存；恢复严格匹配 CUDA 可执行身份、数据、模型、配置、精度和随机状态。
9. SwanLab 生命周期落实提交 `89ddecf`、`d6313ca`、`70a8a2b` 的收据与恢复边界。
10. 源年四格全部封印后才允许首次打开 LSPR24，并报告完整共同整数 FP 预算、首次告警、实体 AP、逐流 AP 和资源。
11. 明确 2026-08-22 当前不能把“源年 Raw83 物化 -> 分阶段选择 -> 四格训练”整链报告为完成。
12. 精确列出后续新增文件所有权、依赖闭包和独立运行身份门。

## 验证命令

- `git diff --check 1cd5ff3 -- .Codex/docs/RWKV/2026-08-22-CUDA-RWKV独立Raw83资格`
- `rg` 扫描禁用表述、占位词、失效相对链接和不允许的活动依赖。
- `git diff --name-only 1cd5ff3` 与 `git diff --cached --name-only` 核对提交范围。
- 不运行 `black` 或其他自动格式化器。

## 状态

方案已冻结并通过文档验收，等待限定提交。所有效果状态仍为“实验待证”。

## 错误记录

- 首次本地文献索引状态命令因默认 `uv` 缓存目录不可写失败；改用 `/private/tmp/codex-uv-cache` 后成功。
- 索引初检为 `stale=true`；按仓库规则重建并复查为 `stale=false` 后才执行混合查询。
- 首次骨架快照提交被 `git diff --check` 拦截，原因是三份新文件末尾存在空行；修正后快照提交 `1cd5ff3` 成功。
- 一次只读路径核验在 zsh 循环中误用变量名 `path`，覆盖了 zsh 的命令路径数组，导致循环后的 `wc` 暂时不可用；改用新进程和非保留变量后核验通过，未写文件。

## 十四、唯一修订复审（2026-08-22）

### 范围

- 修订只修复合并复审提出的工程完整性缺口，不新增模型、科学机制、选择候选或效果主张。
- 继续独占原 14 个生产/报告文件及本目录 `task_plan.md`、`notes.md`、`实施报告.md`；其他工作树变更不修改、不暂存、不回滚。
- 不访问服务器，不训练，不运行测试或 `black`，不修改官方上游原件。

### 修订任务

| 优先级 | 任务 | 状态 | 验收点 |
| --- | --- | --- | --- |
| P0 | 不可绕过的资源收据与原子运行锁 | 已完成 | Python 直达缺收据失败；收据绑定配置/依赖/资源/并发/时间；锁可靠释放 |
| P0 | 源封印与目标动作代码身份 | 已完成 | target 不改写源闭包；配置、闭包、评价代码逐项精确比对 |
| P0 | 目标格缓存完整身份 | 已完成 | 每格绑定目标清单文件/内容、制品闭包、评价、依赖、源封印、检查点，复用前全量复核 |
| P0 | 首次告警无损压缩阶梯 | 已完成 | 每个可达阈值只存一份右连续 1 基暴露阶梯；整数 FP 轴和六档用哈希/索引机械还原 |
| P0 | 训练单元完整清单与顶层清单 | 已完成 | 完成快路径反向验齐全部必需文件、SHA、字节和资源/梯度；源封印引用全选择+四格单元清单 |
| P0 | 20 步前恢复与首步门 | 已完成 | 覆盖身份前先比对；无 latest 但有旧首步收据时同身份重做，身份漂移拒绝 |
| P0 | `g` 门函数 FP32 敏感区 | 已完成 | 两次矩阵乘与 sigmoid 全在禁 autocast FP32 区，然后再转换到核合同精度 |
| P0 | SwanLab 两次上限状态机 | 已完成 | success 反向验 inflight/健康门/标签/生产身份；attempt2 仅绑首次零步 401；末写完整文件清单 |
| P0 | source/target/publish 顶层完成态与清单 | 已完成 | 完成快路径先反向验所有文件；不凭 summary/seal 单文件判完成 |
| P1 | 报告据实修正与唯一静态验收 | 已完成 | 只运行一次指定的语法/导入/help/配置/Shell/限定差异检查，报告不超出实际证据 |

### 直接裁决

- 及时检出阶梯采用无损右连续压缩：阈值定义下的唯一 `exposure_index` 截止点、累计检出实体数、固定正实体分母检出率和未告警率构成一份阶梯。整数 FP 轴只引用阶梯索引与哈希，相同阈值不重复写大数组。
- 资源收据与原子锁是 source/target 入口的强制能力，不是启动器约定；Python 直达也必须持有当前身份的未过期收据。

## 十五、剩余三项定点修复（2026-08-22）

| 任务 | 状态 | 不可变验收 |
| --- | --- | --- |
| 完整 path-max/terminal 整数 FP 曲线与六锚点及时阶梯分层 | 已完成 | 完整曲线仍逐点保存实际 FP/FPR/DR/阈值/并列/next；`unalerted_fraction=1-detection_rate`；及时阶梯只对六个预注册共同实际 FP 锚点生成，按阈值去重，`len(ladders)<=6`且总点数不超过 `6×正实体数` |
| 训练单元资源时窗与本地冻结切片 | 已完成 | 首次启动持久化全局采样起始偏移/时刻，恢复沿用；完成时以完整行终止偏移原子写入单元 `resource-samples.tsv`；资源汇总只读本地切片，无法机械证明时 `fair_evidence=false` 并阻断资格 |
| 业务结果哈希与动作元数分层 | 已完成 | 任何已含 `result_sha256` 的业务对象不再扁平追加 action manifest 或幂等 marker；正常/幂等返回统一使用外层 envelope |

依据边界：完整首次告警 path 曲线服务统一 Goal/N16 的共同整数 FP 比较；暴露及时阶梯只服务六个预注册展示锚点。禁止为所有可达阈值生成“阈值×暴露位置”二维稠密阶梯。

## 十六、CUDA 工具链环境边界定点修复（2026-08-25）

- **真实症状**：B76 上同步后的正式启动器在 `source tools/env/activate.sh` 之后报 `NVCC_AVAILABLE=0`。
- **已知反证**：`/usr/local/cuda/bin/nvcc` 已由 `761f2c6` 对应的真实自身门验证；PyTorch `2.13.0+cu130`、CUDA `13.0` 和 RTX 5090 正常。因此不是编译器未安装或 GPU 故障。
- **根因**：项目环境激活后的 `PATH` 未保留 CUDA `bin`，首版配置也未冻结 `CUDA_HOME`/物理落点/nvcc 路径，后端只能依赖漂移后的 `shutil.which("nvcc")`。
- **单一假设**：在环境激活后、任何 Python/torch/CUDA 导入前，显式冻结 `CUDA_HOME=/usr/local/cuda`，只允许物理落点 `/usr/local/cuda` 或 `/usr/local/cuda-13.0`，并前置导出 `PATH`/`LD_LIBRARY_PATH`，就能恢复且唯一化正式 nvcc 发现。

| 修复层 | 状态 | 验收 |
| --- | --- | --- |
| 正式启动器 | 已完成 | 激活后冻结/解析 CUDA 根，导出 `CUDA_HOME/PATH/LD_LIBRARY_PATH`，`command -v nvcc` 必须精确为 `/usr/local/cuda/bin/nvcc` |
| 冻结配置 | 已完成 | 登记逻辑根、允许物理根、nvcc 和 `lib64` 路径，静态验证精确值 |
| 官方 CUDA 后端 | 已完成 | 在编译前比对环境、物理根、PATH 首命中、`LD_LIBRARY_PATH` 和 nvcc 逻辑/物理路径，并纳入 CUDA 执行身份 |

本修复不改模型、数据、容量、预算、运行身份或资源门槛。
