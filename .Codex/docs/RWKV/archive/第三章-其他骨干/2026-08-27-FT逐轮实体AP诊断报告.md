# FT 四格逐轮检查点目标年实体 AP 诊断报告（不可行收档）

日期：2026-08-27。执行代理：`ft_per_epoch_entity_ap_diag_sonnet`（模型 `claude-sonnet-5`，本任务未显式指定 `effort`，按默认强度执行只读核验，未运行任何 GPU 计算）。

## 一、任务与预注册依据

`.Codex/docs/RWKV/2026-08-26-第三章跨骨干机制实验负结果档案.md`「二点五」节预注册的「决定性最小实验（零训练，服务器恢复后执行）」：对 FT 四格逐轮检查点计算目标年实体 AP，用途是判断 FT 的 `C00=0.7508` 是否可信、选轮（C11 落在 epoch 14）是否为盲区。原文两种结局都不产出正文文字——本报告即该实验的第三种结局：**实验前置条件不成立，无法执行**。

任务简报已预判此风险并明确指示：「若 FT 未持久化逐轮检查点则本实验不可行，立即报告并停止，不得用别的东西凑」。核验结果确认该风险为真。

## 二、核验方法

### 2.1 源码核验（本地）

文件：`thesis/experiments/llm_probe/tools/ch3_ft_transformer_field_token_protocol_a.py`。

- `_EXPECTED_ARTIFACT_POLICY`（第 420-426 行）登记的落盘策略：
  ```
  "persist_selected_checkpoint_per_cell": True,
  "persist_inflight_epoch_checkpoint": True,
  "persist_per_flow_scores": False,
  "persist_per_entity_scores": False,
  "persist_complete_budget_curve_aggregate": True,
  ```
  没有任何「逐轮归档全部检查点」的策略字段。
- `train_cell` 函数（第 3280 行起）文档字符串明确三层同身份断点恢复语义（第 3307-3316 行）：
  - **完成层**：`checkpoints/selected-{cell}.pt` + `receipts/selection-{cell}.json`，只保存 `best_epoch` 的 `best_state`。
  - **在途层**：`inflight/{cell}.pt`，**单个文件、每轮 `optimizer.step()` 完成后原子覆盖写入**（第 3631-3650 行），用途是训练中断后恢复，不是逐轮归档。
  - 训练循环内 `best_state` 只在 `validation_ap > best_ap`（严格大于）时更新为当前轮权重快照（第 3603-3609 行），非最优轮的权重从不单独落盘。
  - 格训练完成后 `inflight_path.unlink(missing_ok=True)`（第 3775 行）删除该滚动文件。
- 全文 `grep` 未发现任何 `checkpoints/epochs/`、`epoch-\d+\.pt` 或类似逐轮归档路径字面量。

结论：该工具的设计从落盘策略层面就不支持逐轮回放，不是本次运行「跑丢了」某个环节。

### 2.2 服务器实测核验

运行根：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/diagnostics/ch3-ft-transformer-field-token-protocol-a-seed42-v1/`。

- `checkpoints/` 目录内容（`ls -la` 实测）：
  ```
  selected-C00.pt   4027970 字节   Aug 25 23:37
  selected-C01.pt   4027970 字节   Aug 26 16:08
  selected-C10.pt   4027970 字节   Aug 26 18:40
  selected-C11.pt   4027970 字节   Aug 26 21:20
  selected-whole-vector-projection-structure-matched-control.pt  3957818 字节  Aug 26 23:29
  ```
  恰好 5 个文件，每格 1 个（最优轮），无逐轮细分。
- `inflight/` 目录：**实测为空**（`ls -la` 仅 `.` `..`，0 文件）。四格训练全部完成后已被清空，与源码 `unlink(missing_ok=True)` 行为完全吻合。
- 全目录 `find`（含 `*checkpoint*`、`*epoch*` 通配，`maxdepth 3`）未发现 `checkpoints/epochs/<cell>/epoch-XX.pt` 或任何逐轮权重文件。

两条独立证据（源码设计 + 服务器实测）互相印证：**逐轮检查点从未存在过，不是被清理丢失，而是这次运行的落盘合同本身就没有生成它们。**

## 三、判定与后续路径

- **本次预注册的零训练诊断实验不可行，就此停止，未提交任何新工具代码，未启动任何 GPU 任务，未消耗 GPU 时间。**
- 若仍需回答「`C00=0.7508` 是否可信」「选轮是否为盲区」这两个问题，唯一路径是**重新训练并显式逐轮持久化权重**——这不再是零训练诊断，而是一次新的、需要完整训练时长（4 格 × 20 轮）的正式或诊断性重跑，且需先修改工具的落盘策略（新增逐轮归档分支）并重新走数据集-模型适配与身份冻结流程。该路径与「决定性最小实验」的「零训练」定性不符，是否值得投入由用户按第四章设计动机的优先级裁决。
- **降级替代信号（用户 2026-08-27 明确否决，不采用）**：四格 `receipts/selection-{cell}.json` 的 `history` 字段含逐轮 `validation_flow_ap`（LSPR23 源年验证口径，非目标年）、`p`（ELP 学得指数）、`mean_training_loss`。用户裁决：该指标就是负结果档案「二点五」节通道 3 已证饱和、无分辨力的同一逐流 AP（格间差 `3.39e-06` < 格内波动 `6.37e-06`，X6），四格实测该曲线全程收敛于 `0.994–0.9999` 区间、格内波动远大于任何可解释信号，读取它回答不了「目标年实体 AP 逐轮走向」这一决定性问题，只会引入噪声。故本报告不采用该信号，不将其写入任何证据表。

## 四、最终处置

- **结局：不可行收档。** 预注册的「决定性最小实验」两种设计结局（选轮盲区被证实 / 各轮均低）均未达成，因为其前置条件（逐轮检查点存在）不成立；核验本身即为有效交付——查明「实验不可执行」并及时止损，避免了在仅剩约 2 小时的 GPU 窗口内启动一个不可能产出目标数据的任务。
- 负结果档案第一节暂不追加 FT 结论行；FT 在「二」节的既有记录（`训练未完` → 现已训练完成但选轮问题「未解决」）维持现状，不升级为 PASS 也不降级为 FAIL。
- 不重新训练。理由：与当前 GPU 窗口不匹配；且 FT 已按跨骨干机制消融判据定为 FAIL（负结果档案「二点五」节：ELP 效应 `-0.1891`、CPA 效应 `-0.1451`、联合 `-0.4712`），正文本就零出现，选轮盲区问题不影响该判据结论，仅影响「C00=0.7508 是否对已发表基线构成真实威胁」这一辅助问题，其优先级不足以启动全量重训。
- 本任务到此结束，未产出新工具代码，未启动任何 GPU 任务，未消耗 GPU 时间。

## 五、边界

- 本报告不得进入正文、主表、图件或摘要；与负结果档案第一节「决定性最小实验」条目同一约束，只作过程记录。
- 本报告不否决、不确认 FT 判据的 PASS/FAIL；「决定性最小实验」预注册的两种结局均未达成，问题保持「未解决」状态，负结果档案第一节暂不追加 FT 结论行。
