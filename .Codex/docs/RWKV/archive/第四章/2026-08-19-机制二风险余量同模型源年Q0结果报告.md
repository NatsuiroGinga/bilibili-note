# 机制二风险余量同模型源年 Q0 结果报告

- **日期**：2026-08-19
- **运行标识**：`ch4-xgb-confidence-margin-fallback-same-model-source-q0-seed42-v1`
- **展示名称**：风险余量有限样本确认与源阈值回退同模型源年 Q0
- **记录代理**：`ch4_mechanism2_result_update_terra_medium → gpt-5.6-terra → effort=medium`
- **证据等级**：LSPR23 源年、单种子、筛选证据；非独立测试，非正式论文证据
- **机械状态**：`finished / complete / exit_code=0`
- **最终裁决**：`MECHANISM2_CONFIDENCE_MARGIN_SAME_MODEL_SOURCE_Q0_REJECTED`

## 一、结论

机制二风险余量变体在同模型三分源年 Q0 中被有效否决。每方向的开发、校准和持出角色均使用同一冻结折外模型生成分数，且角色隔离成立；因此不存在此前旧 Q0“不同模型生成三角色分数”的无效根因。

三方向校准的 Clopper-Pearson 假阳率上界为 `0.04281238254`、`0.03727371209`、`0.04181792561`。只有方向 1 满足 `U_CP <= 0.04`，而冻结规则要求三方向全部获证，故活动规则按合同回退。池化候选虽将 FPR 降至 `0.03551700070`，却保持 DR=`0.98717948718` 不变，不能改变“三方向全获证门失败”的裁决。

该结果是当前冻结分数阈值再校准路线的有效源年否决。停止该路线，不继续搜索风险余量。跨年度仍未验证，不得写成目标年度无效、正式论文结论或有限样本方法的普遍否定。

## 二、运行有效性

| 项目 | 实际值 |
| --- | ---: |
| 源年度 | `LSPR23` |
| 随机种子 | `42` |
| 目标年数组读取 | `0` |
| 新模型训练 | `0` |
| 墙钟时间 | `33.5499165` 秒 |
| 峰值 RSS | `1264.9375 MiB` |
| 峰值 GPU 显存 | `0 MiB` |

本运行状态为 `finished/complete/exit=0`。其同模型三角色设计与上一轮同模型三分修正 Q0 一致，故本次否决可作为机制二当前冻结分数阈值再校准路线的有效筛选结论。

## 三、三方向确认结果

| 方向 | 校准假阳实体数 | Clopper-Pearson 上界 | 是否获证 |
| --- | ---: | ---: | --- |
| `0` | 660 | `0.04281238254` | 否 |
| `1` | 571 | `0.03727371209` | 是 |
| `2` | 644 | `0.04181792561` | 否 |

全局失败概率 `δ=0.05` 按三方向固定均分为 `δ/3`；`δ` 是证书总失败概率，不是实体假阳率预算 `q=0.04`。本运行不因结果调整 `δ`、方向数、角色划分或比较规则。

## 四、池化持出结果与回退

| 规则 | FP | TP | FPR | DR |
| --- | ---: | ---: | ---: | ---: |
| 源阈值基线 | 1,947 | 77 | `0.03882740054` | `0.98717948718` |
| 风险余量候选 | 1,781 | 77 | `0.03551700070` | `0.98717948718` |
| 最终活动回退 | 1,784 | 77 | `0.03557682720` | `0.98717948718` |

风险余量候选与源阈值相比未增加真阳实体；但候选池化读数不替代逐方向证书。因方向 0 与 2 未获证，活动规则必须回退，机械裁决为 `MECHANISM2_CONFIDENCE_MARGIN_SAME_MODEL_SOURCE_Q0_REJECTED`。

## 五、裁决边界

- 停止当前冻结分数上的机制二阈值再校准路线，不继续搜索风险余量。
- 不调整 `q`、`δ`、方向数、角色划分或比较符以追逐本次结果。
- 不读取 LSPR24；跨年度有效性仍未验证。
- 不把本结果写入论文正文、正式结果表、摘要或结论。

## 六、制品索引

- 远端运行根：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/candidates/ch4-xgb-confidence-margin-fallback-same-model-source-q0-seed42-v1/`
- 原始证据：运行聚合结果、状态、资源收据、制品清单与运行日志。

本机工作树尚未同步该运行目录。本报告依据已核验的远端原始收据记录，待运行制品同步后应逐项比对本报告数值；同步前不追加任何事后分析或外推。
