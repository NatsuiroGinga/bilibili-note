# PBC 父运行恢复证明修复实施报告

## 结论

已完成单一合同修复：不向中断父运行补写或伪造 `manifest.json`，改由独立生成器在
`runs/recovery/ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1-for-ch4-xgb-pbc-q0-seed42-v1-v1/`
生成 `parent-recovery-proof.json`，PBC 在读取任何 LSPR24 标签前显式核验该证明及已完成的父评价
`continuation2`。

恢复证明明确保留父运行 `running/source_selection/exit_code=null`、完成状态为假、缺失原始结果和
原始清单的事实。它固定核验选择收据、11 项有效配置收据、`model_raw83`、`model_semantic168`
和三份 `semantic168` 折外模型，并通过实际加载确认五份模型均为 800 树。

## 变更文件

- 新增 `thesis/experiments/llm_probe/tools/build_ch3_xgb_parent_recovery_proof.py`。
- 修改 `thesis/experiments/llm_probe/tools/ch4_xgb_pbc_q0.py`。
- 修改 `thesis/experiments/llm_probe/configs/ch4-xgb-pbc-q0-seed42-v1.json`。
- 修改 `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch4_xgb_pbc_q0_seed42_v1.sh`。
- 新增本报告。

未修改父运行目录、父配置、父评价制品、数据切分、标签、指标、四方法、先导比例、预算、
BBSE 与回退公式、运行身份及其他文件。

## 门禁顺序

1. 启动器只读检查父状态、选择收据、有效配置收据、五份依赖模型和父评价制品。
2. 恢复证明生成器验证固定摘要、父身份、中断状态、缺失完成制品、11 项有效配置收据与五份
   800 树模型，并在父目录之外原子写证明。
3. PBC `--validate-inputs` 逐项比较证明、实际文件、父状态、父评价结果及父评价清单。
4. 工作进程再次执行相同输入门，随后才进入 PBC 主流程。
5. PBC 主流程仍先处理 LSPR23，再处理无标签 LSPR24 特征并原子落盘选择封印，之后才首次读取
   `y24.npy`。

## 验证结果

最终有效检查均在指定工作树本地执行，没有运行 `--validate-inputs`、人工单元测试、集成测试、
冒烟实验或真实实验，也没有访问远端。

| 检查 | 退出码 | 结果 |
| --- | ---: | --- |
| 两个 Python 文件 `py_compile` | 0 | 通过 |
| 两个 Python 模块导入 | 0 | 通过 |
| 恢复证明生成器 `--help` | 0 | 通过 |
| PBC `--help` | 0 | 通过 |
| PBC `--validate-config` | 0 | 输出 `PBC_CONFIG_VALID` |
| PBC 配置 `json.tool` 解析 | 0 | 通过 |
| 启动器 `bash -n` | 0 | 通过 |
| 两个 Python 文件 Ruff | 0 | 输出 `All checks passed!` |

首次使用工作树空虚拟环境执行模块导入、PBC 帮助和配置校验时退出码为 1，原因是该空环境没有
`numpy`；首次 Ruff 退出码为 2，原因是空环境没有 `ruff`。随后通过
`UV_PROJECT_ENVIRONMENT=/Users/bilibili/personal/note/thesis/experiments/llm_probe/.venv`
复用项目既有虚拟环境重跑，以上四项均退出 0。未安装、同步或修改任何依赖。

本次只复用仓库中已经运行的同构 XGBoost 接口 `Booster.load_model` 与
`Booster.num_boosted_rounds`，未引入或猜测新的第三方接口，因此未触发第三方接口文档查询门。

## 生产文件 SHA-256

- `fc7d27b62bca4c7e78ad82b4f67c2a4c0172e0215634d71ff95482123acf5d03`：`build_ch3_xgb_parent_recovery_proof.py`
- `5e05f93d6a05852303e85776cbc355ac03f52b9497a1d45e80c846d510804082`：`ch4_xgb_pbc_q0.py`
- `8ba50aef40fb6a0b8ff67590520b7e4409ae06c47adf0cf070ef674ec8e68052`：`ch4-xgb-pbc-q0-seed42-v1.json`
- `16131f52d6957a684c9d685982aea487ea0bde62b38cfd0f79c50da3d0acae9f`：`run_ch4_xgb_pbc_q0_seed42_v1.sh`

## 远端启动命令

同步并核验四个生产文件后，在 B76 项目根执行：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
bash scripts/remote_launchers/run_ch4_xgb_pbc_q0_seed42_v1.sh
```

本实施代理没有执行该命令。真实恢复证明内容和 PBC 结果仍须由 B76 冻结数据运行验证。
