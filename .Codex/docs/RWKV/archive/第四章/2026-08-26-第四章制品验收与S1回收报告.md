# 第四章制品验收与 S1 回收报告

- **日期**：2026-08-26
- **执行代理**：ch4_artifact_acceptance_sonnet
- **性质**：只读服务器核验 + 本地写副本，未修改任何服务器文件
- **服务器项目根（下称 `$P`）**：`/root/autodl-tmp/thesis/experiments/llm_probe`

## 一、任务①：Tong 最终单模型 rerun2 验收

### 1. 运行身份与状态

- 运行根：`$P/runs/candidates/ch4-xgb-path-max-tong-final-model-source-seed42-v1-rerun2`
- `status.json`：`run_id=ch4-xgb-path-max-tong-final-model-source-seed42-v1-rerun2`、`source_year=LSPR23`、`seed=42`、`target_year_arrays_read=0`、`state=finished`、`stage=complete`、`detail=success`、`exit_code=0`。核验通过，与恢复卡先前记录的"结果待验收"状态一致，本报告是首次填补该验收缺口。
- `manifest.json`：`complete` 隐含（含 7 个文件哈希清单）、`script_sha256=3df448ddb8e316440e42a58fd2bb035f656d3e5a475144ba9e6dccfde2ecc376`、`config_sha256=178ecd51b619e05898e347e3a0d6cb4b01eafd946b1bd3cfc94eb9d4e007d12b`、`training_runs_started=0`、`models_saved=0`、`models_reused=1`、`resume_source_run_id=ch4-xgb-path-max-tong-final-model-source-seed42-v1-rerun1`。

### 2. 模型溯源（关键发现：rerun2 未重新训练，复用 rerun1 的模型）

同名 `v1` 与 `rerun1` 两次运行均以 `failed/final_single_model_source/python_failed/exit_code=1` 收尾。核验 `rerun1/run.log` 尾部，失败发生在评价阶段末尾：

```
File "$P/tools/ch4_xgb_path_max_tong_final_model_source_seed42.py", line 1099, in main
    primary_pair = comparisons['M1_path_max_tong_minus_B0_first_empirical']
KeyError: 'M1_path_max_tong_minus_B0_first_empirical'
```

即模型已训练完成、六臂评价指标已算出，仅在脚本最后取用 `paired_comparisons` 字典键名时崩溃（工程 bug，非数据问题）。核验：

- `rerun1/model_semantic168_final_source_seed42.json` 与 `rerun2/model_semantic168_final_source_seed42.json` 的 `sha256sum` **完全一致**：`f1397ae76fff46039e8ad9b32acba90b8e00edfefeb2a5369ef6d35b09741269`。
- 当前 `$P/tools/ch4_xgb_path_max_tong_final_model_source_seed42.py` 的 `sha256sum` 为 `3df448ddb8e316440e42a58fd2bb035f656d3e5a475144ba9e6dccfde2ecc376`，与 rerun2 `manifest.json` 记录的 `script_sha256` 一致，说明 rerun2 使用的是已修复上述 `KeyError` 之后的脚本版本，完整跑完评价并封印结果。
- `model_binding`：`model_sha256=f1397ae7...`、`binding_valid=true`，模型与阈值收据强绑定门核验通过。

结论：rerun2 是对 rerun1 已训练模型的**纯重新评价**（`models_reused=1`、`training_runs_started=0`），未重新训练，模型字节级一致，且使用了修复后的脚本完整跑完流程，工程溯源无异常。

### 3. 六臂实体级指标（`source_six_arm_results.json`，评价集 `evaluation_fold2_half0`：负实体 25,074、正实体 40）

| 臂 | 实体 FPR | 检出率 DR | FP | TP/正实体 | Tong 证书 `certificate_claimed` |
| --- | ---: | ---: | ---: | --- | :---: |
| B0_first_empirical | 0.039881949 | 0.975 | 1000 | 39/40 | （无证书字段，纯经验基线） |
| B1_all_fixed_empirical | 0.048815506 | 0.975 | 1224 | 39/40 | （无证书字段） |
| B2_first_tong | 0.038087262 | 0.975 | 955 | 39/40 | `false`（设计为对照方向，不主张证书） |
| B3_path_max_empirical | 0.039881949 | 0.975 | 1000 | 39/40 | （无证书字段，与 B0 数值相同） |
| B4_summable_exposure_budget | 0.020818378 | 0.975 | 522 | 39/40 | （无证书字段） |
| M1_path_max_tong | 0.037927734 | 0.975 | 951 | 39/40 | `true` |

六臂中仅 B2、M1 两个 `_tong` 后缀方向持有 Tong 阈值校准对象；B2 是对照方向，按设计不主张证书（`certificate_claimed=false` 不是缺陷）；M1 是候选机制，`certificate_claimed=true` 且 `mechanical_verdict.m1_certificate_valid=true`（Tong 有限样本证书本身有效）。

### 4. 机械裁决（运行自封印的结论）

`mechanical_verdict`：

```json
{
  "qualified": false,
  "m1_certificate_valid": true,
  "verdict": "MECHANISM1_FINAL_SINGLE_MODEL_SOURCE_SEED42_NOT_SUPPORTED",
  "rule": "M1_certificate_valid AND FPR(M1)<=0.04 AND delta_DR(M1-B0)>0 AND paired_95pct_lower>0",
  "claim_boundary": "只裁决种子42的LSPR23最终单模型源年支持，不外推训练随机性或跨年度"
}
```

即：Tong 证书本身成立、`FPR(M1)=0.037928≤0.04` 满足，但 `delta_DR(M1-B0)>0` 与 `paired_95pct_lower>0` 两个条件不满足——因为 M1 与 B0 在这个 40 个正实体的评价集上都恰好检出 39 个，`paired_comparisons.M1_path_max_tong_minus_B0_first_empirical.detection_rate`：`candidate_only=0`、`reference_only=0`、`difference=0`、`paired_wald_95_interval=[0,0]`。FPR 方向确实改善且统计显著：同一比较对的 `entity_fpr`：`candidate_only=173`（仅 M1 命中的负实体误报）、`reference_only=222`（仅 B0 命中）、`difference=-0.001954`、`exact_mcnemar_two_sided_p=0.015623`。

**因此该运行自身机械裁决的整体结论是 `NOT_SUPPORTED`（`qualified=false`），不是"验收通过"。** 这不是本代理的科学论断，而是运行制品自己封印的 `mechanical_verdict` 字段。

### 5. 与交叉校准 Q0 数字的对比

Q0 数字来源（全文核验）：`.Codex/docs/RWKV/2026-08-19-机制一实体路径最大风险控制正式方法与实验方案.md` 第 252 行表格、`RWKV第四章恢复卡.md` 第 99 行：B0 FPR=`0.03999575`/DR=`0.97071130`/FP=`6017`/TP=`232`；M1 FPR=`0.03711089`/DR=`0.98326360`/FP=`5583`/TP=`235`。恢复卡明确 Q0 是"六个同模型、类别二分并交换校准与评价角色的方向汇总"，池化负实体约 150,441、正实体约 239（约为 rerun2 单一方向 25,074/40 的 6 倍，`150441/25074≈6.00`），且 Q0 本身已通过预注册机械门（"该事实仅支持进入正式实验"）。

| 比较项 | Q0（6 方向池化，population≈150,441/239） | rerun2 最终单模型（单一方向，population=25,074/40） | 方向 |
| --- | --- | --- | --- |
| B0 FPR | 0.03999575 | 0.039881949 | 数值接近，同方向基线 |
| M1 FPR | 0.03711089 | 0.037927734 | M1<B0，两者方向一致 |
| B0 DR | 0.97071130 | 0.975 | — |
| M1 DR | 0.98326360（严格 > B0） | 0.975（与 B0 打平，delta=0） | **不一致：Q0 为严格改善，rerun2 为打平** |
| FP 绝对数 | 6017→5583 | 1000→951 | 规模不同（population 相差约 6 倍），不可直接比对绝对值 |
| TP 绝对数 | 232→235 | 39→39 | 同上，且 rerun2 评价集仅 40 个正实体，颗粒度 2.5%/实体 |

**判定：数字方向部分一致、部分不一致，不是简单的"验收通过"。** FPR 方向与 Q0 一致（M1 优于 B0，且 rerun2 内部该差异统计显著 `p=0.0156`）；但 DR 方向未复现 Q0 的严格改善——rerun2 评价集仅 40 个正实体，1 个实体的检出差异即对应 2.5% 的 DR 变化，本次两臂检出数完全相同（39/40），配对差值精确为 0，不满足运行自身机械规则要求的 `delta_DR>0` 严格条件。这不是数据矛盾（没有出现方向相反的结果），而是运行自身封印的 `mechanical_verdict.qualified=false`——**按运行制品自身的裁决，本次验收结论为「验收不通过」**（NOT_SUPPORTED），精确差异已在上表列出，证据路径见 `source_six_arm_results.json` 的 `mechanical_verdict`、`metrics.*`、`paired_comparisons.M1_path_max_tong_minus_B0_first_empirical`。本报告不对"该结果是否说明机制一在最终单模型口径下无效"作进一步科学推断，该裁决权交还所属路线总控与用户。

### 6. 数据完整性与运行边界

`integrity`：`flow_coverage_count=16,353,511`、`duplicate_flow_coverage_count=0`、`within_segment_time_reversals=0`、`cross_segment_time_reversals=0`、`calibration_scored_flows=1,511,764`、`evaluation_scored_flows=2,280,310`、`target_year_arrays_read=0`、`per_flow_scores_persisted=0`、`per_entity_scores_persisted=0`、`entity_ids_persisted=0`（注意：逐流/逐实体分数未持久化，只有汇总指标制品，无法离线复算单实体轨迹）。

`evidence_boundary`：`single_seed_source_year_only=true`、`training_randomness_not_estimated=true`、`cross_year_claim=false`、`target_year_arrays_read=0`，与 `status.json` 一致，确认本次运行仅评估 LSPR23 源年、种子 42，未接触目标年数组。

角色实体计数（`roles`）：`training_fold0_plus_fold1` 100,454（正160/负100,294）；`calibration_fold2_half1_all` 25,112（正39/负25,073）；`calibration_fold2_half1_negative` 25,073（正0/负25,073，用于 Tong 阈值校准，population 与 `thresholds.M1_path_max_tong.population=25073` 一致）；`evaluation_fold2_half0` 25,114（正40/负25,074，即指标表使用的评价集）。四组角色内部数字互相自洽，无重复或矛盾。

### 7. 资源与墙钟（`resource_receipt.json`）

- 总墙钟：`wall_seconds=170.5099184513092`（约 170.51 秒）。
- 峰值主机常驻内存：`peak_host_rss_bytes=17,373,261,824`（约 16.18 GiB）；训练阶段内部采样另记 `host_peak_gib=16.998279571533203`（约 17.00 GiB），量级一致，测量口径不同（顶层收据 vs 阶段内采样）。
- GPU：`cuda_device="NVIDIA GeForce RTX 5090"`；顶层 `cuda_peak_allocated_bytes=0`、`cuda_peak_reserved_bytes=0`（该字段未对本次 GPU 直方图路径生效）；训练阶段内部采样 `gpu_peak_observed_gib=2.62109375`（约 2.62 GiB）。
- 模型体积：`model_bytes=3,645,701`，与模型文件实测字节数一致。
- XGBoost 训练超参数回显核验（`resource_receipt.json` 内嵌）：`updater=grow_gpu_hist`、`objective=binary:logistic`、`max_depth=8`、`max_bin=256`、`eta≈0.05`、`subsample≈0.8`、`colsample_bytree≈0.8`、`lambda=1.0`，均与预期值匹配。

## 二、任务②：S1 源年制品回收

### 1. 来源与运行根

依据 `.Codex/docs/RWKV/2026-08-26-全容量MLP独立实体头S1/S1实验计划.md` 第 120 行，运行根为：

- 相对路径：`runs/diagnostics/ch3-full-mlp-independent-entity-head-s1-seed42-v1/`
- 服务器绝对路径：`$P/runs/diagnostics/ch3-full-mlp-independent-entity-head-s1-seed42-v1`
- 本地目标路径：`thesis/experiments/llm_probe/runs/diagnostics/ch3-full-mlp-independent-entity-head-s1-seed42-v1/`（`runs/` 已被 Git 忽略，直接落盘）

远程 `ls -la` 核验该目录共 8 个文件：`aggregate-results.json`、`config.json`、`curves.npz`、`entity-heads.pt`、`manifest.json`、`resource-receipt.json`、`run.log`、`status.json`。

### 2. 回收执行

发现本地该目录在回收前已存在且文件名、字节数、修改时间（`Aug 26 15:29`）与远程完全一致（很可能是 `.Codex/docs/RWKV/2026-08-26-全容量MLP独立实体头S1/结果报告.md` 生成过程中已由其他代理拉取）。本任务仍按要求执行了一次 `tools/remote_exec/gpu_rsync_pull.exp`（未加 `--delete`，只读源不受影响），rsync 输出 `Transfer starting: 9 files`（含目录项）、`total size is 445876`，未发现差异需要覆盖传输。

### 3. 逐文件哈希核验（本地 `shasum -a 256` vs 远程 `sha256sum`）

| 文件 | 字节数 | 本地 SHA-256 | 远程 SHA-256 | 一致 |
| --- | ---: | --- | --- | :---: |
| `aggregate-results.json` | 64,016 | `709bce78adb562748ef2f6f946e213063a7035683423670069e37585cc099f4c` | 同左 | 是 |
| `config.json` | 2,245 | `3717688936fc3f1e5fcf3dc47e1d5ed147e1528574e442055ca257e5c77cac4d` | 同左 | 是 |
| `curves.npz` | 373,643 | `215621dee6a11f9c0ef448f8a077bb29497b55782084f2edad94efb860470ab4` | 同左 | 是 |
| `entity-heads.pt` | 3,837 | `dd52e1dab9e4cd98f96edbb338cce58ba67226938e00d13ffd2e66b7067c1a4c` | 同左 | 是 |
| `manifest.json` | 1,106 | `e32278cfccdf1c793b16755389833f5cdee17168f4456c4eb8c59d1c22a66e0c` | 同左 | 是 |
| `resource-receipt.json` | 230 | `aaed860180f9f2b02b68a3b01142f076a2e19fc2ad40b15953d231bc80c44151` | 同左 | 是 |
| `run.log` | 595 | `4fac673399265952330fdf85af3258305749c43995a0604c5640fe213a5e7314` | 同左 | 是 |
| `status.json` | 204 | `a926f0d51d2524fbab81c95c699a1463c4c2e2de49f56e420bae395ea29408b9` | 同左 | 是 |

**文件数：8；总字节数：445,876（与 rsync 报告的 `total size is 445876` 一致）；哈希核验：8/8 全部一致。** 前 6 个文件的哈希同时与 `.Codex/docs/RWKV/2026-08-26-全容量MLP独立实体头S1/结果报告.md` 第 53-58 行登记的清单哈希逐字节一致，三方（远程原件、本地回收副本、既有结果报告登记值）互相印证。

## 三、结论摘要（供路线总控参考，本报告不改写恢复卡）

1. Tong 最终单模型 rerun2：机械完成（`finished/complete/exit=0`），模型对 rerun1 字节级复用且脚本已修复此前的 `KeyError` bug；六臂指标齐全、完整性门（覆盖流、重复、时间逆序）全部为 0。
2. 运行自身封印的 `mechanical_verdict.qualified=false`，`verdict=MECHANISM1_FINAL_SINGLE_MODEL_SOURCE_SEED42_NOT_SUPPORTED`：FPR 方向与 Q0 一致且统计显著改善，但 DR 相对 B0 在该评价集（仅 40 正实体）打平（delta=0），未满足严格 `delta_DR>0` 条件，**验收结论为不通过**，需路线所有者裁决后续动作（是否需要更大评价集/多种子以获得可分辨的 DR 差异，或接受 FPR-only 的部分支持）。
3. S1 源年制品已完整回收到本地 `thesis/experiments/llm_probe/runs/diagnostics/ch3-full-mlp-independent-entity-head-s1-seed42-v1/`，8 个文件、445,876 字节，与远程逐文件 SHA-256 全部一致，回收完成。
