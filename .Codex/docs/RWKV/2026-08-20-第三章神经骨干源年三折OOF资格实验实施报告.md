# 第三章神经骨干源年三折 OOF 资格实验实施报告

## 结论

已完成首个 `resmlp2` 六训练单元资格门的本地实现。实现只新增独立工具、单候选冻结配置和独立启动器，不包含 `resmlp4`、`resmlp6` 或 `tabm4` 配置与启动逻辑。当前状态为**实现完成、效果实验待证**；未访问服务器，未读取任何目标年数组，未启动真实实验。

## 变更文件

1. `thesis/experiments/llm_probe/tools/ch3_neural_backbone_source_oof_gate.py`
   - 从第四章 E1 源年门派生独立命名空间，保留五数组、实体三折、C00/C11、检查点、恢复、资源、聚合与 SwanLab 聚合上报合同。
   - 新增一个预归一化两层残差块，宽度为 `139`，结构为 `LayerNorm -> Linear -> ReLU -> Dropout -> Linear -> Dropout -> residual add`，每块后对补零位置重新施加掩码。
   - `LayerNorm` 参数、全部偏置和共享 `p_log` 进入零权重衰减参数组；二维权重矩阵使用 `0.01` 权重衰减。
   - 训练收据新增优化步数、每步序列数、总编码序列数、有效流暴露数、训练时间、选择期推理时间和最终聚合推理时间；聚合结果记录运行时实际 PyTorch 版本。
   - 准备阶段严格核验现有多层感知机 E1 的六单元、三折、清单、全精度 pooled 值和五数组摘要；严格核验 XGBoost-C11 的选择收据、恢复证明、三折源年模型、折统计、五数组摘要和 `p=1.0` 曲线。
   - 聚合输出候选相对现有多层感知机和 XGBoost-C11 的 pooled 与逐折差值，并按预注册规则输出深度信号和三类互斥资格字段。
2. `thesis/experiments/llm_probe/configs/ch3-resmlp2-cpa-elp-source-oof-gate-seed42-v1.json`
   - 冻结 `seed=42`、实体三折、C00/C11、`20 x 1000` 步、批量 `64`、学习率 `0.002`、权重衰减 `0.01`、梯度裁剪 `1.0` 和辅助损失权重 `1.0`。
   - 输入白名单逐字为 `X23/y23/I23/M23/E23`，`target_year_arrays_read=0`。
   - 只允许 `resmlp2`，宽度 `139`，一个残差块，精确参数量 `89,796`。
   - XGBoost 父输入摘要收据采用配置中的显式路径；文件缺失或未列全五个源年数组时严格失败，不使用文档数字替代。
3. `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_resmlp2_cpa_elp_source_oof_gate_seed42_v1.sh`
   - 固定单候选 `prepare -> 六单元 -> aggregate -> publish-aggregate` 流程。
   - 数据读取前检查配置、候选参数、SwanLab 目的地、父基线文件、五数组文件和资源门。
   - 保留串行或双并行机械资源准入、持久状态、日志、恢复、管道双退出码和聚合上报有界重试。
4. `.Codex/docs/RWKV/2026-08-20-第三章神经骨干源年三折OOF资格实验实施报告.md`
   - 本报告。

## 参数量核验

冻结公式为：

```text
P(B,d) = (2 + 2B)d^2 + (86 + 4B)d + 2
P(1,139) = 89,796
```

代码同时用逐模块展开公式计算参数量，并在正式 `build_model` 后再次对 `model.parameters()` 求和。C00 与 C11 只改变是否启用 CPA/ELP 计算，不改变参数外壳，因此两格均必须为 `89,796`。相对现有多层感知机 `90,242` 少 `446` 个参数，差异为约 `-0.494%`，位于预注册 `±1%` 内。

## 验证情况

文档修改前执行：

```bash
git status --short -- .Codex/docs/RWKV/2026-08-20-第三章神经骨干源年三折OOF资格实验实施报告.md
```

输出为空，目标报告不存在未提交前置改动，当前 `HEAD` 构成修改前快照。

在 `thesis/experiments/llm_probe` 下执行以下检查：

```bash
source tools/env/activate.sh
UV_CACHE_DIR=/tmp/uv-cache PYTHONPYCACHEPREFIX=/tmp/codex-pycache uv run --no-sync python -m py_compile tools/ch3_neural_backbone_source_oof_gate.py
```

结果：通过，无输出。

```bash
UV_PROJECT_ENVIRONMENT=/Users/bilibili/personal/note/thesis/experiments/llm_probe/.venv UV_CACHE_DIR=/tmp/uv-cache PYTHONPYCACHEPREFIX=/tmp/codex-pycache uv run --no-sync python tools/ch3_neural_backbone_source_oof_gate.py --help
```

结果：通过；命令入口列出 `prepare/train-unit/aggregate/publish-aggregate`、`C00/C11`、三折、恢复、资源收据和 SwanLab 授权参数。

```bash
UV_PROJECT_ENVIRONMENT=/Users/bilibili/personal/note/thesis/experiments/llm_probe/.venv UV_CACHE_DIR=/tmp/uv-cache PYTHONPYCACHEPREFIX=/tmp/codex-pycache uv run --no-sync python tools/ch3_neural_backbone_source_oof_gate.py --config configs/ch3-resmlp2-cpa-elp-source-oof-gate-seed42-v1.json --validate-config
```

输出：`配置核验通过`。

```bash
UV_PROJECT_ENVIRONMENT=/Users/bilibili/personal/note/thesis/experiments/llm_probe/.venv UV_CACHE_DIR=/tmp/uv-cache PYTHONPYCACHEPREFIX=/tmp/codex-pycache uv run --no-sync python -c '<导入工具，计算 residual_parameter_count(83,139,1)，并与配置冻结值断言相等>'
```

输出：`PARAMETER_FORMULA_OK C00=89796 C11=89796 actual=89796`。

```bash
bash -n scripts/remote_launchers/run_ch3_resmlp2_cpa_elp_source_oof_gate_seed42_v1.sh
```

结果：通过，无输出。

```bash
rg 'X24|y24|I24|M24|E24|0[.]565078' tools/ch3_neural_backbone_source_oof_gate.py configs/ch3-resmlp2-cpa-elp-source-oof-gate-seed42-v1.json scripts/remote_launchers/run_ch3_resmlp2_cpa_elp_source_oof_gate_seed42_v1.sh --line-number
```

结果：零命中。

```bash
rg 'guarded_load\(config, "(X23|y23|I23|M23|E23)"\)|ALLOWED_ARRAYS = \("X23", "y23", "I23", "M23", "E23"\)|self[.]p_log = nn[.]Parameter|torch[.]cumsum\(hidden, 1\)|return \(values \+ residual\) \* mask[.]unsqueeze\(-1\)' tools/ch3_neural_backbone_source_oof_gate.py --line-number
```

结果：确认唯一数组白名单、五个显式加载分支、单个共享 `p_log`、仅沿时间轴前缀累计和残差恒等加法路径均存在。

未运行 `black` 或其他自动格式化器，原因是仓库规则明确禁止。未创建或运行单元测试、集成测试、独立冒烟实验或真实数据最小子集。

## 第三方接口核验

本实现只复用仓库中已经存在并经过真实运行的 PyTorch 同构接口：`nn.Linear`、`nn.LayerNorm`、`nn.ModuleList`、`nn.Dropout`、`torch.cumsum`、`torch.logsumexp`、`AdamW` 和梯度裁剪；没有引入签名不确定的新第三方接口，因此未触发 Context7 查询。

隔离工作树自带虚拟环境缺少 `numpy`；主项目现有本地虚拟环境可完成配置入口导入，但不含 `torch`。因此本机没有实例化真实 PyTorch 模型。正式运行时 `build_model` 会对实际模块参数重新求和，不等于 `89,796` 时在读取训练数据后的模型构建阶段立即停止；聚合制品会记录服务器实际 `torch.__version__`。计划目标环境为 PyTorch 2.8 系列，服务器实际版本仍须在后续授权运行前核验。

## 未变更边界

- 未修改 `ch4_e1_source_entity_oof_gate.py`。
- 未修改任何第三章既有工具、XGBoost 父工具或第四章机制一、机制二文件。
- 未新增 `resmlp4`、`resmlp6`、`tabm4` 配置或启动器。
- 未修改、生成或删除任何运行制品。
- 未访问服务器，未启动训练，未注册 SwanLab 运行。
- 未读取、哈希、枚举或推导任何目标年数组。

## 风险与后续门禁

1. 配置中的 XGBoost 父输入摘要收据路径是显式严格路径。实施阶段无法访问服务器确认该文件是否确实包含五个源年数组摘要；若其内容只记录代码文件或缺少任一数组，启动器会在数据读取前失败。主代理必须先回收或生成具有明确来源的同义源输入摘要收据，再允许真实运行，不能猜造摘要或跳过门禁。
2. 本机缺少 PyTorch，未完成真实张量前向与参数实例化核验。服务器授权后必须先执行正式入口内的依赖、配置和 `build_model` 参数量门，不得建立独立冒烟运行身份。
3. 未产生 LSPR23 指标。残差深度信号、相对 XGBoost 的差距和源年资格均为实验待证；不得据本实现报告启动 `resmlp4` 或写入论文效果结论。
