# 第四章 MLP 底座重构与长度分层 Tong 证书（机制 a）实施计划

日期：2026-08-26。状态：**活动**。执行者：主进程（用户指令「主进程进行 a 的实验」）。

## 一、用户决定登记（2026-08-26）

| 编号 | 决定 | 依据与边界 |
| --- | --- | --- |
| V1 | 第四章机制 2 采用**方案 a：长度分层 Tong 证书**；MalMoE 式门控仅作 a 被科学否决后的备用 | 反射累积已被 rerun2 有效科学否决（`REJECTED_SOURCE_Q0`，零新增 TP、长桶门 False）；源年检出天花板 +1 TP，机制 2 必须立在风险侧 |
| V2 | **第四章底座换为第三章胜出模型**：全量 MLP＋CPA＋ELP（O11，bf16，seed42） | 用户原话「第三章最终胜出的模型是全量 mlp+两机制」；论文连贯性要求第四章消费第三章胜出模型的分数。XGB semantic168 链全部结果降为历史过程证据 |
| V3 | 章级问题收束为「重复观测下的实体级预算告警」：机制 1＝池化路径预算控制（Tong 路径最大），机制 2＝分面条件预算控制（长度分层证书）；跨年度阈值失配降为应用背景与压力测试 | 用户 2026-08-26 方案，经反射累积否决证据修正机制 2 轴 |

## 二、底座合同

- 分数来源：`runs/diagnostics/ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1-bf16-v1/checkpoints/selected-O11.pt`（冻结，不重训、不微调）。
- 逐流分数在每次诊断运行内由冻结检查点前向计算（实测 BF16 约 7.6M 流/秒，LSPR23 全量 16,353,511 流前向为秒级），**不物化、不持久化逐流分数**，沿用 XGB 链既有模式。
- LSPR24 对一切选择、校准、证书、参数零参与；封印后仅整批描述性评价一次。
- 单种子 seed42（U4）。年度隔离、`training-identities.json`、`code_sha256` 三类事实记录保留。

## 三、机制 a 数值依据（魔法数字门禁台账）

| 数值 | 用途 | 依据 | 核验状态 |
| --- | --- | --- | --- |
| q=0.04 | 实体误报预算 | 既有冻结预算（六档 DR 网格成员，第三/四章沿用） | 已核验 |
| 长桶支持 n：101-1000 桶良性实体 2,977、1001+ 桶 972；全部实体 150,680 | 分层证书校准样本量 | `runs/diagnostics/ch4-xgb-path-risk-length-bucket-lspr23-diagnostic-seed42-v1/aggregate-results.json` `conditional_risk_summary.*.long_bucket_support`（XGB 底座实测；MLP 底座桶结构以 D2 再确认为准，实体-桶归属与模型无关故预期同值，标待验证） | 部分待验证 |
| Clopper-Pearson 可行性（本机精确二项计算，2026-08-26）：n=3949/δ'=0.05⁄12 → 可证最大 FP 125、经验率 3.17%、证书税 0.83pt；n=2977 → 3.06%/0.94pt；n=972 → 2.37%/1.63pt；n=486（最细分裂）→ 1.65%/2.35pt | 证明分层证书不因样本饥饿不可行；量化「证书税」 | 主进程精确二项 CP 上界计算，脚本内联于会话记录；δ' 枚举 0.05/6、0.05/12、0.05/30 全部可行 | 已核验（数学） |
| δ 总预算 0.05，按「方向×层」均分 | 有限样本证书族错误率 | 沿用 Tong 既有合同的 δ=0.05 与均分惯例；具体分裂数待 D2 后冻结 | 待冻结 |
| 长度桶界 1-2/3-10/11-100/101-1000/1001+ | 分层定义 | 沿用长度桶诊断既有冻结桶界，不因结果调整 | 已核验 |

## 四、阶段与门（按序，前一阶段无信号则否决或缩小，不得跳步）

- **D0 折外评分模型物化（前置硬门，2026-08-26 补）**：O11 最终模型在源年训练区上是
  **样本内**的，直接用它给 LSPR23 打分属于样本关联泄漏（第四章 XGB 链当初用 3 份折外
  模型规避的正是这一点，见恢复卡 §5.1「三角色分数来自不同模型即无效」的教训与 E1 合同）。
  故源年一切诊断、校准与证书使用折外分数：复用 E1 冻结的 150,680 实体确定性三折
  （折收据在 `runs/diagnostics/ch4-e1-source-entity-oof-gate-seed42-v1`），按 bf16 运行
  config 冻结的 O11 超参数与已选 epoch 数训练 3 份折外模型（不重新选择任何超参数或
  epoch），BF16 profile；单格实测约 6 分钟，3 折约 18 分钟 GPU。每个实体的分数只来自
  未见过它的模型。LSPR24 描述性评价仍用单一最终 O11 模型（跨年天然样本外）。
- **D1 曝光病灶再确认（MLP 底座）**：在 O11 逐流分数上复刻曝光病灶诊断。病灶门：`FPR(all)>0.04 且 N_late_positive>0`。不过门→第四章问题陈述须重做，停止后续阶段并报告用户。
- **D2 长度桶条件风险再确认（MLP 底座）**：复刻长度桶诊断。病灶门：存在长桶 FPR>0.04。同时输出 MLP 底座的每桶良性/正实体支持数，替换台账中的待验证项。
- **M1' Tong 池化路径最大 Q0（MLP 底座）**：门＝全部方向证书成立 且 FPR≤0.04 且 DR>B0'。
  **评价人群预注册（2026-08-26 教训）**：DR 比较用全量源年折外人群（约 150,680 实体、239 正实体），
  禁止另切小持出集——XGB 链最终单模型验收即因评价集仅 40 个正实体（1 实体=2.5 点分辨率）
  而测不出 +3 TP 量级的差异，门失败判为 `NOT_SUPPORTED`（见
  `2026-08-26-第四章制品验收与S1回收报告.md`）；该失败属评价设计功效不足，MLP 链不得复制。
- **S_a 分层证书 2×2**：C00=固定阈值；C01=仅分层校准；C10=仅池化 Tong；C11=分层 Tong＋联合告警事件重校准。通过门（预注册，写死）：①池化 FPR≤0.04；②全部桶 FPR≤桶级预算（默认 0.04）；③DR(C11)≥DR(C10)；④全部证书成立；⑤共同正实体配对首告警位置中位不恶化。任一失败→机制 a 否决或缩小（如仅合并长桶一层），MalMoE 备用路线才进入评估。
- **封印与评价**：LSPR23 完成全部选择与封印后，LSPR24 对最终四格各做恰好一次整批描述性评价。

## 四点五、文献裁决对机制 a 的三条约束（2026-08-27，六篇 E3 全文入库）

1. **命名边界**：组条件/分层有限样本控制是标准做法（Vovk 2012 条件 ICP，Prop 3），
   第四章不得声称「分层证书」本身是创新；差量只能写「实体级·路径最大统计量·观测长度分桶·
   逐桶精确二项（CP）证书」的组合（路径极值进组条件证书本次检索无近邻，标待验证）。
   Bates RCPS Remark 4 支持二元损失下用精确二项界。
2. **δ 均分写成局限**：Barber & Ramdas p-filter Thm 4 证明多层预算可经联合阈值搜索免
   Bonferroni 均分；本课题沿用均分（工程简单、更保守），在正文作中性局限表述，不作贡献。
3. **不可能性定理边界**：Barber et al. 2021 Prop 1（对象条件不可达）不阻断本机制——
   式 (7) 明确「预先固定的有限划分内覆盖」可达，长度桶远在 `VC≥2n+2` 失效阈值可行侧；
   引用 Prop 1 必须同引式 (7)。
4. **S_a 冻结前设计核验——单调性已裁决（2026-08-27 数学分析，实现后需重放核验）**：
   - **反例**：若把告警按「首次穿越时刻所在桶」归属，抬高 τ_b 可使实体在桶 b 不告警而在
     后续桶 b' 告警，桶 b' 计数上升——桶级风险对阈值向量非逐坐标单调，Bates Thm 1 前提破坏。
   - **恢复选项 A（默认冻结候选）**：分组键取实体固定属性（最终长度桶），事件取
     「在整条路径上曾按当前桶阈值表告警」；该事件指示对每个 τ_b 非增 → 各组计数逐坐标单调。
     决策规则保持在线因果（按决策时刻已观测长度查表），分组仅作校准记账，无因果问题。
   - **恢复选项 B**：一维公共分位参数 λ 驱动全部桶阈值（τ_b = 桶 b 良性校准分数的
     (1−λ) 分位），回到 Thm 1 的一维嵌套族；比 A 少自由度、证书更简单，但桶间预算不可独立调。
   - S_a 合同冻结时二选一并登记；实现后用校准数据重放验证单调性断言（升 τ_b 各组计数确实非增）。

## 四点六、本机筛选支线（2026-08-27，用户指令「拉依赖到本机验证机制」）

- 服务器 cgroup 内存被压到 2GiB，训练全灭（D0/TabM32/FT 均 SIGKILL）；CUDA 分配实测可用，
  阻塞点是内存配额，解锁动作在用户控制台。
- 本机（M1/16GiB/60GiB 空闲盘）按 `screening_only` 规则跑筛选级 D0→D1/D2：源年六数组
  （5.91GB）已在拉取；venv（torch 2.13.0 MPS）已建；X23 留 CPU 零拷贝、逐批上 MPS 控内存。
- 规则冲突披露：本机大规模放置默认规则被用户当轮指令覆盖；筛选运行自选精度（fp32）与设备
  合法，结果不进正文；正式 D0 在服务器资源恢复后按冻结 BF16 合同重跑（约 25 分钟）。
- 运行身份：`ch4-mlp-o11-oof-fold-models-local-screen-v1`，筛选训练预算 10 epoch/折
  （对齐父封印 selected_epoch=10，非 20），折分配逻辑与正式 D0 同构并记录折哈希供事后对账。

## 四点七、朱论文第四章对位表（2026-08-27 读原件印刷 p.52-62，仅取组织模式）

| 对位项 | 朱第四章（DF-ACR） | 本课题第四章（分层 Tong） |
| --- | --- | --- |
| 研究问题拆分 | 效率瓶颈拆成空间（哪些样本）×时间（何时重算）两个正交维度 | 预算告警拆成池化（路径层面预算）×分面（长度层面预算）两个正交维度 |
| 机制联合方式 | **级联决策路径**：第一级 DF 门控定「谁」、第二级 ACR 缓存定「何时」，每样本走一条路径（图 4-1，p.52） | 在线告警循环每次曝光走一条路径：更新路径最大统计量（机制1 定统计量）→ 查当前长度层取证书阈值（机制2 定阈值族）→ 比较告警 |
| 单一算法框 | 算法 4.1（表 4-1，p.56-57）：四步骤循环，两机制各占一步，接口是样本划分与缓存状态 | 算法框：实体流式循环四步骤——统计量更新／层定位／证书比较／联合事件登记；接口是层成员与阈值表 |
| 理论=单机制极限＋联合公式 | 式 4.21-4.23 联合成本，式 4.24（仅缓存）/4.25（仅过滤）/4.26（联合 4.4×）三个极限情况 | 逐层 CP 上界 → 池化 FPR 合成上界；三个极限＝C10（仅池化）/C01（仅分层）/C11（联合），理论消融与 2×2 实验一一对应 |
| 收敛/有效性 | 两类近似误差分别有界（空间采样偏差＋时间梯度陈旧性，式 4.27-4.28） | 两类代价分别有界（层内样本减少→证书变宽已实算证书税；在线层迁移的时序效应→待 S_a 合同冻结口径） |
| 单骨干选型论证 | p.61 明确只聚焦 ET-BERT，依据＝计算瓶颈紧迫性＋技术适配性 | 只聚焦第三章胜出 MLP，依据＝论文连贯性（消费第三章胜出模型分数）＋病灶在该底座确认；写法照此体例 |
| 数据集衔接 | p.61「继续沿用第三章基准数据集，预处理严格一致」 | 同构：沿用第三章冻结数据与实体口径，LSPR24 封印评价 |
| 实验组织 | 4.6 八小节：设置/主效率/资源/消融/敏感性/类别平衡/扩展/工程综合 | 对应：设置/主预算-检出/分面风险/2×2 消融/δ 与层粒度敏感性/长度层平衡/在线扩展性/工程收益 |
| 超参表体例 | 表 4-2 软硬环境＋表 4-3 各方法关键参数 | 照此体例列冻结数值台账 |
| 不可迁移边界 | 朱的机制（重要性过滤/扰动缓存）、公式、加速比数字一概不借 | 只取组织模式；本课题统计量、证书、门槛全部自主构造并有自家制品支撑 |

**对 S_a 合同的新增冻结项（源自对位）**：在线层定位必须用**决策时刻的已观测长度**（因果），
不得用最终长度（事后）；层迁移语义（实体随曝光跨层时证书归属）在 S_a 合同中显式冻结——
这是朱的级联结构映射到在线告警时暴露的本课题特有设计点。

## 五、先验证据与待验证边界

- 支持病灶迁移的先验：第四章恢复卡 §3.1 感知机分桶 C11−C00 在 101-1000/1001+ 桶为 −0.322915/−0.302212（长实体受损在感知机族已观测）；§3.1 良性长实体逐流假阳尖峰 0.9999 出现在感知机。以上是 AP 口径的旧诊断，**不能代替 D1/D2 的 FPR 口径再确认**。
- 反射累积否决、DTEP、PBC、再校准、直接实体 AP 优化均保持关闭，只留过程文档（用户既定）。
- 文献侧：分层/组条件风险控制的机制来源与新颖性差量由定向文献代理并行核验，不阻塞 D1/D2。

## 六、制品与恢复

- 运行根：`runs/diagnostics/ch4-mlp-o11-exposure-lesion-source-diagnostic-seed42-v1`、`ch4-mlp-o11-path-risk-length-bucket-lspr23-diagnostic-seed42-v1`、`runs/candidates/ch4-mlp-o11-path-max-tong-source-q0-seed42-v1`、`ch4-mlp-o11-stratified-tong-2x2-source-q0-seed42-v1`（依阶段创建）。
- 每运行：唯一身份、status.json、原始日志、聚合制品、资源收据、`target_year_arrays_read=0` 断言；中断由同身份原子恢复。
