# LSPR24 RWKV 原子机制快速消融结果

- **日期**：2026-08-08
- **性质**：冻结开发区的种子 42 快速筛选，不是正式论文结果，不访问最终测试区。
- **远端结果根**：`runs/candidates/lspr24-rwkv-screen-seed42-v1`
- **远端启动根**：`runs/launchers/lspr24-rwkv-screen-seed42-v1`
- **运行状态**：控制器 `finished`；四个变体退出码均为 `0`；SwanLab 均为 `finished`。

## 1. 共同设置

- 训练轮数：5。
- 开发训练样本：100,000。
- 开发验证样本：20,000。
- 输入字段：408。
- 批量大小：512。
- 种子：42。

四个变体共享上述预算。当前结果仅能比较本次原子机制的方向，不能据此作多种子、置信区间、效率或最终泛化主张。

## 2. 指标

| 内部键 | 展示名称 | PR-AUC | 宏平均 F1 |
| --- | --- | ---: | ---: |
| B1 | 基础因果 Transformer | 0.9989051999805907 | 0.9913132575388889 |
| B1F | 字段注意力 Transformer | 0.9997750317365747 | 0.9948583299922971 |
| A2 | RWKV 状态条件字段 Transformer 原子代理 | 0.9997778729511367 | 0.9960671008288451 |
| B3 | RWKV 时间混合模型 | 0.9997885303651409 | 0.9961194517087317 |

## 3. 裁决

1. 字段注意力 Transformer 相对基础因果 Transformer 有正向信号，但两者 PR-AUC 已接近饱和，不能由本任务估计章节级实际提升空间。
2. A2 相对 B1F 的 PR-AUC 增量为 `+0.000002841214562`，低于快速筛选门槛 `0.005`。因此，**“当前实现的 RWKV 状态条件字段调制”获得弱不利证据**。
3. 该证据只否定当前原子代理在本预算和本开发验证集上存在足够独立增益，**不证伪方案 B 的完整双机制方法**，更不构成最终第三章创新点。
4. A2 的实现为探索性代理：上一时刻状态以有界加性调制影响字段标记，不等同于完整 RWKV-7 状态更新，也没有第二机制、章节级理论、正式比较或完整消融支撑。不得以“RWKV 机制移植到 Transformer”表述为论文创新。
5. 远端 A2 既有展示名中的“主候选”属于历史标签，已由本次裁决废止。后续文档统一称为“RWKV 状态条件字段 Transformer 原子代理”。

## 4. 资源与后续门禁

- 本轮结束后服务器没有残留训练进程，数据盘使用率约 71%。
- 下一步不是直接冻结方案 B，而是先基于本轮结果重构具有章节级双机制、独立理论性质与组合消融的候选，再以共同强基线和多种子开发实验判断其是否值得进入正式矩阵。

