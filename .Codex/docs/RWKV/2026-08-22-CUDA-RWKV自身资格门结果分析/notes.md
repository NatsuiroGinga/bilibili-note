# 分析笔记：CUDA-RWKV 自身资格门

## 输入范围

- 运行根：`thesis/experiments/llm_probe/runs/diagnostics/ch3-rwkv-cuda-self-gate-v1/`
- 证据类型：完成收据、尝试结果、环境收据、扩展清单、启动日志及其退出码文件、本地回收的扩展二进制。

## 证据纪律

- 本任务不是性能评估、训练结果分析或论文结果报告。
- 没有种子级性能数据、真实数据读取、训练曲线、评测输出或基线比较，因此不产生统计推断、效果量或图件。
- 所有结论必须区分“工程自身资格门通过”与“科学效果实验待证”。

## 待核验项目

- 完成收据的模式、门标识、通过状态、输入哈希与执行身份。
- 尝试结果与完成收据逐字节一致。
- 四类资格检查、六个核输入梯度、三个模块更新、环境、扩展、日志退出码与资源记录。
- 无数据读取、无训练、无 SwanLab 的边界证据，以及首次 `exit 69` 失败和提交 `761f2c6` 的单变量修复边界。

## 已核验事实

- `completion-receipt.json` 与 `attempts/attempt-1/result.json` 的原始字节及规范 JSON 均完全一致。
- 完成收据为 `ch3-rwkv-cuda-self-gate-result-v1`，`gate_id=ch3-rwkv-cuda-self-gate-v1`，`passed=true`，科学状态为“实验待证”。
- 四类检查、六个 BF16 核输入梯度和三个模块更新均通过；本地 `.so` 为 `267680` 字节，SHA-256 与扩展清单一致。
- 两个日志退出码均为 `0`。运行总历时约 `18.784454` 秒；吞吐记录只适用于自身资格门，不能外推训练性能。
- 运行根未回收启动器状态目录；报告不把恢复消息或先前声明当作可独立复核的 `launcher status` 证据。
- 首次 `exit=69` 发生于前置命令检查，原因是非交互 `PATH` 缺少已存在的 CUDA 工具目录。提交 `761f2c6` 只冻结并传播 `CUDA_HOME=/usr/local/cuda`，并增加路径一致性断言；未改变 CUDA 核、形状、输入或门阈值。
