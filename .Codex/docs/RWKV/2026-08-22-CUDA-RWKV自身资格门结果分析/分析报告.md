# CUDA-RWKV 自身资格门结果分析

- **日期**：2026-08-22
- **代理映射**：`rwkv_cuda_self_gate_results_terra_medium` → `gpt-5.6-terra` → `effort=medium`
- **分析模式**：结果分析的证据优先只读审计。未产生统计推断、图件或论文结果文字。
- **输入运行根**：`thesis/experiments/llm_probe/runs/diagnostics/ch3-rwkv-cuda-self-gate-v1/`

## 结论

**CUDA 小型容量实现自身资格门通过，允许进入合法 Raw83 真实训练前置链。**

该结论仅覆盖固定构造输入下的 CUDA 编译、重复前反向、单输入微扰、外层掩码、一次参数更新和资源记录。它**不**证明模型有效、训练已完成或论文得到支持；科学状态仍是“实验待证”。

## 证据与机械核验

| 核验项 | 本地证据 | 结果 |
| --- | --- | --- |
| 收据合同 | `completion-receipt.json` 的 `schema_version=ch3-rwkv-cuda-self-gate-result-v1`、匹配 `gate_id`、`passed=true`，并含输入、启动器输入和执行身份哈希 | 通过 |
| 尝试结果一致性 | `attempts/attempt-1/result.json` 与完成收据原始字节一致，规范 JSON 也一致 | 通过 |
| 四类资格检查 | `determinism_and_gradients`、`single_input_perturbations`、`outer_model_masking`、`single_optimizer_update` 均为 `passed=true` | 通过 |
| 六梯度 | `r,w,k,v,a,b` 均为 `torch.bfloat16`、`cuda:0`、有限且非零；重复梯度逐位一致 | 通过 |
| 模块更新 | 输入投影、CUDA 块、输出头均有有限参数更新；参数梯度均有限 | 通过 |
| 环境 | PyTorch `2.13.0+cu130`、CUDA `13.0`、`sm_120`、`CUDA_HOME=/usr/local/cuda`、`nvcc=/usr/local/cuda/bin/nvcc`、BF16 可用 | 通过 |
| 扩展二进制 | `.so` 名称 `rwkv7_k0_clampw_h16_c112_t128_v1`，本地 `267680` 字节，SHA-256 为 `ebabb49f18a4847910746f11e9e6038102378c960312c834a7da6eb53fc8d9de`，与完成收据和 `extension-binaries.json` 一致 | 通过 |
| 供应商清单 | `rwkv7-k0-fused-vendor-manifest-v1`，上游提交 `952102498e9ed367ea0a59ee64106916d474d30f`，许可证 Apache-2.0 | 通过 |
| 启动日志 | `command-exit-code.txt=0`，`tee-exit-code.txt=0` | 通过 |

完整字段、命令结果和断言见 [机械核验](mechanical-validation.json)。

## 运行资源记录

- 收据总历时：`18.784453630447388` 秒。
- 吞吐测量：预热 `3` 次、测量 `20` 次、测量段 `0.0010799206793308258` 秒、`37039.7574` 序列/秒、`4741088.9503` 词元/秒。
- CUDA 内存：最大已分配 `69255680` 字节，最大保留 `71303168` 字节。

这些值的收据范围是“自身资格门资源记录”。固定构造输入、单一小型容量和一次优化更新不代表 Raw83 训练的吞吐、显存、收敛或性能。

## 范围与统计限制

- 完成收据中的 `data_access`、`training`、`swanlab` 均为 `null`，且运行根没有数据集、训练检查点、训练曲线、评测结果或基线结果。
- 因而不存在可定义的性能比较单位、种子数、主指标、置信区间或效果量；任何统计检验、优劣比较和论文结果表述都被阻断。
- 本地回收目录只包含运行根，没有启动器状态目录。因此 `launcher status` 未在本地独立复核；本报告不以恢复消息或先前陈述替代该缺失制品。

## 历史失败与修复边界

首次真实前置检查以 `exit=69` 失败，发生在 `validate_commands` 的 `command -v nvcc`，早于运行根身份登记。证据显示 CUDA 工具链实际存在，但非交互 `PATH` 未包含 `/usr/local/cuda/bin`。

提交 `761f2c641c84cbf803be33e42e4f4999bbc1568e` 只增加了冻结 `CUDA_HOME=/usr/local/cuda`、启动器环境传播与 `nvcc` 解析路径双重断言；CUDA 核、形状、构造输入、门阈值和 `gate_id` 未改变。因此本次通过的工程解释限定为该环境传播故障已排除，不能把它改写为模型或训练效果改善。

## 允许与禁止表述

| 类别 | 表述 |
| --- | --- |
| 允许 | CUDA 小型容量实现自身资格门通过，允许进入合法 Raw83 真实训练前置链。 |
| 禁止 | CUDA-RWKV 模型有效。 |
| 禁止 | CUDA-RWKV 已完成训练。 |
| 禁止 | CUDA-RWKV 已获得论文支持。 |
| 禁止 | 资格门吞吐或显存等同于真实训练资源表现。 |

## 后续门槛

进入真实训练前，仍须满足冻结的合法 Raw83 数据与训练合同，并以真实源年训练、封印选择、四格消融、完整指标和资源制品进行独立裁决。该资格门不替代任何数据、训练、评价或论文证据门。
