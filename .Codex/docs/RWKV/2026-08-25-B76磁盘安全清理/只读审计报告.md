# B76 磁盘安全清理只读审计报告

## 结论

本阶段没有可授权删除的 B76 路径。四个指定历史目录在 B76 当前项目根均不存在；本机保留目录也未证明是旧远端大制品的完整回收副本。活动 CUDA-RWKV 训练正在运行，Raw83、Dijk 缓存、虚拟环境、原始数据和指定 GRANDE 目录均未触碰。

## 指定历史目录

| 历史目录相对路径 | B76 当前状态 | 本机位置 | 普通文件数 | 逻辑字节 | 逐文件 SHA-256 聚合 | 检查点可读性 | 裁定 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| `runs/diagnostics/ch3-baselines-full` | 不存在 | 主工作区同相对路径 | 5 | 80,941,179 | `fecf89048fb2f8eade99e016706989462a01cd7b2e9e91d0d13ff1eed9dafa1c` | 未发现 | 不可删；本机字节远低于旧远端记录 `2,730,007,405` |
| `runs/diagnostics/ch3-baselines-param-matched` | 不存在 | 主工作区同相对路径 | 7 | 242,835,109 | `5034d68e67b5938fc15fd76644bbce3d27d4fbc356702c15a9e9a5ede897d22a` | 未发现 | 不可删；缺少旧远端完整目录的逐项比对 |
| `runs/diagnostics/ch3-final-weights` | 不存在 | 主工作区同相对路径 | 19 | 1,538,010 | `6c3acddf64c6a3dce9e64a96f2ce15f0db7eecd928bcd5aa9c164255f1556ffb` | 未发现；`manifest.json` 可读 | 不可删；缺少旧远端完整目录的逐项比对 |
| `runs/candidates/crossyear-rare-mass-protected-asymmetric-pot-q0-seed42-v1-rerun2` | 不存在 | 当前工作树同相对路径 | 42 | 1,123,975,312 | `e5b39c226e7258242a318c972c7c37593a1a6f10546731ee7756a6d8052b9676` | 未发现；`status.json` 可读 | 不可删；远端对象缺失，无法逐项比对 |

聚合算法为：按相对路径字节序排序普通文件，逐项计算 SHA-256，再对文本清单计算一次 SHA-256。它能识别同一目录内容，但不能代替与缺失远端目录的逐文件等值证明。

## 本轮目录盘点

| 相对路径 | 字节 | 状态 | 裁定 |
| --- | ---: | --- | --- |
| `runs/diagnostics/ch3-cuda-rwkv-dual-extension-build-b76-v1` | 1,440,674 | 新建构建诊断 | 保留；工程证据未完成生命周期裁决 |
| `runs/diagnostics/ch3-cuda-rwkv-raw83-qualification-seed42-v1` | 27,448,881 | 活动 CUDA-RWKV 训练 | 严禁触碰 |
| `runs/diagnostics/ch3-cuda-rwkv-raw83-qualification-seed42-v1-microbatch4-engineering-archive` | 12,829,892 | 新建微批四工程归档 | 保留；工程归档尚无完整回收或删除授权 |
| `runs/diagnostics/ch3-grande-lspr24-zero-train-descriptive-eval-bf16-v1` | 770,411 | 用户指定保护范围 | 严禁触碰 |

## 风险与继续条件

1. 2026-08-22 审计中的四个可释放历史目录总计约 `6.10 GB`，但本次远端当前路径均已缺失，不能把旧审计数值当作当前可释放空间。
2. 本机前三个基线目录没有检查点，且字节量远小于旧远端目录，不能声明“用户已拉回完整副本”。
3. 本轮两个较小工程目录合计约 `14.27 MB`，即使未来获准清理也不足以解决历史 `25 GiB` 可用空间门。
4. 只有主进程对某个仍存在的精确远端路径再次下达继续指令，并满足远端普通文件清单、逐项大小和 SHA-256 等值、本机检查点可读性以及删除前最终活动状态复查，才可进入删除阶段。

## 验证

- 通过项目内脱敏直连入口读取 B76 `screen`、目标进程和磁盘；未写入远端。
- 对四个本机目录完成普通文件计数、逻辑字节、聚合 SHA-256、状态文件和检查点模式检查；未写入运行目录。
- 未删除、移动、压缩或修改生产代码、数据或运行制品。
