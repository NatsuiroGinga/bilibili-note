# M-E 门作用域的源年量化与预注册含义

<!-- RESEARCH_ROUTE=RWKV -->

- 日期：2026-08-31
- 机制：**M-E｜实体门控的分段线性数值分词**
- 证据等级：**源年数据刻画**（`source_year_data_characterization`），`target_reads = 0`
- 前置：[M-E 机制形式化与复杂度规约](2026-08-31-M-E机制形式化与复杂度规约.md)（提交 `e6e74df`）
- 状态：**实测结论**。本文件只报告数据事实与由其推出的判据约束，不含任何有效性主张。

## 一、被量化的问题

形式化第 3.3 节的实体门含指示函数：

```
γ_θ(s_{e,t}) = σ(w_2^⊤ · ReLU(W_1 s_{e,t} + b_1) + b_2) · 𝟙[c_{e,t} ≥ 1]
```

`𝟙[c ≥ 1]` 使**实体首片段的门恒为零**。由零门退化命题（形式化第 4.3 节），门为零时
M-E 的输出与不含 M-E 的基线**逐位相等**。因此该指示函数直接决定 M-E 的作用域：
**门不激活的样本，其分数恒等于 C00。**

形式化只记了「`c` 的中位数为 `0`，超过一半的序列是其实体首序列」，未量化这对
**实体级主指标**意味着什么。本文件补上该量化。第三章主指标是实体 AP，
而作用域的实体侧分布与片段侧分布差异极大，不能互相替代。

## 二、方法与自校验

两个只读脚本，源年 LSPR23 缓存
`runs/diagnostics/dijk-repro/cache/{E23,T23,I23,M23,y23}.npy`，不触及任何 LSPR24 制品。

严格过去片段计数按全局时间序向量化计算，与部署期语义一致：

```python
order = np.lexsort((stamp, entity))
is_group_start[0] = True
np.not_equal(entity_sorted[1:], entity_sorted[:-1], out=is_group_start[1:])
group_id = np.cumsum(is_group_start) - 1
position_in_group = np.arange(n) - group_start_index[group_id]
prior_segments[order] = position_in_group          # 即 c_{e,t}
gate_active = prior_segments >= 1
```

源年切分逐字复现 `base.source_split`（`seed = 42`、`validation_fraction = 0.1`、
`time_tail_fraction = 0.15`）。

**三重自校验全部通过**，口径确认无误：

| 校验项 | 本次实测 | 独立来源 | 结果 |
| --- | --- | --- | --- |
| 切分序列数 | 训练 `208598`、验证 `22444` | `PROTOCOL_A_SPLIT_STATISTICS`（代码硬断言） | 逐位相等 |
| 恶意实体总数 | `239` | `entity-history-availability.json` | 逐位相等 |
| 恶意实体多片段比例 | `0.4895397489539749` | 同上 | 逐位相等 |
| 良性实体多片段比例 | `0.021456916665004884` | 同上 | 逐位相等 |
| 验证集正实体数 | `20` | 恢复卡第八条独立记载 | 一致 |

## 三、实测结果

### 3.1 全源区

| 量 | 值 |
| --- | --- |
| 实体数 / 片段数 | `150680` / `271815` |
| 门激活片段数 | `121135`（`44.57%`） |
| 门恒零片段数 | `150680`（`55.43%`） |
| **门激活片段覆盖的流份额** | **`93.47%`** |
| 有任一激活片段的实体 | `3345`（`2.22%`） |
| **恶意实体中可被门影响的** | **`117 / 239`（`48.95%`）** |
| 良性实体中可被门影响的 | `3228 / 150441`（`2.15%`） |
| **恶意富集比** | **`22.82` 倍** |

片段侧与实体侧的巨大差异是本节的核心：门激活片段只占 `44.57%`，却覆盖 `93.47%` 的流；
而按实体计，只有 `2.22%` 的实体有任何激活片段。**用片段份额或流份额描述 M-E 的作用域
会严重高估它对实体级指标的影响面。**

### 3.2 按切分分层

| 量 | 训练集 | **验证集** |
| --- | ---: | ---: |
| 序列数 | `208598` | `22444` |
| 实体数 | `121501` | `13529` |
| 恶意实体数 | `165` | **`20`** |
| 门激活序列份额 | `41.75%` | `39.72%` |
| **恶意实体中门可达的** | `80`（`48.48%`） | **`13`（`65.00%`）** |
| 良性实体中门可达的 | `2688`（`2.22%`） | `350`（`2.59%`） |

验证集正实体的门可达比例（`65.00%`）显著高于训练集（`48.48%`）与全局（`48.95%`）。
这是随机按实体划分在 `20` 个正实体上的抽样波动，**不是设计优势，正文不得据此宣称任何倾向**。

## 四、对四格判据的约束

### 4.1 增益空间的上界

源年验证集实体 AP 由 `20` 个正实体与 `13509` 个负实体的排序决定。M-E 能改变分数的是：

- 正实体 **`13` 个**，其余 **`7` 个（`35%`）分数恒等于 C00**
- 负实体 `350` 个，其余 `13159` 个（`97.4%`）分数恒等于 C00

**M-E 在源年的全部增益空间就是这 `13` 个正实体与 `350` 个负实体的重排。** 该上界是
机制定义与数据结构共同决定的，与实现质量、超参数或训练轮数无关，也无法通过调参扩大。

### 4.2 统计分辨力风险

四格判据要求源年 `C10 > C00`。在只有 `13` 个可动正实体的验证集上，单个正实体的排序
变动即可显著改变实体 AP。该风险与恢复卡第八条记载的「源年验证仅 `20` 个正实体，
任何方法都无法解决」同源，但 M-E 的情形更窄：BER 作用于损失，影响全部实体的排序；
M-E 作用于表示且只在门激活样本上生效，作用面小一个量级。

**这不构成否决**：判据本身未变，且 BER 在同一验证集上已通过目标年正增益门。
但它要求本机制的源年结论必须附带可动实体计数，**不得以「实体 AP 提升」这一笼统表述
掩盖增益仅来自 `13` 个正实体的事实**。

### 4.3 由本节实测产生的预注册断言

以下断言在 M-E 实现完成后、源年正式实验前机械执行，结果写入运行收据：

1. **零门退化的数据侧实例化**：源年验证集中那 `7` 个门永不激活的恶意实体，
   其 M-E 模型输出分数必须与 C00 **逐位相同**。该断言同时检验实现正确性与因果正确性，
   比合成输入的构造真值更强，因为它走的是真实数据与完整前向路径。
2. **负实体侧同断言**：`13159` 个门永不激活的良性实体分数同样必须与 C00 逐位相同。
3. **可动实体计数收据**：正式运行须落盘源年验证集的
   `malicious_entities_gate_reachable = 13` 与 `benign_entities_gate_reachable = 350`，
   与本文件的数字一致，防止实现引入意外的门激活路径。
4. **梯度可达性**：位移通道 `u^{(j)}_t` 与门参数 `θ` 只应从门激活样本接收梯度；
   在全部样本门为零的构造批次上，二者梯度范数须为零。

## 五、风险与边界

- **门本身可能是判别信号**。`c ≥ 1` 在恶意与良性实体上的分布差异达 `22.82` 倍
  （`48.95%` 对 `2.15%`）。若 M-E 取得增益，**必须能区分「实体状态调制数值表示」
  与「门当作判别特征」两个来源**，否则该增益与本课题早期已用实验证伪的流数先验同类。
  区分所需的消融另行确定，本文件不预设其形式。
- **跨年稳定性未知且不可预先检验**。上述富集比是源年事实；目标年封印，不得读取。
  CEM 的失败正来自源年与目标年的历史密度差异（目标年每链跨段恢复为源年的 `4.95` 倍）。
  M-E 必须在设计上不依赖该先验的跨年稳定，或在正文如实登记这一适用边界。
- 本文件全部为数据刻画，**不含任何关于 M-E 有效性的主张**，也不构成对 M-E 的支持或否决。
  机制的存废只由源年四格与封印后目标年结果裁决。
- 两个诊断脚本是一次性制品，置于会话临时目录，随任务清理；本文件的数字由上述
  三重自校验保证可重算，不依赖脚本留存。
