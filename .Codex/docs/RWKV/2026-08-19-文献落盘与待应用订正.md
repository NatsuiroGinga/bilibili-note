# 文献落盘执行记录与待应用订正

<!-- RESEARCH_ROUTE=RWKV -->

- **日期**：2026-08-19
- **上游**：`2026-08-19-第一二章文献建设/联网补齐结论.md`（文献代理 `ab31585d4a8db8234`，`claude-opus-5`，effort 未记录）
- **为什么单开此文件**：该代理无 `Bash`，写不了 PDF 二进制，落盘由主代理执行；
  同时其交付中有一条与实测不符，须留痕。

## 一、已落盘（三篇，均已核为真 PDF）

| 落盘路径 | 页数 | 大小 | SHA-256 前 16 | 来源 |
| --- | ---: | ---: | --- | --- |
| `raw/papers/methodology/ranking/2019-Radenovic-GeM-Fine-Tuning-CNN-Image-Retrieval-TPAMI-arXiv1711.02512.pdf` | 14 | 7.4M | `fdf055745c0f6365` | Zotero 存储 `ZFFNL4Q9` |
| `raw/papers/traffic-classification-evolution/2016-DraperGil-ISCX-VPN-nonVPN-Time-related-Features-ICISSP.pdf` | 8 | 764K | `6a945f720b2bdb10` | `scitepress.org/Papers/2016/57407/57407.pdf` |
| `raw/papers/attack-detection/fingerprinting/2018-Sirinam-Deep-Fingerprinting-CCS-arXiv1801.02265.pdf` | 16 | 972K | `130f2749d1dc7b05` | `arxiv.org/pdf/1801.02265` |

落盘的 GeM 副本是 **arXiv:1711.02512v2（2018-07-10）**，非 TPAMI 排版版；引用页码时须注明是 arXiv 页序。

## 二、交付中与实测不符的一条（须留痕）

交付第 4.1 节称「Zotero 存储中已有 PDF，直接复制（5 篇）」。**实测只有 1 篇成立。**

| Zotero 键 | 交付声称 | 实测 |
| --- | --- | --- |
| `ZFFNL4Q9`（Radenović） | 可直接复制 | **成立**，已落盘 |
| `RDT8GVZJ`（Panchenko NDSS'16） | 可直接复制 | `/Users/bilibili/Zotero/storage/RDT8GVZJ` **目录不存在** |
| `3BS8TBSZ`（Taylor TIFS） | 可直接复制 | 同上，目录不存在 |
| `UIBQVTXN`（Garg） | 可直接复制 | 同上，目录不存在 |
| `3K6INJU2`（NoDoze NDSS'19） | 可直接复制 | 同上，目录不存在 |

推断（**未验证**）：这四个是 Zotero **条目键**而非附件键，或该条目只有云端元数据无本地附件。
后续取原文须另找入口，**不得因交付写了「可直接复制」就当作已在库**。

**Garg 一条无需再取**：本库既有 `methodology/2020-Garg-Unified-View-Label-Shift-Estimation.pdf`
实测 21 页可正常读取；同目录的 `2020-Garg-Unified-Label-Shift.pdf`（320K）`pdfinfo` 读取失败，
证实交付所称「损坏」。按 `raw/AGENTS.md` 只增不改，不删该损坏文件。

## 三、待应用订正（**当前不改，因自审代理正在读目标文件**）

第三章定稿自审工作流 `wf_df019744-d2e` 的 `四_引用完整性` 代理正在读
`thesis/chapters/参考文献.md`。此刻改动会使其报告失效。**自审报告落地后按下表应用。**

### 3.1 GeM 出版年订正（证据等级 A）

`thesis/chapters/参考文献.md` 第 72 行与第 89 行两处记「Radenović 等，TPAMI **2018**」。

**实测为 2019。** CrossRef 出版商存缴元数据（`api.crossref.org/works/10.1109/TPAMI.2018.2846566`）：

```
卷期 41 (7)    页码 1655-1668
印刷出版年 2019-07-01    在线优先年 无
```

`2018` 是 DOI 铸号年而非出版年，误记应由此而来。正确著录：

> RADENOVIĆ F, TOLIAS G, CHUM O. Fine-tuning CNN image retrieval with no human
> annotation[J]. IEEE Transactions on Pattern Analysis and Machine Intelligence,
> 2019, 41(7): 1655-1668.

### 3.2 第 72 行「原件缺失」表述已过期

原文：「广义均值池化的原始出处（Radenović 等，TPAMI 2018）**本机原件缺失**，
已按文件名与全文检索递归查过 `raw/papers/` 全目录无命中」。

**该原件已于本日落盘**（见第一节），此句须改写。第 89 行的待补项
「补下广义均值池化的原始出处原件」**可关闭**。

### 3.3 不受影响的部分

第三章 `[9]` 现引的是 Revaud 的 AP-GeM（ICCV，`raw/papers/methodology/ranking/2019-Revaud-AP-GeM-ICCV.pdf`），
与 Radenović 的 GeM 是**两篇不同论文**。正文只述「广义均值的指数与检索目标联合训练」，
未声称该文首创广义均值，故 `[9]` 的措辞不因本次订正而改变。
仅当正文改为追溯提出者时，才需按「提出＋应用」两条著录，此时 Radenović 一条已有原件可用。

## 四、下一步

1. 自审报告落地后，按第三节应用两处订正，并按 `raw/AGENTS.md`「下载即写笔记」
   为三篇新原件补 `wiki/papers/` 笔记与 `INDEX.md`。
2. 交付第 2.3 节列的 10 篇仅题录论文，按本日「正文不写字段预算」的裁定筛选后，
   属刚需的只有三条：Axelsson（告警预算）、Saerens（跨年度先验校正）、Apruzzese（实体级周期通信）。
   其余七条属轴一叙事装饰，不阻塞第一二章。
3. 新建的两个目录 `raw/papers/attack-detection/fingerprinting/` 与
   `raw/papers/traffic-classification-evolution/` 尚无 `wiki/papers/` 对应索引，须补。
