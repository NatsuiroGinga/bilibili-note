# 全局进度快照（2026-08-28，防上下文压缩）

会话哨兵：`RESEARCH_ROUTE=RWKV`。本快照汇总 08-27 深夜至 08-28 的全部裁决、结果与在飞任务；
逐项证据见各链接文档，冲突时以原始制品为准。

## 一、第三章状态

### 已完成
- **正文**：审查 9 阻断全关（`679ed81`）＋图文同步（`f4cda03`）；主消融=MLP bf we16 四格
  `36.46/46.41/44.75/58.55`（位语义源码钉死 `tools/ch3_full_mlp_complete_entity_lp_protocol_a_q0.py:36-41`）；
  DR 全部统一总表实际 FPR 口径；参考文献 0 缺口。
- **图件**：11 幅全部换底重生成、SimHei 根治、图 3-12 新建、数字 1e-12 自校（`e8053f4`/`42c80ff`）。
- **跨基座**：XGB 分数层四格（`0.5132/0.5353/0.5304/0.5651`，全正）以「机制移植增益」进正文消融部分；
  CNN/GRU/Transformer 三骨干四格全 FAIL 剔除并立档（`d551b97`）；C00→C11 移植表撤销。
- **ResNet 同源重评完成，X2 关闭**（`996a18c`）：`0.4317/0.6004/0.3487/0.4039`——ELP 单开 +16.9 点为
  全项目最大单机制增益，CPA 负；判据 FAIL 留档。

### 关键新事实与裁决
- **FT-Transformer 正式四格**：C00 `0.7508`（全项目最高）/机制全负/联合崩溃 `0.2796`；
  TabM32 正式四格：C00 `0.7119`（第二）/机制全负。两强骨干同模式。
- **比较集成员规则（08-28 查清，尚待用户最终确认）**：TabM/FT 在 LSPR 数据集族全部四篇
  已发表文献（Dijk 2024/Dijk 2026/Leoste 2025/DiGennaro 2026，本地全文核验）中**零出现**；
  表 3-7 基线池=数据集文献方法（XGBoost/GRU/全注意力/BigBird/Longformer/RF/CNN），
  该成员规则先于 FT/TabM 运行存在 → 排除它们不构成事后剔基线；正文主张措辞应为
  「对该数据集已发表方法全面胜出」。FT 分数层移植救援已证走不通（增益 +0.4~0.9 点 <1×SE）。
- **FT `C00=0.7508` 可信度**：程序初查干净（封印/预算/参数量）；对抗校验被用户手动停止；
  逐轮零训练裁决物理不可行（无逐轮检查点，`8f7e142`）——处置悬置。

### 第三章重锚线（新开，构思中）
- 研究问题卡 `2026-08-28-第三章FT-TabM底座新机制研究契约/研究问题卡.md`（`9ef3cad`）：
  以 FT/TabM 为基座设计两个新机制；四候选菜单（M-A 实体历史字段 token／M-B 实体级选择-校准／
  M-C 成员分歧聚合／M-D IAT 字段——撞冻结字段预算待裁决）；推荐配对 M-A×M-B。
- **禁区（有制品）**：表示层挂载双证伪、分数层太弱、辅助目标夺权（H3 通道 1）。
- **结构裁决（用户 08-28）：单基座主线，不做对称双适配**——重锚方法若为「FT＋两机制」
  一体，单基座是自然结构（与「朱的双骨干是其挂载式方法内生需求、本章不照抄」的既有裁决
  自洽）。主基座取 FT（C00 最高）；TabM 不做对称适配小节，其四格若也过判据则按 XGB 行
  既有框架作「机制可移植性」第二行进消融部分。
- **工作量测算（按单基座修正）**：新增训练臂约 **7-9 个**（FT 四格新臂 3 个＝C01/C10/C11，
  C00 现成；动机与敏感性 4-6 个；TabM 移植行可选 +3），约 1 GPU 天＋一周写作图表理论；
  **增益可写下限 ≥1×SE(0.036)=3.6 点**，低于此不投臂。正文论证链已按朱 3.1-3.7 模板逐段
  映射（3.4 只写 FT 适配＋机制实现）。
- **待用户裁决**：①是否接受重锚代价（正文大部分重写）；②四选二。

### 服务器待办（下次有卡开机）
1. 协议 B 目标年评价续跑（8 训练格已完成在数据盘；两条命令分钟级）→ U6 全注意力/GRU 定案。
   注意：实体 AP 选轮口径下 **GRU 的 lr 择优是 2e-3 胜出**（`38f6bb7`）——「2e-3 人为削弱」
   结论只绑定旧逐流 AP 口径。
2. TabM32 分数层回填（工具已部署服务器，`39d918a`）。
3. paper-self-review 定稿门：压在「强 C00 处置＋重锚与否」裁决之后。

## 二、第四章状态

### 底座与病灶（MLP O11，V2）
- 三病灶全部实验确认（校准 bug 修复重跑后，`recal-v1` 家族）：曝光累积 FPR(B1)=`6.48%`、
  长桶 FPR 单调递增 `2.41%→27.47%`、跨年失配（预算利用 `22.2%`、DR `−16.62` 点，`983ff49`）。
- **校准 bug 事故链（三例同型「首中即返」缺陷）**：主进程校准函数→诊断工具→代理 CP 阈值函数；
  曾致「病灶不存在」误判，logit 复跑「一致」实为同 bug 一致；已全部修复留痕（`a43909c`/`1301aef`），
  应写入故障预防手册（待办）。

### 机制 1（Tong 路径预算控制）：SUPPORTED（本机筛选级）
- 三臂：B0′ `0.0400/0.8577`、B1′ `0.0625/0.9038`、**M1′ `0.0370/0.8954`**（+3.77 点，六证书全立，
  迟到检出 14）；效应三倍于 XGB 链。正式版待服务器按冻结 BF16 合同重跑。
- **创新性拆账（08-28 口头版）**：借 Tong（NP 伞形）有限样本工具；自造三层（实体路径最大被控
  统计量／六方向同模型交叉校准／曝光病灶三臂绑定）——结构上够朱式改造，但**新颖性差量表
  与理论适配引理两块板未补**；差量核验已派（见在飞任务）。

### 机制 2：三构造原门否决 → 朱式结构不可谈判 → MalMoE 朱式改造线
- A（层窗口边际）败于跨层序贯并集（17.07%）；B（一维 λ）仅门③败（DR −6.28 点，①②全层
  ≤1.89% 大幅过）；C（前向逐层预算）贴边校准顶破证书①②③④败（`5ec56af`）。
- **用户裁决：对标朱第四章，双机制＋联合算法结构没有三选余地，机制 2 迭代直至正结果。**
- **当前候选＝MalMoE 朱式改造（机制 2″）**：原文 p.3-8 已核读（`7141720`），五维差异表冻结
  （算子专家零训练／门控状态在线因果／硬选择保留／CP 证书层为 MalMoE 所无／E0 oracle 立题）。
- 关键路径数据：C10（M1′）自身长层条件 FPR `22.9%/27.7%`——D2 病灶存活于机制 1 合规臂内，
  机制 2 的治理对象即在此；门③结构性不对等分析在案（约束臂对非约束臂比检出）。
- 构造 D（分组 λ）降为备选；fold2 方向 4/5 阈值异常待查（不阻断）。

### 连锁依赖
第四章底座=第三章胜出模型（V2）。**若第三章重锚 FT/TabM，第四章 D0→M1′ 链随迁重跑**
（工具链已底座无关，约一天＋GPU 时数），病灶须在新基座分数上重新确认（XGB→MLP 的教训）。

## 三、在飞任务（08-28 上午进程重启后重排）

前一 Claude Code 进程 02:19 退出，三个在飞代理全部随进程死亡（MCP Connection closed）：

- **E0 oracle 存废实验**：死亡时 `--construction e0` 分支**未实现**（工具只有 a/b/c 分支），
  运行目录 `ch4-mlp-o11-operator-oracle-e0-v1` 为空。按用户「先聚焦第三章」裁决**暂缓**，
  不重派；第三章机制定型后随第四章续办。
- **机制 1 新颖性差量核验**（sonnet）：部分成果已落盘——wiki 新增并校验 4 篇方法论笔记
  （Barber 2021 jackknife+、Wang 2024 分层 NP、Bates 2023 保形 p 值、Maillard 2019 扫描统计量）。
  已重派新代理，从 `2026-08-26-分层风险控制定向文献/` 检查点续跑。
- **FT 机制文献调研**（opus）：死亡于向 Zotero 批量导入 9 个 arXiv 号（含 2203.05556 数值嵌入）
  的调用中，导入是否落库未知。已重派新代理，范围按两项新裁决更新：单基座 FT、不要求可移植，
  新增数值嵌入（M-E）一束，砍掉 TabM 成员分歧束。

两项新用户裁决（08-28 上午）：①**不要求机制跨基座可移植**，机制只需在 FT 上成立，可用 FT
特有结构；②**主问题聚焦第三章**「两个新机制让 FT 提升效果」，第四章议题暂缓。
文献代理均已授权 alphaXiv/Scite/Elicit 等 MCP 通道；下载不了的文献列清单交用户亲自补。

## 四、等用户拍板清单

1. 第三章：比较集成员规则确认（据此放行 paper-self-review）或走重锚（含四选二）。
2. 第四章：无待裁决——机制 2″ 按 E0 结果自动推进或否决后回构造 D/门③′议题。
3. FT `0.7508` 对抗校验重启与否（悬置中）。
