# 第三章「两级风险聚合框架」审核（第六个组合方案）

**审核对象**：`thesis/methods/第三章-两级风险聚合框架.md`（提交 `3f7d36d`）
＋ `.Codex/docs/RWKV/2026-09-04-联合框架实证审核/soft_k_probe.py`
**立场**：证伪。**日期**：2026-09-04。
**证据分类**：`【实测】`本次本机实跑；`【文献】`已核验原件；`【推论】`数学推导，无实验；`【无法核验】`。

**全部实验零 GPU，本机 CPU，`target_reads = 0`（只读 `E23/T23/M23`，不读 `y23`）。
未启动任何训练，未接触服务器 GPU。**

---

## 零、一句总裁定

> **方案没有被打死，但它现在这个形态不该开工。**
> 三件必做全部执行完毕：最便宜的证伪点（真实袋分布）**未能证伪**——`α` 的作用面是
> `32.19%` 而非「极低」；`§四` 第 3 项的 `c` 缩放风险**被证实存在但不致命**
> （是减速不是阻断）。真正的阻断项出在别处：**一个会翻转梯度符号的实现缺陷**、
> **与 C11 读数的口径断裂**、以及**判据 2 无法把「软算子」与「α 可学」两个变量分开**。
> 前两项可修，第三项是实验设计问题，必须在开工前改判据。

---

## 一、实验环节（先做，因为最便宜）

### 1.1 结论：最便宜的证伪点**没有把方案打死**

任务简报的假设是：若 `α` 只能在 `(0, 1/2]` 内移动，而 `67.8%` 的实体 `m_e ≤ 2`，
则 `α` 的移动对多数实体不改变任何东西，占比极低时判据 2 结构上不可能成立。

**实测该占比不低，假设不成立。** 脚本 `bag_alpha_reach.py`，
数据 `runs/diagnostics/dijk-repro/cache/{E23,T23,M23}.npy`，
切分逐字复刻 `source_split`（`seed=42`、`validation_fraction=0.10`、`time_tail_fraction=0.15`），
并对 `probe.json` 的两个锚点复核通过（验证侧实体 `13529`、`m=1` 实体 `6290`）。

`【实测】`训练侧 `121,501` 实体（截断 `8192` 后），验证侧 `13,529` 实体：

| 量 | 训练侧 | 验证侧 |
| --- | ---: | ---: |
| `m_e = 1` 占比 | `0.4622` | `0.4649` |
| `m_e = 2` 占比 | `0.2159` | `0.2188` |
| `m_e ≤ 2` 占比（＝文档的 `67.8%`） | `0.6781` | `0.6837` |
| **`α ∈ (0, 1/2]` 内行为改变面的上确界 `P(m_e ≥ 3)`** | **`0.3219`** | **`0.3163`** |
| `α = 0.5` 处梯度作用面 | `0.3219` | `0.3163` |
| 袋大小 P50/P75/P90/P95/P99 | `2 / 3 / 9 / 30 / 511` | — |

`n_neg = 64` 均匀采样下，`α = 0.5` 时**每步期望有 `20.6 / 64` 个负实体向 `α` 回传梯度**，
其余 `43.4` 个的 `∂S_e/∂α` 精确为 `0`。

**`32%` 的实体、每步 `20` 个负样本，不是「结构上不可能」。判据 2 的先验可行性成立。**
而且这 `32%` 恰是大袋实体，在 `g_e²/k_e` 加权下占的梯度能量远高于其计数占比
（依据：`2026-09-01-机制耦合观测预注册.md` §四点九作废节已实测活动实体偏向大袋）。

### 1.2 顺带证实的一条：软算子确实消除了硬算子的平台区

`【实测】`同一张表的另一列，相对 `α = 0.5` 的行为改变面：

| `α` | 软算子 | 硬算子 |
| ---: | ---: | ---: |
| `0.40` | `0.3219` | `0.1485` |
| `0.3333` | `0.3219` | `0.2696` |
| `0.20` | `0.3219` | `0.3219` |
| `0.05` | `0.3219` | `0.3219` |

硬算子在 `α = 0.40` 只改变 `14.85%` 的实体（`⌈0.4m⌉` 与 `⌈0.5m⌉` 在多数 `m` 上相等），
软算子对全部 `m_e ≥ 3` 实体连续生效。**这是软算子相对硬算子的一个真实的、可测的差异**，
不是文字上的好处。

### 1.3 两个端点都是梯度平坦点

`【实测】`梯度作用面随 `α` 的变化：`α = 0.02` 时仅 `0.0401`，`α = 1.00` 时精确为 `0`
（`t` 被上夹）。**`α → 0` 方向存在自我强化的收缩**：`α` 越小，向它回传梯度的实体越少，
信号越弱。这与文献第 4 问（可学风险水平参数退化到端点）直接相关，
**该问的答案会实质影响判据 2 的先验合理性**（文献环节结论见 §三）。

---

## 二、代码环节：`§四` 四项核验

脚本 `alpha_controller_interaction.py`、`alpha_param_bitwise.py`，
**用的是生产模块本身**（`ch3_ft_gradient_controller.combine_gradients`、
`ch3_ft_entity_ranking_loss.cvar_pauc_loss` / `tail_aggregate`），
不是复刻；`_shared_parameters` / `_flat_grad_from_params` 按
`ch3_ft_c00_dual_selection.py:2346,2351` 逐字同义重写。

### 2.1 `§四` 第 3 项（`α` 的梯度是否被 `c` 缩放）：**是，逐位成立；但不致命**

`【推论】`先给闭式，再给实测。`_combine_and_step`（`ch3_ft_c00_dual_selection.py:3015-3035`）
把 `combine_gradients` 的输出按 `_shared_parameters(model)` 顺序切片写回 `.grad`。
逐流损失不含 `α`，故 `g_flow[α] = 0`；单向 PCGrad 投影
`g_r⁺ = g_r − (⟨g_r,g_f⟩/‖g_f‖²) g_f` 在该坐标上不改变任何东西（被减项含因子 `g_flow[α] = 0`）。
于是

```
combined[α] = g_flow[α] + c · g_rank⁺[α] = c · g_rank[α]
```

`【实测】`两种步都逐位相等：

| 情形 | `c` | `g_flow[α]` | `g_rank[α]` | `combined[α]` | `combined[α] == c·g_rank[α]` |
| --- | ---: | ---: | ---: | ---: | :---: |
| `ξ = 0`，活动集非空 | `0.096705` | `+0.000000e+00` | `−2.1598233283e−01` | `−2.0886618644e−02` | **逐位相同** |
| `ξ = 1e4`，活动集全空 | `0.000000` | `+0.000000e+00` | `+0.000000e+00` | `+0.000000e+00` | **逐位相同** |

**所以简报担心的机制确实存在：`c = 0` 的步上 `α` 的梯度精确为 `0`。**
按恢复卡记载，C11 第 `8` 轮 `c_scaling_median = 0`、空活动集率 `0.559`
（`RWKV第三章恢复卡.md:139-146`），即**过半的步 `α` 收不到任何梯度**。

**但这与已证伪方案不同型，差别是决定性的**：

- 已证伪方案是 `torch.ceil` 的导数**恒为零**，Adam `200` 步位移精确 `0.000000e+00`——**永远学不动**。
- 本方案是**占空比**问题。`【实测】`（`alpha_param_bitwise.py` R3）Adam 在
  **`90%` 的步梯度为零**、每 `10` 步才给一次 `grad = −1` 的极端占空比下，
  `50` 步后 `α` 位移 `+0.161854`；而梯度**恒为零**时 `50` 步位移精确 `0.0`。

**裁断：`§四` 第 3 项的风险属实，但它是「`α` 的有效学习率被一个不受控的全局标量 `c` 调制」，
不是「`α` 学不动」。** 需要在方案里明写这一条并给 `α` 独立的学习率，
不能沿用共享参数的 `lr`——`c` 在实测里取到 `0.0967`，即 `α` 的步长被压了约一个数量级，
且该压缩量逐步变化、不可控。

### 2.2 `§四` 第 4 项（边界数值行为）：**发现会翻转梯度符号的缺陷**

`【推论】`闭式：`S_e = (Σ_{i<k} s_i + f·s_k)/t`，`t = α·m`，则
`∂S_e/∂t = (s_k − S_e)/t`，`∂t/∂α = m`，故

```
∂S_e/∂α = (s_⌊t⌋ − S_e) / α     且  s_⌊t⌋ ≤ S_e  ⟹  ∂S_e/∂α ≤ 0 恒成立
```

`【实测】``200` 组随机 `(m, s, α)` 严格内点，自动微分与闭式不符或梯度为正的组数 = **`0`**。
**在严格内点上，`α` 对每个实体都是单调的分数压低算子。**

**但在 clamp 的打平点上，PyTorch 会漏出一个不该存在的梯度，且符号相反：**

| `m_e` | `α` | `t` | `软 S` | `硬 S` | 逐位同 | `∂S/∂α` |
| ---: | ---: | ---: | ---: | ---: | :---: | ---: |
| `0` | `0.5` | `1.0000` | `+0.00000000` | `+0.00000000` | 是 | `0.0` |
| `1` | `0.5` | `1.0000` | `+2.60184336` | `+2.60184336` | 是 | `0.0` |
| **`1`** | **`1.0`** | `1.0000` | `+2.60184336` | `+2.60184336` | 是 | **`−1.3009`** |
| **`2`** | **`0.5`** | `1.0000` | `+0.54956013` | `+0.54956013` | 是 | **`−3.4531`** |
| **`2`** | **`1.0`** | `2.0000` | `−0.31372389` | `−0.31372389` | 是 | **`+0.1569`** |
| **`3`** | **`0.5`** | `1.5000` | `−0.00253450` | `−0.10393868` | **否** | `−0.8112` |
| `3` | `1.0` | `3.0000` | `−0.60500598` | `−0.60500598` | 是 | **`+0.3025`** |

三个问题，按严重度：

1. **`α · m_e = m_e`（上夹打平）处 `∂S/∂α` 为正**（`m=2` 时 `+0.157`，`m=3` 时 `+0.303`）。
   这违反上面刚证明的「恒 `≤ 0`」。根因：`t = min(α·m, m)` 在打平点被 PyTorch 按平局
   分摊梯度，漏出 `−S/α` 这一支，其符号是 `−sign(S)`；**对分数为负的实体（负实体的常态）
   该项为正。** 数学上正确的单侧导数是 `(s_{m−1} − S)/α ≤ 0`。
   **这是实现缺陷，不是设计选择**，且只在 `α → 1` 附近发作——恰是 §1.3 指出的端点。
2. **`α · m_e = 1`（下夹打平）处同样漏梯度**：`m_e = 2`、`α = 0.5` 得 `−3.4531`，
   而左导数应为 `0`（`α` 略降时 `t` 仍被夹在 `1`）。**`α = 0.5` 正是初始化点，
   而 `m_e = 2` 占训练实体的 `21.59%`**——即第一步的 `α` 梯度里混进了
   五分之一实体的单侧次梯度，`α` 一旦离开 `0.5` 该项立刻消失。
   起步方向因此不可复现地依赖于打平点的平局规则。
3. **`m_e = 3`、`α = 0.5` 时软算子与硬算子不逐位相同**（`−0.00253` 对 `−0.10394`）。
   见 §2.4。

**处置**：`t` 的两侧夹紧必须写成显式 `detach` 的分支（夹住时把 `t` 从图上摘掉），
不能依赖 `clamp` / `minimum` 的平局梯度。这是开工前必须先改的实现细节。

### 2.3 `§四` 第 1、2 项：可以通过，但各带一个必须机械核验的前置条件

`【实测】`**第 1 项（生产路径下 `α` 梯度非零）**：`ξ = 0` 的步上 `g_rank[α] = −2.16e−01`，
经 `c` 后 `combined[α] = −2.09e−02`，非零。**通过。**
**但有一个会静默吃掉全部位移的陷阱**：若 `α` 不在 `model.parameters()` 里，
`_combine_and_step` 的 `optimizer.zero_grad(set_to_none=True)` 之后
`optimizer.step()` 根本不遍历它——`【实测】`该情形下 `α` 反传拿到 `grad = −0.7834`，
`step()` 后位移**精确 `0.0`**，**不报错、不告警**。与已证伪方案的失败现象完全一样，
根因却完全不同。**`α` 必须注册进 `model.parameters()` 且在优化器构造之前完成。**

`【实测】`**第 2 项（`z1 = 0` 四格臂逐位一致）**：两条独立风险，一条安全一条不安全。

- **控制器侧安全**：给 `g_flow` / `g_rank` 各追加一个恒零坐标后，
  `c` （`0.34104203917057513`）、内积、`‖g_r⁺‖`、投影触发标志**全部逐位不变**，
  合成梯度前 `P` 个坐标逐位相同，新增坐标合成梯度精确为 `0`。
  即 `alpha=None` 的臂上多挂一个不接梯度的参数，**不改变任何既有数值**。
- **初始化侧不安全**：`【实测】`若 `α` 用消耗随机数的方式构造（`torch.randn`）
  且排在其他参数之前，`Linear.weight` 与基线**不再逐位相同**；
  用常量 `torch.tensor(0.5)` 构造则逐位相同。
  **`α` 必须用不消耗 RNG 的常量初始化**，否则四格逐位断言会以一个与 `α` 无关的原因失败。

### 2.4 主代理三条自测：**逐位复现，但第三条的结论被高估了**

`【实测】`直接跑 `soft_k_probe.py`，三条全部复现，数字与文档记载完全一致：

| 自测 | 复现结果 |
| --- | --- |
| `α` 梯度非零 | `α = 0.50/0.30/0.12` → `−2.010896 / −1.533531 / −5.424150` |
| 整数点与硬 top-`k` 逐位相同 | `α·m = 1/3/5/9` 四点全部 `float` 相等 |
| `α·m = 1` 与 `max` 逐位相同 | `1.540996108244043` 对 `1.540996108244043` |

**但文档 §二 由第三条推出的结论过强。** 文档写：

> 第三条决定了兼容性：`z1 = 0` 臂的硬断言 `assert_tail_aggregation_degenerates_to_max`
> **不受威胁，且是精确而非渐近**——这是本算子相对 RU 松弛的关键优势。

两处收窄：

1. `z1 = 0` 臂走的是 `alpha is None ⟹ k ≡ 1` 的分支（`ch3_ft_entity_ranking_loss.py:242-243`），
   **根本不进入新算子**。§2.1 自己也写了「新增算子，`tail_aggregate` 一行不动」。
   所以该断言**不受威胁的原因是它不在新算子的路径上**，与插值精确性无关。
   把它记成新算子的优势属于**归因错误**。
2. 「精确退化为 `max`」的成立条件是 `α ≤ 1/m_e`，**是逐实体的、不是全局的**。
   训练侧最大袋 `m = 8192`（截断后），要让**全部**实体同时精确退化为 `max`
   需要 `α ≤ 1.22e−4`。`α = 0.5` 时只有 `m_e ≤ 2` 的那 `67.81%` 精确等于 `max`。
   **文档把逐实体的性质写成了全局性质。**

真正剩下的、经得起核验的优势是：**在 `α·m_e` 恰为整数处，软算子与硬算子逐位相同**
（自测二，已复现）。这条成立，可以写。

---

## 三、文献环节

**状态：进行中。** 已派后台代理（`literature-reviewer`，模型 `opus`，
`effort` 继承会话设置——Claude Code 的 `Agent` 工具无 `effort` 入参，
不得把简报里的措辞报告为已生效的运行参数）。
简报要求按「本地混合索引 → Zotero → 在线」顺序、`pdf-converter`(MinerU) 读 PDF、
每答完一问立即落盘并提交，产出
`.Codex/docs/RWKV/2026-09-04-两级风险聚合框架审核/文献核查.md`。

四问：①插值式软 top-`k`／连续分位数先例（重点核对 **RU 的 CVaR 在离散样本上
本身就含分数权重项**，若等价则算子不新颖）；②先例报告的已知问题；
③「两级风险聚合、级间参数需相容」有无先例；④可学风险水平参数退化到端点的报告。

**在文献结论回来之前，本审核对「软 top-`k` 新颖性」只给一条本机可判的结论**：

`【推论】`文档 §五 已自认「不得称『首次』：可学聚合参数（AutoPool 2018）与
插值式软 top-`k` 均有先例族」。**该自认与 §二 把插值算子作为「使框架可行的零件」
并列举其「关键优势」之间，存在语气上的不一致**，但不构成事实错误。
**待文献回来后据实收窄差量。**

---

## 四、阻断项清单

**等级定义**：`致命` = 方案作废；`阻断` = 开工前必须关闭；`须改` = 可开工但文本必须先改。

| # | 阻断项 | 等级 | 证据 |
| --- | --- | --- | --- |
| **A1** | `t` 的两侧 clamp 打平点漏梯度，`α·m = m` 处 `∂S/∂α` **符号为正**，违反闭式；`α·m = 1` 处漏出单侧次梯度，而 `α = 0.5` 初始化恰在 `21.59%` 实体的打平点上 | **阻断** | `alpha_controller_interaction.py` 检验 D |
| **A2** | 判据 2 无法把「软算子替换硬算子」与「`α` 可学」两个变量分开归因（见 §五） | **阻断** | `【推论】`，见 §五 |
| **A3** | `α` 未注册进 `model.parameters()` 时 `optimizer.step()` 静默跳过，位移精确 `0`，与已证伪方案现象相同而根因不同 | **阻断** | `alpha_controller_interaction.py` 检验 B |
| **A4** | `α` 若用消耗 RNG 的方式初始化，`z1 = 0` 臂权重初始化整体平移，四格逐位断言以无关原因失败 | **阻断** | `alpha_param_bitwise.py` R2 |
| **A5** | §1.1 引为「不相容」`【实测】`证据的 C11 读数（`0.888374`、六档活动率 `62%`、`c_scaling_median = 0`）**产自硬算子**；软算子在 `α = 0.5` 上与硬算子对 `17.35%` 的训练实体给出不同 `S_e`，读数不能直接转用 | **须改** | `bag_alpha_reach.py`「软≠硬」列 `0.1735`；根 `AGENTS.md`「被否决实现的读数隔离」同型 |
| **A6** | §二「硬断言不受威胁且精确而非渐近，是本算子的关键优势」归因错误：该断言走 `alpha is None` 分支，不进新算子；且「精确退化」是逐实体条件 `α ≤ 1/m_e`，不是全局性质 | **须改** | §2.4 |
| **A7** | `α` 的有效学习率被全局标量 `c` 调制（实测 `c = 0.0967`，压约一个数量级），且 `c = 0` 的步（C11 实测占空比 `0.559`）梯度精确为零；方案未给 `α` 独立学习率 | **须改** | §2.1 |
| ~~A8~~ | ~~「`α` 作用面极低 ⟹ 判据 2 结构上不可能成立」~~ | **不成立** | 实测 `0.3219`，见 §1.1 |

**没有致命项。** 简报预设的最便宜证伪点（A8）**实测未能证伪**，如实记录。

---

## 五、对两个核心问题的裁断

### 5.1 「相容点存在」（判据 2）

**裁断：先验可行性成立，但判据本身不可用，必须改写。**

- **可行性成立**：`α` 在 `(0, 1/2]` 内对 `32.19%` 的训练实体（且是梯度能量占优的大袋实体）
  连续生效，每步约 `20.6 / 64` 个负实体回传；`∂S_e/∂α ≤ 0` 对正负实体同号，
  故 `∂L/∂α = Σ_n (≥0)(≤0) + Σ_p (≤0)(≤0)` **符号不定**，
  内点驻点在数学上没有被排除。`【推论】`
- **判据不可用**：判据 2 写「末轮源年实体 AP `> 0.918431` 即相容点存在」。
  该运行相对 C01 同时改了两件事——**换了聚合算子**（硬 → 软，`α = 0.5` 处即对
  `17.35%` 实体给出不同分数）**和让 `α` 可学**。
  **单臂无法把增益归给哪一个。** 而框架的主张是关于后者的。
  最小修法是补一个 `α` 冻结在软算子某定值的对照臂，或把判据改写成
  「存在某个 `α` 优于 C01」——后者**用现有硬算子做 `α ∈ {0.2, 1/3, 0.4}` 的定值扫描即可测，
  不需要任何新代码**（实测这三点的行为改变面分别为 `0.3219 / 0.2696 / 0.1485`，
  确实是三个不同的工作点）。

### 5.2 「软 top-`k` 新颖性」

**裁断：待文献。** 本机可判的部分：§2.4 已证伪文档给出的「关键优势」归因（A6）；
经得起核验的差异只剩「整数点逐位等于硬算子」与「消除硬算子的平台区」两条，
后者本次实测为真（§1.2）。**新颖性本身必须等文献代理的四问结论，尤其第一问中
RU 的 CVaR 离散样本估计是否本就含同一个分数权重项。**

---

## 六、建议的处置顺序

1. 先关 A1（clamp 打平点 `detach`）、A3、A4——三条都是几行代码的机械修正，且都有确定性核验。
2. 再关 A2：改判据，或补对照臂。**这一步不改，跑出来的数说明不了框架的主张。**
3. A5、A6、A7 是文本修正，改完再冻结方案。
4. 文献回来后按结论收窄 §二 的新颖性表述与 §五 的不可声称清单。

**本审核未运行任何训练，未占用服务器 GPU，未修改被审文档与任何生产代码。**
新增文件仅 `.Codex/docs/RWKV/2026-09-04-两级风险聚合框架审核/` 下的三个探针脚本与计划笔记。
