# 协议 A 训练区 Raw83 共享预处理设计计划

## 任务边界

- 只设计供表格 ResNet、FT-Transformer 与后续神经骨干共用的 Raw83 生产端、状态制品和加载器合同。
- 只新增本目录的 `task_plan.md`、`notes.md`、`实施方案.md`。
- 不修改生产代码、配置、启动器、恢复卡、总控或服务器状态，不启动任何实验。
- 不接触 LSPR24 标签来拟合、选择或修订输入臂。

## 文件所有权

- 本任务拥有：`.Codex/docs/RWKV/2026-08-22-协议A训练区Raw83共享预处理/`。
- 本任务不拥有：工作树中其余已修改或未跟踪文件。

## 执行步骤

| 优先级 | 步骤 | 状态 |
| --- | --- | --- |
| P0 | 恢复 RWKV 第三章与协议 A 当前合同 | 已完成 |
| P0 | 核验 N14 审计、N14 计划及现有表格 ResNet 实现缺陷 | 已完成 |
| P0 | 核验 FT-Transformer、`select_signal`、`dijk_fields` 与共同字段合同 | 已完成 |
| P0 | 核验固定实体不相交切分、字段基数与原始数据读取事实 | 已完成 |
| P1 | 比较双 `float32` 视图与 Raw83 映射加在线变换两种架构 | 已完成 |
| P1 | 冻结共享模式、状态、消费者接口、哈希和恢复合同 | 已完成 |
| P1 | 设计最小真实数据验证与失败门 | 已完成 |
| P0 | 自审、链接检查、差异检查并只提交本目录 | 进行中 |

## 验收条件

1. `实施方案.md` 覆盖父任务列出的十项要求。
2. 明确推荐的物化策略、容量上界、分块算法、原子断点与恢复语义。
3. 冻结最小模式、主键、切分、标签连接、状态制品、发现清单、兼容策略和消费者接口。
4. 记录官方 GitHub 主分支固定提交 `e3ed46c` 与 `lib/data.py` 的精确预处理参数及证据路径。
5. 所有科研数值均登记来源、边界与核验状态；未知数不得伪装为冻结事实。
6. 仅提交本目录；链接目标存在；无占位词；未运行 `black`。

## 阻塞条件

- 既有合同相互冲突且无法由原始代码或制品判定。
- 官方 `e3ed46c` 源码未在本地归档且无法核验精确参数。
- 固定 Protocol A 切分的权威行索引与实体集合无法定位。

## 当前裁决

- `DESIGN=READY`：共享接口、变换、资源、恢复和验证门已在 `实施方案.md` 冻结。
- `IMPLEMENTATION=NOT_STARTED`：本任务不修改生产代码。
- `EXPERIMENT=NOT_RUN`：没有真实物化或模型效果证据。
