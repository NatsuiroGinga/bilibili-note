# 协议 A 训练区 Raw83 共享预处理设计计划

## 任务边界

- 只设计供表格 ResNet、FT-Transformer 与后续神经骨干共用的 Raw83 生产端、状态制品和加载器合同。
- 只新增本目录的 `task_plan.md`、`notes.md`、`实施方案.md`。
- 不修改生产代码、配置、启动器、恢复卡、总控或服务器状态，不启动任何实验。
- 不接触 LSPR24 标签来拟合、选择或修订输入臂。

## 文件所有权

- 本任务拥有：`.Codex/docs/RWKV/2026-08-22-协议A训练区Raw83共享预处理/`。
- 本任务不拥有：工作树中其余已修改或未跟踪文件。

## 执行步骤

| 优先级 | 步骤 | 状态 |
| --- | --- | --- |
| P0 | 恢复 RWKV 第三章与协议 A 当前合同 | 已完成 |
| P0 | 核验 N14 审计、N14 计划及现有表格 ResNet 实现缺陷 | 已完成 |
| P0 | 核验 FT-Transformer、`select_signal`、`dijk_fields` 与共同字段合同 | 已完成 |
| P0 | 核验固定实体不相交切分、字段基数与原始数据读取事实 | 已完成 |
| P1 | 比较双 `float32` 视图与 Raw83 映射加在线变换两种架构 | 已完成 |
| P1 | 冻结共享模式、状态、消费者接口、哈希和恢复合同 | 已完成 |
| P1 | 设计最小真实数据验证与失败门 | 已完成 |
| P0 | 自审、链接检查、差异检查并只提交本目录 | 已完成 |

## 验收条件

1. `实施方案.md` 覆盖父任务列出的十项要求。
2. 明确推荐的物化策略、容量上界、分块算法、原子断点与恢复语义。
3. 冻结最小模式、主键、切分、标签连接、状态制品、发现清单、兼容策略和消费者接口。
4. 记录官方 GitHub 主分支固定提交 `e3ed46c` 与 `lib/data.py` 的精确预处理参数及证据路径。
5. 所有科研数值均登记来源、边界与核验状态；未知数不得伪装为冻结事实。
6. 仅提交本目录；链接目标存在；无占位词；未运行 `black`。

## 阻塞条件

- 既有合同相互冲突且无法由原始代码或制品判定。
- 官方 `e3ed46c` 源码未在本地归档且无法核验精确参数。
- 固定 Protocol A 切分的权威行索引与实体集合无法定位。

## 当前裁决

- `DESIGN=READY`：共享接口、变换、资源、恢复和验证门已在 `实施方案.md` 冻结。
- `IMPLEMENTATION=IN_PROGRESS`：2026-08-22 已交给唯一实现所有者，只新增冻结的共享生产模块、配置、启动器、官方快照与实施报告。
- `EXPERIMENT=NOT_RUN`：没有真实物化或模型效果证据。

## 实现阶段

| 优先级 | 步骤 | 状态 |
| --- | --- | --- |
| P0 | 核验依赖版本、第三方签名、现有切分与启动器惯例 | 已完成 |
| P0 | 实现 Raw83 模式、清单、源年流式解析、切分绑定和消费者权限 | 已完成 |
| P0 | 实现 A/B 训练区状态、共享视图和原子恢复 | 已完成 |
| P1 | 实现 P0–P5 默认源年入口、资源门、配置与启动器 | 已完成 |
| P1 | 归档官方快照与实施报告，执行冻结静态验收 | 进行中 |

## 实现文件所有权

- 生产代码：`src/flow_probe/protocol_a_raw83.py`、`src/flow_probe/protocol_a_preprocessing.py`。
- 入口与合同：`tools/ch3_protocol_a_raw83_prepare.py`、`configs/ch3-protocol-a-raw83-shared-v1.json`、`scripts/remote_launchers/run_ch3_protocol_a_raw83_prepare_v1.sh`。
- 官方快照：`vendor/rtdl_revisiting_models/LICENSE`、`vendor/rtdl_revisiting_models/lib/data.py`、`vendor/rtdl_revisiting_models/manifest.json`。
- 交付记录：`.Codex/docs/RWKV/2026-08-22-协议A-Raw83共享预处理实现报告.md`。
- 不修改、不暂存、不回滚上述范围外的工作树改动。

## 实现阶段错误记录

- 本机 `uv run --no-sync` 导入 NumPy 失败：当前工作树未安装锁文件依赖。处置为按 `uv.lock` 与官方文档实现，本轮只做语法、导入入口和静态合同验收；服务器真实物化前仍须机械核对实际版本。
- Context7 的 PyArrow 库查询返回空输出；未静默回退为凭记忆实现，改用官方 Arrow/NumPy/scikit-learn 文档与现有锁定环境签名双重核对。

## `c581a12` 唯一修订复审计划

| 优先级 | 阻断发现 | 修订动作 | 状态 |
| --- | --- | --- | --- |
| P0 | 旧 `I/M/E/T` 与切分制品只计数和现场哈希，没有冻结期期望 SHA | 源配置强制登记 `I/M/E/T`、两组行、验证实体成员与训练有效流位图 SHA；P2 逐项比对 | 已完成 |
| P0 | P1/P5 批收据只验重算批，没有验已写目标切片；多文件逐个改名可暴露半发布 | 每批重读目标切片比对 SHA；阶段目录整体发布并原子更换当前指针 | 已完成 |
| P0 | 源配置含 LSPR24 路径，P6 无独立生产身份 | 源三文件对 LSPR24/目标路径零字符串；新增独立目标配置与启动器，完整封印后才生成目标产品 | 已完成 |
| P0 | 状态哈希受绝对路径污染，消费者未自验清单内容 SHA | 状态身份只保留逻辑键、内容 SHA、模式和算法；构造加载器时必验清单内容 SHA | 已完成 |
| P0 | 有限禁止文件黑名单可漏过未知标签/预测制品 | 发布后只允许完整制品白名单和明确恢复制品 | 已完成 |
| P0 | 5 GiB 制品可被重复哈希，标签解析器可每批重放 CSV，P2 缺少严格权重聚合收据 | 每进程以文件身份缓存一次哈希；标签最多顺序解析一次并驻留 `int8`；P2 输出训练流/序列权重聚合 | 已完成 |
| P0 | `float64 -> float32` 溢出可在规范化后留下非有限值 | 转换后再执行有限检查，归一 NaN 并单独计数 | 已完成 |
| P1 | P5 成功后仍保留 B 臂两个大型临时映射 | 视图和清单封印后按精确清单删除临时映射；失败保留 | 已完成 |
| P1 | 源/目标入口的资源、恢复和年度隔离尚未成对 | 两份配置、工具和两个启动器共享 cgroup/磁盘/断点门，但使用独立身份和制品根 | 已完成 |

### 修订文件边界

- 继续拥有 `c581a12` 的十个文件。
- 只可新增 `configs/ch3-protocol-a-raw83-target-v1.json` 和 `scripts/remote_launchers/run_ch3_protocol_a_raw83_target_v1.sh`。
- 不访问服务器或真实数据，不创建测试，不运行 `black`。

### 修订验收

1. 最终只执行一组 Python 语法/导入/帮助/双配置门、两个 `bash -n`、官方快照哈希、源三文件目标字符串零命中和差异检查。
2. 真实冻结 SHA 未由主进程提供时，保持显式外部阻断，不伪造或以运行时首见哈希代替。

## 最后针对性修复

- [x] P1/P5 按单文件收据独立选择、重建或补完混合 `final/partial` 状态。
- [x] 已存在世代幂等补建当前指针、P5/P6 收据、`sealed` 状态和完成状态。
- [x] P6 全部源资格值由配置外部冻结，不再从封印自取期望值。
- [x] 上游收据以去路径规范语义摘要进入阶段身份。
- [x] 源清单发布 P2 训练权重聚合；年度清单发布 Raw83/胜出视图无头内容 SHA 和源变换状态 SHA。

## 验证记录

- `git diff --check`：通过。
- 附录 A 与 `dijk_fields.DIJK_FEATURES` 自动逐行对比：零差异。
- 字段表机械检查：83 项、索引 0 至 82 连续、字段名无重复。
- 容量公式复算：LSPR23/LSPR24 单视图分别为 `5.056491/6.254280 GiB`，推荐方案特征总载荷 `27.678033 GiB`，与正文四舍五入值一致。
- 占位词扫描：无 `TODO/TBD/FIXME/NotImplemented`。
- Markdown 相对链接：本目录没有相对链接，因而没有失效链接目标；官方源码使用固定完整 URL。
- 未运行 `black`、其他格式化器、生产代码、真实物化、服务器或实验。
