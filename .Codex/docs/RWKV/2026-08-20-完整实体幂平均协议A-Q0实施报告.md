# 完整实体幂平均协议 A Q0 实施报告

## 结论

已按提交 `69e6fbe` 的冻结计划实现协议 A Q0 单入口。本轮只新增工具、配置、启动器和本报告；未访问服务器，未启动实验，实际效果为**实验待证**。

实现代理：`literature_hybrid_search_eval_sol_high`，模型 `gpt-5.6-sol`，`effort=high`。

## 变更文件

1. `thesis/experiments/llm_probe/tools/ch3_full_mlp_complete_entity_lp_protocol_a_q0.py`
   - 冻结深度 8、宽度 512、`2,144,258` 参数的全容量 MLP。
   - 训练 B00/B10/O01/O11 四单元，每单元跑满 20 轮，保存逐轮权重、完整恢复状态和选中检查点。
   - B00/B10 只使用训练实体可达标签计算逐流主损失权重；O01/O11 另加旧片段辅助损失。
   - 从 B00/B10 同一份逐流分数派生 max、mean 和 N01/N11 有限幂平均，记录分数与流身份哈希。
   - 在 LSPR23 验证完整实体上选 `p={0.5,1,2,4,8}`，完成逐流 AP/ROC-AUC、实体 AP、max AP、六档 DR、`0-8%` 完整曲线及面积/占优/交叉、五长度桶和资源收据。
   - 源年门否决时直接结束，`target_year_arrays_read=0`；通过后才加载 LSPR24 一次，每个训练单元前向一次，同分数派生全部目标指标。
2. `thesis/experiments/llm_probe/configs/ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1.json`
   - 冻结骨干、协议 A 预算、四单元、幂网格、六档 DR、长度桶、资源门、证据身份和零初始目标年读取。
3. `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_full_mlp_complete_entity_lp_protocol_a_q0_seed42_v1.sh`
   - 提供唯一首次启动与同身份 `--resume`，包含静态合同、源年输入、SwanLab、GPU/主存/磁盘、锁、日志、资源采样和制品后验收。
   - 启动前只枚举源年六数组；目标年数组是工具通过源年门后才能达到的分支。
4. `.Codex/docs/RWKV/2026-08-20-完整实体幂平均协议A-Q0实施报告.md`
   - 本报告。

## 关键边界

- 禁止的三折、OOF 和嵌套逻辑均未实现。
- 候选 2 未并入；B10/O11 仍使用当前自包含 CPA。
- N01/N11 不是训练单元，不反传实体梯度。
- 同一 LSPR23 验证集同时选 epoch、选 `p` 和描述源年指标，存在选择偏差；不做显著性、置信区间或泛化主张。
- 不持久化逐流或逐实体分数；聚合恢复时从封印检查点重生成。

## 资源合同

- 运行身份：`ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1`。
- 预估四单元训练墙钟：15–25 分钟。
- 计划峰值：GPU 约 9 GiB，主存约 8 GiB；以真实收据为准。
- 启动门：GPU 空闲显存 12 GiB、控制组可用主存 30 GiB、可用磁盘 10 GiB。
- 并发度：1。

## 第三方接口

未引入新依赖或新接口形态。实现只复用已完成真实运行的协议 A 工具中 PyTorch、NumPy、scikit-learn 和 SwanLab 同构调用，未触发第三方 API 新鲜度查询。

## 验证

本轮按用户限制只集中执行一次：Python 语法编译、模块导入、`--help`、`--validate-config` 和 Bash `-n`。未运行 `black`、单元测试、集成测试、冒烟实验或真实数据子集。

| 检查 | 结果 |
| --- | --- |
| `py_compile` | 通过，退出码 0 |
| 模块导入 | 通过，输出 `IMPORT_OK` |
| `--help` | 通过，列出配置、恢复、发布和 SwanLab 授权入口 |
| `--validate-config` | 通过，输出“配置核验通过” |
| `bash -n` | 通过，退出码 0 |
| 撤销路径扫描 | 三个生产文件对 `fold/OOF/nested` 零命中 |

三个可执行输入的 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| `tools/ch3_full_mlp_complete_entity_lp_protocol_a_q0.py` | `45ad7d1be85832e4b235a8942615e4aad7b94c154ec2cef7ad00ec2510feb2da` |
| `configs/ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1.json` | `f13ddd96d8a09b122004fb2c281cedb4411f03420a0eddc04cfc03b46f943f34` |
| `scripts/remote_launchers/run_ch3_full_mlp_complete_entity_lp_protocol_a_q0_seed42_v1.sh` | `437eadb76ab7e5901091b018ac1dcdf71b66bf5e4bc9bc7d0027f6a140e5e95d` |

## 唯一实验入口

后续只有在服务器、输入、依赖、SwanLab 目的地、GPU 时长和费用获得当轮确认后，才允许执行：

```bash
bash scripts/remote_launchers/run_ch3_full_mlp_complete_entity_lp_protocol_a_q0_seed42_v1.sh
```
