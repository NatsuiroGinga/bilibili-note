# 全注意力与门控循环 2.14M 容量重锚消融预注册合同

日期：2026-08-27。状态：**预注册，未启动**。触发条件：协议 B（90k）重跑结果出来后由用户
拍板是否执行；不自动启动。运行身份（届时启用）：`ch3-backbone-2x2-rerun-capacity-anchored-seed42-v1`。

## 一、动机与风险（都写在前面）

- **动机**：等参数池的锚点已过期——原池按 90,242 参数旧 MLP 配平，现主载体是
  `2,144,258` 参数全容量 MLP（8×512），90k 骨干只有主载体 1/24 容量，四格比较落在
  退化区间（90k 全注意力 C00 仅 `0.0917`）。重锚使跨骨干比较回到与主载体同容量档。
- **风险（实测反向证据，如实登记）**：FT-Transformer 在 998k 容量下两机制均转负
  （ELP `−0.1891`、CPA `−0.1451`）——容量升高可能进入逐流判别饱和区，机制增益反而消失。
  本轮是检验，不是保票。

## 二、容量匹配（魔法数字台账，2026-08-27 主进程精确解）

目标参数量 = 主载体冻结值 `2,144,258`。参数公式沿用协议 A-v2 注释登记式
（`ch3_backbone_protocolA_v2.py:318-329`），逐宽度枚举取最小偏差：

| 骨干 | 公式 | 解 | 参数量 | 偏差 |
| --- | --- | --- | --- | --- |
| 门控循环 | `5h²+(3D+8)h+2`（D=83） | `h=630` | `2,146,412` | `+0.100%` |
| 全注意力 | `14d²+(D+16)d+2`（ff=4d） | `d=388`，`heads=4`（head_dim=97） | `2,146,030` | `+0.083%` |

- 头数由 7 改 4：`d=388` 在 4–16 范围内仅被 4 整除；保持 7 头需 `d=385`（偏差 `−1.445%`，
  超出原池 ±1% 惯例）。头数变更属任务化设定，无直接文献依据，以参数量对平为先。
- 偏差均小于原 90k 池的既有偏差上限（CNN `+0.931%`），配平严于旧池。

## 三、协议（与协议 B 完全一致，仅容量与身份不同）

- 两项预注册修订原样继承：选轮指标 = LSPR23 验证**实体 AP**（并列取最早轮、无早停，
  逐流 AP 双记录不参与选择）；学习率网格 `{2e-3, 3.162277660168379e-4}` 只在 C00 上选、
  冻结后四格齐用。
- 训练预算不变：seed42、批 64、序列 128、20 epoch × 1000 步——与主载体同步数预算。
- 精度：`cuda-bf16-amp-fp32-sensitive-v1` 统一 profile。
- 四格语义：C10=仅 CPA、C01=仅 ELP；LSPR24 于四格源年选择全部封印后每格恰好一次。

## 四、判据（预注册，写死，与 U6/协议 B 相同）

`C01 > C00` 且 `C10 > C00` 且 `C11 ≥ max(C01, C10)`（LSPR24 实体 AP）。
任一不过即该骨干在该容量档定案，不再追调；**不得**以「换个容量再试」无限延伸——
本合同是容量轴上的最后一轮，跑完后无论结果，跨基座纳入清单即冻结。

## 五、资源估算（依据实测）

2.14M 档与主载体同量级：主载体实测单格训练约 6 分钟（`337.8s`，RTX 5090 BF16）；
Transformer/GRU 因注意力与递归结构预计 1.5–3 倍 → 单格 10–20 分钟，
10 个训练单元（2 骨干 × [2 lr 选择 + 3 剩余格]）合计约 2–3.5 GPU 小时。
显存：主载体峰值远低于 32GB，2.14M 档无风险；可与其他运行并发（收据记
`resource_contention=true`）。

## 六、正文边界

失败骨干与一切限定语不进正文（用户 2026-08-27 裁决）；通过判据的骨干才进入
跨基座机制增益呈现。本合同本身不产出正文文字。
