# XGBoost＋CPA-ELP C11 运营指标零训练回填实施报告

## 结论

已完成独立生产入口、冻结配置和远程持久启动器。实现只为既有 XGBoost＋CPA-ELP C11 回填目标年运营制品，不训练、不重选、不执行三折或折外评价，不建立 `raw83` 评价分支，也不修改父运行、旧 continuation、统一总表或任何既有运行根。

当前状态为 **实现 READY，真实运行待主进程授权与执行**。本任务按要求没有访问服务器，因此尚无实际六档 DR、完整曲线或首次告警数字，不能将实现完成写成实验结果完成。

## 新增文件

- `thesis/experiments/llm_probe/tools/ch3_xgb_cpa_elp_operational_backfill.py`
- `thesis/experiments/llm_probe/configs/ch3-xgb-cpa-elp-operational-backfill-v1.json`
- `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_xgb_cpa_elp_operational_backfill_v1.sh`
- 本报告

计划与证据笔记已先以提交 `8773c6a` 独立保存。

## 冻结身份

| 项 | 实现门禁 |
| --- | --- |
| 父训练运行 | `ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1` |
| 父完成评价 | `ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1-eval-continuation2` |
| 模型 | `model_semantic168.json`，800 树 |
| 完整模型 SHA-256 | `1805e15d96ac4ebb57df910640a4c5e1af58f692659db2eb80b724851efe42e9` |
| 适配器与幂指数 | `semantic168`，`p=1.0` |
| 新运行标识 | `ch3-xgb-cpa-elp-c11-operational-backfill-v1-rerun1` |
| 新展示名称 | `XGBoost＋CPA-ELP C11目标年完整运营指标零训练回填` |
| 目标隔离 | `target_retrained=false`、逻辑 `target_score_calls=1`、`target_scores_persisted=false` |

输入门同时固定父选择收据、11 项有效配置收据、父恢复证明、父完成评价结果与清单的已知摘要。父训练未完成的历史事实由独立恢复证明保留，父完成评价不能反向伪装成父训练原生完成清单。

## 数据与评价实现

1. 输入白名单严格等于 `X24/y24/I24/M24/s24/d24/t24`。工具先对七个缓存计算并记录完整 SHA-256，再各加载一次。
2. `X24＋I24＋M24` 只在内存重建一次 `semantic168`。实现复现父 continuation 的 83 个当前字段、76 个数值残差、5 个类别前缀同值频率、2 个布尔前缀真值率与 2 个支持量。
3. 冻结最终模型实际加载后核验 XGBoost 3.2.0、CUDA 构建、`cuda:0` 与 800 树，再执行一次逻辑目标打分。分批 `inplace_predict` 次数只作为同一次逻辑调用的工程明细记录。
4. C11 最终实体分数是 `p=1` 的全曝光算术平均。完整预算曲线从零误报边界开始，对每个负实体同分组只保留一个完整可达点。
5. 六档 DR 从 `realized_fpr` 不超过名义预算的最近可达点读取，记录阈值、实际 FPR、误报实体数和并列组大小，不插值名义点。
6. 首次告警按实体内冻结流索引升序，第一条合法流记为曝光 1；在线 C11 分数是前缀算术平均。每档使用同一完整并列阈值，输出实际首次告警 FPR、恶意实体未告警率、`minimum/p25/median/p75/p90/p95/p99/maximum` 与右连续按时检出曲线。
7. `t24` 同时用于序列内和实体内时间非降失败门，但不解释为完整流 `available_ns`；所有制品明确 `time_delay_available=false`。

## 制品与恢复

计算阶段原子写入：

- `input-validation-receipt.json`
- `target-year-metrics.json`
- `complete-alert-budget-curves.npz` 及对应收据
- `first-alert-timing-curves.npz` 及对应收据
- `compute-complete-receipt.json`

发布阶段在资源采样结束后写入：

- `resource-receipt.json`
- `swanlab-receipt.json`
- `operational-backfill-receipt.json`
- `stage-status.json`
- 最后生成 `manifest.json`

`compute-complete-receipt.json` 与最终 `manifest.json` 都逐文件核验字节数和 SHA-256。中断后只允许 `--resume`：计算收据完整时跳过重算，最终清单完整时只做幂等自检；已有收据与制品不一致则拒绝覆盖。逐流分数、逐实体分数、逐实体首次告警位置、派生矩阵和新模型均不得持久化。

## 资源门

- GPU 空闲显存至少 11 GiB。
- 控制组可用主存至少 48 GiB。
- 项目盘可用空间至少 10 GiB，使用率须低于 80%。
- 同任务计算单元最多 1 个。
- 不设置墙钟上限。

启动器保存五秒资源采样、峰值 GPU 使用、峰值控制组主存与并发污染标记。资源收据完成后才允许最终 manifest 固定其哈希。

### 首次运行资源门失败与修复

首次运行 `ch3-xgb-cpa-elp-c11-operational-backfill-v1` 保留为独立工程失败，不覆盖、不改写。启动器状态为 `failed/controller/exit_code=11`；资源日志确认控制组有效可用主存为 88.80 GiB，高于门禁所需 62.40 GiB，但并发扫描把启动器自己的 `tee` 进程识别为另一个 `llm_probe` 任务。

根因是旧调用顺序先在 `worker_entry` 建立 `worker 2>&1 | tee`，随后才由管道内的 `worker` 调用 `admit_resources`。修复仅改变这一顺序：`worker_entry` 先持有工作锁并完成原资源门，成功写入资源收据后才创建控制器 `tee` 管道；`worker` 只核验并复用该收据，再启动资源监控。没有设置 `ALLOW_CONCURRENT=1`，没有放宽 GPU、控制组主存、磁盘或真实并发门，也没有忽略任何外部进程。修复运行使用新身份 `ch3-xgb-cpa-elp-c11-operational-backfill-v1-rerun1` 和屏幕名 `ch3-xgb-c11-op-backfill-r1`。

## 验证结果

以下检查均已通过：

- `uv run --no-sync python -m py_compile tools/ch3_xgb_cpa_elp_operational_backfill.py`
- 无 NumPy 环境下的模块导入检查
- `uv run --no-sync python tools/ch3_xgb_cpa_elp_operational_backfill.py --help`
- `uv run --no-sync python tools/ch3_xgb_cpa_elp_operational_backfill.py --config configs/ch3-xgb-cpa-elp-operational-backfill-v1.json --validate-config`
- 配置 JSON 解析与完整模型 SHA、七数组、零训练、单调用、零分数持久化静态身份检查
- `rerun1` 的工具、配置、输出根、启动器与屏幕身份一致性检查
- 静态分支断言：`admit_resources` 在控制器 `tee` 前执行，`worker` 仅复用资源收据后启动监控，启动器不含 `ALLOW_CONCURRENT`
- `bash -n scripts/remote_launchers/run_ch3_xgb_cpa_elp_operational_backfill_v1.sh`
- `git diff --check` 限定文件检查

本机默认 `uv` 缓存目录受沙箱限制，验证改用 `UV_CACHE_DIR=/tmp/codex-uv-cache`。当前无同步环境没有 NumPy，因此工具将 NumPy 与 scikit-learn 延迟到计算阶段；这不改变服务器冻结运行环境。

仓库禁止人工夹具、单元测试、集成测试和自动格式化，本轮未运行这些检查，也未运行 `black`。没有执行真实数据计算或服务器命令。

## 生产文件 SHA-256

- `990393ca08889eef1d1d1a834b7976f695322841cdd972edb96d46674f1a0fec`：`ch3_xgb_cpa_elp_operational_backfill.py`
- `9deaa7f260d5ddb24c611f4f0d30b49289731468e9a3b119e6834223c0831649`：`ch3-xgb-cpa-elp-operational-backfill-v1.json`
- `2d788aead867eb1ca6ee77821366d6f4d1057650121848ba8b6da12ce2891c0e`：`run_ch3_xgb_cpa_elp_operational_backfill_v1.sh`

上述摘要对应实施报告撰写时的生产文件；提交前再次核验，若文件发生变化须同步更新。

## 第三方接口边界

本实现只复用仓库中已有且同版本运行过的 `Booster.load_model`、`Booster.num_boosted_rounds`、`Booster.set_param`、`Booster.save_config`、`Booster.inplace_predict` 和 SwanLab 聚合上报调用，没有引入新的第三方接口或猜测属性，因此未触发首次接口的文档查询门。PyTorch 只用于既有 CUDA 可用性和显存检查，不新增或修改模型、训练循环、混合精度、数据加载器或检查点，故不触发 PyTorch 训练模式技能。

## 唯一运行命令

在远程项目根执行：

```bash
bash scripts/remote_launchers/run_ch3_xgb_cpa_elp_operational_backfill_v1.sh
```

启动器自动识别未开始、计算完成待发布和最终完成三种状态，不接受外部参数，也不覆盖既有完成制品。

## 阻断与未变更范围

- 阻断：真实父制品、七个缓存、CUDA、资源门或 SwanLab 前置门任一不通过即停止；当前未在服务器核验这些条件。
- 未变更：`ch3_xgb_cpa_elp_eval_continuation.py`、父模型、父选择、统一总表代码与来源配置、第三章恢复卡、路线总控、现有运行目录和所有其他代理文件。
