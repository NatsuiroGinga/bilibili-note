# 第三章 FT 四格进度与错误记录（供 Codex 查验）

<!-- RESEARCH_ROUTE=RWKV -->

- 日期：2026-08-31
- 用途：一次性交代 CEM-BER 四格的当前实测状态、口径边界、本轮工程变更，以及主代理在
  2026-08-29 至 08-31 期间犯下的全部错误。写给接手的 Codex 会话查验，不是论文材料。
- 状态：**四格未齐，判据不可评估**。本文件不含任何有效性结论。

## 一、四格实测（源年 LSPR23 验证集实体 AP，`target_reads = 0`）

数据源为本机回传副本 `runs/diagnostics/<run_id>/receipts/selection.json`，
经 `tools/ch3_ft_emit_four_cell_summary.py` 汇总到
`runs/diagnostics/ch3-ft-four-cell-summary.json`。

| 格 | 机制 | 源年实体 AP | 最佳轮 | 对 C00 | 墙钟 | 状态 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| C00 | 裸 FT | `0.7887951742121221` | 17 | — | 50.5 分 | `finished/exit 0` |
| C10 | ＋因果实体记忆 | `0.8150313407035376` | 17 | `+0.0262361665` | 25.5 分（续训） | `finished/exit 0` |
| C01 | ＋预算感知实体排序 | `0.9537273846736761` | 17 | `+0.1649322105` | 123.8 分 | `finished/exit 0` |
| C11 | 两机制 | 未完成 | — | — | — | 服务器关机中断 |

三格的最佳轮同为第 17 轮。C11 中断时 `state=running` 且**没有 `inflight.pt`**，
崩在首个检查点之前，须整臂重跑。

**判据当前进展**：`C10 > C00` 成立，`C01 > C00` 成立；
`C11 ≥ max` 与交互 `I = C11 − C10 − C01 + C00 > 0` 均不可评估。
按仓库规则，此状态下不得写任何机制有效性结论。

### 双选轮协议读数

| 格 | 实体规则选中轮 | 该轮实体 AP | 逐流规则选中轮 | 该轮实体 AP | 两规则实体 AP 差 |
| --- | ---: | ---: | ---: | ---: | ---: |
| C00 | 17 | `0.7887951742` | 16 | `0.6538549422` | `+0.1349402320` |
| C10 | 17 | `0.8150313407` | 20 | `0.7747666536` | `+0.0402646871` |

即按逐流 AP 择优选轮，C00 的实体 AP 损失 `13.49` 个百分点。
两格的逐流最佳轮不同（16 与 20），实体最佳轮一致为 17。
早期文档中的「相差 `16.4` 个百分点」建立在错误的第 19 轮上，已作废。

### C00 逐轮波动（如实记录，不作分析建议）

后 10 轮实体 AP 均值 `0.6073`、标准差 `0.0843`、极差 `0.3380`，
第 17 轮的 `0.7888` 是曲线上的孤立高点。
按路线总控第三节「第三章按单次运行报告且不评冠军」，不追加运行、不做多种子。
选轮规则已于 `cf2cf65` 冻结在实验之前。

## 二、口径边界（跨口径比较一律无效）

### 2.1 XGBoost 没有源年实体 AP

用户 2026-08-31 问及 XGBoost 的源年读数，核验结论是**不存在**。
依据 `2026-08-26-XGBoost同数据集论文配方来源审计/审计报告.md` 第 35 行：
全部 LSPR23 `16,353,511` 流一次训练到底，**不留源年验证集**，训练结束后一次读取 LSPR24。

XGBoost 侧的全部实体 AP 均为**目标年 LSPR24**：

| 方法 | 目标年实体 AP | 实体聚合口径 | 制品 |
| --- | ---: | --- | --- |
| plain XGBoost | `0.512898847990404` | 最大池化 | `runs/diagnostics/ch3-baselines-full/trees_results.json` |
| XGBoost＋CPA-ELP C11 | `0.5650784187138617` | CPA-ELP 前缀算术均值 | `ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1-eval-continuation2/xgb_cpa_elp_results.json` |
| XGBoost＋CPA-ELP C11 | `0.5303760126246202` | 最大池化 | 同上 |

FT 四格的源年读数与上表**不可比**，理由有二：年度不同；XGBoost 侧不存在源年验证集这一对象。
要比较必须等 FT 的 LSPR24 描述性评价产出，且两侧统一取最大池化口径
（`2026-08-21-已发表神经基线运营指标严格分析.md` 第 440 行已指出同列混装两种聚合会误导）。

### 2.2 FT 自身的三条数值口径互不通用

| 口径 | C00 读数 | 说明 |
| --- | ---: | --- |
| 旧路径源年（`torch.compile` 关闭） | `0.7380` | 已被 compile 重跑取代，只作追溯 |
| compile 路径源年 | `0.7887951742121221` | 当前四格判据的唯一有效口径 |
| 目标年描述性评价（历史 FT） | `0.7508` | 仅用于判断重训未退化 |

### 2.3 `torch.compile` 覆盖不完整（预注册协议偏离，用户已裁决不重跑）

服务器实测（PyTorch `2.13.0+cu130`）：

```
c.encode.__self__ is m   → True    # encode 绑定回原模块，绕过编译
c.forward is m.forward   → False   # 只有 forward 被替换
```

`torch.compile` 只编译 `forward`。C00／C01 通过 `model(...)` 走编译路径；
C10／C11 走 `model.encode／inject／predict`，**实际未编译**。
四格数值路径因此不一致，差异量级为已实测的 `8.94e-07`（远小于实体 AP 的轮间波动）。
旁证：C00 步速 `6.915` 步/秒对 C10 的 `4.933`。

该问题由目标年前向实现代理在读码时发现，主代理在服务器复测确认。
**用户 2026-08-30 裁决：如实记录偏离，不重跑。**
正文与恢复卡须写明这一点，不得声称四格严格同数值路径。

## 三、本轮工程变更

| 提交 | 内容 |
| --- | --- |
| `199c6dd` | 新增 `tools/ch3_ft_emit_four_cell_summary.py`，每臂结束自动把四格读数与判据写入固定路径 JSON；接入四格守护脚本 |
| `be4b17d` | 修复回传脚本：改用 `expect` 运行 expect 脚本、不再吞 stderr、区分「远端不存在」与「传输失败」 |
| `ca80b60` | 回传参数改经环境变量传入；轻量轮改整目录 rsync 加 `--max-size=5m`；新增周期回传守护 `watch_and_pull_ch3_ft.sh` |
| `68e2197` | 实现 `ch3_ft_lspr24_descriptive_eval.py` 的目标年前向（代理交付，14 项实现验证通过） |
| `3344e0f` | 排序阶段前向改用梯度检查点，修复 C01 的 CUDA 显存溢出 |
| `da14915`／`7e05ee5`／`8dc90fb` | 恢复卡、四格进度、正文骨架台账三份文档同步 |

### 3.1 C01 显存溢出与修复

C01 首次启动即 `torch.OutOfMemoryError`，申请 394 MiB 时仅余 389 MiB，进程已占 `30.97 GiB`。
GPU 当时无其他进程（实测 `0 MiB` 空闲），故非争用所致。

根因：`_entity_ranking_bare_forward` 虽已按 `micro_batch_sequences = 64` 分批前向，
但把每个微批的输出连同完整反向图累积到列表最后 `cat`，激活随微批数线性增长。
实体排序批规模由 `n_pos + n_neg = 66` 个实体乘每实体至多
`ceil(truncate_length / sequence_length) = 64` 段决定，实测达 6403 序列、即 100 个微批。
CVaR-pAUC 损失要求全体实体分数同时在同一张图中，无法分批反传。

修法：对每个微批的纯计算段套 `torch.utils.checkpoint.checkpoint(..., use_reentrant=False)`，
前向只保留微批输出，反向重算中间激活。超参数、损失定义、采样口径与数值语义全部不变。

实测效果：峰值显存 `30,970 MiB` → `15,175 MiB`；代价是步速 `6.9` → `2.6` 步/秒，
C01 单臂墙钟 123.8 分钟。

C11 的记忆路径已做同样处理，但把 `scratch_memory_state.read／write` 留在检查点之外，
段内只消费已读出的张量，重算时不会重复写状态。
**该修复尚未推送到服务器**——推送时连接中断，开机后必须先推，否则 C11 会同样 OOM。

这套 BER 配置此前只在本机 MPS 上验证过 20 批，从未在 CUDA 上运行。

### 3.2 检查点体积构成（已核实，非缺陷）

C10 的 `inflight.pt` 为 `810.85 MiB`，其中 `mechanism_state` 占 `794.32 MiB`：
`entity_memory` 的 `queue` 是 `(134997, 7, 192)` float32 = `692.12 MiB`，
`mean` 是 `(134997, 192)` float32 = `98.88 MiB`。模型权重本身只有 `4.12 MiB`。
即检查点的 `99.5%` 是每实体记忆状态。C00 无此项，故只有 `18.6 MiB`。

### 3.3 制品去向

C00、C10、C01 的完整制品已回传本机 `runs/diagnostics/`；
C00 另已上传私有 HuggingFace 仓库 `Heehobino/cember-ft-lspr23-ch3` 的 `c00-bare-ft/` 路径
（13 个文件，含两份选轮权重）。**该仓库为私有，不得按公开制品引用。**
C11 无制品。

## 四、主代理错误记录

以下八项按严重程度排列。前六项同一根因：**提交前没有真实执行过，
`bash -n` 与 `py_compile` 全部放行**。

| # | 错误 | 后果 | 处置 |
| ---: | --- | --- | --- |
| 1 | 回传脚本用 `bash` 运行 `#!/usr/bin/expect` 的 Tcl 脚本，同时把 stderr 重定向到 `/dev/null`、退出码被管道掩盖 | 三重掩盖使一次必然失败的回传显示为「远端尚无该项，跳过」，回传实际一次未成功 | `be4b17d` 修复，改 `expect -f`、保留 stderr、区分错误类型 |
| 2 | 给目标年前向代理写的规格第 1 条方向写反：照抄了为表格 ResNet 写的 docstring 警告「不能读 X24.npy」 | FT 训练在 dijk 标准化缓存的 `X23.npy` 上，目标年必须用同管线的 `X24.npy` 才同源。照原规格实现会产出无效读数 | 代理实测挡下并报告冲突，主代理裁决改正，docstring 已改写留痕 |
| 3 | compile 路径 C00 训完只把「最佳轮 = 第 17 轮」抄进文档，漏记实体 AP 数值本身 | 服务器关机后取不回，基线门槛缺失 | 复机后取回，并写 `ch3_ft_emit_four_cell_summary.py` 自动落盘 |
| 4 | C10 进度记成「第 3/20 轮」，实际关机前已跑到第 15 轮 | 时间估算错约一倍；`0.3935/0.5096/0.6429` 是前三轮读数被误当成中断时最新状态 | 复机后由日志更正 |
| 5 | 回传脚本对 4 个臂逐项拉取，36 次 SSH 握手，单轮超过 2 分钟 | 用作周期守护时每轮都超时 | 改整目录一次 rsync 加 `--max-size=5m`，实测降到 6 秒 |
| 6 | `expect -c 'script' a b` 不填充 `argv` | 参数传不进去，`lrange $argv` 报错 | 改经环境变量传参 |
| 7 | 写 300 行脚本未调用 `daily-coding` 与 `pytorch-patterns` 技能 | 违反仓库技能编排规则，也是第 1、5、6 项的直接原因；同一轮还要求子代理必须调用这两个技能，属双标 | 已补调 `daily-coding` 并按其清单复查 |
| 8 | 派发子代理未标注模型档位 | 违反已有记忆条目与仓库子代理规则 | 已补标；后续派发均标注 |

### 4.1 已机械化的防复发措施

第 3、4 项的根因是「关键读数靠人工转录」，第 1、5、6 项的根因是「靠静态检查代替真跑」。
两者都已转为程序动作：

- `tools/ch3_ft_emit_four_cell_summary.py`：从运行收据自动提取四格读数、逐轮历史与判据，
  写入 `runs/diagnostics/ch3-ft-four-cell-summary.json`，接在守护脚本的启动时与每臂结束时；
- `scripts/remote_launchers/watch_and_pull_ch3_ft.sh`：本机周期回传守护，每 5 分钟拉轻量制品，
  臂完成时自动拉一次含检查点的全量并打 `.pulled-full` 标记，四格全完成即退出；
- 两条记忆条目：`syntax-check-is-not-execution`、`make-it-mechanical-not-remembered`。

### 4.2 协作代理报备的一项违规

文档代理在一次批量替换中使用了 `sd`，而根 `AGENTS.md` 规定手工修改文件只用补丁工具。
替换内容已用 `rg` 逐条复查正确，产出无差异，该代理其余改动均改用补丁工具。
主代理裁决不回滚。此处如实记录以备查验。

## 五、当前阻塞与开机后动作

### 阻塞

1. 服务器不可达（端口连测三次 CLOSED），C11 中断且无 `inflight.pt`，须整臂重跑。
2. C11 的记忆路径 OOM 修复（提交 `3344e0f`）**尚未推送到服务器**。
3. 目标年前向已实现但四格未齐，工具预检会正确拒绝执行。
4. 正文未起草（用户明确要求实验跑完前不写正文）。

### 开机后动作序列

```
# 1. 先推送 OOM 修复，否则 C11 会重蹈 C01 的覆辙
rsync tools/ch3_ft_c00_dual_selection.py <远端>/tools/

# 2. 重跑守护脚本，它会跳过已完成的 C00/C10/C01，只跑 C11
cd /root/autodl-tmp/thesis/experiments/llm_probe
setsid nohup bash scripts/remote_launchers/run_ch3_ft_four_cell_serial.sh \
  > runs/four-cell-serial.log 2>&1 < /dev/null &

# 3. 本机启动回传守护（凭据从环境变量传入，不写盘）
nohup bash scripts/remote_launchers/watch_and_pull_ch3_ft.sh > runs/watch-and-pull.log 2>&1 &
```

C11 预计墙钟按 C01 的 123.8 分钟起算，因叠加 CEM 可能更长。

### 四格齐备后

1. `ch3_ft_lspr24_descriptive_eval.py --dry-run` 核验封印与判据
2. 同工具全量运行，产出 LSPR24 描述性读数（`X24` 整载约 6.7 GiB，八次全量前向）
3. `results-analysis` → `results-report`
4. 按论断台账填正文 → `citation-verification` → `writing-anti-ai` → `paper-self-review`

## 六、给 Codex 的查验入口

- 四格读数机器可读汇总：`runs/diagnostics/ch3-ft-four-cell-summary.json`
- 逐格原始收据：`runs/diagnostics/<run_id>/receipts/selection.json`
- 恢复卡：`RWKV第三章恢复卡.md`（已同步至 `8dc90fb`）
- 四格进度：`2026-08-28-CEM-BER四格实验进度.md`
- 正文骨架与论断台账：`2026-08-29-第三章CEM-BER正文骨架与论断台账.md`
- XGBoost 口径依据：`2026-08-26-XGBoost同数据集论文配方来源审计/审计报告.md` 第 35、37、64 行

事实冲突时以原始收据和运行日志为准，本文件次之。
