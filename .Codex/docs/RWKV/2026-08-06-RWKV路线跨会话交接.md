# RWKV 路线跨会话交接

<!-- RESEARCH_ROUTE=RWKV -->

- **状态时间**：2026-08-06
- **仓库根目录**：`/Users/bilibili/personal/note`
- **实验工程**：`/Users/bilibili/personal/note/thesis/experiments/llm_probe`
- **当前总状态**：`G0-D=NOT_READY`、`G0-F=NOT_READY`
- **当前 GPU 需求**：不需要 GPU
- **正式全量进程**：未启动，无 PID、无运行目录、无正式物化制品

## 1. 新会话恢复顺序

新会话必须设置研究路由为 `RESEARCH_ROUTE=RWKV`，随后按顺序完整读取：

1. `.Codex/docs/RWKV/AGENTS.md`
2. `.Codex/docs/RWKV/RWKV路线总控.md`
3. 本交接文档

当前会话是 PINN/R2 之外的 RWKV 替代路线。默认不要读取 `output/开题改进交接文档.md`、`output/第一创新点实验总控.md` 或 R2/PINN 过程文档，也不要把本路线状态写回那两份总控。只有用户明确决定 RWKV 替换论文主线或要求跨路线比较时才迁移。

新会话不得假定本会话的子代理仍可访问。先查询实际代理状态，再只读核对文件、日志和运行目录；没有 PID、状态文件和日志就不能声称实验已启动。

## 2. 当前研究合同

1. 研究任务是长时网络攻击状态检测，不宣称逐包、逐连接或加密流量检测。
2. 当前唯一主数据集是 LSPR24。UGR'16 已否决为主监督入口，只保留历史筛选证据。
3. 正式骨干与主公式固定为 RWKV-7；RWKV-8 仅作未来兼容候选。
4. 第三章候选仍有两套，尚未由实验裁决：
   - 方案 A：受序动态记忆与正常状态生成；
   - 方案 B：字段交互与动态状态演化。
5. 数据合同已经拆为：
   - `G0-D`：只处理前 80% 的四段开发区；
   - `G0-F`：方法、字段、阈值、困难面板和超参数全部冻结后，才实施严格封存并一次性评价最终 20%。
6. 当前禁止访问、持久化、统计、评价或向模型提供最终 20% 的标签、特征、成员、排除原因、计数和哈希。

冻结合同版本为 `lspr24-g0-v5-staged`，SHA-256 为：

```text
877488f3529c6c862b060a74782d1904aae81512ea1169c713ba20d8c3f31a0e
```

合同文件：`.Codex/docs/RWKV/2026-08-05-LSPR24-G0数据与切分合同.md`。

## 3. 数据与环境事实

### 3.1 LSPR24

- 原始 Parquet：`thesis/experiments/llm_probe/data/raw/lspr24-v1/lspr24_v2.parquet`
- 字节数：`2,775,972,952`
- 行数：`20,227,356`
- 官方 MD5：`a407011e9edbe615ce58a1e625c0a340`
- 本地 SHA-256 已核验，路线总控与数据合同有记录。
- `attack_narratives.json` 已下载并验收，但在方法冻结前继续禁止解析。

本机此前约有 60 GiB 可用空间、磁盘使用率约 86%。正式运行前必须重新核对空间，不能沿用旧数值。

### 3.2 依赖

- Rust：本机可用，任务 1 至当前修复均使用 Rust 实现。
- Python：项目使用 `uv`；本机全局缓存受限时使用独立临时缓存目录。
- HGB/XGBoost 的离线轮子已预取，但正式环境是否已安装必须重新核对。
- RWKV-LM、Time-Series-Library、Mamba 和 `causal-conv1d` 的冻结源码与本机备份已经完成。
- 神经公平基线尚未开始；启动前必须通知用户需要 GPU、显卡、并行进程数和预计时长。

## 4. 已完成且可复用的实现

### 4.1 Rust G0 基础组件

- 任务 1：时间列扫描、严格模式与全局 `source_row_index`，目标测试 `3/3` 通过。
- 任务 2：规范编码、语义哈希、稳定外部排序，目标测试 `5/5` 通过。
- 任务 3：不可覆盖输出、收据解析、自哈希和门禁依赖框架，目标测试 `5/5` 通过。
- 任务 4：Python 开发样本参考与命令入口，两项目标测试分别 `1/1` 通过。
- 任务 5 开发路由/JSON 物化合同：两组目标测试分别 `3/3` 通过。
- 正式入口修复：
  - `formal_development_entry_contract`：`3/3` 通过；
  - `materialize_contract`：`5/5` 通过。

正式入口修复已经验证：

- 外部切分收据绑定合同哈希、Parquet 哈希、切分算法、80% 切点、开发成员计数和成员哈希；
- 收据被自洽重哈希后仍能识别篡改；
- 最终区成员不能由请求参数注入为开发成员；
- 第二遍先依据 80% 边界丢弃最终行，再执行开发标签逻辑；
- 发布后删除暂存失败、父目录刷新失败均不留下不一致正式对象或 `.partial`；
- CLI 不再虚假宣称 READY，明确保持 `G0-D/G0-F/A12=NOT_READY` 和 `final_access_authorized=false`。

独立范围复核认为上述旧严重项均已处理，无须因最终区泄漏风险中止后续实现。混合行组会使 Arrow 批投影标签列页，但代码对最终行不执行标签规范化、计数或输出；这属于合同允许的受限路由器边界。

### 4.2 HGB/XGBoost 共享适配器

实现文件：

- `src/flow_probe/lspr24_g0_tabular_adapter.py`
- `src/flow_probe/tabular_baselines.py`
- `scripts/run_lspr24_g0_tabular_baselines.sh`
- `tests/test_lspr24_g0_tabular_adapter.py`

最终目标测试：`20 passed in 3.92s`。

已验证：

- HGB/XGBoost 使用相同的样本、字段顺序和 `H×F` 输入；
- 拒绝重复 `sample_id`、额外禁入列、未绑定标签侧车和最终区路径；
- 验证字段谱系、历史收据、同端点、同连续段、同分区和旧到新顺序；
- 四段执行顺序固定为四个历史候选的 `architecture-selection`，冻结唯一候选后仅该候选进入 `dev-validation`，再进入 `calibration`；
- 三阶段收据内容不同并形成哈希依赖链；开发验证与校准不重复拟合模型。

该适配器只通过合成夹具测试，尚未读取真实 LSPR24，也未运行真实 HGB/XGBoost，不能写成实验结果。

## 5. 当前最重要的阻塞

**不要立即运行当前全量命令。**

现有正式入口配置为：

```text
thesis/experiments/llm_probe/tools/lspr24_g0/configs/lspr24-g0-development-v4.json
```

其中仍包含：

```json
"feature_columns": ["INSTANTIATE_ALLOWED_INT64_FEATURE"]
```

当前 `formal_parquet.rs` 只接受原始 `Int64` 特征列，并输出 `development-rows.jsonl`。这只是两阶段安全路由和发布语义的生产骨架，**不等于合同要求的最终 G0-D 数据视图**。合同要求的是按 5 秒窗口聚合的完整合法字段、字段谱系、开发窗口、历史索引、推断锚点、评价簇和配套收据。

禁止为了启动而任意选择一个原始整数列。单字段原始行即使成功跑完，也不能给公平基线或方案 A/B 使用，会造成一次无效的 2022 万行物化。

当前正式全量进程已明确暂停，未创建运行目录或制品。

## 6. 新会话必须先完成的 P0 任务

### P0-1：建立完整 5 秒窗口物化实施计划

在修改生产代码前，新建持久化实施计划，至少覆盖：

1. 按合同第 6.1 节冻结完整 `field-manifest.json`，每列记录来源、模式、可用时刻、聚合器、公式、单位、缺失规则、变换、生成目标和允许模型。
2. 从原始流构造 5 秒窗口，而不是直接把原始流行写入 JSONL。
3. 完成被防御端点方向语义、无标签去重、会话连续段、跨界隔离和开发区重复标签冲突整类隔离。
4. 输出合同要求的：
   - `views/development-windows.parquet`
   - `views/history-samples.parquet`
   - `manifests/inference-anchor-manifest.parquet`
   - `manifests/evaluation-clusters.parquet`
   - `manifests/field-manifest.json`
   - `receipts/field-lineage-and-tensor-probe.json`
   - 开发标签侧车、分割收据、语义哈希和 G0-D 原始门禁收据。
5. 继续保证最终 20% 零持久化、零统计和零模型输入。
6. 使用 Rust 流式/分段实现，避免把 2022 万行或全部标签请求一次性载入内存。

开发范式仍为 `writing-plans` → `subagent-driven-development`，实现或缺陷修复代理在自己的任务边界内使用一次 `test-driven-development`。只运行目标测试；格式化、全仓检查和独立复审不得阻塞实验。

### P0-2：完成正式 G0-D 物化

只有 P0-1 的窗口与字段合同目标测试通过后，才实例化运行配置并启动单次全量运行。

预期命令形态为：

```bash
cd /Users/bilibili/personal/note/thesis/experiments/llm_probe
cargo run --manifest-path tools/lspr24_g0/Cargo.toml --locked \
  --bin lspr24_g0_materialize -- <运行专属配置>
```

运行目录命名：

```text
runs/lspr24-g0-d/lspr24-g0-d-formal-<UTC>-<PID>/
```

至少记录：

- `logs/materialize.log`
- `status.json`
- `pid`
- `machine-receipt.json`
- 原始门禁收据与实际输出清单

只运行一次，不做冒烟、不做容器逐字节双跑、不覆盖旧目录。正式运行完成不等于 `G0-D=READY`；必须从原始制品复算当前开发门禁，不能信任生产者布尔汇总。

### P0-3：真实 HGB/XGBoost 基线

仅在完整 G0-D 视图和门禁通过后：

1. 核对并安装 `scikit-learn`、`xgboost`；优先使用已预取离线制品和 `uv`。
2. 使用 `scripts/run_lspr24_g0_tabular_baselines.sh`。
3. 先运行 HGB，再运行 XGBoost；两者均为 CPU 基线，不需要 GPU。
4. 只使用四段开发区状态机，不读取最终 20%。
5. 记录原始预测、配置、日志、运行收据和指标制品路径；此时才会出现第一批真实基线结果。

## 7. 后续顺序

1. G0-D 完整数据视图与门禁。
2. HGB、XGBoost 公平基线。
3. GRU、因果 Transformer、TimesNet、Mamba、普通 RWKV-7 公平基线。
4. 使用冻结开发验证区确定困难面板。
5. 方案 A/B 种子 42 的结构机制与生成目标 `2×2` 筛选。
6. 胜出方案进入种子 42/43/44 正式矩阵和效率实验。
7. 冻结方法、字段、阈值、困难面板、超参数、代码、种子、指标和统计模板。
8. 实施 `G0-F` 严格封存并只做一次最终测试。

## 8. 禁止事项

- 不得把当前测试绿灯写成模型效果提升或 G0-D 已通过。
- 不得解析 `attack_narratives.json`。
- 不得读取或输出最终 20% 的标签、特征、统计、成员或哈希。
- 不得按方案 A/B 或基线效果选择样本、字段、数据比例或困难面板。
- 不得人为削弱基线、查看最终结果后改样本，或保证指定 F1。
- 不得把一个原始整数列冒充完整合法字段预算。
- 不得在当前阶段开 GPU；神经基线准备启动时先通知用户。
- 不得让格式化、全仓测试、独立复审或文档归档阻塞已满足门禁的正式实验。

## 9. 关键文件

- 路线总控：`.Codex/docs/RWKV/RWKV路线总控.md`
- G0 合同：`.Codex/docs/RWKV/2026-08-05-LSPR24-G0数据与切分合同.md`
- 分阶段修订：`.Codex/docs/RWKV/2026-08-06-LSPR24-G0分阶段合同修订记录.md`
- Rust 实施计划：`.Codex/docs/RWKV/2026-08-06-LSPR24-Rust-G0实施计划.md`
- Rust 任务 5 报告：`.Codex/docs/RWKV/2026-08-06-LSPR24-Rust-G0D任务5实施报告.md`
- Rust 正式入口修复报告：`.Codex/docs/RWKV/2026-08-06-LSPR24-Rust-G0D正式入口修复报告.md`，若文件尚未出现则先核对当前实现和代理状态，不得假定报告已经完成。
- HGB/XGBoost 适配计划：`.Codex/docs/RWKV/2026-08-06-LSPR24-HGB-XGBoost适配实施计划.md`
- HGB/XGBoost 实施报告：`.Codex/docs/RWKV/2026-08-06-LSPR24-HGB-XGBoost适配实施报告.md`
- 方案 A 第三章：`.Codex/docs/RWKV/2026-08-05-方案A第三章-受序动态记忆与正常状态生成.md`
- 方案 B 第三章：`.Codex/docs/RWKV/2026-08-05-方案B第三章-字段交互与动态状态演化.md`

## 10. 对新会话的第一条执行指令

先不要运行当前全量配置。完整读取本交接后，核对 `formal_parquet.rs`、合同第 6.1/11.2 节和现有输出模式，创建“LSPR24 5 秒窗口与完整字段清单物化实施计划”，再拆分 Rust 实现任务。当前最短的正确路径不是启动单字段原始行物化，而是补齐能够被 HGB/XGBoost 和方案 A/B 共同消费的冻结 G0-D 数据视图。
