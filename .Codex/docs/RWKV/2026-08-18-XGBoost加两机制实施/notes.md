# XGBoost 加两机制实施笔记

## 已核验证据

- 执行合同：`.Codex/docs/RWKV/2026-08-18-XGBoost加两机制执行文档.md`
- 文献矩阵：`.Codex/docs/RWKV/2026-08-18-XGBoost-CPA-ELP-GPU冻结方案/文献证据矩阵.md`
- 接口勘察建议：独立新工具、配置和启动器，不修改历史 XGBoost 锚点脚本。

## 真实缓存核验

- B76 XGBoost：`3.2.0`
- `X23=(16,353,511,83)`，`X24=(20,227,356,83)`
- `E23=(271,815,)`，唯一实体 `150,680`
- `3,345` 个实体跨多个序列块，单实体最多 `19,338` 块；序列块不能替代实体折号。

## 第三方接口核验

- Context7：`/websites/xgboost_readthedocs_io_en`
- 采用 `device="cuda"`、`tree_method="hist"`、`QuantileDMatrix`、验证矩阵 `ref=dtrain`、统一 `max_bin=256`。
- NumPy 输入构造量化草图发生在 CPU，树训练使用 GPU；不假定同卡双进程一定加速，以必要真实折测量调度。

## 决策

- CPA 挂载不凭文字裁决：`mean166` 与 `semantic168` 在相同 LSPR23 实体折外协议下实测，按 CPA 单机制实体 AP 选择。
- ELP 网格固定为 `{0.5,1,2,4,8}`，只用 LSPR23 折外实体 AP 选择。
- 历史 CPU XGBoost 只作诊断锚；新四格使用同次 GPU `C00`。
- 不持久化折外或目标年逐样本分数，只保存模型、聚合选择、指标和资源收据。

## 运行检查点（2026-08-18 16:34）

- 运行身份：`ch3-xgb-cpa-elp-gpu-oof-seed42-v1`。
- 运行根：`runs/diagnostics/ch3-xgb-cpa-elp-gpu-oof-seed42-v1`。
- 持久会话：`ch3-xgb-cpa-elp-gpu-s42-v1`。
- 状态：`running/source_selection/detail=started/exit_code=null`。
- 工具／配置／启动器 SHA-256：`09796a92eacb60106d03740705ee0b49d3c8da2666074d68fea062b2e80b0f6d`、`b4b6a053e2bb846dd4cfa360dd909b9118a78c67a4a28b91a1ff9349583b13d1`、`c72adaf22999d9c10b077e5f229af6f075b0f1b3a0017403e4abb5403a74ca4e`；远端与本机一致。
- SwanLab 前置 `ping/verify` 均为 `0`，已在用户授权的工作区与项目创建聚合运行。
- 源年规模：`16,353,511` 条流、`271,815` 条序列、`150,680` 个实体、`239` 个正实体。
- 三折实体数为 `50,227/50,227/50,226`，正实体数为 `80/80/79`；同一实体只进入一折。
- 源年时间非降核验覆盖 `16,081,696` 个有效相邻对，已通过。
- `mean166` 已完成，且 `16,353,511` 条流各被恰好一个序列位置写入一次。
- `semantic168` 当前为 `172,032/271,815（63.3%）`；日志最近吞吐约 `1,974` 条序列／秒。
- 拟合 `0/11`，首折、适配器选择、`p` 选择和模型制品均尚未产生；LSPR24 未读取。
- 错误扫描零命中。GPU 使用／总量为 `9842/32607 MiB`、利用率约 `34%`；用户已明确授权在资源门通过时与卷积并行。
- 当前证据等级：**实验正在运行／实验待证**，不得据此写入有利或不利效果结论。
