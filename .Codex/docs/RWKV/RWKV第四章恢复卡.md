# RWKV 第四章恢复卡

> **快照指针（2026-08-28）**：本卡多数章节为 08-20 至 08-26 状态。最新事实——底座已按 V2
> 迁移至第三章胜出 MLP O11、三病灶在该底座全部确认（含校准 bug 事故链与修复）、
> 机制 1（Tong 路径预算）本机筛选 SUPPORTED、机制 2 三构造原门否决后转 MalMoE 朱式改造
> （E0 oracle 存废实验在飞）、朱式双机制结构不可谈判裁决——统一见
> [2026-08-28 全局进度快照](2026-08-28-全局进度快照.md) 与
> [机制 a 实施计划](archive/第四章/2026-08-26-第四章MLP底座重构与分层证书机制a/实施计划.md)。

<!-- RESEARCH_ROUTE=RWKV -->

- **状态日期**：2026-08-26（本轮只新增文献启发链接；既有运行状态未访问服务器重查）
- **用途**：第四章唯一当前恢复卡。只承载 DTEP、PBC、E1、长实体诊断、第四章 Q0 载荷、实验状态和下一动作。
- **总证据等级**：固定二进多尺度 DTEP、无标签 BBSE-PBC 与旧机制二两次同模型三分有限样本确认均已完成有效单种子 Q0 且不晋级；旧机制二最早的 Q0 因三角色使用不同模型而无效。机制一的 LSPR23 重复曝光联合病灶已通过资格诊断，Tong 有限样本同模型实体路径最大控制已通过正式修正源年 Q0。新机制二“路径风险长度桶源年诊断”已完成有效零训练源年运行：池化 FPR 掩盖长度条件风险，病灶获单种子诊断支持；反射累积仅获进入最小 Q0 的资格，未证明有效。机制一当前证据等级为源年单种子 Q0 实验支持，不是跨年度或正式论文证据；不得写成第四章双机制候选已支持。
- **2026-08-26 文献启发边界**：数字、页码、可引用动机和新候选边界见[四篇漂移文献对第四章启发](archive/第四章/2026-08-26-四篇漂移文献对第四章启发/启发记录.md)。MalMoE 映射仅为待验证新候选，不属于当前 A 线；若推进须重新执行 `research-ideation → brainstorming → 新契约`。两次同模型再校准否决保持关闭，反射累积不得借壳恢复该路线。

## 〇、2026-09-02：底座更换导致本卡以下各节整体失效（新会话必读）

**本节之后的全部内容（第一至十节）依附于一个已经不存在的底座，只作历史证据保留，
不得作为当前第四章的前提。**

**失效原因不是决策层路线被否决，而是它依附的第三章底座已被更换。** 本卡第一节
冻结的是「第三章 XGBoost＋CPA＋ELP 的 `semantic168` 模型、`p=1` 和实体分数定义」；
当前第三章已改为 **FT-Transformer ＋ ETA-BER**，那个模型、那套分数、那个阈值口径都不再产出。
DTEP、PBC、E1 门禁、长实体诊断、Tong 路径预算的全部读数**都出自旧底座**，
按[被否决实现的读数隔离](../../AGENTS.md)一节，它们只能用于说明旧路线为何停止，
**不得用作新方向的性质、局限或设计依据**。

**新方向：训练效率。** 依据是参照论文第四章本身（承接第三章暴露的训练效率问题、
新机制直接介入训练、模型参数继续更新，摘要报 `4.6×` 加速），
而非本仓库任何过程文档——**本卡第 25 行早已写明这一点**，当时因底座未换而选了决策层。

用户 2026-09-02 的原话：「C01-half 与 C11-half 带 BER，会慢得多，
你可以现在就寻找机制来改善这个问题，正好可以留为第四章的内容，**要做好开关能够做消融**」。

### 参照论文第四章的做法：**在已有加速家族里做任务特化改造，不是自创**

原件核实（`朱焱雷-全文提取-mineru.md`，`rg -a` 强制文本模式；该文件含 `\0` 字节，
默认被当二进制，**检索必须加 `-a`**）。他第四章名为 **DF-ACR**，两个机制的来源
在第二章末尾逐句点名：

| 他的机制 | 来源家族 | 原文原话（行号） |
| --- | --- | --- |
| `4.3.2` 动态重要性数据过滤 | 核心集选择：Toneva `[64]`、CRAIG `[65]`（式 2.12）、梯度范数重要性采样 `[66]`（式 2.13）、数据瘦身 `[67]` | 「这种基于重要性动态筛选的思路，**正是本文第四章构建 DF-ACR 训练方法的理论基础**」（`675`–`712`） |
| `4.3.3` 流量缓存复用 | Free-AT 的扰动复用 `[68]`（式 2.14） | 「这种利用历史扰动信息的思想，**为本文第四章设计对抗扰动缓存策略提供了思路**」（`715`–`733`） |

他还把 YOPO `[69]`（式 2.15）标为「具有极高的参考价值」。**第二章先把家族讲透并抄进公式，
第四章再讲改造**——自创的是「怎么在流量场景里做」，不是「做什么」。
这也是他能摆出 `6` 个外部加速基线的前提：**有家族才有基线可比。**

### 候选与证据状态（2026-09-02 机制设计代理实测，**推翻了本节初稿的全部推演值**）

方案文档：[第四章候选-BER 训练效率机制](../../../thesis/methods/第四章候选-BER训练效率机制.md)
（`1400+` 行，`f613b19` → `af91d94`；**判据冻结于 `f613b19`，早于读取任何实验读数**）。

| 候选 | 机制 | 端到端上限 | 数值语义 | 状态 |
| --- | --- | ---: | --- | --- |
| 一 ASB | 活动集稀疏反传 | — | 逐位不变 | **按冻结判据证伪** |
| 二 ASR | 聚合稀疏重打包 | `1.31`–`2.05` | 保形逐位；重打包 `~8 ulp` | **保形无收益**，重打包待裁 |
| 三 HSE | 主机同步消除 | **`<1.01`** | 逐位不变（掩码已实测） | **支持判据不成立**，降为随手整改 |
| 四 SAC | 选择性重算 | `1.09` | 逐位不变（CPU 已实测） | 只能作配套 |
| 五 VFP | 有效流稠密打包 | `1.31`–`2.05` | `~8 ulp` | 判据冲突，待裁决 |
| **六 EAS** | **空活动集短路** | `1.22` | **唯一可断言逐位精确** | **待测空步占比** |
| 七 RSC | 排序步频下调 | `2.10` | **改变，需重跑第三章（约 16 h GPU）** | 待单种子筛选臂 |

**推翻本节初稿的四处实测**（初稿写于代理返回前，其推演值一律作废）：

- **排序批只有 `78.5` 片段（中位）、`2`–`4` 个微批**，不是代码注释里的 `6403` 序列 `100` 微批。
  这直接证伪候选一，也使候选二的保形变体在 `M ≤ 4` 时吃不到稀疏。
- 初稿的 ASR「`3.88×`」不成立，端到端上限 `1.31`–`2.05`。
- 初稿的 HSE「量级未知、可能可观」不成立：因果截断的 Python 循环实测 `0.281` 毫秒，
  **比代理自设的 `50` 毫秒门槛低 `178` 倍**。
- **两遍播种反传逐位相等**（`loss`／`xi.grad`／`logits.grad` 三项 `torch.equal` 全真，
  `max|Δ| = 0.0`，max 与 `α=0.5` 两种聚合、fp64 与 fp32 均通过）。

**另两条与四格直接相关的实测**：max 聚合下梯度密度只有 `0.678%`（非零元恰为实体数 `66`），
ETA 打开升到 `9.57%`——**这是候选二与第三章四格的真实交互**；活动集全空的步里
`grad_norm_rank` 精确为 `0`、`c_scaling = 0`，整个排序阶段纯浪费，
**v1 运行 `20` 个末步快照中 `5` 个如此**（候选六的动机）。

### 两个必须由用户裁决的冲突

1. **范围冲突（更严重）**：**`4.6` 倍在「只优化 BER」的范围内算术上不可达。**
   由 `rsync -t` 保留的收据 mtime 实测 C01 单轮 `1181` 秒、C00 `475` 秒 = **`2.49` 倍**，
   即删除全部 BER 的上限就是 `2.49`；只优化排序阶段模型计算的上限是 `1.31`–`2.05`。
   要对标那个量级**必须把固定开销（`265` 秒/轮，占 C00 单轮 `56%`）纳入第四章，
   而那不是第三章造成的代价**。
2. **数值判据冲突**：有效流打包误差 fp64 `1.13e-15`、fp32 `9.56e-07`（相对，约 `8 ulp`）。
   按代理卡里的「fp64 逐位相等」判据**被证伪**，按主代理简报 4.1 的
   「差异仅来自浮点重结合律＋可验证误差上界」**通过**。代理未改判据，上交裁决。

### 文献栏：已从「全空」变为「家族已定位，基线仍为零」

文献子代理（`opus`，`effort` 继承会话设置——Claude Code 的 `Agent` 工具无 `effort` 入参）
已提交方向 1 与 5（`69dcedb`／`9341730`／`4e18533`／`b4995e3`），方向 2–4 在跑。两条实质结论：

- **候选一、二、六属「安全筛除」家族（可证明精确等价），而非 Selective-Backprop 的启发式家族。
  正文应放等价性证明，不是收敛性论证。** 这与参照论文走的是不同家族，但同样有家族。
- **本候选池外部基线为零，是立章硬缺口**——参照论文有 `6` 个。

代理还用抽样算术关掉了「跨轮次复用」这条时间维（对位参照论文的缓存复用）：
负实体池是批规模的 `1896` 倍，轮内 `77.7%` 的负实体只出现一次，
跨步复用最多命中 `22.3%` 的抽取、折算收益 `2.1%`。

### 已完成的一处（不是研究机制）

`bag_policy_diagnostics` 的两支非生效袋处置移入 `torch.no_grad()`——它们只写诊断字典
却照样建图持图。**提交 `6c274f8`**：三支 `loss_value` 逐位不变、生效支梯度 `torch.equal` 全真、
另两支 `grad_fn = None`（改动前三支恒为 `requires_grad=True`，证明不是空操作），
六份配置 `science_identity_sha256` 逐位不变（含补测 C11-half
`383457025029c09483b15972058cb5a4f886d60c782139608c78f4ad1078f231`）。
**这是消除纯浪费的工程修复，不需要文献支撑，也不得作为第四章的机制贡献。**

### 下一门禁

**T3：排序阶段五段计时**（`_ranking_phase` 内插 `torch.cuda.Event`，只落聚合标量，
不改数值语义）。BER 的 `706` 秒/轮里模型计算只解释 `40%`，
片段均值 `91` 时残差 `425` 秒/轮——**残差是什么，决定候选二／五值不值得付浮点代价，
也可能指向一个目前不在卡里的数据侧候选**。同时做候选六的只读计数
`empty_active_step_frac`（零风险零成本）。

文献调研独立指出：**耗时成分分解图是效率章的必备图件**，不是可选诊断。

**在 T3 出数前，任何候选都不得写入正文，也不得称第四章机制已找到。**

---

## 一、当前候选边界

当前第四章研究对象暂定为“跨年度、重复曝光条件下的实体级预算告警”，不是已经成立的算法名称。章级结构固定为一个总问题和两个已观测子问题：

- **总问题**：第三章冻结的实体分数与静态阈值不能直接满足跨年度、重复曝光的在线实体告警需求。
- **分布维度**：跨年度阈值失配导致目标年度预算利用不足；机制二两次同模型三分有限样本确认已经在源年 Q0 否决，当前冻结分数阈值再校准路线停止，问题仍存在但没有合格解法。
- **时间维度**：重复曝光挽回迟到恶意证据的同时累积良性误报；源年病灶已经通过资格诊断，控制方法实验待证。
- 冻结第三章 XGBoost＋CPA＋ELP 的 `semantic168` 模型、`p=1` 和实体分数定义，不更新树模型、不改变排序。
- 这里的“冻结第三章”是本课题快速实验的因果隔离策略，不是朱焱雷第四章的原样做法。朱焱雷第四章承接第三章暴露的训练效率问题，但新机制直接介入训练且模型参数继续更新；本课题只有在决策层路线被真实实验否决后，才可另立训练层新候选，不能在当前 Q0 内解冻模型追调。
- 旧 `10%/10%/80%` 双段合同因选择和确认实体均为 0 而不可执行，已关闭；不得通过移动比例或改用完整实体队列追跑。
- 固定二进多尺度 DTEP、无标签 BBSE-PBC 和机制二当前实现不再追调；长实体排序退化只保留为历史诊断，不作为当前第三个章级子问题。

## 二、E1 源年实体折外信号门禁

### 2.1 冻结合同

E1 只使用 LSPR23、`seed=42`、协议 A 多层感知机、C00/C11 和 150,680 个源实体确定性三折，共 6 个训练单元。每个实体恰好作为一次留出实体，训练与留出实体交集为空；任何 LSPR24 读取均应立即失败。实施合同见[E1 实施计划](archive/第四章/2026-08-18-第四章E1源年实体折外信号门禁实施计划.md)。

### 2.2 原始机械状态

- 运行身份：`ch4-e1-source-entity-oof-gate-seed42-v1`。
- 远端原始聚合制品：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/diagnostics/ch4-e1-source-entity-oof-gate-seed42-v1/aggregate-results.json`。
- 原始状态字段：`status=complete`、启动器 `finished`、`exit_code=0`，即 `complete/finished/exit=0`。
- `target_year_arrays_read=0`，零 LSPR24 读取。
- C00 pooled OOF 实体 AP：`0.5109066464488172`。
- C11 pooled OOF 实体 AP：`0.5447784766847485`。
- pooled 增量：`+0.03387183023593132`。
- 三折差值极差：`0.211731480949845`，约为 pooled 增量的 `6.25` 倍。
- 机械裁决：`weak_allow_coarse_rejection_only`。

旧恢复文档中的 E1 前置阶段状态已被上述原始完成收据覆盖。E1 说明源侧并非完全没有信号，但只能粗粒度淘汰方向不一致的完整候选，不能精细选择单一近期窗长、尺度子集、融合权重或时间基准；候选效果仍为实验待证。

## 三、长实体诊断

### 3.1 感知机分桶

运行 `ch4-entity-length-bucket-diagnostic-seed42-v1` 已以 `finished/complete/exit=0` 完成。四格相对冻结值逐项零差，覆盖 `47,115` 实体、`752` 正实体和 20 个检查点，`seen_all=true`，无逐样本分数。证据仅为 `screening_only=true`、`formal_paper_evidence=false`、`independent_test=false`。

C11-C00 在五个实体流数桶 `1-2/3-10/11-100/101-1000/1001+` 的实体 AP 增量为：

`+0.123948 / +0.239138 / +0.137851 / -0.322915 / -0.302212`。

现有机制在短中实体有利、在长实体显著有害，禁止写成全局提升。`1001+` 桶良性实体在 C11 下出现 `0.9999` 的逐流假阳尖峰，说明感知机上的长实体受损出现在逐流分数和表示层，不在聚合指数。

### 3.2 XGBoost 分桶

XGBoost 冻结 `raw83 + max` 与 `semantic168 + p=1.0` 的五桶实体 AP 增量为：

`+0.028998 / +0.121857 / +0.082025 / -0.073974 / -0.018679`。

两个长桶分别有 `84`、`40` 个正实体，均超过预注册最低支持数 `10`；方向翻转跨越感知机与 XGBoost，但 XGBoost 的 `1001+` 桶没有感知机的良性高分尖峰，不得假设两骨干根因相同。总体实体 AP 仍由 `0.513185` 提高到 `0.565078`，总体均值掩盖长实体退化。

## 四、已关闭的 DTEP/PBC 单种子 Q0

- 固定二进多尺度 DTEP 的源域三折实体 AP 差值为 `-0.005899`、`-0.000762`、`+0.000684`，汇总差值为 `-0.002455`，未通过预注册方向门，停止该候选。
- 无标签 BBSE-PBC 在 `4%` 实体 FPR 预算下回退到源阈值，实际 FPR 为 `0.006148`、DR 为 `0.440160`，未产生方法增益，停止该候选。
- 两项都是有效单种子筛选，不是工程失败；不得放宽门槛、查看 LSPR24 后调整，或生成 DTEP `v2` 尺度表。
- DTEP/PBC 的旧运行载荷、父清单与资源快照只保留为历史证据，不再是当前阻塞或下一启动条件。

## 五、机制二与机制一病灶状态

### 5.0 跨年度阈值失配病灶

有效诊断 `ch4-drift-v2` 在源年留出实体上把假阳率校准为 `0.0400`，对应阈值 `0.0010688512`、检出率 `0.9787`；该阈值冻结迁移到 LSPR24 后，实际假阳率为 `0.0153`、检出率为 `0.5505`。目标年度事后 `4%` 假阳率参考阈值为 `0.0001113068`、检出率为 `0.6157`。源阈值约为目标参考阈值的 `9.60` 倍，只使用了约 `38%` 的经验误报预算，检出率相对目标参考工作点少 `0.0652`。

该诊断使用目标年度标签计算事后参考阈值，只能证明跨年度阈值失配病灶存在，不能作为可部署阈值选择、独立正式测试或机制有效证据。旧 `ch4-drift` v1 存在源内过拟合，相关读数全部失效，不得与 v2 混用。

### 5.1 机制二同模型三分有限样本确认与风险余量否决

旧运行 `ch4-xgb-confidence-fallback-source-q0-seed42-v1` 的开发、校准与持出角色分数来自不同模型，故模型一致性不成立；其数值与否决裁决均无效，只保留为根因记录。修正运行 `ch4-xgb-confidence-fallback-same-model-source-q0-seed42-v1` 使每个方向的三角色分数均来自同一冻结折外模型，并保持角色实体两两不交。运行 `finished/complete/exit=0`，墙钟 `12.797283172607422` 秒、峰值 RSS `1264.1953125 MiB`、峰值 GPU 显存 `0 MiB`，且 `target_year_arrays_read=0`、`new_models_trained=0`。

全局 `δ=0.05` 固定按三方向均分为 `δ/3=0.01666666667`。三方向 Clopper-Pearson 上界为 `0.04647516894`、`0.04107173757`、`0.04616501157`，均大于 `q=0.04`，因此均按合同回退源阈值。池化源阈值 FPR=`0.03882740054`、DR=`0.98717948718`、FP=`1947`、TP=`77`；未确认候选 FPR=`0.03888722704`、DR=`0.98717948718`、FP=`1950`、TP=`77`。候选没有严格检出增益且三方向证书均失败，机械裁决为 `MECHANISM2_SAME_MODEL_SOURCE_Q0_REJECTED`。

上述结果只否决机制二当前源年单种子修正 Q0，不外推跨年度或正式论文结论。随后风险余量变体 `ch4-xgb-confidence-margin-fallback-same-model-source-q0-seed42-v1` 以同模型三分角色完成 `finished/complete/exit=0`，墙钟 `33.5499165` 秒、峰值 RSS `1264.9375 MiB`、GPU `0 MiB`，且 `target_year_arrays_read=0`、`new_models_trained=0`。其三方向校准 FP 为 `660/571/644`，Clopper-Pearson 上界为 `0.04281238254/0.03727371209/0.04181792561`，仅方向 1 获证；三方向全获证门失败，机械裁决为 `MECHANISM2_CONFIDENCE_MARGIN_SAME_MODEL_SOURCE_Q0_REJECTED`。

池化风险余量候选为 FP=`1781`、TP=`77`、FPR=`0.03551700070`、DR=`0.98717948718`；源阈值为 FP=`1947`、TP=`77`、FPR=`0.03882740054`、DR 同值；最终活动回退为 FP=`1784`、FPR=`0.03557682720`。候选池化读数不能替代逐方向证书。两次同模型三分 Q0 都是有效否决，停止当前冻结分数阈值再校准路线，不得继续搜索风险余量或调整 `q`、`δ`、方向数和角色划分重启。跨年度仍未验证。详细证据见[机制二同模型三分源年修正 Q0 结果报告](archive/第四章/2026-08-19-机制二同模型三分源年修正Q0结果报告.md)与[机制二风险余量同模型源年 Q0 结果报告](archive/第四章/2026-08-19-机制二风险余量同模型源年Q0结果报告.md)。

### 5.2 机制一重复曝光联合病灶

有效运行身份为 `ch4-xgb-entity-repeated-exposure-pathology-lspr23-q0-seed42-v1-rerun1`，机械状态 `finished/complete/success/exit=0`。首次曝光到全部已记录曝光的实体假阳率由 `0.0399957458` 升至 `0.0487167727`，实体检出率由 `0.9707112971` 升至 `0.9874476987`；新增 `1312` 个良性误报实体和 `4` 个恶意检出实体。预注册联合门 `FPR(all)>0.04 AND N_late_positive>0` 通过，机械裁决为 `MECHANISM1_PATHOLOGY_QUALIFIED`。

运行覆盖 LSPR23 全部 `16353511` 条流，重复覆盖、段内时间逆序和跨段时间逆序均为 0；加载 3 份折外模型，训练 0 个新模型，读取目标年数组 0 个，不持久化逐流或逐实体分数。完整曝光支持曲线为 `2475228` 行且未截断。

该结果只说明源年存在“迟到检出收益与累计误报成本”联合病灶，不证明任何控制机制有效。远端结果已核验，本地完整制品仍待远程代理通道恢复后回收。详细记录见[实体重复曝光误报时延病灶诊断结果记录](archive/第四章/2026-08-19-实体重复曝光误报时延病灶诊断结果记录.md)。

### 5.3 机制一 Tong 实体路径最大同模型交叉校准

正式修正源年 Q0 运行 `ch4-xgb-entity-path-max-tong-same-model-q0-seed42-v1` 的诊断与启动器状态均为 `finished/complete/success/exit=0`，机械裁决为 `MECHANISM1_PATH_MAX_TONG_Q0_QUALIFIED`。运行加载 3 份冻结折外模型，训练运行数为 0、目标年数组读取数为 0，并覆盖 LSPR23 的 `16353511` 条流；重复覆盖、段内时间逆序与跨段时间逆序均为 0。墙钟时间为 `204.2444303035736` 秒，峰值 RSS 为 `16565.99609375 MiB`，峰值 GPU 显存为 `0 MiB`。

六个同模型、类别二分并交换校准与评价角色的方向汇总为：首曝基线 B0 的实体 FPR 为 `0.03999574584056208`、DR 为 `0.9707112970711297`、FP 为 `6017`、TP 为 `232`；旧固定阈值全曝光 B1 的实体 FPR 为 `0.04871677268829641`、DR 为 `0.9874476987447699`、FP 为 `7329`、TP 为 `236`；Tong 路径最大 M1 的实体 FPR 为 `0.03711089397172313`、DR 为 `0.9832635983263598`、FP 为 `5583`、TP 为 `235`。全局 `δ=0.05` 按 6 个方向均分，`all_certificates_valid=true`，因此 M1 满足 `all(v(k*)<=delta/6) AND FPR(M1)<=0.04 AND DR(M1)>DR(B0)`。

相对 B0，Tong M1 少 `434` 个 FP、多 `3` 个 TP；相对经验分位路径最大，少 `434` 个 FP、少 `1` 个 TP。Tong 有限样本保证只适用于本次冻结的 6 个方向和源年路径支持，不外推至跨年度或正式论文结论。该结果的证据等级仅为源年单种子 Q0 实验支持，详细证据见[实体路径最大风险控制源年 Q0 结果记录](archive/第四章/2026-08-19-实体路径最大风险控制源年Q0结果记录.md)。

用户已于 2026-08-20 取消三种子扩展。机制一当前只允许 `seed=42` 的最终单模型 LSPR23 源年实验，保持 Tong 合同和冻结的 `semantic168` 六臂框架。**2026-08-26 服务器 status.json 核验**：`runs/candidates/ch4-xgb-path-max-tong-final-model-source-seed42-v1` 与 `-rerun1` 均 `failed/final_single_model_source/python_failed/exit=1`；`-rerun2` 为 `finished/complete/success/exit=0` 且 `target_year_arrays_read=0`。最终单模型运行机械完成，结果数字待验收；不得再写「实现仍在进行」。不得追加 `seed=43/44` 或把已观察的 LSPR24 包装为独立最终评价域。

### 5.4 机制二路径风险长度桶源年诊断

机制二“路径风险长度桶源年诊断”运行 `ch4-xgb-path-risk-length-bucket-lspr23-diagnostic-seed42-v1` 已完成 `finished/complete/success/exit=0`。诊断与父 M1 摘要哈希一致，B0/B1/M1 轮次和聚合读数精确复现；覆盖 LSPR23 全部 `16,353,511` 条流，重复覆盖、段内时间逆序、跨段时间逆序均为 0，`target_year_arrays_read=0`、`models_trained=0`，只加载 3 个冻结折外模型。

M1 池化 FPR=`0.03711089397`、DR=`0.9832635983`，但五个长度桶 FPR 为 `0.0271466/0.0450469/0.0904020/0.1333557/0.0720165`；除 `1-2` 桶外均超过 4%，其中 `101-1000` 桶为 `13.3356%`。故“池化 FPR 掩盖长度条件风险”获 LSPR23 源年单种子诊断支持。4 个 M1 漏检正实体的 `path_maximum/threshold` 均小于 1，最大 `0.977794`，没有逐实体轨迹或重复证据，不能写成持续次高分。该诊断不选择反射累积参数，不构成机制有效性或正式论文证据；反射累积只可进入冻结输入、模型和 Tong 参数下的最小 Q0。详细制品见[机制二路径风险长度桶诊断结果](archive/第四章/2026-08-20-机制二路径风险长度桶诊断结果/2026-08-20--机制二路径风险长度桶--r00--结果记录.md)。

## 六、历史第四章实体 AP 候选 Q0

「实体均匀训练与直接实体 AP 优化」只用于提高 C11 实体 AP，不替换 PBC。其方案册、Q0 计划和原创性审计已完成；可学习 Lp/GeM 加直接 AP 已被占用，不能作为原创。随机袋 p 阶矩及导数的条件无偏性不推出 SOAP/SOX 非线性 AP 复合梯度无偏。

最终有效运行身份为 `ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`，运行根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1-rerun2`，机械状态 `COMPLETED_Q0_REJECTED/COMPLETE/exit=0`，启动器 `finished/complete/exit=0`。`strict_2x2_complete=true`、`candidate_advanced=false`、`target_labels_read_after_selection_seal=true`、`failed_run_artifacts_reused=false`、`random_lp_gate=PASSED`。

- 四格实体 AP：`0.4563850872 / 0.2892240420 / 0.3158986812 / 0.4234940164`。
- DR@4%FPR：`0.6502659574 / 0.4747340426 / 0.4853723404 / 0.6954787234`。
- 逐流 AP：`0.3013773042 / 0.2240005305 / 0.3201337105 / 0.1592074936`。
- 交互：`0.2747563804`；尾部实体 AP 增量：`-0.1070404121 / -0.0718340368`；历史 M0R0 差：`0.0066029128`。
- `entity_uniform_measure_supported=false`、`direct_entity_ap_supported=false`。
- 峰值 RSS `12658.45 MiB`、GPU `1893.14 MiB`；不持久化逐流或逐实体分数与源索引。

该结果仅为 `screening_only=true`、`formal=false`、`formal_paper_evidence=false`、`independent_test=false`，不进入论文正式表。首次 v1 的 M0R0 完成 20,000 步、累计编码 50,627,995 条流后，第二次 `swanlab.init` 返回 `401 Unauthorized`；准确状态是 `TRAINING_TRACKING_FAILED`，不是旧代码的 `EVALUATION_FAILED`。失败根 `runs/candidates/ch4-entity-uniform-direct-ap-q0-seed42-v1` 必须保留，`target_labels_read=false`，无科学结果。

原三份生产文件 SHA-256：工具 `564d6bc18e2442f25c532e624886aababbcf75e3ec022c60e0d58f4f13f1f7d3`、配置 `d9cb3a6d1ef886b44797b0c0138f61a877044f49cc8735de0f9060486a7f97f3`、启动器 `33ae46f502d53261c357a55f61840df26057dc529b2f912e2e656f5f3846cdb9`。

## 七、共享筛选视图陷阱

视图根 `runs/data-prepared/lspr24-screen-wide-v1`，合同 `lspr24-screen-6-2-2-v1`。

1. `label=-1` 占 `60.4075%`，即 `4,800,524` 窗口；`label=0` 占 `37.9779%`，`label=1` 占 `1.6145%`。可评价窗口只有 `3,146,375 / 7,946,899 = 39.59%`，消费前必须过滤到 `{0,1}`。
2. 训练区与验证区正类率为 `0.048423` 与 `0.022486`，相差 `2.15` 倍；同年任务也存在先验漂移。
3. 历史视图的 `final_split=final-not-materialized`；该事实只适用于该视图的旧合同，不得覆盖当前跨年度全量评价裁决。
4. 同年 XGBoost 验证 AP 为 `0.9690348794483452`、ROC-AUC 为 `0.9956477692756596`，余量 `0.03096512055165479`。这只是同年筛选背景，不是 DTEP 或 PBC 的效果证据。

## 八、当前候选状态与下一动作

- 机制一已取得“病灶存在”资格，并已通过正式修正源年 Q0 的 Tong 有限样本实体路径最大控制；当前只得到源年单种子 Q0 实验支持。机制二旧 Q0 无效，随后两次同模型三分 Q0 均有效否决；路径风险长度桶诊断已支持源年长度条件风险病灶，但反射累积仍为最小 Q0 待证候选。因此尚不存在可命名的第四章双机制算法。
- A 线状态已由 2026-08-26 服务器制品更新：M1 最终单模型 rerun1 `failed/python_failed`，**rerun2 `finished/complete/exit=0`（结果待验收）**；M2 反射累积 `ch4-xgb-reflected-cumulative-dual-evidence-lspr23-q0-seed42-v1-rerun2` 为 `finished/complete/exit=0` 且机械裁决 **`REJECTED_SOURCE_Q0`**——九方向联合 Tong 证书全部成立、池化 J1 FPR `0.03613377` ≤ 4%，但 `J1_adds_at_least_one_TP_with_reflected_only_new_TP=False`（`reflected_only_TP=0`，J1 与 M1 的 TP 完全重合 `235`）且 `both_long_buckets_J1_FPR_le_0_04_and_not_above_M1=False`。**反射累积为有效科学否决**，不得再写「running」或「工程待修」；证书封印于评价指标之前（`certificate_sealed_before_evaluation_metrics=true`），覆盖两遍共 `32,707,022` 流。旧的双线推进记录见[第三章骨干升级与第四章双证据方法双线推进记录](archive/基础设施/2026-08-20-第三章骨干升级与第四章双证据方法双线推进记录.md)。
- 朱焱雷第四章只提供“一个总问题拆成两个互补维度”的组织参照。本课题的两个维度是跨年度阈值失配和重复曝光决策，不借用其过滤、缓存、公式或效果数字。
- 本章基线与对标采用同一冻结 `semantic168` 骨干六臂，主指标为实体实际 FPR、DR、迟到检出、首次告警曝光或时延、预算曲线和效率，AP 不作决策层主指标；精确对标范围见[实体路径最大风险控制源年 Q0 结果记录](archive/第四章/2026-08-19-实体路径最大风险控制源年Q0结果记录.md)。
- 当前候选输入暂冻结为 `semantic168`，只用于单变量诊断；历史上已比较 `raw83`、`mean166` 与 `fixed_dyadic251`，不得把 `semantic168` 写成永久最优或据此扩展模型选择。

1. 机制二零训练“路径风险长度桶源年诊断”已满足病灶门；保留其运行制品与[严格分析包](archive/第四章/2026-08-20-机制二路径风险长度桶诊断结果/analysis-report.md)，不得将单种子诊断升级为机制有效。
2. 执行反射累积最小 Q0；该对照须保持冻结 `semantic168` 单变量输入、模型和 Tong 参数，不得借机重选输入、模型或反射参数。
3. 机制一最终单模型实现完成后，先核验 GPU 可用性；当前服务器无 GPU，训练保持未启动。三种子实验已取消。
4. 文献代理只检索重复查看误报累积、任意时点有效错误控制、序贯纽曼—皮尔逊和实体级早期告警的直接近邻；检索与实验并行，不阻塞零训练诊断。
5. `α_j=α/[j(j+1)]` 只作逐曝光预算压力对照，不作主机制；当前没有已核全文直接覆盖同一实体相关前缀的该构造。
6. 不重启旧机制二已否决实现，不调整旧时间比例，不把机制一源年结果或机制二诊断实现写成章级双机制已成立。

## 十、证据索引

- [第四章候选与文献方向分析](archive/第四章/2026-08-18-第四章候选与文献方向分析.md)
- [双段先导置信预算告警 Q0 方案与实施计划](archive/第四章/2026-08-19-双段先导置信预算告警-Q0方案与实施计划.md)
- [实体暴露感知置信预算告警 Q0 方案与实施计划](archive/第四章/2026-08-19-实体暴露感知置信预算告警-Q0方案与实施计划.md)
- [机制二同模型三分源年修正 Q0 结果报告](archive/第四章/2026-08-19-机制二同模型三分源年修正Q0结果报告.md)
- [机制二风险余量同模型源年 Q0 结果报告](archive/第四章/2026-08-19-机制二风险余量同模型源年Q0结果报告.md)
- [实体重复曝光误报时延病灶诊断实施计划](archive/第四章/2026-08-19-实体重复曝光误报时延病灶诊断实施计划.md)
- [实体重复曝光误报时延病灶诊断结果记录](archive/第四章/2026-08-19-实体重复曝光误报时延病灶诊断结果记录.md)
- [实体路径最大风险控制源年 Q0 结果记录](archive/第四章/2026-08-19-实体路径最大风险控制源年Q0结果记录.md)
- [机制二路径风险长度桶诊断结果](archive/第四章/2026-08-20-机制二路径风险长度桶诊断结果/2026-08-20--机制二路径风险长度桶--r00--结果记录.md)
- [DTEP 固定多尺度重构方案](archive/第四章/2026-08-18-DTEP固定多尺度重构方案.md)
- [DTEP 固定二进多尺度 Q0 实施计划](archive/第四章/2026-08-18-DTEP固定二进多尺度-Q0实施计划.md)
- [PBC 先导段预算标定 Q0 实施计划](archive/第四章/2026-08-18-PBC先导段预算标定-Q0实施计划.md)
- [E1 源年实体折外信号门禁实施计划](archive/第四章/2026-08-18-第四章E1源年实体折外信号门禁实施计划.md)
- [第四章实体流数分桶诊断实施与运行报告](archive/第四章/2026-08-13-第四章实体流数分桶诊断实施与运行报告.md)
- [XGBoost 长实体分桶诊断结果分析](archive/第四章/2026-08-18-XGBoost长实体分桶诊断结果分析.md)
- [第四章实体平均精确率原创性与数学边界](archive/第四章/2026-08-13-第四章实体矩AP原创性与数学边界.md)
- [第三章骨干升级与第四章双证据方法双线推进记录](archive/基础设施/2026-08-20-第三章骨干升级与第四章双证据方法双线推进记录.md)
- [四篇漂移文献对第四章启发](archive/第四章/2026-08-26-四篇漂移文献对第四章启发/启发记录.md)
