# 第三章「两级风险聚合框架」§二之六（任务化改造）证伪式审核

**审核对象**：`thesis/methods/第三章-两级风险聚合框架.md`，提交 `68ba3b1`
（文件在 `e638a9d` 时工作树干净，`git log` 确认该文件最后一次改动即 `68ba3b1`）。
**立场**：证伪。**方法**：文献、代码、实验三样都做。
**日期**：2026-09-04。

> **本文件分阶段写入。** 每完成一件立即落盘并提交，未完成部分留明确占位。

---

## 状态

| # | 环 | 状态 |
| --- | --- | --- |
| 1 | 真实配对损失分布复算 `β̃/β` | 进行中 |
| 2 | `lr_K = c·β_K` 生产落地范围 | 待办 |
| 3 | §2.6.3 位移上界常数核实 | 待办 |
| 4 | 探针脚本可复现性 | 待办 |
| 5 | 文献前提可迁移性（§2.5 三条） | 已派子代理，待回 |

---

## 零、服务器只读核验（先做，防抢占）

`【实测】`2026-09-04 审核时刻，服务器只有一个任务在跑：

```
14464  42:11  uv run --no-sync python tools/ch3_ft_lspr24_descriptive_eval.py
       --cell-runs c00=...,c10=...,c01=...,c11=ch3-ft-c11-entitybce-halfwidth-screening-v1
       --output-root runs/diagnostics/ch3-ft-lspr24-eval-halfwidth-final
```

GPU `22%` 利用率、`2692 MiB / 32607 MiB` 显存。
**`ch3-ft-c01-tailbase` 训练臂在进程表中不存在**（`ps -eo pid,etime,args | grep [p]ython`
只返回上述评价进程与平台自带的 tensorboard／jupyter）。
本审核全程只读，未启动任何远端计算。

---

（后续小节按完成顺序追加）
