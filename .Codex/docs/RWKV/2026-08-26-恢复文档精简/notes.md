# RWKV 恢复文档精简笔记

## 基线与范围

- 实施前基线：`5a9bc596f80b9b6c8dec9dc29e361f10ac13ef1d`。
- 规划提交：`a485a5f`。
- 五个目标在修改前均为干净状态，共享暂存区为空；没有创建空快照提交。
- 本轮未访问服务器、未改生产代码、实验计划、研究问题卡或其他代理文档。

## 当前权威状态

- 有效 `2×2` 计划提交 `a3a4cd4` 已冻结；实现任务已开启，但本次只读核对时工具、配置、启动器和实施报告尚未出现，不能写成实现完成。
- 四篇漂移全文审计提交 `5a9bc59` 已完成：只放行一轮 LSPR23 固定 mean-max 零训练 S0，不放行训练层多算子。
- 规则链 P0 提交 `f960bf42` 已完成，远程脚本链余量为 `4,977` 字节。
- XGBoost 同数据集论文配方来源审计进行中；独立代理所有权仅限其审计目录，已核 Dijk 2026 PDF 第 23 页与表 5，最终四层矩阵尚未提交。
- 研究问题卡存在其他代理未提交修改，本轮不触碰。

## 前后收据

| 文件 | 前行数 | 后行数 | 行变化 | 前字节 | 后字节 | 字节变化 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `RWKV路线总控.md` | 74 | 50 | -24 | 8,826 | 5,595 | -3,231 |
| `RWKV当前恢复卡.md` | 14 | 15 | +1 | 883 | 1,088 | +205 |
| `2026-08-08-第三章候选方案登记册.md` | 92 | 52 | -40 | 16,451 | 6,640 | -9,811 |
| `RWKV第三章恢复卡.md` | 319 | 98 | -221 | 45,074 | 10,148 | -34,926 |
| `2026-08-20-第三章神经骨干跨进程协作看板.md` | 719 | 79 | -640 | 144,251 | 9,021 | -135,230 |
| **合计** | **1,218** | **294** | **-924** | **215,485** | **32,492** | **-182,993** |

## 删除与保留依据

### 路线总控

- 保留：恢复顺序、路线边界、83 字段、LSPR23/LSPR24 边界、独立 CUDA Raw83 从头训练、规范本地与远端路径、凭据键和章节索引。
- 下沉：ProxyCommand、Shell 初始化、内存门退出码等运行细节改为链接运行、远程脚本和神经训练合同。

### 当前恢复卡

- 继续只作兼容跳转页；增加当前协作看板和精简审计入口，不承载活动状态。

### 候选登记册

- 保留每个候选的状态、最小否决或待证理由、证据路径。
- 删除并发内存事故时间线、完整 Q0 数字、历史排序和跨章流水；这些仍可从专项报告与 Git 基线读取。
- 新增当前有效 `2×2` S0 行，明确“实现中、实验待证”。

### 第三章恢复卡

- 保留当前 Goal、S0 合同、八项已核实结果、四项活动任务、三项状态待核运行、七个阻断、六个下一动作、不得重开路径和证据索引。
- 删除 8 月 22 日和 25 日重复覆盖段、旧未提交候选清单、已关闭探针事故、过期连接和磁盘叙事、旧任务编号流水及重复数值。

### 协作看板

- 保留当前任务表、Claude 接手条件、有效四格合同、远端防重复边界、文件所有权协议和证据入口。
- 删除逐分钟消息日志、过期资源快照、N-01 至 N-16 历史执行合同、旧磁盘释放推导、会话额度移交、代理自述验收和已关闭事故全文。
- 历史消息只通过 `git show 5a9bc59:<路径>` 与专项快照追溯，不再常驻活动入口。

## 恢复能力检查

新会话按 `AGENTS.md → RWKV路线总控.md → RWKV第三章恢复卡.md` 可以直接回答：

1. 当前 Goal 是什么：只实现有效 `2×2`，随后运行一轮 LSPR23 S0。
2. 哪些事实已完成：漂移审计 `5a9bc59`、规则 P0 `f960bf42` 与关键历史结果。
3. 哪些任务有所有者：有效 `2×2` 实现与 XGBoost 配方审计均保留所有权，禁止重复领取。
4. 哪些运行不能重复启动：CUDA-RWKV、TabM32 和 FT-Transformer 均标状态待核。
5. 当前阻断与下一动作：恢复卡和看板均提供可执行表格。
6. 历史如何追溯：基线提交、全量进度快照、候选详情和故障手册均有入口。

## 验证错误记录

- 首次相对链接脚本使用 `${item#*](`，被 zsh 当作非法模式；改用固定两字符切片。
- 第二次脚本误把变量命名为 `path`，触发 zsh 的特殊 `path/PATH` 绑定，导致后续 `rg` 不可见；改名 `link_target` 后通过。
- 尝试创建只读辅助探索代理时触发团队线程上限，未创建代理、未产生文件变化；改由主任务完整分段回读。

## 未关闭风险

- 本轮未访问服务器，三项远端运行只能写“状态待核”，不能确认仍在运行、完成或失败。
- 有效 `2×2` 三件套在本次核对时尚未出现；实现完成状态必须以后续差异和入口验证为准。
- XGBoost 配方审计仍在进行，当前恢复文档只登记已核范围，不预写最终来源裁决。
- 研究问题卡的他人未提交修改仍需主控协调，不得由本轮精简覆盖。
