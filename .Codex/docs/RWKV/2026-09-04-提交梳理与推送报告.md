# 2026-09-04 提交梳理与推送报告

- 工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- 分支：`codex/ch4-dtep-pbc-20260819`（非 `main`）
- 梳理时间窗口：`2026-09-03 17:00` 至今（`2026-09-04`）
- 本窗口提交数：**37**（`git log --oneline --since='2026-09-03 17:00' | wc -l`）

## 一、按主题分组

### A. 代码修复（fix，改动 `thesis/experiments/llm_probe/tools/*.py` 运行逻辑）—— 4 条

| 提交号 | 时间 | 说明 |
| --- | --- | --- |
| `eee9aaf2` | 09-03 20:07 | fix(ch3-ft): 本机筛选档 `d_token` 从 48 回调到 32（仍单层），依据本机实测加速比（d=32 相对 d=96/3层 得 8.47 倍，四格约 21 小时；d=48 只有 6.02 倍）改判 **[改代码]** |
| `aa884263` | 09-03 21:04 | fix(ch3-ft): 修复 `_flow_phase_bare`（C01/z2=1 流相位）从未接入实体级 BCE 公共基线项的接线缺陷——`mechanism.entity_bce.enabled=true` 时该相位此前静默失效，而 C00/C10 已生效 **[改代码]** |
| `21e95ccd` | 09-03 21:32 | fix(ber): 把耦合快照两处 `require()` 硬性断言改为披露-降级（打印 + 空快照/截断集合），符合仓库"披露优先于中止"的软观测量规范；31 份配置的身份哈希回归复核通过 **[改代码]** |
| `5a9943aa` | 09-03 19:43 | fix(ranking): 首位 `amin` 运算改用 fp32，修复该算子在 MPS 设备上的兼容性问题 **[改代码]** |

### B. 新增能力（feat/refactor，新增或改造筛选档 / 观测量）—— 4 条

| 提交号 | 时间 | 说明 |
| --- | --- | --- |
| `da6c27d4` | 09-03 17:12 | feat(ch3): 新增本机 MPS 筛选档配置（两份 JSON，含显式环境标记），仅新增配置文件，未改 `.py` |
| `b83e374e` | 09-03 20:03 | feat(ch3-ft): 新增 quarter-width 单层筛选档 `local_screening_quarter_width_single_layer`（`d_token=48`，参数量 27487，由 `base.expected_parameter_count` 同一闭式算得），供本机 MPS 跑完第三章四格筛选 **[改代码]** |
| `574d48d1` | 09-03 21:29 | feat(ber): 按 2026-09-01 预注册实现机制耦合观测量 `CV_K`（xi 波动性）、`J_K`（活动集 Jaccard）、`tau`（尾部嵌套一致性），写入 `receipts/mechanism-coupling-<epoch>.json` **[改代码]** |
| `766888a5` | 09-04 10:42 | refactor(ch3-ft): 废弃本机 `LOCAL` 档（删除两份 `ch3-ft-LOCAL-mps-*.json` 配置），新增 width56 筛选档 **[改代码]** |

### C. 工具（hook 扩展 / 档位核对工具）—— 2 条

| 提交号 | 时间 | 说明 |
| --- | --- | --- |
| `65f678d3` | 09-04 10:33 | feat(hooks): 扩展 `.Codex/hooks/on_demand_rule_reminder.py`，注入 0903 事故的工作流派发教训（非 `tools/` 下文件，**不影响 `tools/*.py` 的 code_sha256**） |
| `6b092274` | 09-04 10:35 | feat(ch3-ft): 新增档位身份核对工具 `tools/ch3_ft_tier_identity.py`（新文件，149 行），供上线前复核档位一致性 **[改代码]** |

### D. 文档裁决（docs）—— 27 条

| 提交号 | 时间 | 说明 |
| --- | --- | --- |
| `fbaabf1c` | 09-03 17:43 | docs(recovery): 记录数据集的真实存放位置 |
| `87009c65` | 09-03 17:38 | docs(recovery): 记录本机改造与子抽样验证 —— **注意：本提交标 `docs`，但同时给 `tools/ch3_ft_c00_dual_selection.py` 新增 148 行实体分层抽样代码（`entity_subsample_spec_from_config` 等），实质改了代码。类型前缀与内容不符，见下文合规性检查 §3** |
| `1ca36aa8` | 09-03 17:52 | docs(recovery): 二次精简前先做恢复卡快照 |
| `2e8e8cf0` | 09-03 18:03 | docs(recovery): 新增恢复卡二次精简归档文件 |
| `fe1aadb2` | 09-03 18:07 | docs(recovery): 修复恢复卡内部自相矛盾之处 |
| `58a2537e` | 09-03 18:15 | docs(recovery): 把已定论内容从恢复卡移入归档 |
| `efd86105` | 09-03 18:20 | docs(recovery): 压缩恢复卡剩余小节 |
| `beb3644b` | 09-03 18:23 | docs(recovery): 统一步速读数并精简卡片小节 |
| `a0fafd7d` | 09-03 18:23 | docs(recovery): 修正过期的调度器与联系方式记录 |
| `7c0bc450` | 09-03 18:24 | docs(recovery): 记录本机筛选臂的实测状态 |
| `765428f1` | 09-03 19:26 | docs(methods): 新建 `BER活动集坍缩与安全筛除机制.md`，测量 CVaR 活动集坍缩并起草安全剪枝方案（首次提交） |
| `d27ba25f` | 09-03 19:35 | docs(rwkv-ch3): 依据 10 轮次回归证据（`empty_active_step_frac` 在 6-10 轮为 0.146-0.287）修正 EAS 误判登记条目 |
| `cb20d1da` | 09-03 19:37 | docs(methods): 为安全剪枝补充理论深度、耦合与工作量说明 |
| `f08084683` | 09-03 19:37 | docs(ch4): 依据全轮次活动性证据恢复 EAS 候选资格（HSE 维持否决） |
| `8f05ce8d` | 09-03 19:39 | docs(methods): 修正活动率分母口径为"对"而非"实体" |
| `693af689` | 09-03 19:57 | docs(methods): 新建 `第三章定案.md`，落定状态与机制耦合主张（首次提交） |
| `a263b3de` | 09-03 20:06 | docs(methods): 在第三章定案中撤回交互项判据 |
| `89717ff5` | 09-03 20:07 | docs(recovery): 撤回交互项判据，登记新的四格设计 |
| `58572e5e` | 09-03 20:12 | docs(methods): 修正参数量计数并采用第六档宽度 |
| `b9a2af1d` | 09-03 20:08 | docs(ch3-ft): 记录本机筛选缩容档实现与回归的收据 |
| `558a91d4` | 09-03 20:54 | docs(review): 在核验前抢救 54 条复审发现 |
| `bb9d4b5c` | 09-03 20:56 | docs(methods): 按四臂设计重写执行计划 |
| `9e2e3753` | 09-03 21:08 | docs(rwkv): 记录 C01 实体级 BCE 接线回归报告 |
| `31997c67` | 09-03 21:11 | docs(methods): 落地"controller 折入 g_flow"的裁决，关闭该争议 |
| `fedbc632` | 09-03 21:18 | docs(methods): 记录病态验证批次的测量结果并修正预算 |
| `d5e1e329` | 09-04 10:28 | docs(prereg): 在跑 C11 前为预注册补五处记号歧义规则 |
| `e0a6a301` | 09-04 10:35 | docs(methods): 第三章定案新增档位辨析表与启动前复核清单 |

## 二、`code_sha256` 变更提交（改了 `thesis/experiments/llm_probe/tools/*.py` 的提交）—— 本轮最要紧的一类

按时间先后，共 **9 条**，涉及文件 `tools/ch3_ft_c00_dual_selection.py`、`tools/ch3_ft_entity_ranking_loss.py`、`tools/ch3_ft_tier_identity.py`（新增）：

1. `87009c65`（09-03 17:38，标 docs，实为代码变更）—— 新增实体分层抽样支持
2. `5a9943aa`（09-03 19:43，fix）—— 首位 amin 改 fp32
3. `b83e374e`（09-03 20:03，feat）—— 新增 quarter-width 筛选档
4. `eee9aaf2`（09-03 20:07，fix）—— 筛选档 d_token 48→32
5. `aa884263`（09-03 21:04，fix）—— 接入 C01 实体级 BCE
6. `574d48d1`（09-03 21:29，feat）—— 新增机制耦合观测量 CV_K/J_K/tau
7. `21e95ccd`（09-03 21:32，fix）—— 耦合快照断言改披露-降级
8. `6b092274`（09-04 10:35，feat）—— 新增档位身份核对工具（新文件）
9. `766888a5`（09-04 10:42，refactor）—— 废弃 LOCAL 档、新增 width56 档

**结论：本窗口内四格实验相关的运行代码在 `09-03 17:38` 之后经历了至少 9 次实质性改动，最后一次改动落在 `766888a5`（09-04 10:42）。任何早于该提交拉取或缓存的代码状态都不能代表当前 `code_sha256`；四格正式实验必须基于 `766888a5` 或之后的工作树重新拉取/校验代码身份哈希。**

## 三、纯文档提交（不影响运行代码）

除上表"D. 文档裁决"27 条外，`65f678d3`（hook 扩展，改的是 `.Codex/hooks/`，不在 `tools/` 下）也不影响 `tools/*.py` 的 `code_sha256`，但改变了 hook 行为，非纯文档，单列于"C. 工具"分组。

**纯文档提交（27 条，见上表 D）**：均只修改 `.md` 文件（`thesis/methods/*.md`、`.Codex/docs/RWKV/*.md`），不影响任何 Python 运行代码。

## 四、提交信息合规性检查

检查项：Conventional Commits 格式 `type(scope): description`、type ∈ feat/fix/refactor/docs/chore/test/revert、description 英文动词开头首字母小写无句号、单行 ≤72 字符、禁止 `Co-Authored-By`。

### 4.1 通过项

- **格式**：37 条提交主题行全部匹配 `^[a-z]+\([a-zA-Z0-9_-]+\): .+$`，无一违反。
- **type 取值**：全部落在 `docs`(27) / `feat`(5) / `fix`(4) / `refactor`(1)，均属允许集合。
- **description 首字母**：全部小写开头，无大写违规。
- **句末标点**：全部无尾随句号。
- **Co-Authored-By**：**全部 37 条提交均未发现任何 `Co-Authored-By` 署名**（逐条 `git log -1 --pretty=%B` 扫描确认）。

### 4.2 不合规项（仅列出，未改写历史）

**主题行超过 72 字符（2 条）**：

| 提交号 | 长度 | 主题行 |
| --- | --- | --- |
| `574d48d1` | 76 | `feat(ber): add mechanism coupling observables per 2026-09-01 preregistration` |
| `d27ba25f` | 80 | `docs(rwkv-ch3): correct EAS misjudgment registry entry after full-round evidence` |

**正文（body）中文说明存在单行超过 72 字符的情况（8 条，规则文本对 body 是否强制 72 字符换行表述模糊，此处如实列出供裁决）**：
`21e95ccd`、`574d48d1`、`aa884263`、`b9a2af1d`、`eee9aaf2`、`b83e374e`、`f0808468`（英文 body 两行超限）、`d27ba25f`。
这些提交的中文/英文背景说明段落里有若干行超过 72 字符（最长 101 字符），规则原文"body 可中文说明背景原因"未明确 body 是否豁免 72 字符换行要求，故只如实登记，不视为确定违规。

**类型前缀与内容不符（1 条，语义问题非格式问题）**：

- `87009c65` 标 `docs(recovery)`，但实际给 `tools/ch3_ft_c00_dual_selection.py` 新增 148 行代码（实体分层抽样支持）。按 Conventional Commits 语义应属 `feat(ch3-ft)`。已在 §2 中标注为 `code_sha256` 变更提交，不影响本条的可追溯性，但提交类型标签具有误导性。

**均未改写历史**——上述问题只登记，不做 `git commit --amend` 或 `rebase`。

## 五、推送

### 5.1 推送前状态核对

- `git status --short`：工作树存在大量未提交改动/未跟踪文件（详见下文"未触碰清单"），**均未纳入本次任何暂存或提交**。
- 远端 `codex/ch4-dtep-pbc-20260819` **不存在**（`git ls-remote --heads origin codex/ch4-dtep-pbc-20260819` 空输出；`git branch -a` 亦确认远端只有 `main` 与 `codex/ns3-pinn-m012-20260720`），因此 `git log origin/<当前分支>..HEAD` 不适用。
- 该分支相对 `origin/main`（merge-base `6f73b873`）领先 **831** 个提交（`git rev-list --count origin/main..HEAD`）——即本分支此前从未推送过，此次是**整分支首次推送**，本轮任务梳理的 37 条只是其中最新的一段，**不是**本次推送的全部内容。
- 本地仓库对象总量约 `1.57 GB`（pack）+ `170 MB`（loose），首次推送预计耗时较长。

### 5.2 将要推送的提交清单

- 因远端无此分支，推送内容 = 当前分支全部 831+ 个提交（相对 `origin/main`），而非仅 37 条。
- 本轮任务范围内的 37 条提交（`2026-09-03 17:00` 至 `2026-09-04 10:42`）已在 §1/§2/§3 完整列出，均包含在待推送范围内。

### 5.3 推送结果

**成功。** 执行 `git push -u origin codex/ch4-dtep-pbc-20260819`，退出码 `0`：

```
To github.com:NatsuiroGinga/bilibili-note.git
 * [new branch]      codex/ch4-dtep-pbc-20260819 -> codex/ch4-dtep-pbc-20260819
branch 'codex/ch4-dtep-pbc-20260819' set up to track 'origin/codex/ch4-dtep-pbc-20260819'.
```

- 远端新建分支 `codex/ch4-dtep-pbc-20260819`，共推送 **869** 个提交（`git rev-list --count HEAD`），其中相对 `origin/main` 领先 **831** 个提交。
- 推送后核对：`git rev-list --left-right --count HEAD...origin/codex/ch4-dtep-pbc-20260819` 返回 `0 0`，本地与远端完全同步。
- GitHub 返回两条非阻断警告（不影响推送结果）：`thesis/experiments/genis/runs/q0/track-a-60s-subcategory13-xgb-support-tail-seed42-v1{,-rerun1}/data/fit/features.npy{,.partial}` 各 51.29 MB，超过 GitHub 建议的 50 MB 阈值，建议后续改用 Git LFS 管理该类运行产物（本次任务范围外，未处理）。
- 未创建 PR、未触碰 `main`、未使用 `--force`。

## 六、未触碰清单（按任务约束核实）

- `thesis/experiments/llm_probe/tools/` 下 15 个未提交改动（`ch3_cuda_rwkv_raw83_qualification.py`、`ch3_full_mlp_strict_past_cpa_protocol_a_q0.py`、`ch3_grande_protocol_a_source_q0.py`、`ch4_xgb_two_stage_confidence_budget_q0.py` 等）——**未暂存、未提交、未格式化、未 checkout**。
- `.claude/settings.json` 存在未提交改动——**未提交**；仅在本机工作树生效，换机器需手动重加。
- 全程未使用 `git add .` / `git add -A`，未运行任何格式化器或实验/训练命令。

## 七、发现但未处理的问题（供用户裁决）

1. `87009c65` 提交类型 `docs` 与内容（新增 148 行代码）不符，建议后续同类改动改用 `feat(ch3-ft)` 或 `fix(ch3-ft)` 前缀，避免代码变更被文档提交淹没、影响 `code_sha256` 变更追溯。
2. `574d48d1`、`d27ba25f` 两条主题行超过 72 字符规范上限。
3. 8 条提交的 body 中文说明存在超长行（最长 101 字符），是否豁免 72 字符规则需用户明确裁定；本报告不做主观判断。
4. 本分支相对 `origin/main` 落后推送 831+ 个提交，属于长期未同步的历史分支，首次推送体量较大，建议用户知悉后续网络/耗时情况。
5. 推送成功但 GitHub 报告两份 `.npy` 运行产物（各 51.29 MB）超过 50 MB 建议阈值（`thesis/experiments/genis/runs/q0/track-a-60s-subcategory13-xgb-support-tail-seed42-v1{,-rerun1}/data/fit/features.npy{,.partial}`），未阻断本次推送，但仓库中可能已积累其他同类大文件；是否引入 Git LFS 或调整 `.gitignore` 需用户裁定（改 `.ignore`/`.gitignore` 涉及仓库边界规则，本任务未处理）。
