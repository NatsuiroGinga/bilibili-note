# 骨干专属论文配方冻结任务计划

## 目标

基于 ResMLP 与 TabM 原论文全文及官方源码，冻结可审计的骨干专属训练配方候选；明确论文直接参数、官方源码默认、当前任务适配和最小预注册候选，且不接触 LSPR24 最终测试标签或结果。

## 边界

- 工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- 独占输出：`.Codex/docs/RWKV/2026-08-20-骨干专属论文配方冻结/`
- 允许新增：缺失的 TabM 原件、全文笔记、方向索引条目、资源笔记和 Zotero 记录。
- 禁止修改：实验代码、共享配置、恢复卡、路线总控、其他代理文件。
- 禁止访问：服务器、远程实验制品和 LSPR24 最终测试结果。

## 阶段

- [x] 阶段 1：读取根、`raw/`、`wiki/`、`.Codex/docs/`、RWKV 局部规则并建立任务检查点。
- [x] 阶段 2：读取 RWKV 总控、第三章恢复卡、统一重跑计划和现有实施报告，冻结任务适配边界。
- [x] 阶段 3：核验 ResMLP 原论文、He 残差论文、Gorishniy 表格基线论文及现有结构化笔记；定位官方源码并提取可迁移范围。
- [x] 阶段 4：核验 TabM 原论文正式题录、全文和官方源码；若缺失，完成 `raw → wiki → INDEX → Zotero` 归档。
- [x] 阶段 5：建立参数证据矩阵，区分论文直接参数、官方源码默认、任务适配和无直接依据的任务化设定。
- [x] 阶段 6：写入 `骨干专属论文配方冻结.md`，冻结仅在 LSPR23 实体不相交验证集选择的最小候选，并声明 LSPR24 封印后只评价一次。
- [x] 阶段 7：执行引用、路径、PDF 元数据、Git 范围和文档格式自检；仅提交允许范围。

## 关键问题

1. ResMLP 的图像补丁混合配方中，哪些参数可迁移到固定维度表格输入，哪些依赖图像令牌结构而不可直接迁移？
2. TabM 论文与官方源码对架构、初始化、优化器、学习率、权重衰减、批量大小、训练步数和模型集成规模分别给出什么直接证据？
3. 在约 9 万参数共同线下，哪些差异是骨干结构所必需，哪些只能作为 LSPR23 验证选择的最小任务化候选？
4. 如何防止 LSPR24 目标年结果参与超参数选择，同时不把参数匹配公平性误写成模型族上限？

## 验收条件

- 每个精确数值都注明证据类型、原文页码或源码路径；没有直接依据时明确标注“任务化设定，实验待证”。
- ResMLP 与 TabM 各自至少形成一张四层参数表和一个最小预注册候选集合。
- TabM 若为新增论文，仓库中存在原件、结构化全文笔记、方向索引链接、Zotero 条目和题录核验记录。
- 最终文档明确：候选只用 LSPR23 实体不相交验证选择；LSPR24 封印后一次评价；禁止按目标年结果调参。
- 最终文档明确：约 9 万参数共同线只回答效率与可迁移性，不裁决模型族性能上限。

## 决策记录

- 证据优先级：原始 PDF 与官方源码固定提交高于现有实施报告；实施报告只用于当前任务接口和已做适配的事实恢复。
- 外部搜索结果、摘要和仓库首页只作定位线索，参数主张必须回到全文页码或源码行。
- 不运行实验；本任务只冻结可证伪的预注册候选。

## 错误与阻塞

- OpenReview PDF 直链返回 HTTP 403；已从 arXiv 官方入口取得同一篇 v3 全文。
- MinerU 精确解析缺少本机 API 令牌；已按仓库规则回退本地 `pdftotext` 并逐页回核原始 PDF。
- 本机未安装 `qpdf`；不影响 `pdfinfo`、逐页提取与 SHA-256 完整性核验。

## 当前状态

全部阶段完成。最终冻结文档、TabM 原件、全文笔记、资源笔记、索引和 Zotero 条目均已核验并提交。
