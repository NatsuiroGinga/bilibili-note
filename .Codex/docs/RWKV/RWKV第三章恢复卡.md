# RWKV 第三章恢复卡

<!-- RESEARCH_ROUTE=RWKV -->

- **状态日期**：2026-08-25（快照落盘于 2026-08-26）
- **用途**：第三章唯一当前恢复卡。只承载 CPA-ELP、第三章实验、正文定稿阻断、制品和下一动作。
- **证据边界**：第三章正文主体已写完，但用户已重新开放“可微神经骨干替代 XGBoost”和机制版本化修订；达到目标前仍是定稿中。候选存废追溯才追加读取[第三章候选方案登记册](2026-08-08-第三章候选方案登记册.md)。
- **Goal 与职责分层**：旧 Goal 已清除，新 Goal 为 `active`；职责边界见[第三章 Goal、规则与研究契约的三层职责](2026-08-26-第三章Goal-规则-研究契约三层职责.md)，不得在本卡复制 Goal 全文。
- **当前长期任务**：修正有效 `2×2` 后，只做一轮 LSPR23 选择实验。研究问题卡存在并行未提交修改，仅登记为待修契约，不得据此覆盖本卡。

> **2026-08-25 当日全部实验进度、四格读数、总表帕累托、门禁与探针修复、磁盘回收和待办，
> 完整落在[2026-08-25 第三章实验全量进度快照](2026-08-25-第三章实验全量进度快照.md)。
> 本卡只保留状态与指针；需要精确数字、六档曲线、缺陷因果链或核验状态时读那一份。**

## 一、权威文档

| 文档 | 管什么 |
| --- | --- |
| [第三章实验台账](2026-08-17-第三章实验台账.md) | 每出结果即更新，路径裁定的唯一来源 |
| [协议改正与 2×2 重跑及独立审核](2026-08-17-第三章协议改正与2x2重跑及独立审核.md) | 检查点协议 |
| [主对比表写法与 3.6.2 改写方案](2026-08-17-主对比表写法的四篇文献提取与3.6.2改写方案.md) | 主表结构、主指标次序和落后指标写法 |
| [朱焱雷骨干选型与章节承接实测启发](2026-08-17-朱焱雷骨干选型与章节承接实测启发.md) | 第三章研究问题、双模块组织、骨干适配、消融工作量和表述体例的第一参照 |
| [第三章双机制组合创新的论证结构](2026-08-13-第三章双机制组合创新的论证结构.md) | 机制边界与论证结构 |
| [第三章可微骨干与机制多指标统一合同](2026-08-20-可微神经骨干替代XGBoost目标合同.md) | 唯一规范合同：目标、逐模型适配、开放机制形态、指标层级、年度隔离、帕累托裁决与资源恢复 |

冲突分层：章节组织、工作量和对标体例以朱论文原件为先；运行事实以原始日志和制品为先，路径以实验台账为准，协议以协议改正文档为准。不得为了模仿朱论文覆盖本课题的数据、泄漏、标签、指标、因果、预算或运行有效性事实。

## 二、方法与论证裁决

第三章当前正文版本为 **CPA-ELP**：表示层因果前缀跨流聚合，决策层实体级可学幂平均池化。它是当前强基线而不是永久冻结机制；后续版本允许依据全文和 LSPR23 开发实验修订或替换。两个机制均非首创，增量在于具体化、落到网络攻击状态检测及组合验证；「因果前缀跨流聚合无先例」的主张已作废，Lee & Stolfo 2000 已系统化提出同类计数聚合。

**第三章的论文贡献来自机制，不来自骨干排名。后续骨干与机制实验统一以[多指标统一合同](2026-08-20-可微神经骨干替代XGBoost目标合同.md)和[机制快速验证分级合同](2026-08-21-第三章机制快速验证分级合同.md)为规范：目标是进入或推进同池多指标帕累托前沿，不再要求全面超过单一 XGBoost 参照；`4%` 仅为 Gehri 2023 同源主机级经验区域导出的预注册比较锚点，所有源年选择与目标年描述均按合同记录真实可达 FPR、完整并列组和首次告警的独立实际 FPR，且以六档和完整曲线裁决。下文只记录当前状态与历史事实。**

1. 第三章报告全部骨干，不评冠军。主实验基座沿用先注册的多层感知机，依据是先注册而非最优。
2. 核心表采用「骨干 × 有无机制」同构结构，问题是机制在每个起点上抬升多少，不是哪个骨干最强。
3. 若机制在某骨干上无效或有害，结论改为「适用性受骨干限制」，必须照报，不得删行、改判据或换架构重跑。
4. 禁止用目标年结果挑骨干。第四、五章若按用途选骨干，必须另行冻结同骨干基线与消融合同。
5. 骨干比较不能只看 XGBoost 实体 AP。最终同协议表必须同时报告逐流 AP、实体 AP、`0.1%/0.5%/1%/2%/4%/8%` 六档告警预算曲线和资源开销；LSPR23 选择/折外结果与 LSPR24 跨年度结果分表，禁止混用。
6. 新骨干或机制实施计划在“数据集－模型适配表”后必须完成“朱论文对位表”：研究问题、模型专属输入、模块挂载、对照/消融、指标及不可迁移边界逐项对位；未完成不得实施。朱论文仅作章节体例和工作量第一参照，其他文献辅助机制来源与实现，不得静默替代其组织参照。

## 三、冻结实验事实

### 3.1 主 2×2 与全精度锚点

检查点采用协议 A：LSPR23 实体不相交验证集上单 epoch argmax，理由是先注册。协议 B 给出相反结论，只留档不进论文。已知风险是换检查点策略结果不稳，用户已知悉并接受。

- C00 `33.50%`、C01 `40.85%`、C10 `38.60%`、C11 `51.84%`。
- 本课题的消融增益 `+18.34` 点、交互 `+5.89` 点只由本课题制品支撑。朱表 3-7 的 `+12.30` 是 PGD-20 鲁棒准确率绝对百分点，`+3.30` 是同一绝对百分点口径的超和量；二者只作工作量与量级对位，不等价于本课题指标，亦不得改写为相对错误下降。
- 本章方法全精度锚点为 `e_lp=0.518350415398`、`fap=0.291842625120`、`dr_main=0.703457446809`，来自 `ch3-2x2-fairsel/ch3_2x2_fairsel_results.json` 的 `/cells/C11/`。后续差值不得用印刷值相减。

### 3.2 四骨干同构结果

2026-08-18 协议 A 单次运行结果如下；每格使用自身设计的聚合算子，四骨干参数量均在 `90,242` 的 `±2%` 内。

| 骨干（参数量） | 无机制 | 加两机制 | 增益 | 交互 |
| --- | ---: | ---: | ---: | ---: |
| 多层感知机（90,242） | 33.50% | 51.84% | +18.34 点 | +5.89 |
| 沿流序列因果卷积（91,082） | 27.94% | 43.52% | +15.58 点 | +14.04 |
| 全注意力（90,631） | 9.17% | 21.68% | +12.51 点 | +9.18 |
| 门控循环（90,134） | 16.48% | 19.21% | +2.73 点 | −11.00 |

四行增益均为正；门控循环的负交互按适用范围发现报告。正文原四骨干表仍不含 RWKV-7。RWKV 唯一活动训练路径为独立 CUDA 融合实现，须在合法 Raw83 数据门通过后以独立身份从头训练；真实结果产生前不得进入正文。各格是约 90 千参数等预算实现，不是已发表基线本身；已发表门控循环为 `5,355,649` 参数、全注意力为 `10,729,345` 参数，不得互相替代。旧协议的等参数一维卷积 `52.07%` 也不是本表卷积无机制格 `27.94%`。

### 3.3 全量已发表基线

运行根 `runs/diagnostics/ch3-baselines-full/`，隔离断言通过；XGBoost 四个数字与冻结值逐位一致。

| 方法 | 参数量 | 逐流 AP | 实体 AP | DR@4%FPR |
| --- | ---: | ---: | ---: | ---: |
| 随机森林 | 树集成 | 13.43% | 26.05% | 42.55% |
| XGBoost | 树集成 | 22.24% | 51.29% | 69.28% |
| 一维 CNN | 34,529 | 12.71% | 40.96% | 64.89% |
| GRU | 5,355,649 | 28.77% | 16.00% | 30.85% |
| 全注意力 Transformer | 10,729,345 | 32.57% | 35.36% | 74.07% |
| **CPA-ELP** | **90,242** | 29.18% | **51.84%** | 70.35% |

本方法逐流 AP 低于全注意力 `3.39` 点，DR@4%FPR 低 `3.72` 点；实体 AP 全精度领先 `16.47` 点。Transformer 参数量约为本方法的 119 倍，但不得用参数量冲抵两处落后。GRU 在 `lr=2e-3` 下验证 AP `0.045`，在 `3e-4` 下为 `0.998`；基线须在源年验证集完成多学习率选择。随机森林基线文献归属仍待核：本机 `ssrn-6597680.pdf` 全文无随机森林基线，旧标注「Dijk 2024」指向另一篇工作。

### 3.4 XGBoost 加两机制

源年运行 `ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1` 完成 `11/11` 次拟合，冻结 `semantic168`、`p_raw=2.0`、`p_semantic=1.0`。目标年评价接续运行 `ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1-eval-continuation2` 以 `finished/complete/exit=0` 完成。

- 四格实体 AP：`0.513185 / 0.535333 / 0.530376 / 0.565078`。
- 组合相对无机制提升 `+0.051894`，实体配对重抽样区间 `[0.034673, 0.068907]`。
- 交互 `+0.012554`，区间 `[0.005189, 0.020628]`。
- DR@4%FPR：`0.706117 / 0.815160 / 0.867021 / 0.889628`。
- 预算曲线不全域占优：组合格在 `0.1%`、`0.5%` FPR 下低于无机制格，从 `1%` 起占优。
- 接续运行不重训源年、不持久化目标逐样本分数；结果为 `runs/diagnostics/ch3-xgb-cpa-elp-gpu-oof-seed42-v1-rerun1-eval-continuation2/xgb_cpa_elp_results.json`。

### 3.5 差值订正

二十个差值均用全精度制品复算，三个印刷值相减错误已订正且不改变结论：幂平均相对最大值 `22.67→22.66`，相对全注意力实体 AP 领先 `16.48→16.47`，相对一维卷积实体 AP 领先 `10.88→10.87`。表 3-7 表注的借用指数读数由 `53.19%` 订正为 `53.31%`；双机制指数为 `1.0562171936035156`，对应 `0.533104`，`53.19%` 所用 `p=1.223554` 属序列长度消融。

### 3.6 其他工程事实

- RWKV-7 官方源码固定 BlinkDL/RWKV-LM 提交 `952102498e9ed367ea0a59ee64106916d474d30f`，许可证为 Apache-2.0。当前仅允许独立 CUDA 融合实现从头训练，其工程与效果仍为实验待证。
- 等参数卷积质量检查运行根 `runs/diagnostics/ch3-cnn-backbone-2x2-same-run-c00-v2`，最终退出码 `0`、八格齐全。主口径实体 AP 为 `41.21/64.62/43.32/38.77/51.75/59.81/38.78/30.31`，组合增益 `−2.45`、交互 `−27.96`，三次 C00 极差 `10.91` 点；该实验配方不同于正文同构表，只作内部质量检查，不进正文。
- 该卷积运行曾因第 1165 行过严断言以退出码 `1` 中断；修复前后 `DESIGN_SIGNATURE` 均为 `0508ebd8a57a338d`，六格零重训复用，失败日志保存为 `run.log.failed-assert-1644`。生产脚本 SHA-256 为 `a5ebe329d3d6ce305a6df6cb26ba2863ecb13bc0688799dd47b8e28cd453bd20`，远端一致，无数据入口检查 `43/43` 通过。

### 3.7 骨干升级资格实验

| 候选 | 当前有效进度 | 当前裁决与边界 |
| --- | --- | --- |
| ResMLP2 | 协议 A 四格已完成；LSPR24 C11 逐流 AP=`0.110920`、实体 AP=`0.298882`，相对 C00 分别为 `−0.039514/+0.022818`；组合只在约 `2.44%` 以下误报预算局部占优 | 统一约 9 万参数下不构成 MLP 升级，结果分析提交 `7e03aeb`；只否决本配置，不否决 ResMLP/表格 ResNet 模型族。 |
| TabM4 | 协议 A 四格已完成；LSPR24 实体 AP 为 `0.472662/0.478663/0.360866/0.197274`，C11 为四格最差且六档检测率全档最低 | 当前协议下不支持升级；但源年四格验证逐流 AP 均在 `0.9989–0.9996`，检查点选择信号饱和，骨干失配与选择规则失效尚不可分。 |
| TabM32 论文配方 | 2026-08-25 阶段一已封印，胜出输入候选 `tabm32-input-all-numeric`（口述，**未核验**）。分位数自检探针缺陷已由 `b46950a` 修复。 | 运行中、实验待证。**当日结束时刻状态未核验**（B76 `Connection refused`），恢复时先只读监控不重启。完整规则见 [TabM32 运行交接](2026-08-25-TabM32运行交接.md)；LSPR24 仅在四格选择封印后可评价。 |
| 全容量 MLP | BF16 运行 `ch3-full-mlp-complete-entity-lp-protocol-a-q0-seed42-v1-bf16-v1` 已 `complete/finished/exit0`；严格分析提交 `f951ae8`，总表接入以 `0b88a14/6cfe0d3` 为准。源年开发前沿为 B10/O11；目标 O11 的逐流/实体/最大池化实体 AP=`0.1327105943/0.5854895986/0.5713766131`，真实 FPR≤`0.04` 的最优 DR=`0.7898936170` @ 实际 FPR=`0.0372063930`。 | 在总表已覆盖的目标年完整性能维度中为非支配行，不是冠军；名义 4% 首告警实际 FPR=`0.7905010461`，不得称“4% 全检出”。资源并发使 `fair_efficiency=false`。 |
| RWKV-7（独立 CUDA 融合） | **源年资格已封印**：`seal_sha256=611f4f9b…8b40`、`winning_arm=B`、`winning_adapter=R2`、`winning_capacity=large`，三个目标读取计数均 `0`。LSPR24 四格零物化描述性评价已完成（提交 `525e3f9`），实体 AP 排序 `C01 > C00 > C11 > C10`。 | 源年四格逐流 AP 全在 `0.9999937–0.9999972`，**格间最大差 `3.39e-06` 小于格内末轮与最优落差 `6.37e-06`（C11）**，不能支撑“CPA 有效”或“ELP 有效”任何一句话。LSPR24 读数为描述性评价（`independent_test=false`），只作机制对比与量级判断。全部数字与六档曲线见[全量进度快照](2026-08-25-第三章实验全量进度快照.md)第 1.1、1.2 节。 |
| GRANDE | 当前实体主指标：G-A 实体 AP=`0.5453207104868114`，G-B=`0.43855489543921944`，G-A 高 `0.10676581504759196`；目标读取为 `0`，证据仅 `20` 个正实体。 | 旧逐流 AP 源年门仅属旧协议历史裁决。G-B 只是在逐流选轮信号上胜出，不能据此单独否决 GRANDE 模型族；当前比较仍为实验待证。 |
| 严格过去 CPA | 曲线横轴修复运行 `ch3-full-mlp-strict-past-cpa-protocol-a-q0-seed42-v1-rerun1` 已 `complete/finished/exit0`，`verdict=source_gate_rejected`，只读复用旧 S10/S11 两个 20 轮检查点，`new_cells_trained=[]`、`target_year_arrays_read=0`。S10 对 B10 的实体 AP 与逐流 AP 门失败；S11 对 O11 全门通过。 | 预注册要求两分支共同通过，完整严格过去 CPA Q0 候选源年否决且不读取 LSPR24。S11 正向信号只作交互线索，不能据此改门或宣称机制有效。详见[源年 Q0 结果记录](2026-08-21-严格过去CPA协议A源年Q0结果记录.md)。 |
| FT-Transformer | 2026-08-25 已在 B76 真实训练中，口述最后已知进度约 `epoch 10/20`（**未核验**）。启动器磁盘百分比门与 TabM32 互斥门已改披露，改动**未提交**。 | 运行中、实验待证。**当日结束时刻状态未核验**（B76 `Connection refused`）。仍待关闭口径互斥与对照 LSPR24 评价资格裁决，不得把在跑进度写成效果结论。 |
| 表格 ResNet v2 | 源年已 `source-qualified`、四格封印、目标读取 `0`（口述）。LSPR24 评价实现提交 `66873dd`，四格实体 AP `0.733315514678 / 0.715067122411 / 0.644183250571 / 0.619348096091`，排序 `C00 > C01 > C10 > C11`。 | **该 LSPR24 结果口径存疑，不得进入总表或正文。** 其评价从 `dijk-repro/cache/X24.npy` 取特征并叠加候选 A/B 变换，而源年训练消费的是共享 Raw83 产品，两侧不同源（源码已逐行核实）。须按 CUDA-RWKV 的正确路径重跑，详见[全量进度快照](2026-08-25-第三章实验全量进度快照.md)第三节。 |
| RWKV-8 | 官方当前反转/ROSA 路径缺少与冻结协议兼容的反向传播 | 只能前向构造，当前不可作为公平可训练骨干。 |

ResMLP2 与 TabM 的统一协议 A 重跑见[实施计划](2026-08-20-ResMLP2与TabM协议A四格重跑实施计划.md)。两者均执行 `C00/C01/C10/C11`，在 LSPR23 实体不相交验证上选轮次，选择封印后各自在 LSPR24 评价一次；LSPR24 已被历史实验访问，因此本轮是直接可比实验，不重新获得不可见最终测试身份。

### 3.8 骨干比较的双轨证据合同

朱焱雷第三章没有让 ET-BERT 与 BUPT-CNN 等参数或共用训练超参数；表 3-3 分别采用 AdamW 与 Adam、`2×10^-5` 与 `10^-3` 学习率、批量 32 与 128、20 与 50 轮，以及不同的权重衰减和学习率调度。第三章骨干升级因此拆为两条证据：

1. **统一预算协议 A**：约 9 万参数、相同训练预算、四格消融，回答机制可迁移性、轻量化效率和受控比较；不得据此裁决 ResMLP2、TabM 或神经模型族的能力上限。
2. **骨干专属论文配方**：候选必须有全文论文、官方实现或既有真实实验依据，只在 LSPR23 实体不相交验证集选择，封印后在 LSPR24 评价一次；回答合理配方下的可达能力。

两条结果分表保留，均报告逐流 AP、实体 AP、六档告警预算曲线和资源开销。XGBoost 的 800 棵树不能机械折算成神经网络参数，因此约 9 万参数只定义轻量化部署预算，不是跨模型族公平性的唯一尺度。

上述边界适用于所有曾在协议 A 中压缩到约 9 万参数的神经骨干，而不只适用于 ResMLP2 与 TabM。约 9 万参数结果统一归入「等预算迁移与效率表」；另建「合理资源范围和论文配方下的可达性能表」，允许各骨干在全文论文、官方实现和 LSPR23 实体不相交验证支持的范围内选择容量与训练配方，封印后才可评价 LSPR24。不得用第一张表宣称模型族能力上限，也不得用已读 LSPR24 反向选择第二张表的候选。

可达性能补跑优先级为：全容量多层感知机、论文配方 TabM、论文配方表格 ResNet/ResMLP、FT-Transformer；RWKV-7 需先证明独立 CUDA 训练合同可合理适配当前任务。两张表都必须报告逐流 AP、实体 AP、六档告警预算曲线和资源开销，不以单一实体 AP 裁决。后续独立骨干实现先按[可微神经骨干统一合同的逐模型适配矩阵](2026-08-20-可微神经骨干替代XGBoost目标合同.md)和[LSPR 数据与 RWKV 适配性分析](2026-08-20-LSPR数据与RWKV适配性分析.md)冻结输入适配；83 字段只约束信息预算，不要求扁平张量或规范化相同。

神经骨干实验的跨进程所有权、GPU 排期、断点恢复和消息交接统一记录在[第三章神经骨干跨进程协作看板](2026-08-20-第三章神经骨干跨进程协作看板.md)。N-01 ResMLP2、N-02 TabM 与 N-06 全容量 MLP BF16 已完成；独立 CUDA-RWKV 资格入口静态 `READY`、真实实验待证；N-11 GRANDE G-A/G-B 训练完成但发布失败；N-13 实现交付仍待裁决；N-14/N-15/N-16 出现 Claude Code 未提交实现或文档，均须由 Codex 独立审查后才能接受。任何进程不得绕过看板重复启动或修改对方运行根。

### 3.9 机制按轮次封印，不永久冻结

CPA 与 ELP 不是永久不可改的结构。每个实验轮次必须在读取该轮评价数据前封印机制、超参数、选择规则和运行身份；轮次结束后允许依据全文文献、开发区真实实验和完整消融修订、重新组合或替换机制。旧版本结果必须保留，新版本使用独立名称、配置和运行根。

机制替换至少需要：可复核全文或官方来源、明确对应的已证病灶、LSPR23 开发区同预算强基线、无机制/单机制/组合消融、逐流 AP/实体 AP/六档告警预算曲线/资源完整指标，以及预注册的通过与否决门。由已读 LSPR24 触发的修改必须标为 `target-informed`，LSPR24 不得再次冒充独立最终测试；没有更好候选时允许保留旧机制或缩小适用范围。

后续新机制统一执行[机制快速验证分级合同](2026-08-21-第三章机制快速验证分级合同.md)（提交 `b6edbec`）：不改骨干的规则先做 `zero_train`，只新增少量参数时做 `frozen_backbone` 双臂 Q0，需要共同适配时才做 `full_train` 最小因果消融。快速筛选只决定是否增加计算，不能替代正式资格实验。机制发现优先使用全容量 MLP BF16 工作台；RWKV 只允许独立 CUDA 融合实现从头训练；GRANDE 不作为默认机制发现工作台。

## 四、正文定稿状态

- `thesis/chapters/第三章-CPA-ELP/` 的 3.1 至 3.7 已写完；第三章处于定稿阶段，不是已通过自审的定稿章。
- 3.6.2 已按改写方案重写：同构骨干表、交互、已发表基线表、落后指标、告警预算曲线。3.3 已把式 (3-6) 抽象为 `h_t = Enc(x_1,…,x_t) ⊙ m_t`，形状改为 `d` 与 `2d`，式 (3-7) 未改。
- 3.7 已删除会被表 3-7 推翻的断言；3.6.1 已补主口径次序的先验理由；3.1 末句改为实体口径；3.5 的跨年度比较口径冲突已修复。
- 「种子」和统计装置已清除。第三章按单次运行报告，不增补多种子、置信区间或显著性写法。

## 五、定稿阻断项

1. 图 3-6、3-8、3-9、3-10 使用旧数据快照，须重绘；新增图 3-12 告警预算曲线与骨干对照图。图 3-8 图注的 C01 `0.6069` 与正文 `0.5233` 不一致，且 `0.6069<1` 与图注范围冲突。图 3-6 至图 3-11 在 `00-目录.md` 中仍缺图题行。
2. 3.5.1 定理 3-1 的证明与抽象化式 (3-6) 断裂：证明中「只依赖 `x_t`」和一次 `192×83` 矩阵乘均已不成立；定理结论不受影响，须限定到感知机实例或改写常数代价条件。
3. 3.4.4 草稿与 3.6.2 同占表 3-7，须在并入 3.4 时统一重排。
4. 算法 3-1 引用已不存在的符号 `W_1`，须改为编码器参数或先经式 (3-33) 引入。
5. 字数记录存在不同阶段口径：旧卡记录 3.6 为 `9,052` 字、全章 `19,538` 字；2026-08-18 交接记录全章 `19,911` 字。压缩前必须重新实测，不能沿用任一旧数。回收位优先为 3.6.2、3.6.4、3.6.6。
6. 每章定稿须过 `paper-self-review` 六项门禁；阻断项关闭且自审通过前，第三章状态只能写「定稿中」。

## 六、权重与归档

- `runs/diagnostics/ch3-final-weights/` 含 C00/C01/C10/C11 四目录、`manifest.json`、`verification.json`、`inference.py` 和复现分数；主代理尚未独立复核。
- 第四章分桶诊断重跑第三章四格后，逐流 AP、实体 AP(Lp)、DR@4%FPR 对冻结值的绝对差均为 `0`。证据为 `runs/diagnostics/ch4-entity-length-bucket-diagnostic-seed42-v1/aggregate-results.json`。
- 第三章四格模型已上传私有 Hugging Face 仓库 `Heehobino/lspr23-24-causal-prefix-lp-pooling-ch3`，提交 `9081f8b028e6c7a63cce0a64d2dd894958163ab8`，`private=true`，含 20 个 `safetensors` 检查点和 7 个配套文件；清单 SHA-256 为 `024daf41044a0a61476515e46edca8140c3e5cea0aef8a7e7d2d5213964790fb`，推理入口 SHA-256 为 `12766a81f05d81cef2574158d220b24ad343a77316191c1adf1c4671ed7694e5`。

## 七、不得重开的路径

双栈端点归并、规模校准、P2 数值保真、24 配置超参搜索和排序键消融均已关闭。旧「P1 重建选择信号判死」只对第三章同年验证选模结论有效；第四章 E1 已用实体 pooled OOF 给出新的粗筛证据，二者不得互相覆盖。

已否决或不晋级的 C12、C12-R、R1、候选 B、跨年度 P0 和 GeNIS 均为 `screening_only=true`、`formal_paper_evidence=false`，不得写成正式证伪或论文结果。完整状态只在需要候选追溯时读取登记册。

## 八、下一动作

1. 关闭图件、证明、编号和算法符号阻断项。
2. 重新实测字数后按用户裁决压缩，不沿用旧阶段字数。
3. 独立复核定稿权重清单与复现分数。
4. 执行 `paper-self-review` 六项门禁；全部通过后才能把第三章标记为定稿。
5. 活动 Markdown 总表保留 12 个具有当前资格或有效历史证据的模型、LSPR24 11 行，显式记录六档终端 `actual_fpr` 与 `first_alert_actual_fpr`。独立 CUDA RWKV 在合法 Raw83 数据产生真实结果前不登记占位行。XGBoost、CNN、GRU、Transformer 和全容量 MLP 在目标年完整性能维度均为无缺项的非支配行，不是冠军。全容量 MLP 名义 4% 终端实际 FPR=`0.03720639302892392`、首次告警实际 FPR=`0.7905010460927895`。
6. N-07 由 Claude Code 只读盘点 TabM 检查点；无可用末轮/末五轮制品时停止，不擅自重训。
7. 骨干专属论文配方已冻结，后续效果实验使用独立身份，不覆盖约 9 万参数结果。
8. 第三章机制替换文献与实验审计进行中；候选可替换 CPA/ELP，但必须先通过全文和开发区最小实验门。
9. 独立 CUDA-RWKV 正式源年训练正在执行 `A-R2-small-C00`；当前首单元已有 `first-complete-step` 收据、step `900` 完整断点，step `914` 时六项梯度有限且非零、目标读取为 `0`。状态仅为运行中、实验待证。
10. GRANDE 当前按实体主指标比较：G-A 实体 AP 高于 G-B `0.10676581504759196`，但目标读取为 `0` 且仅有 `20` 个正实体。旧逐流 AP 源年门只保留为历史协议，G-B 的逐流选轮信号不单独否决模型族。
11. 严格过去 CPA 的曲线横轴修复 `rerun1` 已完成，旧 S10/S11 两个 20 轮检查点只读复用，未训练新单元，目标年读取为 0。S10 对 B10 的实体 AP 与逐流 AP 门失败，虽 S11 对 O11 全门通过，预注册共同通过规则仍使完整严格过去 CPA Q0 候选源年否决；不读取 LSPR24。S11 仅保留为交互线索，不改门、不宣称机制有效。
12. 骨干升级属于补充资格比较，不把第三章改写成“寻找冠军模型”；任何源年或历史目标年胜出都不能自动替换正文基座。
13. 字段基数诊断 `ch3-lspr23-field-cardinality-receipt-v1` 已完成，仅读 `X23/I23/M23/E23/T23`、目标年读取为 0；收据路径见证据索引。N-12 继续输入候选与参数/资源核算，不自动冻结端口编码，且只在 LSPR23 C00 选择。
14. 所有新神经正式训练默认 [统一 BF16 重训注册表](2026-08-21-神经模型统一BF16重训注册表.md)的 `cuda-bf16-amp-fp32-sensitive-v1`；注册表提交 `930ae2e`、范围修订 `0a53c9d`，精度脚手架 `b74d858/d92296d`、规则提交 `586f8c1/c758763`。GRANDE 的 G-A/G-B 未封印时为筛选同 BF16 比较，TabM32/FT 先在 LSPR23 选输入。只有正文要写候选优劣才补同 BF16 全族；只报最终配置对基线时不重跑淘汰候选。该 profile 是工程默认，不是已证明效果最优。
15. N-12/N-13 的词表和固定源码裁决提交 `956abcb`：两端口保持训练区数值表示，`Protocol` 与 `L3/L4 Protocol` 使用训练区词表加越界桶，三个二元粗拓扑字段按 `{0,1}` 直通。N-12 已完成实现与词表封印，待主进程服务器准入；N-13 实现已交付，仍待口径互斥与对照 LSPR24 评价资格裁决，均不得自行访问服务器或启动。
16. 统一帕累托表提交 `f898277/73b6e67`，长度/连续状态消融设计提交 `6d75824/8e21e2c/b7ba2b9`；初始只做 `reset-128` 对 `ordered-carry-128` 的最小对照，不立即重跑四档长度。结果产生前不得把设计写成连续状态有效。
17. 新机制按[快速验证分级合同](2026-08-21-第三章机制快速验证分级合同.md)（提交 `b6edbec`）推进：S0 零训练、S1 冻结骨干双臂、S2 正式最小因果消融。禁止每个想法默认执行四格 20 轮，也禁止用快速筛选结果替代正文证据。
18. 后续新骨干/机制持续以朱论文原件为第三章第一参照：先完成数据集－模型适配表，再完成朱论文对位表，才实施；章节组织、双模块/互补维度、骨干适配、消融与表述体例优先对照朱，真实数据有效性始终以本课题原始制品裁决。
19. FPR 完整并列组合同已提交 `7e4225c`。XGBoost＋CPA-ELP 运营回填只用 `semantic168/p=1`、零训练：旧 `v1` 因启动器自身 `tee` 误判并发而 `exit=11`，`rerun1` 因错误 `t24` 时间门而 `exit=1`，两者均未进入打分；`rerun2` 已 `finished/complete/exit=0`。严格 1% 点为 `399/752`，名义 4% 终端 `actual_fpr=0.03998878415978258`、DR=`0.8896276595744681`，首次告警 `actual_fpr=0.04941440372710998`、未告警率=`0.08909574468085106`；最大池化实体 AP 与同 `semantic168` C10 一致，为 `0.5303760126246202`。严格分析提交 `e3fc326`。已发表神经三模型回填完成，严格分析提交 `5cb91bc`：CNN 仅在目标年运营子向量支配 GRU；纳入逐流 AP 的完整性能向量后二者均为前沿，Transformer 不在完整曲线上支配 GRU；缺少源年同口径完整曲线，禁止扩写为源年、跨年度或全局结论。新机制修订提交 `b535403`，主候选为独立因果实体排序头、ForkMerge 为辅助更新过滤；唯一下一 S0 为 BF16/FP32×概率/对数几率四臂。
20. [第三章统一资格 Goal 完成审计](2026-08-21-第三章统一资格Goal完成审计/审计报告.md)裁定 Goal 仍为 `active`，当前**不能完成**：全容量 MLP 虽有实质 S2 信号，但缺实施前模型/数据适配表和朱论文对位表，`formal_paper_evidence=false`；关键目标强基线运营回填已完成，但首次告警 `actual FPR` 尚未在共同整数 FP 预算下比较，空精确交集不能自动构成前沿；源年强基线曲线、完整灾备与公平资源仍缺。最低下一步为 N-16 的共同实际首次告警 FP 预算包络；N-12 至 N-15、GRANDE 与 CUDA-RWKV 是可选扩展，不是最低阻断。
21. 当前合同与实现入口：快速机制合同 `b6edbec`、三表/指标层级 `2062427`、朱论文第一参照 `41f9a84`、N-14 计划/数据适配门 `1a1ebbd/9adba6f`；后续执行以这些冻结文档为准。
22. [LSPR 指标体系与 N-16 文献审计](2026-08-21-LSPR指标体系与N16文献审计.md)已把指标职责写回[统一 Goal 合同](2026-08-20-可微神经骨干替代XGBoost目标合同.md)：实体 AP/最大实体 AP 与共同实际 FP 预算的终端、首次告警曲线是应用主裁决；逐流 AP 是必须报告但非一票否决的退化护栏。源阈值跨年迁移、告警负担与困难分面为 P0 独立账，时间片/AUT、校准、合法真实秒延迟与资源完整性为 P1；均不加入主帕累托。它们服务内部全面资格评价，最终论文仅按主论断证据价值进入主表、附表、诊断报告或不披露。N-16 的 `D3=0`，只可称设计可行、实验待证。
23. [全容量 MLP 新机制候选与快速实验](2026-08-21-全容量MLP新机制候选修订/候选与快速实验.md)的当前裁决是：保留 CPA 的因果历史能力，ELP 改为冻结 B10 上的独立实体头；只有 S1 在 LSPR23 共同实际 FP 预算实体运营子向量给出非支配且至少一处严格改善、无低预算新退化的信号后，才从头同 BF16 执行 S2 受控联合训练。旧 CPA-ELP 保持历史强基线；N-16 是评价修复不是机制，严格过去 CPA 同义变体和扩大 `p` 网格均不继续。S1/S2 失败均不得以 LSPR24 追调。
24. 指标文献审计提交 `b2800d2`、N-16 计划提交 `fe00c71`、指标披露合同提交 `065e2ba`。Claude Code 后续写出了 N-15/N-16 工具和“CPA 保留＋独立实体头”方案，但均停留在工作树未提交状态，且尚未经过 Codex 独立审查或真实运行；当前只能写“计划完成、实现候选待审”，不得写实现完成或效果成立。
25. 2026-08-22 远端实测：B76 的 RTX 5090 正常、总显存 `32607 MiB`，当前空闲；不得继续沿用“服务器无 GPU”。GRANDE G-B 已跑满 20 轮，发布失败原因是 SwanLab 标签超过 20 字符，训练制品有效，只做零重训发布修复。
26. Claude Code 在暂停交接前留下 N-15/N-16 工具、机制方案和 N-14 计划修改等未提交内容；这些文件只算待审候选，不因看板自报验收通过而接受。Codex 必须独立检查差异、合同、公式、入口和真实制品后再决定提交、修正或否决。
27. 运行规则提交 `2360d96/82a5982` 已固化四类阻断门和最小可证伪实验原则；它们只约束运行资格与实验设计，不构成效果结论。

## 九、证据索引

见第十二节（本节原为空标题，链接列表一直落在文末，2026-08-26 归位）。

## 十、2026-08-22 当前进度覆盖

本节覆盖本卡与看板中较早的活动状态描述；历史记录保留以便追溯。第三章 Goal 仍为 `active`，以下均为运行事实或待验收状态，尚不构成新的科学结论。

1. **GRANDE**：零重训发布修复已于 2026-08-22 完成，运行根为 `runs/diagnostics/ch3-grande-swanlab-publish-repair-v1`，状态 `complete/exit0/attempt1`。`manifest` 记录 `publish_completed=true` 与 `11` 个文件；SwanLab 运行 `kugxzlt1` 已上传 `69` 条记录，收据 `metric_count=28`。父制品未改、目标读取 `0`、GPU 读取 `0`，本地完整 `27` 文件目录已回收。该运行只修复发布，不构成实验支持；科学资格仍受后续合法 Raw83 数据审计。
2. **N-16**：Claude 实现提交 `a7451d5` 已被拒绝。修复计划 `a2f1070`、生产修复 `5476798` 已关闭科学、内存和哈希问题；与 N-15 共用的单运行 SwanLab 恢复提交 `d6313ca` 已落盘。主进程独立复核后状态为“本地静态候选就绪、未同步/未运行”，不得写为实验支持。
3. **N-15**：Claude 未提交实现已被拒绝。修复计划 `d955096`、生产修复 `a3cf1c5`、报告 `56a1295` 已落盘；与 N-16 共用的单运行 SwanLab 恢复提交 `d6313ca` 已落盘，标签收据路径的确定性阻断已由 `70a8a2b` 修复。主进程独立复核后状态为“本地静态候选就绪、未同步/未运行”，不得写为实验支持。
4. **活动入口清理**：文档与入口清理提交 `d71333c`、`bcefc22`、`8103911` 已完成，持久记忆删除说明已写。活动恢复链只保留独立 CUDA 融合、合法 Raw83 与从头训练。
5. **CUDA 自身门**：实现 `9149128` 的首次真实预检因 `PATH` 缺少 `nvcc` 以退出码 `69` 停止，未创建运行身份；单变量修复为 `761f2c6`。2026-08-22 在 B76 的真实运行 `ch3-rwkv-cuda-self-gate-v1` 已 `finished/complete/exit0`，完成收据 SHA 为 `e85839b...`：四门通过、六项 BF16 梯度均有限/非零/位级重复、三模块更新；环境为 `torch2.13+cu130`、CUDA 13、`sm120`，约 `19` 秒，`max_reserved=71303168 B`。全程无数据、训练或 SwanLab；分析提交 `703c3d3`，只允许进入 Raw83 训练前置链，不能形成模型效果结论。
6. **共享 Raw83 产品**：设计 `eeb0f4a`，最终实现链 `c581a12/f35ad91/ebea0f4/180c405`。B76 源年运行 `ch3-protocol-a-raw83-shared-v1` 已 P0-P5 全部封印，`status=finished`、`sealed_through_stage=P5`。主进程另起只读持久会话执行 `--validate-product`，退出码 `0`，`18` 项制品哈希全部通过；最终 `manifest` 的文件 SHA-256 为 `703b3e20fd26b16bfe1a57c8260df7a16f45c1c66858af5a5f897259eb543929`，内容 SHA-256 为 `93be34eb43ef3dfb3fb2a6586c27416a20b824b81709e3d31fe096261c7b7abc`。源年 `16,353,511` 行、`83` 字段，目标特征与标签读取均为 `0`。P5 阶段收据中的 `partial` 路径仅为发布前记录；活动消费只能认 `current-source` 指针和最终 `manifest`。
7. **Raw83 视图与回收**：A/B 视图物理 SHA 分别为 `3f459c...3325` 与 `c59c6c...08ca`，训练有效流 `11,991,315`、序列 `208,598`。`437` 份非矩阵小制品约 `10.47 MB` 已回收至本地运行根，未回收大矩阵；该事实不构成模型效果支持。
8. **磁盘修订审计**：提交 `f7a44cd` 证明精确 `used=79.7483%` 的比例门通过，但 `free` 门失败，需释放 `5,098,430,464 B`。`.venv`、Raw83、Dijk 与原始数据不可删除；四个已结束历史运行目录共 `5.6851 GiB` 仅可在完整拉取、逐项 SHA 核验及用户明确批准后释放。主进程的非破坏性拉取已被安全门拒绝，当前未复制、未删除，等待用户授权；不得绕门或自动清理。
9. **独立 CUDA Raw83 唯一修订完成**：修订链 `601d529/cd7e5db` 已完成；修订复审发现首次告警二维膨胀、单元资源未隔离和返回哈希外层封装问题，定点修复 `f2ace8c/a4f3f3c` 已完成。最终代码为本地静态候选，可进入目标环境门：完整整数 FP 曲线加仅六锚点 1 基及时阶梯、单元资源全局采样起止窗口加本地冻结切片、源/目标返回外层封装；资源准入、原子锁、代码身份、目标缓存、单元/顶层 `manifest`、20 步恢复、`g` 门 FP32 与 SwanLab 两次上限均已实现。仍未训练或服务器验证，不得写为实验支持。
10. **同步与连接阻断**：主进程尝试同步时，旧 `guarded_rsync` 因硬编码已禁用的 `/tmp` 入口在首文件前失败，未发生传输；随后项目内直连同样失败，当前 `GPU_SSH_B76` 端口 TCP 探测为 `TCP_OPEN=0`。这是远程连接阻断，不是 GPU、代码、磁盘或科学失败。已要求用户在本机环境更新 `GPU_SSH_B76`/`GPU_PWD_B76`，不得在聊天提供凭据。磁盘仍须用户授权拉取历史制品后才可释放，当前无删除。
11. **环境与协作边界**：第三章 Goal 仍为 `active`。B76 已核验为配备 RTX 5090 且 GPU 空闲的服务器，不得再表述为“无 GPU”。Claude Code 与任何子代理的计划、实现、验收和结论均为待核候选，必须独立复核后才可执行、接受或启动。

详见[第三章神经骨干跨进程协作看板](2026-08-20-第三章神经骨干跨进程协作看板.md)第十节的活动状态和执行边界。

## 十一、2026-08-25 当日进度覆盖

本节覆盖第十节中较早的活动状态。**全部精确数字、六档曲线、缺陷因果链与逐条核验状态在
[2026-08-25 第三章实验全量进度快照](2026-08-25-第三章实验全量进度快照.md)**；本节只给状态与指针。

### 完成状态

| 实验 | 状态 | 证据等级 |
| --- | --- | --- |
| CUDA-RWKV 源年资格 | 已封印，四格逐流 AP `0.9999937–0.9999972` | 源年资格门槛证据；**格内波动大于格间差异，不支撑机制有效性** |
| CUDA-RWKV LSPR24 四格评价 | 已完成，提交 `525e3f9` | 描述性评价，`independent_test=false` |
| 表格 ResNet v2 LSPR24 评价 | 已完成，提交 `66873dd` | **口径存疑，须重跑**（见 3.7 表） |
| N-16 共同实际首次告警 FP 预算包络 | 已完成，`envelope_closed=true`、`unreachable_methods={}`、`training_runs=0`，SwanLab `nkylcsif` | 零训练描述性评价；**是评价口径修复，不是机制证据** |
| 全容量 MLP S0 四臂诊断 | 已完成，`exit_code=0` | 零训练筛选；四臂是**精度 × 聚合空间**二因子设计，**不是 CPA/ELP 消融** |
| FT-Transformer、TabM32 | 运行中 | **当日结束时刻状态未核验**，B76 `Connection refused` |

**N-16 裁决**：全容量 MLP O11 在目标年共同首次告警子向量上保持非支配。
该结果关闭了[统一资格 Goal 完成审计](2026-08-21-第三章统一资格Goal完成审计/审计报告.md)点名的最低阻断项。

### 统一总表

运行根 `runs/diagnostics/ch3-metrics-table-20260825b/`，`pending` 由 `9` 降到 `3`
（`available=false` 的三行为旧身份 RWKV-7、GRANDE、TabM32）。
帕累托前沿为全容量 MLP、XGBoost＋CPA-ELP、一维卷积、全注意力、门控循环五行。

**不得据此宣称优劣**：全容量 MLP 实体 AP 比 XGBoost＋CPA-ELP 高 `0.02041118`，
但源年验证集只有 `65` 个正实体、标准误约 `0.124`，该差在噪声内；
DR@4% 一列 XGBoost＋CPA-ELP 反超，且两者实际 FPR 不同
（`0.03998878` 对 `0.03720639`），**不同工作点不可直接比**。

总表另有两处已在源码核实的缺陷未修：`_performance_dominates` 把名义键 DR 与实际 FPR 重算值一视同仁；
不同证据线共用一个帕累托池（3.8 节要求的两条证据线未分表）。

### 数据源硬缺陷（最高优先级，须写入故障预防）

**`runs/diagnostics/dijk-repro/cache/X23.npy`／`X24.npy` 不是 raw83 原始数据，
是 dijk 复现管线标准化后的特征矩阵。** 两者形状同为 `float32 (16353511, 83)`、
**文件字节数相同**，`ls` 与肉眼都看不出差别，但有 `1,224,589,881` 个有限值单元不同。
喂进按 `raw83 → 候选A → 候选B` 拟合的模型会得到从未见过的输入分布，**结果无效却看起来正常**。

判据是**训练侧与评价侧是否同源**，不是「是否用了缓存」：GRANDE 源年本身就训练在该缓存上，口径自洽；
表格 ResNet 与 CUDA-RWKV 按 raw83 拟合，用缓存即错。
正确路径是直读冻结 Parquet ＋ 内存内重建候选视图（CUDA-RWKV 已验证：
重建视图 `mismatched_cells=0`、源年复算七项指标最大绝对差 `0.0`、LSPR24 实体常量全部命中）。

### 门禁与探针修复

已提交：`b46950a`（TabM32 分位数自检探针改地标恒等式，`78832` 个地标偏差 `0`）、
`639710b`（RWKV 核自身门探针改合法秩一约束，`|max|` 由 `2.031e+24` 降到 `0.0718`，25 个数量级）、
`3add5bf`（内存准入门可回收量口径改 `inactive_file + active_file − shmem`；
**B76 cgroup `memory.max=90 GiB`，不是宿主机 754 GiB**）、
`fb3943c`（门禁精简五条判据写进 `llm_probe/AGENTS.md` ＋ 当日缺陷记录）、`dbc51ad`（磁盘盘点）。

未提交：四道并发阻断改披露、S0 启动器 `set +u` 绕 `PS1`、
FT 启动器磁盘百分比与 TabM32 互斥两处、ResNet 启动器补 `PYTHONPATH`。

### 磁盘

删除九个非 LSPR 历史路线目录，实测释放 `9,816,543,232` 字节，
已用比例 `80.6125% → 71.4701%`，可用 `20,817,158,144 → 30,633,701,376` 字节。
盘点报告见[B76 磁盘回收盘点审计报告](2026-08-25-B76磁盘回收盘点/审计报告.md)（提交 `dbc51ad`）。
**注意口径**：部分当日文档写作 `80.5% → 71.2%`，以审计报告的精确读数为准。

### 当日待办

1. 表格 ResNet 的 LSPR24 评价用正确数据源重跑。
2. CUDA-RWKV 与 TabM32 接入总表配置 `configs/ch3-metrics-table-sources-v1.json`；
   CUDA-RWKV 须新增独立身份行，不得复用旧 RWKV-7 行。
3. 修统一总表两处缺陷（名义键 DR 与实际 FPR 混比；不同证据线共池）。
4. 源年验证集仅 `65` 个正实体、实体 AP 标准误约 `0.124`，选择信号精度不足未解决。
5. N-16 那个 `screen -dmS` 第二次会话死因未定位。
6. 规则链在 `scripts/` 下超限（合计 `33,783` 字节 > `32,768`，改动前已存在，另有代理处理）。

## 十二、证据索引

- [2026-08-25 第三章实验全量进度快照](2026-08-25-第三章实验全量进度快照.md)
- [2026-08-25 门禁精简与探针缺陷记录](2026-08-25-门禁精简与探针缺陷记录.md)
- [CUDA-RWKV 四格 LSPR24 零物化描述性评价实施报告](2026-08-25-CUDA-RWKV四格LSPR24零物化描述性评价/实施报告.md)
- [B76 磁盘回收盘点审计报告](2026-08-25-B76磁盘回收盘点/审计报告.md)
- [TabM32 运行交接](2026-08-25-TabM32运行交接.md)
- [第三章实验台账](2026-08-17-第三章实验台账.md)
- [协议改正与 2×2 重跑及独立审核](2026-08-17-第三章协议改正与2x2重跑及独立审核.md)
- [主对比表写法与 3.6.2 改写方案](2026-08-17-主对比表写法的四篇文献提取与3.6.2改写方案.md)
- [第三章双机制组合创新的论证结构](2026-08-13-第三章双机制组合创新的论证结构.md)
- [XGBoost 基线对照与门槛测算](2026-08-17-第三章XGBoost基线对照与门槛测算.md)
- [第三章叙事组织改写清单](2026-08-18-第三章叙事组织改写清单.md)
- [第三章骨干升级与第四章双证据方法双线推进记录](2026-08-20-第三章骨干升级与第四章双证据方法双线推进记录.md)
- [TabM 骨干源年资格实验实施与结果报告](2026-08-20-TabM骨干源年资格实验实施报告.md)
- [独立 CUDA-RWKV Raw83 资格任务计划](2026-08-22-CUDA-RWKV独立Raw83资格/task_plan.md)
- [独立 CUDA-RWKV Raw83 资格实施方案](2026-08-22-CUDA-RWKV独立Raw83资格/实施方案.md)
- [ResMLP2 与 TabM 协议 A 四格重跑实施计划](2026-08-20-ResMLP2与TabM协议A四格重跑实施计划.md)
- [第三章神经骨干跨进程协作看板](2026-08-20-第三章神经骨干跨进程协作看板.md)
- [ResMLP2 协议 A 四格结果分析](2026-08-20-ResMLP2协议A四格结果分析.md)
- [全容量 MLP 骨干专属配方实验设计](2026-08-20-全容量MLP骨干专属配方实验设计.md)
- [全容量 MLP 源年筛选实施报告](2026-08-20-全容量MLP源年筛选实施报告.md)
- [严格过去 CPA 协议 A 源年 Q0 结果记录](2026-08-21-严格过去CPA协议A源年Q0结果记录.md)
- [第三章机制快速验证分级合同](2026-08-21-第三章机制快速验证分级合同.md)
- LSPR23 Protocol A 字段基数收据（远端相对制品路径，尚未回传本工作树）：`runs/diagnostics/ch3-lspr23-field-cardinality-receipt-v1/field-cardinality-receipt.json`
- [骨干专属论文配方冻结](2026-08-20-骨干专属论文配方冻结/骨干专属论文配方冻结.md)
- [LSPR 数据与 RWKV 适配性分析](2026-08-20-LSPR数据与RWKV适配性分析.md)
