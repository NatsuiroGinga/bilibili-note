# Claude Code `fork 2` 接管笔记

## 会话末态

- 原 XGBoost 双机制方案代理因会话限额失败，没有可复用交付。
- 卷积等总参数量 2×2 在会话结束前已启动，但未收到完成通知。
- RWKV-7 四格、等参数基线和排序键消融的最终状态须以 B76 制品核验。

## 接管纪律

- 已排队的测量返回前不裁决。
- 已运行任务只读核验，不重复启动。
- 新 XGBoost 方案经用户审阅后才实施。
- 所有 LSPR24 重分析须登记评价次数，不得据此追加目标年选参。

## B76 只读核验结果

- 当前无 `screen`、无训练进程；GPU 显存占用为 `0/32607 MiB`，磁盘可用 `41 GiB`。
- 跨基座续训 v2 的门控循环、全注意力和一维卷积已完成并回收本机：完整机制实体平均精确率分别为 `0.192112`、`0.216826`、`0.435233`。
- RWKV-7 四格未启动，预期运行目录与退出状态均不存在。
- 等参数神经基线已完成并回收：门控循环 `0.277799`、全注意力 `0.329253`、一维卷积 `0.520653`。
- 卷积等总参数 2×2 在 C00、`lr=0.001`、第 12 轮因回归门失败停止：`0.998778107922 - 0.997611053791 = 0.001167054131 > 0.001`。没有四格结果，不能作科学裁决。
- 排序键事实审计已完成；`available_ns` 排序消融由用户于 2026-08-18 暂停，不实施、不启动、不作为共同基座选择的前置门禁。

## 用户新增裁决

- 第三章完成跨骨干比较后，选择效果最好的一个共同基座供第四章和第五章使用。
- 共同基座选择以完整 `CPA+ELP` 的实体平均精确率为主排序；告警预算曲线、`DR@4%FPR`、逐流平均精确率和效率并列报告，不事后加权。
- 该做法只对标朱论文“第三章跨骨干、后续章节收窄”的结构；朱本人并非按第三章最高分选骨干，正文不得误述。
- LSPR24 已被观察，选择存在目标年偏差；基座一旦冻结不得在第四、第五章因结果不利而更换。
- 所有骨干沿用 `mTimestampStart` 稳定顺序比较，只能支撑“相对序列顺序因果”的表述；严格在线可用性作为限制披露。
