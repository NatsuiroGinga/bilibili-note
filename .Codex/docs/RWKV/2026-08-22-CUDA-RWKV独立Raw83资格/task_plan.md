# 独立 CUDA-RWKV Raw83 资格方案任务计划

## 目标

形成一份只依赖官方固定 CUDA 核、独立模型/训练/评价实现与统一精度/跟踪模块的 Raw83 资格实施方案；本轮只设计、核验并提交文档，不修改生产代码，不访问服务器。

## 文件所有权

- 本任务只拥有本目录中的 `task_plan.md`、`notes.md` 与 `实施方案.md`。
- 仓库中其他已修改、未跟踪或已暂存文件均不修改、不暂存、不提交。

## 阶段

- [x] 阶段一：读取根、RWKV、论文、实验与过程文档规则，恢复第三章路线。
- [ ] 阶段二：核验共享 Raw83 合同、官方 CUDA 源码、当前入口差异、数据适配证据与朱论文全文证据。
- [ ] 阶段三：冻结独立实现边界、分阶段源年选择、数据集－模型适配表、数值门、恢复与资源门。
- [ ] 阶段四：完成实施方案，核对今天可执行的真实前置链、文件所有权和活动路径迁移。
- [ ] 阶段五：执行禁词、链接、差异和提交范围检查，只提交本目录。

## 验收

- 三份文档齐全，正文统一使用“独立 CUDA 融合实现”或“CUDA-RWKV”。
- 明确源年物化、分阶段选择、四格训练的依赖顺序，任何选择不读取 LSPR24。
- 数据集－模型适配表和朱论文对位表逐项完整。
- 官方 CUDA 编译、精度岛、六梯度、首步更新、数值稳定、断点恢复、跟踪与资源门均有机械判据。
- 新增/替换文件所有权与既有入口退出活动路径策略精确到路径。
- `git diff --check`、链接检查和提交范围检查通过；不运行 `black`。

## 状态

当前处于阶段二：证据核验。

## 错误记录

- 首次本地文献索引状态命令因默认 `uv` 缓存目录不可写失败；改用 `/private/tmp/codex-uv-cache` 后成功，并完成增量重建。
