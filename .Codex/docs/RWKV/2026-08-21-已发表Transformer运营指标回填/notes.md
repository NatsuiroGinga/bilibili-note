# 已发表 Transformer 运营指标回填笔记

## 已核事实

- 已发表神经性能包络的严格初始集合为 Transformer、CNN、GRU；Transformer 不能单独代表整个包络。
- 本地只保留发表配置 Transformer 的目标年逐流分数。CNN、GRU 的逐流分数和检查点在本地、Git 引用、对象、暂存与私有模型归档中均未找到。
- Transformer 三个输入数组均为 `20,227,356` 项，单文件 `80,909,552` 字节。
- 只读复算确认六档完整并列组读数为：
  - `0.1%`：FP `46`，实际 FPR `0.0009921705`，DR `0.1183510638`
  - `0.5%`：FP `231`，实际 FPR `0.0049824213`，DR `0.2074468085`
  - `1%`：FP `463`，实际 FPR `0.0099864116`，DR `0.3776595745`
  - `2%`：FP `927`，实际 FPR `0.0199943921`，DR `0.6369680851`
  - `4%`：FP `1,854`，实际 FPR `0.0399887842`，DR `0.7406914894`
  - `8%`：FP `3,709`，实际 FPR `0.0799991372`，DR `0.7952127660`
- 全部实体终端分数含 `46,913` 个完整阈值并列组，其中 `46,161` 个阈值组含负实体。
- 实体内升序逐流索引是现有正式首次告警合同；真实时间不可用。

## 未关闭项

- 远端是否仍保留 CNN/GRU 的逐流分数或选中检查点尚未核验。
- Transformer 只具备目标年后处理证据，不能补源年运营指标。
- 纯模型推理时间、权重字节、检查点字节和模型级独立峰值显存不能由现有分数反推，必须保持缺失。

## 错误记录

- 无。

