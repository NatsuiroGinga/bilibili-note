# 全容量表格 ResNet／ResMLP 骨干协议 A 实施计划

## 一、任务定位

- **编号**：N-14
- **所有者**：Claude Code
- **性质**：骨干专属满血配方实验，不是修改或覆盖当前普通 MLP。
- **目的**：检验表格残差归纳偏置能否改善主要运营指标、实体排序或逐流表示质量，并形成新的端到端可微帕累托点。
- **比较边界**：统一 83 字段、实体定义、LSPR23 固定实体不相交开发划分和统一评价代码；LSPR24 只在全部选择封印后作一次分表描述性评价。

## 二、文献与官方实现

- 原件：`raw/papers/methodology/2021-Gorishniy-Revisiting-Deep-Learning-Tabular-Data.pdf`
- 全文笔记：`wiki/papers/methodology/2021-Gorishniy-表格数据深度学习模型再审视.md`
- 冻结配方：[骨干专属论文配方冻结](2026-08-20-骨干专属论文配方冻结/骨干专属论文配方冻结.md)
- 官方源码：`yandex-research/rtdl-revisiting-models@e3ed46cac38568785289d8fa16b8cfa585bde27e`，Apache-2.0；只读复用 N-13 归档，不与 N-13 并行修改 vendor。

## 三、数据集－模型适配前置门

N-14 在创建工具、配置或启动器前，必须先提交“数据集－模型适配表”。适配表至少覆盖长期硬门所列字段、变换、时间、因果、标签、接口、参数公式及哈希，并明确以下 N-14 合同；未完成不得实现或启动。

| 项 | N-14 必须封印的适配合同 |
| --- | --- |
| 字段分组 | 连续、计数、端口、协议与二元粗拓扑字段逐项列出名称和索引；原始 IP 只作实体/分组键，不得进入模型。 |
| 顺序与因果 | 使用冻结实体内时间顺序，并登记 `I/M` 的读取、切片、补零、有效掩码和历史边界；当前流的表示不得读取未来流、未来标签或目标年信息。 |
| 训练区变换 | 缺失补零、标准化、词表和任一分位数状态均只由 LSPR23 训练区拟合；记录字段清单、训练实体、拟合状态和变换哈希。LSPR24 零参与。 |
| 模型接口 | 记录逐流输入、残差状态、CPA 因果上下文、输出、损失标签/权重及最终实体目标对参数的梯度接口；给出 `387,074` 的参数公式和实例化断言。 |

输入候选只能按如下顺序比较，先在 LSPR23 C00 封印唯一输入，再进入第五节的优化器选择；禁止将输入候选、优化器、容量、结构或机制做无约束笛卡尔扫描。

| 顺序 | 输入候选 | 适用与封印条件 |
| ---: | --- | --- |
| 1 | `standardized-83` | 冻结 83 字段的现有标准化数值视图，保留端口、协议和二元粗拓扑字段的已审计数值处理。 |
| 2 | `source-quantile-83` | 仅在有全文或官方依据支持时比较；分位数只拟合 LSPR23 训练区的连续/计数字段，不作用于端口、协议或二元粗拓扑字段。必须记录依据、拟合状态和变换哈希；任一项无法封印即停用该候选。 |

输入候选比较使用适配表预先指定的单一 C00 锚定配方、固定种子与共同预算；输入封印后才比较第五节的两个优化器候选。不能封印合法输入候选或其训练区变换时，停止 N-14，不以默认输入替代。

当前实验状态只引用既有文档提交 `71076c8`；本计划不构成服务器读取，不访问服务器。

## 四、冻结结构

1. 输入投影 `83→192`。
2. 两个残差块，每块为 `LayerNorm→Linear(192→384)→ReLU→Dropout(0.15)→Linear(384→192)→Dropout(0)→Add`。
3. LayerNorm 替代原论文 BatchNorm，作为当前因果序列和补零安全的任务适配；不得写成论文原配置。
4. CPA 打开时拼接 192 维当前表示与 192 维因果上下文，经 `Linear(384→192)→ReLU→Dropout(0.1)`；关闭时上下文为零，参数量不变。
5. 输出头 `Linear(192→1)`；ELP 分支含一个 FP32 `p_log`。
6. 预期参数量 `387,074`：输入投影 `16,128`，两残差块 `296,832`，融合层 `73,920`，输出与 `p_log` `194`。实例化不一致立即停止。

禁止增加块数、宽度扫描、注意力、门控、多尺度、PLE、端口嵌入或额外归一化层。

## 五、源年配方选择

结构与第三节封印的唯一输入固定后，只比较两个 C00 优化器候选：

| 顺序 | 配方 | 学习率 | 权重衰减 |
| ---: | --- | ---: | ---: |
| 1 | 论文对数中位 | `0.00031622776601683794` | `0` |
| 2 | 论文高四分位 | `0.0017782794100389228` | `0` |

- 每候选 `20×1000` 个完整优化步、有效批量 64、种子 42。
- 每候选内部按 LSPR23 验证逐流 AP 选择最早最大轮次；候选间仍按该值选择，完全相等时按表中顺序。
- 不用实体 AP、预算曲线、资源或 LSPR24 选择配方。

## 六、固定配方后的最小消融

训练 `B00/B10/O01/O11`：分别为无机制、仅 CPA、仅联合 ELP、CPA＋联合 ELP。从 `B00/B10` 同一逐流分数零训练派生完整实体幂平均 `N01/N11`，不得为每个 `p` 重训。

该消融与当前全容量 MLP 完全同构，才能区分残差骨干与机制差异。新机制不在本任务追加，后续遵守[机制快速验证分级合同](2026-08-21-第三章机制快速验证分级合同.md)。

## 七、精度、恢复与制品

- 使用 `cuda-bf16-amp-fp32-sensitive-v1`；线性和激活走 BF16 autocast，参数、AdamW、LayerNorm、损失、概率、实体聚合、评价与 `p_log` 保持 FP32；无 `GradScaler`。
- 梯度裁剪 1.0，无学习率调度；不得减少有效批规避显存。
- 每 20 个完整 `optimizer.step()` 原子保存模型、优化器、轮次、步、最佳状态、采样器、全部随机状态、精度身份和恢复计数；完成候选、完成格和目标评价幂等跳过。
- 不设墙钟或 GPU 小时上限；不持久化逐流/逐实体分数、标签或拆分缓存。

## 八、指标与裁决

指标顺序不可改变：

1. 主要运营指标：六档**实际可达** DR、完整预算曲线、首次告警 1 基 `exposure_index` 的未告警率、分位数与按时检出曲线。
2. 主要排序指标：实体 AP、最大池化实体 AP。
3. 表示护栏：逐流 AP。
4. 工程指标：参数、权重、时间、吞吐、显存、主存、检查点和恢复开销。

名义工作点因并列分数跳出预算时必须报告实际 FPR。主要对照为当前全容量 MLP BF16、约 9 万参数 ResMLP2 历史锚及 XGBoost 性能参照。结果进入骨干专属满血配方表和同年度帕累托表，不覆盖统一预算负结果。

## 九、Claude Code 文件所有权

只新增：

1. 本计划。
2. `thesis/experiments/llm_probe/tools/ch3_tabular_resnet_paper_recipe_protocol_a.py`
3. `thesis/experiments/llm_probe/configs/ch3-tabular-resnet-paper-recipe-protocol-a-seed42-v1.json`
4. `thesis/experiments/llm_probe/scripts/remote_launchers/run_ch3_tabular_resnet_paper_recipe_protocol_a_seed42_v1.sh`
5. `.Codex/docs/RWKV/2026-08-21-全容量表格ResNet骨干协议A实施报告.md`
6. 协作看板 N-14 行和消息日志。

不得修改 MLP、RWKV、GRANDE、TabM32、FT-Transformer、统一总表或活动运行根。不得访问服务器、同步或启动；服务器动作归后续主进程。

## 十、本地验收

- 调用 `$pytorch-patterns`，第三方接口先按 Context7 核验。
- 在代码前提交并核验第三节的数据集－模型适配表；适配表未封印时，任何实现、同步和启动检查均不得开始。
- 通过 `py_compile`、导入、`--help`、`--validate-config`、JSON 和 `bash -n`。
- 生产阶段无 `NotImplementedError`；真实干入口按依赖给出明确错误，不能以函数存在冒充实现完成。
- 机械核验 `387,074` 参数、目标年前置封印、BF16/FP32 敏感区、无缩放器、原子恢复和禁止逐样本制品。
- 共享暂存区非空时停止协调；只提交目标文件。
