# R1 双层尾部风险重构快速实验记录

## 已确认输入

- 历史 R1 v1 已完成：B0 AP `0.008107276872491208`，GroupDRO AP `0.007641231311145235`，稳定－私有 M1 AP `0.013709929550763025`。
- 历史 M1 环境探针 AUC 未下降；纯阈值 M2 未产生恶意召回，R1 v1 裁决为 Q0 不晋级。
- 同 Q0 数据和合法字段合同下最强共享表格基线为 XGBoost，目标开发 AP `0.038902589823969644`；HGB 为 `0.037147851122101574`，随机森林为 `0.015572256018043917`。
- 神经诊断参照 K-DELTA 为 `0.01697727694684599`。Dijk 2026 XGBoost `0.03469597079642003`、Leoste RF 无 IAT `0.015073121655584231`、含 IAT `0.011423360215491575` 均为论文约束独立复现，只作诊断，不冒充原论文严格基线。
- Dijk 2024 作者 8 万行示例使用身份哈希、同年随机切分和不同成员，禁止进入直接比较。

## 实现事实

- 现有 `r1_source_environment.py` 已具备环境侧车、环境×标签均衡采样、ERM、GroupDRO、概率封存和延后标签连接的基础接口。
- 现有 R1 v1 不编码目标前缀；需复用 `c12_crossyear_train.py` 的 `encode-target-prefix` 入口。
- 新实验不得覆盖历史 R1 v1；必须使用独立模块、配置、启动器和运行根。

## 错误与阻塞

- 本地实现、集中入口检查和三个生产文件的 C56 精确同步均已完成，不再存在“等待同步授权”阻塞。
- 目标前缀表示已生成：`262144` 行、`3820` 个序列、隐藏维 `192`；环境侧车与共同训练顺序收据已发布。
- 持久会话 `r1-tail-risk-q0-s42-v1` 已完成并正常退出；启动器为 `finished/complete/evaluation_finished`，退出码 `0`。
- 五变体原始 AP：ERM `0.0122180649394674`、GroupDRO `0.012877901366876719`、V-REx `0.012054515654151374`、CVaR `0.011156523682837203`、双层尾部风险 `0.012246728127212125`。
- GroupDRO 未达到共享 XGBoost `0.038902589823969644`，环境探针 AUC 未下降；完整组合未超过最佳单机制。
- 最终裁决为 `R1 Q0不晋级并转向候选B`；仍是单种子快速筛选，不是正式证伪或论文证据。

- 暂无。
