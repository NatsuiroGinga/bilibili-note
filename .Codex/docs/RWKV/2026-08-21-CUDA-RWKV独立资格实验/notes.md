# CUDA RWKV 独立资格实验记录

## 当前事实

- RWKV 唯一活动训练路径为独立 CUDA 融合实现。
- 固定官方来源为 `BlinkDL/RWKV-LM@952102498e9ed367ea0a59ee64106916d474d30f`，许可证为 `Apache-2.0`。
- CUDA 生产入口提交 `f207f6e` 仅静态就绪，尚未同步、编译或真实运行。
- 当前证据状态为“设计可行、实验待证”。

## 独立身份边界

- 合法 Raw83 数据门通过后，从随机初始化开始训练。
- 不读取任何历史训练实现的权重、优化器、断点、代码锚、运行根或对照门。
- 检查点只允许在同一 CUDA 实现、配置摘要和数据语义哈希下恢复。
- 入口不可用或实验失败时保留失败证据并停止，不切换其他训练实现。

## 待补证据

- 合法 Raw83 数据清单、模式、行数、切分、字段、标签连接和语义哈希收据。
- 目标环境 CUDA 编译、前向、反向、显存和恢复收据。
- 源年最小四格训练、共同预算基线、指标与机制消融制品。
- 封印后一次目标年评价和严格结果分析。
