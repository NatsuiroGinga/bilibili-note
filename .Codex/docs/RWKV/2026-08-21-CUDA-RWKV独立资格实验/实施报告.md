# CUDA RWKV 独立资格实验实施报告

## 结论

RWKV 唯一活动训练路径已收敛为独立 CUDA 融合实现。现有生产入口仅完成本地静态检查；在合法 Raw83 数据门通过并从头完成真实实验前，状态保持“设计可行、实验待证”。

## 当前实现边界

- 官方来源固定为 `BlinkDL/RWKV-LM@952102498e9ed367ea0a59ee64106916d474d30f`，许可证为 `Apache-2.0`。
- 本项目实现是面向当前任务的 CUDA 派生实现，不宣称未经修改的官方完整 RWKV。
- 运行必须使用独立身份、独立运行根和独立检查点，从随机初始化开始。
- 不复用任何历史训练实现的权重、优化器、断点、代码锚或对照门。
- 恢复只接受同一 CUDA 实现、同一配置摘要和同一数据语义哈希产生的本次独立断点。

## 数据与实验门

1. 先验收合法 Raw83 数据清单、模式版本、行数、字段、实体不相交切分、标签连接与语义哈希。
2. 源年训练前目标年数组读取计数必须为 `0`。
3. 在共同预算下完成最小四格因果消融、强基线比较、运行诊断和完整指标。
4. 源年选择封印后只执行一次目标年评价，再由严格结果分析裁定。

## 验证状态

- CUDA 生产入口提交 `f207f6e`：本地静态就绪。
- 服务器同步：未执行。
- 目标环境编译与真实训练：未执行。
- 合法 Raw83 数据实验：待执行。
- 科学结论：实验待证。

## 未变更范围

- 未修改代码、配置、启动器、运行制品、服务器或 Git 历史。
- 未改写官方来源、许可证、共享数据合同和评价指标。

## 下一门禁

合法 Raw83 数据收据齐全后，对独立 CUDA 融合实现执行从头训练。若编译、资源或科学门失败，记录失败并停止，不切换其他训练实现。
