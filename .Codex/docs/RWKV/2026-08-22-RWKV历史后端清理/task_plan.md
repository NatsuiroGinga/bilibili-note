# RWKV 历史后端文档清理计划

## 目标

从活动文档中移除已废弃的 RWKV 历史后端入口、恢复说明、速度对照和检查点复用叙述，只保留独立 CUDA 融合实现是唯一活动训练路径且须从头实验的事实。

## 边界

- 文件所有权：`.Codex/docs/`、`thesis/`、`output/`、`wiki/` 下命中指定模式的活动文档，以及本任务三份过程文档。
- 排除：`archive/`、`vendor/`、`runs/`、代码、配置、运行制品、服务器与 Git 历史。
- 阻塞条件：共享暂存区非空，或任一目标文档已有未提交改动。
- 编辑方式：仅使用 `apply_patch`；不运行 `sed`、`sd`、Python 写文件或自动格式化器。

## 阶段

- [x] 阶段一：读取根规则、文档规则、RWKV 路线规则与恢复资料。
- [x] 阶段二：机械枚举命中，逐文件检查 Git 状态并分类。
- [x] 阶段三：删除专用历史文档，修订混合文档和核心恢复入口。
- [x] 阶段四：验证指定模式零命中、删除链接无活动引用、差异仅含任务文件。
- [x] 阶段五：精确暂存并提交，记录提交标识。

## 验收命令

```bash
rg -n "<指定的历史后端模式>" .Codex/docs thesis output wiki --glob '*.md' --glob '!**/archive/**' --glob '!**/vendor/**' --glob '!**/runs/**'
git diff --check
git diff --name-status HEAD
git diff --cached --name-only
git log -1 --oneline
```

## 决策

- CUDA 融合实现使用独立身份从头训练，不复用任何历史训练实现的权重、优化器、断点、代码锚或对照门。
- 仅清理 RWKV 历史训练实现；其他论文和通用 PyTorch 内容不在本任务范围。

## 错误与阻塞

- 一次引用修订因连续状态候选目录日期写错而整体未应用；确认正确路径后重试，无部分修改。
- 一次只读检索中的反引号被 shell 解释为命令；该命令未写文件，随后改用无反引号模式完成核验。
- 看板批量删除的首次安全计数预期为 10、实际识别为 13，保护门阻止执行；回读精确行后按 13 行重试成功。
- 最终暂存前共享暂存区被 Raw83 预处理任务占用 10 个文件；本任务未暂存、未提交、未清空他人暂存，等待其所有者释放。
- Raw83 所有者提交 `c581a12` 后释放暂存区；本任务重新核验为空，并精确暂存 41 个文档清理路径。
- 首次暂存因工作树索引锁超出沙箱写权限失败，未产生暂存；受控重试前发现两个并发 CUDA Raw83 文档出现新改动，保护门阻止扩大范围，排除后精确暂存成功。

## 当前状态

清理、验证与精确暂存完成；提交标识由当前文档提交产生并在最终汇报中记录。
