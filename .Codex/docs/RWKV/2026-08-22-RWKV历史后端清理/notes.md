# RWKV 历史后端文档清理笔记

## 已确认边界

- 共享暂存区在任务开始时为空。
- 工作树存在大量无关改动，不得暂存、覆盖或回滚。
- 清理只覆盖活动文档，不修改代码、配置、运行制品、服务器或 Git 历史。

## 待登记

- 指定模式在任务开始时命中 22 份既有活动文档。
- 初始唯一文件所有权疑点经主代理确认解除：遗留审计目录无活动所有者，且 `HEAD` 中不存在；已按用户明确清理授权删除，不另建含旧内容的快照提交。
- 代码、配置和启动器另有同名实现标识，但不属于文档清理范围，不修改。
- 删除 21 份专用旧文档：16 份已跟踪文件与 5 份未跟踪遗留。
- 修改 22 份活动文档：核心恢复链 4 份、独立 CUDA 文档 3 份、混合合同与登记文档 8 份、连续状态相关文档 3 份、派生 Markdown 总表 4 份。
- 新增本任务 `task_plan.md`、`notes.md` 与 `实施报告.md`。

## 删除范围

- 训练差异审计主文档、笔记和计划。
- 字段感知四格计划与报告。
- 首轮资源门目录三份文档。
- 三折资格计划与报告。
- 旧融合核比较目录三份文档。
- 统一 BF16 旧入口目录三份文档。
- 无活动所有者的旧接入审计目录两份文档。
- 无活动所有者的二次恢复目录三份文档。

## 保留边界

- 不修改代码、配置、启动器、运行制品、服务器或 Git 历史。
- DeltaNet 与门控 DeltaNet 的通用框架原生参考说明保留，只移除会触发指定模式的措辞。
- 全容量 MLP 候选文档在任务开始前已有未提交改动，本任务未编辑、未暂存。
- `output/`、`wiki/` 与论文正文没有需要修改的 RWKV 历史训练入口；非 RWKV 的历史时长说明和通用 PyTorch 内容保持原样。

## 验证记录

- 指定模式在活动 Markdown 文档中零命中。
- 21 份删除文档的文件名和目录名在活动 Markdown 文档中零引用。
- 旧 RWKV 工具路径、运行身份、速度比和兼容替换表述在活动 Markdown 文档中零命中。
- `git diff --check -- .Codex/docs/RWKV` 通过。
- 四张派生表结构核验：源年选择 12 行、源年性能 12 行、目标年评价 11 行、资源 12 行，均不计表头。
- 最终暂存前发现共享暂存区已有 10 个 Raw83 预处理文件，本任务未触碰；其所有者提交 `c581a12` 后释放。
- 精确暂存 41 个本任务路径，排除全容量 MLP 既有改动和两个并发 CUDA Raw83 文档；`git diff --cached --check` 通过。
- 提交标识由当前提交产生，在最终汇报中记录。
