# 方案 A/B 数学阻断项修订记录

- **修订日期**：2026-08-06
- **任务性质**：数学定义、实现断言语义与实验公平控制修订
- **结论**：A-S1、A-I1 至 A-I3、B-S1、B-I1 至 B-I4 已完成文档级关闭，仍须由新的独立复审确认；本记录不表示代码已经实现或实验已经验证
- **范围边界**：本轮不迁移 LSPR24 数据章节，不改动数据、标签、窗口、拆分或 G0 合同

## 1. 输入与输出

输入依据：

- [方案 A/B 第三章修订版独立复审](2026-08-05-方案AB第三章修订版独立复审.md)
- [RWKV 第三章工作量与实验证据矩阵](2026-08-05-RWKV第三章工作量与实验证据矩阵.md)
- [RWKV 恶意流量替代路线研究合同](2026-08-05-RWKV恶意流量替代路线研究合同.md)
- [RWKV 第一创新点理论与算法设计](2026-08-05-RWKV第一创新点理论与算法设计.md)

修改文件：

- [方案 A 第三章](2026-08-05-方案A第三章-受序动态记忆与正常状态生成.md)
- [方案 B 第三章](2026-08-05-方案B第三章-字段交互与动态状态演化.md)
- 本修订记录

明确未修改：

- LSPR24 数据接入、标签、窗口与分区合同
- UGR'16 G0 审计及其裁决
- RWKV-7 官方仓库提交、参考核或实现代码
- 两份恢复文档、工作量矩阵和独立复审原文

## 2. 方案 A 阻断项关闭情况

| 编号 | 文档级状态 | 修订内容 | 后续核验 |
| --- | --- | --- | --- |
| `A-S1` | 已关闭，待复审 | 安全上限定义为每头的 `u=min(rho_max, bar_gamma/(chi+epsilon))`，可行下界定义为 `ell=eta*u`，其中 `0<eta<1`；因此所有有限头都满足 `0<ell<u`。快慢门只在该区间内受序生成，固定最小尺度分离降为相对可行宽度上的软约束。 | 核对 `||G||<=chi` 的秩一项推导；实现后故障注入极大 `chi`、非有限量和窄区间头。 |
| `A-I1` | 已关闭，待复审 | 普通 RWKV-7 拆成参数匹配 `C0-P` 与计算／状态预算匹配 `C0-C`。`C0-P` 只控制可训练容量；`C0-C` 同时控制每批计算量和递归状态字节，允许参数不同。无合格 `C0-C` 时必须报告不可匹配，不能让 `C0-P` 代替三类公平结论。 | 资源预跑确认 ±5% 双预算是否存在可行配置，并独立核验测量口径。 |
| `A-I2` | 已关闭，待复审 | 冻结同一告警时间轴：读完窗口 `t` 后，`z_t` 产生 `p_hat_t` 与下一窗口分布；当期生成惊讶度由 `z_(t-1)` 对 `x_t` 的预测得到；后置校准器只组合同一时刻可用的两类分数。连续段首窗口仅预热。 | 公式—代码逐步序列测试需拒绝 `z_t` 重构 `x_t` 和未来窗口提前可见。 |
| `A-I3` | 已关闭，待复审 | 双状态固定在每个 RWKV-7 时间混合层；同层快慢状态共享一次 `G/B` 计算，层内融合后只向下一层传递单一隐藏流。历史矩阵积固定为 `G_(j+1) G_(j+2) ... G_t` 的从早到晚右乘顺序，禁止逆序。 | 核对官方张量方向；实现后断言共享对象、状态形状、层间单一路径，并以非交换矩阵做展开测试。 |

方案 A 的代码断言语义已经冻结为：所有门相关量有限；逐层逐头满足 `chi>=0`、`0<ell<u`、`ell<=rho_f<=rho_s<=u`、`rho_s*chi<=bar_gamma`；同层两路共享转移与写入；任一断言失败即使运行无效，不允许事后裁剪掩盖。

## 3. 方案 B 阻断项关闭情况

| 编号 | 文档级状态 | 修订内容 | 后续核验 |
| --- | --- | --- | --- |
| `B-S1` | 已关闭，待复审 | 状态差分完整加入 `Delta S_(t-1) G_t`、`S'_(t-1) Delta G_t` 与 `Delta B_t`。先由统一的 `rho` 和 `B_max` 得到有限域状态界，再引入编码器关于输入、状态的常数和 `G/B` 关于编码的常数，形成闭环递推。主结论收窄为有限历史 `H` 的递推上界。 | 只有全部常数可由冻结谱范数或逐步雅可比上界监控，且所有预注册连续段满足 `L_cl<=1-delta_cl`，才能追加几何衰减推论；否则删除闭环收缩措辞。 |
| `B-I1` | 已关闭，待复审 | 主消融扩为无交互、当前窗口交互、上一状态条件交互三档，并分别覆盖无生成头和有生成头：`B00/B01/B10-current/B10-state/B11-current/B11-state`。 | “状态条件”保留门槛是两个生成设置下的状态版本都稳定优于当前交互版本。 |
| `B-I2` | 已关闭，待复审 | 上一状态摘要固定为最高层全部头矩阵状态的归一化查询投影，与闭环递推中的状态变量一致；层归一化使用 `epsilon_LN>=epsilon_0>0`；注意力温度满足 `tau>=tau_min>0`；全缺失窗口在注意力前显式分流；非全缺失池化打分受界且分母满足 `D_t>=exp(-A_pi)>0`，权重和严格等于 1。 | 核验全掩码 softmax 不会执行；故障注入全缺失、单字段可见、极小方差和温度下界。 |
| `B-I3` | 已关闭当前措辞问题，文献核验仍未闭合 | 明确状态条件注意力、字段标记、缺失池化、门控残差前端和 RWKV 动态状态均非单独创新；当前只登记面向任务的候选组合，不使用“首次提出”“首创”或广义新颖性措辞。 | 仍须定向一级文献核验状态条件特征交互、时间序列表格 Transformer、门控残差网络、循环状态调制注意力及其网络安全组合。 |
| `B-I4` | 已关闭，待复审 | 新增门控残差前端＋同一 RWKV-7 的 `B-GR0/B-GR1` 强基线，分别对应无生成头和有生成头；共享数据、历史、状态核、训练步数与调参次数，并进入参数 ±3%、计算 ±5% 容差。 | 若 `B10-state` 不胜 `B-GR0` 或 `B11-state` 不胜 `B-GR1`，不得把注意力形式写成章级核心创新。 |

方案 B 的理论主张不再由 `||G_t||<1` 单独推出闭环稳定。若 `L_cl` 不可监控或不小于 1，只允许报告有限时域条件界、状态范数曲线与故障注入，不允许写成扰动几何衰减。

## 4. 仍未解决的事项

以下事项不属于本轮数学与公平控制修订，或必须依赖后续制品：

1. LSPR24 数据合同尚由独立任务修订，本轮保留两份方案现有数据章节，不能据此启动实验。
2. RWKV-7 官方提交、参考核、矩阵张量方向、初始化和参数分组仍缺实现收据。
3. 方案 A 的 `C0-C` 是否存在满足双 ±5% 容差的配置仍须资源预跑；受序门是否出现大量窄区间头仍须实测。
4. 方案 B 的定向先验文献核验仍未完成；`L_cl` 各常数是否可可靠监控也未由代码证明。
5. 两套方案均没有训练、消融、三种子、效率或检测效果结果；所有数学性质仍是带前提的待证责任。

## 5. 建议独立复审范围

新的独立复审应只检查本轮新增内容，不重复裁决正在并行处理的数据合同：

1. 方案 A：`||G||<=chi` 推导、全头非空区间、浮点断言、逐层双状态计算图、有序右乘展开、`C0-P/C0-C` 责任分离和告警索引。
2. 方案 B：状态界前提、闭环差分符号、有限时域展开指数、全缺失前置分流、层归一化与温度下界、池化分母下界、六组主消融和 `B-GR0/B-GR1` 公平性。
3. 共同边界：确认文档没有把设计、待证命题、资源目标或候选新颖性写成已实验验证结论。

## 6. 验证记录

- 纯 Markdown 文档修订，不适用测试驱动开发，不运行 Prettier。
- 已用 `rg` 核对全部九个审查编号均在本记录中逐项登记；方案 A 的非空区间、逐层双状态、有序矩阵积、双预算对照与因果分数索引均可定位；方案 B 的闭环递推、六组主消融、全缺失分支、稳定项、池化下界、门控残差基线与定向文献核验均可定位。
- 数学显示块配对：方案 A 为 29 组开始／29 组结束，方案 B 为 33 组开始／33 组结束，本记录无显示数学块。
- 控制字符检查与尾随空白检查均无命中；三份相对链接依据文件均存在。
- 已回读所有新增数学与实验控制段落；没有写入训练结果、效果数值或“已经优于”类结论。
- `.Codex/docs/RWKV/` 受 Git 忽略规则管理，`git diff --check` 不返回这些文件的差异，因此使用逐文件 `rg` 检查替代；本轮仅通过补丁修改两份指定方案并新建本记录。
