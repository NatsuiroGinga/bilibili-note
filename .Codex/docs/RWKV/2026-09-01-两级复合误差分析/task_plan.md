# 两级复合误差分析任务计划

- 日期：2026-09-01
- 代理：two_level_error_analysis_fable（模型 fable，effort 继承会话）
- 目标：裁决第三章恢复卡登记的理论债务「两级复合（实体内 top-α 尾均值 → 实体间 CVaR-pAUC）的误差控制未论证」能否偿还，三选一（甲：补出误差分析；乙：证明当前设定下不可还；丙：绕开表述），产出 `分析报告.md` 并提交。

## 步骤

1. [x] 建检查点目录与本计划、notes.md
2. [x] 读第三章恢复卡（worktree）确认债务原文与目标函数冻结状态（第四章卡与本债务无涉，未读）
3. [x] 读 FCCO 笔记、ATk 全文笔记、SoRR 全文笔记（主仓 wiki 只读）＋ MIDAM、pAUC-DRO、NSWC-FCCO 笔记
4. [x] 定位实现事实：truncate_length=8192（损失侧确定性截断）、n_neg=64、effective_budget()、六档 K、α=0.5、**内层穷举无抽流**（采样器＋OOM 实测佐证）、评价侧完整袋
5. [x] 读 `2026-08-31-CVaR预算失配数学分析/分析报告.md`（复用其源码对照与 ξ 动力学结论，不重复推导）
6. [x] 本地文献索引：status 三哈希齐全；hybrid 查询无新增相关命中（向量通道已运行）
7. [x] 解析分析：引理 1（无偏）、命题 2（非 FCCO 定位）、B1a（非弱凸）、B1b（算法偏离）、B2（泛化不可证）、截断偏差改登记口径
8. [x] 最小数值实验 E1/E2/E3（合成数据，CPU，1.3 秒；脚本副本已入本目录）
9. [x] 写 `分析报告.md`：推荐甲（有界偿还），附丙的正文范例与乙的断裂点
10. [x] git add 精确路径并提交

## 阻塞条件

- 不连服务器、不占 GPU；数值实验只用本机 miniconda rwkv 环境小规模运行。
- PDF 只在全文笔记不足时经 mineru 提取（主仓已有 mineru 全文提取笔记，优先读之）。
