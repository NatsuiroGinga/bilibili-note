# 任务计划：机制二风险余量与漂移评价文献审计

## 目标

以已核全文和最新源年修正 Q0 制品为依据，裁决风险余量候选、源域资格门与独立漂移开发协议，不改代码、恢复卡、正文或实验。

## 阶段

- [x] 建立三文件检查点并读取最新 Q0 原始结果。
- [x] 核验 Tong、COVCAL、SAFEVPR、C3R、Gurram、Deng、Choi 的既有原件与笔记。
- [x] 复用既有精确混合检索与直接近邻矩阵；本地证据已覆盖四问，不新增宽泛联网候选。
- [x] 回答四个冻结问题并完成证据分级与自检；待提交。

## 关键问题

1. Tong 或风险上界能否在开发段产生带安全余量的唯一候选，再由独立校准段确认？
2. 无漂移源域资格是否应改为假阳率受控且检出率非劣，把增益要求留给漂移域？
3. 如何建立不污染最终评价的独立漂移开发域？
4. 是否存在不引入魔法数字的下一候选；若无，应否决到什么范围？

## 决策记录

- 只把全文页码和原始实验制品作为承重证据；摘要、题录与概念类比只作候选。
- 最新 Q0 的源阈值不是独立统计证书，不能把其历史工作点冒充已获有限样本保证。
- 唯一下一候选为 Tong 风险余量阈值；开发段仅生成候选，最终证书由独立校准段签发。
- 源域门改为 FPR 受控与 DR 经验非下降；严格增益留给独立漂移开发域。

## 错误记录

- 本地混合检索首次因受限全局缓存失败；改用 `/tmp/uv-cache` 后需补锁定依赖。
- 依赖补齐后状态命令未返回可解析 JSON；本轮改为读取既有精确检索检查点、原件和结构化笔记，没有把索引状态冒充检索结果。
- 直接导入现有 Clopper—Pearson 计算函数时，本地 `uv --no-sync` 环境缺少 NumPy；未绕过环境安装，`614` 使用任务给定的已核计算，并要求实现前由门禁脚本复算。

## 状态

全文核验与四问裁决完成；本目录三个文件构成唯一提交范围。
