# CEM-BER 四格实验进度

<!-- RESEARCH_ROUTE=RWKV -->

- 更新时间：2026-08-28 17:40 CST
- 方法：基于因果实体记忆（CEM）与预算感知实体排序（BER）的跨年低误报检测方法（CEM-BER）
- 主基座：FT-Transformer，裸主干参数 `924,283`
- 服务器：RTX 5090，`32,607 MiB`；PyTorch `2.13.0+cu130`，算力 `(12, 0)`

## 一、四格状态

| 格 | 展示名 | `z1` | `z2` | 状态 | 源年实体 AP |
| --- | --- | ---: | ---: | --- | ---: |
| C00 | 裸 FT | 0 | 0 | **完成** `exit_code=0` | **`0.7380`**（第 17 轮） |
| C10 | ＋因果实体记忆 | 1 | 0 | **运行中** 17:36 起 | 待出 |
| C01 | ＋预算感知实体排序 | 0 | 1 | 配置已生成，等集成验收 | — |
| C11 | 因果状态共享 | 1 | 1 | 配置已生成，等集成验收 | — |

**判据**：`C10 > C00` 且 `C01 > C00` 且 `C11 ≥ max`，交互 `I = C11 − C10 − C01 + C00 > 0`。
C00 基线 `0.7380` 即三个机制臂必须超过的门槛。

## 二、C00 已确立的结果

运行身份 `ch3-ft-c00-dual-selection-cuda-formal-v1`，16:04–17:25，`target_reads = 0`，
两份检查点均落盘（`selected-by-entity.pt`、`selected-by-flow.pt`）。

**选轮规则冻结（已提交 `cf2cf65`，四格统一）**：用源年实体 AP 选轮。

| 指标 | 第 10–20 轮动态范围 | 选中轮 |
| --- | ---: | ---: |
| 逐流 AP | `0.99859 – 0.99983`（幅度 `1.2e-3`） | 第 19 轮 |
| 实体 AP | `0.3243 – 0.7380`（幅度 `0.4137`） | **第 17 轮** |

逐流 AP 自第 3 轮起饱和于 `0.9985` 以上；实体 AP 的动态范围是其 **345 倍**。两条规则
选中的检查点在实体 AP 上相差 **16.4 个百分点**。这是本章「双选轮」协议实验的直接产出，
也是逐流指标不适合作选轮依据的实测证据。

逐轮实体 AP（第 10 轮起）：`0.5745 / — / — / 0.5069 / 0.6573 / 0.5352 / 0.3243 /
0.7380 / 0.6100 / — / 0.5285`。

## 三、C10 运行实测

| 项 | C00 | C10 | 说明 |
| --- | ---: | ---: | --- |
| 步速 | `4.757` 步/秒 | **`4.807` 步/秒** | CEM 未带来速度损失 |
| 峰值显存 | `30,280 MiB` | `24,116 MiB` | 微批同为 64、累积 1 |
| GPU 利用率 | 约 `66%` | **`95%`** | CEM 的额外计算填补了原有空档 |
| 预计完成 | — | **18:59 前后** | 20 轮 × 244 秒 |

CEM 新增参数 `150,913`（占裸 FT `16.33%`），交叉注意力时间开销约为自注意力的 `0.1%`——
实测步速印证了该估算。

## 四、时间账

| 项 | 实测／预计 |
| --- | --- |
| 单臂时长 | **`81` 分钟**（训练 `208` 秒/轮 ＋ 验证 `36` 秒/轮 × 20 轮） |
| 四格串行总计 | 约 **`5.4` 小时** |
| 已完成 | C00（`81` 分钟） |
| 进行中 | C10（预计 `83` 分钟） |
| 剩余 | C01 ＋ C11，约 `2.7` 小时 |

早期给出的「3–4 小时」是**单臂保守估算**，实测后修正为 81 分钟/臂；四格串行 5.4 小时
的估算与当前进度吻合。额外损耗来自等待实现代码、三次 C10 启动失败（文件未推送、
预算未冻结、步数配置手误）与 `torch.compile` 探针绕路。

## 五、已作废的优化路线

| 优化 | 裁决 | 依据 |
| --- | --- | --- |
| X23 显存常驻 | **实测否决** | 服务器数据路径仅占单步 `1.3%`，收益上限 `1.2%` < `5%` 门槛 |
| `torch.compile` | **实测否决** | 微批 4 实测：逐位等价 `False`、最大绝对差 `8.94e-07`、训练步提速 `1.316×`。按预注册二值门，非逐位等价即不可用于四格（C00 已在无 compile 下完成且不重跑）。TF32 同理不启用 |
| 双臂并行 | 暂缓 | 用户裁决优先让 C10 满配跑最快；C01 可在 C10 完成后接续 |

## 六、当前阻塞与下一动作

1. **等 BER 宿主集成代理交付**——须先通过 `z2=0` 对 C00 路径的**逐位**零门核验
   （`torch.equal`），未通过不得启动 C01/C11，否则 81 分钟白费。
2. C10 完成后按顺序启动 C01，再 C11。
3. 四格齐备后：`results-analysis` → 统一总表 → LSPR24 封印后描述性评价。

## 七、边界

- 上述实体 AP 均为**源年 LSPR23 验证集**读数，`target_reads = 0`；不得与目标年数字混比。
- 历史 FT 的 `C00 = 0.7508` 是**目标年**描述性评价，与本表源年 `0.7380` 口径不同，
  仅可用于判断重训未退化，不得并列比较。
- 方法有效性尚未验证：C10 未超过 `0.7380` 即 CEM 无效，相关缺陷分析与第四章候选一并作废。
