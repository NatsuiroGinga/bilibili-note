# 任务计划：第四章外部加速基线清单

- 日期：2026-09-04
- 路线：RESEARCH_ROUTE=RWKV，第四章（训练效率）
- 交付：`thesis/methods/第四章外部加速基线清单.md`

## 环境约束（本次代理实测，影响交付形式）

- **本代理无 Bash 工具**。可用工具仅：Read / Write / Glob / Grep（ripgrep 内核）/
  WebSearch / WebFetch / Zotero MCP。
- 由此产生四项无法执行的既定流程，已在交付文件与最终报告中如实标注：
  1. 无法运行 `uv run --project scripts/literature_search ... status/query`
     （本地混合索引的向量通道）。**代偿**：用 Grep（rg 内核）对
     `wiki/papers/**`、`thesis/methods/**` 做词法检索，等价于 `--mode lexical`，
     向量通道**未运行**。
  2. 无法 `git commit`。**代偿**：分阶段 Write 落盘，提交交由主代理执行。
  3. 无法下载 PDF 到 `raw/papers/`，无法调用 `pdf-converter`(MinerU)。
     故本次新增来源**一律不升级为「在线全文已下载」**，最高只标「在线摘要」，
     并逐条登记待入库清单。
  4. 无 Skill / ToolSearch 工具；Zotero 走 MCP 直连。

## 已确认的前置事实（读文档所得，非本次推演）

- `thesis/methods/第四章外部基线清单.md` **已存在**（2026-09-03，10 条基线，
  判定 2 可直接复现 / 3 需改造 / 5 不可比）。本次交付**不重复**其 10 条，
  只做增量并显式交叉引用。
- 参照论文第四章 6 个基线已从本地全文提取（`朱焱雷-全文.md` 行 2152–2208）。

## 分阶段落盘计划（每阶段完成即写文件）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 建 task_plan / notes；提取参照论文 6 基线 | 已完成 |
| P1 | 交付文件骨架 ＋ 第二条不可省判断（参照论文对位） | 已完成 |
| P2 | 线索一：梯度稀疏化与安全筛除（精确等价家族） | 已完成 |
| P3 | 线索二：样本／实例选择加速 | 已完成 |
| P4 | 线索三：排序损失／pAUC／CVaR 高效实现 | 已完成 |
| P5 | 线索四：通用训练加速与正交性判断 | 已完成 |
| P6 | 三条不可省判断 ＋ 缺口清单 ＋ 待入库登记 | 已完成 |
