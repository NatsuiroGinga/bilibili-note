# GeNIS 支持量收缩跨场景极端尾部排序 Q0 实验结果报告

## 结论

完整候选 `support-shrunk-scene-tail` 在单种子 `seed=42` Q0 中不晋级，`candidate_advanced=false`。本结果仅为筛选证据：`screening_only=true`、`formal_paper_evidence=false`、`final_accessed=false`、`official_test_accessed=false`，不能写成论文结果、方法有效或正式证伪。

本 GeNIS 路线为对齐朱焱雷论文的单次运行口径，种子范围冻结为 `seed=42`；不追加 `seed=43/44`，不对完整候选追调。该分支唯一下一步是依据当前结果选择新候选。

## 运行事实

- 运行根：`/root/autodl-tmp/thesis/experiments/genis/runs/q0/genis-60s-support-shrunk-extreme-tail-ranking-q0-seed42-v1`。
- 完成状态：`112/112` 单元完成，失败 `0`，`FINISHED`，`exit=0`。
- 资源：耗时 `1283.6032036095858` 秒，峰值 RSS `1.9923820495605469 GiB`。
- 隔离与证据边界：`final_accessed=false`、`official_test_accessed=false`、`screening_only=true`、`formal_paper_evidence=false`。

## 裁决与诊断边界

- 完整候选 `support-shrunk-scene-tail` 未达到预注册 Q0 晋级门槛，停止该候选，不追加种子，也不依据开发结果改变阈值、指标、基线或参数。
- 随机森林 `sample-cvar-tail` 是已发表机制的强基线诊断，不是本课题创新：其最坏场景 `recall@budget` 从 `uniform-balanced` 的 `0.029527617` 到 `0.408825707`，最大每万良性误报为 `9.250694`。
- 上述随机森林增益主要由场景 4 驱动，且 XGBoost 未复现；因此不能将其解释为跨模型机制信号、晋级候选或论文贡献。

## 下一步

GeNIS 分支仅根据本次 Q0 的完整结果选择新候选。不得追加 `seed=43/44`，不得追调 `support-shrunk-scene-tail`，不得把任何 Q0 指标用于正式论文结论。
