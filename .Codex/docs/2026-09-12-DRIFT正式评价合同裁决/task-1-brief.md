# 任务1：正式配置与共享合同

## 独占文件

只新建以下四个生产文件（以下路径相对 `thesis/experiments/llm_probe/`）：

- `configs/ch3-drift-formal-evaluation-v1.json`
- `configs/ch3-drift-official-p2p3-formal-v3.json`
- `configs/ch3-drift-bresnet-p2p3-formal-v1.json`
- `tools/ch3_drift_formal_contract.py`

另保存本目录 `task-1-report.md` 与 `task-1-skill-receipt.json`。不改其他文件、通用启动器、旧脚本、依赖锁或恢复卡；本任务尚不需要SciPy，不为未来可能使用的依赖改环境。不得创建子代理或独立审查代理。

## 约束与输入

- 用户已批准正式评价，主代理已验收可先实施的CPU合同范围；本文是该范围的任务简报。
- 读取同目录 `implementation_plan.md` §一、§三及任务1的必要约束，不加载其他实施任务的全文。
- 规范依据是 `thesis/methods/第三章-正式评价与F指标空间科研裁决.md` 与 `第三章-DRIFT数据角色与评价协议.md`；指标呈现合同现已主代理核验。
- 训练参数从既有 `official_p2p3_full.py`、`bresnet_p2p3.py` 及主方案 §5.7/§5.11 核对，不能凭记忆重填。
- 共同模型接口后续需要消费配置哈希、骨干、单臂、完整输入角色、成员与检查点身份；本任务定义并在报告列出准确函数签名，后续只复用该接口。

## 必须实现

1. 严格JSON模式：拒绝未知键、重复角色、越界路径、未批准面板、`member_seed`、额外攻击或阈值覆盖。评价配置枚举T17–T19共18个源角色及T20–T25共12个无后缀目标角色。
2. 固定 `k=1/k=2/random_half`、每年每类至多 `200000` 唯一实体、逐臂共同源良性成员的 `0.001/0.01` 工作点。保留原文总体指标和本课题追加指标的合法群体语义，不生成纯DGA混合指标。
3. 源数据角色合并后 `unique()`；目标固定哈希抽样。成员身份采用版本命名空间和UTF-8长度前缀的SHA-256；命名空间是版本标识而非自由随机种子。未冻结资格与配对重采样参数不能填入猜测默认值。
4. 训练配置只允许入口选一个A/B/D/F/G臂；不允许运行时随意覆盖科学参数。BResNet B是后续拟实现，不把历史B读数标为合法强基线。
5. 标准库实现规范JSON、流式文件SHA-256、长度前缀编码、路径界限、原子写入和状态迁移；状态只含created/running/complete/failed，错误说明中文，不记录凭据。
6. `--help` 与本次可执行的合同审计入口；不要为尚未实现的下游门禁返回虚假通过。明确尚缺生产模块/制品时失败关闭或标未实现，不创建空收据充当完成。
7. 模块导入不加载PyTorch、不分配MPS或CUDA。MP正在本机训练，本任务只做轻量CPU检查。

## 验收与报告

- 修改前读取并核对近层AGENTS和必要输入。
- required_skills：`/Users/bilibili/.codex/skills/backup/daily-coding-20260811-112930/SKILL.md`；改码前把技能路径、SHA-256及读取时刻登记到 `task-1-skill-receipt.json`。
- Python仅做语法、导入与入口检查；JSON解析与三份真实配置的严格加载检查；对30个实际Parquet只读元数据角色核验，不扫描大规模数据，不新增人工夹具或单元/集成测试。
- 无自动格式化器，无Git写操作。通过检查后立刻写报告，列四个文件、真实命令/返回值、准确公共接口、当前可用功能及后续未实现边界。
- 若发现会改变科学合同的歧义，精确报告并继续不依赖该歧义的部分；不得自行新增门槛或缩窄任务。
