# 实施台账：implementation_plan.md

- 计划：本目录 `implementation_plan.md`。
- 唯一工作树：`.worktrees/ch3-drift-20260908`；唯一分支：`exp/ch3-drift-20260908`。
- 派发基点：`1e2201f5576f9d78feabc3e042e9c65423b4ef4d`。工作树有大量既有修改，不暂存其他改动。
- 流程：按独立实现、主代理最小验收推进；过程制品按用户要求放本目录，不创建平行活动分支。实验仓库不运行人工测试与自动格式化器，真实数据及入口检查代替夹具。

## 预检与裁决

| 任务／共享接口 | 核验结果与裁决 |
| --- | --- |
| 任务 1 配置与共享合同 → 任务 2–7 | 数值沿已批准三档、200000/类/年及0.1%/1%工作点；任务1先落实严格模式、身份、状态和哈希接口，后续消费同一模块。 |
| 任务 2 成员 → 任务 3 编辑 → 任务 4/5 训练与任务 6 评价 | 已纠正旧正式脚本保留重复曝光的偏离；规范源合并后unique。源训练/验证交集先审计，未决处置不自动过滤；新实体面板资格另行冻结。 |
| 任务 3 → 任务 6 | 成员、编辑流及预算严格确定性；短域预算重合保留；三档不冒充三个独立攻击家族。 |
| 任务 4/5 → 任务 6 | 共同数据接口、各自合法检查点；旧BResNet B未实现增广的身份疑点已隔离，后续正确实现。 |
| 任务 6 → 任务 7 | 固定评分身份，分数文件哈希验证已保存制品；不要求跨硬件/批量浮点逐位一致。纯DGA面板无伪混合指标。 |
| 任务 7 → 任务 8 | 配对重采样合同尚未冻结时保留数据与点值，不伪造区间；指标呈现原则已经主代理核验，不自动追加全指标获胜门。 |
| 任务 1–8 自洽性 | 任务8为主代理状态写回，实施代理不改恢复卡。新的基线清单不自动扩展已批准五臂接口；可以后续追加合法臂而不更换指标。 |
| 计划格式化条款 | 与当轮用户要求及实验规则一致，本次不运行自动格式化器；JSON用语法和严格模式验证。不可因格式化工具缺失停止真实实验。 |
| 文献索引 | 新增文档使索引过期；当前实现不依赖检索重建，禁止借此变更研究问题或重复文献调研。 |

## 实施状态

- 任务1：两轮修订后主代理代码/规格核验通过，三配置加载、真实审计状态读回及角色绑定均已核。唯一一次技能收据验证器发现两处字段遗漏，原代理已补齐；不重复验证器，不阻断后续实现。检查与实际命令见task-1-report.md。
- 任务2：原物化派工已于2026-09-13 14:37按用户重申的禁止物化要求中断。只读检查未发现此次拟新增物化源文件、Rust目录、启动器或正式资产目录；后续改为现有Parquet直读接口，不执行旧brief。
- 任务2接续（新计划任务1）：已派`direct_data_luna_high`，模型`gpt-5.6-luna`、实际`high`；按direct-data-brief.md独占三配置、合同模块和新直读模块，不触碰编辑算子、MP或训练。原因是跨文件身份/角色/去重耦合；本轮只做完整T17两验证文件CPU检查，全源/目标/服务器容量后续核验。
- 任务3：按需共享编辑已通过主代理最终验收。修订代码SHA-256为`6a430727fa50d6b4b14e8ba4b83cbf828c6cadfb2b3c72f89226bbb5abdd00bd`；共同改动位置的替换字符一致与全文件行数守恒已补齐，两个独立进程以4096/16384批量各处理150000条，三档根摘要一致、失败计数为空，源文件哈希已独立复核。唯一一次技能收据验证返回valid=true。只证明新正式评价算子的工程性质，不追认旧V4或方法有效性；首次收据保留。详见direct-edit-report.md。
- 任务4：未开始，DRIFT单臂正式训练。
- 任务5：未开始，BResNet单臂正式训练。
- 任务6：未开始，统一阈值与逐实体评价。
- 任务7：未开始，配对比较和统计。
- 任务8：进行中，按已验证进度写回恢复卡。

## 独立进行的筛选与外部研究

MP 在持久screen会话中运行，不能被新实现覆盖。网页读取只影响外部研究，不阻断已批准评价接口的实现。

2026-09-13 15:04：已核MP进程77528存活，日志第3轮201/469；本轮为有效运行推进，不重启。原网页代理继续回收已发送的公式/来源格式补充，不发起新一轮宽泛调研。原Sol仅依据新增B实现与候选核事实修订计划，不承担源码实现。

## 任务2工程裁决

**以下原裁决已由2026-09-13用户禁止物化要求覆盖，仅保留历史；不得继续执行。**

- Python入口保留；扫描、哈希、去重和排序热路径遵守按需运行合同的Rust默认要求，新增文件见task-2-brief.md。旧crate只参考依赖版本，不导入旧研究算法。
- 大规模物化在服务器执行。本机仅做完整T17验证文件的有界工程核查并标engineering_only；元数据齐全不等于全量物化完成。
- 合同模块只允许增加成员阶段分派；保持同一时间仅一位实现代理修改生产代码。

## B-ResNet有效性修复

已核验 `bresnet_b_fix_luna_high` 的B分支、标签追加与扩展批分母，四个既有臂保持原语义；空/重复/未知臂关闭，合法执行顺序保留，代码身份在加载数据前冻结。最终源码SHA-256 `2b6ba9a4784054f2143dbc9500c1db38f7249ad62edc6b4648c3fded5d409549`；唯一技能验证器返回valid=true。尚未重跑真实第二骨干训练，不把工程修复当效果验证。

## 计划验收与接续边界

主代理已核验Sol修订的D−B整套差异/D−A组件开关/纯选择器控制接口、按依赖传播阻断、旧八任务到新五任务映射及禁止物化规则。增强缓存的精确恢复尚需独立简报，不能仅保存RNG即称等价；此缺口不阻断直读模块和无缓存依赖路径。本轮首次计划状态补丁因整行上下文不全未应用，已以唯一完整行精确修改，无文件回滚或覆盖。
