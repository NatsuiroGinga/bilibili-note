# B-ResNet 朴素增广基线：实现修复简报

- 日期：2026-09-13。
- 性质：修复已批准 B 角色的实现缺失，不提出新机制、数值或数据合同。
- 依据：主代理已核对 `bresnet_p2p3.py` 只在D/F分支生成增广，CLI却可传B，导致B落入无增广路径；旧B运行源码身份缺失，不能作为有效朴素AT结果。
- 无物化：直接使用既有输入与内存候选，不新增数据集、成员副本或预生成攻击资产。当前MP继续，不能覆盖其源码依赖或输出。

## 所有权

实现者使用 `gpt-5.6-luna`，父代理选 `effort=high`。只修改 `.Codex/docs/2026-09-10-DGA对抗鲁棒组相对方案/bresnet_p2p3.py`；只读 `official_p2p3.py`。报告与技能收据分别写本目录 `bresnet-b-fix-report.md`、`bresnet-b-fix-skill-receipt.json`。不得覆盖旧日志/结果，不做Git写操作、不建子代理。

## 精确修改

1. 实现B：每个主批的恶意成员生成一个 `perturb2` 变体，按原样本索引缓存；追加恶意标签，用既有子批梯度累积及扩展批总数作损失分母。这与当前DRIFT B的语义对应，不能误用D的两阶段四候选选择。
2. A/D/F/G路径、随机种子、初始化、数据成员、学习率、批量、轮数、三旧筛选面板及固定0.5阈值均不改变。新正式源阈值评价另由直读接口完成，不在本修复中替换。
3. CLI明确只接收A/B/D/F/G，拒绝未知或重复臂，杜绝再次把未知名字当A运行。展示名同时标明“朴素随机编辑增广（B）”。
4. 在结果spec中记录实现版本与代码身份，历史结果保持原样；运行时通过已有P2P3_OUT指向新目录，不覆盖当前bresnet-p2p3-result.json。
5. 不新增测试夹具或独立玩具实验；不运行自动格式化器。先做最小语法和CLI检查。当前不启动MPS/CUDA训练，等待主代理安排与MP不竞争的真实运行。

## 验收

required_skills为 `/Users/bilibili/.codex/skills/backup/daily-coding-20260811-112930/SKILL.md` 与已适用的PyTorch训练规范；先按现有技能收据模板保存实际路径、读取时刻和哈希。原源码已在版本库可追溯，不复制旧实现回活动目录。

报告具体B分支、追加样本/标签/损失分母、保持不变的四臂、CLI返回码和新版本字段。主代理独立核对后，在新运行身份上完整重跑共同A/B基线；未取得新真实结果前不得说B效果已经验证。
