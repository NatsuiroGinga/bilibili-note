# 服务器 CSE-CIC-IDS2018 清理记录

## 结论

GPU 服务器 `/root/autodl-tmp/thesis/datasets/` 下不存在名称明确匹配 `CSE-CIC-IDS2018` 或 `CSE-CIC-IDS2018-PCAP` 的目录或常见归档文件。本次未运行任何删除命令，可归因的释放空间为 0 字节。

## 审计范围

- 操作根目录：`/root/autodl-tmp/thesis/datasets/`
- 精确名称：`CSE-CIC-IDS2018`、`CSE-CIC-IDS2018-PCAP`
- 允许核对的归档扩展：`.zip`、`.tar`、`.tar.gz`、`.tgz`、`.tar.bz2`、`.tbz2`、`.tar.xz`、`.txz`、`.7z`、`.rar`
- 审计时间：2026-07-23 17:49:10（服务器时区 `+08:00`）

服务器交互 shell 中的 `fd` 是 `fdfind` 的别名，实际可执行文件为 `/usr/bin/fdfind`。已按要求先加载本机 `~/.zshrc` 和服务器 `~/.bashrc`，并使用该 `fd` 实现完成审计。

## 候选核对

递归精确名称匹配结果为 0。额外执行的递归、忽略大小写 IDS2018 名称核对结果也为 0。

指定根目录顶层只有以下三个目录：

- `/root/autodl-tmp/thesis/datasets/DEDALE-2.0`
- `/root/autodl-tmp/thesis/datasets/GeNIS-2025`
- `/root/autodl-tmp/thesis/datasets/HIKARI-2021`

由于不存在候选路径，没有候选类型或候选大小可报告。`du -sh /root/autodl-tmp/thesis/datasets` 返回 4.2G。

## 删除结果

- 未执行 `rm`、通配符删除或其他删除命令。
- 未触碰上述三个现存数据集。
- 未触碰 `/root/autodl-tmp/thesis/experiments/`、运行制品、模型、代码或其他服务器目录。
- 未触碰本机 `/Users/bilibili/personal/note/raw/datasets/`。

## 空间验证

| 检查点 | 总量（字节） | 已用（字节） | 可用（字节） | 使用率 |
| --- | ---: | ---: | ---: | ---: |
| 审计前 | 53,687,091,200 | 41,055,526,912 | 12,631,564,288 | 77% |
| 最终验证 | 53,687,091,200 | 41,056,534,528 | 12,630,556,672 | 77% |

两次检查的 `df -h` 均显示 50G 总量、39G 已用、12G 可用、77% 使用率。由于本任务没有删除对象，可归因释放量为 0 字节；同期已用空间增加 1,007,616 字节，不计为本任务结果。

## 最终验证

- `fd` 递归精确匹配：0 项。
- `fd` 递归宽松 IDS2018 名称匹配：0 项。
- 服务器数据盘当前使用率为 77%，可用空间约 12G，未触发 80% 或 10GiB 以下告警门槛。
