# R2 UNKNOWN 残差上下文接口修复报告

- 红灯：正式 F-A 启动已复现 `UnknownFallbackExpert.forward()` 不接受 `residual_context` 的 `TypeError`；本机缺少同时具备 `pytest` 与 `torch` 的受控环境，未重复运行目标测试。
- 修复：`src/flow_probe/r2_final_experts.py` 中的 `TorchUnknownFallback.forward` 已接收可选 `residual_context`，并立即 `del residual_context` 显式忽略。
- 绿灯：待主进程同步至已有 `torch` 与 `pytest` 的服务器后，仅运行 `tests/test_r2_unknown_fallback_residual_context.py::test_unknown_fallback_accepts_and_ignores_residual_context` 一次。
