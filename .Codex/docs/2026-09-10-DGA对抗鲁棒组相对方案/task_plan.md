# 第三章新病灶方向：DGA 对抗规避鲁棒的组相对对抗训练方案（GRPO 家族结构思想任务化）

<!-- RESEARCH_ROUTE=DRIFT -->

- **状态**：方案已起草（`pending_design`），**待用户裁决**。未实现、未运行、未创建配置或启动器、未访问 T19–T25。
- **日期**：2026-09-10
- **上游依据**：用户 2026-09-10 裁决（病灶轴从“跨年概念漂移”切换为“DGA 对抗规避鲁棒”，方向原文为“从 GRPO 家族提取机制”）；[DRIFT 第三章恢复卡](../DRIFT/DRIFT第三章恢复卡.md) §五／§六；[DGA 检测系统综述](../2026-09-07-DGA检测系统综述/文献综述.md)；[GRPO 家族论文索引](../../../wiki/papers/grpo/INDEX.md)（32 条去重条目，2026-09-10 盘点）
- **数据合同**：[第三章 DRIFT 数据角色与评价协议](../../../thesis/methods/第三章-DRIFT数据角色与评价协议.md)
- **性质**：新病灶方向的方案文档（Sol 职责）。本文件是**设计草案与门结构**，不构成任何已支持机制、已冻结判据或已批准方向；数值门待前置病灶复现完成后另行冻结。

## 零、待用户裁决清单（本方案唯一的开放口）

| # | 裁决项 | 拟定稿所在节 | 主代理倾向 |
|---|---|---|---|
| D1 | 病灶轴切换的正式化：是否把“跨年概念漂移”从第三章主病灶改为“对抗规避鲁棒”；N13（TTA）与其运行中的 T18 源年臂如何并列 | §一、§4.5 | 切换，N13 保留为独立平行方向；两者不合并叙事、不共享底座 |
| D2 | 最小臂单年取 T17 源年（`screening_only`）还是另选年份 | §5.1 | 取 T17，与源期止损口径及既有 T17 评价制品一致 |
| D3 | 机制开关上线顺序：组相对优势＋子集过滤先行，熵受控与信任域按单因素后开 | §三、§5.2 | 按此顺序；熵受控**方向未定前不得实装** |
| D4 | 判据数值门：本方案只写门结构，数值待 P0 病灶复现后一次冻结 | §5.3 | 采纳；复现与冻结之间不得训练 |
| D5 | 与综述 §8 第三类候选（源侧拼写近邻一致性，已被降级为对照）的关系定位 | §4.4 | 承认其为直接近邻；差量必须落在组相对结构，否则按既有降级结论处理 |
| D6 | 若立项，第三章 Goal 与章级创新主语如何改写（恢复卡 §一与路线总控“当前目标”需同步） | §八 开放问题 | 待 D1 裁决后由主代理处理，本文件不预先改写 |

未获裁决前：不新建代码、配置、启动器，不创建 SwanLab 运行，不读 T19–T25。

## 一、研究问题与方向依据

### 1.1 病灶轴切换（用户裁决）

- **原病灶轴**：跨年概念漂移（DRIFT 源年 2017–2019 → 目标年 2020–2025 的逐年退化，原论文 FPR `0.032849 → 0.096520`）。该轴上的九个候选臂（M1-v1、M1-v2、M2/J06、R02/R07、N12/G2、固定全局更新专家混合、N14/CARS 等）已陆续止损。
- **新病灶轴（本方向）**：**DGA 检测器在对抗规避下的检出率维持**。攻击者通过字符级扰动把恶意 DGA 域名伪装成良性外观以绕过检测，这是 DNS 侧的真实攻防行为，与时间漂移是**不同的风险轴**。
- **数据复用**：仍使用 DRIFT 域名字符串与官方检测器，但**不依赖跨年结构**——扰动组由同一年的干净域名生成，最小臂在单年内部即可完成，不需要 T20–T25 面板参与。

### 1.2 可机械判定的研究问题

> 在**只使用源年数据、不接触目标年份**的条件下，对冻结的干净训练流程加入离散字符扰动的对抗训练，能否在**不提高干净样本误报**的同时，提高检测器对**扰动变体**的检出率；其中“组相对”结构相对“朴素对抗训练”是否有可分离的增量？

- 可机械判定形式：同一干净基线与同一评价集上，配对比较干净 FPR 与对抗 TPR/F1（判据门结构见 §5.3）。
- 证伪成本：P0 为单年零训练前向与扰动生成；训练臂为单年单种子短训，均可单卡执行。
- 证伪顺序：**病灶复现（P0）→ 朴素对抗训练（臂 B）→ 组相对结构（臂 C）**，逐级止损（§七）。

### 1.3 与既有方向的边界声明

- 本方向**不承接**任何时间漂移臂的读数、失败解释或设计依据（被否决实现的读数只说明其自身为何被否决，根规则）。
- 本方向与 N13（测试时适应）**同属第三章但病灶轴不同**：N13 处理部署期的无标签分布变化，本方向处理训练期的对抗输入分布；TENT 式 T18 源年臂的结果与本方案无关，既不支持也不否决本方向。
- 本方向**不声称“首次在 DGA 上做对抗训练”**——该空间已有先例（§二、§4.2、§4.3），差量主张必须落在组相对结构上。

## 二、病灶证据矩阵（已核验在库）

> **读数纪律**：本节所有数字**只引 DGA 检测系统综述已核验的读数**（数字与页码锚点在综述内），不引原始论文的直接页码。MaskDGA 与 CharBot 的**原文入库由另一代理进行中**，页码锚点待补（§六）。他人数据上的读数**不替代本课题复现**：本课题数据上该病灶的实测读数当前为 **0 条**（见最后一行）。

| # | 证据读数 | 出处（本路线已核验） | 核验状态 | 对本方案的作用 |
|---|---|---|---|---|
| E1 | MaskDGA 使**四个字符模型平均 F1 从 `0.977` 降至 `0.495`** | [DGA 综述](../2026-09-07-DGA检测系统综述/文献综述.md) §1 第 4 条（该文第 10 行）；原始论文 arXiv:1902.08909 | 综述已核；**原文入库进行中，页码锚点待补** | 字符级检测器的静态高分不代表规避稳健，病灶规模巨大 |
| E2 | CharBot 在 **FPR 0.1%** 下把**未针对训练的 LSTM.MI／B-RF 的 TPR 压到 `5.58%／1.69%`** | 同 E1 出处 | 综述已核；原文入库进行中 | 低误报工作点（本课题主口径）正是规避收益最大的区域 |
| E3 | 针对 CharBot **重训后 LSTM.MI 仍只有 `55.19%`** | 同 E1 出处 | 综述已核；原文入库进行中 | **朴素对抗重训不足以闭合缺口**——这是本方案最关键的既有对照（§4.3） |
| E4 | CharBot 从 Alexa 良性域**改两个字符**即可骗过多种结构；MaskDGA 说明**代理梯度也可迁移** | [DGA 综述](../2026-09-07-DGA检测系统综述/文献综述.md) §5.3；原始论文第 7–10 页（由综述转引） | 综述已核 | 攻击面小（两个字符）而后果大，扰动族可廉价构造 |
| E5 | 综述已把规避列为**最低表与评价的强制项**：“对抗表至少列 CharBot”（§7）、“规避：CharBot 式良性近邻与至少一种代理梯度攻击”（§10.4） | 同综述 §7、§10.4 | 综述已核 | 即使本方案不立项，规避评价也须进入第三章的最低比较集 |
| E6 | **直接近邻风险（负向）**：综述 §8 第三类源侧候选“源侧拼写近邻一致性”已被定为“与 CharBot 攻击十分接近，**原创性弱**，优先作为**鲁棒训练对照**而非正式机制” | 同综述 §8 第三类候选 | 综述已核 | 本方向若退化为“加入字符扰动训练”，将被该既有降级结论直接覆盖（§4.4） |
| E7 | 低误报工作点本身尚未饱和：Drichel 2023 在 17 个月真实数据上平均 `TPR=0.85735@FPR=0.00506`，FPR 降至 `0.001–0.002` 时 TPR 约 `0.67–0.78` | 同综述 §1 第 1 条、[基线证据表](../2026-09-07-DGA检测系统综述/基线证据表.md) | 综述已核 | 干净域下低误报也未饱和，对抗轴是**叠加**在既有困难之上，不是替代 |
| E8 | **本课题复现：无** | — | **未实测** | P0 病灶复现是本方向的第一道硬门（§5.1）；未复现前不得进入任何训练臂 |

**病灶成立性的当前判断**：E1–E4 来自他人数据与不同检测器（LSTM.MI、B-RF、字符模型），**不能直接外推到 DRIFT 双分支或 B-ResNet**（综述亦明确此边界）。因此本方向在获得本课题 P0 读数前，状态只能是**实验待证**。

## 三、机制设计：组相对对抗优化（设计草案，未冻结）

### 3.1 结构思想来源与任务化映射

| GRPO 家族结构 | 出处与核验状态 | 本任务对应物（任务化定义） |
|---|---|---|
| **组相对基线**（去掉价值网络，用同一输入多条输出的组内统计量作基线） | GRPO 原文 arXiv:2402.03300 式(3) p.13、结果监督实例化 §4.1.2 p.14（[笔记](../../../wiki/papers/grpo/2024-Shao-DeepSeekMath与GRPO开山.md)）；**本地全文笔记已核，本方案已复核其 key_finding** | **同一干净域名的 `K` 个字符扰动变体构成“组”**；组内相对量 = 检测器在该扰动组内**被骗程度的相对模式**（而非绝对置信度） |
| **子集过滤**（滤掉全对／全错组；只在满足目标属性的保留子集内算优势） | DAPO arXiv:2503.14476（Dynamic Sampling，式(10)–(13) p.5–7）；GFPO arXiv:2508.09726 式(2) p.3–4（[笔记](../../../wiki/papers/grpo/2025-Shrivastava-GFPO组过滤策略优化.md)）；**两篇笔记均已入库，DAPO 本方案未逐条复核、GFPO 已复核** | **只有能骗过检测器的扰动产生梯度**；全组同质（全部被检出或全部规避）的组不产生学习信号 |
| **熵受控**（低熵模型上丢弃高熵完成的优势；成功序列高熵加成／失败序列低熵重罚） | Simoni-GTPO arXiv:2508.03772 式(10)(11) p.5（[笔记](../../../wiki/papers/grpo/2025-Simoni-GTPO梯度与熵控制.md)）；Tan-GTPO arXiv:2508.04349 §2.1 p.2（[笔记](../../../wiki/papers/grpo/2025-Tan-GTPO与GRPO-S熵加权.md)）；**两篇笔记均已入库，本方案已复核其 key_finding** | 对抗训练中**防止过拟合单一扰动族**：约束检测器在扰动组上的输出熵，使决策边界不塌缩到只克制一种扰动模式 |
| **信任域**（温度控制的连续软门替代硬裁剪，越界样本平滑压低而非清零） | SAPO arXiv:2511.20347 式(6)(8) p.3、序列极限 §4.1 p.5–6（[笔记](../../../wiki/papers/grpo/2025-Gao-SAPO软自适应门控.md)）；**笔记已入库并已复核 key_finding** | **对抗步长约束**：攻击侧限制扰动可行域（编辑预算），更新侧用连续权重代替“越界即清零”，避免单一极端扰动族支配更新 |
| **组内 std 归一化禁用**（组内差异极小时造成难度偏置） | Dr. GRPO arXiv:2503.20783 §3.1 p.6；LitePPO arXiv:2508.08221（组级均值＋批次级 std）（[家族索引](../../../wiki/papers/grpo/INDEX.md)）；**笔记已入库，本方案未逐条复核** | 组内相对量**不得用组内 std 归一化**，须用绝对基线或批次级尺度（与本路线 TTA 方案 §5.2 机制 1 已登记的同一纪律） |

**引用纪律（强制，继承自家族盘点）**：引用“GRPO 不稳／有偏”必须指明是哪一条修正线；同名缩写（GTPO `2508.04349` vs `2508.03772`、SAPO、SRPO）必须带 arXiv 编号；单篇结论不得跨模型族外推（家族全部实验为 LLM 推理任务，**无表格数据或分类任务证据**）。

### 3.2 记号与组构造

- 干净域名 `x`（标签 `y = 1`，DGA 侧），检测器 `f_θ`（干净基线的同一架构与冻结初始化）。
- 扰动生成器 `T`：在**编辑预算** `b`（字符替换／插入／删除的次数上限，先冻结为设计常数）内产生保持标签语义的扰动。两族并行登记：
  - `T_rand`：CharBot 式黑盒随机／启发式替换（不读模型）；
  - `T_grad`：MaskDGA 式代理梯度引导替换（读冻结副本或当前模型的梯度，只用于**构造扰动**，不进入更新路径）。
- 组：`G(x) = {x̃_0 = x, x̃_1, …, x̃_K}`，`x̃_j = T(x; ξ_j)`，`K` 为组大小（冻结值待 P0 后确定；`K` 决定前向成本，属**判定性超参**）。
- 逐样本对抗损失（“被骗程度”的可微代理）：`ℓ_j = ℓ(f_θ(x̃_j), y)`，二分类取 BCE；规避成功的硬判据 `s_j = 1[f_θ(x̃_j) < τ]`（`τ` 为源冻结阈值）。

### 3.3 组相对优势（草案）

```
ℓ̄   = (1/K) Σ_j ℓ_j                       # 组内基线：不使用组内 std
Â_j = ℓ_j − ℓ̄                             # 越大 = 该扰动越能骗过当前检测器
```

- **语义**：`Â_j` 度量的不是“模型有多不确定”，而是“**同一干净域名的各扰动变体之间，哪一个更能骗过当前检测器**”。这是把 GRPO 的“组内相对回报”换成“组内相对被骗模式”。
- **与既有工作的构造差异（如实标注）**：家族盘点的逐页答复已登记——**“批内／组内相对量”在已读来源中没有可直接搬用的构造**，最接近的是 Tan-GTPO 式(14) p.6 的协方差项与命题 2.2 p.5 的零和守恒。因此本机制在报告与正文中的身份是**本课题推论，不是文献结论**。
- **可证伪形式**：若 `Â_j` 与绝对损失 `ℓ_j` 高度共线（组内相对化无增量信息），则本机制无独立价值（判据门 G4-b）。

### 3.4 子集过滤（草案）

- 保留子集 `S = {j : Â_j > 0}`（只让“比组内平均更能骗过检测器”的扰动产生梯度），被拒样本权重为 0。
- 全组同质（`|S| = 0` 或 `|S| = K`）时不产生对抗梯度——对应 DAPO 滤掉全对／全错组、GFPO 只在保留子集内算优势。
- **必须报告的量**：被过滤样本比例，以及被过滤样本与保留样本的干净侧／对抗侧指标分面（防止用极小样本量刷指标）。
- 与家族来源的差异：GFPO 的过滤度量是“目标属性”（长度、token 效率），本方案的过滤度量是“当前检测器是否被骗”——**度量随训练变化**，属任务化改动，须在报告中显式声明。

### 3.5 熵受控项（草案；方向未定前不得实装）

- 候选形式：对扰动组内检测器输出的平均熵 `H̄ = (1/K) Σ_j H(f_θ(x̃_j))` 加权或加门。
- **方向未定（强制前置）**：来源给出两个方向的证据——提示熵**下降**（GMPO §4.4 p.9、DAPO §1／§3.1 p.2、p.4–5）；低熵模型上应**丢弃高熵完成**（Simoni-GTPO 式(10) p.5，`<H>_ini < ln2` 时削减更新）。因此**方向必须由源期诊断先定，不得凭来源直接移植**（与 [N13 TTA 方案](../2026-09-10-DRIFT-TTA任务化方案/task_plan.md) §5.2 机制 2 同一纪律）。
- 任务化目标（若实装）：**防止对抗训练把决策边界塌缩到只克制一种扰动族**，即保持对**未见扰动族**的检出率——这是本方向唯一有判别力的泛化主张，须单列评价。

### 3.6 信任域（草案）

两侧含义，均须独立登记：

1. **攻击侧（扰动可行域）**：`d(x, x̃_j) ≤ b`，编辑预算与字符集约束固定、**不随时间变化**。与朱焱雷第三章的 DS 有本质区别：DS 是训练过程中对攻击难度的时间调度（`ε(t)`、`K(t)`、`α(t)`），本方案不做课程。
2. **更新侧（连续权重）**：用平滑权重 `w(Â_j)` 压低极端组相对量，取代“越界即清零”的硬截断（SAPO 式(6)(8) 的结构思想）；门只读组相对量与符号，不读奖励内部结构。

### 3.7 总目标与开关独立性（草案）

```
L = L_clean + λ · L_adv(组相对优势 + 子集过滤) [+ γ · H̄ 项，方向未定前不加]
```

- 三个候选开关：**组相对优势**、**子集过滤**、**熵受控**（信任域为两侧约束，其中攻击侧预算是构造约束而非开关）。
- **开关独立性要求**：任一开关的关闭必须真的改变优化路径（`λ = 0` 时退化为臂 A 的干净训练；`Â_j → ℓ_j` 时退化为臂 B 的朴素对抗训练）。若某开关只在另一开关开启时才有作用，即惰性开关，不得计入机制数量（根 `AGENTS.md` 因素数量条款）。
- 只有最终自然形成两个独立开关时才做严格 `2×2`；当前不预设机制数量。

### 3.8 与 RL 的边界（强制声明）

本方案**不实现** PPO／GRPO 的策略优化循环：不引入策略分布、重要性比率、裁剪目标或 KL 项，也不把检测器视为策略。“GRPO 家族”在此是**结构思想来源**（组相对基线、子集过滤、熵受控、连续信任域），不是算法移植对象。若后续要引入重要性比率或裁剪目标，须另立方案与理由，不得在本方案内顺带实现。

## 四、边界表

### 4.1 与朱焱雷第三章的边界（必须写扎实）

| 维度 | 朱焱雷第三章（DS-RGO） | 本方案 |
|---|---|---|
| 攻击面 | **连续**扰动：图像式 PGD 谱系；ET-BERT 嵌入空间扰动、BUPT-CNN 像素空间盒约束（印刷第 16–18、27–32 页） | **离散**字符扰动：字符替换／插入／删除（CharBot／MaskDGA 谱系） |
| 训练信号 | 内层攻击轨迹＋外层展开优化：`δ*(θ)` 反应梯度、双层／Stackelberg、Hessian 向量积（印刷第 27–30 页） | **组内相对被骗模式**：无内层轨迹、无二阶项、无展开 |
| 难度控制 | DS 对扰动半径 `ε(t)`、迭代步数 `K(t)`、步长 `α(t)` 做**时间调度**（印刷第 26–27 页） | 无课程：编辑预算 `b` 为固定可行域，不随时间变化 |
| 备用机制维度 | 无熵控制、无子集过滤、无连续信任域 | 熵受控（防过拟合单一扰动族）、子集过滤（只让成功的扰动产生梯度）、连续软门 |
| 病灶同域 | 是（对抗逃逸） | 是（对抗规避） |

**结论**：病灶同域、机制不同族。**不照抄** DS、RGO 的公式、算法、组件数量与调度结构；本方案不写课程公式，不做展开求导。

### 4.2 与 MaskDGA 原法的边界

- MaskDGA 的贡献在**攻击侧**：用代理模型的 Jacobian 显著性选择字符位置并替换，得到黑盒可迁移的扰动；其**防御结论**是对抗重训与蒸馏只能部分恢复，作者建议引入字符串之外的特征（由综述 §1 第 4 条与本地短笔记转述，**原文入库进行中**）。
- 本方案**不主张扰动生成器的创新**：`T_rand`／`T_grad` 复用已有攻击族；差量只落在**优化结构**（组相对、子集过滤、熵受控、信任域）。
- 若消融显示收益完全来自“加入更多样的扰动样本”而与组相对结构无关，则本方案退化为 §4.4 的既有降级结论，按停止条件 §七.5 处理。

### 4.3 与 CharBot 重训实验的边界（本方案臂 B 的既有对照）

- CharBot 的防御实验（把 CharBot 样本加入训练后重测）**就是“朴素对抗训练”的一种实现**；其实测读数（FPR 0.1% 下重训后 LSTM.MI 仍只有 `55.19%`，综述 §1 第 4 条）说明该类防御**未闭合缺口**。
- 因此本方案的臂 B 不是新方法，而是**复现该既有防御形态**；臂 C 相对臂 B 的增量才是本方向要证的东西。若臂 C 不优于臂 B，本方向退化为“已知不足”的防御形态，按 §七.3 止损。

### 4.4 与综述 §8 第三类候选（源侧拼写近邻一致性）的边界（最直接近邻）

- **既有结论**：综述 §8 已把“从源良性域生成受约束拼写近邻、使模型保持良性预测一致”定为**原创性弱、优先作为对照而非正式机制**。
- **差异**：该候选作用于**良性侧一致性**（让良性域不被误报），本方案作用于**恶意侧规避**（让保持 DGA 标签的扰动变体仍被检出）；病灶侧不同，但共享“字符扰动增强”这一表层形态。
- **纪律**：本方案在正文与报告中**不得**把“引入字符扰动”写成创新；差量主张必须指向组相对结构。若组相对结构的增量不成立，本方向**直接接受**该既有降级结论，不作为正式机制。

### 4.5 与本路线已否决机制、与 N13 的边界

| 对象 | 既有否决／状态 | 与本方案的边界 |
|---|---|---|
| 九个时间漂移臂（M1-v1/v2、M2/J06、R02/R07、N12/G2、固定全局更新专家混合、N14/CARS） | 均死于时间漂移轴上的“修复—外溢取舍”（全局损失族的双侧交换）或资格门失败 | 本方案的对抗病灶**无该结构**：扰动组内可观测“被骗／未被骗”，信号天然分组。**这是预判，不是已证结论**，须由臂 B／C 的实测验证；上述读数不得作为本方案的性质或设计依据 |
| N13 测试时适应（TTA） | 当前下一方向，TENT 式 T18 源年臂运行中；方案待裁决 | 病灶轴不同（部署期无标签分布变化 vs 训练期对抗输入分布）；**不合并、不共享底座、不互相引用为支持**。T18 臂结果与本方案无关 |

## 五、实验臂矩阵与判据（数值门待 P0 后冻结）

### 5.1 P0 病灶复现（零训练，第一道硬门）

| 项 | 内容 |
|---|---|
| 目的 | 在**本课题数据与官方 DRIFT 权重**上验证：字符扰动确实降低检出率（对照 E1–E4 的他人读数） |
| 对象 | 官方检查点 `runs/models/drift-official-dsn2026/finetuning.pt`（SHA-256 见恢复卡 §二既有记录）；域名为 T17 `val` 的 DGA 与良性全量 |
| 扰动族 | `T_rand`（CharBot 式两字符替换）与 `T_grad`（代理梯度引导）各一组，编辑预算 `b` 冻结 |
| 读数 | 干净 TPR@源冻结阈值 vs 扰动 TPR@同阈值；规避成功率（ASR）；扰动后仍满足域名合法性规则的比例 |
| 成本 | 单年零训练前向 + 扰动生成，MPS 可跑（目标 < 1 小时，实测后登记） |
| 通过门 | 扰动 TPR 下降 ≥ 预注册最小效应（**数值待本实验的复现读数出来后一次冻结，不得先定后改**） |
| 失败态 | `failed_lesion_not_reproduced`：两种扰动族均无显著下降 → **本方向在复现门止损**，不进入训练臂 |

### 5.2 三臂（同一冻结数据、同一检测器起点、同一预算口径）

| 臂 | 主展示名 | 训练信号 | 成本口径 |
|---|---|---|---|
| **A** | 静态基线（干净 BCE） | 干净样本 BCE，无对抗 | 基准计算预算 `B`；须给**等价总算力**的干净训练对照（更多步或更大批量），否则 B／C 的增益不能排除“更多计算”解释 |
| **B** | 朴素对抗训练（CharBot/MaskDGA 式重训的复现形态） | 同组扰动 + 最坏／平均对抗损失 | 与臂 C **完全相同**的扰动生成器、组大小 `K`、每步前向次数、更新步数、优化器与阈值规则 |
| **C** | 组相对对抗训练（本方向候选） | 组相对优势 + 子集过滤（熵受控／信任域按 D3 顺序单因素后开） | 与臂 B 相同 |

- **公平预算（强制）**：对抗训练成本 ≈ `K` 倍前向 + 反向；臂 A 的等价算力对照、`K` 值与采样开销必须计入并报告，否则不构成公平比较（本路线既有的“共同预算基线”纪律）。
- **单年数据**：先 T17 源年（`screening_only=true`，`seed=42` 单种子），不访问 T19–T25。
- **未见扰动族评价**：训练用扰动族与评价用扰动族必须分组报告——**同族**（训练见过的扰动分布）与**异族**（训练未见的扰动方式／不同引导信号）分开；泛化主张只能基于异族列。

### 5.3 判据门结构（先写结构，数值待冻）

| 编号 | 判据 | 通过条件（结构） | 失败登记 |
|---|---|---|---|
| **G1 病灶门** | P0 复现 | 见 §5.1 | `failed_lesion_not_reproduced` |
| **G2 鲁棒增益门** | 臂 C 相对臂 A | 对抗样本（异族优先）TPR/F1 改善 ≥ 冻结值，且相对臂 B 有可分离增量 | `failed_no_robust_gain` |
| **G3 干净护栏门** | 臂 C 相对臂 A | **ΔFPR ≤ 0**（干净样本不增误报）且干净 F1 不降（容忍值冻结） | `failed_clean_regression` |
| **G4 近邻解释门** | 臂 C 的增量归因 | （a）不能被额外算力解释；（b）不能被“`Â_j` 与 `ℓ_j` 共线”解释；（c）不能被“只是更多样扰动样本”解释 | `failed_explained_by_controls` |
| **G5 非退化与可复现护栏** | 更新与读数 | 组内存在被骗／未骗混合（非退化）；参数位移有限、无 NaN/Inf；主判据 Δ 超出**位级噪声地板**（同一静态模型固定前缀批重复两次前向的读数差） | `failed_no_update`／`failed_nonfinite`／`indeterminate_within_noise_floor` |
| **G6 操作点一致性** | 源冻结阈值与默认 `0.5` | ΔFPR 符号一致 | `indeterminate_operating_point_conflict` |

**噪声地板必须实测的理由（本路线已付代价的记录）**：2026-09-09 正锚点核查实测到**同一模型、同一数据、两次前向流给出相反符号**的裁决（MPS 位级非确定性，见 [正锚点核查 task_plan](../2026-09-09-DRIFT正锚点核查/task_plan.md) §八）。因此本方案的任何 Δ 读数必须先过噪声地板门；符号在地板内翻转的读数不得进入候选排序。

**判据冻结纪律**：G2／G3 的具体数值**只在 P0 完成后冻结一次**，冻结前不得开跑训练臂，冻结后不得因结果不利而下调、删除或重新定义。

### 5.4 制品与运行身份（计划，不构成本文件的实现授权）

- 运行目录（计划）：`thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-dga-evasion-grouprelative-t17-v1/`
- 必须保存：`effective-config.json`、`status.json`、`result.json`、`run.log`、逐样本分数、扰动组清单（含种子与编辑预算）、数据/代码/配置/检查点哈希、`time_roles`、允许／禁止论断清单与停止原因。
- 环境：本机 `/opt/miniconda3/envs/rwkv/bin/python`（MPS）或服务器入口，按根规则实测吞吐后确定。

## 六、与 MaskDGA／CharBot 原文入库的依赖

| # | 依赖项 | 当前状态（2026-09-10） | 影响 |
|---|---|---|---|
| 1 | CharBot 原文（arXiv:1905.01078）与 MaskDGA 原文（arXiv:1902.08909）的**完整全文笔记与页码锚点** | **入库进行中（另一代理）**：本地已存在 2026-09-07 建立的短笔记（`wiki/papers/attack-detection/dga/2019-Peck-CharBot规避攻击.md`、`2019-Sidi-MaskDGA规避攻击.md`）与原件（`raw/papers/attack-detection/2019-*.pdf`），但该目录在本工作树**尚未提交**；页码锚点待入库完成后回填 | 本方案 §二 与 §4.2／§4.3 的原文级论断在入库完成前**不得写入正文**；当前只用综述读数 |
| 2 | 两文中的**防御／对抗训练消融细节**（MaskDGA 的对抗重训与蒸馏消融、CharBot 的重训实验配置） | 待入库复核 | 决定 §4.2／§4.3 的逐条对照精度与臂 B 的复现形态 |
| 3 | 两文的**低误报工作点与统计口径**（CharBot 明确在 0.1%／1% FPR；两文均无多种子区间） | 综述已转述 | 本课题评价若报低误报列，须单列口径差异 |
| 4 | 综述 §8 的**最低规避检查清单**是否已有本课题实现 | 未实现 | P0 的扰动族设计须逐条对照该清单 |

**阻断声明**：在依赖 1 与 2 关闭前，本方案**只能作为设计草案**；不得据未提交笔记写出“某论文报告了某数字”的正文级断言（本文件 §二 的写法即遵守此纪律）。

## 七、停止条件

1. **P0 不复现 → 停**：两种扰动族均无显著下降 → 本方向在病灶门止损，不进入训练臂，不调扰动预算重试。
2. **臂 B 无信号 → 组相对不加**：朴素对抗训练相对臂 A 在对抗 TPR 上无改善，或干净 FPR 越界 → **不进入臂 C**（这是本方案的核心止损点：组相对结构的前提是朴素对抗训练本身有效）。
3. **臂 C 不优于臂 B → 只保留已知形态**：组相对增量不成立，或由 G4 控制解释 → 如实登记“朴素对抗训练在本数据上的读数”，本方向不作为正式机制。
4. **护栏越界 → 停**：`ΔFPR > 0`（干净误报增加）、G5 非退化／有限性失败 → 停止，不调参重跑。
5. **退化为既有降级结论 → 停**：若消融表明收益与组相对结构无关、只来自“更多样扰动样本”，按综述 §8 第三类候选的既有降级结论处理（对照而非正式机制）。
6. **不可分辨 → 先修测量**：G5 噪声地板或 G6 操作点冲突触发 → 不追加成本，先解决测量问题（确定性核或 CPU 复跑）再决定是否重开。
7. **禁止**：不得把任何负结果写成“对抗训练对 DGA 无效”；只能写“本方案在本数据、本预算与冻结判据下未获资格”。

## 八、开放问题（不得在正文当成已证）

1. **扰动族的标签语义**：扰动后“仍是 DGA 域名”的判据（字符集、TLD、长度、合法字符）尚未定义；定义不严会产生“评价的是无效域名”的失真风险。
2. **未见扰动族的定义**：异族评价需要事先注册“族”的划分方式（不同引导信号？不同编辑算子？），否则泛化主张不可判定。
3. **熵受控方向未定**：须先做源期诊断（低熵 vs 高熵），与 N13 方案 §5.2 机制 2 同一纪律；方向证据取得前该开关不得实装。
4. **`K` 与预算**：`K` 属判定性超参，须在冻结前按实测吞吐确定并与臂 B 共享；改变 `K` 即改变机制强度。
5. **与第三章整体方法的关系**：对抗训练是独立主线，还是与现有 C00（官方监督双分支／B-ResNet）组合？本文件不预设；D6 裁决后由主代理改写 Goal。
6. **数据合同适用性**：本方向不依赖跨年结构，但 T20–T25 面板是否、何时进入评价，须另立协议；当前禁止访问。
7. **与 N13 是否共享底座**：默认不共享（不同病灶轴）；若未来合并叙事，须先证明两机制在同一评价下均可分离。
8. **恢复卡 §一 Goal 尚未改写**：§一 当前仍写“跨 family、伪未见 family 与跨年新 eSLD 泛化”，与本方向不一致，属 D1／D6 待裁决事项。

## 九、证据入口

- [DRIFT 第三章恢复卡](../DRIFT/DRIFT第三章恢复卡.md)（§五 本方向登记、§六 唯一下一动作）
- [DGA 检测系统综述](../2026-09-07-DGA检测系统综述/文献综述.md)（§1 第 4 条、§5.3、§7、§8、§10.4）与[基线证据表](../2026-09-07-DGA检测系统综述/基线证据表.md)
- [DRIFT 路线总控](../DRIFT/DRIFT路线总控.md)
- [GRPO 家族论文索引](../../../wiki/papers/grpo/INDEX.md)；机制笔记：[GRPO 原文](../../../wiki/papers/grpo/2024-Shao-DeepSeekMath与GRPO开山.md)、[GFPO](../../../wiki/papers/grpo/2025-Shrivastava-GFPO组过滤策略优化.md)、[SAPO](../../../wiki/papers/grpo/2025-Gao-SAPO软自适应门控.md)、[Simoni-GTPO](../../../wiki/papers/grpo/2025-Simoni-GTPO梯度与熵控制.md)、[Tan-GTPO](../../../wiki/papers/grpo/2025-Tan-GTPO与GRPO-S熵加权.md)
- [朱焱雷第三章机制构思与创新声明分析](../../../thesis/methods/第三章-朱焱雷第三章机制构思与创新声明分析.md)
- [第三章 DRIFT 数据角色与评价协议](../../../thesis/methods/第三章-DRIFT数据角色与评价协议.md)
- [N13 TTA 任务化方案](../2026-09-10-DRIFT-TTA任务化方案/task_plan.md)（平行方向，不同病灶轴）
- 过程记录：[notes.md](notes.md)
