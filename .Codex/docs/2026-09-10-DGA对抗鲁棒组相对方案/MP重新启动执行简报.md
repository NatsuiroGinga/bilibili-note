# 混合曝光与误报保护：MP 筛选重新启动

- 日期：2026-09-13。
- 授权：用户要求继续真实实验筛选；接受额外计算开销，任务外指标不作默认否决项。网页调研与本次已有方案实验并行。
- 科研规格：复用 `task_plan.md` §5.12.6 的 MP 缺失格，不新增机制或数值，不修改生产代码。相对 M 只加入 F 的现有良性误报权重；相对 F 只改变为既有三档混合曝光。
- 旧运行：最后日志为 2026-09-12 21:28:23 的第 2 轮第 1 批；2026-09-13 再查无日志句柄，无完整 MP 结果。原记录保留。
- 新运行：从同一官方初始检查点重新完成 3 轮；本脚本没有可用的 MP 中途恢复入口，不将新运行称为从第 2 轮接续。

## 一、独占职责与边界

执行代理为 `gpt-5.6-terra`，推理强度 `high`，只负责本次运行的只读准备、独立持久启动、进度收据与结果文件。无权修改训练脚本、评价阈值、旧结果、旧日志、训练数据或正式合同。若发现实现不可运行，记录准确异常并交回主代理，不自行更改科研行为。

本机解释器固定 `/opt/miniconda3/envs/rwkv/bin/python`。读取 `official_p2p3.py` 的实际环境、模型、数据和参数入口；使用 `--arms MP` 与独立 `--result-name`，禁止触及默认的 `official-p2p3-result.json`（该文件目前是 P 臂结果）。

目标运行目录为 `thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-mixed-exposure-fp-protection-mps-20260913-rerun1/`；若已存在，先核验其中身份和状态，禁止覆盖或重复启动。通过 `P2P3_OUT` 指向新目录。所有结果、控制台和启动收据留在该目录；过程说明只更新本文件的执行记录。

## 二、执行顺序

- [ ] 读取所属规则、当前恢复卡、§5.12.6 与实际脚本；仅做针对本次运行的检查，不重新设计或调参。
- [ ] 检查新目录存在性、对应训练进程和旧日志句柄，确认 MPS 未被另一训练占用。进程检查只输出 PID、程序名和资源摘要，禁止输出完整命令行、环境变量或凭据。
- [ ] 只读核对官方初始检查点、词元器、T17 训练与 T18 验证四个文件及中和均值缓存；记录实际文件身份和输入行数。固定成员为当前行序前 30000/类训练和前 7500/类验证，不称随机抽样。
- [ ] 仅做命令入口检查，然后以独立持久进程启动；使用现有 MPS 回退配置。源代码、随机种子、3 轮、批量和学习率保持原规格。
- [ ] 记录启动时间、PID、脚本及模型身份、结果路径和真实首个训练心跳；有 PID 无训练心跳只能记加载中。记录峰值或当前资源，不估报未实测耗时。
- [ ] 首个有效心跳后立即向主代理报告，结果未完成时保持“运行中”。完整结果产生后核对 MP 键、3 轮曲线、干净与三攻击面板计数，并交主代理裁决。

## 三、结果解释与停止边界

历史筛选判据仍按原式单独计算，不事后篡改；用户最新的主任务优先取向在研究裁决中另作说明。各候选优缺点须按同一骨干、成员、阈值和运行身份比较。此次是固定源期筛选，不代替正式源冻结低误报工作点或 T20–T25 结果。

只有输入缺失、身份不符、并发冲突、实现异常或实际资源不可运行时停止该次启动并保存证据。网页尚未返回、辅助指标不是最优、没有新增文字审查均不阻断本次已冻结实验。

## 四、执行记录

### 2026-09-13 准备完成

- 目标目录在本次创建前不存在；启动前未发现匹配的本机 Python 训练进程，故无 MPS 训练竞争。
- 官方快照、初始检查点、词元器、T17 训练、T18 验证和 T17 中和均值缓存均已只读核对；实际全量行数分别为 T17 良性/恶意 `1,500,000/1,500,000`、T18 良性/恶意 `150,000/150,000`，本运行固定取当前行序前 `30,000/30,000` 与 `7,500/7,500`。
- 准备收据：[preparation-receipt.md](../../../thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-mixed-exposure-fp-protection-mps-20260913-rerun1/preparation-receipt.md)。下一步只做命令入口检查并以持久进程启动 `MP` 三轮。

### 2026-09-13 持久启动与首个训练心跳

- `official_p2p3.py --help` 入口检查返回 `0`。首次 `nohup` PID 未留下控制台内容且在约 20 秒内退出，未产生训练心跳；该空控制台保留在新运行目录，不把它计作运行开始。
- 已改用独立 `screen` 会话 `ch3_mp_20260913_rerun1` 启动，训练 PID `77528`；语义为 MPS 回退开启、`P2P3_OUT` 固定至独占目录、`--arms MP --result-name mp-screen-result.json`，没有改变脚本或科研参数。
- 首个真实心跳已出现：epoch `1`、批 `1/469`、loss `2.7051`、日志 ETA `54.8 min`。当前为训练中；首个心跳后 PID 的资源快照为 CPU `19.3%`、内存 `1.1%`，未发现其他匹配 MPS 训练进程。控制台：[console.log](../../../thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-mixed-exposure-fp-protection-mps-20260913-rerun1/console.log)。
