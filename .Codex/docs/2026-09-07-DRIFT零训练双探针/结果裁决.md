# DRIFT 零训练双探针 formal-v3 结果裁决

<!-- RESEARCH_ROUTE=RWKV -->

- **状态日期**：2026-09-07
- **裁决层级**：包含 T25 的隔离探索证据，不得用于正式候选存废
- **运行身份**：`ch3-drift-t17-t25-rosa-deepembed-coverage-probe-v1`
- **原始制品**：[formal-v3.json](../../../thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-t17-t25-rosa-deepembed-coverage-probe-v1/formal-v3.json)
- **候选状态入口**：[统一候选筛选台账](../../../thesis/methods/第三章-数据模型机制统一候选筛选台账.md)

> **证据隔离**：主代理误在正式合同冻结前读取 T25 标签分面。原始制品保留，但其中 T25 统计及 T17/T25 聚合量全部退出候选裁决；正式候选只在 T17→T18→T19 源期重新裁决。该修复不得以删除 T25 正式测试为代价，T25 仍须作为 T20–T25 正式主结果的最新年列。

## 一、结论

1. `formal-v3` 已完成，状态 `completed`，墙钟 `546.629945041961` 秒。数据 revision 为 `3b31077020cd1c013d0a75cad51042a2327c4521`，配置 SHA256 为 `d611d85f8a51ab7f80da2ea9f88a44e3fc308b7fa0c53e7e73858e2084b54289`。
2. 本报告的 T25 与跨 T17/T25 聚合读数只作隔离探索描述，不得支持或否决 `R01/R02/R07/J03/J04`。
3. `R02/R07` 恢复为待证，须在 T17→T18→T19 源期重新完成 OOV、低支持、错误相关和二选一门。
4. `R01/J03` 的否决只引用 `formal-v3` 的 T17 分面和独立的 T17→T18 源冻结快速实验，不引用 T25。

## 二、关键读数

| 项目 | 结果 | 边界 |
| --- | ---: | --- |
| ROSA 总覆盖率 | `0.24722423803658208` | 与一阶 n-gram 完全相同 |
| ROSA 后继命中率 | `0.061499026830021196` | 一阶 n-gram 为 `0.06059207157905339`，绝对增加 `0.000906955250967803` |
| ROSA 匹配长度 | `4,008,210 / 4,289,076` 为长度 1，即 `93.45159656765233%` | 长度至少 2 为 `280,866`，至少 3 仅 `26,551` |
| T25 字符 OOV | 良性 `0`，DGA `0` | 只证明字符表闭合 |
| T25 2-gram 域名 OOV | 良性 `0`，DGA `2.084069270294406e-7` | 几乎完全闭合 |
| T25 3-gram 域名 OOV | 良性 `0.000914800655379574`，DGA `0.0031227172928773804` | 有少量跨年新键 |

ROSA 的后继命中存在微小正差，但没有同内存精确对照与配对区间，不能据此宣称预测或分类机制收益。DeepEmbed／Engram 的 OOV 结构只说明条件记忆输入可构造，不证明低支持、碰撞或新键与 C00 错误相关，也不证明查表优于普通嵌入、等参数前馈网络或随机键负控。

## 三、抽样偏差与缺口

- T17 良性预注册样本量为 `366,200`，实际为 `365,121`；实际最坏情形 95% 半宽 `0.0016218089369437283`，略高于预注册 `0.00161942`。不得声称四个分面全部满足预注册抽样精度。
- `formal-v3` 未做原始记录配对、家族分面、逐域名错误相关、等容量负控、配对重采样，也未做 ROSA 与固定 n-gram 的同内存精确对照。
- 原始记录审计为 `enabled=false`，状态为“机械拒绝；当前 SQLite 路径未验收”。本次制品不能裁定跨年重复或泄漏。
- 本次是单个确定性零训练运行，没有分类预测、检查点或可用于论文效果论断的统计比较。

## 四、状态与下一动作

| 候选 | 本次状态 | 下一动作 |
| --- | --- | --- |
| `D02` | P1 输入资格继续补证 | 在 T17→T18→T19 源期复现强 C00，补 raw→eSLD 映射、跨家族门、低误报曲线、错误相关与配对区间 |
| `R01` | 仅以 T17 分面和 T17→T18 源期证据否决 | 不引用 T25；无新源期接口或合同不重开 |
| `R02` | 待证 | 在 T17→T18→T19 重做 OOV 与 C00 错误相关 |
| `R07` | 待证 | 与 `R02` 共用源期门，再二选一等容量短训 |
| `J03` | 因 R01 的源期失败而停止 | 不引用 T25，不启动四格 |
| `J04` | 保留为待证条件方向 | 仅在 D02、条件记忆唯一胜出实现和 `N02` 后续分别过门后评估 |

**当前唯一动作**：进入 `Q002`，只在 T17→T18→T19 源期补齐 D02 的强 C00 与误差级证据；全部冻结后才整批开启 T20–T25 正式测试。
