# R2 QUIC v8.1 正式矩阵启动修复计划

## 目标

解除三轮定向门禁对 `formal` 全矩阵的错误拦截，在不改变 v8.1 科学参数、工具链、队列合同和 600 连接组合的前提下启动正式生产。

## 边界

- 仅修改 `scripts/r2_quic_controlled_collect.py` 中三轮身份检查的阶段作用域。
- `field` 定向门禁仍只允许冻结的 12、32、12 三个目标。
- `formal` 仍固定为 2 个实现、30 个网络配置、5 个种子和 2 个负载，共 600 条独立连接。
- `formal` 按每条连接的 `connection_id` 独立派生方向随机种子，不复用三轮门禁索引 12/32 的冻结种子。
- 首次失败目录保持不变，新运行使用 `r2-quic-v8-1-matrix600-v2`。

## 验收

1. 已观察红灯：正式阶段在采集前报“定向连接不属于冻结的三轮门禁”。
2. 同步修复后直接以新身份启动正式矩阵，首条正式连接作为真实运行验证，不另做重复采集。
3. 持久化状态、日志和单连接完成收据齐全；非阻断复审不延迟采集。

## 制品

- 正式数据：`runs/data-raw/r2-quic-v8-1-matrix600-v2/`
- 启动状态：`runs/launchers/r2-quic-v8-1-matrix600-v2/status.json`
- 启动日志：`runs/launchers/r2-quic-v8-1-matrix600-v2/launcher.log`

## 当前状态

- 三轮聚合门禁：`GO_TO_NEW_FIELD_MATRIX_QUIC_SEED_ONLY`。
- 首次正式启动 `r2-quic-v8-1-matrix600-v1` 在采集前失败，没有产生连接，失败日志保留。
- 修复后运行 `r2-quic-v8-1-matrix600-v2` 于 `2026-08-06T17:15:20+08:00` 启动。
- 首条连接 `054433c5dea3f5c4fd69614d` 已完成，组合为 aioquic、p01、种子 42、小负载。
- 600 条总连接冻结为 400 条训练、100 条校准和 100 条开发验证；学习曲线仅在 400 条训练池内使用嵌套 100/200/300/400，充分性比较 300→400。
- 物化、专家训练与检查点绑定完成前，`quic_expert_ready=false`。
