# DEDALE 2.0 解压任务计划

## 目标

在不占用 GPU、不覆盖现有文件的前提下，安全解压 `zeek_orange_dmz.zip`，并完成 Zeek 表、字段、时间范围及官方标签关联入口盘点。

## 阶段

- [x] 阶段一：恢复课题上下文并建立持久化记录。
- [x] 阶段二：只读审计压缩目录、文件数量、解压大小、路径安全和标签入口。
- [x] 阶段三：确认解压后至少保留 15 GiB，且唯一目标目录不存在。
- [x] 阶段四：以 CPU 解压到唯一目录，不覆盖任何已有文件。
- [x] 阶段五：只读审计 Zeek 表、字段、时间范围和标签关联入口。
- [x] 阶段六：格式化、验证并归档最终记录。

## 强制条件

- 原始压缩包保持不变。
- 解压目录固定为 `/root/autodl-tmp/thesis/datasets/DEDALE-2.0/extracted/zeek_orange_dmz/`。
- 目标目录只要已经存在就停止，不合并、不覆盖。
- 解压后预计可用空间不足 15 GiB 时停止。
- 发现绝对路径、`..` 路径穿越、符号链接或其他危险成员时停止。
- 不启动、不停止、不干扰任何 GPU 任务。

## 错误记录

- 首次解压在写入任何 ZIP 成员前因服务器不存在 `/usr/bin/time` 退出，退出码为 127；仅留下本次创建的空目标目录和失败日志。处理方式是确认目录为空后使用 `rmdir` 清理，改用 Shell 时间戳计时再重试。
- 重试解压本体成功，但自检中的 NUL 字节计数转义错误将文件数误报为 0；文件字节总量已经正确。未删除或重新解压数据，改用修正后的只读计数完成验收。
- 首轮字段审计使用以连字符开头的 `printf` 格式串，导致表名行打印失败；字段和标签统计仍已完成。随后改用固定格式串重跑精简审计，确认 18 类 Zeek 日志的字段与类型在各天均只有 1 个版本。

## 状态

**任务已完成**：解压和独立验收通过，字段、时间、标签及官方使用协议已经归档到 `.Codex/docs/2026-07-20-DEDALE解压与标签关联准备.md`。橙色 DMZ 视角仅观察到 2 条攻击流，因此只能作为时间外推挑战的一部分，不能单独承担模型训练或主要性能结论。
