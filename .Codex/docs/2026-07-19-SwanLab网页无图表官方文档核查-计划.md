# SwanLab 网页无图表官方文档核查计划

## 目标

依据 SwanLab 官方文档、官方源码、官方问题记录和公开运行实测，解释运行 `rd2jwog6` 云端已有指标摘要但网页无图表的原因，并给出可独立验收的最小修复方案。

## 阶段

- [x] 恢复交接文档、实验计划和最近实验记录。
- [x] 核查官方文档与源码中的图表创建、指标列和公开访问规则。
- [x] 实测公开运行页面、云端接口与客户端数据结构。
- [x] 形成根因排序、最小修复方案和验收步骤。
- [x] 使用 Prettier 格式化并检查引用链接和文档差异。

## 关键问题

1. 单个标量数据点是否会自动生成或显示折线图？
2. 指标键含多层 `/`、指标数量为 274 时，是否触发自动图表或工作区限制？
3. 为什么运行摘要有值，而指标列接口和图表面板为空？
4. 公开运行、客户端版本和网页端版本是否影响图表可见性？

## 边界

- 不修改实验代码、交接文档和现有运行制品。
- 不把未被官方证据或可重复实测支持的猜测写成结论。
- 只在 `.Codex/docs/` 新建本次核查文档。

## 状态

核查完成。结论见 `2026-07-19-SwanLab网页无图表官方文档核查.md`。

## 已遇到错误

- 首轮检索误用不存在的 `docs/zh` 路径；仓库实际采用根目录 `zh/` 和 `en/`，已按真实结构继续核查。
- 首轮把上传器记录数近似当成指标键数；通过 `summary` 和当前 `series` 接口复核后，两个目标运行均为 274 个唯一自定义标量键。
- 最初请求 `/static/...` 资源返回 404；当前生产前端资源实际位于 `/assets/static/...` 与 `/assets/dynamic/...`。
- 当前会话没有可用浏览器运行时，无法重新截取目标页；已改用公开页面 HTTP 状态、已登录只读 API、生产前端接口和既有验收截图交叉核验。
- 误以为图表分区属于单个运行；当前生产前端证实分区和图表定义属于项目视图，指标数据才按运行编号查询。
