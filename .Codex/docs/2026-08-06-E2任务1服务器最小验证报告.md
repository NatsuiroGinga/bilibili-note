# E2 任务 1 服务器最小验证报告

## 状态

- 状态：`BLOCKED`。
- 阻塞原因：执行环境的安全审批拒绝将本机 E2 源码同步到远程服务器。审批指出，用户虽要求同步，但当前上下文无法验证该远程主机和目标路径的归属；因此不得绕过或以替代方式上传。
- 已在收到控制器转述的授权后提交精确单文件白名单同步请求；审核器仍拒绝，理由是该转述不是当前对话中可核验的用户直接确认。
- 未传出源码，未修改本机生产代码，未启动训练，未读取最终测试或未分配捕获组，未执行第二次或重复测试。

## 已完成的只读预检

- 已确认 `GPU_SSH` 和 `GPU_PWD` 环境变量存在，未打印或记录其值。
- 已确认远端实验根目录存在，`uv` 可用，远端磁盘可用空间约 44 GiB。
- 已查询持久会话与 `e2_hard_domain`、`pilot120`、`r2-quic` 相关进程；除本次查询命令自身外，未发现活动实验进程。
- 已创建远端 `src/flow_probe/` 和 `tests/` 目标父目录；未写入任何源码或测试文件。
- 已计算待同步四个文件的本机 SHA-256：
  - `src/flow_probe/e2_hard_domain_data.py`：`ab7868a1af32a1bb0fc2a7aaea95e186b206b0b6c3ce5a5a1185090436e2affe`
  - `src/flow_probe/e2_hard_domain_metrics.py`：`94163fa041f38e528a98d1d94f435d45ef85815fbf305a62ed5b033c0decf8f3`
  - `tests/test_e2_hard_domain_data.py`：`d491057d3bdf503c78cc8174049341b20eecaa7932ea06968157688705f4bbf6`
  - `tests/test_e2_hard_domain_metrics.py`：`d427ff127f24b7611c4f75c8ce1ac17732784745edc77925f433c4b2d463fb33`

## 未执行项

- 未执行远端 SHA-256 对照，因为四个文件未同步。
- 未执行唯一允许的目标测试命令，因此无测试退出码和测试日志。
- 未执行既有 E1 开发制品的只读加载或面板构造。

## 解锁条件

需要用户在当前对话中直接确认当前远程服务器及目标目录归属，并授权把上述四个指定源码与测试文件同步至该目录。获得授权后，按既定顺序执行一次同步、SHA-256 对照、一次精确目标测试；仅在测试通过后执行一次 E1 开发制品只读加载与面板构造。
