# 统一实验环境报告

## 修改

- 根 `AGENTS.md:45` 明确本地文献混合检索的 uv 项目仅是检索工具环境。
- 实验规则 `AGENTS.md:31-32` 将本机所有实验统一到 `/opt/miniconda3/envs/rwkv/bin/python`，删除旧 `.venv` 缺包与备用入口描述。
- 脚本规则 `scripts/AGENTS.md:11` 将 `uv run --no-sync` 明确限定为服务器正式进程。

## 未修改

- 未修改代码、配置、Goal、论文、恢复卡或 Claude 薄导入。

## 验证

- 统一解释器版本：Python `3.14.5`；torch `2.12.0`、numpy `2.4.6`、scipy `1.18.1`、scikit-learn `1.9.0`、pyarrow `25.0.1`、pandas `3.0.5`、polars `1.44.1`、transformers `5.16.1`、tokenizers `0.23.2`、swanlab `0.9.0`；`tqdm`、`torchinfo`、`matplotlib`、`seaborn` 已安装。
- 沙箱内 MPS 为 `False`，沙箱外以同一解释器实测为 `True`；规则明确采用主机边界。
- 本机旧解释器和 `.venv` 实验入口无命中；服务器 `uv run --no-sync` 与本地文献检索 uv 项目仅以各自非本机实验角色命中。
- 最长实验脚本链为 `28,238` 字节（根 `14,563` + 论文 `2,684` + 实验 `9,251` + 脚本 `1,740`），距 `28,500` 目标余 `262` 字节；其余活动链更短且均低于 `32,768`。
- `git diff --check` 通过。
