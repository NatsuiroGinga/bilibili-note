# R2 QUIC v3 三次端到端门禁报告

## 结论

v3 三轮端到端门禁已完成，机器审计唯一状态为 `NO-GO_KEEP_QUIC_UNKNOWN`。

原因是 run2（quiche）的代理队列丢包为 `404`，使 `queue_drop_zero=false`。该轮其余已核验条件均通过，但三轮合同要求全部条件同时为真。因此 QUIC 保持 `UNKNOWN` 回退，不进入字段矩阵，不建立正式 QUIC 训练或统计主张。

## 执行边界

- 仅运行既有 QUIC v3 正式包装器和机器审计器。
- 未修改源码、配置、旧 v1/v2 制品、TCP/UDP 物理拟合代码或运行目录。
- 未重试任何一轮，未补跑，未启动第四次运行。
- 未等待或干预并行的 TCP/UDP GPU 拟合。

## 三轮执行记录

| 轮次 | 运行身份 | 实现与索引 | 终态 | 关键结果 |
| --- | --- | --- | --- | --- |
| run1 | `r2-quic-determinism-repair-v3-run1-aioquic` | aioquic，index 12 | `finished` | `driver_exit=0`、`tee_exit=0`、`all_checks_passed=true` |
| run2 | `r2-quic-determinism-repair-v3-run2-quiche` | quiche，index 32 | `finished` | `driver_exit=0`、`tee_exit=0`；`queue_drop_packets=404`，`queue_drop_zero=false` |
| run3 | `r2-quic-determinism-repair-v3-run3-aioquic` | aioquic，index 12 | `finished` | `driver_exit=0`、`tee_exit=0`、`all_checks_passed=true` |

三轮开始和结束时间已由启动器状态文件机器核验为严格串行。每轮使用独立启动器目录和独立数据根。

## run2 已通过条件

除队列丢包条件外，run2 已通过下列核验：

- quiche 固定源端口。
- 冻结四元组合同。
- 服务端原始退出码与控制器 `SIGTERM` 收据。
- 64 MiB 响应。
- 解密错误为零。
- 发送错误为零。
- 关停残留队列为零。

## 制品路径

- run1 数据根：`runs/data-raw/r2-quic-controlled-field-userspace-v3-run1-aioquic/`
- run2 数据根：`runs/data-raw/r2-quic-controlled-field-userspace-v3-run2-quiche/`
- run3 数据根：`runs/data-raw/r2-quic-controlled-field-userspace-v3-run3-aioquic/`
- run1 启动器：`runs/launchers/r2-quic-determinism-repair-v3-run1-aioquic/status.json`
- run2 启动器：`runs/launchers/r2-quic-determinism-repair-v3-run2-quiche/status.json`
- run3 启动器：`runs/launchers/r2-quic-determinism-repair-v3-run3-aioquic/status.json`
- 唯一审计摘要：`runs/data-audit/r2-quic-determinism-repair-v3-three-gate-summary.json`

## 验证情况

- 主代理已核验三份启动器状态均为 `finished`，三个主管道的 `driver_exit` 与 `tee_exit` 均为 `0`。
- 主代理已核验三轮严格串行。
- 主代理已运行 v3 审计器并确认其唯一摘要状态为 `NO-GO_KEEP_QUIC_UNKNOWN`。
- 本任务不涉及源码或配置修改，因此未运行格式化、静态检查或测试驱动开发。

## 风险与下一步

本次失败是 v3 既定三轮合同下的有效负面证据，不能通过补跑掩盖。QUIC 必须继续保持 `UNKNOWN`，后续若要重新评估，须先以新的用户授权、独立计划、最小修复和独立复审建立新的运行合同。
