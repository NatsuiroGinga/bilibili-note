# 技能路由 Hook 上下文瘦身验收

## 结论

- 状态：通过。
- 修改对象：`/Users/bilibili/.codex/hooks/skill-forced-eval.js`。
- 原脚本备份：`/Users/bilibili/.codex/backups/2026-07-31-skill-route-context-slimming/skill-forced-eval.js`。
- 脚本体积由 `10,419` 字节降至 `1,114` 字节，减少 `9,305` 字节，约 `89.3%`。
- 普通提示和研究提示的标准输出均为 `357` 字节，其中 `systemMessage` 为 `318` 字节，低于 `600` 字节目标。
- 斜杠命令标准输出为 `18` 字节，不包含 `systemMessage`。

## 任务边界

本次只修改技能路由脚本，不修改其他 Hook、配置或研究实验。以下引用配置保持原样：

- `/Users/bilibili/.codex/hooks.json`
- `/Users/bilibili/.codex/hooks/hooks.json`

修改前后两份配置的校验值一致：

```text
7a7d1c80a6c1306636ace869d03aaba8256d9e78abc1096fee3ee3fc989fc154  /Users/bilibili/.codex/hooks.json
3c56c21de4820a9a47bb240176ec3a00b14c42047789ba188f79809fe691e72e  /Users/bilibili/.codex/hooks/hooks.json
```

## 变更内容

- 删除每轮对本地技能和插件技能目录的动态扫描。
- 删除技能分类、关键词预匹配、完整技能列表拼接和 Obsidian 技能追加逻辑。
- 删除强制输出 `Activating` 或 `No skills needed` 的要求。
- 改为标准 JSON 输出，普通请求返回 `continue` 与固定中文 `systemMessage`。
- 固定规则要求依赖本会话已加载的技能清单，静默选择最少且直接相关的技能。
- 有专用 `Skill` 工具时调用该工具，否则只读取所选技能的 `SKILL.md`。
- 无匹配技能时直接执行，不枚举技能、不输出路由过程、不在同一轮重复读取技能。
- 保留原有斜杠命令跳过语义：以 `/` 开头且后续不再含 `/` 的输入只返回 `{"continue":true}`；绝对路径仍按普通提示处理。
- 未保留额外 Obsidian 提示，因为当前仓库已有项目规则负责研究知识写回，继续注入会产生重复上下文。

## 备份证据

原脚本备份校验值与修改前记录一致：

```text
11120733ce70bd00b76580bd6f87b6548d261bf12d4a2c4b73fdb3d8630b8ef2  /Users/bilibili/.codex/backups/2026-07-31-skill-route-context-slimming/skill-forced-eval.js
```

新脚本校验值：

```text
3f889526e8a0b79d23f3425fa67a2c792974cbb834d8277d8f1e064b55a960ed  /Users/bilibili/.codex/hooks/skill-forced-eval.js
```

## 验证情况

### 语法检查

执行：

```bash
node --check /Users/bilibili/.codex/hooks/skill-forced-eval.js
```

结果：退出码 `0`，无语法错误。

### 标准输入样例

使用 `child_process.spawnSync` 将三个 JSON 样例写入脚本标准输入，并解析脚本标准输出。

| 样例 | 用户提示 | 退出码 | 输出字节数 | 消息字节数 | 输出字段 | 结构 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 普通提示 | `请优化这个函数` | `0` | `357` | `318` | `continue, systemMessage` | 有效 |
| 斜杠命令 | `/commit` | `0` | `18` | `0` | `continue` | 有效 |
| 研究提示 | `分析当前基线实验结果并给出下一步` | `0` | `357` | `318` | `continue, systemMessage` | 有效 |

验证程序同时断言：退出码为 `0`、`continue` 为 `true`、普通与研究提示包含字符串类型的 `systemMessage`、消息不超过 `600` 字节、斜杠命令不包含 `systemMessage`。总体验证退出码为 `0`。

### 静态残留检查

执行：

```bash
rg "collectSkills|categorizeSkills|readdirSync|SKILL_LIST|Available skills|Activating:|No skills needed" /Users/bilibili/.codex/hooks/skill-forced-eval.js --line-number
```

结果：退出码 `1` 且无输出，表示没有匹配项。脚本不再扫描或拼接完整技能列表。

## 未执行事项

- 按任务要求未运行格式化工具。
- 未修改任何实验代码、实验配置或运行状态。
- 未提交 Git，也未推送远程仓库。

## 生效要求与风险

Hook 在会话启动时加载，当前 Codex 会话不会热更新。必须退出并重新启动 Codex，之后的新提示才会使用瘦身后的脚本。

本轮直接执行脚本的验证已通过，但没有在重启后的完整 Codex 会话中验证可见行为。重启后如需复核，可提交一个普通提示和一个斜杠命令，确认普通提示不再显示技能枚举或路由过程，斜杠命令不附加技能规则。
