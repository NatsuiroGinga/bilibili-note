# 2026-08-04 强制恢复文档精简报告

## 1. 结论

两份强制恢复文档已按职责重新压缩：

- `output/开题改进交接文档.md` 只保留长期稳定边界，现为 115 行、10,434 字节。
- `output/第一创新点实验总控.md` 只保留当前 R2 执行态，现为 182 行、17,936 字节。
- 精简前全文已归档到 `.Codex/docs/archive/`，没有删除历史证据。
- 过期的 2026-07-23 单私有结构活动快照已从两份当前文档移除，并改为 2026-08-04 R2 最终物理旁路状态。
- 当前仍需单独修复 PreCompact Hook 的活动计划配置与生成模板，否则后续 Hook 触发可能再次写回旧计划和重复恢复顺序。

## 2. 输入

按任务要求完整读取：

1. `AGENTS.md`
2. `output/AGENTS.md`
3. `.Codex/docs/AGENTS.md`
4. 精简前 `output/开题改进交接文档.md`
5. 精简前 `output/第一创新点实验总控.md`
6. `.Codex/docs/2026-08-04-R2最终物理旁路正式实验计划.md`
7. `.Codex/docs/2026-08-04-R2最终旁路统计裁决合同.md`

为核对当前 R2 事实，额外读取：

- `.Codex/docs/2026-08-04-R2最终旁路数据准备报告.md`
- `.Codex/docs/2026-08-04-R2最终旁路模型实现报告.md`
- `.Codex/docs/2026-08-04-R2最终旁路中期独立审查.md`
- ns-3 attempt5 实施与独立复审报告
- QUIC qlog 修复与独立复审报告
- TQH-C2 双物化报告
- 当前并行数据任务提供的候选制品、开发拆分和服务器只读核验状态

## 3. 并发保护

编辑前重新完整读取两份目标文件，并核对 SHA-256：

| 文件 | 编辑前字节 | 编辑前 SHA-256 |
| --- | ---: | --- |
| `output/开题改进交接文档.md` | 15,688 | `9bec0de9eb1b8542f62415e69f5b7f38773a11bed49cdf2a4de0dc16dd74a12f` |
| `output/第一创新点实验总控.md` | 51,008 | `d159e5198cd03b1e55da024ebcecce6d0a98db1791e02ad8abd1aa84e6f01c8d` |

两次核对之间摘要未变化，未发现其他代理并发修改目标文件。并行数据任务的新进度通过消息与本机制品语义合并进入总控：

- 本地 R2 主候选、QUIC seed 与连接分区制品已经形成。
- 受控只读 SSH 已确认服务器三份 shared-B0 BERT 上游视图存在；先前只是本机受限网络导致连接失败，不是服务器数据永久缺失。
- 新的来源组 calibration/dev-validation 派生视图仍待物化、验收和回收，任务 5 不启动。

## 4. 备份

| 原文件 | 归档路径 | 归档行数/字节 | 归档 SHA-256 |
| --- | --- | ---: | --- |
| `output/开题改进交接文档.md` | `.Codex/docs/archive/2026-08-04-开题改进交接文档-精简前备份.md` | 168 行 / 15,699 字节 | `c40450cb3c841ab1941c258c5bac64ed6d46d4023dde1ab8409ef5e0d73ec2f0` |
| `output/第一创新点实验总控.md` | `.Codex/docs/archive/2026-08-04-第一创新点实验总控-精简前备份.md` | 300 行 / 51,019 字节 | `adad408db9d7194bf40b3929de37a09f3c70dd94cab12e8924b2e8e87ab20fce` |

归档使用 `apply_patch` 原地移动，仅把一级标题改成“2026-08-04 精简前备份”以避免与当前执行版混淆；其余精简前正文完整保留。

## 5. 修改结果

| 文件 | 精简前 | 精简后 | 减少 |
| --- | ---: | ---: | ---: |
| `output/开题改进交接文档.md` | 15,688 字节 | 10,434 字节 | 5,254 字节，约 33.5% |
| `output/第一创新点实验总控.md` | 51,008 字节 | 17,936 字节 | 33,072 字节，约 64.8% |

当前 SHA-256：

- `output/开题改进交接文档.md`：`a1dcce8249c019621aa8fd906dd1e9e6f27bff1eea0d5184265d53ed5b71f5b8`
- `output/第一创新点实验总控.md`：`a14d1b59a83b1d3e9c2066d80ea5b02d098e567d98dda89954262c34f61501cb`

### 5.1 开题交接保留内容

- 同一生成式大模型的恶意流量检测课题定位。
- `Qwen3-1.7B` 主基座与冻结后的非 Qwen 跨基座验证。
- PINN 强制保留及公式、可观测性、量纲、梯度和真值边界纪律。
- 第三章结构与物理建模、第四章训练策略的严格分工。
- 第三章固定编排与第四章简单、中等、困难域复用关系。
- 朱焱雷论文的组织密度参照及禁止复制方法的边界。
- GeNIS、TQH-C2、ns-3 与 QUIC qlog 的稳定证据职责。
- 7,579 条 train-fit 不再切分、10,399/4,000 验证来源按来源组无标签拆分、最终测试不可见。
- F 五组、三种子、检测非劣、物理至少改善 10%、置信区间、禁止挑种子和测试调参等冻结门槛。
- 当前 R2 一句话状态和关键链接。

### 5.2 实验总控保留内容

- 当前 R2 唯一活动裁决，静态 18 维旁路失败与完整 R2 未裁决的边界。
- 共享守恒、TCP、UDP、条件 QUIC 和 `UNKNOWN` 回退合同。
- 冻结底座、TQH-C2 Merkle、R2 主候选 Merkle、角色清单和开发拆分合同摘要。
- ns-3 attempt5 的 512 次运行、256 个严格配对、61,440 窗、15,360 条四窗序列、零守恒违反与关键哈希。
- 59 条 qlog、439,527 窗、41/9/9 连接分区、PTO 缺失、正式训练权重为 0 和不进入硬统计主张的证据边界。
- `F-A/F-H/F-P/F-S/F-R` 的定义、42/43/44 三种子与单组单进程要求。
- `G0-G3`、`-0.021` 非劣界、10% 物理幅度、两层配对自助法、同时置信下界、异常增益审计和互斥最终状态。
- SwanLab 唯一运行标识、在线指标、本地日志、配置/代码/数据哈希、预测与统计包要求。
- 数据任务、模型中期审查 7 项重要门禁、当前阻塞及下一步唯一顺序。

## 6. 迁出内容

以下内容不再占用默认恢复上下文，保留在 2026-08-04 归档及其原始过程报告中：

- 重复的强制恢复顺序和证据优先级说明。
- 过时的 R1/R3 路线展开、旧 R1/R2/R3 筛选顺序与旧时间估算。
- TQH、GeNIS、Qwen、树模型、DistilBERT、状态监督和标准 PINN 的逐轮基线流水账。
- HGB 与静态 DistilBERT 失败过程、包装器失败、SwanLab 401 的重复细节。
- 任务十六至二十一、G2-G5、G4R、B0/B1、S4、D0-D3 等失败路线的重复数值和逐轮叙述。
- Rust/Python 物化语言讨论、旧 A/B/C/D 归因合同和已被 F 五组替代的展开。
- 哑铃、停车场、历史软令牌、网络演算等当前非活动路线的详细计划。

这些内容没有删除，只是不再由两份默认恢复文档重复承载。

## 7. PreCompact Hook 裁决

当前文档各保留一对可识别标记：

- `<!-- PRECOMPACT_SNAPSHOT_START -->`
- `<!-- PRECOMPACT_SNAPSHOT_END -->`

**Hook 源仍需修复。**

1. `.Codex/precompact_snapshot.json` 的 `active_plan` 仍指向 `.Codex/docs/sdd/task-single-private-structure-diagnosis/task_plan.md`，这是 2026-07-23 的过期单私有结构计划。
2. `.Codex/hooks/precompact_snapshot.py` 会在每次触发时按该配置重新生成标记区块；若不修配置，当前人工校正的 R2 快照会被旧任务覆盖。
3. 生成模板还固定注入一份“强制恢复顺序”，会重新制造本次已删除的重复内容。
4. 即使只把 `active_plan` 改为当前 R2 计划，计划的“当前状态”也必须同步维护，否则 Hook 仍可能写入落后于总控的状态。

本任务按范围没有修改 Hook 配置、脚本或测试。建议另立最小 Hook 修复任务：更新活动计划来源，并让快照只写活动计划、状态与门禁，不再复制恢复顺序。

## 8. 验证

已执行的最小验证：

| 检查 | 命令或方法 | 结果 |
| --- | --- | --- |
| 字节与行数 | `wc -l -c` | 两份当前文档分别为 115/10,434 与 182/17,936，均在目标区间 |
| 标题层级 | `rg '^#{1,6} '` | 一级、二级、三级标题层级连续，无残留旧章节 |
| 过期快照 | `rg 'task-single-private-structure-diagnosis|D1、D2|2026-07-23T11:25:35|8 至 12 小时'` | 当前两份文档 0 命中 |
| Hook 标记 | `rg -c 'PRECOMPACT_SNAPSHOT_START|PRECOMPACT_SNAPSHOT_END'` | 每份文档各 2 个标记，即一对起止标记 |
| 关键保留项 | 对 31 个冻结关键词逐项执行 `rg --fixed-strings --quiet` | 31/31 命中，缺失 0 |
| 相对链接 | 对当前两份文档引用的 16 个唯一目标执行存在性检查 | 16/16 存在，缺失 0 |
| 配置摘要 | `shasum -a 256` | 角色清单与开发拆分合同摘要和总控登记值一致 |
| 候选 Merkle | 在三个本地 `artifact-manifest.json` 中用 `rg` 核对 | 主候选、QUIC seed、QUIC 分区三个摘要均与总控一致 |
| 当前与归档摘要 | `shasum -a 256` | 四个文件均已登记 |

按任务要求未执行格式化、Prettier、pytest、全仓检查或任何 Git 操作。

## 9. 未修改

- R2 实验计划、统计合同和其他过程报告。
- 训练代码、数据代码、配置、测试、运行制品和服务器状态。
- `.Codex/precompact_snapshot.json`、`.Codex/hooks/precompact_snapshot.py` 及 Hook 测试。
- 论文正文、Obsidian 笔记和原始数据。

## 10. 残余风险

1. 数据冻结与模型实现代理仍在推进；来源组三划分制品和最终复审闭合后，必须按新制品路径、行数、SHA-256 和门禁状态精准更新实验总控，不能把本报告当作未来状态。
2. Hook 未修前，下一次 PreCompact 可能覆盖两份文档的人工快照区块；正文不会被 Hook 修改，但快照会重新过期。
3. 当前 59 条 qlog 不支持正式 QUIC 主张；不得因已有大量窗口而把同一连接内窗口当成独立样本扩大统计证据。
4. 候选统计尚未开始，当前 `NO-GO/NOT_READY` 是操作和证据状态，不是 R2 方法失败裁决。
