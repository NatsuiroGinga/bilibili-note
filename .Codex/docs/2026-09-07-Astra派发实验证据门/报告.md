# Astra 派发实验证据门报告

## 改动

- 根 `AGENTS.md:86-89` 新增 Astra 派发前置证据门和允许范围。
- 首轮文献与方案继续由 Sol 承担，工程和诊断由 Terra/Luna 依 Sol 计划执行。
- Astra 必须同时取得可查 Sol 制品、直接相关真实制品和明确裁决问题；零训练真实数据病灶探针合格，合成夹具、计划、公式、未运行代码、聊天、自报 `READY` 和仅静态检查不合格。

## 未修改

- `CLAUDE.md` 保持 `@AGENTS.md` 薄导入。

## 验证

- `git diff --check` 通过。
- 路由检索仅命中新增的 Astra 门禁，以及既有“自报 `READY`、静态检查不能自动接受”的通用复核规则；没有旧分档或无证据 Astra 派发规则。
- `CLAUDE.md` 的 `@AGENTS.md` 薄导入和“唯一来源”声明仍在。
- 全部活动链低于 `32,768` 字节：实验脚本链 `29,094`（最长，余 `3,674`）；论文正文链 `20,537`；论文图件链 `18,147`；RWKV 链 `21,343`；原件链 `17,197`；知识库链 `16,067`；高层交付链 `15,524`；过程文档链 `16,753`。
