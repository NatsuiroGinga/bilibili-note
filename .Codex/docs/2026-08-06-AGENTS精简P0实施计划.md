# AGENTS 精简 P0 实施计划

- **日期**：2026-08-06
- **合同**：`.Codex/docs/2026-08-06-AGENTS精简P0实施前审查.md`
- **裁决**：`GO_WITH_FIXES`
- **目标**：先修复并验证允许范围内的机械门禁，再基于当前工作副本精简三份长期规则，使常用发现链低于 32,768 字节且关键安全、恢复和实验边界不丢失。

## 文件边界

允许修改：

- `/Users/bilibili/.codex/AGENTS.md`
- `AGENTS.md`
- `thesis/experiments/llm_probe/AGENTS.md`
- `/Users/bilibili/.codex/hooks.json`
- `/Users/bilibili/.codex/hooks/run-node-hook.sh`
- `/Users/bilibili/.codex/hooks/security-guard.js`
- `.codex/hooks/run_node_hook.sh`
- `.codex/hooks/tests/test_run_node_hook.py`
- `.codex/hooks/tests/test_security_guard.py`（新增）
- 本计划、备份清单、备份文件和最终实施报告

禁止修改：

- `thesis/AGENTS.md`、`scripts/AGENTS.md`、项目 `.codex/hooks.json`、PreCompact 与项目策略守卫
- 用户级嵌套历史 Hook 配置、`config.toml` 信任哈希和 Hook 状态、用户级 `.rules`
- RWKV 总控、两份 PINN 总控、论文正文、实验源码、配置、数据、运行制品、服务器和 Git 历史
- `project_doc_max_bytes`

## 阶段与门禁

- [x] **P0-0 基线与备份**：记录三份规则的字节、SHA-256 和定向差异；备份 9 个目标文件并完成 `cmp -s` 逐字节核验。
- [x] **P0-1 Hook 红灯**：新增最小目标测试，分别证明启动器拒绝 `security-guard.js`、普通 Bash 产生旧 `continue` 输出、活动配置仍含错误路径与 `UserPromptSubmit` 注册；启动器目标测试 `1/5` 预期失败，安全守卫目标测试 `4/4` 预期失败。
- [x] **P0-2 Hook 绿灯**：仅把 `security-guard.js` 加入两个启动器白名单；安全守卫对中性 Bash 退出 0 且无输出，对危险 Bash 拒绝，对确认分支只输出 `PreToolUse` 支持字段；用户级配置改经启动器调用、matcher 收窄为 `Bash`，删除整个活动 `UserPromptSubmit` 组。启动器 `5/5`、安全守卫 `4/4` 通过。
- [x] **P0-3 Hook 验证**：`jq empty`、两个 JavaScript 语法检查、两个 Shell 语法检查均退出 0；正反例由安全守卫 4 项测试覆盖；旧错误路径、`node -e` 与活动 `UserPromptSubmit` 搜索无命中；两个启动器逐字节一致。
- [x] **P0-4 服务器事实源门禁**：总控所有者已在 RWKV 总控“GPU 状态”写入唯一活动服务器与 `GPU_SSH`、`GPU_PWD` 状态事实，并规定切换先更新总控；本任务只读核验到该条，不修改总控。
- [ ] **P0-5 三份规则精简**：仓库根已由 10,585 字节精简到 5,490 字节，`llm_probe` 已由 5,722 字节精简到 4,382 字节并通过保留项核对。用户级 2,314 字节候选已生成，但写回 `/Users/bilibili/.codex/AGENTS.md` 被安全审批拒绝；原 12,302 字节文件保持不变，等待用户对该精确覆盖给出明确授权。
- [x] **P0-6 字节与哨兵验收**：五条活动发现链均低于 32,768 字节；关键哨兵和保留项命中，易变服务器/模型/数据硬编码及行尾空白无命中。因用户级规则未精简，`llm_probe` 与 `scripts` 尚未达到建议的 8 KiB 余量。
- [x] **P0-7 差异审计**：`git diff --check` 退出 0，定向差异符合允许范围；用户级规则和历史技能脚本与备份逐字节一致。
- [x] **P0-8 报告**：已写 `.Codex/docs/2026-08-06-AGENTS精简P0实施报告.md`，记录红绿灯、语法、格式化、字节证据、既有 Hook 全套单一失败及新会话限制。

## 可证伪假设

唯一假设：安全门禁失效源于启动器白名单/配置路径错误与 `PreToolUse` 输出协议漂移；移除活动技能注入注册可直接停止旧字段导致的重复注入，不需要修改历史脚本。若最小测试不按该原因失败，停止实施并重新调查，不叠加第二个修复猜测。

## 验收命令

- `python3 .codex/hooks/tests/test_run_node_hook.py`
- `python3 .codex/hooks/tests/test_security_guard.py`
- `jq empty /Users/bilibili/.codex/hooks.json`
- `node --check /Users/bilibili/.codex/hooks/security-guard.js`
- `node --check /Users/bilibili/.codex/hooks/skill-forced-eval.js`
- `/bin/sh -n /Users/bilibili/.codex/hooks/run-node-hook.sh`
- `/bin/sh -n .codex/hooks/run_node_hook.sh`
- `wc -c` 对目标规则及五条发现链核算文件执行
- `rg` 核对简体中文、路线入口、凭据禁写、GPU 通知、SwanLab、防泄漏和用户改动保护哨兵
- `git diff --check`

## 已遇错误

- 沙箱首次拒绝在 `.Codex/docs/` 创建备份目录，错误为 `Operation not permitted`；按权限协议申请精确升级后创建成功，未改动其他路径。
- 首次用 `python3 -m unittest` 加文件路径执行时因以点开头的路径被解析为空模块而退出 1；改为直接运行两份测试文件后取得真实红灯，不把调用错误计作需求失败。
- 写回用户级 `AGENTS.md` 的精确升级被安全审批拒绝：当前用户消息只有进度查询，未明确授权大幅删减全局长期规则。按审批要求未绕行，候选保留在 `/private/tmp/agents-p0-user-AGENTS.md`，活动文件与实施前备份逐字节一致。

## 当前状态

本轮可执行工作已收尾；**P0-5 用户级规则写回等待明确授权**。新会话 `/hooks` 信任与加载核验仍待后续完成，相关文字安全边界继续保留。
