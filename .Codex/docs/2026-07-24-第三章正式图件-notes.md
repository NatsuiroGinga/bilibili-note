# 第三章正式图件笔记

## 现有图位

- 原图 3-1 位于 3.4.5 末尾，要求画完整方法计算图。
- 原图 3-2 至图 3-5 位于 3.6.9，均为依赖未冻结制品的结果图。
- 方法图增加为图 3-1 至图 3-5；原结果图顺延为图 3-6 至图 3-9。
- 仓库尚无 `thesis/figures/第三章/` 目录，也无可复用的正文图片引用样式。

## 工具与字体

- `uv 0.11.17` 可用。
- 系统 Python 未安装 `pubfig` 或 Matplotlib，Pillow 可用。
- `rsvg-convert` 与 Inkscape 不可用；`pdfinfo` 与 `pdffonts` 可用。
- 中文字体使用 `/System/Library/Fonts/Hiragino Sans GB.ttc`，脚本直接注册字体文件。
- 计划新建独立 `pyproject.toml` 并由 `uv.lock` 锁定 Matplotlib 与 Pillow 版本。

## 正文插入位置

- 图 3-1：3.2.4 之后，先给出整体方法。
- 图 3-2：3.3.1 容量归一化之后，图解四窗口五锚点。
- 图 3-3：3.3.5 之后，图解平移自由度和分段守恒。
- 图 3-4：3.4.4 之后，图解四头交叉注意力和有界残差。
- 图 3-5：3.5.2 之后，对比训练与推理路径。

## 出版验收

- PDF 使用 `pdfinfo` 检查页面尺寸与页数，使用 `pdffonts` 检查字体嵌入。
- PNG 使用 Pillow 检查像素尺寸和可解码性，并逐张可视化审查。
- SVG 检查 `viewBox`、宽高和可解析性，不将文字转为不可编辑路径。

## 2026-07-24 实验裁决更新

- S3 与 E2 已完成种子 42、43、44 的同测试分区物理重算。
- 种子 42 `overall_pass=true`，未观测状态误差和物理残差分别改善 13.34% 与 60.06%。
- 种子 43 因未观测状态误差相对 S3 恶化 9.89% 而 `overall_pass=false`。
- 种子 44 的未观测状态误差恶化 53.68%，物理残差只改善 8.59%，`overall_pass=false`。
- 三个种子的检测门槛均通过，家族宏平均 F1 点差依次为 `+0.005671`、`+0.007955`、`+0.001066`。
- 因此，机制图不得暗示 E2 已完成最终结构冻结；结果型图仍等待主控裁决后再依据已核验制品生成。
