# R2 F-A 修复后重新拟合与启动计划

## 目标

在保留旧成功物理检查点完整证据的前提下，将已通过回归测试的 `checkpoint_binding` 名称遮蔽修复纳入新的不可变绑定，使用完全相同的科学配置重新拟合物理专家，并仅重启种子 42 的 `F-A`。

## 不变项

- 数据、拆分、种子、步数、优化器、损失、物理公式和训练预算不变。
- `run_r2_final_physics_fit.sh` 与 `run_r2_final_probe.sh` 字节不变。
- `F-P/F-S` 只更新合法检查点绑定，不启动。
- QUIC 继续 `UNKNOWN`、正式权重为 0；最终测试保持不可见。

## 顺序

- [ ] 新建成功制品归档脚本，使用一次精确测试驱动开发；脚本必须拒绝活动进程、缺失或不完整成功制品、覆盖已有归档和非空重建根。
- [ ] 将当前成功输出根整体移动到唯一 `superseded-by-probe-fix-<UTC>` 归档，记录旧检查点与清单摘要，重建空输出根。
- [ ] 同步修复后的 `r2_final_distilbert_probe.py`，核对远端 SHA-256 `7da76322aa4918bbdb0f8c918566763fd5f906b5fe9cf25f335955a6edbf36cc`。
- [ ] 用同一物理拟合配置和新的唯一身份运行；采用 `UV_NO_SYNC=1`，不改变包装器摘要。
- [ ] 核对运行完成、零坍缩收据、检查点、不可变清单、代码/数据绑定和 SwanLab；立即回收本机。
- [ ] 一次性回填 `F-A/F-P/F-S` 三配置的新检查点与清单摘要，临时同步、三份摘要全部匹配后原子提升。
- [ ] 仅启动 `F-A` 新身份；前两分钟采样日志、GPU、显存、吞吐与 SwanLab。

## 失败边界

- 归档、新拟合、绑定或三配置事务任一失败即停止，不修改旧证据，不启动探针。
- 启动故障不计科学失败，但每个身份、状态和日志必须保留。
- 不因本轮修复改阈值、样本、模型容量或评价协议。

## 状态

**进行中：** 名称遮蔽修复已通过一次精确红绿验证，等待成功检查点原子归档与重新拟合。
