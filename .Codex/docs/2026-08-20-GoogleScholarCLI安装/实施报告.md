# Google Scholar CLI 安装实施报告

## 结论

已将 `scholar` 安装至 `/Users/bilibili/.local/bin/scholar`。安装资产来自技能维护方 `edwinhu/workflows` 明示的 `edwinhu/google-scholar-cli` GitHub Release，适用于 Apple Silicon，哈希已核对一致。混合检索来源级调用已抵达真实 CLI，但因未认证而正确降级为 `failed_missing_auth`。

## 已变更文件

- `.Codex/docs/2026-08-20-GoogleScholarCLI安装/任务计划.md`
- `.Codex/docs/2026-08-20-GoogleScholarCLI安装/核验记录.md`
- `.Codex/docs/2026-08-20-GoogleScholarCLI安装/实施报告.md`

未修改检索代码、依赖锁、`raw/`、`wiki/`、Zotero 或论文正文。

## 验证

- 发布资产 SHA-256 与 GitHub Release 公布值一致。
- `command -v scholar` 与 `scholar --help` 均成功。
- 通过 `uv run --project scripts/literature_search --locked --no-sync` 运行 `machine learning` 来源级适配器调用。
- 适配器返回 `failed_missing_auth`，并未泄露 cookie 或其他凭据。

## 遗留风险

- `scholar --version` 没有独立版本输出，需以标签、提交和资产哈希溯源。
- Google Scholar 返回的候选仍仅是未核外部候选，不构成全文或论文论断证据。
- 认证前无法验证真实结果字段；认证后需要重新执行一次相同来源级调用。
