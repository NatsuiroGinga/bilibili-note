# AGENTS 分层拆分任务计划

## 目标

在不丢失仓库特有强制规则的前提下，将根 `AGENTS.md` 拆为按目录加载的规则，并用只读 Codex 进程验证实际指令源。

## 阶段

- [x] P0：读取根规则、全局规则与 Git 基线，识别重复项和既有改动。
- [x] P0：创建根规则及六个局部规则，建立显式子域路由。
- [x] P0：格式化全部修改文件，检查差异与累计字节预算。
- [x] P0：从七个目录运行只读 Codex 指令源验证并记录哨兵。
- [x] P1：完成迁移矩阵、验证报告和最终复核。

## 决策

- 全局 `~/.codex/AGENTS.md` 已覆盖的身份、沟通、规划、通用技能路由、执行和交付规范不在项目根重复。
- 根规则保留恢复协议、仓库特有开发范式、调试组合、默认搜索边界、凭据底线和子域主动读取路由。
- 第三、四章与 PINN 纪律放入 `thesis/AGENTS.md`；GPU 实验工程纪律放入 `thesis/experiments/llm_probe/AGENTS.md`。
- 原始材料、知识笔记、高层交付和过程文档分别放入 `raw/`、`wiki/`、`output/`、`.Codex/docs/` 的局部规则。
- 以全局文件与当前目录项目文件之和核算 32,768 字节预算。
- `raw/README.zh-CN.md` 中仍适用的稳定标识符、全文优先、证据晋级和数据集来源管理规则已合并；上游安装、旧目录路由和 TDD 说明不迁移。

## 错误记录

- 官方 Codex 手册助手在沙箱内因 DNS 失败；批准联网后返回 HTTP 403。后续以任务给定的官方行为基线、本机 `codex-cli 0.144.5` 和实际只读进程验证为准。
- 验证报告首次由 Prettier 写入时在默认命令沙箱中返回 `EPERM`；经批准仅提升该格式化命令后写入成功，最终格式检查通过。
- 只读 Codex 进程出现模型缓存、MCP、WebSocket 和钩子告警，但均回退成功并以 0 退出；路径生成字段不可靠，因此最终只采信禁止磁盘读取条件下返回的唯一哨兵。

## 状态

全部阶段完成。
