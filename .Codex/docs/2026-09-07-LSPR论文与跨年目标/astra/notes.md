# 第三章跨年方向裁决证据笔记

<!-- RESEARCH_ROUTE=RWKV -->

## 当前约束

- 用户最新裁决高于旧源年最优合同：主问题必须是跨年性能退化，两个独立机制加联合框架，不预限模块层级。
- 路线总控明确 LSPR24 已被历史访问，须标记 `target_informed=true`。
- 本任务仅形成待审核裁决，不在聊天中或文档里宣称新机制有效。
- 跨工作区记忆关于独立 CUDA RWKV 路线与当前规则一致；本任务骨干是 FT，不启用任何历史 RWKV 实现。

## 检索收据

- `research_knowledge.knowledge_search`：`query="LSPR 跨年 选择 模型"`，`mode="lexical"`，通用作用域，`stale=false`，零命中，未推断项目无证据。

## 待核证据

- 根规则、路线总控、恢复卡、第三章定案和 09-04 对抗审核。
- 两名 Sol 的方法综述与 LSPR 原文证据综述。

## 已核查的关键分歧

- 旧卡以边际区间重叠、外推区间宽度判断配对差异，方法不成立；09-04 对抗审核正确指出该点。
- 09-04 审核把源尾部 AP 近饱和推成所有时间环境均无用、把 AP 近一推成 BCE 近零，均超过证据。
- BER 训练损失回传 FT；不能把它与不改变排序的纯阈值后处理一起按构造排除。
- Sol 的相邻对称距离对整体时间倒序不变，且 E=2 时不可能由该项识别方向；其私有分支只有正交惩罚，零映射即可取零，未定义防退化目标。
- AIRL 全文第 5 节、式（1）至（6）与算法 1/2 已重新读取。原法包含有向前缀表征映射和分类器演化，不能简化成一个固定表示的相邻距离后仍沿用 AIRL 理论。
- 多示例 TPAUC 与 pAUC-DRO 已明确占用袋级池化、顶部负袋和可微风险；本课题不能把这些组合称为原始创新。
- 机制一与机制二均只能标记设计待核、实验待证；先做源侧前向可行性门，不在 Goal 内固化有效机制。

## 最后裁决与验证

- 主数据扩大为公开可得网络安全数据，优先 2026/2025 发布但必须有真实时间、主体、标签、强前沿和非饱和余量；发布日期不等于采集年份。
- 不固定 FT；RWKV 须通过自然历史增量和状态污染诊断，不能以人为切段清零弱化原生基线。记忆/遗忘必须在 A/B 单开时均有作用。
- LSPR25 不可获取，不列完成条件。推荐有限反馈的固定未来开发池多保真协议，明确目标知情，不恢复独立性措辞。
- Terra 本地数据门首次混同 Raw83 与当前 FT，已要求回到消费配置纠正；当前 FT 只消费 Dijk 六数组，T23 为段首开始时间，不含已核准的逐流完成时间。
- 无候选新训练或真实效果实验；当前数据门仍缺真实时间与每窗正负实体计数。
- 7 份文档围栏、尾随空白、链接检查及目标路径 `git diff --check` 通过；无冲突标记；不运行 prettier/black。
