# LSPR23 源侧严格时间前向数据门计划

## 任务边界

- 只审计 LSPR23 源侧数据接口与既有清单；`target_reads=0`。
- 不训练、不评价候选机制、不读取任何 LSPR24/LSPR25 数组。
- 文件所有权限于本目录的新建文档；不修改生产代码、配置或恢复卡。

## 执行项

1. 已完成：读取路线、实验工程和脚本规则，以及研究问题卡的源侧前向要求。
2. 已完成：核验当前 FT half/entitybce 实际消费的 Dijk 标准化缓存、其入口与 Protocol A 切分逻辑。
3. 已完成：将 Raw83 清单加载器降级为相邻但不同数据产品的接口参照，不作为当前 FT 事实来源。
4. 已完成：判断当前本地证据能否给出严格前向的实测计数；若不能，登记必需的源侧制品与最小诊断接口。

## 状态与阻塞

- 状态：**阻断，当前 FT 缓存尚未证明存在严格时间前向开发切分。**
- 阻塞不是机制或训练失败：当前 FT 消费的 Dijk 缓存仅声明 `T23`，而其构建代码显示它来自 `mTimestampStart`；缓存接口未含 `mTimestampLast` 或完整特征形成时间，无法按流验证截止时间、实体跨界清除和角色可见标签。
- 不编写诊断脚本：在缺少当前 FT 缓存的原始时间列或等价旁车时，脚本不能产出有效的严格前向计数。

## 验收

- 已执行只读代码、清单和收据核查。
- 未执行训练、张量运算、大规模数组扫描或目标年读取。
