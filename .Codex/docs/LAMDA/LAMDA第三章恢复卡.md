# LAMDA 第三章恢复卡

**当前状态：阶段 A、阶段 B、阶段 C 均已完成并完成本地复算；阶段 C 的本次 M/O 具体实现已筛查否决，研究设计仍待重新冻结。** 不得将下述资格筛查读数写成新算法有效性结论。

## 规范材料直链

恢复本卡前必须依次读取以下材料；这些链接与路线规则中的恢复链保持一致，不能用摘要或聊天内容替代：

1. [LAMDA 原始论文 PDF](../../../raw/papers/datasets/2026-Haque-LAMDA-Android-Malware-Concept-Drift.pdf)
2. [ICLR 2026 官方演示版](../../../raw/papers/10011850_e9IemFB.pdf)
3. [LAMDA 结构化全文笔记](../../../wiki/papers/datasets/2026-Haque-LAMDA-Android恶意软件长期漂移基准.md)
4. [LAMDA ICLR 2026 官方演示版笔记](../../../wiki/papers/datasets/2026-Haque-LAMDA-ICLR2026官方演示版.md)
5. [LAMDA 原论文与时间漂移相关工作综述](../../../wiki/papers/malware/LAMDA-2026-原论文与时间漂移相关工作综述.md)
   - 其中的[回放与安全回归方法家族](../../../wiki/papers/malware/LAMDA-2026-原论文与时间漂移相关工作综述.md#回放与安全回归方法家族)已更新为当前入库文献的统一综述入口。
6. [朱焱雷第三章机制构思与创新声明分析](../../../thesis/methods/第三章-朱焱雷第三章机制构思与创新声明分析.md)
7. [LAMDA 路线总控](LAMDA路线总控.md)
8. [阶段 A Replay 改造候选网页报告](<../../../output/LAMDA 阶段 A：针对 Replay 安全保持—适应取舍的改造候选.md>)
9. [阶段 C 决策角色记忆与双侧修复计划](../2026-09-09-LAMDA决策角色记忆双侧修复/task_plan.md)

## 已恢复事实

- 数据、代码和未来协变量边界以 [LAMDA 路线总控](LAMDA路线总控.md) 为准。
- 年份角色依据须单独区分：原论文为 `TRAIN+IID=2013--2014`、`NEAR=2016--2017`、`FAR=2018--2025`，且 `NEAR/FAR` 原论文均仅作测试；本项目把 `NEAR` 作为“项目开发层”只用于机制筛选，把 `FAR` 作为封印评价，这一用途是本项目协议而非原论文原句。
- 已完成的 `screening_only` v3 资格探针位于 `thesis/experiments/llm_probe/runs/diagnostics/lamda-iclr2026-source-near-screening-budget1m-v3/`：完整发布特征视图 IID AP `0.9944039`，2016/2017 AP `0.9373960`/`0.3462362`，2016/2017 FNR `0.2224487`/`0.9184887`，MPS 墙钟 `513.6264` 秒。
- 上述结果只支持存在时间退化余量。发布 `4561` 维特征空间使用未来协变量，故该探针不能作为无泄漏正式基线。

## CPU 哈希与标签时序审计（2026-09-08）

- 审计制品：[JSON 收据](../2026-09-08-LAMDA回放安全回归/hash-label-audit/hash-label-audit.json) 与 [可读报告](../2026-09-08-LAMDA回放安全回归/hash-label-audit/hash-label-audit.md)。
- 审计仅投影 `hash`、`label`、`year_month`，在本机 CPU 上读取 24 个年度 Parquet；未读取 `family`、`vt_count` 或任意 `feat_*`，未写出原始哈希。
- 实测总行数 `1,008,381`，非空哈希唯一值 `1,008,381`；全局重复行、跨年份重复哈希、同年训练/测试重叠和跨年标签冲突均为 `0`。
- 空哈希、非法标签、空 `year_month` 和目录年份不匹配均为 `0`。该结果说明当前发布分片未发现哈希级重复或标签时序冲突；它不改变发布特征空间使用未来协变量的披露边界。
- `year_month` 覆盖不均匀：2013/2014/2016/2017 分别为 5/8/11/11 个月，2018--2024 各为 12 个月，2025 仅 1 个月；跨年结果不得把 2025 解读为完整年度时间分布。
- 口径说明：原论文第三节规定每年目标为 50,000 个恶意与 50,000 个良性样本，但明确允许样本不足的年份，并点名 2025；论文与官方页面给出 2025 总量 `44,663`、恶意 `23`，但没有直接写出“只有 2025-01”。“单月”是本次发布 Parquet 元数据审计所得，不是论文原句。

## 当前运行进度（2026-09-08）

- 阶段 A：`ch3-lamda-domain-il-naive-replay-screening-seed42-v1` 已完成，Naive/Replay 的 2020--2025 预测与年度指标均已回收；[阶段 A 本地复算报告](../2026-09-08-LAMDA回放安全回归/stage-a-qualification-reanalysis.md) 已逐样本重算并与服务器摘要一致。Replay 的 FPR 与旧恶意负向翻转率下降，但 AP/FNR 略差，结论仅为 `screening_only` 资格通过。
- 阶段 B：`ch3-lamda-security-regression-ablation-seed42-v1` 已从同一身份恢复并完成；[阶段 B 描述性复算报告](../2026-09-08-LAMDA回放安全回归/stage-b-analysis/analysis-report.md) 与 [复算 JSON](../2026-09-08-LAMDA回放安全回归/stage-b-security-regression-reanalysis.json) 已核对两臂 12 个年度指标和 34 个预测文件。联合臂 pooled FNR、回溯旧恶意负向翻转率略降，但 FPR、AP、AUROC、F1 未全面占优，仍为 `screening_only`。
- 阶段 B 首次运行的 `inline_container.cc:672`／`basic_ios` 写入异常已通过释放磁盘并从同一身份续训解决；它是资源故障，不是机制否决。最终运行资源收据为 GPU 峰值约 119 MiB、墙钟约 805 秒。
- 阶段 C `ch3-lamda-mo-source-dev-screening-seed42-v1` 已完成来源／开发期筛查；[阶段 C 复算报告](../2026-09-09-LAMDA决策角色记忆双侧修复/analysis/analysis-report.md) 与 [JSON 收据](../2026-09-09-LAMDA决策角色记忆双侧修复/stage-c-screening-reanalysis.json) 已核对四臂 4 个年度指标和 40 个逐样本预测文件。`ER+M`、`ER+O`、`ER+M+O` 均为 `reject_screening`，不进入封印年。
- GPU 两项运行完成后，已将不含凭据的配置、逐年指标、汇总结果、资源收据和制品路径摘要发送到同一网页深度研究会话；网页报告已完成并回收到[本地交接记录](../chatgpt-handoffs/inbox/LAMDA-2026-定向漏判修复-FPR约束深度研究报告.md)。网页分析只作外部意见，已由本地原始制品与冻结合同复核后才进入候选排序。
- 该报告实际使用真正的 ChatGPT “深度研究”模式，任务标识为 `WEB:40805147-727c-4418-ae67-b5a9044ad760`，报告列出 36 个公开来源；其 `actual_effort` 是规划估计，不是已执行工时或可核验的模型档位。
- 文献入库要求：网页返回的任何 Replay 改造候选，必须先取得对应全文，按 `raw/` 原件与 `wiki/` 结构化阅读笔记入库并记录版本、页码/公式和实验条件；完成本地核验前只能标为外部候选，不能进入 LAMDA 机制合同或论文主张。

## 网页与本地实测合并裁决（2026-09-09）

- 网页报告把当前病灶限定为三项取舍：保护旧的正确恶意识别、修复当前仍漏判的恶意样本、限制修复过程新增的良性误报；它明确反对只加强蒸馏、只挑难负例或只提高旧样本权重。
- 网页候选四“双侧风险约束下的修复式 Replay”最有机会形成第三章框架，但目前仍是外部候选；PCT、A-GEM、类别／时间配额、GSS、MIR、PBR、CBRS 等必须作为近邻对照或部件来源，不得改名充当创新。
- 更新后的方法族综述进一步确认：Regression-aware/PCT、MADAR、GSS、MIR、PBR、CBRS、A-GEM、ECBRS/PAPA 和 FreeMOCA 已覆盖安全回归、记忆选择、不平衡和参数保持等成熟路线。当前主候选不再预设“角色记忆＋双风险投影”；先完成缺口修复／条件门控探针，再把 PCT 作为直接基线，必要时比较 MIR/GSS 干扰感知记忆。
- 阶段 A 实测只证明标准 Replay 取得有限筛查资格：FPR 和旧恶意负向翻转率下降，但 AP/FNR 略差。
- 阶段 B 实测不支持当前一侧安全记忆 BCE 全面有效：联合臂 pooled FNR 与回溯翻转率略降，但 FPR 上升、AP/AUROC/F1 略降；2025 的 23 个恶意样本两臂均 FNR=1。
- 阶段 B 代码实际在当前年度训练和评价后用当前模型分数筛选安全记忆，不是配置所写的上一冻结模型保护集合；因此不能把它当作文献 PCT 复现，必须先修正定义。

## 下一阶段候选与数据合同

1. **先做来源／开发期筛选**：按数据清单只用来源年 `2013/2014` 和开发年 `2016/2017` 的已到达训练标签与历史记忆完成机制筛选；封印年 `2018--2025` 只在合同冻结后作一次评价。此前 `2020--2025` 筛查结果保留为探索性证据，不回灌选择。
2. **冻结记忆因子 M**：总容量仍为 200，在 `B_{t-1}` 与 `D_t^{train}` 内按已允许的标签、到达时间、旧模型决策和当前 margin 区分保护／修复／误报控制角色；不按未来家族或目标期表现配置配额。
3. **冻结更新因子 O**：保护集合只取上一冻结模型已正确判恶意的历史记忆；修复集合只取当前已到达训练区中真实恶意且旧模型漏判或 margin 不足的样本；良性约束只用已到达良性训练样本的误报代理。先实现最小双侧约束，不同时加入排序损失、复杂干扰采样或强化学习控制器。
4. **执行最小 2×2**：ER、ER+M、ER+O、ER+M+O，固定模型、总容量、回放暴露量、训练轮数和阈值；加入原式 PCT、同配额随机角色记忆及 A-GEM 等直接对照。若任一单因子在来源期没有独立信号，立即缩小或停止该因子。
5. **统一裁决指标**：逐年 AP、AUROC、F1、FPR、FNR、Brier；旧恶意负向翻转、当前恶意正向修复、良性新增误报、最差年份和资源开销同时报告。通过条件是相对 ER 不牺牲 FPR 与旧恶意保持，并在来源期至少改善 AP/FNR 或正向修复之一；目标期只作封印评价。

以上为待冻结的实施方向，不是已验证算法；在新增候选全文完成 `raw/` 与 `wiki/` 入库、合同和数值依据登记前，不启动新的正式 GPU 重训。

## 运行终态与回收边界（2026-09-09）

- 阶段 A 远端状态为 `completed`，摘要收据判定“经验回放获得资格”。其 2020--2025 pooled `current_year` 暂存读数为：Naive AP/FPR/FNR `0.995858/0.007831/0.023198`，Replay AP/FPR/FNR `0.995579/0.006918/0.025087`；旧恶意负向翻转率分别为 `0.048766` 与 `0.025022`。这只满足预注册筛查门的“FPR 不提高且翻转率严格降低”，同时 AP/FNR 方向不占优。
- 阶段 A 的 Replay 指标、预测和摘要已完整回收并与服务器摘要在 `1e-12` 内一致；检查点仍只按需要回收，不影响当前筛查裁决。
- 阶段 B 的最终状态为 `completed`。联合臂 pooled 差值为：AP `-0.000038475`、AUROC `-0.000077779`、F1 `-0.000009827`、FPR `+0.000210704`、FNR `-0.000415596`、Brier `-0.000018667`。该结果不能支持全面优于或正式主机制结论。
- 阶段 B 配置将安全记忆写成“上一模型正确判为恶意”，但实现是在当前年度训练和评价后用当前模型分数筛选；该实现偏差已记录在复算报告，后续必须先修正并重新冻结候选定义。
- 阶段 A/B 本地与服务器工具哈希均与各自运行清单一致；训练循环每批只有一次 `loss.backward()`，无需因分段读取造成的重复显示重训。
- 阶段 C 的 `M` 实际按四角色固定轮转填充约 50 个槽位，`O` 使用等单位保护／修复／良性控制损失；该具体组合导致开发期 FPR、FNR 或旧恶意负向翻转恶化，已按实验报告否决。否决对象是实现，不是“角色记忆”或“双侧约束”抽象概念的普遍否定。
- 阈值诊断显示，在开发期 FPR 约束下仅移动 ER 阈值只能带来 F1 `+0.000079785` 的边界级变化；阈值不作为下一机制。
- 容量边界：全程总容量 `200` 是阶段 A/B 和阶段 C 的公平控制变量，不是文献证明的最优值；LAMDA 原文“每个 experience 200”与本项目总容量口径不同。容量敏感性应在机制筛选后单独进行。
- 双风险梯度投影 `ch3-lamda-dual-risk-projection-screening-seed42-v1` 已完成：[复算报告](../2026-09-09-LAMDA双风险梯度投影/analysis-report.md)。开发期 FPR `0.013148→0.012232`、旧恶意负向翻转率 `0.052107→0.031984`，但 FNR `0.059478→0.070306`、AP `0.987243→0.985101`；判定为“安全保持有信号、恶意修复不足”，不进入封印年。
- 同一历史评价集合的翻转分解已补充：[开发期历史翻转分解](../2026-09-09-LAMDA双风险梯度投影/transition-flip-diagnostic.md)。它确认投影减少部分负向翻转，但没有形成足够的恶意正向修复。
- 已启用真正的 ChatGPT “深度研究”并完成定向增量任务 `WEB:40805147-727c-4418-ae67-b5a9044ad760`；报告建议优先验证“定向恶意漏判修复＋旧正确决策保护＋独立 FPR 约束”，但其中 PCT、TFCO 和 Android 主动学习仍是外部候选，不能替代本地实验。
- 阶段 C 实施计划已建立：[决策角色记忆与双侧风险修复计划](../2026-09-09-LAMDA决策角色记忆双侧修复/task_plan.md) 与 [证据笔记](../2026-09-09-LAMDA决策角色记忆双侧修复/notes.md)。它把网页报告的 `M/O` 候选、来源／开发期筛选、封印期评价和直接近邻对照写成待冻结合同。
- 缺口感知条件风险门控回放筛选已启动：运行身份为 `ch3-lamda-gap-repair-screening-seed42-v1`，本地 `py_compile`、配置校验和差异检查均通过；远端当前推进到 `experience_replay` 的 2013 年第 `1/10` 轮，GPU 进程已出现。该运行只枚举 `2013/2014/2016/2017`，封印年未读取。

## 当前候选的有向门

先验证固定容量经验回放相对朴素逐年更新的正增益。经验回放本身是已发表基线，不承担第三章创新主张。仅在该门通过后，才依据真实负向翻转制品评估覆盖记忆与安全回归约束更新；二者届时构成有向统一算法，而非预设的两个平行模块或机械 `2×2`。目前所有任务化差量均为实验待证。

## 唯一下一动作

阶段 A 已通过有限资格筛查，阶段 B 已完成但未形成全面增益，阶段 C 的本次 M/O 实现已筛查否决，双风险投影也未修复 FNR/AP。网页深度研究已给出“定向恶意漏判修复＋旧正确决策保护＋独立 FPR 约束”的外部候选排序。当前先执行已批准的缺口修复／条件风险门控源年四臂筛选；在取得 PCT/TFCO 全文并核对实现边界前，不把它们写入正式合同，不进入封印年。没有新候选通过时保留 ER 作为已验证基线，不强行凑两个机制。
