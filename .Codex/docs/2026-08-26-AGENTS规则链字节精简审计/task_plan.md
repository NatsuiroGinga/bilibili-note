# AGENTS 规则链字节精简审计计划

## 目标

在不修改任何现有 `AGENTS.md`、不访问服务器、不改生产代码的前提下，机械计算典型路径的有效规则链字节，审计重复与历史叙述，并给出分级、可验证、低风险的精简方案。

## 文件所有权

- 仅新增本目录内的 `task_plan.md`、`notes.md`、`审计报告.md`。
- 不修改、格式化、暂存或提交工作树中其他文件。
- 若目标文件被其他任务修改或暂存，立即停止并报告所有权冲突。

## 审计范围

- 根规则发现协议及 `32,768` 字节上限语义。
- 提交 `435355fdd81566d4e9b7bdd4f3a5255e4af7924e` 的变更依据与效果。
- 根、`thesis/`、`thesis/experiments/llm_probe/`、其 `scripts/`、`.Codex/docs/`、`.Codex/docs/RWKV/` 六级规则。
- 典型路径：仓库根、第三章 `llm_probe/tools/` 工具、`scripts/remote_launchers/` 脚本、RWKV 文档。

## 阶段

- [x] P0：确认目标文件无未提交快照，记录工作树既有改动边界。
- [ ] P0：完整读取规则文件、提交差异和可用配置来源。
- [ ] P0：机械计算各规则文件及四条典型路径的有效组合字节。
- [ ] P1：登记重复规则、层级归属、按需专项合同候选和历史事故叙述。
- [ ] P1：估算逐项节省字节与语义风险，形成最小改进方案。
- [ ] P0：验证链接、字节、关键词、差异和 Git 所有权。
- [ ] P0：仅提交本任务三份文档。

## 验收命令

- `wc -c`：核对单文件和组合链字节。
- `git show 435355f -- <路径>`：核对最近压缩提交。
- `rg`：核对规则关键词、重复条款、文档内链接和禁止修改边界。
- `git diff --check -- <三份文档>`：核对空白与补丁格式。
- `git diff --name-only <快照提交>..HEAD`：核对提交只含三份审计文档。
- `git status --short -- <三份文档>`：核对目标所有权。

## 决策

- 本轮只提出方案，不实施规则精简。
- 字节统计以 UTF-8 文件实际字节数为准，不用字符数代替。
- 组合链按目标路径从仓库根逐层纳入每层第一份非空规则文件；目录中不存在规则文件的层不计入。

## 阻塞项与错误

- 当前无阻塞项。

## 状态

**正在执行规则与提交证据采集。**
