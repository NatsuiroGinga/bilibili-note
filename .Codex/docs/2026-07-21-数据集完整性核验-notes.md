# 数据集完整性核验证据笔记

## 本机证据

- GeNIS `0-info.zip` 为 3,308 字节，MD5 与官方一致且 CRC 通过；本机 `2-flows.zip` 仅 14,520,320 字节，缺少 ZIP 中央目录，是不可用的截断副本。
- HIKARI 原始数据目录为空。
- DEDALE 没有本机原始归档；只同步正式 v3 审计的 `audit.json`、`input_manifest.json` 和 `console.log`。

## 服务器证据

- GeNIS `2-flows.zip` 为 380,755,720 字节，官方 MD5 一致且 CRC 通过，含 44 个流程 CSV；当前只解压 10 秒窗口的 11 个 CSV，共 1,472,133 条数据行。
- HIKARI `ALLFLOWMETER_HIKARI2021.csv.zip` 为 68,403,081 字节，官方 MD5 一致且 CRC 通过；唯一 CSV 为 555,278 条数据行。
- DEDALE `zeek_orange_dmz.zip` 与 `cicflowmeter_orange_dmz.zip` 均通过官方 MD5 和 CRC；各含 D1 至 D28 的 28 个已标注连接流文件。
- DEDALE Zeek 共 839,054 条、CICFlowMeter 共 1,142,442 条；两端均只有 2 条标签 1，标签 2 为 0。
- 数据盘占用率为 54%，可用空间约 24 GiB，未达到告警阈值。

## 官方来源

- GeNIS Zenodo 记录 14919237：5 个组件，约 2.5 GB；服务器只有完整 `2-flows.zip` 组件。
- HIKARI Zenodo 旧记录 5199540 为 v1.3.5、11.7 GB；当前记录 6463389 为 v1.4.0、27.4 GB。两个版本的 2021 聚合 CSV 组件 MD5 相同。
- DEDALE 2.0 数据页 DOI `10.57745/Y5JLDG`：共 40 个文件；服务器只有完整 `orange_dmz` Zeek 和 CICFlowMeter 两个组件。
- DEDALE 官方下载说明确认 `0/1/2` 为良性、攻击、攻击相关，并建议前两周校准、后两周测试。

## 综合判断

- 三套数据均不是整套官方发布完整落地，但服务器已有当前实验所需的完整流程级组件。
- GeNIS 可继续主实验；HIKARI 可继续流程级次级诊断；DEDALE `orange_dmz` 可继续时间外推与低误报外部验证。
- 当前不需要下载新文件。若后续补充分组或完整攻击时间轴，优先候选为 HIKARI `ground-truth.zip`，以及 DEDALE `README.md`、`userbehaviour_logs.zip`、`system_logs_labels.zip`；均须主代理确认后再下载。
- DEDALE 其他 Zeek 视角均超过 3 GiB，不得自动下载。
