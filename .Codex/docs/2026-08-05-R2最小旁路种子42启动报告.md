# R2 最小旁路种子 42 启动报告

## 当前状态

- 状态：`BLOCKED_PHYSICS_REFIT_FOR_NEW_PROBE_BINDING`。
- 目标组别：仅 `F-A`、`F-P`、`F-S`，种子 `42`。
- 正式物理拟合：第四次全新身份已成功完成。
- `F-A`：已用独立进程启动，尚未进入 GPU 或 SwanLab 初始化。
- 阻塞时间：2026-08-05。
- 用户已在当前对话明确授权把本轮 R2 白名单研究源码、配置和三个 v2 数据制品同步到当前 GPU 服务器并启动正式运行；此前的授权阻塞已经解除。
- 已确认前两次退出码 `255` 的直接根因是 `scripts/guarded_rsync.py` 硬编码旧 `/tmp/gpu-exec.exp` 与 `/tmp/gpu-rsync-push.exp`；主代理使用当前 `/tmp/gpu378-exec.exp` 申请非沙箱网络权限后，远端 `REMOTE_READY` 在 4.8 秒内成功。该失败不得归因于服务器、`127.0.0.1:7890`、SSH 配置或平台链路，先前相关判断已经撤销。
- 核心 TCP 残差与检查点证据链的严重问题 1 项、重要问题 3 项已经完成最小修复，种子 42 正式物理拟合重新放行；四段运行时零坍缩收据任一失败即失败关闭，不等待新的独立复审。
- 首次启动使用的 13 项白名单已由主代理同步并逐项核对，第二次启动前已同步修复后的物理拟合包装器。第三次启动前只需覆盖稳定去重修复后的 `r2_final_physics_fit.py` 并核对新 SHA-256，其他文件不得重复改动。
- 冻结训练视图 `runs/data-prepared/r2-final-sidecar-candidate-v1-server/detection-view-train-fit.parquet` 已恢复到本地规范路径，本地与远端 SHA-256 均为 `98224268bfeff83a710c9739ef8294624e379c094ac68e23337cb58f67692ae9`，大小均为 `486122` 字节；拟合后 `F-A` 的该输入可恢复性阻塞已经解除。
- 首次正式身份为 `r2phys-s42-20260805T055908Z`，启动器目录为 `runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42-20260805T060038Z/`。最终状态为 `failed:main=2:tee=0:log_nonempty=1`；日志共 7 行、526 字节，输出根为空，未进入运行时断言、GPU 训练或 SwanLab 初始化。
- 首次失败根因是包装器使用裸 `uv run`，触发项目依赖同步并从外部获取 `joblib-1.5.3` 元数据；三次请求后响应体超时。该失败不是物理拟合、零坍缩、GPU、模型、数据或 SwanLab 科学失败。
- 最小修复只把 `scripts/run_r2_final_physics_fit.sh` 的入口改为 `uv run --no-sync`。集中一次 `bash -n` 已通过，新 SHA-256 为 `0911531f64ad33f24525b344f11091a8ae6c522324ca9eb4089b19f1c19bf22e`；依赖缺失时由正式入口运行时失败关闭，禁止联网同步。
- 第二次启动器目录为 `runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42-20260805T061336Z/`。最终状态为 `failed:main=1:tee=0:log_nonempty=1`；日志共 22 行、1,690 字节，在数据加载阶段因 UDP 六个状态重复展开同一 `truth_udp_observed`，导致 `pd.concat` 对非唯一列抛出 `InvalidIndexError`。未进入 GPU 训练或 SwanLab 初始化。
- 稳定去重最小修复只覆盖 `src/flow_probe/r2_final_physics_fit.py`，新 SHA-256 为 `c494e29e36efd938a6209b8831ed52409bda8f722033e8fdf74190bad0a8db83`；配置、公式、Parquet 和其余白名单 SHA 不变。本地三份冻结 Parquet 验证为 207,360 行、95 列且列唯一。
- 第三次启动前已用持久化脚本把固定输出根整体原子移动到 `runs/failed-archives/r2-minimal-sidecar-physics-fit/third-prep-20260805T062830Z/`，旧 `zero-collapse-receipt.json` 已保留；固定输出根随后重建为空。
- 第三次启动器目录为 `runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42-20260805T062843Z/`。最终状态为 `failed:main=1:tee=0:log_nonempty=1`；日志共 22 行、1,749 字节。数据构建对 TCP sender 序列的非适用 `truth_udp_observed` 执行严格布尔解析，而跨协议拼接后的该字段为空，触发“只允许原生布尔或精确 0/1”失败关闭。
- 三个失败身份、启动器目录和失败归档均保留，不得删除、覆盖或恢复。第三次输出根当前仅有新的 `zero-collapse-receipt.json`，未进入 GPU 训练或 SwanLab 初始化。
- 跨协议掩码最小修复已经放行：`_build_split` 只严格解析共享专家与当前路由适用专家，对侧协议专家固定为零状态和全 `false` 掩码；适用协议仍拒绝空值、字符串和非精确 0/1。唯一需覆盖源码为 `src/flow_probe/r2_final_physics_fit.py`，SHA-256 `19797c72e772fb07e3009e7a2a2bdfea0b4d8f38190519622f9033fa69c459ed`。
- 第四次启动前必须先用泛化后的持久化脚本把第三次输出根整体归档为唯一 `fourth-prep-<UTC>`，证明固定输出根为空，再同步新拟合源码并使用第四个全新身份启动。
- 第四次 screen 身份：`r2phys-s42-20260805T064227Z-v4`；启动器目录：`runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42-20260805T064237Z/`，当前 `status=running`。
- GPU 进程 PID 为 `12322`，首次运行采样显存为 `774 MiB`。SwanLab 运行标识为 `er9dsm9y`，在线项目仍为 `mortiswang/malicious-traffic-llm`。
- 第四次拟合在 `114.636` 秒内完成 1,350 步，启动器状态为 `finished`；训练、校准、验证序列数分别为 34,560、8,640、8,640。采样时任务已经结束，GPU 回到 0 MiB，磁盘使用 52%，剩余 49 GiB。
- 四段 TCP 方程、共享守恒和 UDP 零坍缩收据全部通过；`formal_tcp_udp_dynamics_ready=true`，QUIC 继续为 `quic_expert_ready_after_fit=false`。
- 检查点 SHA-256 为 `65dd7edb11f51b6b81b5ff28587d2d24edf4dca5b784cedc602b14e99fa7c835`；不可变绑定清单 SHA-256 为 `daa7ecd9ac78cf37a0a37fbef4548d3af8fb28dca8c6c68df9afaee4108e1451`。
- SwanLab 地址：`https://swanlab.cn/@mortiswang/malicious-traffic-llm/runs/er9dsm9y`。
- 检查点、不可变清单、运行绑定、零坍缩收据、结果和逐步指标六个制品已回收本机规范输出根；本地与远端 SHA-256 逐项一致，服务器不再保存唯一副本。
- `F-A/F-P/F-S` 三配置已一次性回填同一检查点与清单 SHA，保持 `quic_expert_ready=false`。新配置 SHA-256 分别为 `1ac85c476b33e456e48f5df17517df971c06580aac260a43acc80efa69593f85`、`190230a33feec9e48d3940af97768ced29a295e395973840df45da3442e0e0a3`、`d2e6d4207a9fd1fcb3e01beb3e528c6bd90374ad902f73381afe0959a897d2f6`；当前等待主代理同步核验并只启动 `F-A`。
- 三配置事务提升身份为 `seed42-20260805T070711Z-14237`，收据状态为 `finished`；提升后远端三份正式配置 SHA 与本机逐项一致。
- `F-A` screen 身份为 `r2fa-s42-20260805T070813Z`；启动器目录为 `runs/launchers/r2-final-sidecar/r2_final_distilbert_f-a_seed42-20260805T070839Z/`，首次采样 `status=running`。
- 首次资源采样时，包装器和裸 `uv run` 父进程已运行 52 秒，日志为 0 字节，GPU 显存与利用率均为 0，SwanLab 尚未初始化，因此当前不能给出训练预计完成时间。该症状与首次物理拟合的依赖同步等待相同，继续保留运行并读取最终日志；不得停止、修改已绑定包装器或启动第二个 `F-A`。
- 主代理在本地核验 `UV_NO_SYNC=1` 对应 `uv run --no-sync` 后，主动向首次 `F-A` 的 `uv` 进程发送 `SIGTERM`。该身份最终状态为 `failed:main=143:tee=0:log_nonempty=0`；未进入 Python、GPU 或 SwanLab，只算启动适配失败，不计科学失败。
- `run_r2_final_probe.sh` 的字节和 SHA 必须保持检查点绑定值 `c7c62ce8567eb57b80e3ac14e9baba09761a5b835a5c2cbc5c8d9e73447c361d`。下一身份仅通过 `UV_NO_SYNC=1` 环境开关跳过依赖同步，只启动 `F-A`；不得修改包装器或启动 `F-P/F-S`。
- 三配置事务提升已完成，提升身份 `seed42-20260805T070711Z-14237` 的状态为 `finished`，远端三份正式配置 SHA 与本机一致。
- 主代理的首个 `UV_NO_SYNC` 启动器 `r2_final_distilbert_f-a_seed42-20260805T071855Z/` 因缺少 `flow_probe.r2_final_views` 失败关闭，日志 8 行、473 字节，未进入 GPU/SwanLab。
- 本代理随后以 screen `r2fa-s42-20260805T072238Z-nosync` 和启动器 `r2_final_distilbert_f-a_seed42-20260805T072316Z/` 仅启动 `F-A`；该身份在 `_physics_system` 加载检查点前因 `checkpoint_binding` 局部变量遮蔽同名导入函数触发 `UnboundLocalError`，日志 14 行、999 字节，未进入 GPU/SwanLab。
- 直接根因已定位为同一函数先调用导入的 `checkpoint_binding(fit_config)`，后又赋值 `checkpoint_binding = values.get(...)`。不得自动重启；只允许对后者及其局部引用做单一改名修复，不改变检查点、绑定语义、包装器 SHA 或三配置。
- 名称遮蔽目标测试已完成：红灯复现第 902 行 `UnboundLocalError`，绿灯同一节点 `1 passed in 0.89s`。唯一生产文件 `r2_final_distilbert_probe.py` 的新 SHA-256 为 `7da76322aa4918bbdb0f8c918566763fd5f906b5fe9cf25f335955a6edbf36cc`。
- 现有成功物理检查点的不可变绑定明确包含旧 probe SHA `9e1113b63d0a7026e86eee6a28743816e52b5b0ef34594bedc642cf41b645a7c`。直接同步新 probe 会在训练绑定比较处必然失败；禁止改写旧检查点或不可变清单。
- 后续唯一合法顺序为：整体归档当前成功物理输出根并保留其本机副本；同步新 probe SHA；使用完全相同的科学配置重新物理拟合；生成新的检查点与不可变清单；重新原子回填和提升三配置；最后仅启动 `F-A`。上一次成功拟合耗时 114.636 秒，可作为重拟合预计时间。

## 已完成绑定

1. 物理角色清单已从旧聚合池切换到已通过独立复审的 TCP/UDP v2 正式物理池。
2. 种子 42 拟合配置同时绑定公共窗口、TCP 发送者级真值和 UDP 窗口真值，并绑定非自引用载荷 Merkle。
3. 加载器在打开 Parquet 前验证角色清单、三制品路径、三个 SHA-256、载荷 Merkle、允许划分和最终测试不可见声明。
4. TCP 公共窗口与发送者级真值按 `(sequence_id, source_window_index)` 一对多绑定，并按 `sender_index` 展开为独立训练序列，不跨发送者聚合方程。
5. UDP 公共窗口与窗口真值按同一复合主键一对一绑定。
6. QUIC 正式权重保持关闭，`quic_expert_ready=false`；本次正式物理拟合只允许 TCP/UDP，检测阶段 QUIC 继续回退 `UNKNOWN`。
7. TCP 固定残差包含发送者级拥塞窗口、慢启动阈值、在途字节、确认字节、丢失与超时状态；UDP 固定残差使用计划发送、实际发送和应用丢弃的包/字节守恒。
8. 零坍缩收据已扩展到共享、TCP、UDP 三个固定残差，正式入口必须证明非平凡残差为正且预测状态梯度非零。
9. 三份探针配置共享四个公共历史字段、相同 DistilBERT、训练预算、优化器、早停、样本顺序和评价协议；冻结比较集合仅为 `F-A/F-P/F-S`。
10. 三份探针配置当前故意保持 `checkpoint.ready=false`。只有物理拟合完成并生成合法检查点与不可变绑定清单后，才允许一次性回填两项 SHA-256。

## 冻结身份

- 物理角色清单：`configs/r2_final_physics_role_manifest_v1.json`，SHA-256 `3e610be2471ea6106689b642e5bc92bf3ecfa3feab0bbe18c823584c25d0eb70`。
- 种子 42 物理拟合配置：`configs/r2_final_physics_fit_seed42.yaml`，SHA-256 `7fe0613686b57ab56e37301213c246a7742cf25160b48bd126b37355b2475806`。
- `F-A` 配置 SHA-256：`cd8ca475639db31b5750a0642049251e3279933d6e0b8b5f2aab76c70cd71266`。
- `F-P` 配置 SHA-256：`e25d90d7832292c946e3b5462d9f5e87b3a05b761127b1bac46104ecfd55afdb`。
- `F-S` 配置 SHA-256：`6b6f63ef3771b7999c66e99a8b1b7607953c7265518f8c616a645d133d310a2e`。
- 修复后源码 SHA-256：`r2_final_experts.py` 为 `23caf945b990a7f504ffd8b6743b7efca0fcc9419f9a8f91ba6df95cab7b3132`，`r2_final_physics_fit.py` 为 `19797c72e772fb07e3009e7a2a2bdfea0b4d8f38190519622f9033fa69c459ed`，`r2_final_distilbert_probe.py` 为 `9e1113b63d0a7026e86eee6a28743816e52b5b0ef34594bedc642cf41b645a7c`。
- 物理拟合包装器 SHA-256：`0911531f64ad33f24525b344f11091a8ae6c522324ca9eb4089b19f1c19bf22e`。
- 失败输出原子归档脚本：`scripts/archive_r2_minimal_physics_fit_failure.sh`，SHA-256 `2dc7f94dfe55e59d47cc37b90114392c55a735170f9c343f8751da6481dd4dd1`。
- 修复后 TCP 固定残差维数为 4；检查点新增 `next_epoch`、`next_batch`、完整顺序及 SHA 绑定的 `sampler_state`、Python/NumPy/Torch CPU/全部 CUDA 的 `random_state`，并绑定五个实现或包装器 SHA。旧检查点不兼容且必须失败关闭。
- v2 公共窗口：SHA-256 `69fb11b95c493453a764ff56f0cc15b88eccf6e9a6b6e12d610a73644dd029ad`。
- v2 TCP 发送者级真值：SHA-256 `9a4f20b210a3355405f94ec70819c52c19a75ef27ee746c7a018fdda7c30d231`。
- v2 UDP 窗口真值：SHA-256 `dab0efb9929ea29c0c34c55f6376d7006aa7157cf1d94cad20d1042d57b7e9bd`。
- v2 非自引用载荷 Merkle：`cd55f6fb82c03f8b68f00b9e3224e6ff814b9ac7fe7ac7652cf29dd5a43a5a3e`。
- 物理拟合输出根：`runs/r2-minimal-sidecar-physics-fit/tcp-udp-v2-seed-42/`。
- 三组输出根：`runs/r2-minimal-sidecar-probe/distilbert-v1/seed-42/`。
- SwanLab：在线项目 `mortiswang/malicious-traffic-llm`。

## 修复后静态启动包

以下内容只完成本地静态冻结，不表示当前已经放行或执行。修复代理必须先交付新的精确白名单与 SHA-256，主代理才可通过当前 378 固定入口执行。

### 远端目录与运行身份

- 受信项目根：`/root/autodl-tmp/thesis/experiments/llm_probe`。
- 物理拟合输出根：`runs/r2-minimal-sidecar-physics-fit/tcp-udp-v2-seed-42/`；首次正式拟合前不得存在旧的 `checkpoint-binding-manifest.json`。
- 物理拟合启动器目录模式：`runs/launchers/r2-final-physics-fit/r2_final_physics_fit_seed42-<UTC>/`。
- `F-A` 输出目录：`runs/r2-minimal-sidecar-probe/distilbert-v1/seed-42/f-a/`。
- `F-A` 启动器目录模式：`runs/launchers/r2-final-sidecar/r2_final_distilbert_f-a_seed42-<UTC>/`。
- 物理拟合屏幕身份模板：`r2phys-s42-<UTC>`；`F-A` 屏幕身份模板：`r2fa-s42-<UTC>`。屏幕会话只用于进程托管，不作为恢复证据。

### 正式命令模板

物理拟合只允许执行一次正式包装器：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
screen -dmS "r2phys-s42-<UTC>" bash scripts/run_r2_final_physics_fit.sh configs/r2_final_physics_fit_seed42.yaml
```

检查点冻结、三配置回填和远端 SHA-256 全部通过后，`F-A` 才允许执行：

```bash
cd /root/autodl-tmp/thesis/experiments/llm_probe
screen -dmS "r2fa-s42-<UTC>" bash scripts/run_r2_final_probe.sh configs/r2_final_distilbert_f-a_seed42.yaml F-A
```

### SwanLab 身份

- 在线项目：`mortiswang/malicious-traffic-llm`。
- 物理拟合运行名：`r2-min-sidecar-physics-s42`。
- `F-A` 运行名：`r2-final-sidecar-s42-f-a`。
- 物理拟合标签：`r2-minimal`、`physics-fit`、`tcp-udp-v2`、`seed-42`。
- QUIC 状态继续为 `UNKNOWN`，不得增加正式 QUIC 标签或训练权重。

### 同步白名单模板

三份 v2 Parquet 已同步并核对远端 SHA-256。修复代理交付后，剩余模板以其新白名单为准，至少覆盖：

- `src/flow_probe/r2_final_experts.py`
- `src/flow_probe/r2_final_physics_fit.py`
- `src/flow_probe/r2_final_distilbert_probe.py`
- `configs/r2_final_physics_role_manifest_v1.json`
- `configs/r2_final_physics_fit_seed42.yaml`
- `configs/r2_final_distilbert_f-a_seed42.yaml`
- `configs/r2_final_distilbert_f-p_seed42.yaml`
- `configs/r2_final_distilbert_f-s_seed42.yaml`
- `scripts/run_r2_final_physics_fit.sh`
- `scripts/run_r2_final_probe.sh`

不得沿用本报告早期源码与配置 SHA。主代理必须使用修复代理交付的新 SHA-256，逐文件同步、逐文件核对，禁止 `rsync --delete`；修复白名单新增文件时必须显式加入，不得目录级扩张。

### 检查点冻结与三配置回填

1. 物理拟合完成后，核对 `checkpoint-latest.pt`、`checkpoint-binding-manifest.json`、`zero-collapse-receipt.json`、参数容量收据和运行绑定收据全部存在且非空。
2. 计算检查点与不可变绑定清单的实际 SHA-256；核对清单中的配置语义摘要、拟合配置 SHA、物理输入 Merkle、`seed=42`、`mode=formal` 与检查点内部 `checkpoint_binding` 完全一致。
3. 立即回收检查点、绑定清单、状态、日志、资源收据和 SwanLab 本地日志，避免服务器保存唯一副本。
4. 在本机一次性把 `F-A/F-P/F-S` 三份配置的 `checkpoint.ready` 设为 `true`，清空 `not_ready_reason`，并回填同一检查点 SHA 与同一绑定清单 SHA；拟合配置路径、拟合配置 SHA、检查点路径和清单路径保持逐字一致。
5. 三份配置全部写完后再计算各自新 SHA-256。远端先同步三份暂存文件；只有三份暂存 SHA 全部匹配，才替换正式配置。任一失败时不得启动任何 probe。
6. 用正式入口再次核对三份正式配置 SHA 与本机一致，然后仅启动 `F-A` 独立进程。`F-P/F-S` 保持未启动。

### 启动后首个检查点

- 物理拟合启动后立即记录屏幕身份、包装器启动器目录、`launcher.log`、`status.txt` 和 SwanLab 地址。
- 启动后 2 分钟采样 GPU、显存、CPU、内存、磁盘写入、活动子进程数和实际吞吐，据此给出拟合预计完成时间。
- QUIC 继续记为 `UNKNOWN`；不得等待或读取 QUIC 扩充矩阵来决定本次 TCP/UDP 最小旁路启动。

## 已变更文件

- `thesis/experiments/llm_probe/src/flow_probe/r2_final_experts.py`
- `thesis/experiments/llm_probe/src/flow_probe/r2_final_physics_fit.py`
- `thesis/experiments/llm_probe/src/flow_probe/r2_final_distilbert_probe.py`
- `thesis/experiments/llm_probe/configs/r2_final_physics_role_manifest_v1.json`
- `thesis/experiments/llm_probe/configs/r2_final_physics_fit_seed42.yaml`
- `thesis/experiments/llm_probe/configs/r2_final_distilbert_f-a_seed42.yaml`
- `thesis/experiments/llm_probe/configs/r2_final_distilbert_f-p_seed42.yaml`
- `thesis/experiments/llm_probe/configs/r2_final_distilbert_f-s_seed42.yaml`

## 明确未变更

- 未修改 `F-H`、`F-R`、种子 43 或种子 44 配置。
- 未修改 QUIC 修复文件、QUIC 数据或 QUIC 正式就绪状态。
- 未读取任何最终测试样本、标签、元数据、路径内容或派生统计量。
- 未运行独立 `pytest`、独立预检、GPU 冒烟、格式化、Pyright 或 Git 差异。

## 验证与边界

- 已重新计算本地变更文件和 v2 三制品 SHA-256。
- 只读绑定审计见 `2026-08-05-R2-v2绑定接口只读审计.md`；其确认当前唯一合法顺序为“物理拟合 → 冻结检查点与绑定清单 → 回填三组配置 → 三个独立进程运行”。
- 未执行 Python 独立导入或配置预检；依照任务要求，代码、配置、哈希、设备、SwanLab、最终测试不可见和运行目录断言保留在正式包装器启动阶段。
- 主代理已完成首次 13 项白名单同步与 SHA-256 核对，并启动一次正式物理拟合；该运行在 Python 入口前因裸 `uv run` 依赖同步超时失败，失败目录、状态和非空日志均保留。
- 尚未执行检查点冻结、配置回填或 `F-A` 启动。

## 待执行白名单

连接恢复后，仅同步以下已经获得显式授权的正式运行必需文件：

- 三个 Python 入口/加载器文件。
- 物理角色清单、种子 42 物理拟合配置和三份探针配置。
- 两个既有正式 Shell 包装器。
- v2 公共窗口、TCP 发送者级真值和 UDP 窗口真值三个数据制品。

不得同步 `F-H/F-R`、种子 43/44、最终测试、QUIC 修复中的并发文件或无关运行制品。
