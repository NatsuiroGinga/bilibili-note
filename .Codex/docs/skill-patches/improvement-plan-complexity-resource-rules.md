# 技能改进计划：复杂度与资源约束规则

## 优先级摘要

- **高优先级**：2 项
- **中优先级**：1 项
- **低优先级**：0 项

## 高优先级改进

### 1. 在日常编码检查清单中加入资源预算

**文件**：`/Users/bilibili/.codex/skills/daily-coding/SKILL.md`

**维度**：内容完整性

**影响**：防止大规模物化把输入规模、状态增长和峰值资源当作实现细节。

**建议**：实现前记录时间、空间与 I/O 复杂度，活跃状态基数，峰值内存和磁盘估算；明确分批读取不等于有界内存；为流式队列设置固定容量与背压；完成后记录真实吞吐和峰值资源。

### 2. 为 Rust 流式与生产规模处理建立独立规则

**文件**：`/Users/bilibili/.codex/skills/rust-skills/SKILL.md` 与 `rules/`

**维度**：性能与资源安全

**影响**：将状态基数、磁盘溢写和端到端时间/空间/I-O 预算作为代码审查条件，而非仅依赖性能剖析。

**建议**：新增两条性能模式规则，更新规则总数、类别计数、快速引用和任务映射。

## 中优先级改进

### 1. 为实验工程加入百万级物化门禁

**文件**：`thesis/experiments/llm_probe/AGENTS.md`

**维度**：运行前置条件

**影响**：在启动百万级数据物化前阻止未证明中间状态上界的实现。

**建议**：明确固定上界证明、计算口径和不能以分批读取替代证明的要求。

## 执行顺序

1. 先更新高优先级技能规则。
2. 再写入实验工程硬门禁。
3. 通过内容匹配、规则文件计数和内部链接核验变更。

## 约束

- 不修改 LSPR24 生产源码。
- 不运行测试或格式化命令。
- 仓库外技能目录不可写时生成可直接应用的补丁，不宣称已修改。
