# 技能更新报告：长时任务可观测日志

## 结论

未直接修改用户级技能。已生成可应用补丁，目标为 `/Users/bilibili/.codex/skills/daily-coding/SKILL.md`；项目级强制规则 `/Users/bilibili/personal/note/thesis/experiments/llm_probe/AGENTS.md` 已直接更新。

## 变更内容

- 新增长时任务的生产日志要求，覆盖预计超过 1 分钟的数据处理、构建、训练、评估、下载和远程启动器。
- 将环境准备、依赖解析、编译、数据物化、训练、评估分成独立阶段，避免将依赖解析或编译误判为实验运行。
- 规定可配置且限频的业务进度心跳、无进度阻塞日志、可恢复的错误诊断、控制台与持久化目录双写和失败保留。
- 明确禁止记录密码、令牌、连接地址解析值、原始敏感载荷及标签泄漏字段。
- 增加交付前的最小日志入口验证要求。
- 将上述规则写入项目级长时任务约束，适用于构建、物化、训练、评估、下载和远程启动器。

## 兼容性

补丁仅修改 `### During Coding` 区段，并在 `### After Completion` 中插入独立验收项。它不修改现有复杂度与资源补丁在 `### Before Starting` 和完成阶段新增的任何条目，可按任意顺序与 `rust-skills-complexity-resource.patch` 合并。

## 验证情况

- 已读取 `skill-improver`、当前 `daily-coding` 技能、仓库根规则和过程文档规则。
- 已确认目标技能及其备份目录均不可写，因此遵循回退规则生成补丁，没有声称已写入用户级技能。
- 已通过内容检查确认补丁包含九项要求：阶段状态、阶段分离、进度心跳、阻塞日志、错误诊断、双写与失败保留、敏感信息限制、频率配置、最小入口验证。
- 已确认项目规则位于 `SwanLab、日志与结果写回（强制）` 节，包含阶段分离、进度、阻塞、错误、双写、保密、限频和入口验证。
- 按任务约束，未运行格式化、完整测试或 Git 命令，也未修改实验源码、配置、数据或运行制品。

## 应用方式

在具备 `/Users/bilibili/.codex/skills/` 写权限后，先创建技能备份，再运行：

```bash
patch -p0 < /Users/bilibili/personal/note/.Codex/docs/skill-patches/daily-coding-observability.patch
```

随后使用一次真实运行或最小命令入口，核验关键日志存在。

## 遗留风险

该补丁尚未应用到用户级技能；在取得该目录写权限并完成最小入口验证前，规则不会自动生效。
