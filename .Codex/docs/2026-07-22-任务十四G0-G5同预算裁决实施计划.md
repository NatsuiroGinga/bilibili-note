# 任务十四 G0–G5 同预算裁决实施计划

> **代理执行要求：**使用子代理驱动开发逐项执行；禁止使用测试驱动开发。测试只在实现完成后运行最小目标集合，独立审查与正式实验并行，不阻塞已经通过行为门禁的运行。

**目标：**完成 G0–G5 单随机种子、202 次有效更新的同协议比较，确定第三章最终协调机制的候选范围。

**架构：**在现有 `physics_train.py` 的数据、模型、状态头、评价和制品协议上增加独立协调数学模块与训练入口。共享 LoRA 梯度由协调器组合，状态头私有梯度单独更新；G5 在独立数学门禁后增量接入。

**技术栈：**Python、PyTorch、PEFT/QLoRA、uv、SwanLab、RTX 5090。

## 全局约束

- 禁止调用测试驱动开发技能。
- 不修改物理残差、数据划分、状态掩码或 Qwen3-1.7B 主基座。
- 服务端禁止执行裸 `uv sync`；入口刷新只使用 `uv pip install --no-deps -e .`。
- 本机与服务端只使用白名单 `rsync`，禁止 `--delete`。
- 先通过相关最小测试和 2 步冒烟，再启动正式训练；独立审查可与正式训练并行。
- 每次正式运行必须在线记录 SwanLab，并保存逐步 JSONL 和外部日志。

---

### 任务 1：G0/G1 协议复用审计

**文件：**

- 读取：`.Codex/docs/2026-07-21-任务十一边界锚定稀疏监督实验记录.md`
- 读取：`thesis/experiments/llm_probe/runs/physics-sparse/`
- 新建报告：`.Codex/docs/sdd/task-14-g01-contract-report.md`

**动作：**

- [ ] 核对 S1/S2 的随机种子、202 步预算、输入 SHA-256、状态掩码 SHA-256、物理样本顺序、学习率、批大小和评价函数版本。
- [ ] 若完全一致，将 S1/S2 分别登记为 G0/G1；若不一致，列出差异并生成重跑命令。
- [ ] 报告服务器与本地制品状态，不下载模型、不启动训练。

**验收：**报告必须给出“可复用”或“必须重跑”的单一结论及逐字段证据。

### 任务 2：G2–G4 协调数学与统一训练入口

**文件：**

- 新建：`thesis/experiments/llm_probe/src/flow_probe/game_coordination.py`
- 新建：`thesis/experiments/llm_probe/src/flow_probe/game_train.py`
- 修改：`thesis/experiments/llm_probe/pyproject.toml`
- 新建：`thesis/experiments/llm_probe/tests/test_game_coordination.py`
- 新建：`thesis/experiments/llm_probe/tests/test_game_train.py`
- 新建报告：`.Codex/docs/sdd/task-14-g234-report.md`

**接口：**

- `solve_symmetric_bargaining(gram, resolution) -> CoordinationSolution`
- `solve_asymmetric_bargaining(gram, powers, resolution) -> CoordinationSolution`
- `solve_reliability_protected(gram, reliability, generation_floor, resolution) -> CoordinationSolution`
- `train_game_variant(probe, settings, tracking, raw_config) -> dict[str, object]`
- 命令入口：`flow-probe-train-game`

**动作：**

- [ ] 复用任务十三的 Gram 校验、单纯形和边际定义，不复制第二份求解逻辑。
- [ ] 实现 G2、G3、G4 及确定性并列解排序、零梯度拒绝和纯生成退化。
- [ ] 复用现有数据加载、样本计划、状态头、物理残差、最终评价和制品协议。
- [ ] LoRA 使用协调方向；状态头只用已接受辅助目标的私有梯度。
- [ ] 逐步记录三目标损失、范数、余弦、权重、边际、可靠性、否决/拒绝、吞吐和峰值显存。
- [ ] 实现完成后仅运行新协调器、新训练入口以及直接受影响复用接口的最小目标测试。
- [ ] 只对本任务修改的 Python 文件执行一次 Black 和 Ruff；Ruff 无语法或结构问题后不重跑。

**验收：**数学解满足单纯形和约束；训练入口完成真实反向与优化器更新；制品清单和 SwanLab 指标键完整。

### 任务 3：服务器门禁、冒烟与 G2–G4 正式运行

**文件：**

- 新建报告：`.Codex/docs/sdd/task-14-g234-runs-report.md`
- 制品目录：`thesis/experiments/llm_probe/runs/game-coordination/`

**动作：**

- [ ] 白名单 rsync 同步任务 2 文件，核对 SHA-256，刷新可编辑入口。
- [ ] 服务器只运行任务 2 指定的最小目标测试。
- [ ] 对 G2 和 G4 分别执行 2 步在线冒烟，核对优化器更新、逐步日志、SwanLab 点和制品。
- [ ] 冒烟通过后按两个进程上限启动 G2/G3/G4 的 202 步正式运行。
- [ ] 独立审查与正式运行并行；只有发现影响结果的严重问题才停止运行。
- [ ] 正式结束后回收制品与外部日志，并逐项比对 SHA-256。

**验收：**三组均为 `finished`、各 202 行逐步指标、SwanLab 云端指标完整且无残留进程。

### 任务 4：G5 数学门禁与实现

**文件：**

- 新建：`.Codex/docs/2026-07-22-任务十四G5主从响应数学门禁.md`
- 修改：`thesis/experiments/llm_probe/src/flow_probe/game_coordination.py`
- 修改：`thesis/experiments/llm_probe/src/flow_probe/game_train.py`
- 修改直接覆盖 G5 的最小测试文件
- 新建报告：`.Codex/docs/sdd/task-14-g5-report.md`

**动作：**

- [ ] 先完成主导变量、跟随变量、效用、响应映射和全导数推导。
- [ ] 用固定小矩阵数值验证反应项非零，且停止反应梯度后退化为明确基线。
- [ ] 只实现通过数学门禁的一种响应近似，不并列实现多种未经裁决的双层算法。
- [ ] 接入与 G2–G4 相同的训练、评价、SwanLab 和制品契约。
- [ ] 实现后运行直接覆盖 G5 的最小测试；格式化与 Ruff 各一次。

**验收：**G5 具有独立协调变量和可追踪的非零反应项，停止反应梯度消融可运行。

### 任务 5：G5 运行与 G0–G5 裁决

**文件：**

- 新建报告：`.Codex/docs/2026-07-22-任务十四G0-G5同预算裁决报告.md`
- 更新：`output/第一创新点实验总控.md`
- 按裁决更新：`thesis/03-基于队列物理约束与有界条件注意力的恶意流量检测方法.md`

**动作：**

- [ ] G5 执行 2 步在线冒烟和 202 步正式运行。
- [ ] 汇总 G0–G5 的生成验证损失、全/观测/未观测状态误差、物理残差、训练时间、峰值显存、否决率、辅助参与率和反应项。
- [ ] 按 5%/10%/10% 门槛裁决候选，不按单一指标挑选结果。
- [ ] 对通过候选运行最小组件消融；未通过候选不进入三随机种子扩展。
- [ ] 更新总控与第三章候选状态，未通过前不命名最终算法。

**验收：**形成唯一裁决结论、完整制品路径和下一阶段是否启动的明确决定。
