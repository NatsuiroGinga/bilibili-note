# 新 GPU 服务器模型与检查点只读盘点

## 范围与结论

- 时间：2026-08-05。
- 操作：仅通过既有远程凭据执行目录、文件、硬件、磁盘和 Python 导入的只读核验；未下载、未同步、未启动训练，未修改远端文件。
- 新服务器已具备本轮 R2 最小旁路所需的完整 DistilBERT 基座、完整 Qwen3-1.7B 基座和可用 GPU Python 环境。
- 当前不存在 `r2-final-physics-fit` 训练检查点。因此，旧 Qwen LoRA 或旧 DistilBERT 分类检查点不能替代本轮 TCP/UDP 物理专家；必须先由当前启动包产生新的、与 v2 物理池绑定的检查点。

## 服务器能力

| 项目 | 已核验事实 |
| --- | --- |
| 主机 | 新实例主机名 `autodl-container-ed7c49b1f2-14f609d6` |
| GPU | NVIDIA GeForce RTX 5090，32607 MiB，驱动 580.76.05 |
| 数据盘 | `/root/autodl-tmp` 总计 100G，已用 50G，可用 51G（50%） |
| 工具 | `rg=/usr/bin/rg`、`uv=/root/.local/bin/uv`、`fdfind=/usr/bin/fdfind` |
| Python | `/root/autodl-tmp/thesis/experiments/llm_probe/.venv/bin/python`，3.10.12 |
| 训练依赖 | `torch=2.13.0+cu130` 且 CUDA 可用，`transformers=4.57.6`，`peft=0.19.1`，`swanlab=0.9.0`，均已成功导入 |

## 可复用基座

### 本轮冻结候选基座

- 绝对路径：`/root/autodl-tmp/thesis/models/distilbert-base-multilingual-cased`。
- 大小：520M；清单记录 544,761,453 字节、7 个制品。
- 类型：`distilbert`；清单中的 CPU 加载验证为 `DistilBertTokenizerFast` 与 `DistilBertForSequenceClassification`，输出形状 `[1, 2]`。
- 关键文件完整：`model.safetensors`（517M）、`config.json`、`tokenizer.json`、`tokenizer_config.json`、`vocab.txt`、`README.md`。
- 固定来源：`distilbert/distilbert-base-multilingual-cased`，修订 `45c032ab32cc946ad88a166f7cb282f58c753c2e`；镜像校验清单为 `/root/autodl-tmp/thesis/models/distilbert-base-multilingual-cased.manifest.json`，其校验文件为 `/root/autodl-tmp/thesis/models/distilbert-base-multilingual-cased.checksums.sha256`。
- 结论：本轮 `F-A`、`F-P`、`F-S` 可直接使用此路径，无需下载或缓存恢复。

### Qwen 升级基座

- 绝对路径：`/root/autodl-tmp/thesis/models/Qwen3-1.7B`。
- 大小：3.8G。
- 类型：`Qwen3ForCausalLM`，`model_type=qwen3`，`torch_dtype=bfloat16`，模型配置记录的 Transformers 版本为 4.51.0。
- 关键文件完整：`model-00001-of-00002.safetensors`（3.3G）、`model-00002-of-00002.safetensors`（594M）、`model.safetensors.index.json`、`config.json`、`generation_config.json`、`tokenizer.json`、`tokenizer_config.json`、`vocab.json`、`merges.txt`。
- 结论：这是候选得到允许升级后 Qwen3-1.7B 正式实验的可复用完整基座；不应作为当前冻结的 DistilBERT 最小旁路基座。

## 缓存与历史制品

- Hugging Face 缓存根 `/root/.cache/huggingface` 仅见令牌与 xet 元数据，未发现 Qwen 模型缓存；ModelScope 缓存根 `/root/.cache/modelscope` 不存在。
- 上述缓存缺失不是阻塞，因为 Qwen 与 DistilBERT 均已有项目内完整本地镜像。
- 历史 Qwen LoRA：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/pilot-qwen3-1.7b-seed42/final_adapter` 与 `/root/autodl-tmp/thesis/experiments/llm_probe/runs/baselines/shared-b0-qwen-seed42-200/final_adapter`，均有 `adapter_model.safetensors`、`adapter_config.json` 与 tokenizer 文件；另有对应可恢复 checkpoint。
- 历史 Qwen 物理训练检查点位于 `runs/baselines/shared-b0-qwen-standard-pinn-seed42-200-v1/checkpoints/` 和 `runs/baselines/shared-b0-qwen-state-supervision-seed42-200-v1/checkpoints/`。这些是旧协议制品，不得替代 R2 v2 物理专家。
- 历史 DistilBERT 选择制品：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/baselines/shared-b0-distilbert-seed42-theory-selection-review-pending-v1/model-selection/epoch-003-step-00000939`，包含 `model.safetensors`（517M）、`config.json`、tokenizer 与 `selection_manifest.json`；整个运行根约 5.6G，也有 `checkpoint-900`、`checkpoint-920`、`checkpoint-939`。
- R2 专用检查：`runs/r2-final-physics-fit/` 与 `runs/r2-final-sidecar-probe/` 尚未出现；当前仅存在 R2 检测开发视图根 `runs/data-prepared/r2-final-sidecar-candidate-v1-server/`。因此物理专家检查点尚未产出。

## 正式入口与最短路径

- 物理专家正式包装器：`bash scripts/run_r2_final_physics_fit.sh <物理预训练配置>`，调用 `python -m flow_probe.r2_final_physics_fit`，并由运行时断言生成检查点与绑定清单。
- 单组旁路正式包装器：`bash scripts/run_r2_final_probe.sh <单组配置> <F-A|F-H|F-P|F-S|F-R>`，调用 `python -m flow_probe.r2_final_distilbert_probe`。
- 当前最短启动路径：
  1. 将 TCP/UDP v2 正式物理池的路径、模式、清单哈希和载荷 Merkle 绑定到当前物理拟合配置。
  2. 使用 `run_r2_final_physics_fit.sh` 产生新检查点及不可变绑定清单。
  3. 生成种子 42 的 `F-A`、`F-P`、`F-S` 独立配置和收据。
  4. 分别以 `run_r2_final_probe.sh` 启动三组，且不读取最终测试。

## 阻塞与风险

- 唯一直接阻塞：R2 v2 物理专家检查点和其绑定清单尚未生成。现有旧检查点与本轮冻结数据、角色和物理池不匹配。
- 磁盘可用 51G，当前满足启动要求；创建多个检查点前应继续监控剩余空间，遵守可用空间低于 10GiB 的告警门槛。
- 本报告未执行实际模型加载、下载、同步或训练；依赖导入成功只证明环境可启动，正式有效性仍以包装器的运行时断言为准。
