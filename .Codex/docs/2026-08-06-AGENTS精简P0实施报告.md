# AGENTS 精简 P0 实施报告

- **实施日期**：2026-08-06
- **实施合同**：`.Codex/docs/2026-08-06-AGENTS精简P0实施前审查.md`
- **合同裁决**：`GO_WITH_FIXES`
- **当前结论**：Hook 修复和两份仓库规则精简已完成定向验证；用户级 `AGENTS.md` 因缺少用户对该精确覆盖的明确授权而保持原样，P0 尚未全部完成。

## 1. 基线与备份

实施前保存了 9 个目标文件到 `.Codex/docs/2026-08-06-AGENTS精简P0备份/`，并用 `cmp -s` 完成 9/9 逐字节一致核验。路径与 SHA-256 见 `.Codex/docs/2026-08-06-AGENTS精简P0备份清单.md`。

三份规则实施基线：

| 文件 | 实施前字节 | 当前字节 | 状态 |
|---|---:|---:|---|
| `/Users/bilibili/.codex/AGENTS.md` | 12,302 | 12,302 | 原文件未变；2,314 字节候选待明确授权 |
| `AGENTS.md` | 10,585 | 5,490 | 已精简 |
| `thesis/experiments/llm_probe/AGENTS.md` | 5,722 | 4,382 | 已精简 |

仓库根与 `llm_probe` 的既有并行改动均以实施前工作副本为基线，没有从 `HEAD` 重建。RWKV 总控的唯一服务器事实由其所有者写入，本任务只读核验，没有修改该总控。

## 2. Hook 红绿灯

按测试驱动开发先新增定向失败样例，再修改生产 Hook。

### 红灯

| 命令 | 结果 | 预期失败原因 |
|---|---|---|
| `python3 .codex/hooks/tests/test_run_node_hook.py` | 退出 1，1/5 失败 | 启动器拒绝 `security-guard.js`，返回 64 |
| `python3 .codex/hooks/tests/test_security_guard.py` | 退出 1，4/4 失败 | matcher 过宽；危险分支旧退出协议；确认分支含 `continue/systemMessage`；中性分支输出 `{"continue":true}` |

### 最小修复

- 两个 Node 启动器白名单仅新增 `security-guard.js`。
- 用户级 `PreToolUse` matcher 从 `Bash|Write|Edit` 收窄到 `Bash`，命令改为经用户级启动器执行安全守卫。
- 安全守卫中性 Bash 退出 0 且无输出；拒绝和确认分别输出 `permissionDecision=deny`、`permissionDecision=ask`，只使用 `hookSpecificOutput` 当前字段。
- 从活动用户级 `hooks.json` 删除整个 `UserPromptSubmit` 组；历史 `skill-forced-eval.js` 脚本保持逐字节不变。
- 未扩展 `apply_patch` 解析，未接入项目策略守卫，未修改信任哈希或 Hook 状态。

### 绿灯

| 命令 | 结果 |
|---|---|
| `python3 .codex/hooks/tests/test_run_node_hook.py` | 退出 0，5/5 通过 |
| `python3 .codex/hooks/tests/test_security_guard.py` | 退出 0，4/4 通过 |

## 3. 静态与格式化检查

以下检查均退出 0：

- `jq empty /Users/bilibili/.codex/hooks.json`
- `node --check /Users/bilibili/.codex/hooks/security-guard.js`
- `node --check /Users/bilibili/.codex/hooks/skill-forced-eval.js`
- `/bin/sh -n /Users/bilibili/.codex/hooks/run-node-hook.sh`
- `/bin/sh -n .codex/hooks/run_node_hook.sh`
- 用户级与项目级启动器 `cmp -s`
- `thesis/experiments/llm_probe/.venv/bin/black .codex/hooks/tests/test_run_node_hook.py .codex/hooks/tests/test_security_guard.py`，2 个文件无需改写

定向 `rg` 确认用户级活动 Hook 配置中不再出现旧错误路径、`node -e` 或 `UserPromptSubmit`。本轮禁止且未运行 Prettier。

## 4. 规则精简结果

仓库根保留：

- 加载哨兵、32 KiB 上限和最小子域路由
- 仓库使命、证据晋级和目录边界
- RWKV/PINN 唯一选路、恢复入口、事实优先级和路线专属写回
- 凭据禁写及“从所属路线总控读取服务器和凭据键”
- 搜索证据边界、实验设计例外、三个开发技能的字面触发链和最小调试门禁

`llm_probe` 保留：

- 三份按需合同入口及版本匹配的官方证据核验条件
- GPU 任务、时长、显卡与卡数、显存、开机和费用通知
- 服务器复核、凭据防泄漏、数据与最终测试隔离
- 正式包装器运行时断言、白名单同步、SHA-256、本机唯一副本和删除授权
- SwanLab 在线模式、云端核验、本地原始日志及按当前路线写回

两份活动规则均不再写死 `GPU_SSH*`、`GPU_PWD*`、模型、数据路径、样本数或 SwanLab 固定项目名。

## 5. 当前发现链字节

用户级规则尚未获准精简时：

| 发现链 | 当前字节 | 距 32,768 余量 |
|---|---:|---:|
| 仓库根 | 17,792 | 14,976 |
| `thesis` | 22,400 | 10,368 |
| `llm_probe` | 26,782 | 5,986 |
| `scripts` | 27,823 | 4,945 |
| RWKV | 22,287 | 10,481 |

所有链均低于硬上限，但 `llm_probe` 与 `scripts` 尚未达到审查建议的至少 8 KiB 余量。若用户明确授权写入已核对的 2,314 字节用户级候选，预计链将分别降至 7,804、12,412、16,794、17,835、12,299 字节；该候选目前不是活动规则。

## 6. 修改文件

已修改：

- `/Users/bilibili/.codex/hooks.json`
- `/Users/bilibili/.codex/hooks/run-node-hook.sh`
- `/Users/bilibili/.codex/hooks/security-guard.js`
- `AGENTS.md`
- `thesis/experiments/llm_probe/AGENTS.md`
- `.codex/hooks/run_node_hook.sh`
- `.codex/hooks/tests/test_run_node_hook.py`

已新增：

- `.codex/hooks/tests/test_security_guard.py`
- `.Codex/docs/2026-08-06-AGENTS精简P0实施计划.md`
- `.Codex/docs/2026-08-06-AGENTS精简P0备份清单.md`
- `.Codex/docs/2026-08-06-AGENTS精简P0备份/` 下 9 个备份文件
- 本报告

明确未修改：

- `/Users/bilibili/.codex/AGENTS.md`
- `/Users/bilibili/.codex/hooks/skill-forced-eval.js`
- `/Users/bilibili/.codex/config.toml` 与用户级 `.rules`
- 项目 `.codex/hooks.json`、PreCompact、项目策略守卫与服务器启动守卫
- `thesis/AGENTS.md`、`scripts/AGENTS.md`、RWKV 总控、两份 PINN 总控
- 论文正文、实验源码、配置、数据、运行制品、服务器和 Git 历史

## 7. 保留性限制

1. 安全 Hook 定义已变化，当前会话不能证明新的信任与启用状态。必须在全新 Codex 会话通过 `/hooks` 审查、信任并启用，再验证无害命令放行和无副作用危险样例预执行拒绝。
2. `UserPromptSubmit` 删除也只会在新会话重新加载后生效；当前会话可能仍保留启动时载入的旧定义。
3. 因尚未完成新会话信任，简短的凭据防泄漏、用户改动保护和破坏性操作确认文字规则不得删除。
4. 实施前审查记录的 Hook 全套基线仍为 72 项中 71 项通过、1 项失败；唯一失败是旧 PreCompact 测试与已记录行为漂移。本轮未运行全套测试，不得把定向 9/9 通过表述为“全部 Hook 通过”。
5. 用户级规则候选写回被安全审批拒绝，理由是当前用户消息未明确授权该持久、广泛的全局规则变更。没有绕过审批；完成 P0 需要用户对该精确文件覆盖给出明确授权。
