# R2 冻结训练视图回收报告

## 结论

已从当前 378 服务器按单文件白名单恢复冻结训练视图。本地与远端 SHA-256、文件大小一致，服务器不再是该制品的唯一副本。

## 制品

- 本地路径：`thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-candidate-v1-server/detection-view-train-fit.parquet`
- 服务器路径：`/root/autodl-tmp/thesis/experiments/llm_probe/runs/data-prepared/r2-final-sidecar-candidate-v1-server/detection-view-train-fit.parquet`
- SHA-256：`98224268bfeff83a710c9739ef8294624e379c094ac68e23337cb58f67692ae9`
- 文件大小：`486122` 字节

## 核验

1. 同步前在服务器只读核验文件存在、SHA-256 和大小；结果与冻结值一致。
2. 使用 `GPU_SSH_378`、`GPU_PWD_378` 和 `/tmp/gpu378-exec.exp` 进行远端只读核验。
3. 指定的 `/tmp/gpu-rsync-pull-378.exp` 原先不存在，因此在 `/tmp` 创建一次性拉取包装器；未写入凭据。
4. 首次拉取因本机 rsync 不支持 `--protect-args` 而在传输前失败；移除该不兼容选项后，仅重试同一远端源和本地目标。
5. 拉取完成后在本地重新计算 SHA-256 并读取大小；两项均与远端一致。

## 边界

- 未读取 Parquet 数据内容。
- 未修改任何源码、配置、运行制品内容或恢复文档。
- 未运行 `pytest`、格式化、Pyright、Git 差异或最终测试。
- 本次仅解除训练拟合检测视图的本地可恢复性阻塞，不解除 TCP 残差、检查点可恢复性或正式训练暂停门禁。
