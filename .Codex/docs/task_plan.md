# Claude Code 迁移配置检查计划

## 目标

核验本仓库及 Codex 用户级配置中从 Claude Code 迁移而来的内容是否可被正确加载，并识别失效路径、兼容性问题和运行时阻塞项。

## 阶段

- [x] 阶段 1：定位迁移文件并确认工作区状态。
- [x] 阶段 2：校验仓库说明、Claude 设置和 Codex 主配置。
- [x] 阶段 3：运行 Codex 诊断并核对命令路径。
- [ ] 阶段 4：补充版本控制检查，整理检查报告。

## 关键问题

1. `AGENTS.md` 是否完整承接了 `CLAUDE.md` 的项目说明？
2. `.claude/settings.json` 中的命令许可是否仍有效，且是否能作用于 Codex？
3. Codex 主配置、插件、MCP 与网络连接是否可以正常工作？

## 已作决定

- 本次仅做只读检查；不迁移、不删除、不修改运行配置。
- 报告统一保存到 `.Codex/docs/`，以符合仓库文档存放约定。

## 遇到的问题

- 首次创建 `.Codex/docs/` 被受限文件系统拒绝；经授权后已成功创建。

## 当前状态

**阶段 4**：补充 Git 跟踪状态，并生成最终检查报告。
