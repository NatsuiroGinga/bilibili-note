# 本地文献混合检索优化审计计划

## 目标

基于当前工作树实现与官方一手资料，审计本地文献混合检索工具在准确性、增量维护、性能、降级和自动触发方面的改进空间，给出不超过三项立即实施建议，不修改生产代码。

## 边界

- 工作树：`/Users/bilibili/personal/note/.worktrees/ch4-dtep-pbc-20260819`
- 只读对象：根规则、混合检索计划与笔记、技能、README、`scripts/literature_search/` 全部源码、依赖锁与配置、当前状态。
- 唯一写入范围：`.Codex/docs/2026-08-20-本地文献混合检索优化审计/`
- 不重复运行效果评估代理的查询，不修改、回滚、暂存或提交其他文件。
- 外部证据仅采用官方文档、原论文、官方源码或官方模型卡。

## 阶段

- [x] 阶段一：读取适用规则和技能，确认工作树与独占写入范围。
- [x] 阶段二：盘点计划、笔记、README、源码、依赖、配置和当前索引状态。
- [x] 阶段三：核验 SQLite FTS5、E5、NumPy 及近期混合检索与重排的一手资料。
- [ ] 阶段四：综合 P0/P1/P2 建议并写入 `优化建议.md`。
- [ ] 阶段五：检查文档、核验仅独占目录发生本任务变更，并原子提交。

## 核心问题

1. 当前 511 篇规模下，哪些缺陷真实影响召回、排序或维护，哪些只是架构炫技？
2. 陈旧检测、增量索引、Markdown 分块、中文词法、E5 编码、精确余弦和 RRF 的具体边界是什么？
3. 元数据、引用链、查询扩展、交叉编码器重排、设备批处理、并发和在线降级应如何分期？
4. Codex 自动触发、评估和可观测性还缺哪些可机械验证的合同？
5. 面向当前解析器的论文全文笔记应如何写，哪些字段由作者负责，哪些信息必须由索引器派生？
6. 应扩展通用 Zotero/Obsidian 技能，还是新建仓库级可检索论文阅读编排技能？

## 验收

- `优化建议.md` 覆盖任务指定的全部分析维度。
- 每项建议给出证据、预期收益、成本、风险和验证指标。
- 明确当前规模是否需要向量数据库或近似最近邻。
- 最多三项立即实施建议，且不包含代码实现。
- 文档记录代理模型 `gpt-5.6-sol`、`effort=high`、官方来源、提交与阻塞。
- 给出可直接复用的全文笔记模板、反例、检索评估方法和技能路线规格。

## 决策

- 以当前未提交源码为审计快照，不切换或清理工作树。
- 联网范围扩展到近期混合检索、稠密/稀疏融合、增量索引、重排和小规模本地检索增强生成，但保持问题导向，不写泛化综述。

## 错误与阻塞

- 首次执行规范状态命令时，`uv` 无法写入用户级缓存 `/Users/bilibili/.cache/uv`；改设 `UV_CACHE_DIR=/tmp/literature-search-uv-cache` 后，同一 `--locked` 命令成功。
- Hugging Face 模型卡网页体积过大，网页工具无法直接打开；改为核对固定修订已下载到本地缓存的官方 `README.md`、配置文件和官方 E5 原论文。
- OpenAlex 旧文档 URL 已迁移；采用官方 `ourresearch/openalex-help` 当前源文件与 2026 年变更日志交叉核验。

## 状态

**当前处于阶段四**：整理优先级、全文笔记规范和技能路线裁决。
