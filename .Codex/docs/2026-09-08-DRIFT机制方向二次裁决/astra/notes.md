# DRIFT 二次方向裁决证据笔记

<!-- RESEARCH_ROUTE=DRIFT -->

## 范围与初始事实

- 新工作树：`/Users/bilibili/personal/note/.worktrees/ch3-drift-20260908`。
- 当前路线总控：M1-v1/v2、M2/J06、ROSA、静态条件记忆当前形式已止损；不等于相关家族整体无效。
- 下一候选只能依据当前真实病灶，不把 T20–T25 公开论文数字用于选择具体参数。
- 目标无标签适应是待审协议变化，不等于当前冻结静态协议已允许测试时权重更新。

## 检索收据

- 初次 status 的索引构建于 `2026-09-08T04:55:46.676102+00:00`，2121 文档、153024 块；当前增加/修改导致 `stale=true`。
- 初次直接 uv 缓存权限失败，随后只读 status 获准执行；不把错误说成查询超时。

## 已恢复与核验

- DRIFT 原件通过现有 MinerU 全文 `/tmp/drift-aux-course-20260908/drift-original-p1-14.md` 完整重读；结构化笔记、两份综述、朱论文分析及 DRIFT 当前卡均已完整重读。旧工作树已不可达，不继续用跨树比较代替读取。
- 本轮原始结果 SHA256 与报告一致：M1-v1 conservative `ba9dada9c7c51d83d343565a428a7e020fc789dbc0570e277f1a8a2903e37afd`；M1-v2 原始-对偶 `dfec45a4a743fbe419d2e7f5a508ef3d544f51acd92a30407645599493cfaede`；条件记忆 `fb7d95130a1c0432d12e98e678f2699a08e95b90cc93e50e96f99a16aeb10b92`。
- 原始状态确认 M1-v2 为 `rejected_after_t17_short_window`，R02/R07 为 `rejected_after_source_qualification` 且 `supported_candidates=[]`。
- LAMDA v3 原始状态为 completed，但 `published_feature_space_uses_future_covariates=true`、`strict_source_inductive_protocol=false`；有错误余量，不具当前无泄漏正式替换资格。
- T17 三辅助头恢复正在由主代理既有工程链执行。本代理未启动或干预；新恢复模型不自动继承原 P0 的验证身份，保存辅助头也不等于完成伪未见家族验证。

## 二次裁决关键结论

- 主推源侧伪未见 family 反馈的辅助更新选择；备选受限自监督测试时适应；键条件可靠性路由降权独立竞争，暂不组合。
- ForkMerge 明确区分梯度夹角与泛化迁移；PCGrad 与 GradNorm 分别处理方向和量级，不是验证反馈本身。课程需动态调度优于最佳静态任务子集后才能称课程贡献。
- TPP 原文式（4）是对打乱输入恢复原 token 的去噪任务，不能仅凭简称写成预测位置类别；以源码实际语义为准。
- TTA 需要修改“目标只做冻结模型前向”的合同。算法规则冻结可与参数在线更新共存，但不是当前源侧冻结协议。无目标标签仍须隔离良恶文件名、family、路径和标签驱动的采样。
- DRIFT 年份有序不等于年内真实到达流；缺年内可追溯时刻时只可声明受控顺序实验或年度批式适应。
- 源阈值/冻结头/高置信门均不保证任意目标分布 FPR。NP/保形保证有交换性、密度比、条件机制或漂移项等前提。
- T18/T19 已是假设生成面；T17 已见模型/梯度上分组只支持描述。新嵌套 family 折必须从分词器、自监督、监督阶段隔离外层数据并重训，才是算法级折外证据；仍非全新研究者盲测。

## 成功查询与失败披露

- 成功查询：`DRIFT 自监督 测试时适应 MADCAT SoTTA 辅助任务负迁移 ForkMerge 有序域泛化 风险控制 键条件可靠性`，`scope=paper`、`mode=hybrid`、`offline`、`top-k=10`；MADCAT、DRIFT、ForkMerge 在前列，词法和向量排名均返回。
- 查询自动增量构建耗时 `27.623` 秒，索引 SHA256 `d561dd074b081bea623ccf1bed02abaa26adee753419f692a1a9e45e79af5647`、向量数 `153241`；成功入口已通过非陈旧断言。并发文档继续新增后不能承诺索引始终不陈旧。
- 前后部分细分查询返回“自动构建后索引仍不可用（source_added/source_changed），拒绝查询”；这是并发一致性门，不是零命中或向量模型不可用。已改为精确全文路径补核，不擅自绕过保护。

## 最后定点检索与外包

- TTA 定点混合查询成功，自动增量构建 `32.726` 秒，前五命中 MADCAT、CANDI、SoTTA、TTA-AD、RTTAD；词法和向量通道均返回排名。
- 有序域/风险查询等待同一索引锁后成功，前五命中 Tong NP、AIRL、非交换保形、保形风险控制、V-REx。
- Zotero 定点语义查询返回 MADCAT `59GWHNT2`、TESSERACT 等已有近邻；只作查重，未修改条目。
- 获准联网后仅查询两组公开主题：`DGA test-time adaptation` 与 `malware self-supervised test-time adaptation MADCAT`。主要返回已有 MADCAT 及不相关 DGA 缩写；没有新增全文被用于改变排序。MADCAT 官方入口 `https://arxiv.org/abs/2505.18734` 与既有题录一致。
- 用户要求降低 Astra 消耗后立即停止扩展。公开文献发现、反例和排重交普通 ChatGPT：`.Codex/docs/chatgpt-handoffs/drift-ch3-direction-deep-research-outbound.md`；仅公开题录/问题，无实验、私有路径、目标期读数或凭据。
- 出站要求 deep-research、requested_model=auto 高能力、requested_effort=high；Sider Scholar/Scite/GitHub 按缺口使用。本运行时无普通 ChatGPT 浏览器或桌面控制工具，已通知主代理投递，不自称已发送或已执行外部深度研究。

## 交付检查

- 主报告、台账新增活动区、过程计划/笔记/研究问题卡和出站包共 6 份 Markdown：相对链接、围栏、尾随空白检查零错误。
- 台账源文件未跟踪，保全于 `候选台账-修改前快照.txt`；未操作共享暂存区。完整历史逐字一致检查通过，未删除/修改任何历史否决行。
- 目标路径 `git diff --check` 通过，冲突标记搜索无命中。未运行格式化器、实验或新训练。
