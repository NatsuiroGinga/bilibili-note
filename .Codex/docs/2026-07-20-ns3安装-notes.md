# ns-3.48 安装过程记录

## 已知输入

- 本地资料目录：`/Users/bilibili/personal/note/ns3/`
- 源码包：`ns-3.48.tar.bz2`
- 源码包 SHA-256：`5700ceecef2c9bc862502914b2237fe798d4c7ae07652247050e300e49407c4d`
- 服务端目标目录：`/root/autodl-tmp/thesis/ns3/`

## 待记录

- 配置与编译检查点
- 仿真、版本、构建目录和磁盘验收结果

## 远端预检

- 检查时间：`2026-07-20T10:55:13+08:00`
- `screen -ls`：无会话。
- 当前构建日志：仅含 ns-3 包装器拒绝 root 运行的异常，未进入 CMake。
- 源码目录、源码包和当前构建日志均存在。
- 数据盘：总计 50 GiB，已用 23 GiB，可用 28 GiB，使用率 46%。

## 首次恢复尝试

- 已将 root 防护失败日志归档为 `ns-3.48-build.failed-root-guard.log`。
- 设置 `USER=ns3builder` 后，包装器已进入 CMake。
- 配置失败原因：ns-3.48 要求 CMake 3.25 或更高，系统 `/usr/bin/cmake` 为 3.22.1。
- 后台会话失败后正常退出，无遗留编译进程。

## 隔离 CMake 与配置

- 工具环境：`/root/autodl-tmp/thesis/ns3/.tools/ns3-cmake-3.25.2/`
- 工具版本：`uv 0.11.29`、CMake 3.25.2。
- 源码包远端 SHA-256 复核通过。
- 配置参数：`--build-profile=default --disable-examples --disable-tests`。
- 配置已成功，进入 `gmake -j8` 编译。
- 可选能力探测中未找到 Sphinx、栈追踪库和 128 位整数支持；这些结果未阻断核心构建。

## 编译

- 编译命令：`env USER=ns3builder ./ns3 build -j 8`。
- 编译达到 100%，后台会话正常退出。
- 统计到 968 个对象文件和 41 个可执行制品。

## 独立验收

- 验收时间：`2026-07-20T11:13:21+08:00`。
- ns-3 版本：3.48。
- 独立运行 `scratch-simulator` 输出 `Scratch Simulator`，状态通过。
- 最终统计：968 个对象文件、47 个可执行文件、37 个共享库。
- 源码与构建目录占用 1.9 GiB；隔离工具环境占用 65 MiB。
- 数据盘使用率 50%，可用 26 GiB。
- 验收日志：`/root/autodl-tmp/thesis/ns3/ns-3.48-acceptance.log`。
