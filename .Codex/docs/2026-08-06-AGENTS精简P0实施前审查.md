# AGENTS 精简 P0 实施前审查

- **审查日期**：2026-08-06
- **当前路线**：`RESEARCH_ROUTE=RWKV`
- **审查性质**：一次且仅一次的独立实施前审查
- **总体判断**：`GO_WITH_FIXES`

P0 可以启动，但下列“严重”项必须作为同一实施任务的前置门禁或保留性修正。不得先删除文字规则，再假定尚未接入、已禁用或协议不兼容的 Hook 会接管。非阻断项按本报告直接实施或记录，不要求再发起一轮文字复审。

## 1. 审查基线

本轮先检查了当前差异，再审查磁盘上的实际内容。审查开始时，工作树已有 28 个已跟踪文件修改及大量未跟踪研究制品；与 P0 直接相关的 [仓库根规则](/Users/bilibili/personal/note/AGENTS.md:1)、[论文规则](/Users/bilibili/personal/note/thesis/AGENTS.md:1)、[`llm_probe` 规则](/Users/bilibili/personal/note/thesis/experiments/llm_probe/AGENTS.md:1) 和 [脚本规则](/Users/bilibili/personal/note/thesis/experiments/llm_probe/scripts/AGENTS.md:1) 都已有用户或并行任务改动。实施代理必须以当前工作副本为基线，不能从 `HEAD` 重建或回滚这些文件。

已核对：

- [官方调研](/Users/bilibili/personal/note/.Codex/docs/2026-08-06-Codex-AGENTS精简官方调研.md:1)
- [用户级规则](/Users/bilibili/.codex/AGENTS.md:1)、仓库根规则、`thesis`、`llm_probe`、`scripts`、`.Codex/docs` 与 RWKV 局部规则
- [用户级 Hook 配置](/Users/bilibili/.codex/hooks.json:1)、未被当前状态引用的嵌套配置、[项目 Hook 配置](/Users/bilibili/personal/note/.codex/hooks.json:1) 及引用脚本
- 用户级 `.rules`、项目 `.ignore`、Hook 信任状态、项目 Hook 测试及 RWKV 总控
- OpenAI 当前官方文档：[AGENTS.md 发现链](https://learn.chatgpt.com/docs/agent-configuration/agents-md#how-codex-discovers-guidance)、[PreToolUse](https://learn.chatgpt.com/docs/hooks#pretooluse)、[UserPromptSubmit](https://learn.chatgpt.com/docs/hooks#userpromptsubmit)、[通用 Hook 输出](https://learn.chatgpt.com/docs/hooks#common-output-fields)

官方手册下载辅助程序因本机 DNS 解析失败而未取得正文；随后按技能规定改用 OpenAI 官方文档服务核验上述字段和行为。

## 2. 严重问题

### S1：安全 Hook 当前不能接管任何拟删除的安全规则

证据：

1. [用户级 Hook 配置第 9 行](/Users/bilibili/.codex/hooks.json:9) 把 home 与一个绝对路径传给 `path.join`。实测结果为不存在的 `/Users/bilibili/Users/bilibili/.codex/hooks/security-guard.js`。
2. [`~/.codex/config.toml` 第 264—266 行](/Users/bilibili/.codex/config.toml:264) 明确把该 `PreToolUse` 条目标为 `enabled = false`。
3. [安全脚本第 87—88 行](/Users/bilibili/.codex/hooks/security-guard.js:87) 只识别旧工具名 `Write`、`Edit` 及 `file_path`。当前官方协议对文件编辑报告 `tool_name = apply_patch`，内容位于 `tool_input.command`；实测危险 `apply_patch` 样例被直接放行。
4. [安全脚本第 161 行](/Users/bilibili/.codex/hooks/security-guard.js:161) 对普通命令输出 `{"continue":true}`。当前 `PreToolUse` 不支持 `continue`，Codex 会把该次 Hook 标为失败并继续工具调用。
5. 用户级 `.rules` 目前全是 `allow`，没有 `prompt` 或 `forbidden`，不能补上该缺口。

必须修正：

- 先修复并测试安全 Hook，再在新会话通过 `/hooks` 审查、信任并启用；禁止直接手改 `trusted_hash`。
- 最小可靠方案是让用户级启动器允许 `security-guard.js`，配置通过该启动器调用，并把 matcher 暂时收窄到实际支持的 `Bash`。
- 普通放行必须退出码为 0 且无输出；危险 Bash 必须在执行前稳定拒绝。确认型分支只能输出当前协议支持的字段，不能返回 `continue`。
- 即使上述门禁通过，也必须保留简短的“用户改动保护”和“破坏性操作先确认”文字规则；当前 Hook 不覆盖 `apply_patch`，而且 Hook 本身不是完整安全边界。

### S2：服务器变量有直接冲突，且易变状态写在长期规则中

[根规则第 46 行](/Users/bilibili/personal/note/AGENTS.md:46) 指定当前旧服务器使用 `GPU_SSH`、`GPU_PWD`；[`llm_probe` 第 18 行](/Users/bilibili/personal/note/thesis/experiments/llm_probe/AGENTS.md:18) 反向指定 `_378` 变量。当前 RWKV 会话的已确认事实是使用旧服务器。

必须修正：

- 从两份长期规则中删除“当前服务器”和具体变量名，只保留凭据不得输出、回显或落盘，以及远程动作前读取当前路线事实源并核验服务器身份。
- 当前 RWKV 事实只在 [RWKV 路线总控的 GPU 状态章节](/Users/bilibili/personal/note/.Codex/docs/RWKV/RWKV路线总控.md:64) 增加一条：当前活动服务器为旧服务器，凭据键为 `GPU_SSH`、`GPU_PWD`。服务器切换时只更新该状态源。
- 若该总控正由其他代理修改，先协调所有权；在唯一事实源尚未写入前，不得删除两份规则中的变量说明，也不得启动远程任务。
- 不得把地址、端口、密码或解析值写入规则、总控、测试和报告。

### S3：用户改动保护不能随全局压缩丢失

[用户级规则第 87 行](/Users/bilibili/.codex/AGENTS.md:87) 当前要求验证失败后自动执行 `git checkout -- <file>`，这会覆盖用户原有修改，必须删除。安全 Hook 当前又不能保护 `apply_patch`，因此不能把“工作树可能已有用户或并行任务改动”一并删掉。

用户级精简版必须保留一条明确规则：只修改任务范围；未经明确要求，不覆盖、暂存、回滚或删除用户及并行任务改动；冲突时停止并报告准确路径。

## 3. 重要问题

### I1：`skill-forced-eval` 的输入字段和输出用途都已过时

[脚本第 18 行](/Users/bilibili/.codex/hooks/skill-forced-eval.js:18) 读取 `user_prompt`，当前官方字段是 `prompt`。实测 `{prompt:"/commit"}` 仍输出固定消息，只有旧字段样例才静默。该 Hook 在 [状态第 272—274 行](/Users/bilibili/.codex/config.toml:272) 为启用状态。

此外，它使用的 `systemMessage` 当前用于界面或事件警告；若真要追加开发上下文，应使用 `hookSpecificOutput.additionalContext`。P0 的目标本来就是停止每轮重复注入，最小修复不是把它改成新的常驻注入，而是从活动 [用户级 Hook 配置](/Users/bilibili/.codex/hooks.json:27) 删除整个 `UserPromptSubmit` 注册。脚本和嵌套历史配置本轮不删除。

### I2：项目策略守卫“有测试”不等于“已接入”

[项目 Hook 配置第 2 行](/Users/bilibili/personal/note/.codex/hooks.json:2) 明示其他执行门禁继续暂停，实际只接入 Prettier 拒绝和 PreCompact。[`project_policy_guard.py` 第 447—449 行](/Users/bilibili/personal/note/.codex/hooks/project_policy_guard.py:447) 的命令入口直接返回 0；`server_launcher_guard.py` 未注册。

因此：

- `rg`/`fd`、危险同步、远程入口和用户改动保护不能在 P0 中以“已有 Hook”为由完全删除。
- 本轮不建议顺带接入完整项目策略守卫；其策略面较大，且用户此前明确暂停。保留最小文字规则与现有按需合同即可。
- Prettier 拒绝的定向测试 `3/3` 通过，可用于消除“全局强制 Prettier、项目禁止 Prettier”的冲突；全局规则应改成“使用项目指定格式化器”。

### I3：当前 PreCompact 不能承载 RWKV 路线恢复

[PreCompact 配置](/Users/bilibili/personal/note/.codex/precompact_snapshot.json:1) 仍固定 R2 活动计划，[脚本第 25—28 行](/Users/bilibili/personal/note/.codex/hooks/precompact_snapshot.py:25) 仍固定写两份 PINN `output/` 文档。当前 RWKV 会话的 `git diff` 证明它已在 2026-08-06 刷新两份 PINN 文档的快照时间。

这不阻断 AGENTS 文案精简，但带来两项限制：

- P0 不得把根层唯一选路、RWKV/PINN 入口和路线专属写回边界交给该 Hook 后删除。
- 本轮不扩展为路线感知 Hook 改造；把“禁用或重做跨路线 PreCompact”登记为单独后续。当前 P0 不修改 PreCompact 配置、脚本或两份 `output/` 文档。

### I4：Hook 全套回归当前不是全绿

只读回归共 72 项，71 项通过、1 项失败。Prettier、项目策略、服务器启动器、命令级集成和 Node 启动器分别为 `3/3`、`25/25`、`13/13`、`2/2`、`4/4` 通过。

唯一失败是 [旧测试第 91—106 行](/Users/bilibili/personal/note/.codex/hooks/tests/test_precompact_snapshot.py:91) 仍期待“活动计划不存在时报错且不写文档”，而 2026-08-04 的已记录行为已经改为写入“未设置”并成功。这是测试与已记录行为漂移，不是本次 P0 引入的失败。实施报告必须如实保留该基线，不能宣称全套 Hook 测试通过；本轮安全 Hook 的新增定向测试必须单独全绿。

### I5：工作树已有并行改动，不能整文件回退或从旧版本重写

P0 可重写三份目标规则，但必须先保存它们当前内容和定向差异，只基于当前工作副本精简。尤其不能丢失根规则中新增的 RWKV 路由和测试驱动开发条款，也不能覆盖 `llm_probe` 当前变更。不得使用 `git checkout`、`git restore`、`git reset` 或整仓格式化。

## 4. P0 删除项与等价承载

| 拟精简内容 | 当前等价承载 | 审查裁决 |
|---|---|---|
| 简体中文 | 无可靠机械等价物 | 用户级保留一条强制规则，不能删除 |
| 结论优先、证据、报告格式 | `expression-skill` 与平台交付规则 | 删除长模板，保留最小验证原则 |
| 规划、调试、测试驱动开发、代码审查 | `planning-with-files`、`systematic-debugging`、`bug-detective`、`test-driven-development`、`code-review-excellence` | 删除教程和步骤表；仓库根只保留本项目的触发例外与门禁 |
| Obsidian 写回和技能路由表 | 对应 Obsidian 技能及技能元数据 | 从用户级删除，不再每轮 Hook 注入 |
| 论文实验复审细节 | 根规则、`llm_probe`、当前路线总控与冻结合同 | 从用户级删除；保留“实验不能被文字复审替代”的路线边界 |
| `rg`/`fd`/`sd` 长示例 | 根规则、`.ignore`、通用编辑规则 | 删除示例；保留一条工具偏好和敏感文件边界；`.rules` 当前不能替代 |
| 自动格式化与失败回滚 | 验证技能、项目局部规则、Prettier Hook | 删除语言硬编码和自动 `git checkout`；保留按项目执行最小验证 |
| 子域路由 | 根及局部 `AGENTS.md` | 根层保留短路由，不能全部删除 |
| 路线恢复完整步骤 | RWKV 局部规则与总控、PINN 两份恢复文档 | 根层保留唯一选路、入口、实验前恢复和专属写回边界 |
| 当前服务器、变量、模型、样本数、数据路径 | 当前路线总控、冻结配置、运行合同和制品收据 | 从长期规则删除；当前旧服务器映射先写入 RWKV 总控 |
| 凭据和防泄漏 | 用户级最小安全规则、根规则、局部数据合同 | 至少保留两层短规则；不得只依赖 Hook 或技能 |
| GPU 通知 | `llm_probe` 与路线总控 | `llm_probe` 保留跨路线的任务、时长、显存/卡数、开机与费用确认 |
| SwanLab | 运行配置、`llm_probe` 和路线总控 | 固定项目名可退出长期规则；在线记录、云端核验和本地原始日志必须保留 |
| 同步、唯一副本、运行断言 | 远程执行合同、包装器、`scripts/AGENTS.md` | `llm_probe` 保留短安全边界；项目策略 Hook 未接入，不能全删 |
| 用户改动保护 | 用户级规则、平台安全边界 | 用户级必须保留；当前安全 Hook 不等价 |

## 5. 字节核算

当前 10 份规则总计 `49,302` 字节。调研报告的 `48,254` 已过期：仓库根比其基线增加 `305` 字节，RWKV 局部规则增加 `743` 字节。

| 发现链 | 当前文件字节和 | 相对 32,768 | 按 P0 目标预计 | 预计余量 |
|---|---:|---:|---:|---:|
| 仓库根 | 22,887 | 余 9,881 | 6,800 | 25,968 |
| `thesis` | 27,495 | 余 5,273 | 11,408 | 21,360 |
| `llm_probe` | 33,217 | **超 449** | 14,408 | 18,360 |
| `scripts` | 34,258 | **超 1,490** | 15,449 | 17,319 |
| RWKV | 29,309 | 余 3,459 | 13,222 | 19,546 |

预计值采用用户级 `2,600`、仓库根 `4,200`、`llm_probe` `3,000` 字节，其他文件保持当前大小。由此三个 P0 文件预计减少 `18,809` 字节，10 文件总量约 `30,493`。调研中的 `llm_probe` 预计值 `13,808` 少算 `600` 字节，应更正为 `14,408`。

上表是原始文件字节和，不含 Codex 拼接文件时增加的少量空行；P0 后余量足够，不影响结论。目标字节是改写预算，不得为追求整数删掉关键规则；硬门禁是所有常用发现链低于 32 KiB，且建议至少留 8 KiB 余量。不得通过提高 `project_doc_max_bytes` 代替精简。

## 6. 允许实施的精确范围

### 6.1 允许修改

1. **`/Users/bilibili/.codex/AGENTS.md`：允许整文件精简。** 必须保留简体中文、凭据防泄漏、破坏性操作确认、用户/并行改动保护、最小验证和按项目格式化；删除命令教程、论文专属复审、技能路由表、Obsidian 细节、固定交付模板和自动回滚。
2. **`/Users/bilibili/personal/note/AGENTS.md`：允许在现有七个章节内压缩。** 保留加载哨兵、最小子域路由、仓库与证据边界、唯一选路及专属写回、凭据禁写、搜索证据边界、实验设计例外和最小开发门禁；删除易变服务器变量、重复技能教程和完整调试步骤。
3. **`thesis/experiments/llm_probe/AGENTS.md`：允许整文件精简。** 保留合同入口、GPU 通知、凭据与服务器复核、防泄漏和最终测试隔离、运行时断言、白名单同步与本地唯一副本、SwanLab 在线及双端日志、按当前路线写回；删除服务器变量、固定模型、GeNIS 数量路径、固定 SwanLab 阶段和重复工具流程。
4. **`.Codex/docs/RWKV/RWKV路线总控.md`：仅允许在“GPU 状态”增加当前旧服务器与 `GPU_SSH`/`GPU_PWD` 的一条状态事实。** 不得改研究裁决、G0 状态或其他代理正在更新的任务进度。
5. **`/Users/bilibili/.codex/hooks.json`：仅允许修改用户级 `PreToolUse` 命令和 matcher，并删除整个 `UserPromptSubmit` 组。** 不改 `SessionStart`、`Stop`。
6. **`/Users/bilibili/.codex/hooks/run-node-hook.sh` 与仓库源 `.codex/hooks/run_node_hook.sh`：仅允许把 `security-guard.js` 加入白名单。** 同步更新现有启动器测试。
7. **`/Users/bilibili/.codex/hooks/security-guard.js`：仅允许修正当前 `PreToolUse` 输出协议。** 普通放行无输出，危险 Bash 仍拒绝，确认消息不含不支持字段；本轮不扩展文件补丁解析器。
8. **`.codex/hooks/tests/`：允许新增或更新仅覆盖上述安全 Hook 和启动器的定向测试。** 先观察路径、普通放行输出和活动状态的失败，再实施最小修复。

用户级文件位于工作区外，实施时仍须按沙箱要求申请精确写权限。用户对 P0 的授权不等于可以绕过文件系统审批。

### 6.2 本轮不得修改

- `thesis/AGENTS.md`、`scripts/AGENTS.md` 及 `P1/P2` 目标
- 项目 `.codex/hooks.json`、`precompact_snapshot.py`、`precompact_snapshot.json`、`project_policy_guard.py`、`server_launcher_guard.py`
- `/Users/bilibili/.codex/hooks/hooks.json` 历史嵌套配置
- `/Users/bilibili/.codex/config.toml` 中的 `trusted_hash` 或 Hook 状态记录；信任和启用通过新会话 `/hooks` 完成
- 两份 PINN `output/` 总控、论文代码、配置、数据、运行制品、服务器或 Git 历史
- 用户级 `.rules` 和 `project_doc_max_bytes`

## 7. 强制实施顺序与验收

1. 保存三份目标规则当前字节、摘要和定向差异；确认没有其他实现代理同时修改同一文件。
2. 先用最小失败样例修复安全 Hook 和启动器；删除活动 `UserPromptSubmit` 注册。
3. 运行 `jq empty`、两个 JavaScript 语法检查、启动器 Shell 语法检查和安全 Hook 定向正反例。普通 Bash 必须无输出，危险 Bash 必须拒绝，旧错误路径不得再出现。
4. 完全启动新 Codex 会话，通过 `/hooks` 信任并启用修复后的用户级 `PreToolUse`；验证一个无害命令放行和一个无副作用危险样例在执行前拒绝。未完成此步时，安全文字规则继续保留。
5. 在无并行冲突时写入 RWKV 总控的当前服务器事实，然后同时消除根与 `llm_probe` 的变量冲突。
6. 基于当前工作副本精简三份规则；逐条核对第 4 节的保留项，不从旧备份覆盖当前新增语义。
7. 重新执行 `wc -c`，计算根、`thesis`、`llm_probe`、`scripts`、RWKV 五条链；不得提高 32 KiB 上限。
8. 新会话分别从仓库根、`llm_probe`、`scripts` 和 RWKV 目录核验加载来源、简体中文、路线入口、凭据禁写、GPU 通知、SwanLab、防泄漏和用户改动保护。
9. 执行 `git diff --check` 和定向 `git diff`。最终差异只能落在第 6.1 节允许范围；不得暂存、提交、推送或运行服务器任务。
10. 报告项目 Hook 全套测试的既有单一失败，不得把定向测试通过扩写成“全部 Hook 通过”。

满足上述条件后，P0 精简不会丢失关键规则，且预计把最危险的 `llm_probe` 发现链从 `33,217` 字节降至约 `14,408` 字节。
