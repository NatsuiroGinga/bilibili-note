# DRIFT 更新专家混合折0有限步严格分析

<!-- RESEARCH_ROUTE=DRIFT -->

- **分析日期**：2026-09-08
- **分析模式**：`screening_only` 审计
- **运行身份**：`ch3-drift-update-expert-mixture-fold0-short-step-v1`
- **权威结果**：`thesis/experiments/llm_probe/runs/diagnostics/ch3-drift-update-expert-mixture-fold0-short-step-v1/result.json`
- **结果 SHA-256**：`d7efad0d5e740bcf58604ed7d0698617dae5c9cfd71fb91910c354a71321e328`
- **预注册裁决**：`rejected_after_finite_step`

## 一、分析问题

固定全局风险可行权重在同一折0端点、优化器状态和32768样本固定块上执行32批后，是否同时通过相对 `no_update` 与 `joint_all_equal` 的三风险非增门。

## 二、比较单位与统计边界

- 独立运行单位只有**一个折、一个种子、一个固定块**。
- 三个臂共享同一起点与固定块，属于确定性配对筛查，不是可估计运行间方差的重复实验。
- 32个批次是一次连续优化轨迹，不是32个独立统计样本。
- 225180个预测行共享模型和训练轨迹，也不是独立重复运行。
- 因此只报告精确描述统计和预注册布尔门；不计算均值标准差、置信区间、显著性检验、效应量或多重比较校正。

## 三、精确结果

固定源阈值为 `2.2664829884888608e-05`。

| 臂 | 良性 BCE ↓ | DGA micro BCE ↓ | family-macro BCE ↓ | AP ↑ | AUROC ↑ | 源阈值 FPR ↓ | 源阈值 FNR ↓ | 默认 FPR ↓ | 默认 FNR ↓ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `no_update` | `0.000004203794163` | `9.680440199093` | `9.369876085120` | `0.920325958117` | `0.951885067970` | `0.009913333333` | `0.472412875765` | `0` | `0.999946794360` |
| `joint_all_equal` | `0.000011983742285` | `9.176313145211` | `8.571923523828` | `0.900385027545` | `0.937771766605` | `0.018960000000` | `0.435594573025` | `0` | `0.999866985901` |
| `risk_feasible_update_mixture` | `0.000010687663640` | `9.073915771028` | `8.482062440841` | `0.898708778722` | `0.938016178150` | `0.027633333333` | `0.402234636872` | `0` | `1` |

### 3.1 混合臂相对零更新

| 指标 | 差值 | 方向 |
| --- | ---: | --- |
| 良性 BCE | `+0.000006483869477` | 变差，违反硬门 |
| DGA micro BCE | `-0.606524428064` | 改善 |
| family-macro BCE | `-0.887813644278` | 改善 |
| AP | `-0.021617179395` | 变差 |
| AUROC | `-0.013868889820` | 变差 |
| 源阈值 FPR | `+0.017720000000` | 变差 |
| 源阈值 FNR | `-0.070178238893` | 改善 |

`passes_no_update=false`。良性 BCE 增加已经足以触发预注册否决；AP、AUROC 与源阈值 FPR 的变化提供一致的运营侧伤害披露，但不是替代裁决门。

### 3.2 混合臂相对联合等权

| 指标 | 差值 | 方向 |
| --- | ---: | --- |
| 良性 BCE | `-0.000001296078645` | 改善 |
| DGA micro BCE | `-0.102397374183` | 改善 |
| family-macro BCE | `-0.089861082986` | 改善 |

`passes_joint_all_equal=true`。这只说明固定混合在三项 BCE 上减少了等权更新的伤害，不足以越过 `no_update` 主门。

## 四、优化轨迹诊断

- 混合臂总更新 L2 范数为 `1.479536747582`；联合等权为 `1.581809279467`。
- 混合臂字符分支有 `15/32` 批触发裁剪，子词分支有 `32/32` 批触发裁剪。
- 联合等权字符分支有 `22/32` 批触发裁剪，子词分支有 `32/32` 批触发裁剪。
- 运行使用 Adam、BF16 自动混合精度及 FP32 参数／优化器状态，总墙钟 `174.117206577` 秒。

这些事实说明有限步轨迹并非原始梯度线性规划的简单缩放。它们不能单独识别失败由 Adam 预条件、逐批非线性、裁剪或极小可行裕量中的哪一项造成；当前实验没有独立消融这些因素。

## 五、裁决

1. **实验否决**：当前固定全局更新专家混合未通过相对 `no_update` 的良性风险非增门，状态保持 `rejected_after_finite_step`。
2. **保留的观察**：它相对联合等权在三个 BCE 上均改善，说明固定权重能减少等权更新伤害；这不是安全或有效性结论。
3. **禁止扩展**：不调权、不改尺度、不延长步数、不运行折1、不重跑 N12、不访问目标年份。
4. **后续边界**：仅等待“标准可学习门控正锚点”的资格核查后由主代理裁决；本分析不选择或认可新机制。
5. **会话状态**：上层会话现有 `/goal` 尚未吸收本次否决事实，已经过时；本任务不修改或替换 `/goal`。

## 六、主张候选

- **主张**：折0原始梯度线性可行性没有转化为32批后的良性风险非增。
- **来源证据**：本运行 `result.json` 的 `raw_gradient_lp`、`adjudication_evidence` 与 `verdict`。
- **允许措辞**：当前固定全局混合形式在折0有限步筛查中被预注册门否决。
- **禁止措辞**：所有风险约束方法无效；所有可学习门控无效；方法在跨年数据上失败。
- **不确定性**：只有一个折、一个种子、一个固定块，不能估计跨运行不确定性或机制归因。
- **决定**：保留否决结论；丢弃“线性可行即可保证有限步安全”的主张。
