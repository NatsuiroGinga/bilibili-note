根因：UDP `app_drop_packets`、`app_drop_bytes` 两个状态维在冻结数据中合法恒零，原归一化将其与异常零方差统一拒绝。
修改：仅允许 UDP 索引 4、5 同时精确零均值与零尺度，使用安全尺度 1 并从 G3 标准化状态主指标排除，保留原始单位 MSE 和行制品掩码、索引。
目标测试结果：`uv run pytest tests/test_r2_final_distilbert_probe.py::test_udp_only_allows_paired_zero_state_degeneracy_outside_g3_metric`，1 通过（此前同一目标已按预期红灯）。
