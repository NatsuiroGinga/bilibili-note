# AI 会话缓存
.claudian/

# Obsidian 工作区配置（个人偏好，不同步）
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/plugins/
.obsidian/community-plugins.json

# 第三方插件的会话/缓存
copilot/

# ── .Codex 共享工作记忆：整体纳入版本控制，仅逐项排除 ──
# 理由：.Codex/hooks 与 .Codex/docs 是 AGENTS.md 强制加载的规则与恢复文档来源，
# 整目录忽略会导致克隆后的仓库缺失全部必读规则与路线恢复文档。
# 第三方 npm 安装产物（约 9.6 MB / 57 文件）；package.json 与 package-lock.json 保留入库
.Codex/tools/**/node_modules/
# Python 字节码缓存，可再生且随解释器版本产生噪声 diff
.Codex/**/__pycache__/
.Codex/**/*.py[cod]
# 会话运行时快照，由 .Codex/hooks/precompact_snapshot.py 每次压缩自动重写
.Codex/precompact_snapshot.json
# CUDA smoke 编译产物（ELF 各约 972 KB）；同目录 .cu 源码与 .sha256 校验文件保留入库
# 注意：必须写精确文件名，不可用 minimal_cuda_smoke* 通配，否则误伤 .cu 源码
.Codex/docs/RWKV/**/minimal_cuda_smoke
.Codex/docs/RWKV/**/minimal_cuda_smoke_project_env
# ninja 增量构建缓存
.Codex/**/.ninja_log
# 第三方下载的二进制密钥环（NVIDIA CUDA APT 公开公钥），非本仓库自有资产
.Codex/**/*.gpg
# 大体积原始下载/续传日志（2.0 MB + 324 KB），结论已在同目录 Markdown 留存
.Codex/docs/tqh-c2-*.log
# 兜底：临时物
.Codex/**/*.tmp
.Codex/**/*.temp

# 系统文件
.DS_Store
Thumbs.db

# 本地 Git 工作树，用于隔离章节分支
.worktrees/

# Obsidian 插件（后续如安装社区插件，取消注释）
# .obsidian/plugins/




# Ignore Smart Environment folder
.smart-env
# 大型数据集（不入 git，仅本地保留）
raw/datasets/

# 会话日志与中间产物（不入库）
# 2026-09-04 实测更正：`.claude/logs/` 因含中间斜杠被 git 锚定到仓库根，
# 只保护根级 `.claude/logs/`，保护不到子目录下的同名目录——实测
# `thesis/experiments/llm_probe/tools/.claude/logs/` 与
# `thesis/chapters/.claude/logs/` 均未被忽略且已进版本库。
# 改为 `**/` 前缀以覆盖任意深度。已入库的三份不因本规则自动移除，须另行处置。
**/.claude/logs/
output/pdf-md/

# 子代理开发的本地进度与工具依赖
.superpowers/
.Codex/tools/**/node_modules/

# 实验缓存与本地下载制品
.cache/literature-search/
scripts/literature_search/.venv/
thesis/experiments/__pycache__/
thesis/experiments/llm_probe/**/*/target/
thesis/experiments/llm_probe/**/*/__pycache__/
thesis/experiments/llm_probe/**/*/tmp/
thesis/experiments/llm_probe/**/*/*.pyc
thesis/experiments/llm_probe/artifacts/
thesis/experiments/llm_probe/*.tmp
thesis/experiments/llm_probe/*.log
tmp/
*.tmp
*.temp
*.swp
*.swo
*.swn
*.xaa
mininet/
ns-allinone-*.tar.bz2
ns3/*.tar.bz2
wget-log
*.download.log
zeek_orange_dmz.zip
